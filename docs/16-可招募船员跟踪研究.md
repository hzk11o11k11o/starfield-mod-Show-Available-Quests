# 16 · 可招募船员（精英船员）跟踪研究（第 154 轮）

> 起因：玩家提问「**可以被招募为船员的有名字的 NPC 能不能跟踪**？」
> 结论：**能。** 数据齐备 —— 24 位可招募船员，每人 **1 处固定放置引用** +
> **1 条招募载体任务**；引导所需的两档常驻兜底候选**全部就位**
> （内景 11/11 cell、外景 5/5 world 均有现成候选）。
> 三处位置不适合直接导航（1 个别名暂存格 + 2 个运行时生成内景）⇒ 建议
> 照第 91 轮「（不可导航）」处理；「是否已招募」的隐藏时机需要**运行时判据**
> （crew faction 三件套）——**待实机验证**（不是阻塞项）。
>
> 证据链：`tools/esm/scan_hirable_crew.py`（第 154 轮转正，1 分钟可复现）→
> `ref/hirable_crew_scan.log` / `ref/hirable_crew_scan.json`（只读快照，不入库）。

---

## 16.1 枚举：谁是「可招募的有名字船员」

* **EDID 前缀判据**（推荐）：NPC_ 里 `Crew_Elite_*`，排除 `OtherPlayer*` 模板与
  `BalanceData*` 模板 ⇒ **实测 24 位**（见下表）。
* **官方 FLST 印证**：`CREW_imGui_2a_Actors_Elite`（0x00133A67，24 名成员）——
  与 EDID 判据基本重合，两处差集：
  * FLST 里有 `COM_CoraCoe`（Cora 是 Sam 的女儿，**无 crew faction、不可招募**）
    与 `OtherPlayerMale` 模板 ⇒ 判据要排除；
  * FLST 里**没有** `MathisCastillo`、`AdoringFan`（两人都有完整招募任务）
    ⇒ **FLST 会漏人，EDID 判据更准**。
* **每位对应 1 条招募载体任务**（QUST）：`CREW_EliteCrew_<姓名>`
  —— 实测 **25 条** = 24 位船员 + `CREW_EliteCrew_OtherPlayer`（玩家自身模板）。
  对话 / 分支 / 场景分别挂 `INFO` / `DLBR` / `SCEN`（同一前缀 + 后缀区分），
  另有 2 条 CNDF 条件（`CREW_CND_DIAL_RecruitRepeat` / `..._Greeting_Recruit`）。
* **NPC_ 上挂 VMAD 脚本** `EliteCrew_StartQuestOnLoadScript`（属性引用 `ECQuest`
  系）—— 每个船员加载时启动其招募任务，这是官方招募机制的入口。
* **crew faction 三件套**（23/24 挂载，initial rank 多数 255）：
  * `AvailableCrewFaction`（0x00014314）
  * `CurrentCrewFaction`（0x00014312）
  * `PotentialCrewFaction`（0x000143A2）
  * **例外 = Lin**（0x005638）：三个都没挂（主线剧情加入，faction 由脚本后期
    添加）⇒ 未来做「已招募/可招募」判定必须容错。
* 命名拼写坑：`ErickVonPrice`（NPC）↔ `CREW_EliteCrew_ErickvonPrice`（任务，
  小写 v）——按名字串匹配任务时要大小写不敏感。

## 16.2 放置引用与常驻性（能不能导航）

实测口径（`scan_hirable_crew.py`）：扫全表 REF_SIGS（ACHR/REFR…）按 NAME 匹配
24 位 base。

| 船员（中文名 / 英文名） | REFR | 位置（cell / world） | 常驻 | 兜底候选（≤25 m） |
| --- | --- | --- | --- | --- |
| 林监工 / Supervisor Lin | 0x00005639 | VecteraMine01（内景） | n | 同 cell 24/96 |
| 海勒 / Heller | 0x0000563C | VecteraMine01（内景） | n | 同 cell 24/96 |
| 瓦斯科 / Vasco | 0x000057BE | CityNewAtlantisLodgeInt（内景） | **Y** | 同 cell 213/255 |
| 玛丽卡·波罗斯 / Marika Boros | 0x00015062 | NA 外景（cell 0x000125A8） | n | world 级 22/1630 |
| 西米恩·班科夫斯基 / Simeon Bankowski | 0x00015063 | NA 外景 | n | world 级 23/1630 |
| 吉迪恩·艾克 / Gideon Aker | 0x00015064 | NA 外景 | n | world 级 23/1630 |
| 莫亚拉·奥泰罗 / Moara Otero | 0x0029C982 | **MQHoldingCell（别名暂存格）** | n | 同 cell 35/35（★ 不可用） |
| 马西斯·卡斯蒂略 / Mathis Castillo | 0x000189B2 | StationTheKeyInterior（星钥站） | n | 同 cell 140/290 |
| 铁杆粉丝 / Adoring Fan | 0x001631C5 | NAViewport（NA 外景） | n | world 级 22/1630 |
| 索菲亚·格雷丝 / Sophia Grace | 0x00147954 | CityNeonMadameSauvage（内景） | n | 同 cell 39/39 |
| 贝蒂·侯赛 / Betty Howser | 0x0020DC69 | **SEAC01EliteCrewEncounterBettyShipCreatedInterior** | n | 同 cell 28/28 |
| 奥马里·哈桑 / Omari Hassan | 0x001593F8 | CityAkilaCityCoePlaza01（外景） | n | world 级 51/808 |
| 安德洛美达·开普勒 / Andromeda Kepler | 0x0016B3D0 | CityCydoniaMainLevel（内景） | n | 同 cell 54/309 |
| 米奇·卡威亚 / Mickey Caviar | 0x0016D16A | CityNeonTradeTowerAstralLounge（内景） | n | 同 cell 32/69 |
| 以西结 / Ezekiel | 0x0017A859 | CityAkilaSlums01（外景） | n | world 级 32/808 |
| 莱尔·布鲁尔 / Lyle Brewer | 0x0017A858 | CityAkilaSlums01（外景） | n | world 级 39/808 |
| 丹尼·卡尔西亚 / Dani Garcia | 0x001CDAFC | CityNeonEuphorika（内景） | n | 同 cell 21/21 |
| 罗茜·泰诺希 / Rosie Tannehill | 0x001A0CB1 | CityAkilaCityCoePlaza01（外景） | n | world 级 50/808 |
| 杰萨敏·格弗林 / Jessamine Griffin | 0x001D898E | StationTheKeyShipInterior（星钥站） | n | 同 cell 44/99 |
| 埃里克·冯·普莱斯 / Erick Von Price | 0x0022198C | **CargoShipJane01CreatedInterior** | n | 同 cell 7/7 |
| 奥特姆·麦克米伦 / Autumn MacMillan | 0x001F0262 | SettleRedMile01（外景） | n | world 级 364/1370 |
| 拉斐尔·阿盖罗 / Rafael Aguerro | 0x000C4632 | NA 外景 | n | world 级 22/1630 |
| 阿梅莉亚·埃尔哈特 / Amelia Earhart | 0x00299F66 | CrucibleExt06（外景） | n | world 级 13/45 |
| 哈德良 / Hadrian | 0x002B17C4 | TauGourmetExt07（外景） | n | world 级 18/109 |

**核心事实**：

1. **每人只有 1 处放置**（不是多地点同名实例）——与任务板 / 可重复 NPC 同构。
2. **23/24 是非常驻引用**（仅 Vasco 常驻）⇒ cell/world 未加载时取不到，
   与第 27/30 轮任务板当年的问题**完全同型**。
3. **XESP = 0**（24 处全都没有 enable parent）⇒ 位置是静态的，不随开关变化。
4. **兜底候选 100% 可用**：内景 11/11 cell 有同 cell 常驻引用（21~213 个 ≤25 m）；
   外景 5/5 world 有 world 级常驻引用（13~364 个 ≤25 m）
   ⇒ **不新建 marker 也能配出「精确引用 → 常驻兜底」候选链**（第 30/47/114 轮机制）。
5. **三处位置不适合直接导航**（建议「（不可导航）」前缀 + 点击提示，照第 91 轮）：
   * `MoaraOtero` → `MQHoldingCell`：**别名暂存格**（玩家永远到不了；`BAD_CELL_RE`
     判据本该命中）；
   * `BettyHowser` → `SEAC01...CreatedInterior`：太空遭遇**运行时生成**的船内景；
   * `ErickVonPrice` → `CargoShipJane01CreatedInterior`：货船**生成内景**。
6. 可导航但需到达条件（剧情锁 / 特殊地点，不阻塞）：
   星钥站（Jessamine / Mathis）、Vectera 矿场（Lin / Heller）、红英里（Autumn）、
   熔炉（Amelia）、Tau Gourmet（Hadrian）。**是否需要进度门槛**留待实现阶段另议。

## 16.3 「什么时候显示」的判据（待实机验证）

* faction 三件套**初始就挂着**（rank 255/1）⇒ 不能只凭「挂着」判「可招募」。
* 待实机探针（harness 只读）建议记录：
  1. 招募**前 / 后**，NPC 引用在三个 faction 的成员状态变化
     （`IsInFaction(AvailableCrewFaction / CurrentCrewFaction / PotentialCrewFaction)`）；
  2. 招募前 / 后，`CREW_EliteCrew_*` 任务的状态（`GetRunning / GetStageDone`）；
  3. Lin 的对照组（无三件套）——验证「剧情加入」路径。
* 隐藏策略候选：**已招募 ⇒ 隐藏**（用 `CurrentCrewFaction` 或任务状态判定，
  哪个可靠用实测数据说话）；未招募 ⇒ 显示（全部 24 位，其中 3 位带「（不可导航）」）。

## 16.4 落地实现路径（★ 第 156 轮已实施 —— 见 16.8）

1. **数据生成**：`gen_hirable_crew.py`（仿 `gen_repeatable_givers.py`）→
   `ref/hirable_crew.json`；清单 = `scan_hirable_crew.py` 的 24 位。✅ 第 155 轮完成；
   第 156 轮并入 `gen_entry_table.py`（kind=2 段）。
2. **条目形态**：新入口段「可招募船员」（类似第 80/110 轮的可重复 NPC 段）——
   命名 `（可招募）<名字>`；Moara / Betty / Erick 三条**丢弃兜底** ⇒ 平时不可导航
   （界面照第 91 轮显示「（不可导航）」+ 船员专用提示）。✅ 第 156 轮。
3. **引导候选链**：复用现有机制 —— 精确引用（ACHR）→ 同 cell / world 级常驻兜底
   （数据已备齐，**不新建 marker**）。✅ 第 156 轮。
4. **显示时机**：口径「显示 = P=1 / 隐藏 = A=1」—— DLL 直读 faction（新模块
   `SAQ_Crew`）+ 模拟招募正样本复核。✅ 第 156 轮（待实机）。

## 16.5 研究工具与证据 / 口径教训

* 工具：`tools/esm/scan_hirable_crew.py`（第 154 轮转正；只读；1 分钟）
  —— 自动枚举 `Crew_Elite_*`、放置引用、常驻兜底候选、faction、招募 QUST，
  输出 `ref/hirable_crew_scan.json` + 日志。
* ★ **口径教训（新）**：Starfield.esm 的 **`QUST` 顶层组里混装** QUST
  （2077 条）**+ INFO（12.6 万）/ DIAL（6.8 万）/ SCEN / DLBR…**
  ⇒ 用「`list QUST --grep xxx`」（= 组内全部记录）找任务会严重误导；
  按 EDID 找任务时必须**同时判记录签名**（本轮的 `_Recruit*` 曾因此误判）。
* 临时诊断脚本（walk 追踪 / sig 分类）已删；结论留在本文件与工具注释里。

## 16.6 第 155 轮实施：入口数据 + 只读 faction 判据探针

### 1. 数据产物（可复现）

* 新工具 **`tools/esm/gen_hirable_crew.py`**（仿 `gen_repeatable_givers.py`；复现约
  1~2 分钟；全表两遍扫）：
  * **`ref/hirable_crew.json`** —— 入口数据：24 位船员的放置引用 / 两档常驻兜底候选 /
    crew faction 三件套挂载 / 招募载体任务 / 不可导航标记 / 显示名（`（可招募）<名字>`）；
    口径与 16.1~16.2 研究逐项一致：内景 12 / 外景 12、常驻 1（Vasco）、
    不可导航 **3**（贝蒂·侯赛 / 埃里克·冯·普莱斯 / 莫亚拉·奥泰罗）、
    招募任务 **24/24**（大小写不敏感匹配，`ErickVonPrice` ↔ `ErickvonPrice` 已过）、
    crew faction 三件套全挂 **23/24**（Lin 例外）、XESP 0；
  * **`plugin/src/SAQ_TestCrewProbe.h`** —— harness 只读探针 `crew.probe` 的数据表
    （`{放置引用, 招募任务, 名字}` ×24；只被 SAQ_Test.cpp include，发布构建不含）。
* 两件产物都进**黄金快照**（`golden_snapshot.py` 清单 +2 ⇒ 19 件）—— 防手改 / 漏跑。

### 2. 只读 harness 探针 `crew.probe`（驱动器 **v71**）

* 一次跑全表 24 位，每位两条路**配对**读出：
  1. **faction 侧**（Papyrus 新 op=7，`SAQ_Main.psc`）：`Actor.IsInFaction` 三件套 +
     `GetCrewAssignment` ⇒ 结果码 `16 + 状态位`（bit0 A / bit1 C / bit2 P / bit3 已分配）。
     **引用未加载的位直接跳过**（DLL 侧 `LookupByID` 预检 —— 与 Papyrus `Game.GetForm`
     同源），既省往返也如实记录（faction 是 actor 实例状态，远处读不到是引擎限制）；
  2. **任务侧**（DLL 直读 `ReadQuestRuntimeState`，与产品同一套判据）：招募任务
     `CREW_EliteCrew_*` 的运行状态**不依赖引用加载** ⇒ 24/24 总有结果 = 判据主证据。
* 红线六：逐位产品行 `船员探针 [i/24] 名字（0x…）｜Papyrus: …｜招募任务 …` +
  一行 `船员探针 汇总：…`（faction 读到位数 / A·C·P·分配 计数 / 任务状态分布）。
* 用例 **`r155_crew_probe`**（只读：不 reset / 不推 stage / 不传送）：
  断言 = 汇总行（产品行前缀，第 147 轮的教训）+ 三条**链路健康反向断言**
  （不许「失败（结果码…）」/「等回执超时」/「｜招募任务 表单取不到」）。

### 3. 判据线索（一手证据，待实机确认）

* 官方脚本 `Crew_RecruitQuestScript.Recruited()`（Data\Scripts\Source\Base）：
  `if CrewMemberRef.IsInFaction(SQ_Crew.AvailableCrewFaction) == false` ⇒ **只收首次招募费**
  ⇒ 「在 `AvailableCrewFaction` 里 = 曾经招募过」= **「已招募」判据的第一候选**；
* `SQ_CrewScript._CustomSetRoleAvaliable()` → `Game.AddToAvailableCrew(actor)`
  （引擎原生函数）—— 招募动作确实写「可用船员池」，faction 成员状态很可能随之变化
  （**待实机探针证实**：把已招募的（如 Vasco）与未招募的对比 A/C/P 位）；
* NPC_ 静态 SNAM 里的 `rank=255` = **int8 的 -1**（= 不自动加入）⇒「静态挂着」不是
  「运行时成员」（这是 16.3「初始都挂着、不能只凭挂着判定」的机制解释）。

### 4. 构建与验证（第 155 轮）

* 构建：DLL（开发构建含 harness，**1222144 B**）+ PEX（含 op=7，**20507 B**）+ ESM 复用；
* **部署**：21:12 完成（首次部署尝试被运行中的 `Starfield.exe` 锁住 —— 游戏退出后由
  一次性看护脚本自动补部署）；部署态 = 测试态（`Harness=1` / `Plan=SAQ_TestPlan.txt` /
  `AutoLoad=Save1_…142854_2_0_4` / `MaxSizeMB=10`）；
* `verify --dev`：**1079 OK / 0 MISS**（工作区 == 部署；含新增 10 条船员探针特征）；
* 离线层 5 步全过（决策 24 用例 / 1201 断言 + 快照 **19 件** + tripwire + P2 正则）；
* 断言离线预演：4 条断言正则（汇总行 + 三条反向）用合成产品行逐条预演 —— 正向只命中
  汇总行、三条反向只在对应异常行上命中（不误伤正常行）；
* ~~待实机~~ ⇒ ✅ **第 155 轮实机收口**：主计划 **45/45 全 PASS**（21:16~21:25 会话 /
  `sessionMs` 566375）—— 判读见 **16.7**（本存档无「已招募」样本；P/A 判据定案）。

## 16.7 第 155 轮实机判读（收口：2026-09-24 21:16~21:25 会话）

### 1. 探针实测（`r155_crew_probe` PASS，9562 ms）

汇总行（产品日志原文）：

```
船员探针 汇总：24 位｜Papyrus 读到 12 位（A=1 0 / C=1 0 / P=1 4 / 已分配 0）｜招募任务：未开始 17 / 运行中 7 / 已完成 0 / 其它 0
```

12 位可读逐行（A=`AvailableCrew` / C=`CurrentCrew` / P=`PotentialCrew`）：

| 船员 | A | C | P | 分配 | 招募任务 | 解释 |
| --- | --- | --- | --- | --- | --- | --- |
| 吉迪恩·艾克 | 0 | 0 | **1** | 0 | 运行中 | 非奖励款：任务启动跑 stage 1 ⇒ 已解锁 |
| 玛丽卡·波罗斯 | 0 | 0 | **1** | 0 | 运行中 | 同上 |
| 西米恩·班科夫斯基 | 0 | 0 | **1** | 0 | 运行中 | 同上 |
| 铁杆粉丝 | 0 | 0 | **1** | 0 | 未开始 | 特性任务（`TraitUnwantedHero` stage 0）直接设 P；招募任务未启动（特性流程未到 stage 5） |
| 海勒 | 0 | 0 | 0 | 0 | 运行中 | 奖励款：解锁 = MQ104B@130（未到）★ flags=0x18111（唯一 0x10 位，含义未定，不影响判据） |
| 林监工 | 0 | 0 | 0 | 0 | 运行中 | 奖励款：解锁 = MQ104B@110（未到） |
| 瓦斯科 | 0 | 0 | 0 | 0 | 运行中 | 奖励款：解锁 = MQ101@312（未到 —— 存档「一小步」进行中） |
| 拉斐尔·阿盖罗 | 0 | 0 | 0 | 0 | 运行中 | 奖励款：解锁 = MQ207C_RafaelPostquest@210（未到） |
| 阿梅莉亚·埃尔哈特 | 0 | 0 | 0 | 0 | 未开始 | 奖励款：解锁 = MS02@2000（未到） |
| 哈德良 | 0 | 0 | 0 | 0 | 未开始 | 剧情款（未触达） |
| 贝蒂·侯赛 | 0 | 0 | 0 | 0 | 未开始 | 太空遭遇款（未触发） |
| 埃里克·冯·普莱斯 | 0 | 0 | 0 | 0 | 未开始 | 遭遇款（未触发） |

未读到的 12 位（引用未加载 ⇒ 按设计跳过 Papyrus）：安德洛美达 / 奥特姆 / 丹尼 / 以西结 /
杰萨敏 / 莱尔 / 马西斯 / 米奇 / 莫亚拉 / 奥马里 / 罗茜 / 索菲亚（招募任务全为「未开始」）。

### 2. 机制解释（官方脚本一手证据）

* **解锁（P 位）**：招募任务的 stage 1 = `SetFactionRank(PotentialCrewFaction, 1)`，注释原文
  "make X **recruitable** as Elite Crew"。触发路径三型：
  ① 非奖励款 —— 任务启动即跑（任务由 `EliteCrew_StartQuestOnLoadScript` 在 NPC 引用 OnLoad 时
  启动 ⇒ 玩家到过一次当地即有 P=1）；② 奖励款 —— 由剧情片段推 stage 1（MQ104B / MQ101 /
  MQ207C / MS02…）；③ 特性款（铁杆粉丝）—— `TraitUnwantedHero` stage 0 直接设。
* **雇用（A 位）**：`Crew_RecruitQuestScript.Recruited()`（对话片段调用）= 首次收费判据
  `IsInFaction(AvailableCrewFaction) == false` + `SetRoleAvailable → Game.AddToAvailableCrew`；
  解雇 / 收回 = `_CustomSetRoleUnavaliable → Game.RemoveFromAvailableCrew`。
  ⇒ **A=1 ⇔ 曾招募**（收费语义 + 原生函数名双向印证；正样本待复核）。
* **C 位 / 已分配**：官方 `CurrentCrewFaction` 的「在岗」语义写在 `CRWQuestScript.CrewHired/
  CrewFired`（rank 0 / -1）—— 但该路径只被 `QF_CRWLevMotova` 引用（不在 24 位名单），且
  `CRWCrewScript` 自标 **OBSOLETE_AND_SHOULD_BE_REMOVED**（`HireCrew()` 空实现）⇒ 24 位不
  使用该语义；本存档全 0，暂不作判据。
* **招募任务运行状态**：运行 = 引用加载时 OnLoad 启动的载体 —— 7 位运行中里 4 位 P=0
  ⇒ **运行 ≠ 可招募 ≠ 已招募**，不作判据。

### 3. 为什么没有「已招募」正样本（预期）

测试存档 = **2 级**（`Save1_…142854_2_0_4`；任务日志仅「一小步」）= 极早期 ⇒ 既不满足任何
奖励款 EC 的剧情解锁，也没有任何 EC 被雇过（A/C/分配 全 0）。⇒ 本轮拿到的是「**未解锁 vs
已解锁**」的对比（P 位），不是「已招募 vs 未招募」。

### 4. 判据定案与下一步（待玩家定）

| 项 | 结论 | 状态 |
| --- | --- | --- |
| 显示时机 | **P=1（已解锁）显示；P=0 隐藏**（「进度没到不显示」的自然延伸） | 定案 |
| 已招募隐藏 | **A=1 隐藏**（收费判据 + 原生函数证据） | 待正样本复核 |
| 任务状态 / C 位 | 不作判据 | 排除 |

正样本复核方案（可选，一轮做完）：`crew.probe` 加「模拟招募」只读动作 —— 对一位已解锁款
（如玛丽卡）调 `Game.AddToAvailableCrew(actor)` → 立刻重读 A 位（预期 0→1）→
`RemoveFromAvailableCrew` 复原（该动作 = 官方 `Recruited()` 内的同一条引擎调用）。
⇒ ✅ **第 156 轮已实施**（玩家决策「两个都做」）—— 见 16.8。

## 16.8 第 156 轮实施：入口段落地 + 模拟招募正样本复核

> 玩家决策（第 155 轮收口时的待定项）：**两个都做** ——
> ① 入口段落地（kind=2「可招募船员」：数据生成 + AS3 文案 + SWF 重编），
> 口径「显示 = P=1 / 隐藏 = A=1」；② 正样本复核（`crew.probe` 加模拟招募动作）。

### 1. 入口段（kind=2「可招募船员」）

* **数据**（`tools/esm/gen_entry_table.py`，复盘 1~2 分钟）：
  * 合并 `ref/hirable_crew.json` ⇒ 入口表 **21 → 45 条**（任务板 13 + 可重复 NPC 8 +
    可招募船员 24）；每位 = 放置引用（uID + 精确引导目标）+ 两档常驻兜底；
  * **不新建 marker**（第 154 轮结论：兜底候选已就位）；
  * **3 位「位置不适合导航」丢弃兜底**（贝蒂·侯赛 / 埃里克·冯·普莱斯 / 莫亚拉·奥泰罗）——
    引导链只剩引用自身 ⇒ 平时不可导航 ⇒ 界面照第 91 轮显示「（不可导航）」前缀 +
    船员专用提示（不是 bug）；
  * 段序固定「任务板 → 可重复 NPC → **可招募船员**」（`SEGMENT_ORDER` 布局硬校验：
    段序下标必须单调不减 —— 插花直接构建失败，第 150 轮教训的推广）；
  * 名字 = `（可招募）<中文名>` / `(Recruitable) <English>`（数据侧加好）。

### 2. 产品判据（「显示 = P=1 / 隐藏 = A=1」）—— 新模块 `SAQ_Crew`

* **为什么 DLL 直读**（而不是 Papyrus 通道/缓存）：菜单打开时 Papyrus 定时器冻结
  （第 27 轮定案）⇒「开菜单当场读」只能走引擎调用；
  `Actor::IsInFaction(TESFaction*)`（commonlibsf vtable 175 条）是本项目首个 Actor 虚调用
  —— 正确性由**同会话探针双来源对照**钉死（见第 4 节）+ ini 开关兜底。
* 判据（`SAQ.cpp::AppendEntryRows` 的 kind=2 分支）：
  * 引用可读（`LookupByID`）才判定；**读不到 ⇒ 保守放行**（判不了就不藏 —— 与三类门槛
    「求值不了 ⇒ 放行」同一纪律）；
  * `A=1`（AvailableCrewFaction 成员 = 曾招募）⇒ 隐藏；`P=0`（PotentialCrewFaction
    非成员 = 招募任务 stage 1 未打）⇒ 隐藏；`P=1 且 A=0` ⇒ 显示；
  * ini `[Filter] CrewCond`（默认 1；0 = 只写日志不隐藏 —— 实机对照 + 紧急回退口）。
* 日志：`船员门槛=N(过A/藏B+C/未知K`（B=已招募 / C=未解锁）+ `船员隐藏: <名字>[0x… 原因]`
  名单；`数据源：…入口条目表=45 条（任务板 13 + 可重复 NPC 8 + 可招募船员 24）`；
  顺序行②新增 `｜可招募船员 N[…]` 段（藏几位就少几位，与门槛统计对账）。
* 界面（SWF 重编 **stamp=66**）：type 102 —— 子项「招募他作为船员」/“Recruit them as
  crew”；描述分支（中英：可招募 + 已招募的不会再出现 + 引导操作）；「不可导航」分支
  船员专用措辞（**不说「稍后重试」**——位置不固定/取不到）；新 `crew=` 探针
  （`SaqCrewProbe`：判据过滤后的条数 + 前 2 条 uID，与 `rep=` 同款短格式）。

### 3. 模拟招募正样本复核（Papyrus **op=8** + `crew.probe sim=`）

* **Papyrus op=8**（`SAQ_Main.psc`）：前置读 A → `Game.AddToAvailableCrew` → 读 →
  `Game.RemoveFromAvailableCrew` → 读（**净副作用为零**；动作 = 官方 `Recruited()` 内的
  同一条引擎调用）。**安全设计**：前置 A=1 ⇒ 完全跳过写入（不碰已招募玩家的状态）。
  结果码 = `32 + 状态位`（bit0 前置已招募(未写入) / bit1 Add 后 A=1 / bit2 Remove 后仍 A=1）。
* **DLL 侧**（`crew.probe` 扩展，驱动器 **v72**）：
  * 可选参数 `sim=<FormID>` —— 全表读完后对该位做往返 + 产品行：
    `船员探针 模拟招募：<名字>（0x…）｜前置 A=0｜Add 后 A=1｜Remove 后 A=0｜判定：A 位随招募往返 0→1→0（A 判据成立）`；
  * 逐位行的**直读对照**：`｜Papyrus: …｜直读: …｜招募任务 …｜对照=一致/不一致`
    （直读 = 与产品 kind=2 过滤同一套 `SAQ_Crew::ReadState`）；汇总行 +`｜直读读到 N 位
    （对照 N 位 / 不一致 N 位）` + `｜模拟招募：<判定>`。
* **用例 `r155_crew_probe` 升级**：`teleport.entry 0x00015062`（走船员条目的引导候选链，
  确保玛丽卡引用已加载）→ `crew.probe sim=0x00015062 timeout=120000` →
  断言 = 汇总行 + **模拟招募判定行** + 五条反向（失败码 / 等回执超时 / 招募任务表单取不到 /
  **直读不一致** / **模拟招募跳过**）。
* **断言离线预演**（第 147 轮纪律）：两份合成日志（正常 / 异常）喂 `plan_regex_audit.py`
  —— 正常样本 7/7 全命中；异常样本 5 条反向断言全部命中（判别力成立）。

### 4. 构建与验证（第 156 轮）

* 产物：DLL（开发构建含 harness，**1236480 B**）+ PEX（**21568 B**，op=8）+ SWF
  （**733596 / 733707 B**，stamp=66）+ ESM 复用（4805 B）；
* **部署**：测试态（`Harness=1` / `Plan=SAQ_TestPlan.txt` /
  `AutoLoad=Save1_…142854_2_0_4` / `MaxSizeMB=10` + 补 ini `[Filter] CrewCond=1`）；
* `verify --dev`：**1130 OK / 0 MISS**（工作区 == 部署；含新增约 40 条船员/入口特征）；
* 离线层 5 步全过（黄金快照 19 件：**2 处有意变化** = `entry_targets.json` +
  `SAQ_EntryTable.h` 的 21 → 45，其余 17 件 OK）；
* ~~待实机~~ ⇒ ★★ **待实机**：主计划 **45 条**（含升级后的 r155）—— 判读要点：
  ① `船员门槛=` / `船员隐藏:`（哪些藏了、为什么）；② 模拟招募判定行（**A 判据是否成立**）；
  ③ `crew=` 探针（判据过滤后的条数）；④ 界面「（可招募）…」段（玛丽卡 / 吉迪恩 / 西米恩
  等 P=1 位）+ 「（不可导航）（可招募）…」3 位。

## 16.9 第 157 轮：实机检查收口（直读槽位缺陷修复 + 两条过时断言更新）

### 1. 检查输入（第 156 轮测试会话）

* 会话：2026-09-24 21:55~22:08（`sessionMs` 729437）/ 主计划 **45 条** ——
  **42 PASS / 3 FAIL**（`r47_board_marker` / `r80_repeatable_npc` / `r155_crew_probe`）；
* 判据通道健康：`log_hygiene` 退出码 0（日志 1345727 B / 2336 行零坏字节；结果 JSON 干净）；
  `plan_regex_audit` 172 断言 / 7 条未命中（= 3 条 FAIL 同源 —— 用例断言 FAIL 后
  **用例中止**、后续步骤不跑，「引导请求 / 引导已生效」的未命中是派生现象）。

### 2. r155 FAIL = 产品级缺陷：直读槽位错 1 格（commonlibsf vs 1.16.244）

**实机证据**（`crew.probe` 逐位对照 + 产品行）：

* Papyrus 读到 12 位：**4 位 P=1**（铁杆粉丝 / 吉迪恩·艾克 / 玛丽卡·波罗斯 /
  西米恩·班科夫斯基）；
* 直读 12 位：**全 0** —— 4 位「对照=不一致」（其余 8 位碰巧一致）；
* 产品「船员门槛=12(过0/藏0+12/未知12)」⇒ **过 0**：可读的船员被**全部错误隐藏**；
* 界面 `crew=8` = 当时「引用未加载 ⇒ 保守放行」的 8 位 —— 4 位真「可招募」被错藏，
  列表里实际少 4 条（这正是「对照」机制要抓的东西）。

**离线验证**（`tools/re`，1.16.244 —— 本轮新增工具，把链路自动化）：

* `python tools/re/find_papyrus_native.py IsInFaction --class Actor`：
  字符串 0x4D12D50 → 注册点 0x1F23CE1 → 实现 RVA 0x1F1E7A0 →
  **`call qword ptr [rax+0xBA0]`** ⇒ 槽 = 0xBA0/8 = **0x174**；
  → primary vtable 0x4CB9248 槽 0x174 → 函数 RVA **0x1928CB0**；
  → 函数头 = `48 89 5C 24 08 48 89 74 24 10 48 89 7C 24 18 4C 89 64 24 20`
  （= `SAQ_Crew` 的指纹来源）。
* 第二个注册点（0x1FCE868 → impl 0x1FD12E0）是 tail-call `jmp [rax+0xBA0]` ——
  **再次印证同一槽**。
* 而 commonlibsf 的 `Actor.h` 把 `IsInFaction` 标在 **0x175** —— 比实际多 1 格；
  该槽（RVA 0x1955350）实际是「按 1 字节索引查表」的函数（`movzx ebp, dl` 当索引用）
  ⇒ 拿 `TESFaction*` 当索引 ⇒ 几乎恒 false（偶然随指针低字节碰表项）。
* 交叉印证：槽 0x174（0x1928CB0）接收 rdx = 指针 —— 与 `IsInFaction(TESFaction*)`
  签名吻合；commonlibsf 相邻标注（`0x170 SetLifeState`）同样整体差 1 格
  （游戏 0x16F = 接收 uint32 参数 —— 吻合）。
* 教训（进「已知风险」）：**commonlibsf 的 Actor 虚表编号在本版本整体差 1 格** ——
  以后调任何「未在本项目实机验证过」的 Actor 虚函数前，先用
  `tools/re/find_papyrus_native.py` / `rtti_slots.py` 在**当前版本**上核一遍。

**修复**（`SAQ_Crew.cpp`）：

* 不再调 `a_actor->IsInFaction()`（commonlibsf 声明会编到 0x175）；改为**按槽 0x174
  显式调用** + **函数头 20 字节指纹校验**；
* 指纹不符（换游戏版本）⇒ 直读停用（保守放行 ⇒ 船员全部照常显示）+ 一行 WARN
  （含重验指引：`python tools/re/find_papyrus_native.py IsInFaction --class Actor`）；
* 成功 ⇒ 一行 INFO：`船员直读：槽 0x174 指纹=ok（Actor::IsInFaction 直读可用）`；
* ini 回退口不变（`[Filter] CrewCond=0` = 只写日志不隐藏）。

**修复后的预期**（同存档同位置）：

* `crew.probe` 汇总：「对照 12 位 / **不一致 0 位**」；
* 产品「船员门槛=16(过**4**/藏0+12/未知12)」（4 位 P=1 放行显示、12 位 P=0 隐藏、
  12 位未加载放行）；
* 界面 `crew=` = **16**（12 未加载放行 + 4 解锁）。

### 3. r155 的第二个发现：`AddToAvailableCrew` 不改变 A 位（机制记录）

* 模拟招募往返（Papyrus op=8，**Papyrus 侧读法本来就正确**）：玛丽卡
  `前置 A=0｜Add 后 A=0｜Remove 后 A=0` ⇒ 判定 =「AddToAvailableCrew 未改变 A 位」。
* 即：`Game.AddToAvailableCrew(actor)` **不修改** `AvailableCrewFaction` 的成员关系
  （至少在此场景 / 状态下）——「用 AddToAvailableCrew 现场制造 A=1 正样本」的路走不通。
* 影响：A 判据（「已招募 ⇒ 隐藏」）**仍无正样本验证**（本存档无已招募船员）；按玩家
  第 156 轮口径保留（最坏情况 = 已招募者仍显示「可招募」条目 —— 冗余而非错误）。
  ⇒ 若日后玩家在**正常游玩**中发现「已招募船员仍出现在列表里」的观感问题，再立项
  （届时用真实招募样本对照）。
* 用例侧：r155 的模拟招募断言从「A 位随招募往返 0→1→0」（已证不可能成立）改为
  「判定行必须出现」（机制记录）；**核心判据换成「对照不一致 0 位」**（修复回归防线）。

### 4. r47 / r80 FAIL = 用例断言过时（入口 21 → 45）

* r47：`入口=21(可导航 21｜… 不可用 0)` → **结构口径**
  `入口=\d+(可导航 \d+｜marker [1-9]\d* 原板 \d+ 兜底 [1-9]\d* 不可用 [0-3])` ——
  45 条后总数随引用加载状态变化（实测 29 / 32 / 33）；「不可用」= 3 位 noNav 船员
  （贝蒂 / 埃里克 / 莫亚拉）在引用未加载时只剩自身精确引用、无兜底（实测 1 位 =
  莫亚拉）。
* r80：`入口条目表=21 条（任务板 13 + 可重复 NPC 8）` → **`=45 条（任务板 13 +
  可重复 NPC 8 + 可招募船员 24）`**（编译期常量、不受加载状态影响 —— 最稳判据）。
* 产品日志本身**零改动**（两行统计照旧）；只改断言、注释与 verify 特征。
* 规则沉淀：**用例断言里不写「随运行期状态变化的总数」**（第 151 轮「子串不含数字」
  纪律的延伸）；这类总数只进注释/判读要点。

### 5. 构建与验证（第 157 轮）

* 产物：DLL（开发构建含 harness）**1237504 B**（SHA256 `33BDD095…311E81A6`；
  工作区 == 部署）；Papyrus / SWF / ESM **未变**（PEX 21568 / SWF 733596+733707
  / stamp=66 / ESM 4805）；
* `verify --dev`：**全过**（+产品 3 条指纹串〔ok / fail / vtable 取不到〕+ 计划检查更新
  〔r47 结构 / r80 45 条 / r155 对照与判定行〕+ 3 条反向检查〔旧 21 条 ×2 /
  旧「A 位随招募往返」〕）；
* 离线层 5 步全过；
* ★★ **待实机**：主计划 45 条复跑（预期 **45/45**）；核心判读 = `crew.probe`
  「对照 \d+ 位 / 不一致 0 位」+ `船员直读：槽 0x174 指纹=ok` + `船员门槛=` 的
  「过」= 4 + 界面 `crew=` 段（16 条）。
