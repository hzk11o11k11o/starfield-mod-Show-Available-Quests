# 05 · 任务引导（选中可接任务 → 引擎标记 + 扫描仪路径线）

> 2026-09-19 第 10 轮。对应需求：**「高度整合进游戏原版的任务系统，可以选中跟踪，
> 能调用游戏的任务引导系统正常引导到接取任务的地点（任务目标蓝点，扫描仪任务路径线）」**。

---

## 一、问题：引擎不给「没接的任务」画标记

引擎的画标记判据（BGS 三代一致）：**任务正在运行 + 有一个「已显示」的目标（objective）
+ 那个目标指向某个引用（用别名）**。未接取的任务 `IsRunning == false`
（判据见 `docs/04`），所以它既进不了玩家任务日志，也不会被追踪 —— 没有任何标记。

⇒ 只能**借**：我们自己的 ESM 里放一个「代理任务」，让它带着一个可强制填充的引用别名，
运行时把玩家要去的那个引用塞进去，再把它设为「追踪中」。引擎就会像对待原版任务一样
画出蓝点与路径线。

```
玩家在「可接任务」tab 选中一条 → 按 Enter 或 SET COURSE（键盘 R / 手柄 X）
        │
        ├─ AS3（MissionMenu.as）      SaqGuideSeq++ / SaqGuideQuest = 任务 FormID
        │                             对外暴露 SAQ_PeekGuide() → "<序号>|<FormID>"
        │
        ├─ DLL（SAQ.cpp / SAQ_Guide）  轮询 PeekGuide（100ms）→ 序号变了就是新请求
        │                             按 FormID 在静态表里取「引导目标引用」（下面第二节）
        │                             写 GLOB：SAQ_GuideTargetRef = 目标引用
        │                                       SAQ_GuideState    = 0（=有新请求）
        │
        ├─ Papyrus（SAQ_Main.psc）     0.5s 轮询（StartTimer/OnTimer，Starfield 没有
        │                             RegisterForUpdate）+ 菜单关闭时立刻检查
        │                             GuideState == 0 ⇒ 干活：
        │                               GetAlias(0).ForceRefTo(目标)
        │                               SetObjectiveDisplayed(10, true, true)
        │                               SetActive(true)      ← 引擎开始「追踪」这条代理任务
        │                             然后 GuideState = 1（已应用）/ 2（目标取不到）/ 4（别名不存在）
        │
        └─ 引擎                        目标别名已填充 + 目标已显示 + 任务被追踪
                                       ⇒ 任务标记（蓝点）+ 扫描仪路径线
```

代理任务与别名（`SAQ_MainQuest`）：目标 10 的显示文本是 `<Alias=SAQ_GuideTarget>` ——
引擎会把别名里那个引用的**本地化名字**填进去（中英随游戏语言），所以 HUD 上显示的是
「文森特·萨利纳斯」这样的目标名，而不是一句写死的文案。

---

## 二、「去哪里接」这个引用怎么算出来（离线数据）

`tools/esm/gen_guide_targets.py`（第 17 轮起：遍历**全部 master**（基础游戏 + 各 DLC），
约 3-6 分钟）。

> **多 master（第 17 轮）**：别名数据改成**直接解析 ESM**（`ALST/ALID/ALUA/ALFR/ALFL`），
> 不再依赖 xEdit 的树状导出；每条目标记 `refrMaster`（记录号 + 所属插件）——
> 一条 DLC 任务引用的 NPC / 地点可能在基础游戏里，反之亦然。
> 运行期由 `SAQ_Masters::MakeFormID()` 按加载顺序拼出真正的 FormID。

| 优先级 | 来源 | 说明 | 覆盖（202 条候选） |
| --- | --- | --- | --- |
| 1 | **ALFR「Forced Reference」** | 任务记录里自己写的一个具体引用（`XMarker`/`EnableRef`/地标 `MapMarker` 之类），**通常是常驻引用**；落在「别名暂存格」里的丢掉 | 64 条 |
| 2 | **ALUA「Unique Actor」→ 该 NPC 的放置引用（ACHR）** | 任务发布者；标记会跟着人走 | 104 条 |
| 3 | QUST `LNAM` / ALFL「Specific Location」→ LCTN `MNAM` | 任务地点的**地图标记引用**（只有少部分地点有） | 0 条（能被上面的吃掉） |
| — | 合计 | | **168 / 202 = 83%** |

排序键（`collect_candidates`）：**先「常驻（persistent）」**（即记录标志 `0x400` —— 格子没
加载时 `Game.GetForm` 也拿得到、`ForceRefTo` 也能成；临时引用要等格子加载），
再按来源质量（发布者 > 任务落脚点 > 地点标记）、是否杂兵名（Guard/Settler/…）、出现顺序。

实测：168 条里 **常驻 100 条**、非常驻 68 条（都是「发布者只有临时放置」的情况）。

### 关键数据证据（都可复现）

* `QUST.LNAM` = 任务的 Location（LCTN FormID）。
* `LCTN.MNAM` = 该地点的**地图标记引用**：
  `CityNewAtlantisLodgeLocation`(LCTN 0027C8ED)`.MNAM` = `000F93F1`
  = `NewAtlantisMapMarkerLodge`（`XMRK` 地图标记 + `FULL` 名字「陋室」）。
* NPC 放置引用：全表遍历 CELL/WRLD 组，找 `NAME == NPC_` 的 ACHR。
  `FC_Neon_HuongLe`(NPC_ 0005797C) → ACHR 0005797D `Neon_HuongRef`（cell 00294F32 / world 0027CF9E NeonCity）。
* xEdit 导出里的别名形态（`ref/xedit/quests_typed.txt`）：
  `ALUA - Unique Actor = FC_Neon_HuongLe "Huong Le" [NPC_:0005797C]`；
  `ALFR - Forced Reference = FFNeonZ09_EnableRef [REFR:00089760] (places XMarker … in NeonCity "Neon")`。

排除项（BAD_CELL_RE）：别名暂存格（`…AliasCell` / `DO NOT DELETE` / `Holding`）里的引用
一律不用 —— 玩家到不了那里，标记会指向空气（例：`CF01_InviteSlateRef` 在
`CrimsonFleetAliasCell "CF Alias Cell - DO NOT DELETE"` 里）。

---

## 三、ESM 补丁（`tools/esm/patch_saq_esm.py`）

代理任务原来是 xEdit 生成的一个「空壳」QUST（只有 EDID/VMAD/DNAM）。引导需要它带
**一个引用别名 + 一个目标指向该别名**，这两样由本补丁直接写字节完成（幂等，可反复跑，
`build-saq.ps1` 的 ESM 步骤之后会自动执行）。

记录结构（**格式全部从 Starfield.esm 里的真实样板抄来**，不是猜的）：

```
Reference Alias（脚本填充型，样板 SQ_Followers\AvailableFollowers）
  ALST(4)=别名id | ALID(N)=名字+NUL | FNAM(4)=标志 | ALFG(4) | VTCK(4) | ALED(0)
Objective（样板 MB_Bounty01Far 的目标）
  QOBJ(2)=目标索引 | FNAM(4)=标志 | NNAM(文本) | QSTA(12)=别名id|标志|关键词
ANAM(4) = 下一个空闲别名 id
```

本 MOD 写进去的内容：

```
QOBJ=10 | FNAM=0 | NNAM="<Alias=SAQ_GuideTarget>" | QSTA={alias 0,0,0}
ANAM=1
ALST=0 | ALID="SAQ_GuideTarget" | FNAM=0x02 | ALFG=0 | VTCK=0 | ALED
```

* 文本用**内联字符串**：本文件的 TES4 头 flags = `0x01`（**没有** `0x80` = Localized），
  所以文本字段是内联的，**不需要**往游戏的 strings/`Localization.ba2` 里加 ID。
* 别名不给任何填充方式（脚本填充型的标准做法）⇒ 任务启动时它保持空，`ForceRefTo` 可用；
  也不会因为「别名填不上」把 Start Game Enabled 的任务卡在启动阶段。
* 别名标志取 `0x02`，与同样是「脚本运行时填充」的 `AvailableFollowers` 一致。
* 自校验：写完重新解析一遍，并检查「每条记录都正好填满自己所属的 GRUP」。

### 坑（踩过一次）

`write_esm` 原来用「原组大小 + 增量」修 GRUP 的 size，结果第一次写坏之后错误被继承下去，
记录越过了组的边界。现在改成**整体重新序列化 + 重算组大小**，并用
`walk_linear()`（不依赖 GRUP size 的线性遍历）读文件 —— 对残留错误免疫。

---

## 四、DLL ↔ Papyrus 的通道（GLOB）

SFSE 的接口只有 `QueryInterface / Messaging / Trampoline / Menu / Task` —— **没有** SKSE
那种「C++ 直接给 Papyrus 发 ModCallbackEvent」的通道。所以 DLL→Papyrus 只能借引擎自己
认得的持久对象：**GLOB**（一个 float）。

| GLOB | 记录号 | 方向 | 含义 |
| --- | --- | --- | --- |
| `SAQ_GuideTargetRef` | 0x801 | DLL → 脚本 | 引导目标 FormID 的**低 24 位**（0 = 取消引导） |
| `SAQ_GuideState` | 0x802 | 脚本 → DLL | 0 待处理 / 1 已应用 / 2 目标取不到 / 3 已清除 / 4 别名不存在 |
| `SAQ_Notify` | 0x803 | 脚本 → DLL | **7777 + 菜单打开次数**（身份锚点 + 证据链） |
| `SAQ_TestMode` | 0x804 | 玩家 → DLL | 测试过滤开关（ini / 控制台，见第八节） |
| `SAQ_GuidePrefix` | 0x805 | DLL → 脚本 | 引导目标 FormID 的**高 8 位**（第 21 轮，见下） |

新增记录一律**追加在末尾**（FormID 存进存档，老的不能挪）。

### ★ 怎么找到自己的 GLOB（本模块最容易踩的地方）

FormID = `(加载顺序序号 << 24) | 记录号`，而**序号运行期才知道**（MO2 顺序、DLC 都会影响），
所以不能把 `0x02000801` 写死。做法是让脚本给一个身份锚点：

* `SAQ_Main.psc` 在 `OnInit` 里把 `SAQ_Notify` 写成 `7777`，之后每次菜单打开 +1；
* DLL 遍历 `0x00..0xFF` 个前缀，在 `0x803` 上找「值落在 7777..8776 的 GLOB」= 自己的插件；
  再取同前缀的 `0x801/0x802`。

这一步顺带把整条证据链补齐了：

| 日志里看到 | 结论 |
| --- | --- |
| `ESM 通道：前缀=0xNN 目标=… 状态=… 通知=7777` | ESM 已加载、代理任务在跑、**脚本活着**、GLOB 读写通 |
| `通知` 的值在涨 | 脚本看得到任务菜单的开关事件 |
| `ESM 通道：未认领（…候选: 0x02:0 …）` | ESM 没启用 / 脚本没跑 / 记录号变了（候选里能看到各前缀读到的原始值） |

写之前还有一道自检（`SetGuideTarget`）：`GuideState` 必须落在 `[-0.5, 8]`，
目标分量必须看起来像 FormID —— 认领靠读 `TESGlobal::value`，这一步等于再确认一次
「偏移在本机是对的」，不对就拒绝写内存。

### ★★ GLOB 是 float：完整 FormID 存不下（第 21 轮实机 bug）

**症状**（2026-09-20 实测日志）：引导过一次 DLC 任务后（目标 `0x0107BDB2`「瓦鲁贮藏所」），
**之后每次写入都被拒绝**（结果码 2）——玩家看到「点亮瞬间就取消、其余条目全部点不亮」：

```
[10:24:11.501] 引导请求：同情生者（0x010D5E84）→ 引用 0x0107BDB2（瓦鲁贮藏所）   ← 写入成功
[10:24:11.940] [W] 引导请求：取消失败｜拒绝写入：SAQ_GuideTargetRef 读出来是 17284530（不像 FormID）
```

两个叠加的错：

| # | 错 | 后果 |
| --- | --- | --- |
| 1 | 自检上限写成 `0xFFFFFF`（那是**低 24 位**的上限），而 `target` 里存的是**完整 FormID** | DLC 目标的完整 FormID 会超过它 ⇒ 通道里存过这种值后**每次写入都被拒** |
| 2 | GLOB 是 float（尾数 24 位），完整 FormID > 2^24 后**奇数不能精确表示**（±1） | 轻则 `GetForm` 取不到（状态 2），重则**指向相邻的另一条记录 = 错误的引导目标** |

**修法**：目标的完整 FormID 拆成两个 GLOB —— 低 24 位（`0x801`）+ 高 8 位（`0x805`），
两个分量都在 float 精确区内；脚本按 `GuidePrefix 是否存在` 自动兼容旧通道；
自检改成分量检查，并保留对「修复前残留的完整值」的兼容（会被下一次写入覆盖，不会卡死）。

> 教训：**凡是走 float 通道传数值，先问一句「这个值的量级会不会超过 2^24」**。
> （`TESGlobal::value` 的偏移是 `0x48` —— commonlibsf 头文件里有 static_assert，
> 上一代项目修正过；原头文件写 0x40 是错的。）

---

## 五、Papyrus 侧（`scripts/SAQ_Main.psc`）

* `OnInit`：写身份锚点、`RegisterForMenuOpenCloseEvent("BSMissionMenu")`、`StartTimer(0.5, 1)`。
* `OnTimer`：重新排下一拍 + `ApplyGuide()`。
  （Starfield 的 Papyrus **没有** `RegisterForUpdate`，只有一次性 `StartTimer`，
  所以要自己重排；0.5 秒一次的空判开销可忽略。）
* `OnMenuOpenCloseEvent`：开 → `SAQ_Notify += 1` + `Debug.Trace("[SAQ] 菜单打开 通知=N")`
  （★ 第 19 轮：这行 Trace 是「脚本活性」的直接证据，DLL 侧有对应的活性探测，见六·补三）；
  关 → 立刻 `ApplyGuide()`（不用等下一拍）。
* `ApplyGuide()`：`GuideState != 0` 直接返回；否则按目标 FormID `Game.GetForm(...) as ObjectReference`
  → `ForceRefTo` + `SetObjectiveDisplayed(10, true, true)` + `SetActive(true)`。
* 别名用 **`GetAlias(0)`** 取，而不是写 VMAD「别名属性」—— 后者要自己拼 Object/Alias
  联合体的字节，容易错；`GetAlias(int)` 是原生函数，零风险。
* `AutoConst` 在 Starfield 的 Papyrus 4.7 里**不存在**（实测报 `Unknown user flag autoconst`），
  常量属性要写 `AutoReadOnly`。
* ★ 第 26 轮：**每个「菜单打开」事件都重挂一次 `StartTimer` 并补一次 `ApplyGuide()`**。
  原因（实测）：脚本实例**从存档恢复时 `OnInit` 不跑**（三个会话的 Papyrus 日志里都没有
  `OnInit`），而 `StartTimer` 不随存档恢复 ⇒ 定时器从来没注册过 ⇒ 菜单开着时脚本没有任何
  轮询机会，引导只能靠「菜单关闭」分支里的 `ApplyGuide()` 应用一次。
  `StartTimer` 重复调用同一 id 只是重置，无副作用。

---

## 六、AS3 侧（`ui/missionmenu/src/MissionMenu.as`）

* 「可接任务」条目（`bSaqAvailable`）在原版里 `CanTrackOrUntrack == false`（引擎不认它），
  所以在 `onMissionListItemActivated`（Enter）与 `OnPlotCourseEvent`（SET COURSE：键盘 R / 手柄 X）里
  **先于原版判定**拦下来 → `SaqToggleGuide()`。
* `onMissionSelectionChange`：选中我们的条目时把 SET COURSE 按钮置为可用
  （「显示在地图上」保持禁用 —— 引擎里没有这条任务的位置数据）。
* ★ **提示里的键名不要写死**（第 15 轮踩过）：`SET COURSE` 的 `UserEventData` 事件名是
  `"XButton"`，那是**手柄 X 按钮**的内部名；键盘下同一事件映射到 **R**。文案里的键名要用
  `KeyHelper.GetButtonNameForEvent("XButton","")` 动态取（与底部按钮栏同一来源），
  取不到就退回「使用底部的设定航线按钮」，否则会提示出游戏里不存在的「X 键」。
* `SAQ_PeekGuide()` 暴露 `"<序号>|<任务FormID>"`；DLL 每 100ms 读一次，序号变才处理
  （同一条再按一次 = 取消，FormID 传 0）。
* `SAQ_Report` 尾部加 `guide=<序号>|<FormID>|<最后一次动作>`，日志里能看到玩家点了什么。
* ★ 第 23 轮：**「能不能导航」要看得见**。载荷第 5 列 = `hasGuideTarget`（1/0，来自静态表
  `guideRefLocal`），界面据此：
  * 无目标的条目（261 条里 52 条）：右侧描述直接写明「还没有导航目标」、
    SET COURSE 按钮**置灰**、点击**不发请求**（此前是「亮起 → 结果码 1 → 瞬间回滚」，
    玩家看不出原因，实测以为坏了）；
  * 有目标：照旧（点了就发引导请求）。
  内嵌回退数据的生成器（`gen_quest_table.py::build_payload`）同步加了这一列。

### 六·补、界面状态与「真实引导」的同步（第 16 轮）

AS3 的 `SaqToggleGuide` 只能在**本地立刻**切状态（音效 / 竖条 / 文案），但「这条任务
到底有没有引导目标」「现在实际在引导哪条」只有 DLL 知道。缺回写通道时踩过两个坑
（都有日志实证）：

| 现象（日志） | 根因 | 修复 |
| --- | --- | --- |
| 点「调查结果」→ 界面 `guide=…已设为引导:调查结果`，DLL 却打 `引导请求：…没有引导目标` | DLL 拒绝时只写日志、不回传；界面停在「已设为引导」，实际什么都没变（旧引导还在时更误导） | DLL 回写 `_root.SAQ_GuideReply("<seq>\|<实际FormID>\|<结果码>")`；AS3 按「实际值」对齐：不一致 = 被拒绝 → 回滚竖条 + OFF 音 + 失败文案 |
| 菜单重开后界面 `guide=0\|0\|-`，DLL 还在引导「群众心态」 | 菜单每次打开是新 SWF 实例，`SaqGuideQuest` 归 0 ⇒ ① 竖条丢失 ② 点「正在引导的那条」第一次变成重设而非取消 | 推送成功后 DLL 调 `_root.SAQ_SyncGuideState("<实际FormID>")`；AS3 只在玩家没操作过（`SaqGuideSeq == 0`）时接受 |

结果码约定（DLL → AS3）：`0=成功 / 1=没有引导目标 / 2=写 ESM 通道失败 / 3=静态表里没有这条`
/ ★ 第 19 轮新增 `4=引导未生效（脚本确认超时 / 别名不存在）` —— 界面回滚，见六·补三。
应答（AS3 → DLL，只进日志）：`ok / same / stale（玩家已点了新的，原请求作废）/ skip / bad`。

### 六·补二、第 17 轮：引导链路的三处「沉默」被补上（都有日志实证）

| 沉默 | 现象（第 16 轮日志） | 修法 |
| --- | --- | --- |
| **下发结果没人确认** | 09:18:39 下发成功 → 09:18:46 关菜单，日志里没有一行说明脚本有没有应用 | DLL `PollGuideVerify`：下发后 900ms 起轮询 `SAQ_GuideState` —— `1` 记 `引导已生效：…`；`2/3` 自动重发（≤2 次）；`4` 明确报「别名不存在」；一直 `0` 则超时 WARN。菜单关掉后也继续跑（脚本正是在关菜单时应用引导） |
| **重启/读档后不认领已有引导** | 引导状态存在 GLOB 里（跟着存档走），DLL 内存是空的 ⇒ 日志 `引导状态：无`，而游戏里还挂着旧蓝点，且没有任何入口能取消它 | DLL `AdoptExistingGuide`：开菜单时若「自认为没在引导」而通道里有目标 ⇒ 按目标引用反查静态表认领回来（`认领已有引导：…`），UI 随即同步成「正在引导」 |
| **接到任务后引导不消失** | 只有「已完成」会自动取消；玩家接到手之后 HUD 还一直指着那个地点 | AS3 `SaqAutoCancelIfAccepted`：`SaqRefresh` 里发现被引导的 uID 出现在玩家任务日志（QuestData）⇒ 序号 +1、目标清 0（与手动取消同一条路）；DLL 侧**不需要新协议** |

**另外两处「静默失效」的自愈**（同轮）：

* **脚本重挂**：读档会让 `SAQ_Main` 重新 `OnInit`（一次会话实测 3 次）——别名被清空、
  脚本把 `GuideState` 写成 3，而 DLL/界面仍认为在引导（蓝点其实没了）。
  → 脚本 `OnInit` 里发现 `GuideTargetRef > 0` 就把 `GuideState` 置回 0（轮询会重新应用）。
* **引用当时没加载**（脚本状态 = 2）：非常驻引用在远处取不到，之后玩家飞近了脚本
  也不会自己重试。→ DLL 在每次打开菜单时检查，发现「自己认为在引导 + 状态是 2/3」
  就重写一遍通道（`引导重新下发：…`）。状态 4（别名不存在）不重发 —— ESM 补丁没生效，重发没用。

### 六·补三、第 19 轮：脚本活性探测 + 「引导未生效」的收尾

**起因**（第 18 轮实测日志复查）：该会话的 Papyrus 日志（09:54~09:56，749 行活跃）
里**没有任何 SAQ_Main 痕迹**（历史会话每次都有 `SAQ_Main OnInit`），而 DLL 读到的
`通知=7778` 与上一次会话完全相同 —— 无法区分「脚本实例从存档恢复后僵死（收不到
菜单事件/定时器）」和「DLL 读通道比脚本处理早」。若真僵死，玩家点引导会
「写入成功但没人处理」：界面显示已引导，世界里什么都没有。

| 改动 | 说明 |
| --- | --- |
| 脚本 Trace（`SAQ_Main.psc`） | 开菜单时 `Debug.Trace("[SAQ] 菜单打开 通知=N")` —— Papyrus 日志从此可**直接证明**脚本在响应菜单事件 |
| DLL 活性探测（`SAQ.cpp::CheckScriptLiveness`） | 开菜单 1.5 秒后复读 `SAQ_Notify`：值 +1 ⇒ 脚本活着（静默）；没涨 ⇒ WARN 留证（每轮菜单一次） |
| 「引导未生效」收尾（`AbortUnverifiedGuide`） | `PollGuideVerify` 确认超时（≈11 秒状态仍是 0）或状态 4（别名不存在）时：清 ESM 通道 + 清本侧状态 +（菜单开着时）用**结果码 4** 回写界面回滚。否则界面会一直显示「正在引导」（界面说谎），且下次开菜单还会把通道认领回来，无限重复 |
| UI 桥降噪 | 首次桥解析遇到「UI 里没找到 BSMissionMenu 条目」也降为 INFO（菜单刚开的头几帧是预期状态） |

### 六·补四、第 26 轮：「引导未生效」的三次假失败 —— 确认窗口改绑菜单状态

**起因**（2026-09-20 10:38~10:58 三个会话的日志复查）：出现三次
`引导未生效：… 脚本状态一直是 0`（星海派对 / 回收行动 / 失踪的地球人）——明明有引导
目标、写入通道也成功，却全被判「脚本没响应」并清掉通道（界面回滚「引导未生效:脚本未响应」）。

**证据链（DLL 日志 × Papyrus 日志交叉）**：

* Papyrus 日志（`Papyrus.0~3.log`）里：`引导已应用` **只出现在菜单关闭那一刻**
  （`Papyrus 10:41:56 引导已应用` ↔ DLL `10:41:56.097 菜单关闭`，一一对应）；
  三个会话**都没有 `OnInit`**、也没有任何一条「菜单开着时」的引导应用。
* ⇒ 脚本实例从存档恢复时 `OnInit` 不跑 ⇒ `StartTimer` 从未注册 ⇒ **菜单开着时脚本不会轮询**，
  引导只在关闭事件里被应用一次。
* 唯一一次 `引导已生效`（绝非虚言）是因为玩家在 **1.6 秒内**就关了菜单。
* 旧逻辑：下发后 900ms 起确认，≈11 秒超时 ⇒ 玩家在菜单里挑条目超过 11 秒 = 假失败 + 真失效。

**修法（两侧）**：

| 侧 | 改动 |
| --- | --- |
| DLL `PollGuideVerify` | **菜单还开着时不判失败**：状态仍是 0 就继续等（不计数、不 Abort，每个请求打一行 `引导确认：脚本状态还是 0，但菜单还开着 …`）；状态 1/4 与重发（2/3，≤2 次）逻辑不变，重发改用独立计数 `verifyResends` |
| DLL `OnMissionMenuClosed` | 菜单关闭 = 脚本真正拿到请求的时刻：若还有未确认的下发，把确认窗口**重置**（900ms 首验 + 6×2s）并打 `引导确认：菜单已关 …`。超时判定只发生在菜单关着时 |
| 脚本 `SAQ_Main.psc` | 菜单打开事件里 `StartTimer` 重挂定时器（补齐实例恢复后缺失的轮询）+ `ApplyGuide()` 补一次（处理「菜单关着时 DLL 重发的请求」）；若引擎允许，引导在菜单开着时 0.5 秒内即应用 |

**实测判据（下次进游戏）**：

* 菜单里点一条有目标的条目后**不关菜单**：日志应出现
  `引导确认：脚本状态还是 0，但菜单还开着 …`，**不应**再出现 `引导未生效`；
* 关菜单：应出现 `引导确认：菜单已关 …`，随后 ≤11 秒内
  `引导已生效：… 脚本状态=1`（菜单关着也能记）；
* 若脚本重挂定时器生效：菜单开着时应直接看到 `引导已生效`（1~2 秒内），
  且 Papyrus 日志里 `[SAQ] 引导已应用` 的时间不再与「菜单关闭」绑定；
* 菜单里连点/切换多条：只有最后一条会生效（语义正确 —— 通道只有一个目标）。

---

## 七、失败姿态与日志核对清单

引导的任何一步失败都**不会**影响列表/过滤/原版功能，只在日志里留证：

| 日志 | 含义 | 下一步 |
| --- | --- | --- |
| `ESM 通道：未认领（…）` | ESM 没启用 / 脚本没跑 | 看候选值；确认 MO2 里 ESM 已勾选、`Scripts\SAQ_Main.pex` 在位 |
| `引导请求：… 没有引导目标` | 这条任务离线没算出目标（34/202） | 属于已知缺口，列表照常显示 |
| `引导状态：… 脚本状态=2` | `Game.GetForm` 取不到引用（非常驻引用 + 格子没加载） | 记下来，考虑换常驻锚点 |
| `引导状态：… 脚本状态=4` | 别名不存在（ESM 补丁没生效） | 重跑 `patch_saq_esm.py` 并确认部署的 ESM 是打过补丁的 |
| `引导状态：… 脚本状态=1` | 已应用 ⇒ 世界里应该能看到标记/路径线 | 若没有标记，看 `SetActive`/objective 是否被引擎接受 |
| `脚本活性探测：… SAQ_Notify 仍是 …（没有 +1）`（第 19 轮） | 脚本没在响应菜单事件（实例从存档恢复后僵死） | 看 Papyrus 日志有没有 `[SAQ] 菜单打开`；缺失 ⇒ 重新读档/重开游戏后再试 |
| `引导确认：脚本状态还是 0，但菜单还开着 …`（第 26 轮） | 正常现象：脚本只在关菜单时应用引导，先继续等（不判失败） | 无需处理；关菜单后会自动确认 |
| `引导未生效：…` + `引导结果已回写界面：… 结果码=4`（第 19/26 轮） | 脚本确认超时（**只在菜单关着时判定**）/ 别名不存在 ⇒ 已清通道并回滚界面 | 按原因处理：脚本僵死 → 重新读档；状态 4 → 重跑 patch |

复现全部离线数据：

```powershell
python tools\esm\gen_guide_targets.py        # 引导目标（含覆盖率与样本）
python tools\esm\patch_saq_esm.py --check    # 看 ESM 里现在的别名/目标结构
python tools\esm\esm_probe.py rec LCTN 0027C8ED      # 地点记录 → MNAM 地图标记
python tools\esm\esm_probe.py place X 0005797C       # NPC 的放置引用
python tools\esm\esm_probe.py byid X 000F93F1        # 按 FormID 找记录（含它属于哪个组）
```

---

## 八、测试过滤开关（第 20 轮：ini 文件 / 控制台 GLOB）

测试期列表有 250+ 条，想验证某一件事（引导 / 界面回滚 / DLC）时，在列表里滚动找
条目太费劲。两条通路（同一个「模式」值；控制台非 0 时优先）：

| 通路 | 怎么改 | 状态 |
| --- | --- | --- |
| ① **ini 文件（推荐）** | **mod 目录内** `SFSE\Plugins\SAQ_ShowAvailableQuests.ini` → `[Test] Mode=N`；改完保存，**关开一次任务菜单**即生效（不用重启游戏） | ✅ 首次运行自动生成模板 |
| ② 游戏控制台 | `set SAQ_TestMode to N`（ESM 里的 GLOB，记录号 0x804） | ⚠️ 本机实测不认（见下） |

> ★ 第 22 轮（玩家要求）：**日志与这个 ini 都放在插件目录**（MO2 下 = mod 目录里的
> `SFSE\Plugins\`），删 mod 时一起删掉，不会残留到 `Documents\My Games\...`。
> 插件目录建不出文件时日志才回退到 SFSE 默认目录（并在日志里 WARN）。

> **实机反馈（2026-09-20）**：`set SAQ_TestMode to 4` 报 `Unknown variable 'SAQ_TestMode'.` ——
> 同时日志里 `ESM 通道：… 测试=0` 证明 GLOB 已加载 ⇒ 这台机器的控制台**不按 EDID 解析
> mod 的 GLOB**。所以 ini 是主通路；控制台那条留着碰运气（能认就用）。

| 组件 | 干什么 |
| --- | --- |
| ESM（`patch_saq_esm.py`） | 保证存在 GLOB `SAQ_TestMode`（记录号 0x804，初值 0）。已存在则**保留当前值**（重新构建不会清掉你的设置） |
| DLL（`ResolveTestMode` / `CollectAvailableQuests`） | 每次开菜单读一次：GLOB 非 0 用它，否则读 ini；按模式过滤显示（与既有过滤是「与」的关系） |
| 玩家 | 改 ini（推荐）或控制台（若可用） |

模式（`N`）与静态表里的条数（260 任务全量为基准；另有 12 条「任务板入口」，见第九节）：

| N | 只显示 | 表内条数 |
| --- | --- | --- |
| 0（默认） | 全部（不过滤） | 260 任务 + 12 入口（再减去运行时/玩家日志过滤） |
| 1 | 有引导目标的（点了能出蓝点） | 209 |
| 2 | 没有引导目标的（测界面回滚 / 结果码 1） | 52 |
| 3 | DLC 条目（验证 DLC 支持） | 59 |
| 4 | 有引导目标 **且** 有具名地点的（最少最好找） | 85 |
| 5 | **无限任务入口（任务板）**（第 27 轮；模式 1~4 不混入入口条目） | 12 |
| 6 | **「需要靠近」的任务**（第 46 轮；候选池**全是非常驻引用** —— 远处点引导会走「目标尚未加载 → 待生效 → 退避重试 → 靠近后自动生效」那条链路；历史样本「营救机器人」） | 20 → **0**（第 146 轮起） |

注意：

* ini 与 GLOB 都是「模式值」，**GLOB（控制台）非 0 时优先**；两者都 0 = 不过滤；
* 日志里会醒目打印 `测试模式：N（…）[来源=ini/控制台] —— …`，
  以及 `运行时状态：… 测试过滤=N …`（被它过滤掉的条数）；
* ESM 是旧版（没有这条 GLOB）时 GLOB 恒为 0（ini 仍可用），通道摘要里写 `测试=无(ESM 旧版)`；
* 首次运行 DLL 会在**插件目录**里生成一份带说明的 ini 模板（已存在则不动）；
* 重构 ESM（`-RebuildEsm` / xEdit 重生成）后不用手改：`patch_saq_esm.py` 每次构建
  都会把它补回来（幂等）。
* ★ 第 46 轮：**模式 6 专用于验证大项 B** —— 列表里只剩那 76 条「候选全是非常驻」的任务，
  按任意一条点引导即可复现「待生效 / HUD 提示 / 自动重试 / 靠近后生效」的完整链路；
  模式 6 同样**不混入**入口条目（入口是模式 0 / 5 的事）。
  （判定函数 `AllCandidatesNonPersistent()` 与界面「需要靠近」提示、DLL 的 `approachPending`
  共用同一个判据 —— 静态表候选 flags bit0。）
* ★★ 第 146 轮（DLC world 级兜底收口）：最后 1 条（`SFBGS001_VKaiZ02`）补上
  **DLC world 级常驻兜底**（候选 1020 → 1021）⇒ 本机清单 **1 → 0**；模式 6 **保留作
  回归探针**（新内容再出现这类任务时一眼筛出），文案已同步（DLL ini 模板 +
  `resources/SAQ_ShowAvailableQuests.ini`，`第 146 轮起为 0 条`）—— 明细 = `docs/14` 九节。

> ★ 第 46 轮补丁（玩家实测「过滤失效了，我能看到所有任务」）：`ResolveTestMode` 里
> 有一句写死的上限 `v > 5 ? 0 : v`（第 27 轮留下的）⇒ ini 写 `Mode=6` 被**静默折成 0**
> （=不过滤），而旧代码又只在 `mode > 0` 时才打日志 ⇒ 玩家侧「一行日志都没有」。
> 修法：① 上限改成常量 **`kMaxTestMode = 6`**（加新模式必须一起改，注释里写明了）；
> ② 超范围**打 WARN**（`测试模式：ini 的 Mode=N 超出已知范围（0~6）…[来源=ini(未知值)]`，
>    控制台同理）；③ `测试模式：…` 这一行**无论开没开都打** —— 从此能一眼区分
>    「ini 读到了但值是 0」与「ini 根本没读到（来源=默认）」。
> 产物：DLL 767488 B；verify 增 1 条（`测试模式超范围告警`）。

---

## 九、无限任务入口「任务板」（第 27 轮）

需求（AGENTS.md）：**无限生成任务本身不显示，但接取无限生成任务的目标可以作为一条数据
显示在列表里（如任务板、提供无限任务的 NPC）**。

任务板在数据里是 ACTIVATOR `MissionBoardConsole*`（`esm_probe list ACTI --grep board`
可复现；`explore_boards.py` 列出全部 102 个放置引用）。做法：

| 层 | 细节 |
| --- | --- |
| 生成 | `tools/esm/gen_entry_table.py` 按**白名单**（每个城市/据点一块，12 条）扫 ESM 校验后输出 `plugin/src/SAQ_EntryTable.h`；`build-saq.ps1` 静态表步骤内自动跑（约 1~2 分钟） |
| uID | = 任务板在世界里的放置引用（REFR）的运行期 FormID —— 与任务的 FormID 空间不冲突，且**引导目标就是它自己** |
| DLL | `CollectAvailableQuests` 末尾追加（默认模式 0 与模式 5；模式 1~4 不加）；引导请求 / 认领 / 日志对入口条目同样成立（`DisplayNameOf` 统一取名） |
| AS3 | payload `type=100` → 子项名「前往任务板 / Go to the mission board」+ 专用描述（「这是一块任务板…」） |
| 日志 | `数据源：…；入口条目表=12 条（任务板）`；`菜单打开：待推送≈272`（260 + 12） |

★ **持久性**（第 30 轮定案）：12 条**任务板引用**里只有阿基拉城（0x0014D497）是原生常驻；
其余 11 条非常驻。第 29 轮试过「override 成常驻」—— **被实机否定**（override 只换数据、
不改变引用的加载分类；官方同类 override 的原记录本来就全是常驻 = 零先例，
见 `docs/99` 一·补十五与 `tools/esm/check_persist_precedent.py`）。

第 30 轮的做法 = **新建常驻引用 + 候选链**：

| 档 | 内容 | 谁生成 |
| --- | --- | --- |
| ① | 本插件**新建**的常驻 XMarker（记录号 0x900~0x90A，位置 = 各板坐标，flags 0x400，cell 的 CellPersistent 组） | `tools/esm/create_board_markers.py`（产物 `ref/board_markers.json`） |
| ② | 任务板引用自身（原生常驻的可用；非常驻只有 cell 加载时可用） | `gen_entry_table.py` |
| ③ | 同 cell 里离板最近的原生常驻引用（≤2 个，XMarker 系优先、≤25 m，位置差 1~20 m） | `gen_entry_table.py`（读 `ref/entry_persistent_scan.json`） |

运行期 DLL 依次 `LookupByID` 取第一个命中的（`SAQ.cpp::AppendEntryRows`；marker 的运行期
FormID = Guide 通道前缀左移 24 位再或上 markerLocal）。日志判据：
`入口=12(可导航 N｜marker a 原板 b 兜底 c 不可用 d)` + 没走 ① 的条目带「入口候选诊断」。
统计工具：`python tools/esm/ref_flags_report.py`。

清单（`ref/entry_targets.json`）：新亚特兰蒂斯（井区）/ 阿基拉城（磐石区）/ 霓虹城（核心）/
塞多尼亚 / 霍普镇 / 新家园 / 星座小屋 / 龙神工业 / 狄摩斯星船厂 / 三叉戟星船厂 /
斯特劳德-埃克伦德星船厂 / 星钥站。

### ★ 第 31 轮：候选链改「**精确目标优先**」+ 引导目标动态更新

**起因（玩家反馈 + 13:15 截图）**：任务板的蓝点停在板子**旁边 2~3 m**。日志证据：

```
[13:11:54] 入口=12(可导航 12｜marker 0 原板 2 兜底 10 不可用 0)
[13:11:57] 引导请求：任务板 · 陋室（0x00137573）→ 引用 0x0011B754 …   ← 兜底引用，不是板
```

两层原因：

| # | 原因 | 证据 |
| --- | --- | --- |
| 1 | 11 条**新建常驻 marker 全部取不到**（`marker[未命中]`）—— 引擎里根本没有这些引用 | 第 30 轮设计的「任意位置精确导航」落空；新增 `入口 marker 探测` 日志专门定位它 |
| 2 | 于是目标落到「同 cell 最近的原生常驻引用」= **兜底**，它们离板 1.8~8.1 m（见下表）—— 蓝点当然不在板上 | 陋室那条就是 `MQ204_NoelBasement_Marker01a`（沙盒标记），距板 **3.41 m** = 截图里的偏差 |

★ 关键疏漏：第 30 轮的顺序是 `marker → 原板 → 兜底`，把**兜底排在原板前面**。而玩家走到板前时
板所在 cell 已加载 ⇒ **任务板引用自身其实取得到**（非常驻引用只要 cell 加载就存在），
它才是「精确 = 距离 0」的目标，却永远轮不到。

**改法一：顺序改成精确优先**（`EvaluateEntryGuide`，DLL）

```
① 任务板引用自身        精确（距离 0；cell 加载时可用，且引擎会给它高亮）
② 新建常驻 XMarker      精确（设计上任意位置可用 —— 目前实测取不到，见探测）
③ 同 cell 原生常驻引用   大致位置（1.8~8.1 m；只用于「板所在 cell 还没加载」时给个方向）
```

**改法二：目标**动态更新**（`UpdateEntryGuideTarget`，菜单关着时每 1.5 s 一次）**

玩家「站在别处选 → 飞/走过去」是常见走法，只改顺序还不够（选的时候只能拿到兜底）。所以：

| 时刻 | 目标 | 日志 |
| --- | --- | --- |
| 在霓虹城选「任务板 · 陋室」 | 兜底 0x0011B754（板还没加载） | `引导请求：…→ 引用 0x0011B754（同 cell 常驻兜底（大致位置））` |
| 走进陋室（cell 加载） | **换成板自身 0x00137573**，蓝点随之上板 | `入口引导目标已更新：…0x0011B754 → 0x00137573（任务板自身（精确））` |
| 离开陋室（cell 卸载、板引用消失） | 换回兜底（蓝点回到大致位置，HUD 仍指着） | 同上，末尾带「（原目标已不可用）」 |

细节与安全阀：

* 只在**菜单关着**时复算（菜单开着时脚本本来就不响应，换目标没意义）；
* 静默更新**不碰界面**（uID 没变，界面上的「正在引导」无需变化）；
* 静默更新的确认失败（脚本没响应）时**不清通道、不回滚界面**（`verifySilent`）——
  这是我们自己发起的优化，不该把玩家原本可用的引导一起清掉（区别见 `AbortUnverifiedGuide`）；
* 不会「精确 → 兜底」地乱跳：当前目标还活着时只在能换到更精确的才换。

**改法三：marker 取不到的探针**（`LogEntryMarkerProbe`，每会话一行，只在有兜底条目时打）

```
入口 marker 探测：XMarker 基类(0x00003B)=有｜通道前缀=0x0F（GLOB 认领值，兼作 marker 序号）
｜marker 0x90A@0x0F=无｜该记录号在别的序号上=无｜对照 QUST@0x0F=有
```

* 基类无 ⇒ 记录会被引擎丢掉（XMarker 0x3B 不存在）；
* 别的序号命中 ⇒ 「通道前缀 ≠ 插件序号」（GLOB 和引用被放在不同序号空间）；
* 对照 QUST 有 + marker 无 ⇒ **序号映射没问题，问题在 CELL 组里的新引用没被引擎加载**
  （下一步排查方向：Starfield 对「插件往 master 的 cell 里新增引用」的处理，见 `docs/99` 一·补十六）。

**12 条任务板的兜底距离（离线数据，`ref/entry_persistent_scan.json`）**：

| 任务板 | 兜底引用 | 距板 | 兜底引用是什么 |
| --- | --- | --- | --- |
| 阿基拉城 | 0x0008C56D | 1.83 m | `FC02_MissionTerminalQTMarkerRef`（**官方自己的任务目标标记**） |
| 霓虹城 | 0x002BAB78 | 1.82 m | `FFNeonZ12_VoliiRef` |
| 星钥站 | 0x00315942 | 2.60 m | `CF08_ShinyaMarkerRef` |
| 海神叉造船厂 | 0x00205DE2 | 2.75 m | （无 EDID 的 XMarker） |
| 龙神集团 | 0x000C4078 | 3.19 m | `RI_PlayerOfficeRef` |
| 陋室 | 0x0011B754 | 3.41 m | `MQ204_NoelBasement_Marker01a`（← 13:15 截图的成因） |
| 赛多尼亚 | 0x0029F410 | 3.45 m | `CydoniaBarSandboxMarker` |
| 火卫二造船厂 | 0x003DE5A0 | 3.48 m | （无 EDID） |
| 新家园 | 0x0021A870 | 3.85 m | （无 EDID） |
| 斯特劳艾克伦 | 0x00001712 | 4.43 m | （无 EDID） |
| 新亚特兰蒂斯城 | 0x0002C14F | 7.29 m | `CrewBettyHowserSandboxMarker` |
| 霍普镇 | 0x001FA55F | 8.13 m | （无 EDID） |

（阿基拉城是唯一原生常驻的板 ⇒ 它没有 marker 记录，直接用板自身，无偏差。）

### ★ 第 32 轮：菜单关着时的例行认领 + 通道对账（重启/读档后不进菜单也自愈）

**起因（玩家截图「偏差仍然存在」）**：第 31 轮部署（13:26）后，玩家 13:30 / 13:32 两次
重启游戏、读档直接看 HUD —— DLL 日志里**没有任何「菜单打开」记录**（不进菜单），于是：

| 环节 | 状态 |
| --- | --- |
| 存档里（GLOB）的引导目标 | 还是上次会话（13:12）设的**兜底引用** `0x0011B754`（离板 3.41 m） |
| 脚本（SAQ_Main） | 从存档恢复：别名仍绑着它，世界里标记照常显示（就是那颗偏掉的蓝点） |
| DLL | `g_guide.questFormID == 0`（本会话从没设过引导）⇒ **第 31 轮的动态更新直接短路** |

⇒ 第 31 轮的「认领」此前只发生在**菜单打开**时（`OnMissionMenuOpened`）—— 玩家不进菜单，
认领/更新就永远不跑。修复（`SAQ.cpp::PollGuideUpkeep`，菜单关着时每 2 秒一次）：

| 场景 | 动作 | 日志 |
| --- | --- | --- |
| 没在引导 + 通道里有目标 | **认领**（`AdoptExistingGuide`；同一帧的动态更新接着把目标换成最精确的） | `认领已有引导（任务板入口）：…` → `入口引导目标已更新：…（任务板自身（精确））` |
| 在引导 + 通道里没有目标（读档到了没有引导的存档） | 重置本侧状态 | `引导状态对账：通道里已没有目标…` |
| 在引导 + 通道目标 ≠ 本侧记录（同会话读档 / 外部改动） | 清本侧，下一轮按通道重新配对 | `引导状态对账：通道目标 0x… ≠ 本侧 0x…` |

配套细节：

* 「认不出的通道目标」的 WARN **只对同一个目标引用打一次**（`adoptWarnRef`）——
  避免例行检查每 2 秒刷一行；
* 认领成功时重置 `lastTargetCheckMs` ⇒ 动态更新**同帧**执行（蓝点尽快上板）；
* 若本次会话脚本从未开跑（定时器未注册，见 `docs/05` 五节的菜单事件说明），静默更新的
  确认会走到「通道保持不动」的收尾 —— 目标留在通道里，玩家下次**关菜单**时由脚本应用。

### ★★ 第 80 轮：入口的第二形态 —— 「提供无限任务的 NPC」（8 条）

**需求（玩家 2026-09-21）**：AGENTS.md 原文「接取入口（如任务板、提供无限任务的 NPC）
作为一条数据」—— 任务板已在第 27 轮做过；本轮补 **NPC 形式**：**只有 NPC 条目**在名字前
加「（可重复）」提示（玩家明确「任务板不需要，只需要 NPC 这样做」）。

**两类 NPC 是谁 / 凭什么**（三向交叉验证）：

| 来源 | 内容 |
| --- | --- |
| 官方 Papyrus | 4 城对话里 `RAD03.Start()`（`QF_DialogueCydonia` / `FCAkilaCity` / `FCNeon` / `UCNewAtlantis`）；`QF_DialogueTrackersAllianceA` 里 `RAD04.Start()` |
| Wiki（starfieldwiki.net） | RAD03「The Long Haul」= 向 Trade Authority 商人询问工作 ⇒ 指向任务板（**可重复活动**，Given by 4 位商人）；RAD04 = 向 Trackers Alliance 探员询问 ⇒ 指向任务板（4 城） |
| ESM 数据 | 8 位 NPC 的 ACHR（`ref/repeatable_givers.json`；8/8 **非常驻** —— 与任务板当年一样） |

* **贸易管理局（RAD03）**：邓肯·林奇（阿基拉城）/ 科尔曼·朗（霓虹城）/ 卓伊·卡明斯基
  （新亚特兰蒂斯城）/ 希尔莎·包登（赛多尼亚）；
* **追踪者联盟（RAD04）**：4 城同名探员 ⇒ 列表名带地点后缀区分（「追踪者联盟探员 ·
  阿基拉城」）；显示名 = `（可重复）<组织> · <名字>`（数据侧拼好，见 SAQ_EntryTable.h）。

**引导目标候选链**（与任务板同机制；条目表 +kind 列，0=任务板 / 1=NPC）：

| 档 | 内容 | 说明 |
| --- | --- | --- |
| ① | NPC 引用自身（精确） | cell 加载时可用（蓝点跟着人走） |
| ② | 新建常驻 XMarker（0x90B~0x911） | 7 位**内景** NPC：与任务板同款建档（CELL 空壳 + CellPersistent） |
| ③ | 兜底候选 | 内景 = 同 cell 常驻引用（1.1~7.4 m）；**外景**（阿基拉城广场探员）= 该 worldspace 的**世界级**常驻引用（`WRLD > WorldChildren > CellChildren > CellPersistent`，3.1 m 处有 XMarker） |

★ 外景 cell 里官方**不放 per-cell 常驻引用**（CityAkilaCityCoePlaza01 实测 0 条）⇒ 阿基拉城
的探员不做 marker（没有组头可照抄），远处直接落世界级兜底。

★ **踩到并当场修掉的坑**：marker 记录的**签名**必须写 `REFR` —— 任务板时代模板就是 REFR
（没暴露）；NPC 模板是 **ACHR**，照抄签名会写出「base 指向 XMarker 的 ACHR」非法记录
（自校验当场抓出 —— 见 `create_board_markers.py::build_marker_record` 的注释）。

**协议 / 界面**：载荷 type 新增 **101**（`kNpcEntryQuestType`）—— 子项「与他交谈
（可重复任务）」；描述写明「这位 NPC 会不断提供可重复任务……」；「不可导航」提示与任务板
同路（`SaqIsEntryType` 合并判据）。SWF `stamp=58`，新探针 `rep=[8|0x214684=（可重复）
贸易管理局 · 邓肯·林奇]`（报告里报出条数 + 第一条名字 = C++ → 载荷 → AS3 全链的运行期证据）。

**日志**：`数据源：…；入口条目表=20 条（任务板 12 + 可重复 NPC 8）`；
`入口=20(可导航 N｜marker a 原板 b 兜底 c 不可用 d)`。数据工具：
`tools/esm/gen_repeatable_givers.py`（1~2 分钟全表扫）。

---

## 十、★ 第 36 轮：SET COURSE（键盘 R）对齐原版 —— 打开星图 / 展示目标星球

**玩家反馈**：可接任务里按 R 只有「选中条目」的效果；原版任务按 R 会**打开星图、
展示任务目标所在星球、询问是否导航**。

### 10.1 为什么不能直接照抄原版那一行

原版 `MissionMenu.OnPlotCourseEvent` 就是一行 dispatch：

```as3
BSUIDataManager.dispatchEvent(new CustomEvent("MissionMenu_PlotToLocation",
   {"questID":<任务FormID>, "objectiveID":-1}));
```

引擎按 questID 查**真实任务** → 取「已显示目标」的位置 → 打开星图 / 设航线。
我们的可接任务在引擎侧不存在（未接取，`IsRunning=false`，见 `docs/04`），
把它的 uID 丢进去只会静默失败 —— 这正是第 10 轮改成「自己引导」的原因。

### 10.2 修法：用**代理任务**当载体，走回原版流程

代理任务（`SAQ_MainQuest`）是**真实存在**的任务：它的目标别名（`SAQ_GuideTarget`）
此刻正绑着「接取地点」引用（脚本 `ForceRefTo`）、目标 10 已显示、任务在运行
—— 满足引擎「取目标位置 / 画标记」的全部条件。于是：

```
AS3 按 R（新设定引导）→ ① 我们自己的引导（原有链路，蓝点 + 路径线）
                      → ② dispatch 原版 MissionMenu_PlotToLocation{questID = 代理任务, objectiveID = -1}
                            ⇒ 引擎：取代理任务目标的位置 → 打开星图 / 聚焦星球 / 询问导航
```

（**取消**引导时不发 ② —— 那时玩家要的是「撤掉引导」。）

### 10.3 代理任务 FormID 怎么到界面（载荷 `P` 行）

FormID = `(插件加载前缀 << 24) | 0x800`，前缀**只有 DLL 知道**（`SAQ_Guide.cpp` 的 GLOB
认领，见本文四节）。所以载荷加一行（协议向后兼容：旧 AS3 忽略多余行）：

```
SAQ1
T\t可接任务\tAvailable
P\t184549376            ← ★ 第 36 轮（0 = 通道没认领 ⇒ 界面跳过原版流程）
Q\t…
```

* DLL：`SAQ_UI.cpp::ProxyQuestFormID()`（读 `Guide::EnsureChannel()`，未认领返回 0）；
  推送日志多一段 `代理任务=0x…`；
* AS3：`SaqParsePayload` 解析 `P` 行 → `SaqProxyQuestID`（每次解析先清 0，避免换源残留）；
  `SaqPlotToLocationViaEngine()` 负责 dispatch，成败都写进 `SaqGuideNote`
  （`SAQ_Report` 里能看到 `guide=…｜星图:已请求(代理任务 0x…)` 与 `proxy=0x…`）。

**降级姿态**：载荷没带 `P` 行（内嵌回退表 / 旧 DLL）⇒ 跳过原版流程，只保留自己的引导
（与第 10~35 轮完全一致），不报错。

### 10.4 实测判据（下次进游戏）

1. 日志：`载荷=… 代理任务=0x0F000800`（0x0F = 本机实测前缀）+ `proxy=0x0f000800`；
2. 「可接任务」里选一条按 R → **星图打开**并聚焦到接取地点所在星球（+导航询问），
   世界里同时出现蓝点（两条链并存）；
3. 若星图没打开：日志里 `｜星图:已请求(代理任务 0x0F000800)` 会证明**请求发了、引擎没响应**
   ⇒ 走下面的备选路线。

### 10.5 备选路线（离线已定位，备用 —— 本轮没用到）

| 手段 | 证据（都可复现） |
| --- | --- |
| `Game.ShowGalaxyStarMapMenu()` | **Papyrus 原生函数**（`Data\Scripts\Source\Base\Game.psc`），只负责打开星图 —— 脚本侧最省事的一条 |
| `StarMap::SetCourseToReferenceMsg` | RTTI `?$UIMessageData@VSetCourseToReferenceMsg@StarMap`；vtable `0x4C4AD00`（COL 在 −8）；**对象 16 字节**（析构里 `operator delete(ptr, 0x10)`）= {vptr, 引用指针}；构造点 `0x1368B3B`（`0x22C7320` 分配 0x10 → 写 vptr → 填引用） |
| `StarMap::SetCourseToQuestMsg` | 同上（vtable `0x4C80E20`；构造点 `0x15E0A76`；析构在 `0x15DFD10` 一带） |
| `StarMap::SetBodyFocusForQuestMsg` / `SetSystemFocusForQuestMsg` | RTTI 同族（`StarMap::` 命名空间）；字符串名表在 `.rdata` `0x4B40820` 起（`SetCourseToQuest` / `SetCourseToReference` / `SetSystemFocusForQuest` / `SetBodyFocusForQuest` / `PushJumpWindowInput` …，各被 23 处静态初始化器引用） |
| `FocusStarMapOnBodyMsg` | 独立 RTTI（无命名空间），RVA 可查 `IDs.h` |
| UI 消息总线 | `UIMessageQueue::AddMessage(menuName, type)`（commonlibsf `ID::UIMessageQueue::AddMessage` = 130659）；`StarMap::` 消息通过 `TUIEventDispatcher<…>` 分发 |

> 逆向工具用法（本轮新增的临时探针脚本都在 `ref/`，不入库）：
> `python tools/re/findrefs.py <rva>`（找 RIP 相对引用，vtable/字符串都行）、
> `python tools/re/rtti.py <关键词> --full`（RTTI 名）、
> `python tools/re/rtti_slots.py --name …`（TD → COL → vtable）。


---

## 十一~十四、第 37~40 轮：R（SET COURSE）星图机制的落地过程（已迁至 docs/96）

> 2026-09-20 第 46 轮：`docs/05` 触发 100 KB 上限（AGENTS.md 的单文件限制），把第 37~40 轮
> 的原样复盘（第十一~十四节）整体挪到 **`docs/96-历史轮次记录（第35~40轮）.md`** 的
> 「附：docs/05 第十一~十四节」一节（内容一字未改，只搬家）。仍然生效的结论：
> ① **星图唯一入口** = 脚本的轮询节拍（`StarMapPendingTicks` + `ProcessStarMapPending`），
>    AS3 那条原版 dispatch 已在第 44 轮删除（见第十八节）；
> ② 星图注册名 = `GalaxyStarMapMenu`、节点解析器特征字节见第十五节（第 41 轮修正）；
> ③ 地点候选 5/6/7 与自动重试的机制仍适用，但等待时间已在第 44 轮改为 1.5 秒
>    （与脚本的轮询节拍对齐）。

---

## 十五、★★ 第 41 轮：星图链路两个确定性 bug —— 注册名错 + 特征字节抄反
> 本节明细已迁 `docs/92-历史轮次记录（星图第41~42轮）.md`（逐字）；相关现役实现见第十六/十七节与 `docs/99` 速查。
## 十六、★★ 第 42 轮：R 键「导航目标不是我悬停的那条」—— 证据补齐 + 两条会改掉目的地的缺陷
> 本节明细已迁 `docs/92-历史轮次记录（星图第41~42轮）.md`（逐字）；相关现役实现见第十六/十七节与 `docs/99` 速查。
## 十七、★★ 第 43 轮：任务板条目「按 R 没反应」—— 现场分诊（探针）+ 两处会吞掉现场/请求的缺陷

**玩家反馈**：「我鼠标悬停在阿基拉城的任务板，R 导航却导向了纳里昂星，肯定不对」。

### 17.1 日志复查（17:54~17:57 会话，第 42 轮版本）

| 查什么 | 结论 |
| --- | --- |
| 整个日志里有没有 `press=[R@…]` | **一次都没有**（自第 42 轮 17:47 部署以来，`OnPlotCourseEvent` 从未留下过记录） |
| 该会话有没有 `引导请求：…` | **没有**（从 17:36 之后到会话结束，一条都没有） |
| 界面报告的 press/sel 演变 | 玩家在「平衡账目」「任务板 · 阿基拉城」上都做了**同一套动作**：<br>① 悬停主条目 → ② 按 E 展开 → ③ 悬停子项（`sel=[…@前往接取地点 / 前往任务板]`）→ ④ 菜单关闭 |
| 「菜单关闭：界面最后状态」里的 press | 停在 `E@…`（按键**之前**的值）——**不能**据此断定「按 R 没发生」（见 17.2 ①） |
| 入口可导航统计 | 每次都 `入口=12(可导航 12｜marker 10 原板 2 兜底 0 不可用 0)`、`阿基拉城` 走「原板（精确）」⇒ 数据侧没问题 |

⇒ **两种病一直分不开**（这正是第 42 轮补 press 想解决的问题，但被下面两个缺陷挡住了）：
**(a) 按键根本没进任务菜单**（上游 / 按键映射 / 事件被截）；**(b) 事件到了、回调没跑或提前返回**
（按钮被禁用 / `SaqToggleGuide` 走「无目标」分支 / 请求被关闭瞬间丢掉）。

### 17.2 顺带定位到的两个「吞现场 / 吞请求」的缺陷（已修）

**① `菜单关闭：界面最后状态` 用的是 500ms 轮询的缓存**（`g_poll.lastReport`），不是现场读取。
玩家按 R 与菜单关闭常常落在**同一个轮询周期**里 ⇒ 日志打印的是**按键之前**的缓存，
看起来像「按 R 什么都没发生」——本轮给关闭时刻加了一次**现场读取**（读不到再退回缓存）。

**② `PollGuideRequest()` 只在「菜单开着」那一支里跑**（`Tick` 的 `else if (open)`），
而 `OnMissionMenuClosed()` 紧接着把 `lastPeek/lastSeq` **重置** ⇒
**「按 R 后菜单立刻关掉」的那次请求会被直接丢掉**（界面侧第 38 轮的 `CloseMenu(true)`
是新协议、发生在回写之后；但玩家手动取消 / 关闭动画与下一次轮询交错时仍会漏）。
现在：关闭前**先补一次 poll**（`lastPollMs = 0` 强制读），处理到了就留一行
`菜单关闭：补处理了关闭瞬间的引导请求（序号 x → y）`。

### 17.3 本轮新增的分诊探针（AS3 → 报告 → 日志）

```
AS3  MissionMenu.as
  ProcessUserEvent（本菜单收到的每一个 user event 的入口）
      ⇒ SaqNoteUserEvent()：ev=[XButton↓ XButton↑ YButton↓ …]（相邻重复不记，最多 6 条）
  onMissionSelectionChange
      ⇒ SaqLastBtnNote：btn=[our:plot=1:map=0]
         our/std = 是不是我们的条目（bSaqAvailable 还在不在）；
         plot/map = SET COURSE / 显示在地图上 两个按钮此刻是启用(1)还是置灰(0)
  SAQ_Report ⇒ … press=[…] sel=[…] btn=[…] ev=[…]
```

判读（按 R 一次即可定性）：

| 日志形态 | 结论 |
| --- | --- |
| `ev=[…]` 里**没有** `XButton` | 按键根本没进任务菜单 ⇒ 病在上游（按键映射 / 输入焦点 / 别的菜单截走） |
| `ev=[XButton↓ XButton↑]` 但没有 `press=[R@…]` | 事件到了、回调没跑 ⇒ **按钮被禁用**（`HandleButtonHit` 里 `Enabled && bEnabled` 才调 funcCallback） |
| `press=[R@…]` 但没有 `引导请求` | 回调跑了但提前返回 ⇒ 看 `btn=[our:plot=?]` 与界面 note（`SaqToggleGuide` 的「暂无导航目标」分支不发请求） |
| `press=[R@0x…@…]` 且 `引导请求：…` | 链路通了；此后按第 42 轮的判据看星图那一段 |

### 17.4 顺手修掉一个「沉默失败」：SET COURSE 不再置灰

第 23 轮给「没有导航目标」的条目做了 SET COURSE 置灰（避免「亮起→瞬间回滚」）。
但**置灰是沉默失败**：玩家按 R 时既没有提示、也不留任何日志痕迹 —— 本轮排查
「按 R 没反应」时连「按键有没有到界面」都无从判断，就是它把现场抹掉了。
现在改回**可点**：没有导航目标时点击走 `SaqToggleGuide` 的「该任务暂无导航目标」分支
（界面提示 + OFF 音、**不发请求**）——也更符合需求「不可引导要提示玩家，而不是单纯无法选中」。

### 17.5 本轮改动一览

| 层 | 文件 | 改动 |
| --- | --- | --- |
| AS3 | `ui/missionmenu/src/MissionMenu.as`（`_lrg` 同步） | `SaqEventLog` + `SaqNoteUserEvent()`（`ProcessUserEvent` 入口记录）；`SaqLastBtnNote`（`onMissionSelectionChange` 末尾记录按钮状态）；`SAQ_Report` 加 `btn=` / `ev=`；`SaqIsOurEntry` 分支里的 `PlotToLocationButton.Enabled` 由 `bSaqHasTarget != false` 改为 `true` |
| DLL | `plugin/src/SAQ.cpp` | `OnMissionMenuClosed()`：① 现场读一次界面报告（`UI::ReadUiReport`，读不到退回缓存）；② `lastPollMs = 0` 后补一次 `PollGuideRequest()`，并记 `补处理了关闭瞬间的引导请求（序号 x → y）` |
| 工具 | `tools/ui/verify_saq_build.py` | 第 43 轮特征：SWF `ev=[` / `btn=[` / `SaqNoteUserEvent` / `SaqLastBtnNote`；DLL `补处理了关闭瞬间的引导请求` |
| 未动 | 脚本 / ESM / 星图链路 | 第 40~42 轮的实现保持原样（本轮只补证据与两处关闭时刻的缺陷） |

### 17.6 判据（下次进游戏，一次会话看完）

1. 可接任务 tab → 悬停「任务板 · 阿基拉城」→ 按 R ⇒ 日志里应出现
   `… btn=[our:plot=1:map=0] ev=[… XButton↓ XButton↑]`（`界面状态` 行 或 `菜单关闭：界面最后状态` 行）；
2. **成功路径**：`press=[R@0x14d497@任务板 · 阿基拉城]` → `引导请求：任务板 · 阿基拉城（0x0014D497）→ 引用 0x…（…）｜…｜星图=是｜press=…`；
   若出现 `菜单关闭：补处理了关闭瞬间的引导请求` ⇒ 17.2 ② 的修复起作用（这次请求本来会丢）；
3. **失败分诊**：按 17.3 的表对号入座 —— 把 `ev=` / `btn=` / `press=` 三段抄回来即可定位；
4. 回归：普通任务按 R 行为不变（星图/引导链路）；无目标条目点击给提示（不再置灰）；
   Enter 子项仍可切换/取消引导。

---

## 十八、★★ 第 44 轮：R 键「星图位置永远是上一个导航的位置」—— 第 36 轮那条 dispatch 其实生效（第 37 轮结论被推翻）

**玩家反馈**：「R 键打开的星图位置永远是『上一个导航』的位置：我点了阿基拉城任务板，
再去点火卫二任务板，那么导向目标才会是阿基拉城」。

### 18.1 复查方式与硬证据（18:34~18:35 会话，第 43 轮版本）

把 DLL 日志与 Papyrus 日志按时间对齐：

```
DLL     18:35:00.033  引导请求：任务板 · 阿基拉城（0x0014D497）… 置 5（星图请求）
DLL     18:35:00.042  星图：已打开（GalaxyStarMapMenu 在屏幕上，R 后约 0.0 秒，第 1 次尝试）
Papyrus 18:35:00      [SAQ] 引导已应用：FCMissionBoardREF (0014D497)
Papyrus 18:35:00      [SAQ] 星图请求：1.5 秒后打开（目标 …，地点候选=5）
Papyrus               ← 之后**没有**「打开星图并设定航线」这一行（三次按 R 全都没有）
```

| 观察 | 结论 |
| --- | --- |
| 星图在 DLL 写完通道后 **9 毫秒**就被探测到（且探测名已是第 41 轮修正过的 `GalaxyStarMapMenu`） | 打开它的**不是**脚本（脚本要等菜单关闭 + 1.5 秒延时，根本来不及）⇒ 只可能是 AS3 在按键那一刻**同步** dispatch 的那条原版事件 |
| Papyrus 三次「星图请求：1.5 秒后打开」之后**一次都没有**执行到 `OpenStarMapFor`（无「打开星图并设定航线」Trace） | 因为星图已经被打开了 ⇒ 星图是暂停菜单 ⇒ 游戏暂停 ⇒ 脚本定时器冻结 ⇒ 我们那条「用正确目标再开一次」的补救永远跑不到 |
| Papyrus 的 `引导已应用` 在 0.95 秒后被 DLL 确认为 `脚本状态=1` | 脚本本身是活的、轮询正常 —— 它只是「等不到定时器节拍」（冻结） |

⇒ **第 36 轮加的 `SaqPlotToLocationViaEngine()`（dispatch 原版 `MissionMenu_PlotToLocation`）
其实是生效的**：第 37 轮「引擎无 sink」的离线结论不成立 —— 这条 dispatch 经
`BSUIDataManager` 的 AS3 事件通道被处理，静态扫描「事件源单例的引用」看不到它。
它有两个致命点：

1. 引擎处理 `MissionMenu_PlotToLocation{questID}` 时取的是**那个任务当前的目标位置**。
   我们传的是**代理任务**（`SAQ_MainQuest`），而那一刻脚本还没 `ForceRefTo`
   （引导更新要等菜单关闭）⇒ 星图拿到的是**上一次**绑定的引用 ⇒ 位置永远滞后一条；
2. 它把星图**先**打开了 ⇒ 游戏暂停 ⇒ 脚本那条修正调用再也跑不到（上面第 2 行）。

这条机制同时解释了此前的三条反馈：
「所有任务都被导航至沃利阿尔法星」（别名还空着 ⇒ 引擎按默认焦点 = 玩家所在星球打开星图）、
「有一次打开了其他任务的位置」（别名 = 上一条引导的目标）、以及本轮的「上一个导航」。

### 18.2 修法（三层）

| 层 | 改动 |
| --- | --- |
| AS3 | **删除**那条 dispatch，换成 `SaqNoteStarMapHandoff()`（只播星图音效 + note `星图:交给脚本(菜单关闭后)`）；`SaqProxyQuestID` 保留（报告里的 `proxy=0x…` 诊断用） |
| Papyrus | 星图待办改由**轮询节拍**推进：新增 `StarMapPendingTicks`（`ApplyGuide` 装待办时置 1）+ `ProcessStarMapPending()`（每拍先减计数，减到 0 的那拍做过期校验后调 `OpenStarMapFor`）；`OnTimer` 不再按 id 分支（旧存档遗留的 id=2 定时器走同一路径）；`StarMapDelay/StarMapTimerID` 退役（保留声明，只为读旧存档不缺属性） |
| DLL | 重试等待 `kStarMapRetryAfterAppliedMs` 3 s → **1.5 s**（与脚本新窗口「1 个轮询节拍」对齐，仍不会抢跑）；`RequestStarMapOpen` 与「脚本已应用」两处日志写明星图的新入口 |

**为什么「轮询节拍」比「延时定时器」可靠**：轮询定时器只在**游戏运行时**走
（第 27 轮定案：菜单开着 = 暂停 = 定时器冻结）⇒「节拍到点」本身就等价于
「菜单关闭动画已过、游戏已恢复运行」——这正是引擎接受
`ShowGalaxyStarMapMenuAndPlotToLocation` 的时机，也不会被任何暂停吃掉。
代价是从「按 R」到星图出现约 0.5~1.5 秒（原版几乎是立即；这段等待来自
「先关暂停菜单 → 菜单关闭事件 → 下一个节拍」这条必经链路）。

### 18.3 验证（已做）

* `build-saq.ps1 -SkipTable`：ESM 幂等（marker 11 + CELL 空壳 11）+ Papyrus 1 succeeded
  + SWF 726239 / 726496 B + DLL 重编 + MO2 部署 ✓；
* `verify_saq_build.py` **全部通过**（第 44 轮新增：SWF `星图:交给脚本(菜单关闭后)`
  + 反向检查「旧 `星图:已请求(代理任务` 不应再出现」；PEX `StarMapPendingTicks` /
  `ProcessStarMapPending` / 「下一个轮询节拍」；DLL 两条新文案）。

### 18.4 判据（下次进游戏，一次会话看完）

1. **核心**：依次点两条不同的任务板、各按一次 R ⇒
   * Papyrus 每次都应出现 **`星图请求：下一个轮询节拍（约 0.5 秒）打开（目标 …）`**，
     紧接着 **`星图请求：打开星图并设定航线 → …（采用地点=…）`**（这一行此前**从未**出现过）；
   * DLL：`星图：已打开（GalaxyStarMapMenu 在屏幕上，R 后约 N 秒，第 1 次尝试）`，N ≈ 0.5~1.5；
   * **眼睛**：星图聚焦的是**刚才按 R 的那条**任务板所在星球（不再滞后一条）；
2. 失败形态：只有「下一个轮询节拍…打开」而没有「打开星图并设定航线」⇒
   看 Papyrus 有没有 `星图请求已过期`（玩家换了引导，属正常保护）；
   星图没开 ⇒ DLL 会在 1.5 秒后换地点候选自动重试（`星图：第 2 次尝试…`）；
3. 回归：Enter 子项只引导（不关菜单、不开星图）；同一条再按 R = 保持引导 + 再开星图；
   菜单里停 20 秒+ 无 `引导未生效`。

---

## 十九、第 45 轮（大项 A）：引导质量收口 —— 候选池 + 质量分级

### 19.1 背景（为什么单开一节）

需求回溯：AGENTS.md 的「能调用游戏的任务引导系统正常引导到接取任务的地点」。
第 10 轮起每条任务只有一个引导目标（离线挑一个「最优」），到第 44 轮为止留下两个缺口：

1. **非常驻目标取不到**：209 条有目标的里，76 条任务的唯一候选是 actor 型 NPC
   （非常驻）—— 玩家在远处（cell 未加载）点引导 = 脚本报状态 2 = 引导失效，
   只能等玩家靠近后由重发自愈（第 16 轮语义）；
2. **质量**：40 条任务的目标是引擎内部命名（`*EnableRef` / `*QSRef` / `*XMarker*` /
   空名）。实测反例：玩家被「群众心态」的蓝点引到 `FFNeonZ08_HeadlockEnableMarker001`、
   「平衡账目」被引到 `FFNeonZ09_EnableRef` —— 而这两条任务真正的发布者（有名字的
   NPC「Franchesca Moore」「黄（Huong Le）」）在数据里**存在**，只是被旧排序
   「常驻优先（persistent 第一键）」压住了（31 条任务是这个形态）。

### 19.2 数据侧：候选池（`tools/esm/gen_guide_targets.py`）

**质量分级（`cand_grade` / `quality_key`）**：

| 等级 | 形态 | 说明 |
| --- | --- | --- |
| 1 | 有名字的 NPC（actor、非通用名） | 「去哪里接」语义最强的答案（发布者本人） |
| 2 | 可读名的 ref 落脚点（如 `Trade Tower: Astral Lounge`） | 位置明确 |
| 3 | 通用名 NPC（Guard / Worker…） | 名字弱，但位置通常也对 |
| 4 | 内部名 / 空名 ref（`*EnableRef` / `*QSRef` / XMarker / 空） | 实测抱怨的那一类 |
| 5 | 其它（地点标记等，目前没有） | — |

同级内：`persistent` 优先 → 别名出现顺序。

★ 为什么 `persistent` 不再是第一键：实测反馈里最糟的形态是「有名字的 NPC 发布者被
内部标记压住」；而「取不到」从第 45 轮起由**候选链**兜底（见 19.3）——取舍从
「一定取得到（但位置差）」改为「优先位置准，取不到就自动换」。

**截断策略**：质量 top `MAX_CANDS`=6 + **保证最多再补 2 个常驻候选**。
（实测教训：直接截断前 6 个会把 24 条任务里排名靠后的常驻备胎砍掉 ——
而那是「玩家在远处」的唯一可用路径。）

**产物**：`ref/guide_targets.json` 每条任务新增 `cands` 数组（顶层字段 = 第一候选，
兼容 `ref_flags_report.py` 等旧消费方）。
实测统计：**875 条候选 / 209 条任务**（平均 4.2 个）；第一候选质量等级
1=204 / 2=3 / 4=2；质量分级把 **82 条**任务的第一候选换成了更好的目标
（含玩家实测抱怨的「平衡账目」「群众心态」两条）。

### 19.3 表与 DLL：运行时三件套 + 认领

**表**（`gen_quest_table.py` → `SAQ_QuestTable.h`）：新增
`StaticGuideCandidate kGuideCandidates[]`（`{refrLocal, refrMaster, flags(bit0=persistent), nameZh}`）
+ `StaticQuestInfo.candBegin/candCount`；旧的单目标字段 `guideRefLocal/guideRefMaster`
已移除（DLL 侧 11 处使用点全部改为候选池）。

**DLL 四件事（`SAQ.cpp`）**：

| # | 机制 | 触发 | 说明 |
| --- | --- | --- | --- |
| ① | `PickGuideCandidate` **预选** | 点引导时 | 从候选池取「此刻 `LookupByID` 命中的、质量最优的」候选 —— 与脚本 `Game.GetForm` 是**同一个引擎查询**（入口候选链第 31 轮已实测：非持久引用在 cell 未加载时这里也查不到）⇒ 玩家在远处**一次到位**落到常驻备胎，不必先撞一次失败 |
| ② | `SwitchGuideCandidate` **换候选链** | 脚本报状态 2 | 换下一个候选（循环），最多 2 次（`verifyResends`）；换不上时回到第 16 轮语义（重发等玩家靠近）。状态 3（脚本重挂）与候选无关，照旧重发 |
| ③ | `UpdateQuestGuideTarget` **动态复算** | 菜单关着，每 1.5 s | 与入口共用节流戳：飞近后 NPC 变得可得 ⇒ **升级**回 NPC；当前候选失效 ⇒ **回退**到可得候选（★ 第 49 轮补丁②：回退要先过 20 秒**降级观察期** —— 当前候选取不到时先保持通道不动，防读档/加载抖动误降级；升级仍立即执行）；静默写通道（失败不清通道、不回滚界面，同第 31 轮入口语义） |
| ④ | `AdoptExistingGuide` **认领** | 菜单关着例行 / 开菜单 | `FindQuestByGuideRef` 遍历候选池并**记住下标**（通道里的目标可能是第 2/3 个候选） |

日志形态（一眼可读）：

```
引导请求：平衡账目（0x0000351A）→ 引用 0x…（…）｜候选 [1/3]「黄」｜…        ← 预选结果
引导请求：… 的 3 个候选此刻都取不到（玩家离得远？）—— 先写第 1 个候选…      ← 兜底姿态
引导换候选：… 0x… → 0x…（[1]「黄」→ [2]「…」；脚本报取不到）｜…              ← 换候选
引导目标已更新（候选复算）：… 0x… → 0x…（[2]「…」→ [1]「黄」）｜…           ← 飞近后升级
引导状态：… 目标引用=0x…（候选 [1]「黄」/ 共 3）…                            ← 状态行带下标
```

### 19.4 验证（已做）

* `build-saq.ps1 -SkipTable`：ESM 幂等（marker 11 + CELL 空壳 11）+ Papyrus 1 succeeded
  + SWF 726239 / 726496 B + DLL 重编 + MO2 部署 ✓；
* `verify_saq_build.py` **全部通过** —— 第 45 轮新增：DLL 5 条（`引导换候选：` /
  `引导目标已更新（候选复算）` / `个候选此刻都取不到` / `候选 [` / `（候选 [`）+
  静态表 4 条（结构与数组 / 候选池完整性：**875 条候选、209 条有目标任务、切片越界 0**
  / 旧字段反向检查）。

### 19.5 判据（下次进游戏，一次会话看完）

1. **质量升级**（对照第 27/31 轮的抱怨）：在「可接任务」里点「平衡账目」或「群众心态」
   引导 ⇒ 日志 `引导请求：… ｜候选 [1/N]「黄 / Franchesca Moore」` ⇒ 蓝点落在那个
   **有名字的 NPC** 处（不再是 `FFNeonZ09_EnableRef` / `HeadlockEnableMarker001`）；
2. **预选的用处**：选一条**目标在别的城市**的任务点引导 ⇒ 若 NPC 的 cell 没加载，
   日志应直接写常驻候选（`候选 [2/4]「…」`）**一次到位**；只有「全不可得」时才会看到
   `… 个候选此刻都取不到`，随后最多 2 行 `引导换候选：…`；
3. **飞近后的自动升级**：远处点引导（落到常驻备胎）→ 飞近目标城市 ⇒ 菜单关着时
   应出现 `引导目标已更新（候选复算）：… [2]→[1]`（蓝点跟到 NPC 身上）；
4. **回归**：任务板入口链路不变（`入口=12(可导航 12…)`；`入口引导目标已更新` 照旧）；
   R 键星图链路不变；无目标条目点击仍只有界面提示；菜单里停 20 秒+ 无 `引导未生效`。

### 19.6 补丁（同日日志复查）：「朱诺的计谋」星图里没有目标位置

**现场**（任务 `MS03_Juno`，19:28 会话）：按 R 后星图**正常打开**（链路完整、1.5 秒），
但星图里没有目标位置/定位 —— 玩家反馈「有蓝点引导，但星图菜单里又没看到蓝点」。

**根因**（日志 + 数据双证，**不是链路 bug**）：这条任务的目标是 `MS03_ShipStart001`
（朱诺飞船**内部**的起点标记），地点 = `MS03JunoShip_CreatedInteriorLocationDungeon`
（引擎**动态创建**的内部地点）：

* Papyrus：`行星=None，行星地点=None，父地点候选=None` ⇒ 脚本的三个地点候选全部无效，
  落到候选 6（引用当前地点原样）；
* DLL：`星图诊断` 全层 `节点=0x00000000（未命中）`。

星图的定位机制是「地点 → 星图节点」，而**节点表只登记星系/星球层** ⇒ 引擎
`ShowGalaxyStarMapMenuAndPlotToLocation` 只能把星图按**默认焦点**（玩家当前位置）
打开 —— 玩家看到「星图里没有目标」。原版对这类位置同样表示不了（世界里没蓝点才是 bug，
而这条链路是好的：`引导已生效` + HUD 蓝点/路径线正常）。

**修法（「如实说明」，接 AGENTS.md 的「不可引导要提示玩家」）**：

| 层 | 改动 |
| --- | --- |
| 脚本 | `OpenStarMapFor`：`body == None`（地点链里**一个行星都没有**）⇒ **不打开星图**，改发 HUD 通知 `该目标不在星图上（飞船内/太空），请跟随任务标记 / Target not on the star map` + Trace |
| DLL | `LogStarMapDiagnosis` 统计「地点链全层节点=0」（三态 `NodeProbe`：`kUnknown/kMiss/kHit` —— 只有「显式未命中」才计入，解析器不可用等保持 `false`）⇒ `g_starMapDiagAllMiss`；`CheckStarMapOpened` 据此**不重试**（否则脚本重复跑、通知刷多条），超时文案从 WARN 改为 INFO「按预期未打开（脚本已改发 HUD 提示），不算失败」 |

**验证（已做）**：`build-saq.ps1 -SkipTable -SkipSwf`：Papyrus 1 succeeded + DLL 重编
（760832 B）+ MO2 部署 ✓；`verify_saq_build.py` 全部通过（DLL 2 条 + PEX 2 条新检查）。

**判据**：点「朱诺的计谋」按 R ⇒ **星图不打开** + HUD 通知「该目标不在星图上…」；
Papyrus `[SAQ] 星图请求：目标地点不在星图上…`；DLL `星图诊断…地点链全层节点=0`，
12 秒后 `星图：按预期未打开…`（INFO，不是 WARN）。其他任务（正常行星位置）行为不变。

**顺带查明**（「营救机器人」19:27 会话）：目标「G型」非常驻 + 候选池只有 1 个、玩家在
远处 ⇒ 19 秒重试窗口耗尽后 `引导未生效（清通道成功）` 并按设计回滚界面（第 16/19 轮
语义）。这类「只有非常驻候选」的任务（76 条中候选数为 1 的 **34 条**）在远处暂时无解
（数据里没有备用目标）——后续可选项：给这类任务的界面文案写明「靠近后可导航」。

**补丁 2（同日，玩家反馈）**：HUD 提示「消失太快」——引擎的单条 `Debug.Notification`
只停约 2 秒，Papyrus 没有延长参数 ⇒ 同一条文案**再发 2 次**（首条在 `OpenStarMapFor`
里发，其余由 `ProcessStarMapNotice` 在轮询节拍里补发，间隔 4 拍 ≈ 2 秒），总覆盖约
6 秒；再次按 R 会重新计数。日志：`不在星图上的提示已重复（剩余 N 次）`。
（★ 第 46 轮把这套机制通用化：`NoticeText` / `ShowNotice` / `ProcessNotice`。）

---

## 二十、第 46 轮（大项 B）：引导可用性 + 提示收口 —— 「有目标、但此刻取不到」

### 20.1 起因（实测证据，19:27 会话）

「营救机器人」：目标「G型」（`0x0308ECA5`，非常驻、候选池只有 1 个）+ 玩家在远处 ⇒

```
Papyrus 19:27:29 ~ 19:27:38   引导失败：FormID 50916517 取不到引用   × 5
DLL     19:27:28.906          引导请求：…的 1 个候选此刻都取不到（玩家离得远？）—— 先写第 1 个候选
DLL     19:27:47.796  [W]     引导未生效：… 已放弃本次引导（清通道成功）   ← 19 秒后
界面                          结果码 4 回滚 + OFF 音
```

玩家体感 = **「按了 R 没反应，过一会儿又自己取消了」** —— 与 AGENTS.md 的需求
（「对于不可引导的任务，要做到能提示玩家，而不是单纯无法选中，因为这样看上去像 bug」）
冲突。数据侧统计：**209 条有目标的任务里 76 条的全部候选都非常驻**
（`ref/guide_targets.json`，候选数为 1 的 34 条在远处完全没有可用目标）。

### 20.2 判据（离线可算，不需要新数据）

`StaticGuideCandidate.flags bit0 = persistent`（`gen_guide_targets.py` 的 `persistent`
= REFR flags `0x400`）⇒ **`AllCandidatesNonPersistent(info)`**：`candCount > 0` 且
每个候选都没有常驻位 = 「玩家在远处一定取不到，必须靠近目标区域（其 cell 加载）」。

### 20.3 三层实现

| 层 | 改动 |
| --- | --- |
| 数据/推送 | `QuestEntry.needsApproach`（`SAQ_UI.h`）+ 载荷**第 6 列**（`SAQ_UI.cpp`，旧载体/内嵌回退表缺列按 `"0"`）；DLL 侧运行时算（静态表自带持久位，无需重跑表生成） |
| AS3 | 解析第 6 列 → `bSaqNeedsApproach` → `SaqDescriptionText(..., param3)` 追加 `SaqApproachNote()`（中英各一句「导航目标要靠近目标区域后才加载…」）；`SAQ_GuideReply` 新增结果码 **5 = 目标尚未加载**：**保持竖条**、不回滚、不播 OFF 音、不关菜单（星图也不开） |
| DLL | 点引导时 `PickGuideCandidate` 报 `!anyAlive` ⇒ 置 `approachPending`：回写码 5、**不安排** 19 秒确认窗口、不开星图（`星图：本次不打开（目标尚未加载）`）；菜单关着时新增 `PollApproachRetry` **退避重试**（10/20/40/60 秒…，上限 `kApproachMaxTries=40` ≈ 35 分钟）→ 目标一加载脚本就应用（`引导延迟生效：… 共重试 N 次`）；停手后**引导保持**（开菜单会再试一次） |
| 脚本 | HUD 提示机制通用化（`NoticeText`/`ShowNotice`/`ProcessNotice`，接第 45 轮补丁 2 的「重复 3 次」）：目标取不到时发「目标地点尚未加载，暂时无法导航 —— 靠近后会自动生效」；失败 Trace 节流（前 3 次 + 每 10 次）；提示**冷却 5 分钟**（DLL 会自动重试，不能每次都弹） |

### 20.4 行为对照（玩家侧）

| 场景 | 旧行为 | 新行为 |
| --- | --- | --- |
| 远处点「营救机器人」（Enter / 子项） | 静默 19 秒 → 引导被清、界面回滚 | 列表描述**提前**写明「需靠近」；关菜单后 HUD 提示 ≈6 秒；引导保持；靠近后蓝点自动出现 |
| 远处按 R（设定航线） | 星图不开 + 19 秒后回滚 + WARN | 界面不关菜单、DLL 不打 WARN（`星图：本次不打开`），提示玩家靠近后重按一次 |
| 远处点有常驻候选的任务 | 第 45 轮：落到常驻备胎 | 不变（`anyAlive` ⇒ 走原路径，结果码 0） |

### 20.5 验证（已做）与待实测判据

已做：`build-saq.ps1 -SkipTable`（ESM 幂等 3521 B + Papyrus 1 succeeded + SWF
726531/726812 B + DLL **765440 B** + MO2 部署）✓；`verify_saq_build.py` **全部通过**
（新增 SWF 5 条 / DLL 6 条 / PEX 5 条，含反向检查「旧星图专用提示机制已替换」）。

待实测（一次会话看完）：

1. 选中「营救机器人」（或任何描述里带提示的任务）⇒ 右侧描述结尾应有
   「注意：它的导航目标要靠近目标区域后才加载 — 距离较远时按「设定航线」可能暂时看不到标记（靠近后会自动生效）。」；
2. 远处点它的「前往接取地点」或按 R ⇒ DLL `引导请求：… 的 1 个候选此刻都取不到…保持待生效` →
   `引导确认：目标尚未加载 —— 保持待生效（不做 19 秒超时判定）…`；**界面竖条保持**
   （无 OFF 音、无回滚；R 时菜单不关）；关菜单后 HUD 提示 3 次
   （Papyrus `HUD 提示：目标地点尚未加载…` + `HUD 提示已补发（剩余 N 次）`）；
3. 随后 DLL `引导重试（等待目标加载）：… 第 1 次 / 第 2 次 …`（10/20/40/60 秒退避）；
   再开菜单应看到 `引导状态：…｜待生效：目标尚未加载（已重试 N 次）`；
4. 走进/飞到目标区域（cell 加载）⇒ 下一次重试成功：DLL `引导延迟生效：…（共重试 N 次）`
   + Papyrus `引导已应用：…`，蓝点出现；
5. 回归：有常驻候选的任务（如「平衡账目」）点引导行为不变（结果码 0、无「未加载」提示）；
   R 星图链路不变；未加载任务的 HUD 提示不应刷屏（冷却 5 分钟）。

## 二十一、★★ 第 47 轮（大项 C）：同 cell 常驻兜底 —— 「点了没反应、标记锁死在别处」

### 21.1 起因（玩家实测「营救机器人」+ 两份日志取证）

玩家反馈原文：**「营救机器人这个任务导航点被锁定在亚特兰蒂斯城的同一个地铁上，
也就是说导航基本没用」**。

排查（20:14 会话 DLL 日志 + Papyrus 日志对齐）：

| 查什么 | 结论 |
| --- | --- |
| 玩家对「营救机器人」的操作 | 20:14:03~20:14:44 连点 30+ 次（E/R），**全部结果码 5 = 目标尚未加载** |
| 引导是否生效过 | **从未生效**（Papyrus 三次 `引导失败：FormID 50916517 取不到引用`；DLL 三次 `引导重试…脚本状态=2`） |
| 玩家当时所在 | 太空 → 20:16:52 着陆**新亚特兰蒂斯城太空港**（`NewAtlantisShipLandingMarker` 动画） |
| 世界里的标记是谁的 | 「取不到 ⇒ 脚本 `Return`（别名**保持上一次绑定**）」（`SAQ_Main.psc` ApplyGuide）⇒ 玩家看到的标记是**上一条生效引导的残留**（本次会话：20:14:30 的「所罗门的宝藏」= 阿基拉城；更早的会话因日志滚动已不可查） |

数据侧（本机 ESM 离线解析）：

* 「营救机器人」（`SFBGS022_MinibotRescue`，`0x0008EBDC` @ SFBGS050.esm）的唯一候选 =
  **损坏的机器人**（`ACHR 0x08ECA5`「G型」，**非常驻**）——位于 `CityNewAtlantisWell`
  （新亚特兰蒂斯城 The Well/老住区，NAT 站下方那片）的一条背街角落（周围是垃圾箱、
  涂鸦墙）。它**距「任务板 · 新亚特兰蒂斯城」的 marker 只有 6 米**、
  距 `CityNAWellSub17Marker` 4.5 米 —— 玩家口中的「亚特兰蒂斯城的地铁」指的就是这一片
  （The Well / Sub 区；玩家着陆太空港后能看到的旧导航标记也在这一片）。
* ★ **决定性发现**：机器人 **3.3 米**处有一个**常驻**引用 ——
  `REFR 0x0208ECA6`（base = `FURN SFBGS022_Destroyed_ModelG_FurnitureMarker`
  「损坏的 Model G 家具标记」）——正是这个任务自己的装置，**却没被用上**
  （旧候选生成只从任务别名的引用里选）。

⇒ 一句话：**这类任务（本机 76 条）在目标 cell 之外点引导时一个候选都取不到
=「什么都不发生」，而旧标记残留 ⇒ 玩家观感「导航点被锁死在某个位置、导航没用」。**

### 21.2 修法（数据为主，DLL 只改判定）

| 层 | 改动 |
| --- | --- |
| 数据 | `gen_guide_targets.py` 新增 `find_fallbacks()`（第三遍扫描）：对「全部候选都非常驻」的任务，在**候选目标所在的 cell** 里找**最近的常驻 REFR/ACHR**（排除候选自身；距离只在同 cell 内比较），作为**候选池最后一位**的兜底候选（`src=同 cell 常驻兜底（距目标 N 米）`，名字用 base EDID / `Ref_XXXXXX`，`nameq` 强制内部名质量 ⇒ 排序只在「内部名落脚点」一档、同档常驻优先）。本机 **76 条里 56 条**补到兜底 |
| 数据（副产品） | 候选里新增内部字段 `pos/cell`（第三遍算距离用；输出 JSON 时剥掉）；`scan_world` 的引用 info 带上 `pos`（DATA 前 12 字节） |
| 表 | `gen_quest_table.py` 无需改动（自动切片）——候选总数 **875 → 931**、常驻候选 283 → **339** |
| DLL | 「需要靠近」判据 `AllCandidatesNonPersistent` → **`FirstCandidateNonPersistent`**（只看**首选**候选）：兜底是常驻的，不能把这一类判没 —— 描述里的「需靠近」提示 + 测试模式 6 的 76 条清单照旧（它们描述的是**精确目标**要靠近才加载） |
| 未动 | SWF / Papyrus / ESM（文案「导航目标要靠近目标区域后才加载」仍然成立：远处拿到的是**近似兜底**，精确目标确实要靠近） |

### 21.3 行为对照（玩家侧）

| 场景 | 旧行为 | 新行为 |
| --- | --- | --- |
| 远处点「营救机器人」（有兜底） | 全候选取不到 ⇒ 界面提示「需靠近」+ 世界里**旧标记残留不动** | 预选直接命中兜底（常驻）⇒ **引导立即生效**，蓝点落在 The Well 的机器人附近（`候选 [2/2]「Ref_08ECA6」`） |
| 走进 The Well（cell 加载） | 等下一轮 10~60 秒退避重试 | 运行时「候选复算」把目标升级为精确机器人（`引导目标已更新（候选复算）：… [2]→[1]`） |
| 远处点**没有兜底**的任务（本机 20 条） | 第 46 轮链路（待生效 + HUD 提示 + 退避重试） | 不变 |
| 有常驻候选的任务 | 不变 | 不变（候选池尾部可能多一档兜底，预选仍先命中质量更高的常驻候选） |

### 21.4 验证（已做）与待实测判据

已做：数据管线全量重跑（`gen_guide_targets.py` 含第三遍扫描 + `gen_quest_table.py`）；
`build-saq.ps1 -SkipTable -SkipSwf -SkipPapyrus`（ESM 幂等 + **DLL 重编** + MO2 部署）✓；
`verify_saq_build.py` **全部通过**（新增 3 条：静态表常驻兜底已并入（931/339）、
营救机器人兜底链（`0x08ECA5` 非常驻 → `0x08ECA6` 常驻）、DLL 新判据文案 +
反向检查「旧『全是 非常驻』说法已替换」）。

待实测（一次会话看完）：

1. **核心**：在 The Well 之外（如太空港/太空）点「营救机器人」的引导 ⇒
   `引导请求：营救机器人…｜候选 [2/2]「Ref_08ECA6」`（或先 `[1/2]` 后换）+
   **引导立即生效**（结果码 0；关菜单后蓝点出现在新亚特兰蒂斯城 The Well 那片）；
   不再出现「什么都不发生 / 标记不动」；
2. 走进 The Well ⇒ `引导目标已更新（候选复算）：… [2]→[1]（0x0308ECA5）` ⇒ 蓝点落到
   机器人本体（背街角落）；
3. **回归**：没有兜底的 20 条仍走第 46 轮链路（`保持待生效` + HUD 提示 + 退避重试）；
   有常驻候选的任务行为不变；测试模式 6 的清单仍是 76 条（含「营救机器人」）；
4. 眼睛（如方便）：蓝点应落在 The Well 里机器人所在的那条小巷（垃圾箱/涂鸦墙旁），
   而不是「任务板」或「Sub17 marker」的精确点上（那两点在 4~6 米外）。
   ★ 已实测通过（20:41~20:47 会话）：远处点 ⇒ `候选 [2/2]「Ref_08ECA6」` + `引导已生效`；
   走进 The Well ⇒ `[2]→[1]` 升级；R ⇒ 星图 1.6 秒打开；接取后自动取消引导。

---

## 二十二、第 48 轮：判据回退（Mode=6 / needsApproach）+ 引导侧小修

> 本轮的主体（大项 D：INFO 门槛）在 **`docs/08` 第九节**；这里只记与引导相关的变化。

### 22.1 判据回退：「需要靠近」= **全部**候选都非常驻（20 条）

第 47 轮为把「营救机器人」留在清单里，把判据放宽成「**首选**候选非常驻」——
实测（20:41~20:47 会话）命中 158/209 条：`Mode=6` 推送 155 条、**76% 的任务都带**
「需靠近」提示。其中 138 条（首选非常驻 + 池里有常驻候选）远处点引导会**立即落到
常驻候选**、有即时反馈 —— 提前警告它们只会把真正「点了没反应」的 20 条淹没。

⇒ `FirstCandidateNonPersistent` → **`AllCandidatesNonPersistent`**（回退到第 46 轮语义）：

* **20 条**（无任何常驻候选）：远处点引导 = 待生效链路（结果码 5 + HUD 提示 + 退避重试）
  ⇒ 描述提示**必需**（`bSaqNeedsApproach`）、`Mode=6` 清单 = 这 20 条；
* **138 条**（有常驻候选/兜底）：远处点引导落到常驻候选（立即生效）、靠近后自动升级
  ⇒ 不提示（避免提示噪音化）。

### 22.2 读档窗口的复算抖动（20:42「补给之行」实测）

认领已有引导后**同毫秒**复算把 `[1]艾比·艾丁 → [3]常驻兜底`（读档瞬间 `LookupByID`
对没加载完的引用返回 null），10 秒后又升回 `[1]`；两条
`[W] 入口引导目标更新未被脚本确认`（文案对普通任务不准确、确认窗口落在读档 VM 冻结期）。

修法（第 48 轮）：

* 认领成功时记 `adoptGraceUntilMs = now + 8 秒`（`kAdoptGraceMs`），
  `UpdateQuestGuideTarget` / `UpdateEntryGuideTarget` 在宽限期内**不复算**；
* WARN 文案去「入口」二字，并注明「读档 / 脚本未运行期间出现这一行属预期」；
* ★ 第 49 轮补丁②（同日复查：宽限期仍不够 —— 读档加载实测 ≈19 秒）——**降级观察期**
  （`kDowngradeHoldMs = 20000`）：「当前候选取不到」时先**保持通道不动**，持续 20 秒
  仍取不到才降级；恢复可得立即清观察（记一行 `观察结束（未降级）`）；升级路径不变
  （立即执行）。入口条目同一套语义。详见一·补三十四第 7 节。

### 22.3 文案与标签小修（实机日志复查的尾巴）

* 星图等待：`还开着：无` → `其它打开的菜单：{}`（旧文案自相矛盾）；
* 界面：接取后状态栏 `当前引导:?` → 回退查玩家任务日志的名字（再兜底 FormID）；
  诊断标签 `0xNaN` → `「名字」(子项)`。

---

## 二十三、第 114 轮（引导质量 2.0）：两档常驻兜底（同 cell → world 级）

> 详细复盘见 **`docs/14-引导质量2.0（第114轮）.md`**（含「玩家位置因子」的**否定性判定**
> 与依据；20 条无兜底任务的真相 = 外景城市 cell 里官方不放 per-cell 常驻引用）。

一句话：`find_fallbacks` 从一档扩成**两档**（同 cell 常驻引用 → **world 级常驻引用**，
第 80 轮外景 NPC 条目的同一套口径），两档内都做「任务相关名优先」
（`UC_TualaTravelMaker` 这类引用只有在距离差 ≤30 米内才优先）。结果：候选池
1001 → **1020**，仍无兜底的任务 20 → **1**（DLC 一条），19 条任务的 `needsApproach`
由 1 → 0（远处点引导**直接生效**，不再走「待生效 + HUD 提示 + 退避重试」）。
运行时（DLL）零代码改动；SWF 仅因内嵌载荷的同名列变化重编（`stamp=65`）。
