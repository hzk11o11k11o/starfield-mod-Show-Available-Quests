

---

## 附录 B：docs/99 状态表沿革归档（第 157 轮瘦身 · 2026-09-24）

背景：AGENTS.md 规定 docs 单文件不超过 100KB；docs/99 状态表的 `MO2 部署` /
`游戏内实测` 两行堆了历代记录（约 34 KB）。第 157 轮把**旧轮次原文逐字迁移**
到这里，docs/99 原位只留一句指针（只剪切、不改写；表格行的位置列保留）。

### B1. `MO2 部署` 行 —— 第 152 轮及更早的原文（docs/99 保留最近三轮）

```text
★★★★★★★★★★★★★★★★★★★★★★★★★★★ **第 152 轮：0.1.18 打包 + 已还原常规部署**（发布构建 924672 B → 打包 0.1.18 → 重置：开发构建 **1212416 B** / SHA256 `FFBBC53D…E40697`（工作区 == MO2）；`Harness=0` / `AutoLoad` 清空 / `MaxSizeMB=10`；`verify --dev` 全过）** ★★★★★★★★★★★★★★★★★★★★★★★★★★★ **第 151 轮：排序日志版 DLL 已重建部署（常规开发构建；工作区 == 部署；`verify --dev` 全过；离线层 5 步全过；DLL 1212416 B / SHA256 `080AB7AF…1EE71E47`；待实机看两行顺序日志）** ★★★★★★★★★★★★★★★★★★★★★★★★★★ **第 150 轮：入口顺序修复的 DLL 已重建部署（常规开发构建；工作区 == 部署；`verify --dev` 全过；DLL 1207808 B / SHA256 `0A3D3160…F3415A95`；无实机会话）** ★★★★★★★★★★★★★★★★★★★★★★★★★ **第 149 轮：0.1.17 包完成 + 已还原常规部署**（开发构建 **1207808 B**（SHA256 `C3235C6F…C6795F1`，DLL/ESM/SWF/lrg/PEX 五件哈希 == 工作区）；`-SkipTable -SkipSwf -SkipPapyrus -Harness` + 手工还原 ini `Harness=0` / `AutoLoad` 清空；`verify --dev` **1060 OK / 0 MISS**）+ **第 148 轮：主计划 44 条复跑 44/44 全 PASS（测试态保留；DLL/ESM/SWF/lrg/PEX/Plan 六件哈希工作区==部署）**★★★★★★★★★★★★★★★★★★★★★★ **第 147 轮：新 DLL 已重建部署（测试态就绪，待复跑 44 条）**（DLL **1207808 B** / SHA256 `C3235C6F…C6795F1`（工作区 == 部署）；`-SkipTable -SkipSwf -Harness -AutoLoad Save1_…142854_2_0_4`；`SAQ_TestPlan.txt` 哈希一致（工作区 == 部署）；`verify --dev` **1063 行 / 0 MISS** + 离线层 5 步全过）★★★★★★★★★★★★★★★★★★★★★★ **第 146 轮：开发构建（harness）已部署 + 测试态就绪**（`-SkipTable -SkipSwf -Harness -AutoLoad Save1_…142854_2_0_4`；工作区 == 部署，DLL SHA256 `6A401996…F67DAF`；`verify --dev` **1061 行 / 0 MISS**；待实机主计划 **44 条**）★★★★★★★★★★★★★★★★★★★★★ **第 144 轮：P2 会话 6/6 全 PASS（P5 双形态回归底线达成）+ 已还原常规部署（开发构建 + SWF 覆盖拷回 + `*.p2off` 清理 + Plan 回主计划 + ini `Harness=0` / AutoLoad 清空；`verify --dev` 1049 行 / 0 MISS）** ★★ **第 143 轮：P5 结构指纹产物 —— 常规态 43 条已实机全绿 ⇒ 已按 P2 实验态重新部署（第 144 轮已收口还原）**（DLL 开发构建（harness）**1208320 B**（SHA256 `860D5392…`，工作区 == 部署；常规态 = `-SkipTable -SkipSwf -Harness`（SWF 覆盖在场 733254 / 733359 B + 主计划 + `verify --dev` **1049 行 / 0 MISS**）⇒ 现态 = `-SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`：SWF 覆盖 `*.p2off`（游戏加载原版）/ `Plan=SAQ_TestPlan_p2.txt`（含 `指纹=ok` 断言，工作区 == 部署）/ `[UI] UiMode=auto`；`verify --dev --p2` **868 行 / 0 MISS** + 离线层 5 步全过（决策 24 用例 / 1201 断言）；下一动作 = P2 会话 6 条 ⇒ 0.1.16 包）★★★★★★★★★★★★★★★★★★★★★ **第 141 轮：探针 `ctx.menu` 修复产物已按 P2 态重新部署（待复跑 6 条）**（DLL 开发构建（harness）**1206272 B**（SHA256 `B2FCFA4F…BCB376`，工作区 == 部署；`-SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`，SWF 覆盖 `*.p2off` / ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`（部署副本 = 眼睛窗口去除版）+ `[UI] UiMode=auto`；`verify --dev --p2` **860 行 / 0 MISS**（+1 条源码计数检查）+ 离线层 5 步全过（P2 正则 18/15）；恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`）★★★★★★★★★★★★★★★★★★★★ **第 139 轮：P2 会话重跑收口（5 条 5/5 全 PASS）—— 已实机验证；P2 实验态继续保留（P4 验证仍以原版 SWF 为主）**（DLL 开发构建（harness）**1161728 B**（工作区 == 部署；`-SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`，SWF 覆盖 `*.p2off` / ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`（副本含 `ui.mode` 5 处 = 探针 swf / 产品 auto）+ `[UI] UiMode=auto`；`verify --dev --p2` **829 行 / 0 MISS**（+2 harness 串 +1 产品串 +2 计划形状）+ 离线层 5 步全过（P2 正则 12/9）；恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`）★★★★★★★★★★★★★★★★★ **第 137 轮：P3-b 产物已按 P2 实验态部署（注入形态产品化 —— 已实机收口 = 第 138 轮 r137 PASS）**（DLL 开发构建（harness）**1160704 B**（工作区 == 部署；`-SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`，SWF 覆盖 `*.p2off` / ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`（副本 +1 条 `r137_product_inject`）+ 新增 `[UI] UiMode=auto`；`verify --dev --p2` **824 行 / 0 MISS**（+12 产品串双向 + 源码结构/分层 + ini 模板 [UI]）+ 离线层 5 步全过（P2 正则 12/9）；恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`）★★★★★★★★★★★★★★★★★ **第 136 轮：第 135 轮修复产物 P2 复跑收口（19:55~19:59 会话 4 条 4/4 全 PASS —— r133 五段全 ok + `上下文=ok`，P3-a 判据收口）**（DLL 开发构建（harness）**1142272 B**（工作区 == 部署，SHA256 `3FF24385…43723553`；判据五连全绿 = `check_results` 4/4 + `log_hygiene` 退出码 0（135 行 = 122 I + 13 W + 0 E）+ `plan_regex_audit` 10/10 + `verify --dev --p2` **800 行 / 0 MISS** + 离线层 5 步全过（P2 正则 9/6）；**眼睛达成（玩家确认第 8 tab「可接任务」+ 真实 206 条列表）⇒ P3-a 完全收口**；**下一步 = P3-b（P2 实验态继续保留）**）★★★★★★★★★★★★★★★★ **第 135 轮：第 134 轮修复产物 P2 复跑（19:25~19:29 会话 4 条 3 PASS / 1 FAIL —— r133 第二处真因 = 注入上下文未接线）⇒ 修复后已按 P2 态重新部署（复跑就绪）**（DLL 开发构建（harness）**1142272 B**（工作区 == 部署；`-SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`，SWF 覆盖 `*.p2off` / ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`；`verify --dev --p2` **800 行 / 0 MISS** + 离线层 5 步全过；恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`）★★★★★★★★★★★★★★★ **第 134 轮：第 133 轮产物已实机（4 条 3 PASS / 1 FAIL —— r133 真因 = 数据生命周期缺陷）⇒ 修复后已按 P2 态重新部署（复跑就绪）**（DLL 开发构建（harness）**1141248 B**（哈希 `0D281502…12DAD`，工作区 == 部署；`-SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`，SWF 覆盖 `*.p2off` / ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`；`verify --dev --p2` 全部通过 + 离线层 5 步全过；恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`）★★★★★★★★★★★★★★ **第 133 轮：P3-a 注入模块已按 P2 实验态部署（待实机 —— 4 条用例）**（`build-saq.ps1 -SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`：SWF 覆盖 `*.p2off` 禁用（游戏加载原版）/ ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`（副本 10981 B，含 `r133_inject_data`）/ DLL 开发构建（harness）**1141248 B**（+24064，含 SAQ_UiInject）；`verify --dev --p2` **797 行 / 0 MISS**；工作区 == 部署；**恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`**）★★★★★★★★★★★★★ **第 132 轮：P2 实验态已实机收口（3/3 全 PASS；已实机 = 18:41 会话）**（`build-saq.ps1 -SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`：SWF 覆盖仍 `*.p2off` 禁用 / ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt` / DLL 开发构建（harness）**1117184 B**（+1024）；`verify --dev --p2` **784 行 / 0 MISS**（+1 条新特征串「接线 UserEvents=」）；**恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`**）★★★★★★★★★★★ **第 130 轮：功能迁移探针 v4 已按 P2 实验态部署（**已实机 = 第 131 轮**）**（`build-saq.ps1 -SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`：SWF 覆盖 `*.p2off` 禁用（游戏加载原版）/ ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt` / DLL 开发构建（harness）**1116160 B**；`verify --dev --p2` **782 行 / 0 MISS**；**恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`**）★★★★★★★★★★ **第 128 轮：operator 二期产物已重建部署**（`build-saq.ps1 -SkipTable -SkipSwf`；DLL **1083392 B** / PEX 19133 B / ESM 4805 B —— 尺寸与第 127 轮一致（只改了 ini 文案与注释）；`verify --dev` **946 OK / 0 MISS**；本轮**无玩家可见变化**、无需实机会话）★★★★★★★★★ **第 127 轮：P2 已收口 + 已恢复常规测试态**（`build-saq.ps1 -SkipTable -SkipSwf -Harness`：SWF 覆盖拷回 + `*.p2off` 清理 + ini `Plan` 改回主计划；`verify --dev`（非 P2）全过；PEX **19133 B** / DLL 1083392 B / ESM 4805 B；P2 会话 2/2 PASS + 眼睛双达成 —— 见「游戏内实测」行）★★★★★★★★ **第 126 轮：已按 P2 实验部署（待实机 = P2 会话 2 条 + 眼睛）**（`build-saq.ps1 -SkipTable -SkipSwf -Harness -P2`：SWF 覆盖禁用 `*.p2off`（游戏加载原版）/ ini `Harness=1` + `AutoLoad=…142854_2_0_4` + `Plan=SAQ_TestPlan_p2.txt`；DLL 开发构建（harness）**1083392 B** / PEX **19026 B** / ESM **4805 B**；`verify --dev --p2` 全过（756 OK）；恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`）★★★★★★★ **第 125 轮：路线 D 产物已部署（常规测试态）⇒ 第 126 轮实机 43/43 全绿**（DLL 开发构建（harness）**1083392 B** / PEX **19026 B** / ESM **4805 B**（+GLOB 0x80E）；主用例计划 **43 条**（+`r125_ui_channel_ok`）/ P2 计划 **2 条**（+`r125_ui_conflict`）；ini `Harness=1` + `AutoLoad=…142854_2_0_4` + `Plan=SAQ_TestPlan.txt`；`verify --dev` 全过）★★★★★★ **第 124 轮：已恢复部署（P2 → 常规测试态）**（`build-saq.ps1 -SkipTable -SkipSwf -Harness` —— SWF 覆盖拷回 + `*.p2off` 清理 + ini `Plan` 改回主计划；`verify --dev`（非 P2）**全部通过**；DLL 开发构建（harness）**1079296 B**）★★★★★ **第 123 轮：P2 复跑 PASS 后重新部署（P2 实验态保留，供「眼睛会话」亲验）** —— 眼睛窗口 **8 → 30 秒**（`SAQ_TestPlan_p2.txt` 的 `wait 30000`，部署副本已同步、`verify --dev --p2` 全过）；其余不变（DLL 开发构建（harness）**1079296 B** / SWF 覆盖禁用 `*.p2off` / ini `Harness=1` + `AutoLoad=Save1_…142854_2_0_4` + `Plan=SAQ_TestPlan_p2.txt`）；恢复 = `build-saq.ps1 -SkipTable -SkipSwf -Harness` ★★★★ **第 122 轮：P2 首跑的修复版已重新部署（已复跑 PASS —— 见第 123 轮）** —— 修复 = 注入条目补 `aObjectives` + `bActive` / `iRemainingTime`（`SAQ_UI.cpp` `buildEntries`，见顶部第 122 轮块）；DLL 开发构建（harness）**1079296 B**（SHA256 `727903B7…9AAC10`，工作区 == 部署）；SWF 覆盖仍禁用（`*.p2off`）/ ini `Harness=1` + `AutoLoad=Save1_…142854_2_0_4` + `Plan=SAQ_TestPlan_p2.txt`；`verify --dev --p2` 全过（新增 `aObjectives` 检查项 OK）★★★ **第 121 轮：已按 P2 实验部署（「无 SWF 覆盖」形态 —— SWF 覆盖已改名禁用 `*.p2off`，游戏加载原版 missionmenu.swf；ini 测试态 `Harness=1` + `AutoLoad=Save1_…142854_2_0_4` + `Plan=SAQ_TestPlan_p2.txt`）**（DLL 开发构建（harness）**1078784 B**（含 `ui.research3` 探针 v3）+ P2 计划 **`SAQ_TestPlan_p2.txt`（`r120_gfx_poc`）** + 主计划 42 条 / 728 步；工作区 == 部署哈希一致；**恢复 = `build-saq.ps1 -SkipTable -SkipSwf -Harness`**（SWF 拷回 + `*.p2off` 清理 + Plan 改回）；verify 两模式全过（`--dev` / `--dev --p2`）；★ 首跑结果见顶部第 122 轮块）★ **第 119 轮产物已部署（含 `ui.research2` 探针 v2；测试态 —— ini `Harness=1` + `AutoLoad=Save1_…142854_2_0_4`）**（DLL 开发构建（harness）**1062400 B** + 用例文件 **42 条 / 728 步**（含 4 条 r114 待定任务 + 2 条 r117 研究探针 + 1 条 r119 探针 v2；工作区 == 部署哈希一致；★ 第 115 轮：sg02 用例传送目标已换 `~0:0x0013D006` 常驻门并重新部署）；ESM **4752 B** / SWF **733254 + 733359 B**（★ 第 114 轮**重编** —— 19 条任务 needsApproach 列变化 ⇒ `stamp=65`）/ PEX 17666 B；`stamp=65`、入口 **21 条**、**链式边 90 / 84 条有边任务**、任务表 **280 条**、可重复任务 **21 条**；部署 ini = 测试态（第 119 轮 `-Harness` 部署 —— 打 0.1.15 包时由打包流程还原 `Harness=0`））；verify 全过（含 `部署 == 工作区`）；★ **用例全 PASS 记录**：第 94 轮 26/26 + 第 97 轮 27/27 + 第 100 轮 29/29 + 第 105 轮 30/30 + 第 108 轮 32/32 + **第 113 轮 35/35**（10:52~11:00 会话 / 492609 ms / `驱动器 v70`；`[E]` 0 / `[W]` 40 全老现象；日志 967310 B）+ **第 115 轮 38/39**（12:30~12:40 会话 / 523703 ms；判据四连全绿；唯一 FAIL = sg02 传送目标非常驻 ⇒ 已换 `~0:0x0013D006` 常驻门 + verify 同步 + 部署）+ **第 116 轮 39/39 全 PASS**（13:11~13:20 会话 / 534718 ms；判据五连全绿；sg02 重跑收口 ⇒ 全量首次全绿）+ **第 118 轮 41/41 全 PASS**（14:13~14:23 会话 / 552406 ms / **41 用例 720 步**；判据五连全绿；含 2 条研究探针用例）+ **第 120 轮 42/42 全 PASS**（14:44~14:53 会话 / 550156 ms / **42 用例 728 步**；判据五连全绿 = `check_results`/`log_hygiene` 退出码 0 + `plan_regex_audit` **158/158** + `verify --dev` + 离线层 3/3；含探针 v2 用例）—— 第 111 轮 34/35 的唯一 FAIL 已收口；每轮打完后切回开发构建 + MO2 ini `Harness=0`
```

### B2. `游戏内实测` 行 —— 第 153 轮及更早的原文（docs/99 保留最近三轮）

```text
★ **第 153 轮：第 150/151 轮排序实机确认（常规态 3 次菜单周期：07:08/07:09 = v0.1.17 会话、07:18 = v0.1.18 会话 —— 两行顺序日志逐项吻合、无「顺序异常」；`log_hygiene` 退出码 0；r80 离线预演 2/2；明细 = 顶部第 153 轮块）** ★★★★★★★★★★★★★★★★★ **第 148 轮：主计划 44 条复跑 44/44 全 PASS（判据五连全绿；`r146_vkaiz02_fb` 转 PASS 2141 ms —— 产品行 `[5]`=可得 + 正/反断言双过；`[E]` 0 / `[W]` 49 全老现象；工作区==部署六件一致；明细 = 顶部块）**★★★★★★★★★★★★★★★★ **第 147 轮：主计划 44 条首跑 43 PASS / 1 FAIL**（06:12~06:22:32 / `sessionMs` 588500；唯一 FAIL = `r146_vkaiz02_fb` 的 `assert.log` —— **用例侧两处缺陷**（断言把候选 [1] 名字当任务名 + `guide.probe` 无产品行，红线六第二例），**产品零缺陷**（探针输出 `[5]「…LijanaStandMarker01」0x01114EC2=可得` 已正确）；`[E]` 0 / `[W]` 51 老现象；已修 + 重建部署 ⇒ **待复跑（预期 44/44）**；明细 = `docs/09` 十四·补三十七）★★★★★★★★★★★ **第 144 轮：P2 会话 6 条 6/6 全 PASS（P5 双形态回归底线全部达成 —— 常规态 43/43 + P2 态 6/6）**（22:06~22:08 / 101531 ms / P2 实验态 · 原版 SWF / PID 33976；r137 产品路径 `界面注入：已激活（…接管=ok，指纹=ok）` = 结构指纹自检实机通过 + watchdog 重放 0 次；`[E]` 0 / `[W]` 19 基线；判据 = `check_results` 6/6 + `log_hygiene` 0（236 行零坏字节）+ `plan_regex_audit` 19 断言 / 6 段 + `p2_plan_regex_check` 18/15 + `verify --dev --p2` + 离线层 5 步）★★★★★★★★★★★ **第 143 轮：常规态主计划 43 条 43/43 全 PASS（P5 回归底线达成）**（21:47:54~21:57:13 / 559266 ms / PID 30872 / DLL 1208320 B；判读 = SWF 形态（`推送成功 … 206 条` / stamp=65 / lang=zh）+ `界面注入` 0 行（auto 下 SWF 优先，符合设计）+ `推送放弃` 0 + `[E]` 0 / `[W]` 48（47 条「推送失败（第 1 次）」+ 1 条桥解析诊断 = 基线老现象）；判据 = `check_results` **43/43** + `log_hygiene` 退出码 0（日志 3062 行零坏字节；结果 JSON 1086 行干净）+ `plan_regex_audit` **160 条断言全命中** + `verify --dev` 1049 行 / 0 MISS + 离线层 5 步（决策 24 用例 / 1201 断言）；待 P2 态 6 条）★★★★★★★★★★ **第 141 轮：第 140 轮产物 P2 会话判读收口（6 条 5 PASS / 1 FAIL —— 唯一 FAIL = 探针 `ctx.menu` 未接线；对照产品路径 `接管=ok` 立判；已修 + 待复跑）**（21:11~21:14 / 236203 ms / P2 实验态 · 原版 SWF / 6 用例；`r120_gfx_poc` PASS 42266 ms（干净 7 tab + 五段全 ok + `注入=ok（1→3）`）/ `r125_ui_conflict` PASS 4797 ms（判定行 + 无「推送放弃」+ 同周期产品 auto 自动激活）/ `r130_gfx_migrate` PASS 33344 ms（4a 全绿 + 4b `刷新／文本／关菜单入口=ok`）/ `r133_inject_data` PASS 32328 ms（五段与第 136 轮逐字同型）/ **`r140_interact_takeover` FAIL 10171 ms**（`界面接管 Menu_mc=ok｜接管=fail（ButtonBar_mc 取不到）` —— 探针一行即止）/ `r137_product_inject` PASS 37313 ms（`已激活（…接管=ok）` + `菜单关闭（watchdog 重放 0 次）`）；判据 = `check_results` 5/6 + `log_hygiene` 退出码 0（251041 B / 663 行零坏字节）+ `plan_regex_audit` 19 断言 / 6 段（5 条未命中 = r140 FAIL 同源）+ `verify --dev --p2` 0 MISS + 离线层 5 步；**待复跑 = 6 条（预期 6/6）**）★★★★★★★★★ **第 139 轮：第 138 轮修复产物 P2 会话重跑收口 —— 5 条 5/5 全 PASS（P2 实验态首次全量全绿；判据五连全绿）+ 产品路径真实操作二次验证 ⇒ P3-b 完全收口（眼睛待玩家点头）**（20:36~20:39 / 224750 ms / P2 实验态 · 原版 SWF / 5 用例；`r120_gfx_poc` PASS 42407 ms（干净 7 tab + `扩tab=ok（7→8）` + `注入=ok（1→3）` —— `ui.mode swf` 隔离一击生效）/ `r125_ui_conflict` PASS 4953 ms（判定行 + 无「推送放弃」）/ `r130_gfx_migrate` PASS 33297 ms（探针 v4 全绿：`接管=ok` / `关菜单入口=ok` / `造对象=ok（三级 + 接线 UserEvents=1）`）/ `r133_inject_data` PASS 32062 ms（五段全 ok：`注入数据 1→206` / `对账 0x002C5401:超越极限` / `恢复 →1` / `再注入 →206`）/ **`r137_product_inject` PASS 37562 ms**（`UiMode=auto` → `已激活（tab 7→8，条目 206，语言=zh）` → `菜单关闭（watchdog 重放 0 次）`）；★ 两处修复验证 = 测试侧 `ui.mode` 5 处覆盖全生效 + 产品侧**零「激活失败」**（对照第 138 轮 96 条刷屏）；★★ **会话后玩家手动开菜单（20:39:24.923 auto 自动激活 → 20:39:25.804 切到第 8 个 tab 注入 206 条 ok → 20:39:46.973 关闭）—— 产品路径真实操作二次成立**；判据五连全绿 = `check_results` **5/5** + `log_hygiene` 退出码 0（日志 164796 B / 448 行；本次 PID 26376 = 179 I + 19 W + **0 E** —— W 全为原版 SWF 推送失败系列 6 菜单周期 + 1 条首周期桥解析诊断；结果 JSON 97 行）+ `plan_regex_audit`（P2 计划）**13/13** + `verify --dev --p2` 0 MISS + 离线层 **5 步全过**；产品侧与基线逐项一致（待推送 206 / 入口 21 / 门槛 7·80·68）；明细 = `docs/15` 十三·补五）★★★★★★★★ **第 138 轮：第 137 轮产物 P2 会话判读收口 —— 5 条 3 PASS / 2 FAIL（产品路径 r137 PASS；r120/r133 FAIL = 产品抢先激活撞探针）+ 两处修复（产品：激活失败不再重试；测试：`ui.mode` 隔离）—— 已实机收口 = 第 139 轮（5/5）**（20:20~20:24 / 187094 ms / P2 实验态 · 原版 SWF / 5 用例；r120 **FAIL** 10078 ms（探针五段全 ok，但产品已把 tab 扩到 8 ⇒ 探针扩 `8→9`、切 `切8`，断言硬编码 `切7` 等不到）/ r125 PASS 4828 ms（判定行 + `assert.nolog 推送放弃`）/ r130 PASS 33406 ms（U5~U10 全绿）/ r133 **FAIL** 17079 ms（`扩tab=fail（8→8）` 级联 —— 且产品重试刷 **96 条 WARN / 15 秒**）/ **r137 PASS** 37531 ms（`界面形态：UiMode=auto` → `界面注入：已激活（tab 7→8，条目 206，按键名=(空)，语言=zh）` → `菜单关闭：本轮为注入形态（watchdog 重放 0 次）`）⇒ **产品路径成立**；判据 = `check_results` 3/5 + `log_hygiene` 退出码 0（250 行零坏字节）+ `plan_regex_audit` 11/13（2 条未命中 = 与 FAIL 同源）+ `verify --dev --p2` 0 MISS + 离线层 5 步；**已重新部署待重跑（预期 5/5）**；明细 = `docs/15` 十三·补四）★★★★★★★ **第 137 轮：P3-b 产物待实机（P2 会话 5 条 —— 产品路径验收）**（判据 = `r137_product_inject` 三条断言（`界面形态：UiMode=auto` / `界面注入：已激活（UiMode=auto…）` / `菜单关闭：本轮为注入形态`）+ r120/r125/r130/r133 保持全 PASS；眼睛 = 第 8 个 tab「可接任务」+ 真实列表（产品路径）；观察 = watchdog 计数（应为 0 —— 除非引擎刷新））★★★★★★ **第 136 轮：第 135 轮修复产物 P2 复跑收口 —— 4 条 4/4 全 PASS（`InjectCtx::list` 接线一击生效，五段判据与预测逐字一致）；P3-a 判据收口**（19:55~19:59 / 184000 ms / P2 实验态 · 原版 SWF / 4 用例 35 步；`r120_gfx_poc` PASS 42406 ms（六段全 ok + 注入 1→3）/ `r125_ui_conflict` PASS 5000 ms（判定行 + `assert.nolog 推送放弃`）/ `r130_gfx_migrate` PASS 33390 ms（U5~U10 全绿）/ **`r133_inject_data` PASS** 32063 ms（`上下文=ok｜快照=ok（1 条）｜扩tab=ok（7→8）｜注入数据=ok（1→206，期望 206）｜对账=ok（0x002C5401:超越极限｜可导航1）｜恢复=ok（→1，期望 1）｜再注入=ok（→206，供眼睛）`）；★ 关键诊断 = `切到我们的 tab（idx=7）→ mask=0x40 + 注入 206 条（ok）` + `从我们的 tab 切走（idx=0）→ 恢复引擎列表（ok）`；判据五连全绿 = `check_results` **4/4** + `log_hygiene` 退出码 0（日志 59655 B / 135 行 = 122 I + 13 W + **0 E**）+ `plan_regex_audit` **10/10** + `verify --dev --p2` **800 行 / 0 MISS** + 离线层 **5 步全过**（P2 正则 9/6）；DLL **1142272 B**（工作区 == 部署）；**眼睛达成（玩家确认第 8 tab「可接任务」+ 真实任务列表）⇒ 判据 + 眼睛双达成，P3-a 完全收口**）★★★★★ **第 135 轮：第 134 轮修复产物 P2 复跑收口（4 条 3 PASS / 1 FAIL —— 第一处修复生效、第二处真因已修，待复跑）**（19:25~19:29 / 169828 ms / P2 实验态 · 原版 SWF / 4 用例；`r120_gfx_poc` **PASS** 42312 ms（六段全 ok + 注入 1→3）/ `r125_ui_conflict` **PASS** 4688 ms / `r130_gfx_migrate` **PASS** 33375 ms（U5~U10 全绿）/ **`r133_inject_data` FAIL** 17110 ms = 唯一 FAIL —— 第 134 轮的生命周期修复**一击生效**（`ui.inject` 拿到 206 条，不再「待推送=0」），但暴露**第二处产品级缺陷**：`InjectCtx::list` 未接线 ⇒ `切到我们的 tab（idx=7）→ mask=0x40（写入失败！） + 注入 206 条（fail）`、`对账=fail（0x002C5401:一小步｜可导航1）`、`恢复=ok` 为「1 == 1」巧合（假 PASS）—— **玩家实测所见 = 第 8 个 tab「可接任务」里显示的还是原版那条「一小步」**（列表没被换）；判据 = `check_results` 3/4 + `log_hygiene` 退出码 0（67874 B 零坏字节）；**修复（三处）+ 重建 DLL 1142272 B + 按 P2 态重新部署（待复跑）**）★★★★ **第 134 轮：第 133 轮产物 P2 会话实机检查收口（4 条 3 PASS / 1 FAIL）**（19:14~19:16 / 184282 ms / P2 实验态 · 原版 SWF / 4 用例；`r120_gfx_poc` **PASS** 42625 ms（六段全 ok + 注入 1→3）/ `r125_ui_conflict` **PASS** 4781 ms / `r130_gfx_migrate` **PASS** 33375 ms（U5~U10 全绿）/ **`r133_inject_data` FAIL** 17188 ms = 唯一 FAIL —— `ui.inject` 提前返回「待推送=0」，**真因 = 产品级缺陷**：`TryPushPending` notOurs 分支 `g_pending.quests.clear()`（notOurs 恰是注入形态要工作的场景 ⇒ 注入数据源被清空；证据链 19:15:41.099 待推送=206 → :41.950 判定 notOurs 清空 → :42.909 待推送=0）；判据 = `check_results` 3/4 + `log_hygiene` 退出码 0（57036 B / 130 行零坏字节）+ `plan_regex_audit` 8/10（未命中 2 条 = r133 两条断言，同源）；修复 = 数据生命周期解耦（生命周期 = 一次菜单打开周期；4 个推送子分支不再清数据）+ 重建部署；**待复跑** = 4 条全绿 + 眼睛（第 8 tab「可接任务」+ 真实 206 条列表）；判读表 = `docs/15` 十三节实机收口段）★★★ **第 133 轮：P3-a 注入模块待实机（P2 会话 4 条：r120 / r125 / r130 / r133_inject_data）**——判据 = `快照=ok.*扩tab=ok.*注入数据=ok.*对账=ok.*恢复=ok`（+`再注入=ok`）；★ 眼睛 = 第 8 个 tab「可接任务」+ 真实可接任务列表（真任务名/描述；切回原版 tab = 恢复）；判读表 = `docs/15` 十三节）★★★★ **第 132 轮：第 131 轮产物 P2 会话重跑收口（3/3 全 PASS —— P2 首次全量全绿）**（18:41~18:44 / 162422 ms / P2 实验态 · 原版 SWF · 开发 DLL 1117184 B / 3 用例 27 步；`r120_gfx_poc` PASS 42343 ms（六段全 ok + 注入 1→3）/ `r125_ui_conflict` PASS 4968 ms（判定行 + 无「推送放弃」）/ `r130_gfx_migrate` **PASS** 33328 ms（上轮唯一 FAIL 修复后通过）；判读 = **U5/U6/U7/U8/U9/U10 + 置灰全绿**（`接管=ok（回调收到 1 次——R 键可接管）` + `关菜单入口=ok（可调用=是；菜单仍在=是；返回 false）`；`读Data=fail` = 预期边界）⇒ **探针 v4 全绿 / P3 立项条件达成**；判据五连全绿 = `check_results` **3/3** + `log_hygiene` 退出码 0（日志 44939 B / 104 行 / 94 I / 10 W / 0 E 零坏字节）+ `plan_regex_audit` P2 计划 **8/8** + `verify --dev --p2` **784 行 / 0 MISS** + 离线层 **5 步全过**；产品侧与基线同型；**眼睛达成 = 玩家看到 2 条 `SAQ-Mig-0/1`**；判读表 = `docs/15` 十二·补二）★★★★ **第 131 轮：第 130 轮产物 P2 会话实机检查收口**（18:29~18:32 / 3 用例 **2 PASS / 1 FAIL**；`r120_gfx_poc` PASS 42313 ms（六段全 ok + 注入 1→3）/ `r125_ui_conflict` PASS 4750 ms / `r130_gfx_migrate` FAIL 13375 ms（唯一 FAIL = 4b `关菜单入口=ok` —— 探针判据错）；判读 = **U6/U7/U8/U10 + 置灰全绿**、**U5 接管 fail = 探针接线缺陷**（`ButtonBaseData` 的 param2 需传 UserEventData/Array）、**U9 = 判据错**（原版覆盖返回值）⇒ 两处均已修 + 重建部署待重跑；**眼睛达成** = 玩家看到 2 条 `SAQ-Mig-0/1`；判读表 = `docs/15` 十二·补）★★★★★★★★★★★ **第 127 轮：P2 态 2/2 PASS + 眼睛双达成（P2 + 路线 D 完全收口）**（会话 16:48~16:50 / 118828 ms / `Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖禁用 / 开发 DLL 1083392 B / PEX 19133 B；`r120_gfx_poc` PASS 42532 ms（六段全 ok + 注入 1→3）+ `r125_ui_conflict` PASS 4812 ms（第二个菜单周期独立判定行 16:50:15.809 + 无「推送放弃」）；★ **提示时机修复生效**：提示首发 16:50:03 = 关菜单瞬间（对照第 126 轮 16:25:02 在菜单里）+ 补发 06/09（共 3 次）；判据三连全绿（`check_results` 2/2 / `log_hygiene` 95 行零坏字节 / `plan_regex_audit` P2 计划 4/4）+ `verify --dev --p2` + 离线层 3/3；**眼睛双达成（玩家确认）**：① 第 8 个 tab「SAQ-PoC」+ 3 条 `SAQ-PoC-Item 0/1/2` 可见（显示在当前选中的第 1 个 tab 下 —— 探针末次回调 idx=0 后注入 = 设计行为）② 关菜单后 HUD 中英提示；已恢复常规部署）★★★★★★★★★★ **第 126 轮：43 条 43/43 全 PASS（首次 43 条全量全绿）**（会话 16:01~16:10 / 577703 ms / `驱动器 v70` / `stamp=65` / **43 用例 734 步**；判据五连全绿 = `check_results` + `log_hygiene` 退出码 0（日志 1106423 B / 2117 行零坏字节）+ `plan_regex_audit` **160/160** + `verify --dev` + 离线层 3/3；`[E]` 0 / `[W]` 48 全老现象；**`r125_ui_channel_ok` PASS = 防误报成立**（推送成功 + 零判定行）；产品侧与基线同型（入口 21/21、`需靠近/待生效/引导重试`=0）；待 P2 会话 = 2 条 + HUD 提示眼睛）★★★★★★★★★ **第 124 轮：P2 眼睛判据达成（P2 完全收口）**（会话 15:43~15:45 / `Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖禁用 / DLL 1079296 B；`r120_gfx_poc` PASS 32344 ms；**玩家截图（30 秒窗口内）= 眼睛证据**：tab 条 8 项（前 7 = 探针假数据 `$SAQ-keep0..6`、第 8 = `SAQ-PoC`）+ 列表 3 条 `SAQ-PoC-Item 0/1/2`；关菜单重开无残留；恢复部署后 `verify --dev` 全过）★★★★★★★★ **第 123 轮：P2 复跑 —— `r120_gfx_poc` PASS（六段全 ok + 注入段 `entryCount 1→3`）⇒★ P2 通过（「无 SWF 覆盖」形态技术成立）**（15:25~15:27 会话 / 82172 ms / `Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖禁用 / 开发 DLL 1079296 B；判据五连全绿 = `check_results` + `log_hygiene` 退出码 0（日志 39910 B / 89 行零坏字节）+ `plan_regex_audit` **2/2** + `verify --dev --p2` + 离线层 3/3；**眼睛窗口 = 探针执行后 ~8 秒**（本次未取得眼睛证据 —— 玩家截图为 `menu.close` 后重开状态 = 原版数据、无残留）⇒ 计划窗口已改 30 秒供亲验；判读见顶部第 123 轮块 / `docs/15` 九·补六）★★★★★★★ **第 122 轮：P2 首跑（`r120_gfx_poc` FAIL —— 五段 ok / 注入段 fail）**（15:15 会话 / 94953 ms / `Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖禁用 / `log_hygiene` 退出码 0（日志 2189614 B / 4204 行零坏字节）+ 离线层 3/3 + `verify --dev --p2` 全过；**真因 = 注入条目缺 `aObjectives`（原版 `IsMission` 判定）⇒ 已修 + 重建部署，待复跑**；判读见顶部第 122 轮块 / `docs/15` 九·补五）★★★★★★ **第 120 轮：42 条 42/42 全 PASS**（14:44~14:53 会话 / 550156 ms / `驱动器 v70` / `stamp=65`；判据五连全绿 = `check_results`+`log_hygiene` 退出码 0 + `plan_regex_audit` **158/158** + `verify --dev` + 离线层 3/3；`[E]` 0 / `[W]` 47 全老现象；日志 2162393 B；**探针 v2 判读 = 五段全 ok**（哨兵 `0x20000000` 存活 = 拦截 + 写双成立；对照 / 放行 / 清理 / U2 全成立）⇒ 下一步 = P2（原版 SWF 完整 PoC））；★★★★★ **第 118 轮：41 条 41/41 全 PASS**（14:13~14:23 会话 / 552406 ms / `驱动器 v70` / `stamp=65`；判据五连全绿 = `check_results`+`log_hygiene` 退出码 0 + `plan_regex_audit` **155/155** + `verify --dev` + 离线层；`[E]` 0 / `[W]` 46 全老现象；日志 1072874 B；**研究探针判读 = U0 ✅ / U1 ❌ 能力边界 / U2 ❓ 待补测 / U3 ✅** ⇒ 路线 A 不终止，下一轮探针 v2）；★★★★ **第 116 轮：39 条 39/39 全 PASS**（13:11~13:20 会话 / 534718 ms / `驱动器 v70` / `stamp=65`；判据五连全绿 = `check_results`+`log_hygiene` 退出码 0 + `plan_regex_audit` **150/150** + `verify --dev` + 离线层；`[E]` 0 / `[W]` 44 全老现象；日志 1039927 B；sg02 换常驻门重跑 ⇒ 传送成功（回执 3500 ms 落地）且任务未被启动 ⇒ **4 条待定任务全部定案「不收」**；眼睛 = **全部眼睛判据已完成（玩家确认）**）；★★★ **第 115 轮：39 条 38 PASS / 1 FAIL**（12:30~12:40 会话 / 523703 ms / `驱动器 v70` / `stamp=65`；判据四连全绿 = `check_results`+`log_hygiene` 退出码 0 + `plan_regex_audit` 150/150 + `verify --dev` 全过；`[E]` 0 / `[W]` 46 全老现象；日志 1049232 B；唯一 FAIL = sg02 传送目标非常驻（用例侧，已修待重跑）；4 条待定任务判读：3 条不收 + sg02 待重跑；眼睛 = 19 条 world 级兜底（第 116 轮确认））；★★ **第 113 轮：35 条 35/35 全 PASS**（10:52~11:00 会话 / 492609 ms；判据四连全绿；眼睛：赏金狩猎 / 总部板确认、星际盗贼 = 进度没到、总部引导蓝点（第 116 轮确认））；★ 11:08~11:18 手动会话（`Harness=0` 正常玩）：tab 切换 + 列表 206 条正常（总部引导未点）；★ 第 111 轮（09:48~09:56 会话）：35 条 34 PASS / 1 FAIL（r109 断言过窄，已修，第 113 轮收口）； ★ 更早会话（第 21~108 轮）= 已迁 `docs/93` 归档。
```

### B3. docs/99 顶部历史块 —— 第 151 / 150 轮原文（第 158 轮瘦身 · 2026-09-24）

背景：`docs/99` 超过 100 KB（AGENTS.md 限制）。把顶部第 151 / 150 两轮的速查块
**逐字迁入**这里（只剪切、不改写；`docs/99` 原位留一行指针）。

```text
> 上一轮（**第 151 轮：排序进日志** —— 玩家要求「让排序也可以在日志里体现」）：
> ① **背景**：第 150 轮修的「追踪者联盟任务板混进（可重复）堆」是**顺序问题**，而它
>   当时只能靠眼睛在界面里核（玩家截图）—— 日志里此前只有各段的**计数**与名单，
>   没有顺序本身；
> ② **实现**（`SAQ.cpp`：`OrderScan` / `FormatListOrder` / `FormatEntryOrder`；收集完成 =
>   排序之后打两行）：
>     ① `列表顺序：共 N 条｜分段[势力入口 4｜同伴 8｜其余 239｜可重复 21]｜头部4=<名字链>`
>        `｜入口段 21 条位于其余段末尾（连续）`；
>     ② `入口顺序：任务板 13[任务板 · 新亚特兰蒂斯城 → … → 任务板 · 追踪者联盟总部]`
>        `｜可重复 NPC 8[（可重复）贸易管理局 · 邓肯·林奇 → … → … · 新亚特兰蒂斯城]`；
>     顺序异常（入口段不连续 / 不在其余段末尾 / 头部不是势力入口）⇒ 行内标
>     「**顺序异常**」+ 一条 WARN —— 第 150 轮那类回归从此挡在日志里，不靠眼睛；
> ③ **判据落点**：verify 两条产品特征（`列表顺序：` / `入口顺序：`）+ 用例计划 r80
>   两条断言（子串不含数字与括号 —— 绕开「表条数变化 / 全角括号」两类假 FAIL）；
>   `plan_regex_audit` **165 断言 / 2 条未命中**（= 新增那两条：历史日志里还没有新行，
>   待实机一次会话即转绿 —— 不是正则问题）；反向断言无冲突（新行不含 `INFO没到:` /
>   `链式没到:` / `进度没到:` 前缀，也不含任何断言里的裸名字，逐条核过）；
> ④ **验证**：`verify --dev` 全过 + 离线层 5 步全过 + 黄金快照 17 件全 OK；DLL
>   **1212416 B**（SHA256 `080AB7AF…1EE71E47`，开发构建（含 harness））+ 工作区 == 部署；
> ⑤ **待实机**：下次进游戏开一次任务菜单，日志里就能看到这两行（r80 两条断言随之转绿）。
>
> 上一轮（第 150 轮：入口条目顺序 —— 追踪者联盟任务板与其它任务板归位）：
> ① **玩家反馈**（附图）：`MISSION BOARD - TRACKERS ALLIANCE HQ` 在列表里夹在 4 条
>   `(REPEATABLE) TRACKERS ALLIANCE AGENT - <城市>` 与 `(REPEATABLE) ONE RIOT, ONE RANGER`
>   之间 —— 要求「把他和其他任务板排序在一起」；
> ② **根因** = 第 110 轮的手工条目（`EXTRA_ENTRIES`）在生成时被追加到 `kEntryTable`
>   **表末尾**（8 条可重复 NPC 段之后）—— 而列表顺序 = 表顺序（DLL 先按表追加、
>   再按组稳定排序 ⇒ 组内保持表顺序，第 96 轮机制）⇒ 那块任务板掉进了「（可重复）…」堆；
> ③ **修复**（`tools/esm/gen_entry_table.py`）：`EXTRA_ENTRIES` 追加位置移到**任务板段末尾**
>   （12 条基础任务板之后、可重复 NPC 段之前）+ 新增**布局硬校验**（同类入口必须连成
>   一段；board 出现在可重复 NPC 段之后 ⇒ 构建直接失败 —— 防复发）；
> ④ **产物**：`SAQ_EntryTable.h` 顺序 = 13 张任务板（新亚特兰蒂斯城 → … → 星钥站 →
>   追踪者联盟总部）+ 8 条可重复 NPC；`ref/entry_targets.json` 同步；黄金快照 2 处
>   有意变化（其余 15 件 OK）；DLL **1207808 B**（SHA256 `0A3D3160…F3415A95`，
>   开发构建（含 harness））；`verify --dev` 全过 + 工作区 == 部署；
> ⑤ 本轮**无实机会话** —— 排序变化进游戏目视即可（界面 `order=` 探针的 `|tail=` 段
>   同步可取证）；★ 0.1.17 包内 DLL 仍是修复前的顺序（若要发布本修复 ⇒ 需另打包）。
>   ★★ 玩家决策（本轮收尾）：**暂不打包** —— 先进游戏确认排序效果，发布（0.1.18 或重打
>   0.1.17）等确认后再定；确认要点 = 「可接任务」tab 滚到任务板区：13 块任务板连成一片、
>   末尾一块是「任务板 · 追踪者联盟总部」，其后才是 8 条「（可重复）…」条目。
>
```

### B4. docs/99 顶部历史块 —— 第 156 轮原文（第 159 轮补瘦身 · 2026-09-25）

背景：`docs/99` 逼近 100 KB（AGENTS.md 限制）。把顶部第 156 轮的速查块**逐字迁入**
这里（只剪切、不改写；`docs/99` 原位留一行指针）。

```text
> 上一轮（**第 156 轮：可招募船员入口段落地 + 模拟招募正样本复核
> （玩家决策：两个都做；检查结果 = 42 PASS / 3 FAIL —— 已由第 157 轮收口，见上）**）：
> ① **入口段（kind=2「可招募船员」）**：`gen_entry_table.py` 合并 `ref/hirable_crew.json`
>  ⇒ 入口表 **21 → 45 条**（任务板 13 + 可重复 NPC 8 + 可招募船员 24）；段序固定
>  「任务板 → 可重复 NPC → 可招募船员」（布局硬校验：段序下标单调不减）；
>  **不新建 marker**（第 154 轮结论）；3 位位置不适合导航（贝蒂 / 埃里克 / 莫亚拉）
>  **丢弃兜底** ⇒ 平时不可导航（界面「（不可导航）」前缀 + 船员专用提示，不是 bug）；
>  名字 `（可招募）<名字>` 数据侧加好；
> ② **产品判据**（「显示 = P=1 / 隐藏 = A=1」）：新模块 **`SAQ_Crew`** —— DLL 直读
>  `Actor::IsInFaction`（commonlibsf vtable 175 条；本项目首个 Actor 虚调用，
>  正确性由同会话探针双来源对照钉死）；引用可读才判：**P=0（未解锁）/ A=1（已招募）
>  ⇒ 隐藏**，读不到 ⇒ 保守放行；ini `[Filter] CrewCond`（默认 1；0 = 只写日志）可关；
>  日志 `船员门槛=N(过A/藏B+C/未知K` + `船员隐藏: 名字[0x… 原因]` 名单 +
>  顺序行②新增 `｜可招募船员 N[…]` 段；
> ③ **界面**（SWF 重编 **stamp=66**）：type 102 —— 子项「招募他作为船员」+ 描述分支
>  （含「不可导航」的船员专用措辞 —— 不说「稍后重试」）+ 新 `crew=` 探针（SaqCrewProbe）；
> ④ **正样本复核**（Papyrus **op=8** + `crew.probe sim=`，驱动器 **v72**）：模拟招募往返
>  （Add → 读 → Remove → 读；前置 A=1 跳过写入；净副作用为零）+ 逐位**直读对照**
>  （`｜对照=一致/不一致`）+ 汇总行 +`｜直读读到 N 位` +`｜模拟招募：<判定>`；
>  r155 用例升级（`teleport.entry 0x00015062` → `crew.probe sim=0x00015062` → 断言
>  「A 位随招募往返 0→1→0（A 判据成立）」+ 五条反向断言）；**断言离线预演通过**
>  （正常 7/7 命中 / 异常 5 条反向全捕获）；
> ⑤ **构建 / 验证**：DLL **1236480 B**（开发构建）+ SWF **733596 / 733707 B**（stamp=66）
>  + PEX **21568 B**；已部署（测试态 `Harness=1` / `Plan=SAQ_TestPlan.txt` /
>  `AutoLoad=Save1_…142854_2_0_4` / `MaxSizeMB=10` + 补 ini `[Filter] CrewCond=1`）；
>  `verify --dev` **1130 OK / 0 MISS**（工作区 == 部署）+ 离线层 5 步全过
>  （黄金快照 19 件：2 处有意变化 = 入口 21 → 45）；明细 = `docs/16` 16.8；
> ⑥ ★★ **待实机**：主计划 **45 条**（含升级后的 r155）—— 判读要点 = `船员门槛=` /
>  `船员隐藏:` 名单 / 模拟招募判定行（**A 判据是否成立**）/ `crew=` 探针（过滤后条数）/
>  界面「（可招募）…」段 + 「（不可导航）（可招募）…」3 位（**启动游戏由玩家操作**）。
```



### B5. docs/99 顶部历史块 —— 第 159 / 158 轮原文（第 160 轮瘦身 · 2026-09-25）

背景：同 B4（`docs/99` 逼近 100 KB，AGENTS.md 限制）。把顶部第 159 / 158 轮的速查块
**逐字迁入**这里（只剪切、不改写；`docs/99` 原位留一行指针）。

```text
> 上一轮（**第 159 轮：第 158 轮增量跑检查收口 —— r155 唯一 FAIL =
> 用例**窗口**缺陷（产品全对）+ 判读工具升级**（明细 = `docs/09` 十五·5）**）：
> ① **检查结果**（22:57:21~23:01:33 会话 / `sessionMs` 251625 / 增量 4 条）：`check_results`
>   **3 PASS / 1 FAIL**（`smoke` ✓ / `r47_board_marker` ✓ / `r80_repeatable_npc` ✓ /
>   `r155_crew_probe` ✗）—— 前三条 = 第 157 轮的断言更新（r47 结构口径 / r80 45 条）
>   实机转绿；增量机制本身也对（日志 `Only 增量筛选「smoke,r47,r80,r155」⇒ 本次只跑
>   4/45 条` + 结果 JSON `only` 字段 + `plan_regex_audit` 增量感知提示行）；通道健康 =
>   `log_hygiene` 退出码 0（日志 108755 B / 296 行零坏字节 + 结果 JSON 104 行干净）；
> ② **r155 FAIL 真因 = 用例侧「窗口」缺陷（产品全对）**：第二条断言
>   `汇总：.*对照 \d+ 位 / 不一致 0 位` 用了 `scope=prev` —— 而 `scope=prev` 的窗口
>   **只有下界**（= 上一步开始时的打点），第二条的下界是「上一条断言行」⇒ 探针行全在
>   窗口外 ⇒ 白等 120 s（第 157 轮改断言时埋下；当时离线预演是「整段匹配」、不建模窗口）。
>   **产品侧证据全绿**：`船员直读：槽 0x174 指纹=ok` / 逐位 `对照=一致` ×13 /
>   汇总 `对照 13 位 / 不一致 0 位`（**第 157 轮直读槽修正实机成立**）/
>   `模拟招募…判定：AddToAvailableCrew 未改变 A 位（A 判据不成立）` /
>   `船员门槛=12(过4/藏0+8/未知12)` + `船员隐藏:` 8 位 + 界面 `crew=16` 条；
> ③ **修复（两处 scope）**：`对照` 与 `模拟招募` 断言改 `scope=case`（`prev` 只留给紧跟
>   crew.probe 的那条）；计划内加注释说明窗口规则；`verify` +3（三条 scope 形状）+1 反向
>   （对照不许 `prev`）；
> ④ **判读工具升级**（`plan_regex_audit.py`）：新增 **窗口自检**（照驱动器语义重建窗口 ——
>   含「只有下界」「`note` 先吃掉窗口」「`assert.ui` 读界面实时报告、不走日志窗口」三处
>   对齐）+ **`--self-test`**（合成日志钉住语义，进离线层第 ⑤ 步）+ `--plan-only`
>   （静态窗口体检：列出 `prev` 且上一步是 note/assert 的写法，提示级）；用**本次会话日志**
>   离线预演：修复后 **0 问题**（含「未跑到」的 3 条按 `case` 下界预演 = 重跑预期命中）；
> ⑤ **构建 / 验证**：DLL **1245184 B**（SHA256 `C41DEB33…2D82DD`，工作区 == 部署；
>   Papyrus / SWF / ESM 未变）+ `verify --dev` **1155 行 / 0 MISS** + 离线层 5 步全过；
>   部署态 = 测试态（`Harness=1` / **`Only=r155`** / `Plan=SAQ_TestPlan.txt` /
>   `AutoLoad=Save1_…142854_2_0_4` / `MaxSizeMB=10`）；
> ⑥ ★★ **待实机**（**启动游戏由玩家操作**）：**只跑 r155**（≈2~3 分钟）—— 判读 = 结果 JSON
>   `r155_crew_probe` PASS + `check_results` 的「增量筛选」提示行 + 结尾 5 条反向断言全过；
>   之后主计划 **45 条全量**留到收口 / 打包前（不带 `-Only` 重建 = 自动清空）。
>
> 上一轮（**第 158 轮：增量测试机制落地 + 首次增量实机**（明细 = `docs/09` 十五节）：
> ① DLL 驱动器 **v73**（ini `[Test] Only` 子串筛选；空 = 全量；没命中 ⇒ 不跑 + WARN）+
> `build-saq.ps1 -Only`（不传 = 清空 ⇒ 默认安全方向）+ 判读工具增量感知；
> ② 用法 / 「修改 → 相关用例」映射表 / 纪律（打 Nexus 包前必须全量）= `docs/09` 十五·2~4；
> ③ 部署为增量模式跑 4 条 ⇒ 检查结果见顶部第 159 轮块；实现时踩的 PS 数组坑已修）。
```
