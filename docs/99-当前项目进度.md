# 99 · 当前项目进度

> 最新更新：2026-09-23（**第 145 轮：A3 operator 三期 —— cmp∉{0,1} 折叠产品化 + type
> 低字节解析缺陷修复 + tripwire 三类报红；全量普查确认「外键 + flags 非 OR」表内外 0 条；
> **产物零变化**（8 个数据产物重扫前后哈希逐字节一致、运行时零改动）+ A1「测试后台化」
> 选题池移除（玩家决定：启动游戏由玩家亲自操作）**）：
> ① **数据普查**（新工具 `_tmp_r145_operator3_census.py`，5 master）：INFO 侧 17974 条三函数
>   条件里特殊形态只有 7 条「别名位 + 自引用」（表内 0 条）；记录级表内唯一特殊样本 =
>   SFTA00 的 `GetStageDone(自己,1510)==1510`（medium 档 0xFD 前缀 = 自引用）⇒ 与第 128 轮
>   一致：**没有外键对象**（硬做运行期通道的理由不成立）；
> ② **顺带修真实缺陷**：type 只该读**低字节** —— +0 是 4 字节（低字节 type + 3 字节 unused），
>   stage 条件的 unused 非零（实测 300 条「运算符越界」全是它们）⇒ 第 87/106 轮的「读 u32」
>   统计失真、且记录级一旦带非零 unused 就会漏收；三处解析点全部改回低字节（docs/08 4.8 二节）；
> ③ **落地**：`ctda_ops.fold_operator` 改通用实现（**任意数值常量 cmp** 精确折叠 + NaN 保护；
>   自检 12+2+11+5+10 例）→ `analyze_ctda.py` / `scan_info_gates.py` 放开 cmp（GLOB bit2 单独
>   放行 —— 需运行期读 `TESGlobal`，0 条对象 ⇒ 不实现）；tripwire 升级三类报红（独立条件
>   「应收未收」/ 表内 flags 非 OR 四类细分 / 记录级记录文件）+ 记录级扫描补 SFBGS003；
> ④ **判据全绿**：`run-all-tests.ps1` **5 步全过**（内核自检含 cmp∉{0,1} 11 例）；`verify --dev`
>   **1054 行 / 0 MISS**（+5 条第 145 轮检查）；`survey_gate_coverage.py --check` OK；黄金快照仅
>   `gate_coverage.json` 文案升级（有意，已 `--update`）；明细 = `docs/08` 4.8；
> ⑤ **A1 移除**：虚拟桌面 / 独立会话选题池删除（`docs/09` 九-3 + 十一 ②），**启动游戏由玩家
>   亲自操作**（AGENTS.md 同款注记）。
>
> 上一轮（第 144 轮，已收口 —— 明细 = `docs/15` 十五节）：P2 会话 **6/6 全 PASS** ⇒ P5
> 双形态回归底线达成（常规态 43/43 + P2 态 6/6）+ **0.1.16 包**（1,883,980 B / `83CC33F8…`；
> 双形态发布 + 结构指纹自检；★ 发布构建首次暴露 C1075 与 verify 反向表误报 ⇒ 已修，
> 教训 = 发布构建要定期真跑）—— **UI 注入研究（P1~P5）全部收口**。
>
> 更早（第 133~142 轮，速查 —— 明细 = `docs/15` 十三/十四/十五节）：**142** P2 6/6 全 PASS +
> 「眼睛判据」全部去除（计划零人工观察）；**141** 探针 `ctx.menu` 未接线修复（对照产品路径
> `接管=ok` 一击定位）；**140** P3-b 眼睛达成 + P4 交互接管（按钮劫持 X/Y / `itemActivated`
> 拦截 / 星图交接 = `ProcessUserEvent("Missions",false)` 真关菜单原语）；**139** P2 5/5 全 PASS
> + 真实操作二次验证；**138** 产品退避（`activateFailed`）+ 测试隔离（`ui.mode`）；**137** P3-b
> 落地（`[UI] UiMode=swf|auto|inject` 默认 auto）；**136/135/134** P3-a 两处产品级缺陷修复
> （数据生命周期 / `InjectCtx::list` 接线）⇒ 复跑 4/4 + 眼睛达成；**133** P3-a 落地
> （`SAQ_UiInject.{h,cpp}` + `ui.inject`）。
>
> 更早（第 132~117 轮，速查）：**132/131/130** P2 会话全 PASS + 探针 v4（`ui.research4` +
> `CreateObject` 回调劫持）/ **129** 功能迁移评估 / **128** operator 二期 / **127** HUD 提示时机
> 修复 + P2 双收口 + 0.1.15 包 / **126** 43/43 全 PASS / **125** 路线 D 冲突检测 /
> **124~120** P2 眼睛达成 · 复跑 · 首跑（缺 `aObjectives` 真因）· P2 落地 · 42/42 /
> **119~117** 探针 v2 + UI 注入研究落地。
>
> 更早（第 116~96 轮，速查）：DLC 链式三期 + 可重复任务（`docs/11`）+ 任务专属图标 +
> 日志上限可配（`[Log] MaxSizeMB`）+ 追踪者联盟 + 引导质量 2.0 + operator 全量产品化 +
> 覆盖面复盘 + 0.1.10~0.1.14 包。
>
> 更早（第 95~61 轮，速查）：`plan_regex_audit` + 判据定稿 + 「（不可导航）」前缀 +
> 进度门槛 OR 组（`orBit`）+ operator 全解 + 驱动器 **v70** + 同伴条目文案 + 地球地标 +
> 入口第二形态（可重复 NPC）+ DLC INFO 门槛。
>
> 更早（第 60~43 轮，速查）：分诊探针 `ev=`/`btn=` + 星图由脚本**轮询节拍**打开 +
> 引导候选池 + 结果码 5 / INFO 门槛 + 引擎内 harness + `stamp=` 指纹 + 0.1.1 包。
>
> 明细/复盘：`docs/91`（120~124 及补）/ `docs/93`（106~119）/ `docs/95`（43~60 等）/
> `docs/96`（35~40）/ `docs/97`（53~55 等）/ `docs/98`（9~16）/ `docs/11` / `docs/06`。
> （第 140 轮瘦身：被压缩的原始速查块原文见 `docs/91` 末尾附录。）

## 一、当前状态

| 组件 | 状态 | 位置 |
| --- | --- | --- |
| SFSE 插件（DLL） | ✅ 已构建（多 master 前缀探测 + 引导结果确认 + 认领已有引导 + 脚本活性探测 / 引导未生效收尾（第 19 轮）+ 控制台测试过滤（第 20 轮）+ 目标 FormID 拆分 / 自检上限修复（第 21 轮）+ 确认窗口改绑菜单状态（第 26 轮）+ **无限任务入口条目表 / 引导确认与停顿降噪**（第 27 轮）+ 入口引导候选链（第 30 轮）+ **候选链改「精确优先」（任务板自身）+ 引导目标动态更新 + marker 探测**（第 31 轮）+ **菜单关着时例行认领 + 通道对账**（第 32 轮）+ **菜单关着时预建静态表 / `Masters::Resolved()`（认领不再因表没建好而误判）**（第 33 轮） + **SET COURSE 星图（状态 5 + kHide 关菜单 + 星图探测）**（第 37 轮） + **星图自动重试 / 地点候选状态 5·6·7 / 引擎节点解析只读诊断（第 40 轮）** + **星图注册名修正 `GalaxyStarMapMenu` / 节点解析器特征字节修正（第 41 轮）** + **菜单关闭时现场读界面报告 / 补一次引导请求轮询（第 43 轮）** + **星图重试等待 3 s → 1.5 s 与脚本新窗口对齐 / 日志写明星图唯一入口（第 44 轮）** + **引导目标候选池（第 45 轮：预选可得候选 / 状态 2 换候选 / 菜单关着时动态复算 + 认领带下标）** + **引导可用性/提示收口（第 46 轮：`needsApproach` 判定 / 结果码 5 / `PollApproachRetry` 退避重试 / 星图本次不打开 / 测试过滤新增 `Mode=6` + 上限常量修复）** + **引导可用性收口（第 47 轮：同 cell 常驻兜底）+ 判据收口 + 大项 D（第 48 轮：`AllCandidatesNonPersistent` 判据回退（20 条）/ `EvaluateInfoGates` INFO 门槛 / 认领后 8 秒复算宽限期 / 星图与确认文案修正 + 同日补丁：**引擎已开始 ⇒ 放行**） + **harness 首测复查（第 49 轮补丁：无参入口 0 参调用 / 结束清菜单）+ 读档复算抖动（第 49 轮补丁②：候选降级观察期 20 秒）**） + **同伴好感度任务（第 74 轮：`companionPin` 固定显示 / 名字前缀 / 排序前置）** + **四大势力开头任务（第 75 轮：`factionEntry` 固定显示 + 固定排前四 / `IsGatePinned` 统一放行判据 / `PinnedOrderKey·Less` 排序（离线层）/ `FormatPinStats` 单独一行 / 说明写进载荷第 9/10 列）**）**） | `plugin/build/windows/x64/releasedbg/SAQ_ShowAvailableQuests.dll`（**1161728 B**，开发构建含 harness + 注入层（第 138 轮 +1024：激活失败不再重试 / `ui.mode` 隔离 op）；★ 第 112 轮起日志上限可配） |
| ★★ 同伴好感度任务（第 74 轮） | ✅ **4 同伴 / 8 条**：入口 4 条（个人任务，`companionPin=1` ⇒ 固定显示 + 名字前缀 + 描述好感度提示）；后续 4 条（承诺任务 ⇒ 补齐启动边并入链式门槛，前置没到不显示）；**前置到列表开头**（按同伴分组、入口在后续前；载荷/内嵌回退同序）；日志 `同伴固定=N(…)` + `同伴分组前置=N` + 界面 `order=` 顺序探针；SWF `stamp=56`（★ 第 82 轮：描述文案说透 ⇒ `stamp=60`） | `ref/companion_quests.json`（`tools/esm/gen_companion_quests.py`）｜`SAQ_QuestTable.h`（`companion`/`companionPin`/`kCompanionNamesZh·En`）｜`SAQ_Decision.*`（`DecideGateAction`）｜`MissionMenu.as`（`SaqCompanionNote`）｜`MissionsList.as`（`SAQ_OrderProbe`） |
| 静态任务表 | ✅ **280 条 / 5 个 master**（★★★ 第 128 轮（operator 二期）：门槛 `want` 全部来自**生成期折叠**（`!=`/`>`/`>=`/`<`/`<=` 与 `==` 同源；表内数据零变化，`docs/08` 4.7）；★★★★★ 第 114 轮：候选池 **1020** —— +19 条 world 级兜底，见 `docs/14`）（★★★★ 第 110 轮：+2 条追踪者联盟 —— `SFBGS003.esm` 是 **medium 档**，见 `docs/13`）（★★★ 第 106 轮：+7 条无 QTYP 漏收补收，见 `docs/12`）（★★★ 第 81 轮 +10 条「地球地标任务」）；★ 第 45 轮引导目标升级为**候选池**：`kGuideCandidates[]` **931 条候选 / 209 条有目标**（★ 第 47 轮：为「全非常驻候选」的 76 条任务里 56 条补了**同 cell 常驻兜底**（+56 条、常驻 339 条）；`candBegin/candCount` 切片；旧单目标字段已移除）；★ **第 48 轮（大项 D）新增 `kInfoGroups[]` / `kInfoConds[]`（INFO 门槛：60 条任务 / 290 条对话 / 341 条条件 / `infoGroupBegin/infoGroupCount` 切片）**；★★ **第 74 轮行尾 +2 列（`companion` / `companionPin`）+ 名字前缀**；★★★ **第 75 轮行尾 +1 列（`factionEntry`）+ 4 个数组（`kFactionEntryNamesZh·En` / `kFactionEntryNotesZh·En`）—— 四大势力开头任务固定显示 + 固定排前四；CF01 的 7 个引导候选被清 ⇒ 候选 931→**924**、有目标 209→**208**、链式边 55；★★★★ **第 81 轮：候选 924→**960**、有目标 208→**217**（+10 条地标任务里 9 条各 4 候选 —— 伦敦只给说明 = 0 候选）****；★★★★★ **第 109 轮（大项 A②）：候选 960→**996**、有目标 **224** —— 第 106 轮补收的 7 条补上引导候选（`guide_targets.json` 重跑：新增 7 / 消失 0 / 既有候选 0 处变化），内嵌载荷同步（`stamp=64`）****；★★★ 第 87 轮：`kQuestConds` **11 条**（进度门槛支持 OR 组 —— 见 `docs/08` 4.4）；★★★★ **第 89/90 轮：行尾 +`repeatable` 列 + `kRepeatableNotesZh·En` —— 可重复任务 15 → **20 条**（第 90 轮 +DLC 的 SFFL 家族 5 条：3 条在锚点星际站有接取人、2 条随机太空遭遇无接取点），见 `docs/11`） | `plugin/src/SAQ_QuestTable.h` |
| ★★ 入口条目表（第 27/28/29 + **80** 轮；★ **110** +1） | ✅ **21 条 = 13 任务板 + 8 可重复 NPC**（★★★ 第 110 轮：追踪者联盟总部悬赏信息台 —— SFBGS003 的板，master=1，候选链 = 常驻展示柜 → 总部外 marker）（★ 第 80 轮：`（可重复）贸易管理局 · 邓肯·林奇` 等 4 位商人（RAD03）+ `（可重复）追踪者联盟探员 · <城市>` ×4（RAD04）；「（可重复）」前缀**只在 NPC 条目**上 —— 玩家明确任务板不加）；uID = 引导目标 = 条目引用；ini `Mode=5` 只显示它们；★ **第 30 轮：引导目标 = 候选链**（① 条目引用自身（精确）→ ② 新建常驻 XMarker（任务板 0x900~0x90A / NPC 0x90B~0x911）→ ③ 兜底：内景 = 同 cell 原生常驻引用 / **外景 = 该 worldspace 的世界级常驻引用**），DLL 依次 `LookupByID` 取第一个命中的 + 诊断日志；第 29 轮 override 路线被实机否定（见一·补十五） | `plugin/src/SAQ_EntryTable.h`（`tools/esm/gen_entry_table.py`，+kind 列）；清单 `ref/entry_targets.json`；NPC 数据 `ref/repeatable_givers.json`（`tools/esm/gen_repeatable_givers.py`）；新建 marker `tools/esm/create_board_markers.py`（产物 `ref/board_markers.json`，★ marker 记录签名必须 REFR）；侦查 `tools/esm/scan_entry_persistent.py` |
| ★★ 地球地标任务（第 81 轮） | ✅ **10 条**（Apollo / Cairo / Dubai / HongKong / London / LosAngeles / NewYork / Osaka / Shanghai / StLouis）：豁免「地标」过滤 + 引导 = 书（或书商）的引用 + 同 cell / **world 级**常驻兜底 + 说明文本（载荷最后两列 = 描述第一句）；**伦敦只给说明**（书在书店/书堆，无固定接取点）；`Opportunity`（火星机遇号）**不收**（无触发物品，属主线 Unearthed 流程） | `ref/landmark_quests.json`（`tools/esm/gen_landmark_quests.py`）｜`SAQ_QuestTable.h`（`landmark` 列 / `kLandmarkNotesZh·En`）｜`SAQ.cpp`（说明来源第二分支） |
| 运行时过滤 | ✅ **只挡「已完成」**；「已接取」只认玩家任务日志；**master 没加载 ⇒ 整批跳过** | `plugin/src/SAQ_QuestState.*` / `SAQ_Masters.*` |
| 进度门槛（第 35 轮；★★ 第 87 轮 OR 组） | ✅ **7 条任务 / 11 条条件**（★ 第 87 轮：支持 **OR 组** —— `orBit` 列 + 引擎算法 `acc/orAcc/inOr`、`kUnknown` 一律放行；FFConstantZ06 = `UC04 完成 AND (Z04 完成 OR Z05 完成)`，见 `docs/08` 4.4）：「进度没到不显示」首版（第 35 轮）：记录级条件里「引用别的任务」的 GetQuestRunning/GetQuestCompleted/GetStageDone；求值不了 ⇒ 放行；ini `[Filter] ProgressCond` 可关；日志 `进度门槛=N(过A/藏B/未知K)` + `进度没到:` 名单。**已实测通过**（14:47 会话：过3/藏4/未知0；第 87 轮实测：A 段藏「亲爱的姐妹」/ B 段放行，见一·补二十第 6 节与 `docs/08` 第七节） | `plugin/src/SAQ_QuestCond.*`；数据 `ref/ctda_gates.json`（`tools/esm/analyze_ctda.py` → `gen_quest_table.py`） |
| ★★ INFO 门槛（第 48 轮 · 大项 D；★★ **第 78 轮扩到 DLC**；★ **110** +SFBGS003） | ✅ **84 条任务 / 425 条对话 / 512 条条件**（★★★★ 第 110 轮 +1 组：SFTA00 的总部对话）（★★ 第 106 轮：INFO 侧 OR 组纳入，18 条 orBit=1 条件，见 `docs/08` 4.5）（= 基础游戏 60/290/341 + **DLC 20/128/151**：SFBGS00D 2 / SFBGS050 7 / ShatteredSpace 11；跨 master 前置已支持 —— 前置的 master 下标按 kQuestMasters 顺序写进 `kInfoConds`）（「进度没到不显示」第二判据，对话侧）：任务自己的对话（INFO）里「引用别的任务」的同类条件；按「自引用」分成入口类/中性类（排除推进类）；**全部参与对话都有已知假条件 ⇒ 隐藏**（成对条件自动豁免）；求值不了 ⇒ 放行；★ **实测通过（21:46 会话：命中 58 条 / 藏 21 / 放行 2 / 未知 0，名单抽查合理）**；★ 同日补丁：**引擎已开始 ⇒ 放行**（判据收窄为「引擎没启动 + …」，修掉『全数到期』类误藏）；ini `[Filter] InfoCond` 可关；日志 `INFO门槛=…(过A/藏B/放行C/未知K)` + `INFO没到:` 名单 | `plugin/src/SAQ_QuestCond.*`（`EvaluateInfoGates`）；数据 `ref/info_gates_final.json`（`tools/esm/scan_info_gates.py` 逐 master 扫 4 份 `info_gates*.json` → `analyze_info_gates.py` 合并 → `gen_quest_table.py`；构建脚本在原始扫描缺失时自动补扫） |
| ★★ 任务链门槛（第 67 轮；第 68 轮实测对齐；★★ 第 69 轮扩到非编号链路；★★★★ **第 71 轮「全量入边审计」**；★★★★★ **第 77 轮「RIR 留专项」收口**；★★★★★★ **第 98 轮 DLC 收口**；★★★★★★★ **第 101 轮 DLC 二期 +15 边**） | ✅ **表内 84 条任务 / 90 边**（★★★★★★★★ **第 110 轮 +1 边**：`SFTA00 ← SFBGS003_MiscPointer@1000` —— 追踪者联盟，官方 fragment 原文核验）（★★★★★★★ **第 101 轮 +15 条 DLC 边**：另一边 MQIN ← 狂热逾界@4000 / 信念之争@7000 / 发掘过去@1600；地球舰队六级链（含 need_stage 的 **深入VOID ← 失踪的华庭号@1700**）；自由航道三条；**有意不收** MQIntro / DialogueHVDazra@5 / VKaiZ03* —— 见 `docs/06` 十节）；★★★★★★ **第 98 轮 +6 条 DLC 边**：破碎空间主线 `MQ02←MQ01@10000` / `MQ_Shell←MQ02@2000` / `MQ03·MQ04·MQ05←MQ_Shell@100` / `MQ06←MQ_Shell@1100`（官方 `.pex` 反编译取证，`tools/esm/gen_dlc_chain.py` → `ref/quest_chain_dlc.json`，host master = 3，见 `docs/06` 八节）；★★★ **第 77 轮 +8 边**：龙神全线 —— `RIR01←RI01@1300` / `RIR03←RIR02@300`（**故事事件**形态，新 op `SendStoryEvent`）/ `RI02←RIR03@300` / `RIR04←RI02@600`（玩家报告的那条）/ `RIR05←RIR04@300`（故事事件）/ `RI03←RIR05@10000` / `RIR06·RIR07←RI_Support@610·700`（Masako 可重复 radiant 的 managerial 边）= 编号链 25/26（`tools/esm/gen_quest_chain.py`，工具 31 目标 / 33 边）+ **扩展边 32/34**（第 69 轮 8/8：Eleos 静修地线 / 霓虹城帮派线 / 城市支线预启动；★★ **第 71 轮 +16 目标/18 边**：CF 线全家（深藏不露 ← UC02@860、终局两分支 ← LC088_Space@100/200、星钥站支线 ← CF08_SysDef@10、罪证的负担 ← CF01@310）+ FC08 + 城市线三连 ×2（RedTape / Viewport）+ MS05/MS06 引入任务 + 同伴 ×2（承诺：巴雷特 / 哈特家事）—— 起因 = 玩家实测「深藏不露 / 宝藏号的结局 / 夺回曾经」不该显示；全量审计 = `tools/esm/analyze_start_paths.py`（737 条启动调用逐条人工核实），其余候选按 A~H 八类记入「有意不覆盖」，见 `docs/08` 第十二节；人工核实 + 构建期源码核验）；判据「**全部启动边都没触发 ⇒ 隐藏**」（边之间是「或」：任一条已触发/求值不了 ⇒ 放行）；ini `[Filter] ChainCond` 可关；日志 `链式门槛=N(过A/藏B/未知K)`（N = **走到链式判定**的任务数；15:24 实测 **22** = 表内 25 − 3 条已被 INFO 门槛先藏）+ `链式没到:` 名单；★ 实测：A 段 过0/藏22 ✓、B 段 过1/藏21 ✓（「菜鸟觐见」在/不在名单）；★ 起因 = 玩家实测「深红舰队线的菜鸟觐见 / 风驰电掣在存档没开始时也显示」 | `plugin/src/SAQ_QuestTable.h`（`kChainGates`）；`SAQ_QuestCond.cpp`（`EvaluateChainGates`）；`SAQ_Decision.cpp`（`DecideChainGates`）；数据 `ref/quest_chain.json` + `ref/quest_chain_extra.json` + `ref/quest_chain_dlc.json`（第 98 轮） |
| 引导数据 | ✅ **225/280（80%）**（★★★★★ 第 114 轮：**两档常驻兜底**（同 cell → world 级）—— world 级 19 条、仍无兜底 20→**1**、候选 1001→**1020**、19 条 `needsApproach` 1→0，见 `docs/14`）（★★★★ 第 110 轮：+SFTA00 的 5 个候选 —— 1 号特工领衔；赏金狩猎 = noPickup 0 候选）（★★★ 第 109 轮：7 条补收任务的引导候选已补 —— 候选池 931→967（进表 996 条 / 224 条有目标），首候选见顶部第 109 轮块；新增 7 / 消失 0 / 既有候选 0 处变化）；★ 第 45 轮：**候选池 + 质量分级**（第一候选质量等级 1=204 / 2=3 / 4=2；**82 条**任务的第一候选被换成更好的目标 —— 含玩家实测抱怨的「平衡账目」「群众心态」）；★ 第 47 轮：**同 cell 常驻兜底**（`find_fallbacks`；「营救机器人」= `0x08ECA5` 非常驻 + `0x08ECA6` 常驻兜底链）；**无目标的 54 条已归因**（第 24 轮结论：不补猜的目标；★ 第 106 轮 +7 条补收任务的引导见第 109 轮） | `ref/guide_targets.json`（工具见 `docs/05` / `docs/06`；归因见 `docs/07`；持久性统计 `tools/esm/ref_flags_report.py`） |
| 代理任务（ESM） | ✅ 已挂 Reference Alias + Objective + 测试开关 / 目标高位 GLOB（0x804/0x805，第 20/21 轮，VMAD 已绑定）（手工字节补丁，幂等）；★ **第 30 轮：+11 条新建常驻 XMarker 引用**（记录号 0x900~0x90A，`Top 'CELL' > … > CellPersistent`）；★ **第 33 轮：+11 条 CELL 记录（官方「空壳」写法 = 只留 EDID + flags 0x4000，紧跟各自的 CellChildren 组）** —— 官方对同一条 cell（SFBGS003 → 霓虹城）就是这么写的；xEdit 验证 11/11 + 重存不丢；★ **第 125 轮：+1 条 GLOB `SAQ_UiNotice`（0x80E）⇒ ESM 4752 → 4805 B** | `esm/SAQ_ShowAvailableQuests.esm`（`tools/esm/patch_saq_esm.py` + `create_board_markers.py`；结构对照工具 `tools/esm/dump_cell_group.py`） |
| 引导脚本 | ✅ 已编译（`StartTimer` 轮询 + `ForceRefTo` + `SetActive` + OnInit 重挂自愈 + 菜单事件 Trace + 菜单打开重挂定时器 + 三个地点候选（第 40 轮）+ 星图请求失效校验（第 42 轮）+ **第 44 轮：星图待办改由「轮询节拍」推进**（`StarMapPendingTicks` + `ProcessStarMapPending`；旧的 1.5 秒延时定时器退役 —— 实测它被「星图提前打开 ⇒ 暂停」冻死，一次都没跑到；节拍到点 = 菜单已关、游戏在跑）；★ 第 27 轮定案：菜单开着时定时器不走，引导只在关菜单时应用 —— 与原版一致）+ ★★ 第 46 轮：HUD 提示机制通用化（`NoticeText`/`ShowNotice`/`ProcessNotice`，替代星图专用版）+ 「目标尚未加载」提示（重复 3 次、冷却 5 分钟）+ 失败 Trace 节流（前 3 次 + 每 10 次） | `scripts/SAQ_Main.psc` → `Scripts/SAQ_Main.pex`（**17666 B**，第 49 轮加测试命令执行器后） |
| SWF 内嵌回退数据 | ✅ **9.8 K 字符**载荷（**只含基础游戏 219 条任务**；★★ 第 81 轮 +10 条地标任务（含说明列）；入口条目不在内嵌里，降级窗口 ~0.3s 可接受）；★ 第 74/75 轮：顺序与 C++ **逐条同序**（前 4 条 = 四大势力开头任务 → 8 条同伴任务 → 其余），列序含「简要说明」两列 | `ui/missionmenu/saqdata/SaqEmbeddedPayload.inc` |
| UI（missionmenu.swf / _lrg） | ✅ 已重编译（+ 接取后自动取消引导 + 结果码 4 回滚 + drop= 名单（第 19 轮）+ 任务板入口文案（第 27 轮）+ **第 29 轮：「太远」文案退场** + **第 37 轮：R 传 `map=1/0` 标记（peek 三段协议）** + **第 38 轮：R 成功后界面侧走原版 `CloseMenu(true)` 关掉整个暂停菜单（peek 四段协议）** + **第 39 轮：R 不再取消引导（`SaqToggleGuide` 第三参数；重复按 R = `重复设定航线:` + 再开星图）** + **第 42 轮：报告新增 `press=[…]` / `sel=[…]`（按键那一刻用的是哪一行 + 列表选中项，怀疑「R 选错条目」时的硬证据）+ R/Enter 两条路都先取一次 `selectedEntry`（悬停到分隔行不再抛异常）** + **第 43 轮：分诊探针 `ev=[…]`（`ProcessUserEvent` 收到的 user event，名+↓/↑）/ `btn=[our:plot=1:map=0]`（选中项变化时的按钮启用状态）—— 「按 R 没反应」一次定性；SET COURSE 不再置灰（沉默失败 → 可点 + 有提示）** + **第 44 轮：删掉原版 `MissionMenu_PlotToLocation` dispatch（星图位置滞后一条的病根；换成 `SaqNoteStarMapHandoff`：只留音效 + note `星图:交给脚本(菜单关闭后)`）** + **第 46 轮：描述新增「导航目标要靠近目标区域后才加载」+ 结果码 5「目标尚未加载」分支** + **第 48 轮：诊断标签的「0xNaN」→「(子项)」；状态栏名字查不到时回退玩家任务日志（不再「当前引导:?」）** + **第 49 轮：`SAQ_TestDrive*` 测试入口（切 tab/选中/按键/展开/状态，内部走真实处理函数）+ `MissionsList.SAQ_FindEntryIndexByUID`** + **第 49 轮补丁③：入口发布清单（`SaqPublishEntryPoint`）补挂 5 个 `SAQ_TestDrive*` —— 此前只加进了类里、没挂 root ⇒ `ui.tab` 连续两轮 0 ms 失败（真因；见 `docs/09` 第八·补节）** + **第 74 轮：描述提示好感度 + `order=` 顺序探针** + **第 75 轮：载荷第 9/10 列（简要说明）→ 描述第一句；无导航目标时 `SaqNoPickupNote`（「没有可以直接导航的接取地点」）；`pin=` 探针；`stamp=57`**） + **第 82 轮：同伴条目描述首句 + 尾部落点文案改写（说清「自动开始 / 引导指向同伴本人」）** + **第 89 轮：载荷第 11 列（`bSaqRepeatable`）+ `FilterKnownQuests` 对「已完成 + 可重复」放行 + `rptk=` 探针（★ 第 90 轮**未改 SWF** —— DLC 条目不在内嵌载荷里，`SaqEmbeddedPayload.inc` 逐字节不变）** + **第 91 轮：不可导航条目的显示名加「（不可导航）」前缀（`SaqNotNavigablePrefix` + `sSaqBaseName` / `SaqBaseName` 让提示仍报原名）+ `nonav=` 探针；内嵌载荷逐字节不变** + **第 96 轮：可重复条目名加「（可重复）」前缀（`SaqRepeatablePrefix`；与「（不可导航）」叠加时在前）+ `rq=` 探针 + `order=` 新增 `|tail=` 段（含 lrg 版 `MissionsList.as` 补上漏掉的 `SAQ_OrderProbe`）** | `ui/missionmenu/build/missionmenu.swf`（**733254 B / lrg 733359 B**、`stamp=65` —— ★ 第 114 轮重编） |
| 构建验证工具 | ✅ 产物特征检查（SWF/DLL/PEX；★ 第 30 轮 +ESM 数据侧解析检查「新建常驻 marker 11/11 条」；★ 第 33 轮 +「CELL 空壳 11/11 条」+ 空壳不许写数据字段 + DLL 静态表就绪特征 & 反向检查；★ 第 41 轮 +星图注册名 `GalaxyStarMapMenu` / 节点解析器特征字节（正反向）；★ 第 42 轮 +SWF 的 `press=[`/`sel=[`/`R@`/`E@` + DLL 的 press 抄录与「脚本已应用后才换候选」+ PEX 的星图过期校验；★ 第 43 轮 +SWF 的 `ev=[`/`btn=[` + DLL 的 `补处理了关闭瞬间的引导请求`；★ 第 45 轮 +DLL 5 条 + 静态表候选池完整性；★ 第 45 轮补丁 +DLL 2 条 + PEX 2 条；★ 第 46 轮 +SWF 5 条 + DLL 6 条 + PEX 5 条 + 反向检查；★ 第 47 轮 +静态表 2 条 + DLL 1 条 + 反向检查；★ **第 48 轮 +SWF 1 条（`(子项)` 标签）+ DLL 11 条（`全部候选都非常驻` / INFO 门槛统计与名单 / 误判告警 / ini `InfoCond` 与说明 / 认领宽限说明 / 引导更新未确认新文案 …）+ 静态表 4 条（INFO 结构/计数 290+341/切片越界/大器晚成门槛样本）+ 2 组反向检查（「首选候选非常驻」「入口引导目标更新未被脚本确认」「还开着：」）** + ★ 第 49 轮 +SWF 5 条测试入口 + DLL 10 条 harness + PEX 6 条写侧/通道 + ESM 8/8 GLOB；★ 第 49 轮补丁 +DLL 1 条（`harness 结束清菜单`）；★ 第 49 轮补丁② +DLL 3 条（候选降级观察期 / 观察结束 / 入口降级观察期）；★ **第 49 轮补丁③ +10 条（`入口已挂 root`：两个 patch 源（普通 + lrg）× 5 个 `SAQ_TestDrive*` 入口名，出现 ≥2 次 = 定义 + 挂载 —— 防「加了新入口没挂 root」再犯）**；★ 第 67 轮 **+10 条**（DLL：链式门槛统计 / 名单 / ini `ChainCond`；用例计划：`[case:r67_chain]` + 两条断言；静态表：`StaticChainGate` / `kChainGates` / 完整 26 边 25 任务 / 菜鸟觐见样本）；★★ **第 69 轮 +9 条**（静态表：链式门槛改成**与两个数据源逐边对账**（`34/34 · 33/33 · 边集合一致`）+ 扩展边样本 ×2；DLL：`驱动器 v69` + `用例文件解析失败…` 文案；用例计划：r69 两条断言 + `ui.state` 反向检查；反向检查 v68 已替换）；★★ **第 72 轮 +5 条**（用例计划：r71 三条 `[case:]` / `r71 CF 四条断言` / `遗产的结局任一门槛藏（INFO|链式）` + 两条反向检查）；★★★ **第 75 轮 +30 余条**（SWF 8 / DLL 11 / 静态表 6 / 内嵌载荷 2 / 用例计划 6，含 r65 反向检查收窄到 r65 段）+ 更新受影响旧检查（候选 208 / 行尾四列正则 / 内嵌载荷表头 12 条）累计全通过 + ★ 第 82 轮：SWF +4 条（同伴提示/落点 中英）+ stamp 60 更新 + ★★★ 第 128 轮 +6 条（operator 二期：折叠内核在场 / 两条管线接线 / 离线测试含内核自检 / 门槛 want 只含 0/1 / 反向检查旧「只支持等于」）+ 两个老缺陷修复（`AutoLoad` 取值正则跨行 ⇒ 假 MISS；输出统一 UTF-8 ⇒ GBK 管道不再崩）） | `tools/ui/verify_saq_build.py` |
| ★★ 引擎内测试驱动（第 49 轮 · harness；★ 第 54 轮扩能力 + 落判据；★★ 第 56 轮修断言窗口 + 传送窗口；★★ 第 57 轮修「清场后星图晚开」+ `~0x…` 写回 + 两条用例红线；★★ 第 58 轮加「传送落地静默期 / 加载画面证据 / 卡死自动中止」；★★ 第 59 轮修「落地静默期绑死回执 Tick」= **每 Tick**推进 + 20 秒窗口只管等回执（见「十九」）；★★ **第 66 轮：命令回执窗口 3000 → 8000**（r44 的 `ping` 假 FAIL = 脚本执行了但晚 3.4 秒）+ 慢回执 note + 驱动器 **v66**（见「二十六」）；★★★★ **第 68 轮：断言正则解析期编译校验**（非法正则立刻报「正则非法」，不再退化成「日志里没出现」的误导性超时 —— 反向断言静默 false = 假 PASS）+ 统计行断言用 `scope=case` + 驱动器 **v68**；★★★★★ **第 69 轮：用例文件解析失败 ⇒ 该用例判 FAIL**（不再静默丢步 —— 15:43 会话 r65_icons 的 `ui.state` 踩过）+ 新增 `r69_chain_extra` / `r69_chain_extra_pass`（链式门槛扩展边 A/B）+ 驱动器 **v69**（见「二十九」/`docs/09` 十四·补九）；★★★★★★ **第 83 轮：`ui.*` 遇「桥没通」在步骤超时内自动重试（菜单刚打开那一两拍桥没解析出来）+ 清场延迟复查只在「真的看到并关掉星图」时提前结束（关任务菜单不算）+ 断言超时报文带三个打点 + 新体检工具 `tools/test/plan_regex_audit.py`** + 驱动器 **v70**（见 `docs/09` 十四·补十六）；★★★★★★★ **第 84 轮：r80 断言改判「精确目标二选一」（原板 / marker —— 活体 NPC 引用运行期可得，落原板更精确）+ 结果 JSON 非法 UTF-8 收口（`check_results.py` 容错读出）+ 新工具 `tools/test/log_hygiene.py`**（驱动器仍 v70；见 `docs/09` 十四·补十七）；★★★★★★★★ **第 87 轮：+2 条用例（`r87_or_group` / `r87_or_group_pass` —— 进度门槛 OR 组）⇒ 用例集 24 条**；★★★★★★★★★ **第 89/90 轮：+2 条用例（`r89_repeatable` 基础游戏 / `r90_dlc_repeatable` DLC —— 可重复任务做完一次后仍显示；DLC 用 `~0x…` 记录号，断言容忍加载顺序前缀）⇒ 用例集 26 条**；★★★★★★★★★★ **第 94 轮：26 条 26/26 全 PASS**（定稿判据实测收口 + `plan_regex_audit` 滚动噪声修复）+ **第 96 轮：+1 条 `r96_repeatable_group`（可重复前缀 + 整组排末尾）⇒ 用例集 27 条；第 97 轮 27 条 27/27 全 PASS**；**第 98 轮 +2 条（`r98_dlc_chain` / `r98_dlc_chain_pass` —— DLC 链式门槛 A/B）⇒ 用例集 29 条；第 100 轮实机 29/29 全 PASS**（见 `docs/09` 十四·补二十三 / 十四·补二十四 / `docs/11` 11.4）；**第 101/102 轮 +3 条（DLC 二期 A/B/对照边）⇒ 32 条；★ 第 103 轮：二期 B 段判据并入 `r98_dlc_chain_pass`（`MQ_Shell` 同会话只能推一次）、删 2 条 ⇒ **30 条**；★ 第 104 轮：`quest.probe` 产品行（红线六）；★ **第 105 轮：30 条 30/30 全 PASS**（22:50~22:58 会话 / 455031 ms / 判据脚本退出码 0）**） | ✅ **`[case:smoke]` 已全绿（06:53 会话）**；★ 09:22 会话 6 条 PASS 2/FAIL 4（驱动缺陷：断言窗口清 0 / 传送 3 秒太紧 → 第 56 轮已修）；★ 09:35 会话 6 条 PASS 2/FAIL 4 —— **三条新真因仍在驱动器/用例侧、产品全对**（① 星图在清场**之后**才打开 ⇒ r26 `ping` 超时；② `ui.select/selectchild` 的 `~0x…` 漏写回 ⇒ r45 报「记录号 0x000000」；③ r47 没等推送就切 tab ⇒ 撞上内嵌回退数据 `notfound`；④ 两条断言共享同一批行 ⇒ 第二条须 `scope=case`），**已修、待重跑**；★ 10:31 会话 6 条 PASS 3/FAIL 3 —— **三个 FAIL 同源：游戏端卡在加载画面**（传送回执后 38 ms 又提交一条 MoveTo ⇒ Papyrus VM 卡在 `player.MoveTo`；r45 传送超时 + r48/r44 陪跑超时；界面 = 加载转圈 + HUD 蓝点），驱动器 **v58** 已加「落地静默期 / 菜单证据 / 卡死自动中止」，**待重跑**；★ 11:04 会话 6 条 PASS 4/FAIL 2 —— **两条 FAIL（r47/r45 的传送）是「落地静默期绑死回执 Tick」的假 FAIL**（Papyrus 两次 `MoveTo` 结果=0、超时瞬间菜单=无；与第 58 轮「真卡死」的区别就在这一行），驱动器 **v59** 已改成「静默期每 Tick 推进 / deadline 只管等回执 / 落地完成证据」，**待重跑**：用例文件（ini 风格 DSL）+ DLL 驱动器（造状态/开关菜单/选中/按键/断言）+ AS3 测试入口（**走真实处理路径**）+ 结果 JSON + 报告器。**只允许 `ini [Test] Harness=1` 时激活**（否则零开销）。通道 = ESM 追加的 8 条 GLOB（0x806~0x80D；纯追加，不动旧记录号）；写侧动作走 Papyrus 语言级 API（`Reset/Start/SetStage/CompleteQuest/MoveTo`）——**零新 RE 工作**。★ 第 54 轮新增：`assert.nolog`（反向断言）/ `scope=this\|prev\|case`（日志窗口 —— 动作与日志常在同一次 Tick）/ `ui.selectchild`（Enter 只引导走子项）/ `menu.hide <注册名>`（关星图）/ `teleport.entry`（站远走近）/ `~0x…`（记录号，免写死 DLC 前缀）；用例 6 条（smoke + r26_menu_idle / r44_starmap / r45_candidates / r47_board_marker / r48_info_gate）。★ 跑之前：ini `[Test] Mode=0`（显示全部）。设计与用法：**`docs/09-测试驱动（harness）.md`**（第十四节 = 第 54 轮；★ 第 55 轮两条用例编写红线：**断言「上一步的副作用行」必须 `scope=prev`**（smoke 漏写 ⇒ 假失败）+ **驱动器在每条用例结束都清场**（清引导/关任务菜单/关星图）并留 700 ms 间隔） | `plugin/src/SAQ_Test.{h,cpp}`（驱动器）、`SAQ_TestOps.{h,cpp}`（原语）、`scripts/SAQ_Main.psc`（`ProcessTestCommand`）、`ui/missionmenu/src/MissionMenu.as`（`SAQ_TestDrive*`）+ `MissionsList.as`（`SAQ_FindEntryIndexByUID` / `SAQ_FindChildIndexByUID`）、`tools/test/scenarios/SAQ_TestPlan.txt`、`tools/test/check_results.py`；部署开关 `build-saq.ps1 -Harness`。★★ **第 60 轮：新 op `guide.probe`（只读候选可得性探针 —— 与产品同一套查询，结果进步骤 JSON）+ r45 尾段改判（走远后判「不许早降级 + 引导仍指向 [1]」；旧「先保持」断言需读档窗口、不可达）+ 驱动器 v60**（见「二十」） |
| ★★ 离线层（第 64 轮 · 大项 K；★★ 第 87 轮 +1 用例） | ✅ **决策纯函数 11 个**（`plugin/src/SAQ_Decision.*`，零引擎依赖；调用点全部改用它；★ 第 84 轮 +`Utf8SafeCut` —— 证据通道的 UTF-8 安全截断；★★ 第 87 轮 `DecideProgressGates` 加 OR 组引擎算法）+ **单元测试 18 用例 / 929 断言**（★ 第 89 轮 +1 用例：可重复任务豁免「已完成 ⇒ 隐藏」；★ 第 90 轮**断言数不变** —— 决策逻辑未改，只扩数据；★ 第 96 轮列表顺序用例扩到「可重复垫底」+ `PinnedOrderKey` 四参数）（`plugin/tests`：自写 MiniTest + 独立 xmake target `SAQ_Tests`，不链接 commonlibsf，毫秒级）+ **数据管线黄金快照 14 件**（第 74/75/80/81/89 轮追加：同伴好感度 / 四大势力开头 / 地标任务 / 入口表 / 可重复任务…）（`tools\test\golden_snapshot.py`；规范化哈希 —— 形式改动不算变化、内容改动必报红；ESM 幂等实测）；一键 `tools\test\run-all-tests.ps1`（★ 第 130 轮起 **5 步**：决策单测 + **折叠内核自检 `tools/esm/ctda_ops.py --self-test`**（★★★ 第 145 轮扩至真值表 12 + 不可折叠 2 + cmp∉{0,1} **11** + 组边界 5 + 组装 10 例）+ 黄金快照 + tripwire + P2 计划正则自检）；★ 第 84 轮新工具 `tools\test\log_hygiene.py`（日志/结果「证据通道健康」体检：非法 UTF-8 / 控制字符）；详见 **`docs/10`** | `plugin/src/SAQ_Decision.*`、`plugin/tests/`、`tools/test/` |
| ★★ 四大势力开头任务（第 75 轮） | ✅ **4 条**（UC01 超越极限 / FC01 枝节横生 / RI01 重返职场 / CF01 深藏不露）：`factionEntry` 列（0..3 = **固定顺序**）⇒ 固定显示（跳过三类门槛）+ **固定排前四**；载荷第 9/10 列 = 「简要说明」（描述第一句）；**CF01 的引导候选在生成期被清空**（只给说明 —— 点击给提示、描述写明「按上面的说明推进」）；日志 `势力入口固定=` / `势力入口前置=` / `势力入口固定名单:`（**单独一行**）；界面 `pin=` 探针 + SWF `stamp=57` | `ref/faction_entry_quests.json`（`tools/esm/gen_faction_entry_quests.py`）｜`SAQ_QuestTable.h`（`factionEntry` / `kFactionEntryNamesZh·En` / `kFactionEntryNotesZh·En`）｜`SAQ_Decision.*`（`IsFactionEntryPinned` / `PinnedOrderKey·Less`）｜`SAQ.cpp`（`FormatPinStats`）｜`MissionMenu.as`（`SaqNoPickupNote` / `SaqPinNoteProbe`） |
| MO2 部署 | ★★★★★★★★★★★★★★★★★★★★★★ **第 144 轮：P2 会话 6/6 全 PASS（P5 双形态回归底线达成）+ 已还原常规部署（开发构建 + SWF 覆盖拷回 + `*.p2off` 清理 + Plan 回主计划 + ini `Harness=0` / AutoLoad 清空；`verify --dev` 1049 行 / 0 MISS）** ★★ **第 143 轮：P5 结构指纹产物 —— 常规态 43 条已实机全绿 ⇒ 已按 P2 实验态重新部署（第 144 轮已收口还原）**（DLL 开发构建（harness）**1208320 B**（SHA256 `860D5392…`，工作区 == 部署；常规态 = `-SkipTable -SkipSwf -Harness`（SWF 覆盖在场 733254 / 733359 B + 主计划 + `verify --dev` **1049 行 / 0 MISS**）⇒ 现态 = `-SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`：SWF 覆盖 `*.p2off`（游戏加载原版）/ `Plan=SAQ_TestPlan_p2.txt`（含 `指纹=ok` 断言，工作区 == 部署）/ `[UI] UiMode=auto`；`verify --dev --p2` **868 行 / 0 MISS** + 离线层 5 步全过（决策 24 用例 / 1201 断言）；下一动作 = P2 会话 6 条 ⇒ 0.1.16 包）★★★★★★★★★★★★★★★★★★★★★ **第 141 轮：探针 `ctx.menu` 修复产物已按 P2 态重新部署（待复跑 6 条）**（DLL 开发构建（harness）**1206272 B**（SHA256 `B2FCFA4F…BCB376`，工作区 == 部署；`-SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`，SWF 覆盖 `*.p2off` / ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`（部署副本 = 眼睛窗口去除版）+ `[UI] UiMode=auto`；`verify --dev --p2` **860 行 / 0 MISS**（+1 条源码计数检查）+ 离线层 5 步全过（P2 正则 18/15）；恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`）★★★★★★★★★★★★★★★★★★★★ **第 139 轮：P2 会话重跑收口（5 条 5/5 全 PASS）—— 已实机验证；P2 实验态继续保留（P4 验证仍以原版 SWF 为主）**（DLL 开发构建（harness）**1161728 B**（工作区 == 部署；`-SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`，SWF 覆盖 `*.p2off` / ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`（副本含 `ui.mode` 5 处 = 探针 swf / 产品 auto）+ `[UI] UiMode=auto`；`verify --dev --p2` **829 行 / 0 MISS**（+2 harness 串 +1 产品串 +2 计划形状）+ 离线层 5 步全过（P2 正则 12/9）；恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`）★★★★★★★★★★★★★★★★★ **第 137 轮：P3-b 产物已按 P2 实验态部署（注入形态产品化 —— 已实机收口 = 第 138 轮 r137 PASS）**（DLL 开发构建（harness）**1160704 B**（工作区 == 部署；`-SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`，SWF 覆盖 `*.p2off` / ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`（副本 +1 条 `r137_product_inject`）+ 新增 `[UI] UiMode=auto`；`verify --dev --p2` **824 行 / 0 MISS**（+12 产品串双向 + 源码结构/分层 + ini 模板 [UI]）+ 离线层 5 步全过（P2 正则 12/9）；恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`）★★★★★★★★★★★★★★★★★ **第 136 轮：第 135 轮修复产物 P2 复跑收口（19:55~19:59 会话 4 条 4/4 全 PASS —— r133 五段全 ok + `上下文=ok`，P3-a 判据收口）**（DLL 开发构建（harness）**1142272 B**（工作区 == 部署，SHA256 `3FF24385…43723553`；判据五连全绿 = `check_results` 4/4 + `log_hygiene` 退出码 0（135 行 = 122 I + 13 W + 0 E）+ `plan_regex_audit` 10/10 + `verify --dev --p2` **800 行 / 0 MISS** + 离线层 5 步全过（P2 正则 9/6）；**眼睛达成（玩家确认第 8 tab「可接任务」+ 真实 206 条列表）⇒ P3-a 完全收口**；**下一步 = P3-b（P2 实验态继续保留）**）★★★★★★★★★★★★★★★★ **第 135 轮：第 134 轮修复产物 P2 复跑（19:25~19:29 会话 4 条 3 PASS / 1 FAIL —— r133 第二处真因 = 注入上下文未接线）⇒ 修复后已按 P2 态重新部署（复跑就绪）**（DLL 开发构建（harness）**1142272 B**（工作区 == 部署；`-SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`，SWF 覆盖 `*.p2off` / ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`；`verify --dev --p2` **800 行 / 0 MISS** + 离线层 5 步全过；恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`）★★★★★★★★★★★★★★★ **第 134 轮：第 133 轮产物已实机（4 条 3 PASS / 1 FAIL —— r133 真因 = 数据生命周期缺陷）⇒ 修复后已按 P2 态重新部署（复跑就绪）**（DLL 开发构建（harness）**1141248 B**（哈希 `0D281502…12DAD`，工作区 == 部署；`-SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`，SWF 覆盖 `*.p2off` / ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`；`verify --dev --p2` 全部通过 + 离线层 5 步全过；恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`）★★★★★★★★★★★★★★ **第 133 轮：P3-a 注入模块已按 P2 实验态部署（待实机 —— 4 条用例）**（`build-saq.ps1 -SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`：SWF 覆盖 `*.p2off` 禁用（游戏加载原版）/ ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`（副本 10981 B，含 `r133_inject_data`）/ DLL 开发构建（harness）**1141248 B**（+24064，含 SAQ_UiInject）；`verify --dev --p2` **797 行 / 0 MISS**；工作区 == 部署；**恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`**）★★★★★★★★★★★★★ **第 132 轮：P2 实验态已实机收口（3/3 全 PASS；已实机 = 18:41 会话）**（`build-saq.ps1 -SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`：SWF 覆盖仍 `*.p2off` 禁用 / ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt` / DLL 开发构建（harness）**1117184 B**（+1024）；`verify --dev --p2` **784 行 / 0 MISS**（+1 条新特征串「接线 UserEvents=」）；**恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`**）★★★★★★★★★★★ **第 130 轮：功能迁移探针 v4 已按 P2 实验态部署（**已实机 = 第 131 轮**）**（`build-saq.ps1 -SkipTable -SkipSwf -Harness -P2 -AutoLoad Save1_…142854_2_0_4`：SWF 覆盖 `*.p2off` 禁用（游戏加载原版）/ ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt` / DLL 开发构建（harness）**1116160 B**；`verify --dev --p2` **782 行 / 0 MISS**；**恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`**）★★★★★★★★★★ **第 128 轮：operator 二期产物已重建部署**（`build-saq.ps1 -SkipTable -SkipSwf`；DLL **1083392 B** / PEX 19133 B / ESM 4805 B —— 尺寸与第 127 轮一致（只改了 ini 文案与注释）；`verify --dev` **946 OK / 0 MISS**；本轮**无玩家可见变化**、无需实机会话）★★★★★★★★★ **第 127 轮：P2 已收口 + 已恢复常规测试态**（`build-saq.ps1 -SkipTable -SkipSwf -Harness`：SWF 覆盖拷回 + `*.p2off` 清理 + ini `Plan` 改回主计划；`verify --dev`（非 P2）全过；PEX **19133 B** / DLL 1083392 B / ESM 4805 B；P2 会话 2/2 PASS + 眼睛双达成 —— 见「游戏内实测」行）★★★★★★★★ **第 126 轮：已按 P2 实验部署（待实机 = P2 会话 2 条 + 眼睛）**（`build-saq.ps1 -SkipTable -SkipSwf -Harness -P2`：SWF 覆盖禁用 `*.p2off`（游戏加载原版）/ ini `Harness=1` + `AutoLoad=…142854_2_0_4` + `Plan=SAQ_TestPlan_p2.txt`；DLL 开发构建（harness）**1083392 B** / PEX **19026 B** / ESM **4805 B**；`verify --dev --p2` 全过（756 OK）；恢复 = 不带 `-P2` 跑 `-SkipTable -SkipSwf -Harness`）★★★★★★★ **第 125 轮：路线 D 产物已部署（常规测试态）⇒ 第 126 轮实机 43/43 全绿**（DLL 开发构建（harness）**1083392 B** / PEX **19026 B** / ESM **4805 B**（+GLOB 0x80E）；主用例计划 **43 条**（+`r125_ui_channel_ok`）/ P2 计划 **2 条**（+`r125_ui_conflict`）；ini `Harness=1` + `AutoLoad=…142854_2_0_4` + `Plan=SAQ_TestPlan.txt`；`verify --dev` 全过）★★★★★★ **第 124 轮：已恢复部署（P2 → 常规测试态）**（`build-saq.ps1 -SkipTable -SkipSwf -Harness` —— SWF 覆盖拷回 + `*.p2off` 清理 + ini `Plan` 改回主计划；`verify --dev`（非 P2）**全部通过**；DLL 开发构建（harness）**1079296 B**）★★★★★ **第 123 轮：P2 复跑 PASS 后重新部署（P2 实验态保留，供「眼睛会话」亲验）** —— 眼睛窗口 **8 → 30 秒**（`SAQ_TestPlan_p2.txt` 的 `wait 30000`，部署副本已同步、`verify --dev --p2` 全过）；其余不变（DLL 开发构建（harness）**1079296 B** / SWF 覆盖禁用 `*.p2off` / ini `Harness=1` + `AutoLoad=Save1_…142854_2_0_4` + `Plan=SAQ_TestPlan_p2.txt`）；恢复 = `build-saq.ps1 -SkipTable -SkipSwf -Harness` ★★★★ **第 122 轮：P2 首跑的修复版已重新部署（已复跑 PASS —— 见第 123 轮）** —— 修复 = 注入条目补 `aObjectives` + `bActive` / `iRemainingTime`（`SAQ_UI.cpp` `buildEntries`，见顶部第 122 轮块）；DLL 开发构建（harness）**1079296 B**（SHA256 `727903B7…9AAC10`，工作区 == 部署）；SWF 覆盖仍禁用（`*.p2off`）/ ini `Harness=1` + `AutoLoad=Save1_…142854_2_0_4` + `Plan=SAQ_TestPlan_p2.txt`；`verify --dev --p2` 全过（新增 `aObjectives` 检查项 OK）★★★ **第 121 轮：已按 P2 实验部署（「无 SWF 覆盖」形态 —— SWF 覆盖已改名禁用 `*.p2off`，游戏加载原版 missionmenu.swf；ini 测试态 `Harness=1` + `AutoLoad=Save1_…142854_2_0_4` + `Plan=SAQ_TestPlan_p2.txt`）**（DLL 开发构建（harness）**1078784 B**（含 `ui.research3` 探针 v3）+ P2 计划 **`SAQ_TestPlan_p2.txt`（`r120_gfx_poc`）** + 主计划 42 条 / 728 步；工作区 == 部署哈希一致；**恢复 = `build-saq.ps1 -SkipTable -SkipSwf -Harness`**（SWF 拷回 + `*.p2off` 清理 + Plan 改回）；verify 两模式全过（`--dev` / `--dev --p2`）；★ 首跑结果见顶部第 122 轮块）★ **第 119 轮产物已部署（含 `ui.research2` 探针 v2；测试态 —— ini `Harness=1` + `AutoLoad=Save1_…142854_2_0_4`）**（DLL 开发构建（harness）**1062400 B** + 用例文件 **42 条 / 728 步**（含 4 条 r114 待定任务 + 2 条 r117 研究探针 + 1 条 r119 探针 v2；工作区 == 部署哈希一致；★ 第 115 轮：sg02 用例传送目标已换 `~0:0x0013D006` 常驻门并重新部署）；ESM **4752 B** / SWF **733254 + 733359 B**（★ 第 114 轮**重编** —— 19 条任务 needsApproach 列变化 ⇒ `stamp=65`）/ PEX 17666 B；`stamp=65`、入口 **21 条**、**链式边 90 / 84 条有边任务**、任务表 **280 条**、可重复任务 **21 条**；部署 ini = 测试态（第 119 轮 `-Harness` 部署 —— 打 0.1.15 包时由打包流程还原 `Harness=0`））；verify 全过（含 `部署 == 工作区`）；★ **用例全 PASS 记录**：第 94 轮 26/26 + 第 97 轮 27/27 + 第 100 轮 29/29 + 第 105 轮 30/30 + 第 108 轮 32/32 + **第 113 轮 35/35**（10:52~11:00 会话 / 492609 ms / `驱动器 v70`；`[E]` 0 / `[W]` 40 全老现象；日志 967310 B）+ **第 115 轮 38/39**（12:30~12:40 会话 / 523703 ms；判据四连全绿；唯一 FAIL = sg02 传送目标非常驻 ⇒ 已换 `~0:0x0013D006` 常驻门 + verify 同步 + 部署）+ **第 116 轮 39/39 全 PASS**（13:11~13:20 会话 / 534718 ms；判据五连全绿；sg02 重跑收口 ⇒ 全量首次全绿）+ **第 118 轮 41/41 全 PASS**（14:13~14:23 会话 / 552406 ms / **41 用例 720 步**；判据五连全绿；含 2 条研究探针用例）+ **第 120 轮 42/42 全 PASS**（14:44~14:53 会话 / 550156 ms / **42 用例 728 步**；判据五连全绿 = `check_results`/`log_hygiene` 退出码 0 + `plan_regex_audit` **158/158** + `verify --dev` + 离线层 3/3；含探针 v2 用例）—— 第 111 轮 34/35 的唯一 FAIL 已收口；每轮打完后切回开发构建 + MO2 ini `Harness=0` | `D:\Mod Organizer 2\starfield_mods\mods\Show Available Quests (SFSE)\` |
| Nexus 上传包 | ★★★★★★★★★ **0.1.16 已完成**（第 144 轮）：`dist\SAQ-ShowAvailableQuests-0.1.16.zip`（**1,883,980 B**，SHA256 `83CC33F8382C05B05FD41B7092F4A702D64388BF6033309B6F9FD1031A2D9563`；= 0.1.15 + 双形态发布（界面被覆盖 / 缺失 ⇒ 自动切注入形态 + 结构指纹自检）+ 文案/changelog v0.1.16；版本号 6 处同步；`verify --release` 全过 + 打包后补跑 verify 覆盖新包 + 包内 DLL 哈希 == 工作区（**921088 B** 发布构建）；**可上传 Nexus**）★★★★★★ **0.1.15 已完成**（第 127 轮）：`dist\SAQ-ShowAvailableQuests-0.1.15.zip`（**1,852,719 B**，SHA256 `6359DEAF1FA8CA3E3F0A5D2F2B42DA5A4830CB6B1ED66819FFEDEAEADD95006F`；= 0.1.14 + 第 125 轮 UI 通道冲突检测 + 第 127 轮提示时机修复（提示改为「关菜单后」发出）；版本号 6 处同步 + changelog v0.1.15 + 文案更新（中英功能条目 + 已知限制）；`verify --release` 全过（发布构建 63 项 harness 特征全无）+ **打包后补跑 verify 覆盖新包**（7 文件 ⇒ 0.1.15）+ 包内 6 产物哈希逐一致（发布 DLL **852480 B** / PEX 19133 B）+ ini 玩家默认值 + README `v0.1.15`；打完后已切回开发构建（**1083392 B**）+ MO2 ini `Harness=0` / `AutoLoad` 清空）★★★★★ **0.1.14 已完成**（第 116 轮）：`dist\SAQ-ShowAvailableQuests-0.1.14.zip`（**1,850,136 B**，SHA256 `2FF1ED0804038C36FE22BA60B2B73E2D7A985D084B9DE0C9D2CC97D561624EE9`；= 0.1.13 + 第 114 轮两档常驻兜底（候选池 1020 / 19 条「远处点引导立即生效」）+ 第 115/116 轮验证收口；版本号 6 处同步 + changelog v0.1.14 + 文案更新（「需要靠近」20 → 1 条）；**可上传 Nexus**）；`verify --release` 全过 + **打包后补跑 verify 覆盖新包**（7 文件 ⇒ 0.1.14）+ 包内 6 产物哈希逐一致（发布 DLL **848384 B**）+ ini 玩家默认值 + README `v0.1.14`；打完后已切回开发构建（**1032192 B**）+ MO2 ini `Harness=0`。★ **0.1.13（历史）**：1,849,574 B / `5CD96E4C…B54D17`（第 113 轮 —— 追踪者联盟 medium + 日志上限可配 + 7 条补收引导）；**0.1.12**：1,846,610 B / `1E461BA1…CEF20C11`；**0.1.11**：1,845,427 B / `65928FCD…4A24357`；**0.1.10**：1,845,225 B / `A551D4DD…756113`；**0.1.9**：1,845,170 B / `098E6544…81327E0` | `dist\` |
| 游戏内实测 | ★★★★★★★★★★★★ **第 144 轮：P2 会话 6 条 6/6 全 PASS（P5 双形态回归底线全部达成 —— 常规态 43/43 + P2 态 6/6）**（22:06~22:08 / 101531 ms / P2 实验态 · 原版 SWF / PID 33976；r137 产品路径 `界面注入：已激活（…接管=ok，指纹=ok）` = 结构指纹自检实机通过 + watchdog 重放 0 次；`[E]` 0 / `[W]` 19 基线；判据 = `check_results` 6/6 + `log_hygiene` 0（236 行零坏字节）+ `plan_regex_audit` 19 断言 / 6 段 + `p2_plan_regex_check` 18/15 + `verify --dev --p2` + 离线层 5 步）★★★★★★★★★★★ **第 143 轮：常规态主计划 43 条 43/43 全 PASS（P5 回归底线达成）**（21:47:54~21:57:13 / 559266 ms / PID 30872 / DLL 1208320 B；判读 = SWF 形态（`推送成功 … 206 条` / stamp=65 / lang=zh）+ `界面注入` 0 行（auto 下 SWF 优先，符合设计）+ `推送放弃` 0 + `[E]` 0 / `[W]` 48（47 条「推送失败（第 1 次）」+ 1 条桥解析诊断 = 基线老现象）；判据 = `check_results` **43/43** + `log_hygiene` 退出码 0（日志 3062 行零坏字节；结果 JSON 1086 行干净）+ `plan_regex_audit` **160 条断言全命中** + `verify --dev` 1049 行 / 0 MISS + 离线层 5 步（决策 24 用例 / 1201 断言）；待 P2 态 6 条）★★★★★★★★★★ **第 141 轮：第 140 轮产物 P2 会话判读收口（6 条 5 PASS / 1 FAIL —— 唯一 FAIL = 探针 `ctx.menu` 未接线；对照产品路径 `接管=ok` 立判；已修 + 待复跑）**（21:11~21:14 / 236203 ms / P2 实验态 · 原版 SWF / 6 用例；`r120_gfx_poc` PASS 42266 ms（干净 7 tab + 五段全 ok + `注入=ok（1→3）`）/ `r125_ui_conflict` PASS 4797 ms（判定行 + 无「推送放弃」+ 同周期产品 auto 自动激活）/ `r130_gfx_migrate` PASS 33344 ms（4a 全绿 + 4b `刷新／文本／关菜单入口=ok`）/ `r133_inject_data` PASS 32328 ms（五段与第 136 轮逐字同型）/ **`r140_interact_takeover` FAIL 10171 ms**（`界面接管 Menu_mc=ok｜接管=fail（ButtonBar_mc 取不到）` —— 探针一行即止）/ `r137_product_inject` PASS 37313 ms（`已激活（…接管=ok）` + `菜单关闭（watchdog 重放 0 次）`）；判据 = `check_results` 5/6 + `log_hygiene` 退出码 0（251041 B / 663 行零坏字节）+ `plan_regex_audit` 19 断言 / 6 段（5 条未命中 = r140 FAIL 同源）+ `verify --dev --p2` 0 MISS + 离线层 5 步；**待复跑 = 6 条（预期 6/6）**）★★★★★★★★★ **第 139 轮：第 138 轮修复产物 P2 会话重跑收口 —— 5 条 5/5 全 PASS（P2 实验态首次全量全绿；判据五连全绿）+ 产品路径真实操作二次验证 ⇒ P3-b 完全收口（眼睛待玩家点头）**（20:36~20:39 / 224750 ms / P2 实验态 · 原版 SWF / 5 用例；`r120_gfx_poc` PASS 42407 ms（干净 7 tab + `扩tab=ok（7→8）` + `注入=ok（1→3）` —— `ui.mode swf` 隔离一击生效）/ `r125_ui_conflict` PASS 4953 ms（判定行 + 无「推送放弃」）/ `r130_gfx_migrate` PASS 33297 ms（探针 v4 全绿：`接管=ok` / `关菜单入口=ok` / `造对象=ok（三级 + 接线 UserEvents=1）`）/ `r133_inject_data` PASS 32062 ms（五段全 ok：`注入数据 1→206` / `对账 0x002C5401:超越极限` / `恢复 →1` / `再注入 →206`）/ **`r137_product_inject` PASS 37562 ms**（`UiMode=auto` → `已激活（tab 7→8，条目 206，语言=zh）` → `菜单关闭（watchdog 重放 0 次）`）；★ 两处修复验证 = 测试侧 `ui.mode` 5 处覆盖全生效 + 产品侧**零「激活失败」**（对照第 138 轮 96 条刷屏）；★★ **会话后玩家手动开菜单（20:39:24.923 auto 自动激活 → 20:39:25.804 切到第 8 个 tab 注入 206 条 ok → 20:39:46.973 关闭）—— 产品路径真实操作二次成立**；判据五连全绿 = `check_results` **5/5** + `log_hygiene` 退出码 0（日志 164796 B / 448 行；本次 PID 26376 = 179 I + 19 W + **0 E** —— W 全为原版 SWF 推送失败系列 6 菜单周期 + 1 条首周期桥解析诊断；结果 JSON 97 行）+ `plan_regex_audit`（P2 计划）**13/13** + `verify --dev --p2` 0 MISS + 离线层 **5 步全过**；产品侧与基线逐项一致（待推送 206 / 入口 21 / 门槛 7·80·68）；明细 = `docs/15` 十三·补五）★★★★★★★★ **第 138 轮：第 137 轮产物 P2 会话判读收口 —— 5 条 3 PASS / 2 FAIL（产品路径 r137 PASS；r120/r133 FAIL = 产品抢先激活撞探针）+ 两处修复（产品：激活失败不再重试；测试：`ui.mode` 隔离）—— 已实机收口 = 第 139 轮（5/5）**（20:20~20:24 / 187094 ms / P2 实验态 · 原版 SWF / 5 用例；r120 **FAIL** 10078 ms（探针五段全 ok，但产品已把 tab 扩到 8 ⇒ 探针扩 `8→9`、切 `切8`，断言硬编码 `切7` 等不到）/ r125 PASS 4828 ms（判定行 + `assert.nolog 推送放弃`）/ r130 PASS 33406 ms（U5~U10 全绿）/ r133 **FAIL** 17079 ms（`扩tab=fail（8→8）` 级联 —— 且产品重试刷 **96 条 WARN / 15 秒**）/ **r137 PASS** 37531 ms（`界面形态：UiMode=auto` → `界面注入：已激活（tab 7→8，条目 206，按键名=(空)，语言=zh）` → `菜单关闭：本轮为注入形态（watchdog 重放 0 次）`）⇒ **产品路径成立**；判据 = `check_results` 3/5 + `log_hygiene` 退出码 0（250 行零坏字节）+ `plan_regex_audit` 11/13（2 条未命中 = 与 FAIL 同源）+ `verify --dev --p2` 0 MISS + 离线层 5 步；**已重新部署待重跑（预期 5/5）**；明细 = `docs/15` 十三·补四）★★★★★★★ **第 137 轮：P3-b 产物待实机（P2 会话 5 条 —— 产品路径验收）**（判据 = `r137_product_inject` 三条断言（`界面形态：UiMode=auto` / `界面注入：已激活（UiMode=auto…）` / `菜单关闭：本轮为注入形态`）+ r120/r125/r130/r133 保持全 PASS；眼睛 = 第 8 个 tab「可接任务」+ 真实列表（产品路径）；观察 = watchdog 计数（应为 0 —— 除非引擎刷新））★★★★★★ **第 136 轮：第 135 轮修复产物 P2 复跑收口 —— 4 条 4/4 全 PASS（`InjectCtx::list` 接线一击生效，五段判据与预测逐字一致）；P3-a 判据收口**（19:55~19:59 / 184000 ms / P2 实验态 · 原版 SWF / 4 用例 35 步；`r120_gfx_poc` PASS 42406 ms（六段全 ok + 注入 1→3）/ `r125_ui_conflict` PASS 5000 ms（判定行 + `assert.nolog 推送放弃`）/ `r130_gfx_migrate` PASS 33390 ms（U5~U10 全绿）/ **`r133_inject_data` PASS** 32063 ms（`上下文=ok｜快照=ok（1 条）｜扩tab=ok（7→8）｜注入数据=ok（1→206，期望 206）｜对账=ok（0x002C5401:超越极限｜可导航1）｜恢复=ok（→1，期望 1）｜再注入=ok（→206，供眼睛）`）；★ 关键诊断 = `切到我们的 tab（idx=7）→ mask=0x40 + 注入 206 条（ok）` + `从我们的 tab 切走（idx=0）→ 恢复引擎列表（ok）`；判据五连全绿 = `check_results` **4/4** + `log_hygiene` 退出码 0（日志 59655 B / 135 行 = 122 I + 13 W + **0 E**）+ `plan_regex_audit` **10/10** + `verify --dev --p2` **800 行 / 0 MISS** + 离线层 **5 步全过**（P2 正则 9/6）；DLL **1142272 B**（工作区 == 部署）；**眼睛达成（玩家确认第 8 tab「可接任务」+ 真实任务列表）⇒ 判据 + 眼睛双达成，P3-a 完全收口**）★★★★★ **第 135 轮：第 134 轮修复产物 P2 复跑收口（4 条 3 PASS / 1 FAIL —— 第一处修复生效、第二处真因已修，待复跑）**（19:25~19:29 / 169828 ms / P2 实验态 · 原版 SWF / 4 用例；`r120_gfx_poc` **PASS** 42312 ms（六段全 ok + 注入 1→3）/ `r125_ui_conflict` **PASS** 4688 ms / `r130_gfx_migrate` **PASS** 33375 ms（U5~U10 全绿）/ **`r133_inject_data` FAIL** 17110 ms = 唯一 FAIL —— 第 134 轮的生命周期修复**一击生效**（`ui.inject` 拿到 206 条，不再「待推送=0」），但暴露**第二处产品级缺陷**：`InjectCtx::list` 未接线 ⇒ `切到我们的 tab（idx=7）→ mask=0x40（写入失败！） + 注入 206 条（fail）`、`对账=fail（0x002C5401:一小步｜可导航1）`、`恢复=ok` 为「1 == 1」巧合（假 PASS）—— **玩家实测所见 = 第 8 个 tab「可接任务」里显示的还是原版那条「一小步」**（列表没被换）；判据 = `check_results` 3/4 + `log_hygiene` 退出码 0（67874 B 零坏字节）；**修复（三处）+ 重建 DLL 1142272 B + 按 P2 态重新部署（待复跑）**）★★★★ **第 134 轮：第 133 轮产物 P2 会话实机检查收口（4 条 3 PASS / 1 FAIL）**（19:14~19:16 / 184282 ms / P2 实验态 · 原版 SWF / 4 用例；`r120_gfx_poc` **PASS** 42625 ms（六段全 ok + 注入 1→3）/ `r125_ui_conflict` **PASS** 4781 ms / `r130_gfx_migrate` **PASS** 33375 ms（U5~U10 全绿）/ **`r133_inject_data` FAIL** 17188 ms = 唯一 FAIL —— `ui.inject` 提前返回「待推送=0」，**真因 = 产品级缺陷**：`TryPushPending` notOurs 分支 `g_pending.quests.clear()`（notOurs 恰是注入形态要工作的场景 ⇒ 注入数据源被清空；证据链 19:15:41.099 待推送=206 → :41.950 判定 notOurs 清空 → :42.909 待推送=0）；判据 = `check_results` 3/4 + `log_hygiene` 退出码 0（57036 B / 130 行零坏字节）+ `plan_regex_audit` 8/10（未命中 2 条 = r133 两条断言，同源）；修复 = 数据生命周期解耦（生命周期 = 一次菜单打开周期；4 个推送子分支不再清数据）+ 重建部署；**待复跑** = 4 条全绿 + 眼睛（第 8 tab「可接任务」+ 真实 206 条列表）；判读表 = `docs/15` 十三节实机收口段）★★★ **第 133 轮：P3-a 注入模块待实机（P2 会话 4 条：r120 / r125 / r130 / r133_inject_data）**——判据 = `快照=ok.*扩tab=ok.*注入数据=ok.*对账=ok.*恢复=ok`（+`再注入=ok`）；★ 眼睛 = 第 8 个 tab「可接任务」+ 真实可接任务列表（真任务名/描述；切回原版 tab = 恢复）；判读表 = `docs/15` 十三节）★★★★ **第 132 轮：第 131 轮产物 P2 会话重跑收口（3/3 全 PASS —— P2 首次全量全绿）**（18:41~18:44 / 162422 ms / P2 实验态 · 原版 SWF · 开发 DLL 1117184 B / 3 用例 27 步；`r120_gfx_poc` PASS 42343 ms（六段全 ok + 注入 1→3）/ `r125_ui_conflict` PASS 4968 ms（判定行 + 无「推送放弃」）/ `r130_gfx_migrate` **PASS** 33328 ms（上轮唯一 FAIL 修复后通过）；判读 = **U5/U6/U7/U8/U9/U10 + 置灰全绿**（`接管=ok（回调收到 1 次——R 键可接管）` + `关菜单入口=ok（可调用=是；菜单仍在=是；返回 false）`；`读Data=fail` = 预期边界）⇒ **探针 v4 全绿 / P3 立项条件达成**；判据五连全绿 = `check_results` **3/3** + `log_hygiene` 退出码 0（日志 44939 B / 104 行 / 94 I / 10 W / 0 E 零坏字节）+ `plan_regex_audit` P2 计划 **8/8** + `verify --dev --p2` **784 行 / 0 MISS** + 离线层 **5 步全过**；产品侧与基线同型；**眼睛达成 = 玩家看到 2 条 `SAQ-Mig-0/1`**；判读表 = `docs/15` 十二·补二）★★★★ **第 131 轮：第 130 轮产物 P2 会话实机检查收口**（18:29~18:32 / 3 用例 **2 PASS / 1 FAIL**；`r120_gfx_poc` PASS 42313 ms（六段全 ok + 注入 1→3）/ `r125_ui_conflict` PASS 4750 ms / `r130_gfx_migrate` FAIL 13375 ms（唯一 FAIL = 4b `关菜单入口=ok` —— 探针判据错）；判读 = **U6/U7/U8/U10 + 置灰全绿**、**U5 接管 fail = 探针接线缺陷**（`ButtonBaseData` 的 param2 需传 UserEventData/Array）、**U9 = 判据错**（原版覆盖返回值）⇒ 两处均已修 + 重建部署待重跑；**眼睛达成** = 玩家看到 2 条 `SAQ-Mig-0/1`；判读表 = `docs/15` 十二·补）★★★★★★★★★★★ **第 127 轮：P2 态 2/2 PASS + 眼睛双达成（P2 + 路线 D 完全收口）**（会话 16:48~16:50 / 118828 ms / `Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖禁用 / 开发 DLL 1083392 B / PEX 19133 B；`r120_gfx_poc` PASS 42532 ms（六段全 ok + 注入 1→3）+ `r125_ui_conflict` PASS 4812 ms（第二个菜单周期独立判定行 16:50:15.809 + 无「推送放弃」）；★ **提示时机修复生效**：提示首发 16:50:03 = 关菜单瞬间（对照第 126 轮 16:25:02 在菜单里）+ 补发 06/09（共 3 次）；判据三连全绿（`check_results` 2/2 / `log_hygiene` 95 行零坏字节 / `plan_regex_audit` P2 计划 4/4）+ `verify --dev --p2` + 离线层 3/3；**眼睛双达成（玩家确认）**：① 第 8 个 tab「SAQ-PoC」+ 3 条 `SAQ-PoC-Item 0/1/2` 可见（显示在当前选中的第 1 个 tab 下 —— 探针末次回调 idx=0 后注入 = 设计行为）② 关菜单后 HUD 中英提示；已恢复常规部署）★★★★★★★★★★ **第 126 轮：43 条 43/43 全 PASS（首次 43 条全量全绿）**（会话 16:01~16:10 / 577703 ms / `驱动器 v70` / `stamp=65` / **43 用例 734 步**；判据五连全绿 = `check_results` + `log_hygiene` 退出码 0（日志 1106423 B / 2117 行零坏字节）+ `plan_regex_audit` **160/160** + `verify --dev` + 离线层 3/3；`[E]` 0 / `[W]` 48 全老现象；**`r125_ui_channel_ok` PASS = 防误报成立**（推送成功 + 零判定行）；产品侧与基线同型（入口 21/21、`需靠近/待生效/引导重试`=0）；待 P2 会话 = 2 条 + HUD 提示眼睛）★★★★★★★★★ **第 124 轮：P2 眼睛判据达成（P2 完全收口）**（会话 15:43~15:45 / `Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖禁用 / DLL 1079296 B；`r120_gfx_poc` PASS 32344 ms；**玩家截图（30 秒窗口内）= 眼睛证据**：tab 条 8 项（前 7 = 探针假数据 `$SAQ-keep0..6`、第 8 = `SAQ-PoC`）+ 列表 3 条 `SAQ-PoC-Item 0/1/2`；关菜单重开无残留；恢复部署后 `verify --dev` 全过）★★★★★★★★ **第 123 轮：P2 复跑 —— `r120_gfx_poc` PASS（六段全 ok + 注入段 `entryCount 1→3`）⇒★ P2 通过（「无 SWF 覆盖」形态技术成立）**（15:25~15:27 会话 / 82172 ms / `Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖禁用 / 开发 DLL 1079296 B；判据五连全绿 = `check_results` + `log_hygiene` 退出码 0（日志 39910 B / 89 行零坏字节）+ `plan_regex_audit` **2/2** + `verify --dev --p2` + 离线层 3/3；**眼睛窗口 = 探针执行后 ~8 秒**（本次未取得眼睛证据 —— 玩家截图为 `menu.close` 后重开状态 = 原版数据、无残留）⇒ 计划窗口已改 30 秒供亲验；判读见顶部第 123 轮块 / `docs/15` 九·补六）★★★★★★★ **第 122 轮：P2 首跑（`r120_gfx_poc` FAIL —— 五段 ok / 注入段 fail）**（15:15 会话 / 94953 ms / `Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖禁用 / `log_hygiene` 退出码 0（日志 2189614 B / 4204 行零坏字节）+ 离线层 3/3 + `verify --dev --p2` 全过；**真因 = 注入条目缺 `aObjectives`（原版 `IsMission` 判定）⇒ 已修 + 重建部署，待复跑**；判读见顶部第 122 轮块 / `docs/15` 九·补五）★★★★★★ **第 120 轮：42 条 42/42 全 PASS**（14:44~14:53 会话 / 550156 ms / `驱动器 v70` / `stamp=65`；判据五连全绿 = `check_results`+`log_hygiene` 退出码 0 + `plan_regex_audit` **158/158** + `verify --dev` + 离线层 3/3；`[E]` 0 / `[W]` 47 全老现象；日志 2162393 B；**探针 v2 判读 = 五段全 ok**（哨兵 `0x20000000` 存活 = 拦截 + 写双成立；对照 / 放行 / 清理 / U2 全成立）⇒ 下一步 = P2（原版 SWF 完整 PoC））；★★★★★ **第 118 轮：41 条 41/41 全 PASS**（14:13~14:23 会话 / 552406 ms / `驱动器 v70` / `stamp=65`；判据五连全绿 = `check_results`+`log_hygiene` 退出码 0 + `plan_regex_audit` **155/155** + `verify --dev` + 离线层；`[E]` 0 / `[W]` 46 全老现象；日志 1072874 B；**研究探针判读 = U0 ✅ / U1 ❌ 能力边界 / U2 ❓ 待补测 / U3 ✅** ⇒ 路线 A 不终止，下一轮探针 v2）；★★★★ **第 116 轮：39 条 39/39 全 PASS**（13:11~13:20 会话 / 534718 ms / `驱动器 v70` / `stamp=65`；判据五连全绿 = `check_results`+`log_hygiene` 退出码 0 + `plan_regex_audit` **150/150** + `verify --dev` + 离线层；`[E]` 0 / `[W]` 44 全老现象；日志 1039927 B；sg02 换常驻门重跑 ⇒ 传送成功（回执 3500 ms 落地）且任务未被启动 ⇒ **4 条待定任务全部定案「不收」**；眼睛 = **全部眼睛判据已完成（玩家确认）**）；★★★ **第 115 轮：39 条 38 PASS / 1 FAIL**（12:30~12:40 会话 / 523703 ms / `驱动器 v70` / `stamp=65`；判据四连全绿 = `check_results`+`log_hygiene` 退出码 0 + `plan_regex_audit` 150/150 + `verify --dev` 全过；`[E]` 0 / `[W]` 46 全老现象；日志 1049232 B；唯一 FAIL = sg02 传送目标非常驻（用例侧，已修待重跑）；4 条待定任务判读：3 条不收 + sg02 待重跑；眼睛 = 19 条 world 级兜底（第 116 轮确认））；★★ **第 113 轮：35 条 35/35 全 PASS**（10:52~11:00 会话 / 492609 ms；判据四连全绿；眼睛：赏金狩猎 / 总部板确认、星际盗贼 = 进度没到、总部引导蓝点（第 116 轮确认））；★ 11:08~11:18 手动会话（`Harness=0` 正常玩）：tab 切换 + 列表 206 条正常（总部引导未点）；★ 第 111 轮（09:48~09:56 会话）：35 条 34 PASS / 1 FAIL（r109 断言过窄，已修，第 113 轮收口）； ★ 更早会话（第 21~108 轮）= 已迁 `docs/93` 归档。 | — |

一键构建（表已生成过时；`-SkipTable` 会跳过 3~6 分钟的 ESM 遍历）：
```powershell
& ".\tools\build-saq.ps1" -SkipTable
```
全流程见 `docs/01-构建与环境.md`；引导机制 `docs/05`；**DLC 支持 `docs/06`**。

---

## 一·补六 ~ 一·补三十五、第 21~50 轮（复盘已迁 docs/96 / 97 / 98；速查）

> 完整复盘按轮次迁入 **`docs/96`**（32~34 / 35~40 / 41~46）、**`docs/97`**（48~50）、
> **`docs/98`**（9~16 / 21~25 / 26~31 / 47）；★ **第 126 轮瘦身另建** **`docs/93`**（第 106~119 轮
> 顶部块 + 旧表项）、**`docs/94`**（harness 第 55~81 轮补记）、**`docs/92`**（星图第 41~42 轮）
> —— 均为逐字迁移，原位留指针；仍然生效：① 日志/ini 在 **mod 目录内**（22）；
> 「能不能导航」界面可见（23）；无目标 52 条**不补猜**（24）；打包 `package-saq.ps1`（25）；
> ② 任务板入口 = `Mode=5` / 入口表（27）+ 置灰拦截（28）+ **候选链**（30）+ 精确优先（31）
>   + 入口目标**动态更新**（32）；③ 静态表预建 + 11 条 **CELL 空壳**（33）⇒ 任务板任意
>   位置精确导航（34）；④ 进度门槛首版（35）；星图三层（37~40）+ 注册名
>   `GalaxyStarMapMenu`（41）+ 分诊探针 `press=/sel=/ev=/btn=`（42/43）+ **轮询节拍**开星图
>   （44）+ **候选池**（45）+ 可用性收口（46）+ 同 cell 常驻兜底（47）+ **INFO 门槛** +
>   「引擎已开始 ⇒ 放行」（48）+ **harness** 落地（49）+ 「先构建部署、再启动游戏」+
>   `stamp=`（50）。

## 十一、下一步（按优先级）

0. ✅ **第 145 轮：A3 operator 三期 —— cmp∉{0,1} 折叠产品化 + type 低字节解析缺陷修复 +
  tripwire 三类报红（产物零变化）**（明细 = 顶部块 / `docs/08` 4.8）：
  ① 普查（新工具 `_tmp_r145_operator3_census.py`，5 master）：INFO 侧 17974 条三函数条件里
  特殊形态只有 7 条「别名位 + 自引用」；记录级表内唯一特殊样本 = SFTA00 `==1510`
  （medium 档 0xFD 前缀 = 自引用）⇒ **「外键 + flags 非 OR」表内外 0 条** —— 硬做运行期
  通道的理由不成立，改由 tripwire 三类报红盯数据（出现才立项）；
  ② 修缺陷：type 只该读**低字节**（+0 是 4 字节 = 低字节 type + 3 字节 unused；实测 300 条
  stage 条件的 unused 非零 ⇒ 第 87/106 轮「读 u32」统计失真、且记录级一旦带非零 unused
  会漏收）→ 三处解析点全改（`analyze_ctda` / `scan_info_gates` / `survey_gate_coverage`）；
  ③ 内核：`fold_operator` 改通用实现（**任意数值常量 cmp** 精确折叠 + NaN 保护；自检
  12+2+11+5+10 例）→ 两条管线放开 cmp；GLOB 比较值（bit2）单独放行（需运行期读
  `TESGlobal`，0 条对象 ⇒ 不实现）；tripwire 升级三类报红 + 记录级扫描补 SFBGS003；
  ④ 判据：`run-all-tests.ps1` **5 步全过** + `verify --dev` **1054 行 / 0 MISS**（+5 条）+
  `--check` OK + **8 个数据产物重扫前后哈希逐字节一致** + 黄金快照仅 `gate_coverage.json`
  报告文案升级（有意，已 `--update`）；运行时（DLL / SWF / ESM / PEX）零改动；
  ⑤ **A1 选题池移除**（`docs/09` 九-3 + 十一 ②）：虚拟桌面 / 独立会话删除 ——
  **启动游戏由玩家亲自操作**（AGENTS.md 同款注记）。
0. ✅ **第 144 轮：P2 会话判读 6/6 全 PASS ⇒ P5 双形态回归底线全部达成 + 0.1.16 包完成
  —— UI 注入研究大项（P1~P5）全部收口**（明细 = 顶部块 / `docs/15` 十五节）：
  ① 会话（22:06~22:08 / 101531 ms / 原版 SWF）= **6 PASS / 0 FAIL**（r137 含 `指纹=ok`
  实机证据 + watchdog 重放 0 次）；② 判据全绿 = `check_results` 6/6 + `log_hygiene` 0 +
  `plan_regex_audit` 19 断言 / 6 段 + `p2_plan_regex_check` 18/15 + `verify --dev --p2` +
  离线层 5 步；③ **0.1.16 包** = `dist\SAQ-ShowAvailableQuests-0.1.16.zip`（1,883,980 B /
  `83CC33F8…`；双形态发布 + 文案/changelog v0.1.16；`verify --release` 全过 + 补跑覆盖新包
  + 包内 DLL == 工作区 921088 B）；打完后已切回开发构建 + 常规部署还原（`verify --dev`
  1049 行 / 0 MISS）；④ ★ 发布构建首次暴露的两个问题（已修）：`SAQ_UiInject.cpp` 闭括号在
  `#if SAQ_WITH_HARNESS` 段内 ⇒ C1075 + verify 反向表 3 条「产品与探针共用」串误报其
  为 harness 泄漏 ⇒ 教训 = **发布构建必须定期真跑**（`docs/15` 15.5 表）；
  ⑤ **余项**：P5 收口 ⇒ UI 注入研究全部完成；之后 = 可选新增强 / 低优先项（见下方旧项）。
0. ✅ **第 143 轮：P5 结构指纹落地 + 常规态 43 条实机全绿 —— 已由第 144 轮收口**：
  ① 结构指纹（结构齐即放行 / 缺项宽限期 1500 ms / 超时 WARN + 不重试 + HUD 兜底）；
  ② 常规态 43/43 全 PASS + 判据全绿（DLL 1208320 B）；③ P2 态重新部署 ⇒ 待实机 6 条。
0. ✅ **第 142 轮：P2 会话重跑判读 6/6 全 PASS ⇒ P3/P4 实机收口 + 全部「眼睛判据」
  去除 —— 已重新部署 P2 态，下一步 = P5**（明细 = 顶部块 / `docs/15` 十四·补二）：
  ① 会话（21:23~21:24 / 145532 ms）= **6 PASS / 0 FAIL**（r140 第 141 轮修复一次通过 +
  星图=开 端到端）；② 检查三件套全绿 = `check_results` **6/6** + `log_hygiene` 0
  （347021 B / 901 行零坏字节）+ `p2_plan_regex_check` 18 条 / 15 样例；③ 眼睛判据全部
  去除 = P2 计划零人工观察（r120 的 10 秒 + r137 的 30 秒删除）+ 探针「眼睛=…」输出
  删除 / 注释改写 + verify 新增「P2 零人工观察窗口」反向检查；④ 判据全绿 = DLL
  1206272 B（`3E43FC22…`）+ `verify --dev --p2` 859 行 / 0 MISS + 离线层 5 步（决策
  19 用例 / 936 断言）；之后 = P5 双形态与发布（「随时可回 SWF」纪律不变）。
0. ✅ **第 141 轮：r140 实机判读（5/6）+ 探针 `ctx.menu` 修复 + 眼睛窗口去除 ——
  第 142 轮重跑 6/6 收口**（明细 = 顶部块 / `docs/15` 十四·补）：① 会话（21:11~21:14 /
  236203 ms）= **5 PASS / 1 FAIL**（r120/r125/r130/r133/r137 全 PASS；r140 =
  `接管=fail（ButtonBar_mc 取不到）`）；② 真因 = 探针路径 `ctx.menu` 未接线（对照 = 同会话
  产品路径两处 `接管=ok`）⇒ 修复三处（补接线 / 上下文自检双证据 / verify 计数检查）；
  ③ 计划 = r120/r130/r133 眼睛窗口去除（r137 保留 —— 第 142 轮已去）；④ 判据全绿 =
  DLL 1206272 B + `verify --dev --p2` 860 行 / 0 MISS + 离线层 5 步（P2 正则 18/15）+
  会话判据 `check_results` 5/6 + `log_hygiene` 0（251041 B 零坏字节）+
  `plan_regex_audit` 19 断言 / 6 段（5 条未命中 = r140 同源）。
0. ✅ **第 140 轮：P3-b 眼睛判据达成 ⇒ 完全收口 + P4 交互接管落地（按钮劫持 X/Y +
  activate 拦截 + 星图交接 / 真关菜单原语）—— 已实机（第 141 轮判读：唯一 FAIL =
  探针接线缺陷 ⇒ 已修 + 待复跑）**（明细 = 顶部块 /
  `docs/15` 十四节）：① 眼睛判据达成（三个 30 秒窗口 + 玩家手动一次 ⇒ 第 8 个 tab
  「可接任务」+ 真实任务列表都看到了）⇒ P3-b 最后一环闭合；② P4 三块 = 按钮劫持
  （原 Data protected ⇒ 换法 + 原版条目逐行复刻委托）/ activate 拦截（`itemActivated`
  priority=100；原版树对「有子项」只展开、不派发 ⇒ 子项行才是真链路）/ 星图交接
  （`ProcessUserEvent("Missions", false)` = 原版 `CloseAllMenus()` 原语）；③ 引导接线 =
  `ApplyGuideRequest` 返回结果码 + `RequestGuideFromInject` / `CurrentGuideQuestID`；
  ④ 数据构造补齐 = 每主条目带 1 条子项（★ `CanShowOnMap` 取 `aObjectives[0]` 的字段）；
  ⑤ 判据（离线全绿）= 新 op `ui.interact [key X|Y|state]` + 新用例 `r140_interact_takeover`
  （P2 计划 6 条）；DLL **1206272 B** + `verify --dev --p2` **859 行 / 0 MISS** + 离线层
  **5 步全过**（P2 正则 **18 条 / 15 样例**）；`docs/15` 与 `docs/99` 同日瘦身（回 100 KB 内）；
  ⑥ 待实机 = P2 会话 **6 条**（r120 / r125 / r130 / r133 / r137 / **r140**）；
  之后 = P5 双形态与发布（「随时可回 SWF」纪律不变）。
0. ✅ **第 139 轮：P2 会话重跑收口 —— 5 条 5/5 全 PASS + 两处修复验证生效 + 产品路径
  真实操作二次验证 ⇒ P3-b 完全收口（眼睛：第 140 轮已点头）**（明细 = 顶部块 / `docs/15` 十三·补五）：
  ① 会话 = **5 PASS / 0 FAIL / 0 SKIP**（20:36~20:39 / 224750 ms / P2 实验态 · 原版 SWF）；
  判据五连全绿 = `check_results` **5/5** / `log_hygiene` 退出码 0（本次 PID 26376 =
  179 I + 19 W + **0 E**）/ `plan_regex_audit`（P2）**13/13** / `verify --dev --p2` 0 MISS /
  离线层 **5 步全过**；
  ② 判读逐项与第 138 轮预测一致（r120 干净 7 tab + 扩 tab / r125 判定 + 无「推送放弃」/
  r130 探针 v4 全绿 / r133 五段全 ok / r137 产品路径三条断言）；
  ③ ★ **真实操作二次验证**：会话后玩家手动开菜单（20:39:24.923 auto 自动激活 → 20:39:25.804
  切到第 8 个 tab 注入 206 条 ok → 20:39:46.973 关闭）—— 产品路径非 harness 驱动同样成立；
  ④ **眼睛待玩家点头**（第 8 个 tab「可接任务」+ 真实列表 ×2 形态；三个 30 秒窗口 + 手动一次）；
  ⑤ **下一步 = P4 交互接管**（按钮劫持 X/Y + activate 拦截 + 引导态竖条 + 就地刷新 + 星图交接 /
  真关菜单原语）；P2 实验态继续保留（P4 验证仍以原版 SWF 为主）；之后 P5 双形态与发布
  （「随时可回 SWF」纪律不变）。
0. ✅ **第 138 轮：P2 会话判读收口 + 两处修复（产品：激活失败不再重试；测试：`ui.mode`
  隔离）—— 已实机收口 = 第 139 轮（5/5）**（明细 = 顶部块 / `docs/15` 十三·补四）：
  ① 判读 = **产品路径成立**（r137 PASS：`界面形态：UiMode=auto` → `界面注入：已激活
  （tab 7→8，条目 206，语言=zh）` → 菜单关闭 `watchdog 重放 0 次`）；两条 FAIL（r120/r133）
  = 产品抢先激活（tab 已 8）打破探针「原版 7 tab」前提；
  ② 真缺陷（已修）= r133 期间产品重试无退避/无节流（**96 条 WARN / 15 秒** + 每拍全量
  重建）⇒ `activateFailed` 本菜单周期停止重试 + WARN 带 `tab x→y`；
  ③ 测试侧 = 新 op `ui.mode <swf|auto|inject|reset>` + P2 计划 5 处前置（探针 = swf /
  产品 = auto）；
  ④ 判据（离线全绿）= DLL **1161728 B** + `verify --dev --p2` **829 行 / 0 MISS** +
  离线层 **5 步全过**（P2 正则 12/9）；P2 态已重新部署（工作区 == 部署）；
  ⑤ **待实机** = P2 会话 **5 条重跑**（预期 5/5）+ 眼睛（第 8 tab「可接任务」+ 真实列表）；
  之后 = **P4 交互接管**（按钮劫持 + activate 拦截 + 星图交接 / 真关菜单原语）⇒ P5 双形态
  与发布（「随时可回 SWF」纪律不变）。
0. ✅ **第 137 轮：P3-b 落地（注入形态产品化）—— 已实机收口（第 138 轮判读：产品路径
  r137 PASS；离线判据 = DLL 1160704 B + `verify --dev --p2` 824 行 / 0 MISS + 离线层
  5 步 + P2 计划 +1 条 `r137_product_inject`）** ——
  ① 三块（见顶部块 / `docs/15` 十三·补三）= 完整 `SaqDescriptionText` 迁移（逐字搬运 +
  KeyHelper 按键名）/ watchdog（500ms；entryCount + 首条 uID ⇒ 重放）/ `UiMode` 运行期
  开关（`swf|auto|inject`，默认 auto；HUD 提示「延迟判定」）；注入层**分层**（产品路径
  发布也编译，harness 探针输出格式逐字不变）；
  ② 判据 = DLL **1160704 B** + `verify --dev --p2` **824 行 / 0 MISS** + 离线层 **5 步全过**
  （P2 正则 **12 条 / 9 样例**）+ P2 计划 **+1 条** `r137_product_inject`（产品路径断言三条）；
  ③ **待实机** = P2 会话 **5 条**（r120/r125/r130/r133/**r137**）+ 眼睛（第 8 tab
  「可接任务」+ 真实列表 —— 产品路径）+ 观察 watchdog 计数（应为 0）；
  ④ 之后 = **P4 交互接管**（按钮劫持 + activate 拦截 + 星图交接 / 真关菜单原语）⇒
  P5 双形态与发布（「随时可回 SWF」纪律不变——`swf` 档行为逐字节不变）。
0. ✅ **第 136 轮：第 135 轮修复产物 P2 复跑收口（4/4 全 PASS）+ 眼睛达成（玩家确认第 8
  tab「可接任务」+ 真实任务列表）—— P3-a 完全收口**；「随时可回 SWF」纪律不变
  （明细 = 顶部块 / `docs/15` 十三·补二）：
  ① 会话（19:55~19:59 / 184000 ms / P2 实验态 · 原版 SWF / 4 用例 35 步）= **4 PASS /
  0 FAIL / 0 SKIP**；`r133_inject_data` **PASS** 32063 ms —— 第 135 轮修复**一击生效**，
  五段判据与预测**逐字一致**（`上下文=ok` / `快照=ok（1 条）` / `扩tab=ok（7→8）` /
  `注入数据=ok（1→206，期望 206）` / `对账=ok（0x002C5401:超越极限｜可导航1）` /
  `恢复=ok（→1，期望 1）` / `再注入=ok（→206，供眼睛）`）；关键诊断行 = `切到我们的 tab
  （idx=7）→ mask=0x40 + 注入 206 条（ok）`（修复前 =「写入失败！」）+ `从我们的 tab 切走
  （idx=0）→ 恢复引擎列表（ok）` ⇒ **分时注入闭环产品级成立**；
  ② 判据五连全绿 = `check_results` **4/4** + `log_hygiene` 退出码 0（135 行 = 122 I +
  13 W + **0 E**）+ `plan_regex_audit`（P2）**10/10** + `verify --dev --p2`
  **800 行 / 0 MISS** + 离线层 **5 步全过**；DLL **1142272 B**（工作区 == 部署）；
  ③ ★ 下一步 = **P3-b**（完整 `SaqDescriptionText` 文案迁移 + watchdog + `UiMode` 运行期
  开关）⇒ P4 交互接管（按钮劫持 + activate 拦截 + 星图交接/真关菜单原语）⇒ P5 双形态与
  发布；「随时可回 SWF」纪律不变（P2 实验态当前保留 —— 眼睛确认后再恢复常规部署）。
0. ✅ **第 135 轮：第 134 轮修复产物 P2 复跑（3/4）+ 第二处产品级缺陷修复（`InjectCtx::list`
  未接线）** —— 明细已并入 `docs/15` 十三·补；**已复跑 = 第 136 轮（4/4 全 PASS）**。
0. ✅ **第 134 轮：第 133 轮产物 P2 会话实机检查收口（3/4 —— r133 FAIL）+ 第一处产品级缺陷
  修复（已复跑 = 第 135 轮）** —— 真因 = `TryPushPending` notOurs 分支
  `g_pending.quests.clear()` 清掉注入唯一数据源（`ui.inject` 提前返回「待推送=0」）；
  修复 = 数据生命周期解耦（生命周期 = 一次菜单打开周期；4 个推送子分支不再清数据 ——
  `SAQ.cpp` 注释 `第 134 轮`；DLL 1141248 B + verify 798 行）。
0. ✅ **第 133 轮：P3-a 产品化 PoC 落地（真实数据注入）—— 已实机收口（第 136 轮 4/4；
   缺陷明细见第 134/135 轮）**
  （判读表 = `docs/15` 十三节 / 顶部第 133 轮块）：
  ① 产物：新模块 `plugin/src/SAQ_UiInject.{h,cpp}`（注入层独立；GFx 安全调用工具自带一份）
  + `ui.inject` op（harness）+ P2 用例 `r133_inject_data`（+ 眼睛窗口 30 s）+
  verify +8 特征串 / +5 计划形状 + 正则自检 9 条 / 6 样例；
  ② ★★ 链路 = 环境 → 语言（U8a 同源）→ 回 $ALL → 引擎快照（`GetDataForEntry` 引用数组）→
  构造真实条目（与 SWF 推送**同一份数据**）→ 扩 tab 7→8 → 挂 priority=100 拦截 →
  切 7（拦截 + mask + 注入）→ 对账（前 2 条 uID/名字/置灰）→ 切 0（恢复快照）→
  再切 7（供眼睛）→ 一行汇总；
  ③ ★ 本轮新事实：原版切 tab **只设 mask 不重建列表** ⇒ 分时注入两边都自己做；
  第 8 个 tab 必须拦截（`FilterInfoA[7]` 越界 TypeError）；tab flag=1<<6（配 iType=6）；
  `$ALL` 用原版 0xFFFFFFFF（切走即恢复）；原版 7 项 flag 映射照 SWF 版 PopulateTabs；
  ④ 判据（离线）：DLL **1141248 B** + `verify --dev --p2` **797 行 / 0 MISS** +
  离线层 **5 步全过** + P2 正则 **9/6**；**待实机 = 4 条用例 + 眼睛**（真实列表）；
  ⑤ ★ 之后：**P3-b**（完整 `SaqDescriptionText` 文案迁移 + watchdog + `UiMode` 运行期开关）
  ⇒ P4 交互接管（按钮劫持 + activate 拦截 + 星图交接/真关菜单原语）⇒ P5 双形态与发布。
0. ✅ **第 117~132 轮（UI 注入研究 → P2 全过程）—— 速查**（逐轮明细已迁 `docs/15`
  与 `docs/90` / `docs/91`；第 140 轮瘦身，原文见 `docs/91` 末尾附录）：
  **132 / 131 / 130** P2 会话全 PASS + 探针 v4（U5 回调劫持 / U6 直读 / U7 就地刷新 /
  U8 语言判定 / U10 类通道）+ 两处探针修正（`[ud]` 数组接线 / 关菜单入口判据）；**129**
  功能迁移评估（注入可行 ⇒ 立项 P3）；**128** operator 二期；**127** HUD 提示时机修复 +
  P2 双收口 + 0.1.15 包；**126** 正常态 43/43 全 PASS；**125 起** 路线 D 冲突检测
  （`ProbeChannelIdentity` + 停推降噪 + HUD 提示 GLOB 0x80E）；**124~121** P2 眼睛达成 /
  复跑 / 首跑（注入缺 `aObjectives` 真因）/ P2 落地（探针 v3 + `build-saq.ps1 -P2`）；
  **120 / 118** 42-42 · 41-41 全 PASS；**119 / 117** 探针 v2（拦截 + `filterMask` 哨兵写 +
  `SetTabsData` 补测）+ UI 注入研究落地（能力边界 U0 ✅ / U1 ❌ / U3 ✅）。

0. ✅ **第 87~116 轮的收口项与上传包记录（明细已迁 `docs/93`）** —— 0.1.6 ~ 0.1.13 上传包
  （第 87/88/94/97/100/105/108/113/116 轮）/ 第 105 轮 30/30 / 第 109 轮（大项 A+B：7 条补收 +
  operator 盘点）/ 第 110 轮追踪者联盟收口 / 第 111~116 轮检查收口等；各包 SHA256 与
  发布 DLL 尺寸见「Nexus 上传包」行。
0. ★★★ **下一步大项候选**（按建议优先级）：
   ① ~~**重打上传包 0.1.7**~~ → ✅ **第 88 轮已完成**（`dist\SAQ-ShowAvailableQuests-0.1.7.zip`
   1,839,484 B / SHA256 `1F08EB31…EC8B25F`，含第 87 轮 OR 组；**可上传 Nexus**）；
   小提醒：`verify` 跑在压缩前 ⇒ 它查的总是「上一个包」，新包要补跑一次 verify 覆盖
   （第 88 轮已补 / 全过；已在「Nexus 上传包」行记录）；
   ★★ ~~「可重复任务」全量盘点（第 89 轮）~~ → ✅ **第 89 轮落地 15 条 + 第 90 轮补
   DLC 5 条 = 20 条**（SFFL 家族；三条「未收」已定案不收 —— 见 `docs/11` 第 90 轮一节）；
   ② ~~**自动测试「虚拟桌面 / 独立会话」后台跑**~~ → ❌ **选题池已移除（2026-09-23
   玩家决定）**：**以后启动游戏都由玩家亲自操作** —— 不再考虑后台 / 独立会话 / 虚拟桌面
   方案（自动化止步于「构建 → 部署 → 玩家启动游戏（自动读档）→ 游戏内 harness 自动跑
   用例 → 离线判读」；`docs/09` 九-3 已同款标注）；
   ③ ~~**operator 语义继续产品化**~~ → ✅ **第 128 轮**（`!=`/`>`/`>=`/`<`/`<=` 折叠：
   内核 `tools/esm/ctda_ops.py` + 两条管线接线；表内零变化 —— `docs/08` 4.7）
   → ✅ **第 145 轮（A3）**：**cmp∉{0,1} 也折叠产品化** + type 解析改回低字节（修缺陷）+
   tripwire 三类报红（`docs/08` 4.8；表内零变化、8 产物哈希逐字节一致）。
   **余下**：flags 非 OR（别名 / GLOB / Pack / 交换）—— 表内外**外键形态**实测 0 条
   （INFO 侧 7 条别名全为自引用；记录级唯一特殊样本 = SFTA00 自引用）⇒ **有意不做**
   运行期通道，tripwire 报红盯数据（出现才立项）；
   ④ 低优先增强：「待生效」在列表里可见化 / 候选排序引入玩家位置因子 / 仍无兜底的
   20 条任务补常驻 XMarker；
   ⑤ 低优先：`City_Neon_Chem03` 供应线路的「任务不 Stop + 两个结局」结构值得日后
   实机确认一次（本轮按「无重启机制 ⇒ 不收」处理；若实测能重复接，再补进清单）。
0. ✅ **眼睛判据（验收项 —— 全部完成：2026-09-23 玩家确认）**：
   * ★ **已肉眼确认（第 97 轮）**：可重复条目带「（可重复）」前缀 + 整组排在列表末尾（无异常）；
   * ✅ 同伴固定条目（如「巴雷特：违约」）⇒ 描述首句 = 「…好感度达到一定水平后自动开始」、
     末尾 = 「…引导到这位同伴的位置」—— 已确认；
   * ✅ 列表里 10 条「XX地标任务」；选中一条描述第一句 = 「拾取《…》即可接取（地点）」—— 已确认；
   * ✅ 任务板条目不带「（可重复）」前缀（前缀只在可重复任务与 NPC 入口上）—— 已确认；
   * ✅ 第 91 轮：**不可导航的条目名字带「（不可导航）」前缀**（「（不可导航）深藏不露」+
     「（不可导航）伦敦地标任务」；选中它 ⇒ 右侧描述仍说「该任务暂无导航目标」、提示语里
     **没有**前缀）—— 已确认；
   * ✅ 第 116 轮：19 条 world 级兜底任务（远处点引导**立即生效**、描述无「需靠近」句，
     名单 = `docs/14` 八·3 节）+ 追踪者联盟三项（赏金狩猎 / 总部板 / 总部引导蓝点，
     `docs/13` 第六节）+ 第 106 轮补收 7 条（点击有蓝点）—— 已全部确认；
   ⇒ **剩余验收项清零**（可进入 0.1.14 打包）。



0. **更早的收口与约定（第 16~64 轮 / 发布与打包）—— 已迁 `docs/87`**（速查：改 SWF 后必须先构建部署再启动游戏 / 发布就绪 `-Release`+`package-saq.ps1` / 自动读档 / 离线层 / SET COURSE 星图判据见 `docs/05` / 门槛链见 `docs/08` / 打包 + Nexus 文案）。

## 十二、已知风险

| 风险 | 说明 |
| --- | --- |
| **「已开始」≠「已接取」**（第 11 轮实证） | 引擎会把「玩家仍能接到」的任务提前置成 running（RAD05 全数到期，flags=0x8001）。TESQuest 的 started 位**不能**当「玩家已接」的判据；只有玩家任务日志（QuestData）算数 |
| **GLOB（float）存不下完整 FormID**（第 21 轮踩过） | GLOB 是 float（尾数 24 位）：完整 FormID > 2^24 后**奇数不可精确表示**（±1）。已把引导目标拆成「低 24 位（0x801）+ 高 8 位（0x805）」两个 GLOB。**以后再开 float 通道，先问「值会不会超过 2^24」** |
| **Papyrus 定时器在菜单开着时不走**（第 27 轮定案） | 任务菜单暂停游戏 ⇒ `StartTimer` 不触发：三次会话的 `引导已应用` 都精确落在「菜单关闭」时刻，菜单开着 27~37 秒零应用。**不要设计「菜单开着时脚本会处理」的功能**；引导总是「关菜单生效」（与原版 HUD 交互一致）。DLL 侧也据此不再在菜单开着时轮询确认（降噪） |
| ~~任务板入口多为非常驻引用~~（第 27 轮发现 → 第 28 轮兜底 → 第 29 轮 override **被实机否定** → **第 30 轮新建常驻引用治本**） | 12 条入口里只有阿基拉城是原生常驻 ⇒ 其余 11 条在 cell 未加载时 `Game.GetForm` 取不到。★ **第 29 轮的 override 路线不成立**：override 只换数据、**不改变引用的加载分类**（官方 70 条同类 override 原记录本来就全是常驻 = 零先例；实机 12:45 会话 `可导航 2`）。第 30 轮改为**新建** 11 条常驻 XMarker（0x900~0x90A，位置 = 板坐标）+ 候选链。★ **第 33 轮补齐最后一块**：官方对同一条 cell（SFBGS003 → 霓虹城）的写法 = **空壳 CELL 记录（EDID + flags 0x4000）+ 紧跟 CellChildren 组** —— 我们也照抄了（11 条，xEdit 验证通过；**待实测 `marker 11`**）|
| ~~override 世界引用的冲突面~~（第 29 轮引入 → **第 30 轮已消除**） | 第 29 轮对 11 条任务板 REFR 的 override 已全部移除 ⇒ 不再覆盖任何基础游戏记录。第 30 轮只往那些 cell **新增**自己的记录（mod 加物件级操作），与改同一 cell 的 mod 无冲突 |
| ~~打开任务菜单偶发长停顿~~（第 11 轮发现 → 第 12 轮修复 → 第 16 轮确认） | 根因：失败解析路径里 `IsMapped`（VirtualQuery）累计 ~3.9 秒；`SafeRead` 改纯 `__try` 保护后消失。第 16 轮日志实证：收集 0 ms / 失败 0 ms / **无「主线程停顿」行** |
| **界面状态必须由回写驱动**（第 16 轮踩过） | `SaqToggleGuide` 只能在本地立即切状态，而「这条任务有没有引导目标 / 实际在引导哪条」只有 DLL 知道。凡是「界面先行、DLL 后处理」的动作都必须有回写通道（`SAQ_GuideReply`），否则界面会说谎 |
| **GLOB 锚点有次数窗口**（设计边界） | `SAQ_Notify` = 7777 + 菜单打开次数，认领窗口 7777..8776（1000 次）。单次会话极难超限；脚本重挂会把它重置回 7777（DLL 认领成功后不再读锚点，无影响） |
| **文本子记录必须带 NUL**（第 12 轮实证） | 非本地化 ESM 的 FULL/NNAM 等文本子记录，长度 = 字符数 + 1（含 NUL）。第 11 轮漏写 NUL ⇒ HUD「任务更新」显示 `[…]` |
| commonlibsf 的 `REL::ID = 0` 接口 | 已踩（BGSStoryTeller、`TESQuest::IsStageDone`）；编译期 ID 审计挡着（`docs/01` 坑 8） |
| **commonlibsf 的结构体偏移一律不可信**（第 18 轮升级为通则） | 已踩四次，其中 TESDataHandler 两次：`formArrays` 读到空表（第 5 轮，见 docs/02）、`files` 读到空链表（第 17 轮，四个 master 全被误判「未加载」⇒ 第 18 轮改前缀探测）。读引擎对象字段：先找「引擎自己怎么读它」（反汇编），或改用已实测可靠的引擎函数（`LookupByID` 等）；`TESGlobal::value`（0x48）是上一代项目修正过的 |
| **自己写的注释/文档也可能是错的** | 已踩：哈希指针、模糊 RTTI、误信「位16 = 自动启动」。结论要留原始反汇编/日志，且「改完要能被验证」 |
| **GFx 返回值要按类型读** | `GetNumber()` ≠ 万能（见 `docs/04` 2.4） |
| **TESQuest 运行时偏移来自反汇编** | `0x114 / 0x32C / 0x338` 有虚表核验 + 识别率安全阀；换游戏版本要重跑 `tools/re/quest_api_scan.py` |
| **非本地化插件的文本是内联的** | 本 ESM 的 TES4 flags = 0x01（无 Localized 位）⇒ 目标文本可以内联写 `<Alias=SAQ_GuideTarget>`，不需要动游戏的 strings |
| **非常驻引用** | 168 条引导目标里 68 条是非常驻引用：格子没加载时 `Game.GetForm` 可能取不到（日志会写 `状态=2`） |
| **patch 文件集 ≠ 源码目录** | 改任何 `.as` 都要在 `build-saq.ps1` 的同步清单里登记（普通 + lrg 两套）；lrg 源码由 `make_lrg_source.py` 生成 |
| 静态过滤是启发式 | 宁缺勿滥；规则集中在 `filter_reason()` 一处 |
| 语言判定 | AS3 侧按引擎推来的任务名判定（中文/英文）；C++ 侧 INI 值只作日志 |
| 菜单关掉后 Movie 会销毁 | 已处理：每次「菜单由关变开」都 `UI::Reset()` 清解析缓存 |
| 性能 | 菜单打开时：202 次 `LookupByID` + 202 次标志位读取 + 1~2 次 0x4000 字节扫描；开着菜单时每 500ms 一次 `SAQ_Report`、每 100ms 一次 `SAQ_PeekGuide`。实测无感 |
| **树形列表不能按「数据下标」刷新**（第 14 轮踩过） | 显示下标 ≠ 数据下标（`InitializeEntries` 会把已完成 / misc 条目挪位、展开时插入子项），而且 `BSScrollingList` 没有 `UpdateEntry` 接口 —— 第 13 轮那次调用每次抛 `Error #1006` 被 `try` 静默吞掉。就地刷新一律走 `MissionsList.SAQ_RefreshQuestRow(uID)`（按 uID 在当前显示列表里找行） |
| **DLC 的字符串表是各自一份**（第 17 轮） | 任务名要在**该插件自己的** `<master>_{语言}.strings` 里查（破碎空间在 `ShatteredSpace - Main02.ba2`，地球舰队在 `SFBGS050 - Main.ba2`）。漏抽的症状是「DLC 任务全部消失」（取不到名字 ⇒ 被当成内部任务过滤），而不是报错 |
| **medium / blueprint 档位的 FormID 方案未验证**（第 17 轮） | 本项目实测的四个 master 都是普通插件（fullIndex<<24）。light/ESL（`0xFE000000 \| smallIndex<<12`）已实现但**本机没有样本可验**；medium / blueprint 直接标记不支持并整批跳过（`SAQ_Masters::ResolveOne`），宁可少收也不错收 |
| **master 名大小写不一致**（第 17 轮） | ESM 的 MAST 里写的是 `starfield.esm`，而文件夹里是 `Starfield.esm`；`Masters::FindLoadedFile` 用大小写不敏感比较（日志里会打出实际匹配到的名字） |
| **内嵌回退数据只有基础游戏条目**（第 17 轮） | `SaqEmbeddedPayload.inc` 必须写运行期 FormID，DLC 的离线算不出来 ⇒ 只放 Starfield.esm 的 202 条。C++ 推送成功前的那 0.3~0.8 秒里看不到 DLC 条目（可接受的降级） |
