# 99 · 当前项目进度

> 最新更新：2026-09-20（**第 48 轮：判据收口 + 大项 D —— 「进度没到不显示」扩到对话侧（INFO 门槛）** ——
> 起因：上轮日志复查（20:41~20:47 会话，见下）确认第 47 轮判据实测通过，同时抓出两件事：
> ① **判据过宽**：Mode=6 / 描述提示的「首选候选非常驻」覆盖 158/209 条（实测推送 155 条、
> 76% 的任务都带「需靠近」提示，噪音化）⇒ 回退为「**全部**候选都非常驻」（20 条 =
> 远处点真会待生效的那一类）；② 大项 D 数据侦查完成。本轮实现：
> ① 判据回退（`AllCandidatesNonPersistent`；ini 说明/文档/verify 同步）；
> ② **大项 D：INFO 门槛（对话侧条件）** —— 全表扫对话（2077 QUST / 126347 INFO / 8541 带条件）：
>    按「自引用」分成入口类（含自己==0）/ 中性类（无自引用）/ 推进类（含自己==1，排除），
>    提取「引用别的任务」的进度条件，合成 **60 条任务 / 290 条对话 / 341 条条件**；运行时
>    「全部参与对话都有已知假条件 ⇒ 隐藏」（成对条件自动豁免 + 「引擎已开始」交叉验证 WARN）；
>    两例与记录级门槛完全一致（大器晚成↔孤立无援、亲爱的姐妹↔FFConstantZ05）；
> ③ E 小项：认领后 8 秒复算宽限期（读档抖动）、「入口引导目标更新未被脚本确认」去「入口」、
>    星图等待文案（「还开着：无」→「其它打开的菜单：…」）、界面「当前引导:?」与「0xNaN」。
> 构建/部署/verify（+22 条检查）全过。★ **同日实测（21:45~21:47 会话）大部分通过**：
> 读档认领**无复算抖动** ✓、`Mode=6` 推送 **19 条**（≈20）✓、INFO 门槛
> `58(过37/藏19/放行2/未知0)` ✓、星图链路两次完整 ✓、新文案全部生效 ✓；
> 交叉验证（设计内的 WARN）**抓出 2 条误藏**（『全数到期』RAD05 —— 第 11 轮验证过
> 「玩家能接到」的案例）⇒ **同日补丁：引擎已开始 ⇒ 放行**（隐藏条件收窄为
> 「引擎没启动 + 全部对话都有已知假条件」）。详见一·补三十三 / `docs/08` 第九节）
>
> 上一轮：2026-09-20（**第 47 轮（大项 C）：引导可用性收口 —— 「同 cell 常驻兜底」候选** ——
> 起因是玩家实测「营救机器人」的报告：「导航点被锁定在亚特兰蒂斯城的同一个地铁上、导航基本没用」。
> 两份日志取证：该任务（候选池只有 1 个：新亚特兰蒂斯城 The Well 内景里的损坏机器人，非常驻）
> **从未生效过**（玩家连点 30+ 次全部「目标尚未加载」），而世界里的标记**仍是上一条生效引导的残留**
> （脚本取不到时不改别名，见 `ApplyGuide`）⇒ 玩家观感 =「点了没反应、标记锁死在某处」。
> 数据侧的决定性发现：机器人 **3.3 米**处就有**常驻**的任务装置（`0x0208ECA6`
> 「损坏 Model G 家具标记」）却没被用上 —— 旧候选生成只从任务别名的引用里选。⇒
> ① 数据：`gen_guide_targets.py` 新增 `find_fallbacks`（第三遍扫描：为「全部候选都非常驻」的
> 任务在候选目标所在 cell 里找**最近的常驻引用**，作为候选池**最后一位**兜底；**76 条里 56 条**
> 补到，候选总数 875 → **931**、常驻 283 → **339**）；② 表：自动切片带上；③ DLL：判据
> `AllCandidatesNonPersistent` → **`FirstCandidateNonPersistent`**（只看**首选**候选 —— 兜底是
> 常驻的，不能把这一类判没；描述提示与测试模式 6 的 76 条清单照旧）。行为：远处点引导
> **落到兜底**（蓝点落在 The Well 的机器人附近、不再「没反应」），走进 The Well 后由「候选复算」
> 自动升级为精确目标。构建/部署/verify 全过（3 条新检查，含「营救机器人兜底链
> 0x08ECA5 非常驻 → 0x08ECA6 常驻」）。
> ★ **已实测通过（20:41~20:47 会话，日志侧全绿）**：
> ① 远处点「营救机器人」⇒ `引导请求：…｜候选 [2/2]「Ref_08ECA6」` → `引导已生效 脚本状态=1`；
> ② 走进 The Well ⇒ `引导目标已更新（候选复算）：0x0308ECA6 → 0x0308ECA5（[2]→[1]「G型」）`；
> ③ R ⇒ 星图 1.6 秒打开（第 1 次尝试）、Papyrus 地点链完整（采用地点 = SAlphaCentauri_PJemison）；
> ④ 接取后自动取消引导（`已接取，自动取消引导` → `引导已清除`）。
> 复查顺手排除一项「疑似 mod 问题」：Papyrus 里 26 条 `RevertToOpenStateOnCellLoad`
> 报错 = **官方数据/脚本的既有问题**（全游戏只有 The Well 的 29 条 ref 挂该脚本，其中
> VMAD 属性为空的 26 条会报错；与我们的 CELL 空壳无关）。详见一·补三十二 / `docs/05` 第二十一节）
>
> 上一轮：2026-09-20（**第 46 轮（大项 B）：引导可用性 + 提示收口 —— 「有目标、但此刻取不到」** ——
> 起因是日志复查抓到的实测形态：玩家在远处点「营救机器人」（候选池只有 1 个、且非常驻）⇒
> Papyrus 连报 5 次 `引导失败…取不到引用`、DLL **19 秒后静默放弃 + 界面回滚**，玩家体感
> 「按了没反应，过一会儿自己取消了」—— 与「不可引导要提示玩家」的需求冲突。数据侧统计：
> **209 条有目标的任务里 76 条的全部候选都非常驻**。⇒ ① 数据/推送：`QuestEntry.needsApproach`
> （运行时由候选持久位算出）+ 载荷**第 6 列**；② 界面：描述**提前**写明「导航目标要靠近目标区域
> 后才加载」，并新增结果码 **5 = 目标尚未加载**（保持竖条、不回滚、不播 OFF 音、不关菜单）；
> ③ DLL：点引导时全不可得 ⇒ `approachPending`，不安排 19 秒确认、不开星图，菜单关着时按
> **10/20/40/60 秒退避慢速重试**（≈35 分钟）—— 玩家靠近、cell 一加载即 `引导延迟生效`；
> ④ 脚本：HUD 提示机制通用化（`ShowNotice`），第一次失败发「目标地点尚未加载，暂时无法导航
> —— 靠近后会自动生效」（重复 3 次、冷却 5 分钟）。构建/部署/verify 全过（SWF 5 + DLL 6 + PEX 5
> 条新检查），**待实测**。详见一·补三十一 / `docs/05` 第二十节）
>
> 上一轮：2026-09-20（**第 45 轮（大项 A）：引导质量收口 —— 引导目标升级为「候选池」+ 质量分级** ——
> 玩家实测抱怨的两条（「群众心态」被引到 `FFNeonZ08_HeadlockEnableMarker001`、「平衡账目」被引到
> `FFNeonZ09_EnableRef`）的真因：旧排序「常驻优先」把**有名字的 NPC 发布者**压住了（31 条任务同形态）。
> ⇒ ① 数据侧：每条任务输出按质量排序的候选列表（有名字的 NPC > 可读名落脚点 > 通用名 NPC >
> 内部名落脚点；同级常驻优先；质量 top 6 + 最多补 2 个常驻备胎）—— **875 条候选 / 209 条任务**，
> **82 条**任务的第一候选被换成更好的目标；② 表/DLL：`kGuideCandidates[]` + 三件套
> （点引导时 `LookupByID` **预选**「此刻可得的最优候选」一次到位 / 脚本报状态 2 时**换候选**
> （循环，≤2 次）/ 菜单关着时**动态复算**（飞近自动升级回 NPC、目标失效自动回退））+ 认领带下标。
> 构建/部署/verify 全过（含 DLL 5 条 + 静态表 4 条新检查），**待实测**；
> ★ 同日补丁（实机日志复查：任务「朱诺的计谋」的星图里没有目标位置 —— 目标在飞船
> **动态创建的内部地点**，星图节点表只登记星系/星球层）：脚本在「地点链无行星」时
> **不打开星图**、改发 HUD 通知；DLL 配套不重试、超时文案改「按预期未打开」（见一·补三十第 6 节）。
> 详见一·补三十 / `docs/05` 第十九节）
>
> 上一轮：2026-09-20（**第 44 轮：R 键「星图位置永远是上一个导航的位置」—— 第 36 轮那条原版 dispatch 其实生效（第 37 轮结论被推翻）** ——
> 玩家反馈「点了阿基拉城任务板，再去点火卫二任务板，那么导向目标才会是阿基拉城」。
> 两份日志对齐（18:34~18:35 会话）锤定：DLL 写完引导通道后 **9 毫秒**星图就在屏幕上
> （`星图：已打开（… R 后约 0.0 秒）`，探测名已是第 41 轮修正过的 `GalaxyStarMapMenu`）
> —— 只可能是 AS3 在按键那一刻同步 dispatch 的 `MissionMenu_PlotToLocation`（它**确实生效**，
> 第 37 轮「引擎无 sink」的离线结论不成立）；而 Papyrus 三次「星图请求：1.5 秒后打开」
> 之后**一次都没有**「打开星图并设定航线」—— 星图被提前打开 ⇒ 星图是暂停菜单 ⇒ 游戏暂停
> ⇒ 脚本定时器冻结 ⇒ 补救调用永远跑不到。那条 dispatch 用的是**代理任务当前的目标位置**
> （此刻脚本还没 ForceRefTo）= **上一次**引导的目标 ⇒ 星图永远滞后一条（也解释了此前的
> 「都导航至沃利阿尔法星」（别名空着 ⇒ 默认焦点）与「有一次打开了其他任务的位置」）。
> ⇒ ① **删掉 dispatch**（AS3 只留音效 + note `星图:交给脚本(菜单关闭后)`）；
> ② 脚本侧星图待办改由**轮询节拍**推进（`StarMapPendingTicks` + `ProcessStarMapPending`：
> 节拍到点 = 菜单已关、游戏在跑，不会被暂停吃掉；旧的 1.5 秒延时定时器退役）；
> ③ DLL 重试等待 3 s → 1.5 s 与脚本新窗口对齐。构建/部署/verify 全过（含 4 条新检查），
> **待实测**。详见一·补二十九 / `docs/05` 第十八节）
>
> 上一轮：2026-09-20（**第 43 轮：任务板条目「按 R 没反应」—— 现场分诊（探针）+ 两处会吞掉现场/请求的缺陷** ——
> 玩家反馈「悬停阿基拉城任务板按 R，导航却导向纳里昂星」（= 旧引导「补给之行 → 星际诊所」还在）。
> 复查 17:54~17:57 会话：**全日志没有一条 `press=[R@…]`、也没有一条 `引导请求`**，而「界面状态」
> 显示玩家反复做「悬停 → 展开 → 悬停子项 → 关菜单」——「按键没进界面」与「事件到了但回调没跑」
> 两种病分不开。⇒ ① **新增分诊探针**：`ev=[XButton↓ XButton↑ …]`（`ProcessUserEvent` 收到的
> user event，名+按下/松开）+ `btn=[our:plot=1:map=0]`（选中项变化时的按钮启用状态）——
> 按 R 一次即可对号入座；② 顺带定位两个**吞现场/吞请求**的缺陷：`菜单关闭：界面最后状态` 原来
> 用 500ms 轮询缓存（按 R 与关菜单同周期时打印的是**按键前**的值）⇒ 关闭时刻改**现场读一次**；
> `PollGuideRequest` 只在菜单开着时跑而关闭时立刻重置 lastSeq ⇒ **「按 R 后菜单立刻关掉」的请求
> 会被丢掉** ⇒ 关闭前**补一次 poll**（日志 `补处理了关闭瞬间的引导请求`）；③ 顺手去掉 SET COURSE
> 的**置灰**（沉默失败 → 改回可点，没目标时点击给提示，符合「不可引导要提示玩家」的需求）。
> 构建/部署/verify 全过（含 5 条新检查），**待实测**。详见一·补二十八 / `docs/05` 第十七节）
>
## 一、当前状态

| 组件 | 状态 | 位置 |
| --- | --- | --- |
| SFSE 插件（DLL） | ✅ 已构建（多 master 前缀探测 + 引导结果确认 + 认领已有引导 + 脚本活性探测 / 引导未生效收尾（第 19 轮）+ 控制台测试过滤（第 20 轮）+ 目标 FormID 拆分 / 自检上限修复（第 21 轮）+ 确认窗口改绑菜单状态（第 26 轮）+ **无限任务入口条目表 / 引导确认与停顿降噪**（第 27 轮）+ 入口引导候选链（第 30 轮）+ **候选链改「精确优先」（任务板自身）+ 引导目标动态更新 + marker 探测**（第 31 轮）+ **菜单关着时例行认领 + 通道对账**（第 32 轮）+ **菜单关着时预建静态表 / `Masters::Resolved()`（认领不再因表没建好而误判）**（第 33 轮） + **SET COURSE 星图（状态 5 + kHide 关菜单 + 星图探测）**（第 37 轮） + **星图自动重试 / 地点候选状态 5·6·7 / 引擎节点解析只读诊断（第 40 轮）** + **星图注册名修正 `GalaxyStarMapMenu` / 节点解析器特征字节修正（第 41 轮）** + **菜单关闭时现场读界面报告 / 补一次引导请求轮询（第 43 轮）** + **星图重试等待 3 s → 1.5 s 与脚本新窗口对齐 / 日志写明星图唯一入口（第 44 轮）** + **引导目标候选池（第 45 轮：预选可得候选 / 状态 2 换候选 / 菜单关着时动态复算 + 认领带下标）** + **引导可用性/提示收口（第 46 轮：`needsApproach` 判定 / 结果码 5 / `PollApproachRetry` 退避重试 / 星图本次不打开 / 测试过滤新增 `Mode=6` + 上限常量修复）** + **引导可用性收口（第 47 轮：同 cell 常驻兜底）+ 判据收口 + 大项 D（第 48 轮：`AllCandidatesNonPersistent` 判据回退（20 条）/ `EvaluateInfoGates` INFO 门槛 / 认领后 8 秒复算宽限期 / 星图与确认文案修正 + 同日补丁：**引擎已开始 ⇒ 放行**）**） | `plugin/build/windows/x64/releasedbg/SAQ_ShowAvailableQuests.dll`（782336 B） |
| 静态任务表 | ✅ **261 条 / 4 个 master**；★ 第 45 轮引导目标升级为**候选池**：`kGuideCandidates[]` **931 条候选 / 209 条有目标**（★ 第 47 轮：为「全非常驻候选」的 76 条任务里 56 条补了**同 cell 常驻兜底**（+56 条、常驻 339 条）；`candBegin/candCount` 切片；旧单目标字段已移除）；★ **第 48 轮（大项 D）新增 `kInfoGroups[]` / `kInfoConds[]`（INFO 门槛：60 条任务 / 290 条对话 / 341 条条件 / `infoGroupBegin/infoGroupCount` 切片）** | `plugin/src/SAQ_QuestTable.h` |
| 入口条目表（第 27/28/29 轮） | ✅ **12 条任务板**（新亚特兰蒂斯/阿基拉城/霓虹城/塞多尼亚/霍普镇/新家园/陋室/龙神集团/三家星船厂/星钥站）；uID = 引导目标 = 任务板引用；ini `Mode=5` 只显示它们；★ **第 30 轮：引导目标 = 候选链**（① 新建常驻 XMarker（记录号 0x900~0x90A，精确落点）→ ② 任务板引用自身 → ③ 同 cell 原生常驻引用兜底），DLL 依次 `LookupByID` 取第一个命中的 + 诊断日志；第 29 轮 override 路线被实机否定（见一·补十五） | `plugin/src/SAQ_EntryTable.h`（`tools/esm/gen_entry_table.py`）；清单 `ref/entry_targets.json`；新建 marker `tools/esm/create_board_markers.py`（产物 `ref/board_markers.json`）；侦查 `tools/esm/scan_entry_persistent.py`；先例核查 `tools/esm/check_persist_precedent.py` |
| 运行时过滤 | ✅ **只挡「已完成」**；「已接取」只认玩家任务日志；**master 没加载 ⇒ 整批跳过** | `plugin/src/SAQ_QuestState.*` / `SAQ_Masters.*` |
| 进度门槛（第 35 轮） | ✅ **7 条任务 / 9 条条件**（「进度没到不显示」首版）：记录级条件里「引用别的任务」的 GetQuestRunning/GetQuestCompleted/GetStageDone；求值不了 ⇒ 放行；ini `[Filter] ProgressCond` 可关；日志 `进度门槛=7(过A/藏B/未知K)` + `进度没到:` 名单。**已实测通过**（14:47 会话：过3/藏4/未知0，见一·补二十第 6 节与 `docs/08` 第七节） | `plugin/src/SAQ_QuestCond.*`；数据 `ref/ctda_gates.json`（`tools/esm/analyze_ctda.py` → `gen_quest_table.py`） |
| ★★ INFO 门槛（第 48 轮 · 大项 D） | ✅ **60 条任务 / 290 条对话 / 341 条条件**（「进度没到不显示」第二判据，对话侧）：任务自己的对话（INFO）里「引用别的任务」的同类条件；按「自引用」分成入口类/中性类（排除推进类）；**全部参与对话都有已知假条件 ⇒ 隐藏**（成对条件自动豁免）；求值不了 ⇒ 放行；★ **实测通过（21:46 会话：命中 58 条 / 藏 21 / 放行 2 / 未知 0，名单抽查合理）**；★ 同日补丁：**引擎已开始 ⇒ 放行**（判据收窄为「引擎没启动 + …」，修掉『全数到期』类误藏）；ini `[Filter] InfoCond` 可关；日志 `INFO门槛=…(过A/藏B/放行C/未知K)` + `INFO没到:` 名单 | `plugin/src/SAQ_QuestCond.*`（`EvaluateInfoGates`）；数据 `ref/info_gates_final.json`（`tools/esm/scan_info_gates.py` → `analyze_info_gates.py` → `gen_quest_table.py`） |
| 引导数据 | ✅ 209/261（80%）；★ 第 45 轮：**候选池 + 质量分级**（第一候选质量等级 1=204 / 2=3 / 4=2；**82 条**任务的第一候选被换成更好的目标 —— 含玩家实测抱怨的「平衡账目」「群众心态」）；★ 第 47 轮：**同 cell 常驻兜底**（`find_fallbacks`；931 条候选 / 常驻 339 条；「营救机器人」= `0x08ECA5` 非常驻 + `0x08ECA6` 常驻兜底链）；**无目标的 52 条已归因**（第 24 轮，结论：不补猜的目标） | `ref/guide_targets.json`（工具见 `docs/05` / `docs/06`；归因见 `docs/07`；持久性统计 `tools/esm/ref_flags_report.py`） |
| 代理任务（ESM） | ✅ 已挂 Reference Alias + Objective + 测试开关 / 目标高位 GLOB（0x804/0x805，第 20/21 轮，VMAD 已绑定）（手工字节补丁，幂等）；★ **第 30 轮：+11 条新建常驻 XMarker 引用**（记录号 0x900~0x90A，`Top 'CELL' > … > CellPersistent`）；★ **第 33 轮：+11 条 CELL 记录（官方「空壳」写法 = 只留 EDID + flags 0x4000，紧跟各自的 CellChildren 组）** —— 官方对同一条 cell（SFBGS003 → 霓虹城）就是这么写的；xEdit 验证 11/11 + 重存不丢；ESM 3521 B | `esm/SAQ_ShowAvailableQuests.esm`（`tools/esm/patch_saq_esm.py` + `create_board_markers.py`；结构对照工具 `tools/esm/dump_cell_group.py`） |
| 引导脚本 | ✅ 已编译（`StartTimer` 轮询 + `ForceRefTo` + `SetActive` + OnInit 重挂自愈 + 菜单事件 Trace + 菜单打开重挂定时器 + 三个地点候选（第 40 轮）+ 星图请求失效校验（第 42 轮）+ **第 44 轮：星图待办改由「轮询节拍」推进**（`StarMapPendingTicks` + `ProcessStarMapPending`；旧的 1.5 秒延时定时器退役 —— 实测它被「星图提前打开 ⇒ 暂停」冻死，一次都没跑到；节拍到点 = 菜单已关、游戏在跑）；★ 第 27 轮定案：菜单开着时定时器不走，引导只在关菜单时应用 —— 与原版一致）+ ★★ 第 46 轮：HUD 提示机制通用化（`NoticeText`/`ShowNotice`/`ProcessNotice`，替代星图专用版）+ 「目标尚未加载」提示（重复 3 次、冷却 5 分钟）+ 失败 Trace 节流（前 3 次 + 每 10 次） | `scripts/SAQ_Main.psc` → `Scripts/SAQ_Main.pex`（12064 B） |
| SWF 内嵌回退数据 | ✅ 7.1 K 字符载荷（**只含基础游戏 202 条任务**；入口条目不在内嵌里，降级窗口 ~0.3s 可接受） | `ui/missionmenu/saqdata/SaqEmbeddedPayload.inc` |
| UI（missionmenu.swf / _lrg） | ✅ 已重编译（+ 接取后自动取消引导 + 结果码 4 回滚 + drop= 名单（第 19 轮）+ 任务板入口文案（第 27 轮）+ **第 29 轮：「太远」文案退场** + **第 37 轮：R 传 `map=1/0` 标记（peek 三段协议）** + **第 38 轮：R 成功后界面侧走原版 `CloseMenu(true)` 关掉整个暂停菜单（peek 四段协议）** + **第 39 轮：R 不再取消引导（`SaqToggleGuide` 第三参数；重复按 R = `重复设定航线:` + 再开星图）** + **第 42 轮：报告新增 `press=[…]` / `sel=[…]`（按键那一刻用的是哪一行 + 列表选中项，怀疑「R 选错条目」时的硬证据）+ R/Enter 两条路都先取一次 `selectedEntry`（悬停到分隔行不再抛异常）** + **第 43 轮：分诊探针 `ev=[…]`（`ProcessUserEvent` 收到的 user event，名+↓/↑）/ `btn=[our:plot=1:map=0]`（选中项变化时的按钮启用状态）—— 「按 R 没反应」一次定性；SET COURSE 不再置灰（沉默失败 → 可点 + 有提示）** + **第 44 轮：删掉原版 `MissionMenu_PlotToLocation` dispatch（星图位置滞后一条的病根；换成 `SaqNoteStarMapHandoff`：只留音效 + note `星图:交给脚本(菜单关闭后)`）** + **第 46 轮：描述新增「导航目标要靠近目标区域后才加载」+ 结果码 5「目标尚未加载」分支** + **第 48 轮：诊断标签的「0xNaN」→「(子项)」；状态栏名字查不到时回退玩家任务日志（不再「当前引导:?」）**） | `ui/missionmenu/build/missionmenu.swf`（726586 B / lrg 726866 B） |
| 构建验证工具 | ✅ 产物特征检查（SWF/DLL/PEX；★ 第 30 轮 +ESM 数据侧解析检查「新建常驻 marker 11/11 条」；★ 第 33 轮 +「CELL 空壳 11/11 条」+ 空壳不许写数据字段 + DLL 静态表就绪特征 & 反向检查；★ 第 41 轮 +星图注册名 `GalaxyStarMapMenu` / 节点解析器特征字节（正反向）；★ 第 42 轮 +SWF 的 `press=[`/`sel=[`/`R@`/`E@` + DLL 的 press 抄录与「脚本已应用后才换候选」+ PEX 的星图过期校验；★ 第 43 轮 +SWF 的 `ev=[`/`btn=[` + DLL 的 `补处理了关闭瞬间的引导请求`；★ 第 45 轮 +DLL 5 条 + 静态表候选池完整性；★ 第 45 轮补丁 +DLL 2 条 + PEX 2 条；★ 第 46 轮 +SWF 5 条 + DLL 6 条 + PEX 5 条 + 反向检查；★ 第 47 轮 +静态表 2 条 + DLL 1 条 + 反向检查；★ **第 48 轮 +SWF 1 条（`(子项)` 标签）+ DLL 11 条（`全部候选都非常驻` / INFO 门槛统计与名单 / 误判告警 / ini `InfoCond` 与说明 / 认领宽限说明 / 引导更新未确认新文案 …）+ 静态表 4 条（INFO 结构/计数 290+341/切片越界/大器晚成门槛样本）+ 2 组反向检查（「首选候选非常驻」「入口引导目标更新未被脚本确认」「还开着：」）**；累计全通过） | `tools/ui/verify_saq_build.py` |
| MO2 部署 | ✅ 第 48 轮产物已部署（ESM（11 marker + 11 CELL 空壳，3521 B）/ DLL（781824 B）/ PEX（12064 B）/ SWF（726586 + 726866 B）+ 测试开关 ini（2006 B，Mode 已回 0 + `[Filter] InfoCond`）；日志与配置都落在 mod 目录内） | `D:\Mod Organizer 2\starfield_mods\mods\Show Available Quests (SFSE)\` |
| Nexus 上传包 | ✅ 第 48 轮已重打（含第 45~48 轮全部产物 + 同日补丁）：`dist\SAQ-ShowAvailableQuests-0.1.0.zip`（1,803,247 B，SHA256 `246D583C…6D41B923`）；一键重打 `tools\package-saq.ps1`；文案 `resources\nexus-description.md`（第 48 轮补：**进度没到不会显示**；「需要靠近」提示改为只对 20 条真会待生效的任务） | `dist\` |
| 游戏内实测 | ✅ 第 21 轮 10:39~10:46 通过（连续切换 50 次 + 三种 DLC 目标）；✅ 第 23 轮 10:50 通过（无目标提示）；✅ 第 26 轮 11:12 通过（菜单里停 27 秒无假失败 + 关菜单 0.9 秒生效）；✅ 第 27/28 轮入口链路通过（列表/点击/就近引导成功）；❌ 第 30 轮 `marker 0 兜底 10`（11 条常驻 marker 取不到）→ 第 31 轮修偏移（精确优先 + 动态更新）→ 第 32 轮补「不进菜单自愈」→ 第 33 轮补「静态表预建 + CELL 空壳记录」；★ **第 34 轮复查（14:23~14:26 会话，日志侧全部通过 = 治本成功）**：① 玩家**不进菜单**：`静态表已就绪（菜单关着时预建，认领已有引导）` → `认领已有引导（任务板入口）` → `入口引导目标已更新：…0x0011B754 → 0x00137573（任务板自身（精确））`，Papyrus 14:24:42 真实应用；② `入口=12(可导航 12｜marker 9 原板 3 兜底 0 不可用 0)`；③ 远处点霓虹城/新亚特兰蒂斯城 → 引用 `0x0F000903`/`0x0F00090A`（新建常驻 marker（精确）），Papyrus `引导已应用：SAQ_BoardMarker_21001E (0F00090A)` = **引擎接受 CELL 空壳写法 + 新建常驻引用**；唯一遗留 = 1 行 WARN（读档 VM 冻结期确认窗口耗尽，脚本 1 秒后应用，无害）。**仅剩玩家眼睛最终确认蓝点落板**（详见一·补十九）；★ **第 40 轮待实测**：按 R ⇒ ① `星图诊断（R 请求）` 的地点链每层节点值（0 还是不 0）；② 星图**开起来**（第 1 次尝试，或重试的第 2/3 次 —— 日志写明）；③ 星图聚焦**接取地点所在行星**并画好航线；**同一条再按 R ⇒ 引导不取消、星图再开一次**（判据见一·补二十五第 5 节）；★ **第 41 轮待实测**：按 R ⇒ `星图：已打开（GalaxyStarMapMenu 在屏幕上…第 1 次尝试）`（此前 4 轮全是假阴性）+ `星图诊断` 节点值（特征修正后第一次有真实数据）（判据见一·补二十六第 5 节）；★ **第 42 轮待实测**：按 R ⇒ `引导请求：…｜界面按键行 press=R@0x…@… sel=idx=N:0x…@…`（press 的 uID 必须与请求的任务一致）+ 正常路径**不应**再出现 `星图：第 2 次尝试`（判据见一·补二十七第 5 节）；★ **第 43 轮待实测**：悬停「任务板 · 阿基拉城」按 R ⇒ `界面状态`/`菜单关闭：界面最后状态` 行里应带 `btn=[our:plot=1:map=0] ev=[… XButton↓ XButton↑]`，随后是 `press=[R@0x14d497@…]` → `引导请求：任务板 · 阿基拉城…`（三段任一缺失都能对号入座定位「按 R 没反应」的病灶，见一·补二十八第 5 节 / `docs/05` 第十七节）；★ **第 44 轮待实测**：依次点两条不同的任务板各按一次 R ⇒ Papyrus 每次都应出现 `星图请求：下一个轮询节拍（约 0.5 秒）打开（目标 …）` **且紧接着** `星图请求：打开星图并设定航线 → …（采用地点=…）`（这一行此前从未出现过）+ DLL `星图：已打开（… R 后约 N 秒，第 1 次尝试）`（N ≈ 0.5~1.5）；**眼睛**：星图聚焦的是**刚才按 R 的那条**任务板所在星球（不再滞后一条）（判据见一·补二十九第 5 节 / `docs/05` 第十八节）；★★ **第 44 轮已收口**（玩家 19:0x 会话实测 + 眼睛验收「肉眼观察未异常」；星图由脚本节拍打开、位置不再滞后）；★★ **第 45 轮待实测**：点「平衡账目」/「群众心态」引导 ⇒ `候选 [1/N]「黄」`（有名字的 NPC） + 远处点任务 ⇒ 预选直接落常驻候选（`… 个候选此刻都取不到` 只在全不可得时出现）+ 飞近后 `引导目标已更新（候选复算）`（判据见一·补三十第 5 节 / `docs/05` 第十九节）；★★ **第 46 轮待实测**（**ini `[Test] Mode=6` 会把列表筛到只剩这 76 条「需要靠近」的任务**）：远处点「营救机器人」⇒ 描述写明「需靠近」+ `引导确认：目标尚未加载 —— 保持待生效`（竖条保持、无 OFF 音）+ HUD 提示 3 次 + `引导重试（等待目标加载）第 N 次`（10/20/40/60 秒退避）；靠近目标区域 ⇒ `引导延迟生效` + 蓝点出现（判据见一·补三十一第 4 节 / `docs/05` 第二十节）；★★ **第 47 轮已实测通过**（20:41~20:47 会话）：① 远处点「营救机器人」⇒ `候选 [2/2]「Ref_08ECA6」` + `引导已生效 脚本状态=1`；② 走进 The Well ⇒ `引导目标已更新（候选复算）：0x0308ECA6 → 0x0308ECA5（[2]→[1]「G型」）`；③ R ⇒ 星图 1.6 秒打开（第 1 次尝试）；④ 接取后自动取消引导。P0 未实测项：无兜底的 20 条待生效链路（第 46 轮已验过）。★★ **第 48 轮已实测（21:45~21:47 会话，大部分通过）**：① 判据回退 ✓（`Mode=6` 推送 19 条 ≈ 20、新文案生效）；② 大项 D ✓（`INFO门槛=58(过37/藏21/未知0)` + 21 条名单；抽查合理 —— 『孤立无援』要『新亚特兰蒂斯城有棵树』完成，与玩家日志一致）；③ 顺手项 ✓（读档认领**无复算抖动** ×2 会话、星图等待新文案、星图链路两次完整 R 后 1.5 秒）；④ **同日补丁**：交叉验证抓出 2 条误藏（『全数到期』『陌生人的善意』）⇒ **引擎已开始 ⇒ 放行**；⑤ **待复测**：补丁后这两条应重新出现在列表里、日志 `INFO门槛=…(…/藏19/放行2/…)`；`Mode=6` 的「真会待生效」链路（挑一条远处点引导，看结果码 5 + HUD 提示 + 靠近后生效）；⑥ 「大器晚成」的隐藏 ✓（在 `INFO没到:` 名单里；记录级门槛也名列其中，双重判据） | — |

一键构建（表已生成过时；`-SkipTable` 会跳过 3~6 分钟的 ESM 遍历）：
```powershell
& ".\tools\build-saq.ps1" -SkipTable
```
全流程见 `docs/01-构建与环境.md`；引导机制 `docs/05`；**DLC 支持 `docs/06`**。

---

## 一·补六 ~ 一·补十、第 21~25 轮（已迁至 docs/98）

> 2026-09-20 第 39 轮：`docs/99` 逼近 100 KB 上限（AGENTS.md 的单文件限制），把第 21~25 轮
> 的复盘整体挪到 **`docs/98-历史轮次记录（第9~16轮）.md`** 的「第 21~25 轮」一节
> （内容一字未改，只搬家）。仍然生效的接口约定：
> ① 日志与 ini 落在 **mod 目录内**（第 22 轮，`docs/05` 第八节仍有说明）；
> ② 「能不能导航」在界面上可见（描述写明 + 按钮置灰 + 点击拦截，第 23 轮）；
> ③ 无引导目标的 52 条**不补猜的目标**（第 24 轮，归因见 `docs/07`）；
> ④ 发布打包 = `tools/package-saq.ps1`（第 25 轮，产物在 `dist/`）。

---

## 一·补十一 ~ 一·补十六、第 26~31 轮（已迁至 docs/98）

> 2026-09-20 第 41 轮：`docs/99` 再次逼近 100 KB 上限（AGENTS.md 的单文件限制），把第 26~31 轮
> 的复盘整体挪到 **`docs/98-历史轮次记录（第9~16轮）.md`** 的「第 26~31 轮」一节
> （内容一字未改，只搬家）。仍然生效的接口约定：
> ① **无限任务入口（任务板）** = ini `Mode=5` 测试模式、入口条目表 12 条（第 27 轮）；
> ② 界面对「不可导航」的条目置灰 + 点击拦截（第 28 轮）；
> ③ 任务板引导目标 = **候选链**（第 30 轮起）+ 候选链**精确优先**（第 31 轮）；
> ④ 入口引导目标的**动态更新**（走进 cell 自动换精确目标；第 32 轮扩到「菜单关着时」）。

---

## 一·补十七、第 32 轮：重启/读档后不进菜单也能自愈 —— 菜单关着时例行「认领 + 对账」

### 1. 玩家反馈与证据链（截图「偏差仍然存在」+ 13:26 后的会话日志）

第 31 轮部署时间是 **13:26:48**（MO2 里 DLL 已确认）。玩家随后 13:30:35 / 13:32:34
两次启动游戏，但这两段会话的日志里**没有任何「菜单打开」记录** —— 玩家重启、读档、
直接看 HUD（没有进任务菜单）。于是：

| 环节 | 状态（日志/存档可证） |
| --- | --- |
| 存档里（GLOB）的引导目标 | 还是 13:12 会话设的兜底引用 `0x0011B754`（陋室那块板旁 3.41 m 的沙盒标记） |
| 脚本（SAQ_Main） | 实例从存档恢复，别名仍绑着它 ⇒ 世界里照常显示那颗**偏掉的蓝点** |
| DLL | `g_guide.questFormID == 0`（本会话没设过引导）⇒ **第 31 轮的动态更新直接短路** |

根因一句话：**第 31 轮的「认领」此前只发生在菜单打开时**（`OnMissionMenuOpened`）——
玩家不进菜单，认领和动态更新就永远不跑。

### 2. 修法（全在 DLL）

| # | 改动 | 位置 |
| --- | --- | --- |
| 1 | 新增 `PollGuideUpkeep()`（菜单关着时每 2 秒一次）：① 没在引导 + 通道里有目标 ⇒ **认领**；② 在引导 + 通道没目标（读档到无引导存档）⇒ 重置本侧；③ 在引导 + 通道目标 ≠ 本侧（同会话读档/外部改动）⇒ 清本侧按通道重新配对 | `SAQ.cpp` |
| 2 | Tick 的「菜单关着」分支先跑 `PollGuideUpkeep()` 再跑 `UpdateEntryGuideTarget()` —— 认领成功后**同帧**进入动态更新（精确目标立即可用） | `Tick` |
| 3 | 认领成功时重置 `lastTargetCheckMs = 0`（允许立刻复算）；「认不出的通道目标」的 WARN 用 `adoptWarnRef` 去重（同一目标只打一次，不因例行检查刷屏） | `AdoptExistingGuide` |

### 3. 待实测判据（下次进游戏，一次会话看完）

1. **核心**：重启游戏/读档，**不进菜单**，走到任意任务板前 ≤2 秒：
   * `认领已有引导（任务板入口）：任务板 · XX …`（或 `认领已有引导：…`）；
   * 紧接着 `入口引导目标已更新：… → 0x…（任务板自身（精确））`；
   * `引导已生效：…脚本状态=1`（**前提是脚本在轮询**）→ 蓝点落在板上；
   * 若只有前两行、没有 `引导已生效`：说明本次会话脚本定时器没跑（从未开过菜单）——
     开一次任务菜单再关掉，脚本会在关闭事件里应用通道里的精确目标（蓝点上板）。
2. 顺带：`入口 marker 探测` 一行（决定第 33 轮路线）；若日志没有这行，说明 12 条入口
   全部走了精确目标（原板命中）—— 那更好。
3. 隔离验证（可选）：在板前原地读一次档（同会话）→ `引导状态对账` 或重新认领的日志，
   蓝点 ≤2 秒回到板上。
4. 回归：普通任务引导不受影响（`PollGuideUpkeep`/`UpdateEntryGuideTarget` 只处理入口条目）；
   菜单里停 20 秒+ 无 `引导未生效`；无目标条目只有界面提示、DLL 无请求。

---

## 一·补十八、第 33 轮：日志复查（第 32 轮自愈「跑了但认不出」的真因）+ 静态表就绪修复 + CELL 记录（官方空壳写法）

### 1. 复查对象与部署确认

`mod 目录\SFSE\Plugins\SAQ_ShowAvailableQuests.log`（249 KB，最后一行 13:45:02；滚动删除正常）。
mod 目录 DLL = **13:40:48 / 697856 B**，与工作区构建产物一致，且含全部第 32 轮特征串
（`引导状态对账` / `入口引导目标已更新` / `入口 marker 探测` …）⇒ 本轮会话跑的就是第 32 轮版本。
会话：13:43:16（PID 27076）+ 之前两次「只加载不操作」的 13:30 / 13:32（各 2 行、无菜单）。

### 2. 第 32 轮判据的实测结论（一次会话看完）

| 判据 | 证据（日志原文） | 结论 |
| --- | --- | --- |
| 菜单关着时例行认领 | 13:44:09.880 `[W] ESM 通道里有引导目标 0x0011B754，但静态表里没有哪条任务/入口的引导目标是它（另一个存档留下的？）` | ❌ **机制跑了、认领失败**（真因见第 3 节） |
| 开菜单后同目标认领 | 13:44:48.293 `认领已有引导（任务板入口）：任务板 · 陋室（0x00137573）引导目标=0x0011B754` | ✅ 数据没问题（同一目标、同一数据） |
| 第 31 轮目标动态更新 | 全会话 **0 次** `入口引导目标已更新` | ⚠️ 仍未验（本轮没机会：认领失败 + 玩家 3 秒后就切到别的板） |
| 精确优先（同 cell） | 13:45:01.345 `引导请求：任务板 · 陋室 → 引用 0x00137573（任务板自身（精确））`；Papyrus 13:45:02 `[SAQ] 引导已应用：[TerminalScript < (00137573)>]` | ✅ |
| 远处选板（兜底） | 13:44:51.111 `任务板 · 霓虹城 → 0x002BAB78（同 cell 常驻兜底）`；Papyrus `引导已应用：[ObjectReference <FFNeonZ12_VoliiRef (002BAB78)>]` | ✅（1.8~8 m 偏差的病根仍在） |
| 脚本活性 / 菜单事件 | Papyrus `[SAQ] 菜单打开 通知=7781 / 7782`；DLL 无 `脚本活性探测…没有 +1` | ✅ |
| 回归 | 无 `拒绝写入`；第 27 轮之后无 `引导未生效`；`drop[N]` 正常；4 master 命中 100%；`入口=12(可导航 12)` | ✅ |
| 推送稳定性 | 12:47:13 一次 `推送失败（…没解析出 ASMovieRoot）` → 0.8 s 后 `推送成功` | ✅ 自愈 |

### 3. ★ 真因：认领要用「静态表」，而静态表**只在开菜单时**才建

`AdoptExistingGuide` 的三条反查（任务 `guideRef` / 入口 uID / 入口候选）全都要经过
`Masters::MakeFormID()` 或 `g_runtimeRows`；而 `Masters::Refresh()` 只在 `BuildRuntimeRows()`
里被调用 —— 那是**菜单打开**分支。
`Masters::MakeFormID`（`SAQ_Masters.cpp` 237~248）：master 未解析 ⇒ **返回 0** ⇒ 三个反查全部落空
⇒ 打出那句误导人的「另一个存档留下的？」。

证据对照（同一会话、同一目标、静态数据完全没变，只差就绪状态）：

```
13:44:09.880（本次会话还没开过菜单）认不出 → WARN
13:44:48.293（开菜单之后）          认出了 → 认领已有引导（任务板入口）
```

⇒ 第 32 轮的「重启/读档后不进菜单也自愈」**在当前代码下不可能生效**（修法见十一·1A）。

### 4. ★ 新证据（离线、可复现）：官方动基础游戏 cell 的引用时，**一定连 CELL 记录一起覆盖**

```powershell
python tools\esm\scan_override_persistent.py sfbgs
  SFBGS003：override 引用 248（常驻组 66） → ★ 这些 cell 4/4 同时被 CELL 记录覆盖
  SFBGS008：override 引用 4（常驻组 4）    → ★ 2/2
python tools\esm\find_added_persistent_refs.py      # 产物 ref\added_refs_scan.txt
  全机插件里**只有我们**往基础游戏 cell 新增常驻引用（11 条）；
  BlueprintShips-SFBGS050 的 2382 条新增常驻引用全在它**自己的** cell 里
python tools\esm\scan_override_persistent.py saq
  我们的 ESM：新增引用 11 条、CELL 记录覆盖 = 0
```

⇒ 我们是本机唯一的「只写引用、不写 CELL」写法。第 30 轮的 `marker 0`（新建的 11 条常驻
XMarker 引擎里一个都取不到）最可能就是**缺这条 CELL override**（引擎不把「本插件没有该 CELL
记录」的 CellChildren 组并进那个 cell）。这条线索同时解释第 29 轮（override 引用）与第 30 轮
（新增引用）两次失败 —— 两轮都没写 CELL override。

### 5. 顺手记下的两个小问题

1. 「认不出通道目标」的 WARN 文案在静态表未就绪时会误导 —— 应区分「静态表尚未就绪，稍后重试」。
   （本轮已修：见第 6 节 B。）
2. 兜底引用质量：霓虹城兜底 = `FFNeonZ12_VoliiRef`（enable marker 系），不是「地点门牌」——
   远处看这个蓝点意义有限（第 30 轮遗留的「质量分级」问题，见十一·1C）。

### 6. 本轮实现

**A. DLL：菜单关着时也保证「静态表」可用**（`SAQ.cpp` / `SAQ_Masters.*`）

| 改动 | 说明 |
| --- | --- |
| `Masters::Resolved()`（新） | 本会话 master 前缀解析成功与否 —— 调用方据此区分「表还没就绪」与「目标真的认不出」（`MakeFormID` 未解析时**静默返回 0**，这是这次误报的根源） |
| `EnsureStaticTablesReady(why)`（新，`SAQ.cpp`） | 未解析 ⇒ 试一次 `Masters::Refresh()`；已解析但行表为空 ⇒ `BuildRuntimeRows()` 建表。成功记一行 INFO `静态表已就绪（菜单关着时预建，…）：…；运行期行 N/261…`；失败**不缓存**、下次 Tick 再试（`静态表尚未就绪` 只记一次） |
| `AdoptExistingGuide()` | 开头加这道闸：表没就绪直接 return（`PollGuideUpkeep` 每 2 秒会再试）——**不再**把「表没建好」误判成「认不出」 |
| WARN 文案 | 「另一个存档留下的？」→ 带证据：「（另一个存档 / 老版本留下的？；静态表已就绪：运行期行 N/261）」 |

**B. ESM：11 条 CELL 记录（官方的「空壳」写法）**（`tools/esm/create_board_markers.py`）

先说**为什么不整条照抄 Starfield.esm**（这是本轮改过一版才定下来的）：

* 整条照抄 = 把 cell 的全部字段变成「本插件版本」⇒ 别的 mod 改过同一 cell 时会被覆盖；
* 基础游戏的 cell 名（`FULL`）是**本地化 string ID**（4 字节），而本插件非本地化 ——
  引擎会把它当内联字符串（xEdit 重存后显示成乱码），等于把 cell 名写坏。

改用的、也是**官方对同一条 cell 的写法**（`tools/esm/dump_cell_group.py --plugin SFBGS003.esm`
实测，对象正是任务板所在的霓虹城 `0x00071D5A`）：

```
GRUP Interior Cell Sub-Block 6
  CELL 0x00071D5A flags=0x004000 size=19 子记录=[EDID]     ← 空壳：只留 EDID，不写数据字段
  GRUP Cell Children of CityNeonCore [CELL:00071D5A]
    GRUP Cell Persistent Children …
      REFR 0xFD000F13 flags=0x000400                       ← 官方自己新增的常驻引用
```

⇒ 本插件现在写：**空壳 CELL 记录（EDID + flags 0x4000，非压缩）**，放在子块组的**第 0 个子项**，
紧跟它自己的 `CellChildren > CellPersistent > 我们的 marker`。**不碰 cell 的任何数据**，
也不覆盖本地化名称字段（连 cell 级冲突面都没有）。

| 层 | 改动 |
| --- | --- |
| 生成 | `create_board_markers.py`：`build_cell_stub()` + `record_edid_of()`（解压取 EDID）；重建时校验「Starfield.esm 的 cell EDID == entry_targets.json 的 cell」；自校验新增空壳断言（override 空间 / 组链尾部 [2,3] / flags 0x4000 / 非压缩 / 子记录恰好 `[EDID]` / EDID 相符） |
| 产物 | ESM 从 2948 B → **3521 B**（11 marker + 11 空壳；对比「整条照抄」那版是 1434623 B） |
| 验证工具 | `verify_saq_build.py`：ESM 数据侧解析新增「CELL 空壳 11/11」检查（工作区 + MO2 两份），并断言**不许**写数据字段 |
| xEdit 脚本 | `verify_persist.pas` 增加 CELL 记录计数（脚本必须保持纯 ASCII —— 踩过一次：中文注释直接让 xEdit 编译失败） |

### 7. 验证（已做）

* `build-saq.ps1 -SkipTable -SkipSwf -SkipPapyrus`：ESM 重建 + 入口表 + DLL（700416 B）+ 部署，
  全流程自校验通过（重建路径 `check_data=True`，含空壳断言）；
* `verify_saq_build.py` **全部通过**（新增：`静态表预建日志` / `静态表未就绪日志` +
  反向检查「旧误导文案已替换」；ESM 两份都是 `marker 11/11 + CELL 空壳 11/11`）；
* **xEdit 权威验证**（`tools/run-verify-persist.ps1`，约 5 分钟）：
  `marker hits = 11 (expect 11)`、`CELL overrides = 11 (expect 11)`；
  CELL 记录显示为 master 空间（`CELL 00071D5A … EDID=CityNeonCore`），组名能解析出 cell 名；
  **让 xEdit 重存**一份（`ref/xedit-out/SAQ_resaved.esm`，3527 B）：11 条空壳 + 11 条 marker 仍在
  （空壳 size 与官方同款：霓虹城 19 B）；
* Nexus 包已重打（含改后的兼容性文案）：`dist\SAQ-ShowAvailableQuests-0.1.0.zip`
  （1,762,954 B，SHA256 `41804E70…21F0A501`）。

### 8. 待实测判据（下次进游戏，一次会话看完）

1. **A 的判据**：重启/读档、**不进菜单** ≤2 秒 →
   `静态表已就绪（菜单关着时预建，认领已有引导）…`（每个会话一行）→
   `认领已有引导（任务板入口）：…` → `入口引导目标已更新：…（任务板自身（精确））`
   （+ `引导已生效`）→ 蓝点落在板上；**不应**再出现 `静态表尚未就绪`（除非游戏还没读完档）。
2. **B 的判据（决定性）**：开菜单 → `入口=12(可导航 12｜marker 11 原板 1 兜底 0 不可用 0)`。
   * `marker 11` ⇒ 空壳 CELL 记录写法被引擎接受 =「任意位置精确导航」治本成功；
   * 仍 `marker 0` + `兜底 N` ⇒ 空壳写法不够（下一步：改用**整条照抄**那版做对照实验）；
   * 顺带看 `入口 marker 探测` 一行的三个对照项。
3. **症状确认（玩家眼睛）**：站在任意位置点「任务板 · 陋室」→ 蓝点直接落在板上（不再偏 3.41 m）；
   走进/离开那个 cell 时蓝点不丢。
4. **回归**：cell 里的一切照常（任务板、NPC、光效、地区名不乱码 —— 空壳不覆盖任何 cell 数据，
   若这里出问题说明「EDID-only 覆盖」在引擎里不是合并语义，立刻回退到整条照抄版）；
   普通任务引导不受影响；菜单里停 20 秒+ 无 `引导未生效`；无目标条目只有界面提示、DLL 无请求。

---

## 一·补十九、第 34 轮：日志复查 —— 第 33 轮 A/B 判据全部通过（治本成功）

新会话 14:23:22~14:26:44（跑第 33 轮版本，玩家**没开过菜单**）：

* **A（静态表预建 + 认领）✅**：14:24:30 `静态表已就绪（菜单关着时预建，认领已有引导）…
  运行期行 261/261` → `认领已有引导（任务板入口）…` → 同帧
  `入口引导目标已更新：…0x0011B754 → 0x00137573（任务板自身（精确））`
  → Papyrus 14:24:42 真实应用（**玩家不进菜单，蓝点已被修正到板上**）；
* **B（CELL 空壳 + 新建常驻 marker）✅**：`入口=12(可导航 12｜marker 9 原板 3 兜底 0 不可用 0)`
  —— 对比第 30 轮 `marker 0 兜底 10`：**兜底归零（12/12 全是精确目标）**；
  远处走 `引用 0x0F000903 / 0x0F00090A（新建常驻 marker（精确））`，Papyrus
  `引导已应用：[ObjectReference <SAQ_BoardMarker_21001E (0F00090A)>]`
  = **引擎接受 CELL 空壳写法 + 新建常驻引用**（治本的关键实证）；
* **回归 ✅**：无 `拒绝写入` / `引导未生效` / `主线程停顿`；`drop[0]`；4 master 100%；
* 唯一 WARN（无害）：14:24:41「更新未被脚本确认」—— 确认窗口整段落在读档 VM 冻结期
  （`VM is thawing` 在 14:24:42），脚本解冻后 1 秒应用；通道未动、结果正确，只是文案容易
  误导（可选优化：读档期放宽确认窗口 / WARN 补一句「读档中可忽略」）。

## 一·补二十 ~ 一·补二十五、第 35~40 轮（已迁至 docs/96）

> 2026-09-20 第 45 轮：`docs/99` 逼近 100 KB 上限（AGENTS.md 的单文件限制），把第 35~40 轮
> 的复盘整体挪到 **`docs/96-历史轮次记录（第35~40轮）.md`**（内容一字未改，只搬家）。
> 其中仍然生效的关键结论：
> ① 第 35 轮：进度门槛首版（7 条任务 / 9 条条件，见 `docs/08`）；
> ② 第 37~40 轮：SET COURSE 的星图三层实现（状态 5/6/7 + 关菜单 + 地点候选）；
> ③ 第 40 轮：星图自动重试 + 「地点 → 星图节点」只读诊断。

## 一·补二十六 ~ 一·补三十一、第 41~46 轮（已迁至 docs/96）

> 2026-09-20 第 48 轮：`docs/99` 超过 100 KB 上限（AGENTS.md 的单文件限制），把第 41~46 轮
> 的复盘整体挪到 **`docs/96-历史轮次记录（第35~40轮）.md`** 的「第 41~46 轮」一节
> （内容一字未改，只搬家）。仍然生效的关键结论：
> ① 第 41 轮：星图注册名 `GalaxyStarMapMenu` / 节点解析器特征字节（`4C 8B DC …`）；
> ② 第 42/43 轮：R 键的分诊探针（`press=`/`sel=`/`ev=`/`btn=`）与「关闭时刻补一次 poll」；
> ③ 第 44 轮：星图由脚本的**轮询节拍**打开（原版 dispatch 已删，星图位置不再滞后一条）；
> ④ 第 45 轮：引导目标**候选池**（质量分级 + 预选/换候选/动态复算）；
> ⑤ 第 46 轮：引导可用性收口（结果码 5 / 退避重试 / HUD 提示 / `Mode=6` 待生效清单）。

## 一·补三十二、第 47 轮（大项 C）：引导可用性收口 —— 「同 cell 常驻兜底」

（完整技术细节见 **`docs/05-任务引导.md` 第二十一节**；本节记排查链、修法与验证。）

### 1. 玩家反馈与两份取证

> 「营救机器人这个任务导航点被锁定在亚特兰蒂斯城的同一个地铁上，也就是说导航基本没用」

| 证据 | 内容 |
| --- | --- |
| DLL + Papyrus 日志（20:14 会话） | 玩家对「营救机器人」连点 30+ 次（E/R）**全部结果码 5**（目标尚未加载）；Papyrus 三次 `引导失败：FormID 50916517 取不到引用` —— **引导从未生效** |
| 机制 | 脚本 `ApplyGuide` 取不到引用时 `Return`（**别名保持上一次绑定**）⇒ 世界里的标记仍是**上一条生效引导的残留**（本次会话 = 20:14:30 的「所罗门的宝藏」@ 阿基拉城；更早会话的日志已滚动删除）⇒「点了没反应、标记锁死在某处」 |
| 玩家视角 | 20:16:52 着陆新亚特兰蒂斯城太空港（`NewAtlantisShipLandingMarker`）—— 「亚特兰蒂斯城的地铁」= The Well（老住区 / NAT 站下方那片），「营救机器人」的机器人就在那一带（距「任务板 · 新亚特兰蒂斯城」6 米、距 `CityNAWellSub17Marker` 4.5 米） |
| ★ 决定性发现 | 机器人 **3.3 米**处有一个**常驻**的任务装置（`REFR 0x0208ECA6`，base = `FURN SFBGS022_Destroyed_ModelG_FurnitureMarker`）—— 旧候选生成只从任务别名的引用里选，**没把它用上**（这类「全非常驻候选」的任务本机 76 条，远处点引导 = 一个候选都取不到） |

### 2. 修法（三层）

| 层 | 改动 |
| --- | --- |
| 数据 | `gen_guide_targets.py` 新增 **`find_fallbacks()`**（第三遍扫描）：对「全部候选都非常驻」的任务，在**候选目标所在 cell** 里找**最近的常驻 REFR/ACHR**（排除候选自身；距离只在同 cell 内比较）作为候选池**最后一位**兜底（`src=同 cell 常驻兜底（距目标 N 米）`；名字用 base EDID / `Ref_XXXXXX`、`nameq` 强制内部名质量 ⇒ 只在「内部名落脚点」一档、同档常驻优先）。**76 条里 56 条**补到；候选 875 → **931** |
| 表 | `gen_quest_table.py` 无需改（自动切片）；常驻候选 283 → **339** |
| DLL | 「需要靠近」判据 `AllCandidatesNonPersistent` → **`FirstCandidateNonPersistent`**（只看**首选**候选）—— 兜底是常驻的，判据若看全部会把这一类判没；描述提示 + 测试模式 6 的 76 条清单照旧（它们说的是**精确目标**要靠近才加载） |
| 未动 | SWF / Papyrus / ESM（文案「导航目标要靠近目标区域后才加载」仍成立：远处拿到的是**近似兜底**） |

### 3. 行为对照

| 场景 | 旧 | 新 |
| --- | --- | --- |
| 远处点「营救机器人」（有兜底） | 全候选取不到 ⇒ 提示「需靠近」+ 世界里的**旧标记残留不动** | 预选直接命中兜底（常驻）⇒ **引导立即生效**，蓝点落在 The Well 的机器人附近 |
| 走进 The Well | 等 10~60 秒退避重试 | 「候选复算」升级为精确机器人（`[2]→[1]`） |
| 无兜底的任务（本机 20 条） | 第 46 轮链路 | 不变（待生效 + HUD 提示 + 退避重试） |

### 4. 验证（已做）与待实测判据

已做：数据管线全量重跑（含第三遍扫描）✓；`build-saq.ps1 -SkipTable -SkipSwf -SkipPapyrus`
（ESM 幂等 + DLL **770048 B** + MO2 部署）✓；`verify_saq_build.py` **全部通过**
（3 条新检查：静态表常驻兜底已并入 931/339、营救机器人兜底链
`0x08ECA5` 非常驻 → `0x08ECA6` 常驻、DLL 新判据文案 + 反向检查「旧『全是 非常驻』说法已替换」）；
Nexus 包已重打（1,798,350 B）。

待实测（一次会话看完）：

1. 在 The Well 之外点「营救机器人」⇒ `引导请求：…｜候选 [2/2]「Ref_08ECA6」` +
   **引导立即生效**（结果码 0；解锁菜单后蓝点出现在 The Well 那片）；不再「没反应」；
2. 走进 The Well ⇒ `引导目标已更新（候选复算）：… [2]→[1]（0x0308ECA5）` ⇒ 蓝点落到机器人本体；
3. 回归：无兜底的 20 条仍走待生效链路；有常驻候选的任务行为不变；
   测试模式 6 清单仍是 76 条（含「营救机器人」）；
4. 眼睛（如方便）：蓝点落在 The Well 里机器人所在的小巷（垃圾箱/涂鸦墙旁），
   而不是「任务板」或「Sub17 marker」的精确点上（那两点在 4~6 米外）。

## 一·补三十三、第 48 轮：判据收口 + 大项 D —— 「进度没到不显示」扩到对话侧（INFO 门槛）

### 1. 起因（第 47 轮实测复查，20:41~20:47 会话）

第 47 轮判据全绿（见头部「上一轮」段的实测记录）。复查抓出三件事：

① **判据过宽**：第 47 轮把「需要靠近」判据放宽成「首选候选非常驻」—— 数据侧实测命中
**158/209 条**（`Mode=6` 实测推送 155 条、玩家列表约 152 条，76% 的任务都带「需靠近」提示）。
其中 138 条（首选非常驻 + 池里有常驻候选）远处点引导会**立即落到常驻候选**、有即时反馈，
不该提前警告 —— 提示噪音化，反而淹没真正「点了没反应」的 20 条。
② **读档窗口抖动**：20:42 认领「补给之行」后同毫秒复算 `[1]艾比·艾丁 → [3]常驻兜底`
（读档瞬间 `LookupByID` 对没加载完的引用返回 null），10 秒后又升回 `[1]`；
两条 `[W] 入口引导目标更新未被脚本确认`（文案对普通任务不准确、确认窗口落在读档 VM 冻结期）。
③ Papyrus 26 条 `RevertToOpenStateOnCellLoad` 报错 —— 离线取证为**官方数据/脚本问题**
（全游戏只有 The Well 的 29 条 ref 挂该脚本；VMAD 属性为空的 26 条报错、VMAD=77B 的 3 条正常；
同 cell 混合存在 ⇒ 与我们的 CELL 空壳无关），记档以避免以后再查。

### 2. 判据收口（回退为「全部候选都非常驻」）

`FirstCandidateNonPersistent` → **`AllCandidatesNonPersistent`**：只把候选池里
**一个常驻都没有**（本机 20 条）的任务算「需要靠近否则没反应」——
这一类远处点引导会进入待生效链路（结果码 5 + HUD 提示 + 退避重试），描述提示必需；
对比「首选非常驻 + 有常驻候选」的 138 条：远处点引导**落到常驻候选**（立即生效）、
靠近后自动升级 ⇒ 不提示。ini 模板 / `TestModeNote` / 文档数字同步更新。

### 3. ★ 大项 D：INFO 门槛（对话侧条件）

数据侦查 → 实现（设计细节与两例验证见 `docs/08` 第九节）：

| 层 | 内容 |
| --- | --- |
| 侦查 | Starfield 的对话挂在 QUST children 组下（`INFO → 所属任务` 从组链直接可得）；全表 2077 QUST / **126347 INFO** / 8541 带条件 / 613 条「引用别的任务」的进度条件 |
| 分类 | 按条件组里对该任务**自己**的引用分：入口类（含自己==0）/ 中性类（无自引用）/ 推进类（含自己==1，**排除**） |
| 合成 | 全部参与判定的对话都「有已知为假的条件」⇒ 隐藏；成对条件（X==0 / X==1）自动豁免；任何一条对话无可判定条件 ⇒ 整条任务不进表（保守）。产物 **60 条任务 / 290 条对话 / 341 条条件** |
| 验证 | 与第 35 轮记录级门槛**两例完全一致**（大器晚成↔孤立无援、亲爱的姐妹↔FFConstantZ05）；官方 `Com_Companion_Barrett`（大量成对条件）自动豁免不隐藏 ✓ |
| 运行时 | `SAQ_QuestCond.cpp::EvaluateInfoGates`（单条求值复用 `EvalOneCond`）；`CollectAvailableQuests` 在记录级门槛之后过滤；`INFO门槛=60(过A/藏B/未知K)` + `INFO没到:` 名单 + 「引擎已开始却要隐藏」交叉验证 WARN；ini `[Filter] InfoCond` 可关 |
| 工具 | `tools/esm/scan_info_gates.py`（全表扫描）→ `analyze_info_gates.py`（合成）→ `gen_quest_table.py`（表）；`build-saq.ps1` 第 1 步自动跑合成 |

### 4. E 小项（顺手修）

* **认领后 8 秒复算宽限期**（`kAdoptGraceMs`）：读档/开局瞬间引擎查询不稳定，期内不做候选复算
  （消除「补给之行」式 [1]→[3]→[1] 抖动）；
* `入口引导目标更新未被脚本确认` → **`引导目标更新未被脚本确认`**（+「读档 / 脚本未运行期间
  出现这一行属预期」）；
* 星图等待文案：`还开着：无` → `其它打开的菜单：{}`（旧文案自相矛盾）；
* 界面：接取后状态栏 `当前引导:?` → 名字回退查玩家任务日志（再兜底打 FormID）；
  诊断标签 `0xNaN` → `「名字」(子项)`。

### 5. 验证（已做）

`build-saq.ps1 -SkipTable -SkipPapyrus`：ESM 幂等（3521 B）+ SWF 726586/726866 B +
DLL **781824 B** + MO2 部署 ✓；`verify_saq_build.py` **全部通过**（第 48 轮 +22 条检查）。

### 6. 待实测判据

见「一、当前状态」的 `游戏内实测` 行末尾（第 48 轮段）。

### 7. ★ 补丁（同日实测复查，21:45~21:47 会话）

`Mode=6` 实测全绿（读档无复算抖动 / 推送 19 条 / `INFO门槛=58(过37/藏21/未知0)` /
星图两次完整 / 新文案生效），但交叉验证 WARN 抓出**两条误藏**：

```
[W] INFO 门槛要隐藏『陌生人的善意』（0x0009BD47）但引擎已开始（0x00013497 stageDone=0 期望 1）
[W] INFO 门槛要隐藏『全数到期』（0x002AD3D5）但引擎已开始（0x0029EF60 completed=0 期望 1）
```

『全数到期』（RAD05）正是**第 11 轮验证过「玩家能接到」的案例**（Galaxy Bank NPC 对话可接）。
⇒ **判据再收窄：引擎已开始 ⇒ 放行**（隐藏条件 = 「引擎**没启动** + 全部参与对话都有已知假条件」）。
日志：`INFO门槛=58(过37/藏19/放行2/未知0)` + 放行记一行 INFO（DLL 782336 B）；
verify 同步（+2 条：放行文案 / 统计含「/放行」；旧 WARN 文案反向检查）。

## 二~十、历史轮次记录（第 9~16 轮，已迁至 docs/98）

> 2026-09-20 第 31 轮：`docs/99` 接近 100 KB 上限，把第 9~16 轮的复盘整体挪到
> **`docs/98-历史轮次记录（第9~16轮）.md`**（内容一字未改，只是搬家）。

## 十一、下一步（按优先级）

1. ~~第 34 轮：玩家侧最终验收~~ → ✅ **玩家已验收（2026-09-20 会话确认）**：蓝点/扫描仪路径线
   精确落板等**眼睛判据通过** —— 第 30~33 轮「任务板任意位置精确导航」四轮闭环正式收口。
   剩余小项（低优先，不影响验收）：14:24:41「更新未被脚本确认」WARN 的时机优化 ——
   读档 VM 冻结期确认窗口耗尽（脚本解冻后 1 秒即应用，通道未动、结果正确，只是文案容易误导）。
2. **第 18/26 轮已实测通过**（一·补四第 1 节 / 一·补十二第 1 节）——
   只剩「DLC 任务名显示、蓝点出现」这类需要**眼睛**确认的部分，随第 1 条一起看。
3. ~~任务板入口「任何位置都能精确导航」~~ → 第 29 轮（override）被否定 → 第 30 轮
   （新建常驻 XMarker）**实机判据 `marker 0` 也被否定** → 第 31 轮「精确优先 + 动态更新」
   修掉玩家可见的偏移 → **第 32 轮补上「重启/读档后不进菜单也自愈」**（例行认领 + 对账）。
   ★ 第 33 轮复查给出的答案（一·补十八第 4/6 节）：**缺 CELL 记录** ——
   官方在动基础游戏 cell 的引用时一定同时写一条该 cell 的 CELL 记录（6/6），且写法是
   **空壳（只留 EDID + flags 0x4000）**；我们已照抄（11 条），xEdit 与产物校验都过，
   **待实测 `marker 11` 判定**。次选路线：整条照抄 Starfield.esm 做对照 /
   Papyrus `PlaceAtMe(forcePersist=true)`（治不了「远处」）。

4. ★ **SET COURSE（R）的星图**：第 37 轮落地、第 38 轮修「暂停菜单没了结」、第 39 轮修「R 会取消引导」、
   第 40 轮修「调用时机 + 指错星球诊断」（三层实现见一·补二十二 ~ 一·补二十五 / `docs/05` 第十一~十四节），
   **第 41 轮修两个确定性 bug**：① 星图注册名 `GalaxyStarMapMenu`（此前误查 `MapMenu` ⇒ 4 轮假阴性）；
   ② 节点解析器特征字节抄反（诊断一直不可用）。
   **第 42 轮**（玩家反馈「R 的导航结果不稳定，有时目标不是悬停那条」）：先补「界面用了哪一行」的
   硬证据（`press=`/`sel=` 进报告与引导请求日志），再修两条**会改掉目的地**的缺陷 ——
   ③ DLL 星图重试抢在脚本 1.5 s 延时前面换地点候选（星图被重画 = 航线/焦点跳变）；
   ④ 脚本的星图延时待办在新引导/取消后没作废（会开出上一条任务的航线）。
   **第 43 轮**：补「按 R 没反应」的分诊探针（`ev=`/`btn=`）+ 关闭时刻现场读报告 +
   关闭前补一次请求轮询。
   ★★ **第 44 轮**：查明「星图位置永远是上一个导航」的真因 —— 第 36 轮那条原版 dispatch
   **其实生效**（第 37 轮「无 sink」结论不成立）：它用代理任务**当前**的目标位置打开星图，
   而那一刻脚本还没 ForceRefTo ⇒ 永远是**上一次**的引用；且它先把星图打开（暂停 ⇒
   脚本定时器冻结）⇒ 补救调用再也跑不到。⇒ 已删除 dispatch，星图改由脚本的**轮询节拍**
   打开（`StarMapPendingTicks` + `ProcessStarMapPending`，节拍到点 = 菜单已关、游戏在跑）。
   ★★ **已收口（第 44 轮实测 + 第 45 轮复查）**：玩家 19:0x 会话按 R 四次全部链路完整
   （星图 = 脚本节拍打开、R 后约 1.5 秒、第 1 次尝试），眼睛验收「未异常」；
   `星图诊断` 节点值第一次有真实数据（行星 FormID，如 `0x0005E2B6` 阿基拉）。
   剩余已知差别（接受）：星图是「关掉暂停菜单后」打开的（返回时回到游戏），
   且**不做**原版「未追踪时先问是否追踪」的弹窗（我们传的是地点、不是任务）。
   遗留小项（低优先，来自第 45 轮日志复查）：
   * `星图：等待中（…还开着：无）` 文案自相矛盾（每次成功前 ~38ms 都会先打一行）；
   * 19:01:23 一次「R 后约 0.0 秒」= 上一次星图未关的残留（玩家没报异常，留观）；
   * `星图诊断` 在目标 cell 未加载时退化为 `引导引用取不到（所在格子没加载）`（只影响诊断，
     脚本侧地点链照常工作）。
4. ★★ **引导可用性 + 提示收口（大项 B）→ 第 46 轮已完成、待实测**（一·补三十一 / `docs/05` 第二十节）：
   点引导时「所有候选都取不到」不再 19 秒后静默放弃 —— 描述**提前**写明「需要靠近」+
   结果码 5（保持竖条）+ HUD 提示 + DLL 退避重试（10~60 秒，靠近即自动生效）。
   判据见一·补三十一第 4 节；顺带收口的旧小项：界面文案里「靠近后可导航」的提示已落地。
   后续可选（低优先）：把「待生效」也在列表里做成可见状态（如竖条变暗/小图标），
   或给候选排序引入「玩家当前位置/同星系」因子（目前靠 LookupByID 可得性 + 退避重试已够用）。

4b. ★★ **引导可用性收口（大项 C）→ 第 47 轮已完成、✅ 已实测通过**（一·补三十二 / `docs/05` 第二十一节）：
   玩家实测「营救机器人」的「导航点被锁定在某处、导航没用」—— 真因 = 该任务的全部候选都是
   非常驻引用（远处一个都取不到）+ 脚本取不到时**不改别名** ⇒ 世界里的标记是**上一条引导的残留**。
   修法 = 数据侧 `find_fallbacks`（76 条里 56 条补到「同 cell 常驻兜底」）+ DLL 判据改
   「**首选**候选非常驻」。行为：远处点引导落到兜底（蓝点在目标附近）、靠近后自动升级精确目标。
   后续可选（低优先）：① 仍无兜底的 20 条（目标 cell 解析不到 / 该 cell 里确无常驻引用）——
   可离线往那些 cell 新增常驻 XMarker（第 30~33 轮做法）或让脚本 `PlaceAtMe(forcePersist)`；
   ② 兜底候选再分层（优先挑「任务自己的装置 / 名字带任务前缀」的常驻引用，而非最近的任意常驻引用）。

5. ~~引导质量（覆盖率 + 质量侧）~~ → ★★ **第 45 轮（大项 A）已完成**：引导目标升级为
   **候选池**（质量分级 + 点引导时预选 + 状态 2 换候选 + 菜单关着时动态复算，见一·补三十 /
   `docs/05` 第十九节）；52 条无目标维持「不补猜的目标」（`docs/07`）。**待实测**（一·补三十第 5 节）。
   后续可选（低优先）：给候选排序引入「玩家当前位置/同星系」因子 —— 目前 LookupByID 可得性
   已经覆盖大部分场景；`tools/esm/ref_flags_report.py`（第 27 轮的持久性统计工具）仍然可用。
6. ★ **「进度没到不显示」**：第 35 轮**首版已落地**（7 条任务 / 9 条门槛，`docs/08`）、
   ✅ 实测通过（14:47 会话：过3/藏4/未知0）；**★ 第 48 轮（大项 D）扩到对话侧** ——
   INFO 门槛（60 条任务 / 290 条对话 / 341 条条件；两例与记录级门槛完全一致），
   构建/verify 全过、**待实测**（判据见 `docs/08` 第九节）。后续可扩：
   * **DLC 的 INFO**（3 个 DLC 的 QUST/INFO 还没扫；保守：DLC 任务暂不做 INFO 判定）；
   * operator 0x01/0xA0/0xA4 的语义（要反汇编条件求值器本体）—— 解出来后「位置/遭遇」类
     条件也能覆盖（当前一律放行）。
7. **可扩展性（可选）**：把其它官方 Creation（`SFBGS003/004/006/007/008`、`Constellation`、
   `BlueprintShips*`）也加进 `fetch_sources.py::SOURCES` —— 表里已经有 master 概念，
   DLL 不用改；主要成本是每个 master 要抽一次字符串表。
8. **打包 + Nexus 文案**：第 25 轮已产出 0.1.0（`tools\package-saq.ps1` 可随时重打）；
   第 27/28 轮（任务板入口 + 降噪 + 可用性判定）**需重打一次**（已在各轮构建后重打）；
   ★ 第 46 轮已重打一次（含第 45/46 轮全部产物：DLL 765440 B / SWF 726531+726812 B /
   PEX 12064 B / ESM 3521 B），文案里补上「需靠近的任务会在描述里写明」与
   「点引导后 HUD 会提示目标是否已加载」。
   文案里要写明的已知限制：
   * 首次引导后，任务日志里会出现一条名为「可接任务」的引导用任务（借引擎标记系统的代价）；
   * 部分任务 / 部分任务板入口没有可用的导航目标（列表照常显示，只是点了不会有标记）；
   * 引导总在**关闭菜单后**生效（与原版一致：HUD 蓝点本来就要关菜单才可见）；
   * 地球舰队的 5 条「鸭嘴猪」活动在游戏数据里就叫同一个名字（列表里看起来是重复项）。

### 已完成（保留记录）

* ✅ **第 33 轮 A/B 判据实测通过**（14:23~14:26 会话，第 34 轮复查）：自愈链路
  （静态表预建 → 认领 → 动态更新，玩家没开菜单）+ CELL 空壳 + 新建常驻 marker 被引擎
  真实应用（Papyrus `SAQ_BoardMarker_21001E`）——**任务板「任意位置精确导航」治本成功**
  （一·补十九）。
* ✅ **第 26 轮实测通过**（11:12~11:14 会话）：菜单里停 27 秒无假失败；关菜单 0.9 秒
  `引导已生效`（一·补十二第 1 节）。
* ✅ **第 18 轮实测通过**（09:53~09:56 会话）：前缀探测 4 master 全解析（命中 100%）、
  推送 260 条、认领已有引导、界面 257 条（一·补四第 1 节）。
* ✅ 验证「全数到期」修复 + 卡顿埋点（第 16 轮日志确认：`隐藏=1` 只剩「食宿」、`收集耗时=0 ms`、
  推送失败路径 0 ms、**无「主线程停顿」行**）。

## 十二、已知风险

| 风险 | 说明 |
| --- | --- |
| **「已开始」≠「已接取」**（第 11 轮实证） | 引擎会把「玩家仍能接到」的任务提前置成 running（RAD05 全数到期，flags=0x8001）。TESQuest 的 started 位**不能**当「玩家已接」的判据；只有玩家任务日志（QuestData）算数 |
| **GLOB（float）存不下完整 FormID**（第 21 轮踩过） | GLOB 是 float（尾数 24 位）：完整 FormID > 2^24 后**奇数不可精确表示**（±1）。已把引导目标拆成「低 24 位（0x801）+ 高 8 位（0x805）」两个 GLOB。**以后再开 float 通道，先问「值会不会超过 2^24」** |
| **Papyrus 定时器在菜单开着时不走**（第 27 轮定案） | 任务菜单暂停游戏 ⇒ `StartTimer` 不触发：三次会话的 `引导已应用` 都精确落在「菜单关闭」时刻，菜单开着 27~37 秒零应用。**不要设计「菜单开着时脚本会处理」的功能**；引导总是「关菜单生效」（与原版 HUD 交互一致）。DLL 侧也据此不再在菜单开着时轮询确认（降噪） |
| ~~任务板入口多为非常驻引用~~（第 27 轮发现 → 第 28 轮兜底 → 第 29 轮 override **被实机否定** → **第 30 轮新建常驻引用治本**） | 12 条入口里只有阿基拉城是原生常驻 ⇒ 其余 11 条在 cell 未加载时 `Game.GetForm` 取不到。★ **第 29 轮的 override 路线不成立**：override 只换数据、**不改变引用的加载分类**（官方 70 条同类 override 原记录本来就全是常驻 = 零先例；实机 12:45 会话 `可导航 2`）。第 30 轮改为**新建** 11 条常驻 XMarker（0x900~0x90A，位置 = 板坐标）+ 候选链。★ **第 33 轮补齐最后一块**：官方对同一条 cell（SFBGS003 → 霓虹城）的写法 = **空壳 CELL 记录（EDID + flags 0x4000）+ 紧跟 CellChildren 组** —— 我们也照抄了（11 条，xEdit 验证通过；**待实测 `marker 11`**）|
| ~~override 世界引用的冲突面~~（第 29 轮引入 → **第 30 轮已消除**） | 第 29 轮对 11 条任务板 REFR 的 override 已全部移除 ⇒ 不再覆盖任何基础游戏记录。第 30 轮只往那些 cell **新增**自己的记录（mod 加物件级操作），与改同一 cell 的 mod 无冲突 |
| ~~打开任务菜单偶发长停顿~~（第 11 轮发现 → 第 12 轮修复 → 第 16 轮确认） | 根因：失败解析路径里 `IsMapped`（VirtualQuery）累计 ~3.9 秒；`SafeRead` 改纯 `__try` 保护后消失。第 16 轮日志实证：收集 0 ms / 失败 0 ms / **无「主线程停顿」行** |
| **界面状态必须由回写驱动**（第 16 轮踩过） | `SaqToggleGuide` 只能在本地立即切状态，而「这条任务有没有引导目标 / 实际在引导哪条」只有 DLL 知道。凡是「界面先行、DLL 后处理」的动作都必须有回写通道（`SAQ_GuideReply`），否则界面会说谎 |
| **GLOB 锚点有次数窗口**（设计边界） | `SAQ_Notify` = 7777 + 菜单打开次数，认领窗口 7777..8776（1000 次）。单次会话极难超限；脚本重挂会把它重置回 7777（DLL 认领成功后不再读锚点，无影响） |
| **文本子记录必须带 NUL**（第 12 轮实证） | 非本地化 ESM 的 FULL/NNAM 等文本子记录，长度 = 字符数 + 1（含 NUL）。第 11 轮漏写 NUL ⇒ HUD「任务更新」显示 `[…]` |
| commonlibsf 的 `REL::ID = 0` 接口 | 已踩（BGSStoryTeller、`TESQuest::IsStageDone`）；编译期 ID 审计挡着（`docs/01` 坑 8） |
| **commonlibsf 的结构体偏移一律不可信**（第 18 轮升级为通则） | 已踩四次，其中 TESDataHandler 两次：`formArrays` 读到空表（第 5 轮，见 docs/02）、`files` 读到空链表（第 17 轮，四个 master 全被误判「未加载」⇒ 第 18 轮改前缀探测）。读引擎对象字段：先找「引擎自己怎么读它」（反汇编），或改用已实测可靠的引擎函数（`LookupByID` 等）；`TESGlobal::value`（0x48）是上一代项目修正过的 |
| **自己写的注释/文档也可能是错的** | 已踩：哈希指针、模糊 RTTI、误信「位16 = 自动启动」。结论要留原始反汇编/日志，且「改完要能被验证」 |
| **GFx 返回值要按类型读** | `GetNumber()` ≠ 万能（见 `docs/04` 2.4） |
| **TESQuest 运行时偏移来自反汇编** | `0x114 / 0x32C / 0x338` 有虚表核验 + 识别率安全阀；换游戏版本要重跑 `tools/re/quest_api_scan.py` |
| **非本地化插件的文本是内联的** | 本 ESM 的 TES4 flags = 0x01（无 Localized 位）⇒ 目标文本可以内联写 `<Alias=SAQ_GuideTarget>`，不需要动游戏的 strings |
| **非常驻引用** | 168 条引导目标里 68 条是非常驻引用：格子没加载时 `Game.GetForm` 可能取不到（日志会写 `状态=2`） |
| **patch 文件集 ≠ 源码目录** | 改任何 `.as` 都要在 `build-saq.ps1` 的同步清单里登记（普通 + lrg 两套）；lrg 源码由 `make_lrg_source.py` 生成 |
| 静态过滤是启发式 | 宁缺勿滥；规则集中在 `filter_reason()` 一处 |
| 语言判定 | AS3 侧按引擎推来的任务名判定（中文/英文）；C++ 侧 INI 值只作日志 |
| 菜单关掉后 Movie 会销毁 | 已处理：每次「菜单由关变开」都 `UI::Reset()` 清解析缓存 |
| 性能 | 菜单打开时：202 次 `LookupByID` + 202 次标志位读取 + 1~2 次 0x4000 字节扫描；开着菜单时每 500ms 一次 `SAQ_Report`、每 100ms 一次 `SAQ_PeekGuide`。实测无感 |
| **树形列表不能按「数据下标」刷新**（第 14 轮踩过） | 显示下标 ≠ 数据下标（`InitializeEntries` 会把已完成 / misc 条目挪位、展开时插入子项），而且 `BSScrollingList` 没有 `UpdateEntry` 接口 —— 第 13 轮那次调用每次抛 `Error #1006` 被 `try` 静默吞掉。就地刷新一律走 `MissionsList.SAQ_RefreshQuestRow(uID)`（按 uID 在当前显示列表里找行） |
| **DLC 的字符串表是各自一份**（第 17 轮） | 任务名要在**该插件自己的** `<master>_{语言}.strings` 里查（破碎空间在 `ShatteredSpace - Main02.ba2`，地球舰队在 `SFBGS050 - Main.ba2`）。漏抽的症状是「DLC 任务全部消失」（取不到名字 ⇒ 被当成内部任务过滤），而不是报错 |
| **medium / blueprint 档位的 FormID 方案未验证**（第 17 轮） | 本项目实测的四个 master 都是普通插件（fullIndex<<24）。light/ESL（`0xFE000000 \| smallIndex<<12`）已实现但**本机没有样本可验**；medium / blueprint 直接标记不支持并整批跳过（`SAQ_Masters::ResolveOne`），宁可少收也不错收 |
| **master 名大小写不一致**（第 17 轮） | ESM 的 MAST 里写的是 `starfield.esm`，而文件夹里是 `Starfield.esm`；`Masters::FindLoadedFile` 用大小写不敏感比较（日志里会打出实际匹配到的名字） |
| **内嵌回退数据只有基础游戏条目**（第 17 轮） | `SaqEmbeddedPayload.inc` 必须写运行期 FormID，DLC 的离线算不出来 ⇒ 只放 Starfield.esm 的 202 条。C++ 推送成功前的那 0.3~0.8 秒里看不到 DLC 条目（可接受的降级） |
