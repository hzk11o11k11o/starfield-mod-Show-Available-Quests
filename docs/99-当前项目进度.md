# 99 · 当前项目进度

> 最新更新：2026-09-24（**第 158 轮：增量测试机制 —— 「先只测改动的部分，收口再全量」
> （玩家建议 / `AGENTS.md` 约定；明细 = `docs/09` 十五节）**）：
> ① **机制（三处；产品行为零变化）**：DLL 驱动器 **v73** —— ini `[Test] Only`（逗号分隔的
>   用例 id 子串，大小写不敏感、包含匹配）只跑命中的用例；空 = 全量；**没命中任何用例 ⇒
>   不跑 + WARN**（列出可用 id —— 防「写错后静默跑全量」白等一轮）；结果 JSON +**`only`**
>   字段。`build-saq.ps1 -Only`：部署时按参数重写 ini；**不传 = 清空 = 全量**（默认安全
>   方向 —— 忘传 = 多跑点，不会「残留上次子集 ⇒ 漏测」）。判读工具增量感知：
>   `check_results` 显示「增量筛选」行；`plan_regex_audit` 未选中用例不再计「缺段」bad；
> ② **用法**：增量 `-Harness -Only r47,r80`（≈1~2 分钟）↔ 收口全量 `-Harness`；
>   「修改 → 相关用例」映射表 = `docs/09` 十五·3；前缀写法自动命中同族（`r67` ⇒ 两条）；
> ③ **纪律**：**打 Nexus 包前必须跑一次全量**（判据五连照旧）；增量跑的结果不能当
>   「全量通过」用（结果 JSON `only` 非空 + `check_results.py` 会提示）；
> ④ **构建 / 验证（本轮已做）**：DLL 开发构建 **1245184 B**（SHA256 `FE1DDB7B…A3C61E`，
>   工作区 == 部署）；`verify --dev` **1142 检查 / 0 MISS**（+3 条 DLL 特征〔增量筛选命中行 /
>   未命中文案 / 驱动器 v73 串〕+ 1 条反向〔旧 v72 串不得残留〕+ ini 模板 2 条〔Only 生效键 /
>   说明〕+ 发布包默认值检查加「Only 必须为空」〔旧包无键 = 宽容通过〕）；离线层 5 步全过；
>   部署 ini = 测试态（`Harness=1` / `Only=`（空）/ `Plan=SAQ_TestPlan.txt` /
>   `AutoLoad=Save1_…142854_2_0_4` / `MaxSizeMB=10`）；
> ⑤ ★★ **待实机**（**启动游戏由玩家操作**）：先**增量跑**验证机制本身
>   （`-Only smoke,r47,r80,r155`，预期 4 条 PASS；判读 = 日志
>   `Only 增量筛选「…」⇒ 本次只跑 4/45 条` + 结果 JSON `only` 字段 + check_results 的
>   「增量筛选」提示行）；**全量 45 条**在下次收口 / 打包前跑（用例集无变化）。
>
> 上一轮（**第 157 轮：第 156 轮实机检查收口 —— 直读槽位缺陷修复（产品级）
> + 两条过时断言更新（明细 = `docs/16` 16.9）**）：
> ① **检查结果**（第 156 轮测试会话 21:55~22:08 / `sessionMs` 729437 / 主计划 45 条）：
>  **42 PASS / 3 FAIL**（`r47_board_marker` / `r80_repeatable_npc` / `r155_crew_probe`）；
>  通道健康 = `log_hygiene` 退出码 0（日志 1345727 B / 2336 行零坏字节、结果 JSON 干净）+
>  `plan_regex_audit` 172 断言 / 7 未命中（= 3 条 FAIL 同源 —— 断言 FAIL ⇒ 用例中止、
>  后续步骤不跑）；
> ② **r155 FAIL = 产品级缺陷**：`SAQ_Crew` 直读调的是 commonlibsf 标错的槽（**0x175**）——
>  1.16.244 实际 **0x174**（差 1 格；实机 = 12 位对照 4 位不一致、4 位真「可招募解锁」
>  被错藏、界面少 4 条、产品「过 0」）。**一手证据** = Papyrus native
>  `Actor.IsInFaction` 注册实现（RVA 0x1F1E7A0）就是 `call qword ptr [rax+0xBA0]`
>  ⇒ 槽 0x174（槽函数 RVA 0x1928CB0、接收 rdx 指针、签名吻合）；第二个注册点
>  （tail-call `jmp [rax+0xBA0]`）再次印证；新工具 **`tools/re/find_papyrus_native.py`**
>  把「名 → 注册点 → 实现 → 虚槽 → 槽函数头指纹」链路自动化（换版本一键重验）；
>  修复 = 显式按槽 0x174 调用 + 函数头 20 字节指纹校验（换版本 ⇒ 停用直读 + WARN +
>  保守放行）；日志 +`船员直读：槽 0x174 指纹=ok`；
> ③ **r155 第二发现（机制）**：`Game.AddToAvailableCrew` **不改变** A 位（`AvailableCrewFaction`）
>  成员关系（Papyrus 侧模拟招募往返实测：前置 0 → Add 后 0 → Remove 后 0）
>  ⇒ 「A 判据（已招募 ⇒ 隐藏）」仍无正样本（按玩家第 156 轮口径**保留**；用例断言改为
>  「判定行存在」+ 核心判据换成「**对照不一致 0 位**」）；沉淀 = 若正常游玩中出现
>  「已招募仍显示」观感问题再立项；
> ④ **r47 / r80 FAIL = 断言过时**（入口 21 → 45）：r47 改结构口径
>  （`入口=\d+(可导航 \d+｜marker [1-9]\d* 原板 \d+ 兜底 [1-9]\d* 不可用 [0-3])` ——
>  总数随引用加载状态变化，实测 29/32/33；「不可用」= 3 位 noNav 船员未加载时、实测 1）+
>  r80 改 `入口条目表=45 条（任务板 13 + 可重复 NPC 8 + 可招募船员 24）`（编译期常量）；
>  沉淀 = **断言不写「随运行期状态变化的总数」**；
> ⑤ **构建 / 验证**：DLL **1237504 B**（SHA256 `33BDD095…311E81A6`，工作区 == 部署；
>  Papyrus / SWF / ESM 未变）+ `verify --dev` 全过（+产品 3 条指纹串 + 计划检查更新 +
>  3 条反向检查〔旧 21 条 ×2 / 旧「A 位随招募往返」〕）+ 离线层 5 步全过；
> ⑥ ★★ **待实机**：主计划 **45 条**复跑（预期 **45/45**）—— 核心判读 = `crew.probe`
>  「对照 \d+ 位 / 不一致 0 位」/ `船员直读：槽 0x174 指纹=ok` / `船员门槛=` 的「过」= 4 /
>  界面 `crew=` 段（16 条）（**启动游戏由玩家操作**）；
> ⑦ **文档瘦身**（AGENTS.md「单文件 ≤ 100KB」）：`docs/99` **129735 → 97264 B** ——
>  状态表 `MO2 部署` / `游戏内实测` 两行的历代沿革（第 152 / 153 轮及更早，共 ~32 KB）
>  **逐字迁到 `docs/93` 附录 B**（只剪切不改写），原位留指针。
>
> 上一轮（**第 156 轮：可招募船员入口段落地 + 模拟招募正样本复核
> （玩家决策：两个都做；检查结果 = 42 PASS / 3 FAIL —— 已由第 157 轮收口，见上）**）：
> ① **入口段（kind=2「可招募船员」）**：`gen_entry_table.py` 合并 `ref/hirable_crew.json`
>  ⇒ 入口表 **21 → 45 条**（任务板 13 + 可重复 NPC 8 + 可招募船员 24）；段序固定
>  「任务板 → 可重复 NPC → 可招募船员」（布局硬校验：段序下标单调不减）；
>  **不新建 marker**（第 154 轮结论）；3 位位置不适合导航（贝蒂 / 埃里克 / 莫亚拉）
>  **丢弃兜底** ⇒ 平时不可导航（界面「（不可导航）」前缀 + 船员专用提示，不是 bug）；
>  名字 `（可招募）<名字>` 数据侧加好；
> ② **产品判据**（「显示 = P=1 / 隐藏 = A=1」）：新模块 **`SAQ_Crew`** —— DLL 直读
>  `Actor::IsInFaction`（commonlibsf vtable 175 条；本项目首个 Actor 虚调用，
>  正确性由同会话探针双来源对照钉死）；引用可读才判：**P=0（未解锁）/ A=1（已招募）
>  ⇒ 隐藏**，读不到 ⇒ 保守放行；ini `[Filter] CrewCond`（默认 1；0 = 只写日志）可关；
>  日志 `船员门槛=N(过A/藏B+C/未知K` + `船员隐藏: 名字[0x… 原因]` 名单 +
>  顺序行②新增 `｜可招募船员 N[…]` 段；
> ③ **界面**（SWF 重编 **stamp=66**）：type 102 —— 子项「招募他作为船员」+ 描述分支
>  （含「不可导航」的船员专用措辞 —— 不说「稍后重试」）+ 新 `crew=` 探针（SaqCrewProbe）；
> ④ **正样本复核**（Papyrus **op=8** + `crew.probe sim=`，驱动器 **v72**）：模拟招募往返
>  （Add → 读 → Remove → 读；前置 A=1 跳过写入；净副作用为零）+ 逐位**直读对照**
>  （`｜对照=一致/不一致`）+ 汇总行 +`｜直读读到 N 位` +`｜模拟招募：<判定>`；
>  r155 用例升级（`teleport.entry 0x00015062` → `crew.probe sim=0x00015062` → 断言
>  「A 位随招募往返 0→1→0（A 判据成立）」+ 五条反向断言）；**断言离线预演通过**
>  （正常 7/7 命中 / 异常 5 条反向全捕获）；
> ⑤ **构建 / 验证**：DLL **1236480 B**（开发构建）+ SWF **733596 / 733707 B**（stamp=66）
>  + PEX **21568 B**；已部署（测试态 `Harness=1` / `Plan=SAQ_TestPlan.txt` /
>  `AutoLoad=Save1_…142854_2_0_4` / `MaxSizeMB=10` + 补 ini `[Filter] CrewCond=1`）；
>  `verify --dev` **1130 OK / 0 MISS**（工作区 == 部署）+ 离线层 5 步全过
>  （黄金快照 19 件：2 处有意变化 = 入口 21 → 45）；明细 = `docs/16` 16.8；
> ⑥ ★★ **待实机**：主计划 **45 条**（含升级后的 r155）—— 判读要点 = `船员门槛=` /
>  `船员隐藏:` 名单 / 模拟招募判定行（**A 判据是否成立**）/ `crew=` 探针（过滤后条数）/
>  界面「（可招募）…」段 + 「（不可导航）（可招募）…」3 位（**启动游戏由玩家操作**）。
>
> 上一轮（**第 155 轮收口：主计划 45 条 45/45 全 PASS + 船员探针判读
> （P=可招募解锁 / A=已招募候选）**）：
> ① **会话**（21:16:26~21:25:53 / `sessionMs` **566375** / `驱动器 v71`）：判据五连全绿 ——
>   `check_results` **45/45**（含 `r155_crew_probe` PASS 9562 ms）+ `log_hygiene` 退出码 0
>   （日志 1298048 B / 2535 行零坏字节；结果 JSON 1123 行干净）+ `plan_regex_audit`
>   **169 断言 / 45 段全命中** + `verify --dev` 全过（工作区 == 部署：DLL **1222144 B** /
>   SHA256 `20AEFB7D…2D1FB1`）+ 离线层 5 步全过；`[E]` **0** / `[W]` **49**（48 推送失败
>   + 1 UI 桥 = 两组老现象）；
> ② **船员探针判读**（明细 = `docs/16` 16.7 / `docs/09` 十四·补三十九）：12/24 引用可解析
>   （其余按设计跳过 Papyrus）；**A=1 0 / C=1 0 / P=1 4 / 已分配 0**；招募任务 未开始 17 /
>   运行中 7 / 已完成 0。测试存档 = **2 级早期存档**（任务日志仅「一小步」）⇒ **无「已招募」
>   样本（预期）**；
> ③ **机制结论（官方脚本一手证据）**：**P 位 = 「可招募解锁」**（stage 1 片段
>   `SetFactionRank(PotentialCrewFaction, 1)`，注释 "make X recruitable as Elite Crew"；
>   非奖励款随引用加载 / 任务启动、奖励款随剧情里程碑、铁杆粉丝随特性任务）；
>   **A 位 = 「已招募」候选**（`Recruited()` 首次收费判据 `IsInFaction(AvailableCrewFaction)==false`
>   + `AddToAvailableCrew`；CRW 旧脚本已 OBSOLETE）；**招募任务运行状态不是判据**（运行 =
>   引用加载时 OnLoad 启动的载体；海勒 / 林 / 瓦斯科 / 拉斐尔运行中但 P=0）；
> ④ **下一步（待玩家定）**：入口段落地（kind=2「可招募船员」）—— 建议口径「**显示 = P=1 /
>   隐藏 = A=1**」；可选 = 先补一轮正样本复核（探针加「模拟招募」：`AddToAvailableCrew`
>   → 复查 A 0→1）；
>
> 上一轮（**第 155 轮：可招募船员跟踪 · 第二阶段 —— 入口数据 + 只读 faction 判据探针**）：
> ① **数据**（`docs/16` 16.6）：新工具 `tools/esm/gen_hirable_crew.py`（仿 gen_repeatable_givers.py，
>   复现 1~2 分钟）→ `ref/hirable_crew.json`（24 位：放置引用 / 两档常驻兜底 / crew faction
>   三件套 / 招募任务 **24/24** / 不可导航 **3** / 显示名「（可招募）…」）+ harness 探针数据表
>   `plugin/src/SAQ_TestCrewProbe.h`；两件进黄金快照（**19 件**）；
> ② **只读探针 `crew.probe`**（驱动器 **v71**）：逐位读 24 位 —— faction 侧走 Papyrus 新 op=7
>   （`IsInFaction` 三件套 + `GetCrewAssignment`，结果码 = 16+状态位；**引用未加载的位跳过**），
>   任务侧 DLL 直读招募任务运行时状态（不依赖引用加载 ⇒ 24/24 总有结果）；逐位 + 汇总都打
>   产品日志（红线六）；
> ③ **用例** `r155_crew_probe`（只读：不 reset / 不推 stage / 不传送；用例集 44 → **45**）：
>   断言 = 汇总行 + 三条链路健康反向断言（失败码 / 等回执超时 / 招募任务表单取不到）；
> ④ **判据线索（一手证据）**：官方 `Crew_RecruitQuestScript.Recruited()` 用
>   `IsInFaction(AvailableCrewFaction) == false` 判「首次招募才收费」⇒「在 AvailableCrew 里 =
>   已招募过」；`SQ_CrewScript._CustomSetRoleAvaliable` 调 `Game.AddToAvailableCrew`；
>   NPC_ 静态 SNAM rank=255 = **int8 -1**（不自动加入）——「静态挂着 ≠ 运行时成员」的机制解释；
>   实机探针负责证实（已招募 vs 未招募对比）；
> ⑤ **构建 / 验证（已部署完成）**：DLL（开发构建，**1222144 B**）+ PEX（**20507 B**，op=7）
>   已构建并部署（21:12 —— 首次部署被运行中的 `Starfield.exe` 锁住，游戏退出后由看护脚本
>   自动补部署）；`verify --dev` **1079 OK / 0 MISS**（工作区 == 部署）；离线层 5 步全过
>   （决策 24 用例 / 1201 断言 + 快照 19 件 + tripwire + P2 正则）；部署态 = 测试态
>   （`Harness=1` / `Plan=SAQ_TestPlan.txt` / `AutoLoad=Save1_…142854_2_0_4` / `MaxSizeMB=10`）；
>  ★★ ~~待实机~~ ⇒ ✅ **已实机收口**（21:16~21:25 会话，主计划 **45/45 全 PASS** + 判据五连
>   全绿；判读 = 顶部块 ②③ / `docs/16` 16.7 —— 本存档无「已招募」样本，P/A 判据定案）。
>
> 上一轮（**第 154 轮：可招募船员跟踪研究 —— 纯离线、零产品改动**）：
> ① **问题**（玩家提问）：可以被招募为船员的有名字 NPC 能不能跟踪？
> ② **结论：能**（数据齐备）—— **24 位**可招募船员（EDID 判据 `Crew_Elite_*`，
>   排除 OtherPlayer/BalanceData 模板；官方 FLST `CREW_imGui_2a_Actors_Elite`
>   0x00133A67 互为印证，但 FLST 会漏 `MathisCastillo`/`AdoringFan`）；每人
>   **1 处放置引用** + **1 条招募载体任务** `CREW_EliteCrew_<姓名>`（25 条，
>   含 OtherPlayer 模板款）+ crew faction 三件套（`AvailableCrew` / `CurrentCrew` /
>   `PotentialCrew`；Lin 例外 —— 剧情加入，无三件套）；
> ③ **可导航性**：23/24 非常驻（仅 Vasco 常驻）⇒ 与任务板同型、需兜底链；
>   **兜底候选 100% 就位**（内景 11/11 cell 有同 cell 常驻 · 外景 5/5 world 有
>   world 级常驻）⇒ 不新建 marker 也能配「精确引用 → 常驻兜底」候选链；XESP=0
>   （位置静态）；**3 处不适合直接导航**（Moara → 别名暂存格 / Betty、Erick →
>   运行时生成内景）⇒ 建议照第 91 轮「（不可导航）」处理；
> ④ **显示时机**（是否已招募）：候选判据 = faction 三件套（初始都挂 ⇒ 语义待
>   **实机探针**验证：招募前/后 IsInFaction + 任务状态 + Lin 对照组）；在此之前
>   可先全显示；
> ⑤ **产物**（零产品改动）：新工具 `tools/esm/scan_hirable_crew.py`（只读 · 1 分钟
>   复现）+ 快照 `ref/hirable_crew_scan.{log,json}`（不入库）+ 研究文档 **`docs/16`**；
>   ★ 口径教训：Starfield.esm 的 `QUST` 顶层组**混装** QUST（2077 条）+
>   INFO/DIAL/SCEN 20 万+ 条 ⇒ 按 EDID 找任务必须同时判记录签名。
>
> 上一轮（**第 153 轮：第 150/151 轮排序实机确认收口 —— 纯离线、零产品改动**）：
> ① **实机证据**（部署目录日志，常规态 `Harness=0`）：3 次菜单打开周期（07:08 / 07:09 =
>   v0.1.17 会话、**07:18 = v0.1.18 会话**）均打出第 151 轮两行顺序日志，内容逐项吻合
>   第 150 轮确认要点 —— 头部4 = 四大势力开头任务（超越极限 → 枝节横生 → 重返职场 →
>   深藏不露）、入口段 21 条**连续且位于其余段末尾**、**任务板 13 块连成一片**（新亚特兰蒂斯城
>   → … → 星钥站 → **追踪者联盟总部**）、其后才是 8 条「（可重复）…」NPC；全程**无
>   「顺序异常」**标记、无 WARN；
> ② **`v0.1.17 loading` 疑点闭环**：07:06 / 07:07 两次会话版本行为 v0.1.17（= 第 151 轮部署，
>   早于第 152 轮重部署）、07:17 会话 = **v0.1.18**（第 152 轮还原部署的开发构建）；
>   `Get-FileHash` **部署 == 工作区**（**1212416 B / SHA256 `FFBBC53D…E40697`**，与第 152 轮
>   记录逐字一致）—— 部署正确，无需动作；
> ③ **判据**：`log_hygiene` 退出码 0（日志 159 行 / 122188 B 零坏字节 + 结果 JSON 干净）；
>   r80 两条断言**离线预演命中 2/2、exit 0**（照第 147 轮「产品行原文离线预演」纪律 ——
>   合成日志两行原文**逐字节**取自部署日志、迷你计划两条 step **逐字**取自 `SAQ_TestPlan.txt`，
>   喂 `plan_regex_audit.py` 全部命中；临时文件已删、不入库）—— r80 正式转绿挂下次
>   harness 复跑（预期 165/165，届时「下一动作 = 上传 Nexus」之外零欠账）；
> ④ **0.1.18 的「待实机」标记清除** ⇒ Nexus 上传**标记完成**（2026-09-24 玩家约定：
>   上传时机由玩家自行决定 —— 不列入待办、以后不再记录与提醒）；发布构建 DLL（924672 B）
>   依旧无实机会话（第 149 轮教训：可选 = 包内 DLL 覆盖回部署目录真跑一次；风险低 ——
>   排序代码与开发构建同源）；
>
> 上一轮（**第 152 轮：0.1.18 上传包完成**）：
> ① **本版产品变化** = 第 150 轮入口排序修复（追踪者联盟总部板归位 —— 13 块任务板连成
>   一片、其后才是「（可重复）…」条目）+ 第 151 轮排序进日志（`列表顺序：` / `入口顺序：`）；
>   `dist\SAQ-ShowAvailableQuests-0.1.18.zip`（**1,885,694 B**，SHA256
>   `2C2E37174D609D80D4001ECAF1A8E1CDB91FEC18EA95FC4D3773814B1665E2FC`）；
> ② **版本号 6 处同步**（xmake.lua ×2 / main.cpp / build-saq.ps1 的 meta.ini /
>   package-saq.ps1 默认值 / nexus-description 标题）+ changelog v0.1.18
>   （README 走 {{VERSION}} 占位符；Nexus 中文/英文各一条）；
> ③ **包内 7 文件** = README.txt 10672 / ESM 4805 / SWF 733254 + 733359 / PEX 19133 /
>   发布 DLL **924672 B**（`EC462329…F947A22`，+3072 —— 第 150/151 轮排序代码）/ ini 5817
>   （[Test] Mode=0 + Harness=0 + AutoLoad 空 + MaxSizeMB=1）；`verify --release` 全过
>   + **打包后补跑 verify 覆盖新包**（7 文件、不含测试资产）；
> ④ **打完后已还原常规部署**：开发构建 **1212416 B**（SHA256 `FFBBC53D…E40697`，与第 151 轮
>   同尺寸不同哈希 = 版本号串变化）+ `Harness=0` / `AutoLoad` 清空 / MaxSizeMB=10
>   （工作区 == MO2；`verify --dev` 全过）；
> ⑤ **★ 待实机**：第 150/151 两轮均无实机会话 —— 排序效果与两行顺序日志待进游戏确认
>   （r80 两条断言随之转绿）；⇒ ✅ **第 153 轮已收口**（3 次常规会话实机确认 + r80 离线预演
>   2/2，见顶部块）；
>
> 第 151 / 150 轮的原始速查块 → 已迁 `docs/93` **附录 B3**（第 158 轮瘦身 · 逐字剪切）。
> 仍然生效的结论：第 150 轮 = 入口排序修复（追踪者联盟总部板归位进任务板段 + 生成脚本
> 布局硬校验）；第 151 轮 = 排序进日志（`列表顺序：` / `入口顺序：` 两行 + 「顺序异常」
> 标记）；两轮的实机确认 = 第 153 轮（3 次常规会话）。

> 上一轮（第 149 轮：0.1.17 上传包完成 ——
> `dist\SAQ-ShowAvailableQuests-0.1.17.zip`（**1,884,152 B**，SHA256
> `AD5C2D18605B7A0DB909234AAC99AC4CB5BA508F44D9204A611F205BF7FA6802`）；版本号 6 处同步
> （xmake.lua ×2 / main.cpp / build-saq.ps1 的 meta.ini / package-saq.ps1 默认值 /
> nexus-description 标题）；文案更新 = 「需靠近」清零（README-mod.txt 中英各 1 条 +
> Nexus 描述中英各 1 条 + changelog v0.1.17）；发布构建 DLL **921600 B**
> （`EED25E3C…6914BEF`，包内哈希 == 工作区）；`verify --release` 全过 +
> **打包后补跑 verify 覆盖新包**（7 文件、不含测试资产）；打完后已还原常规部署
> （开发构建 **1207808 B** + `Harness=0` / `AutoLoad` 清空；五件哈希 == 工作区，
> `verify --dev` **1060 OK / 0 MISS**））：
> ① **本版唯一产品变化** = 第 146 轮 DLC world 级兜底（候选池 **1021** / 「需要靠近」清零 /
>   玩家可见修复 = VKaiZ02 引导在任意距离即生效）；第 145 轮产物零变化、第 147/148 轮纯测试侧；
> ② **包内 7 文件** = README.txt 10672 / ESM 4805 / SWF 733254 + 733359 / PEX 19133 /
>   发布 DLL 921600 / ini（[Test] Mode=0 + Harness=0 + AutoLoad 空 + [Log] MaxSizeMB=1
>   强制还原 + 校验通过）；`dist\nexus-description.md` 同步更新；
> ③ **打包前离线层 5 步全过**（决策 24 用例 / 1201 断言 + 折叠内核自检 + 黄金快照 17 件 +
>   tripwire + P2 正则 18）；明细 = 本节 + `docs/14` 九节。
>
> 上一轮（第 148 轮，r147 复跑收口 —— 主计划 44 条 44/44 全 PASS
> （判据五连全绿 + 工作区==部署六件哈希一致）；`r146_vkaiz02_fb` 转 PASS（2141 ms，
> 产品行 `[5]`=可得 + 正/反断言双过）；产品零缺陷、本轮零产品改动）：
> ① **复跑事实**（06:32~06:41:54 会话 / `sessionMs` **571500** / 44 用例）：`check_results`
>   **44/44**（PASS 44 / FAIL 0 / SKIP 0）；`log_hygiene` 退出码 0（日志 **1133074 B** / 2199 行
>   零坏字节；结果 JSON 1104 行干净）+ `plan_regex_audit` **163 断言 / 44 段全命中**
>   （0 未命中；3 条 nolog 提示 = `scope=prev` 窗口语义老现象）+ `verify --dev` 全过 +
>   离线层 5 步全过（决策 24 用例 / 1201 断言 + 折叠内核自检 + 黄金快照 17 件 + tripwire +
>   P2 正则 18）；`[E]` **0** / `[W]` 49（48 条「推送失败（第 1 次）」+ 1 条 UI 桥解析失败
>   = 两组老现象）；
> ② **r146 用例判读**：`r146_vkaiz02_fb` PASS（2141 ms）—— 产品行 `引导探针 候选可得性：
>   快递员（0x0105FC74）｜[1]「莉亚娜·杜尔可夫」0x0105FD14=可得｜…｜[5]「SFBGS001_VKaiZ02_
>   LijanaStandMarker01」0x01114EC2=可得` 如期进日志（红线六第二例修复生效）；正断言
>   （`[5]`=可得）+ 反断言（`[5]`=取不到）双 PASS ⇒ 第 146 轮 DLC world 级兜底实机成立；
>   第 147 轮两处用例侧修复（guide.probe 补产品行 + 断言按产品行格式重写）一击收口；
> ③ **一致性**：工作区 == 部署 —— DLL **1207808 B**（SHA256 `C3235C6F…`）/ ESM 4805 B /
>   SWF 733254 B + lrg 733359 B / PEX 19133 B / 用例计划 44 条，六件哈希全一致；部署态 =
>   测试态（`Harness=1` + `AutoLoad=Save1_…142854_2_0_4` + `Plan=SAQ_TestPlan.txt`）；
> ④ 本轮无产品改动、无数据产物变化；明细 = `docs/09` 十四·补三十七（第 147 轮处置）。
>
> 更早（第 147 轮，r146 首跑检查收口 —— 明细 = `docs/09` 十四·补三十七）：首跑 43 PASS /
> 1 FAIL（`r146_vkaiz02_fb` 断言超时；真因两处全在用例/harness 侧 —— 断言把候选 [1] 名字
> 当任务名 + `guide.probe` 无产品行，红线六第二例；产品侧零缺陷）⇒ `SAQ_Test.cpp` 补
> `REX::INFO("引导探针 {}", detail)` + 用例断言按产品行重写 + verify +2（1061 → 1063 行）；
> ★ 新纪律：改断言后、重建前先拿首跑日志的探针输出**原文离线预演**。
>
> 更早（第 146 轮，DLC world 级兜底收口 —— 明细 = `docs/14` 九节）：`gen_guide_targets.py`
> 两处口径不一致（world 池键低 24 位 vs in-file 全 FormID + 引用号被掩）⇒ DLC world 池从未
> 建立；修后候选池 1020 → **1021**、「需要靠近」1 → **0**、`Mode=6` 清单 1 → **0**（模式保留
> 作回归探针）；旧/新代码 run 逐任务对比**只有 VKaiZ02 变化**；判据全绿（5 步 + 黄金快照仅 2 处
> 有意变化 + `verify --dev` 1061 行 / 0 MISS）；FAB 44 条复审 + `City_Neon_Chem03` 复核收口
> ⇒ 「下一步大项候选」只剩「新官方内容盘点」等未来项。
>
> 更早（第 145 轮，operator 三期 —— 明细 = `docs/08` 4.8）：cmp∉{0,1} 折叠产品化 +
> type 低字节解析缺陷修复 + tripwire 三类报红；**产物零变化** + A1 选题池移除
> （**启动游戏由玩家亲自操作**）。
>
> 更早（第 144 轮，已收口 —— 明细 = `docs/15` 十五节）：P2 会话 **6/6 全 PASS** ⇒ P5
> 双形态回归底线达成（常规态 43/43 + P2 态 6/6）+ **0.1.16 包**（双形态发布 + 结构指纹
> 自检；★ 教训 = 发布构建要定期真跑）—— **UI 注入研究（P1~P5）全部收口**。
>
> 更早（第 133~142 轮，速查 —— 明细 = `docs/15` 十三/十四/十五节）：**142** P2 6/6 全 PASS +
> 「眼睛判据」全部去除（计划零人工观察）；**141** 探针 `ctx.menu` 未接线修复（对照产品路径
> `接管=ok` 一击定位）；**140** P3-b 眼睛达成 + P4 交互接管（按钮劫持 X/Y / `itemActivated`
> 拦截 / 星图交接 = `ProcessUserEvent("Missions",false)` 真关菜单原语）；**139** P2 5/5 全 PASS
> + 真实操作二次验证；**138** 产品退避（`activateFailed`）+ 测试隔离（`ui.mode`）；**137** P3-b
> 落地（`[UI] UiMode=swf|auto|inject` 默认 auto）；**136/135/134** P3-a 两处产品级缺陷修复
> （数据生命周期 / `InjectCtx::list` 接线）⇒ 复跑 4/4 + 眼睛达成；**133** P3-a 落地
> （`SAQ_UiInject.{h,cpp}` + `ui.inject`）。
>
> 更早（第 132~117 轮，速查）：**132/131/130** P2 会话全 PASS + 探针 v4（`ui.research4` +
> `CreateObject` 回调劫持）/ **129** 功能迁移评估 / **128** operator 二期 / **127** HUD 提示时机
> 修复 + P2 双收口 + 0.1.15 包 / **126** 43/43 全 PASS / **125** 路线 D 冲突检测 /
> **124~120** P2 眼睛达成 · 复跑 · 首跑（缺 `aObjectives` 真因）· P2 落地 · 42/42 /
> **119~117** 探针 v2 + UI 注入研究落地。
>
> 更早（第 116~96 轮，速查）：DLC 链式三期 + 可重复任务（`docs/11`）+ 任务专属图标 +
> 日志上限可配（`[Log] MaxSizeMB`）+ 追踪者联盟 + 引导质量 2.0 + operator 全量产品化 +
> 覆盖面复盘 + 0.1.10~0.1.14 包。
>
> 更早（第 95~61 轮，速查）：`plan_regex_audit` + 判据定稿 + 「（不可导航）」前缀 +
> 进度门槛 OR 组（`orBit`）+ operator 全解 + 驱动器 **v70** + 同伴条目文案 + 地球地标 +
> 入口第二形态（可重复 NPC）+ DLC INFO 门槛。
>
> 更早（第 60~43 轮，速查）：分诊探针 `ev=`/`btn=` + 星图由脚本**轮询节拍**打开 +
> 引导候选池 + 结果码 5 / INFO 门槛 + 引擎内 harness + `stamp=` 指纹 + 0.1.1 包。
>
> 明细/复盘：`docs/91`（120~124 及补）/ `docs/93`（106~119）/ `docs/95`（43~60 等）/
> `docs/96`（35~40）/ `docs/97`（53~55 等）/ `docs/98`（9~16）/ `docs/11` / `docs/06`。
> （第 140 轮瘦身：被压缩的原始速查块原文见 `docs/91` 末尾附录。）

## 一、当前状态

| 组件 | 状态 | 位置 |
| --- | --- | --- |
| SFSE 插件（DLL） | ✅ 已构建（多 master 前缀探测 + 引导结果确认 + 认领已有引导 + 脚本活性探测 / 引导未生效收尾（第 19 轮）+ 控制台测试过滤（第 20 轮）+ 目标 FormID 拆分 / 自检上限修复（第 21 轮）+ 确认窗口改绑菜单状态（第 26 轮）+ **无限任务入口条目表 / 引导确认与停顿降噪**（第 27 轮）+ 入口引导候选链（第 30 轮）+ **候选链改「精确优先」（任务板自身）+ 引导目标动态更新 + marker 探测**（第 31 轮）+ **菜单关着时例行认领 + 通道对账**（第 32 轮）+ **菜单关着时预建静态表 / `Masters::Resolved()`（认领不再因表没建好而误判）**（第 33 轮） + **SET COURSE 星图（状态 5 + kHide 关菜单 + 星图探测）**（第 37 轮） + **星图自动重试 / 地点候选状态 5·6·7 / 引擎节点解析只读诊断（第 40 轮）** + **星图注册名修正 `GalaxyStarMapMenu` / 节点解析器特征字节修正（第 41 轮）** + **菜单关闭时现场读界面报告 / 补一次引导请求轮询（第 43 轮）** + **星图重试等待 3 s → 1.5 s 与脚本新窗口对齐 / 日志写明星图唯一入口（第 44 轮）** + **引导目标候选池（第 45 轮：预选可得候选 / 状态 2 换候选 / 菜单关着时动态复算 + 认领带下标）** + **引导可用性/提示收口（第 46 轮：`needsApproach` 判定 / 结果码 5 / `PollApproachRetry` 退避重试 / 星图本次不打开 / 测试过滤新增 `Mode=6` + 上限常量修复）** + **引导可用性收口（第 47 轮：同 cell 常驻兜底）+ 判据收口 + 大项 D（第 48 轮：`AllCandidatesNonPersistent` 判据回退（20 条）/ `EvaluateInfoGates` INFO 门槛 / 认领后 8 秒复算宽限期 / 星图与确认文案修正 + 同日补丁：**引擎已开始 ⇒ 放行**） + **harness 首测复查（第 49 轮补丁：无参入口 0 参调用 / 结束清菜单）+ 读档复算抖动（第 49 轮补丁②：候选降级观察期 20 秒）**） + **同伴好感度任务（第 74 轮：`companionPin` 固定显示 / 名字前缀 / 排序前置）** + **四大势力开头任务（第 75 轮：`factionEntry` 固定显示 + 固定排前四 / `IsGatePinned` 统一放行判据 / `PinnedOrderKey·Less` 排序（离线层）/ `FormatPinStats` 单独一行 / 说明写进载荷第 9/10 列）**）**） | `plugin/build/windows/x64/releasedbg/SAQ_ShowAvailableQuests.dll`（**1207808 B**（第 148 轮实测），开发构建含 harness + 注入层（第 138 轮 +1024：激活失败不再重试 / `ui.mode` 隔离 op）；★ 第 112 轮起日志上限可配） |
| ★★ 同伴好感度任务（第 74 轮） | ✅ **4 同伴 / 8 条**：入口 4 条（个人任务，`companionPin=1` ⇒ 固定显示 + 名字前缀 + 描述好感度提示）；后续 4 条（承诺任务 ⇒ 补齐启动边并入链式门槛，前置没到不显示）；**前置到列表开头**（按同伴分组、入口在后续前；载荷/内嵌回退同序）；日志 `同伴固定=N(…)` + `同伴分组前置=N` + 界面 `order=` 顺序探针；SWF `stamp=56`（★ 第 82 轮：描述文案说透 ⇒ `stamp=60`） | `ref/companion_quests.json`（`tools/esm/gen_companion_quests.py`）｜`SAQ_QuestTable.h`（`companion`/`companionPin`/`kCompanionNamesZh·En`）｜`SAQ_Decision.*`（`DecideGateAction`）｜`MissionMenu.as`（`SaqCompanionNote`）｜`MissionsList.as`（`SAQ_OrderProbe`） |
| 静态任务表 | ✅ **280 条 / 5 个 master**（★★★★★★ 第 146 轮：候选池 1020 → **1021** —— 最后 1 条无兜底任务 `SFBGS001_VKaiZ02` 补上 DLC world 级常驻兜底，见 `docs/14` 九节）（★★★ 第 128 轮（operator 二期）：门槛 `want` 全部来自**生成期折叠**（`!=`/`>`/`>=`/`<`/`<=` 与 `==` 同源；表内数据零变化，`docs/08` 4.7）；★★★★★ 第 114 轮：候选池 **1020** —— +19 条 world 级兜底，见 `docs/14`）（★★★★ 第 110 轮：+2 条追踪者联盟 —— `SFBGS003.esm` 是 **medium 档**，见 `docs/13`）（★★★ 第 106 轮：+7 条无 QTYP 漏收补收，见 `docs/12`）（★★★ 第 81 轮 +10 条「地球地标任务」）；★ 第 45 轮引导目标升级为**候选池**：`kGuideCandidates[]` **931 条候选 / 209 条有目标**（★ 第 47 轮：为「全非常驻候选」的 76 条任务里 56 条补了**同 cell 常驻兜底**（+56 条、常驻 339 条）；`candBegin/candCount` 切片；旧单目标字段已移除）；★ **第 48 轮（大项 D）新增 `kInfoGroups[]` / `kInfoConds[]`（INFO 门槛：60 条任务 / 290 条对话 / 341 条条件 / `infoGroupBegin/infoGroupCount` 切片）**；★★ **第 74 轮行尾 +2 列（`companion` / `companionPin`）+ 名字前缀**；★★★ **第 75 轮行尾 +1 列（`factionEntry`）+ 4 个数组（`kFactionEntryNamesZh·En` / `kFactionEntryNotesZh·En`）—— 四大势力开头任务固定显示 + 固定排前四；CF01 的 7 个引导候选被清 ⇒ 候选 931→**924**、有目标 209→**208**、链式边 55；★★★★ **第 81 轮：候选 924→**960**、有目标 208→**217**（+10 条地标任务里 9 条各 4 候选 —— 伦敦只给说明 = 0 候选）****；★★★★★ **第 109 轮（大项 A②）：候选 960→**996**、有目标 **224** —— 第 106 轮补收的 7 条补上引导候选（`guide_targets.json` 重跑：新增 7 / 消失 0 / 既有候选 0 处变化），内嵌载荷同步（`stamp=64`）****；★★★ 第 87 轮：`kQuestConds` **11 条**（进度门槛支持 OR 组 —— 见 `docs/08` 4.4）；★★★★ **第 89/90 轮：行尾 +`repeatable` 列 + `kRepeatableNotesZh·En` —— 可重复任务 15 → **20 条**（第 90 轮 +DLC 的 SFFL 家族 5 条：3 条在锚点星际站有接取人、2 条随机太空遭遇无接取点），见 `docs/11`） | `plugin/src/SAQ_QuestTable.h` |
| ★★ 入口条目表（第 27/28/29 + **80** 轮；★ **110** +1；★ **150** 定序；★ **151** 顺序进日志；★★★ **156 船员段**） | ✅ **45 条 = 13 任务板 + 8 可重复 NPC + 24 可招募船员**（★★★★★★ 第 156 轮：+24 条**可招募船员**（kind=2，明细 `docs/16` 16.8）—— 数据 `ref/hirable_crew.json`；段序固定「任务板 → 可重复 NPC → 可招募船员」（布局硬校验：段序下标单调不减）；**不新建 marker**（兜底候选已就位）；3 位「位置不适合导航」（贝蒂 / 埃里克 / 莫亚拉）**丢弃兜底** ⇒ 界面「（不可导航）」+ 船员专用提示；DLL **P/A 判据过滤**（`SAQ_Crew` 直读 `Actor::IsInFaction`：P=0 未解锁 / A=1 已招募 ⇒ 隐藏；读不到 ⇒ 保守放行；ini `[Filter] CrewCond`）+ 日志 `船员门槛=` / `船员隐藏:`；类型 102 文案 + `crew=` 探针）（★★★★★ 第 151 轮：**顺序进日志** —— 每次开菜单打 `列表顺序：…`（分段 + 头部 + 入口段位置判定）与 `入口顺序：…`（入口段显示名顺序 = 界面顺序），顺序异常带「**顺序异常**」+ WARN；`SAQ.cpp` 的 `OrderScan` / `FormatListOrder` / `FormatEntryOrder`，判据 = verify 两条特征 + r80 两条断言）（★★★★ 第 150 轮：**行顺序 = 界面列表顺序**（DLL 按表追加 + 组内稳定排序）—— 段序固定「任务板段 → 可重复 NPC 段」；追踪者联盟总部板此前在**表末尾**（混进「（可重复）…」堆，玩家反馈）⇒ 已挪进任务板段末尾 + 生成脚本新增**布局硬校验**）（★★★ 第 110 轮：追踪者联盟总部悬赏信息台 —— SFBGS003 的板，master=1，候选链 = 常驻展示柜 → 总部外 marker）（★ 第 80 轮：`（可重复）贸易管理局 · 邓肯·林奇` 等 4 位商人（RAD03）+ `（可重复）追踪者联盟探员 · <城市>` ×4（RAD04）；「（可重复）」前缀**只在 NPC 条目**上 —— 玩家明确任务板不加）；uID = 引导目标 = 条目引用；ini `Mode=5` 只显示它们；★ **第 30 轮：引导目标 = 候选链**（① 条目引用自身（精确）→ ② 新建常驻 XMarker（任务板 0x900~0x90A / NPC 0x90B~0x911）→ ③ 兜底：内景 = 同 cell 原生常驻引用 / **外景 = 该 worldspace 的世界级常驻引用**），DLL 依次 `LookupByID` 取第一个命中的 + 诊断日志；第 29 轮 override 路线被实机否定（见一·补十五） | `plugin/src/SAQ_EntryTable.h`（`tools/esm/gen_entry_table.py`，+kind 列）；清单 `ref/entry_targets.json`；NPC 数据 `ref/repeatable_givers.json`（`tools/esm/gen_repeatable_givers.py`）；新建 marker `tools/esm/create_board_markers.py`（产物 `ref/board_markers.json`，★ marker 记录签名必须 REFR）；侦查 `tools/esm/scan_entry_persistent.py` |
| ★★ 可招募船员（第 156 轮；★★★ **第 157 轮直读修正**） | ✅ **24 条**（精英船员 `Crew_Elite_*`）：口径「**显示 = P=1 / 隐藏 = A=1**」；**DLL 直读按 vtable 槽 0x174**（`SAQ_Crew` 新模块；★ 第 157 轮修正 —— commonlibsf 的 Actor.h 标 0x175 = 比 1.16.244 实际多 1 格，一手证据 = Papyrus native 实现 `call [rax+0xBA0]`，见 `docs/16` 16.9；带槽函数头 20 字节指纹校验防版本变化；正确性由 `crew.probe` 的 Papyrus×直读双来源对照钉死 —— 修复后应「不一致 0 位」）；★ 机制记录：`Game.AddToAvailableCrew` 不改变 A 位（A 判据无正样本，按玩家口径保留）；引用可读才判（P=0 未解锁 / A=1 已招募 ⇒ 隐藏；读不到 ⇒ 保守放行）；ini `[Filter] CrewCond`（0 = 只写日志不隐藏）；日志 `船员门槛=N(过A/藏B+C/未知K` + `船员隐藏: 名字[0x… 原因]`；界面 type 102（子项「招募他作为船员」+ 可招募描述 + 「不可导航」船员专用措辞）+ `crew=` 探针；模拟招募复核（Papyrus **op=8**：Add→读→Remove→读，净副作用为零；A=1 前置跳过写入）+ `crew.probe sim=`；用例 `r155_crew_probe` 升级（teleport.entry → sim → 判定断言 + 五条反向） | `ref/hirable_crew.json`（`tools/esm/gen_hirable_crew.py`）｜`SAQ_EntryTable.h`（kind=2 段）｜`SAQ_Crew.*`｜`scripts/SAQ_Main.psc`（op=8）｜`ui/missionmenu/src/MissionMenu.as`（type 102 / `SaqCrewProbe`） |
| ★★ 地球地标任务（第 81 轮） | ✅ **10 条**（Apollo / Cairo / Dubai / HongKong / London / LosAngeles / NewYork / Osaka / Shanghai / StLouis）：豁免「地标」过滤 + 引导 = 书（或书商）的引用 + 同 cell / **world 级**常驻兜底 + 说明文本（载荷最后两列 = 描述第一句）；**伦敦只给说明**（书在书店/书堆，无固定接取点）；`Opportunity`（火星机遇号）**不收**（无触发物品，属主线 Unearthed 流程） | `ref/landmark_quests.json`（`tools/esm/gen_landmark_quests.py`）｜`SAQ_QuestTable.h`（`landmark` 列 / `kLandmarkNotesZh·En`）｜`SAQ.cpp`（说明来源第二分支） |
| 运行时过滤 | ✅ **只挡「已完成」**；「已接取」只认玩家任务日志；**master 没加载 ⇒ 整批跳过** | `plugin/src/SAQ_QuestState.*` / `SAQ_Masters.*` |
| 进度门槛（第 35 轮；★★ 第 87 轮 OR 组） | ✅ **7 条任务 / 11 条条件**（★ 第 87 轮：支持 **OR 组** —— `orBit` 列 + 引擎算法 `acc/orAcc/inOr`、`kUnknown` 一律放行；FFConstantZ06 = `UC04 完成 AND (Z04 完成 OR Z05 完成)`，见 `docs/08` 4.4）：「进度没到不显示」首版（第 35 轮）：记录级条件里「引用别的任务」的 GetQuestRunning/GetQuestCompleted/GetStageDone；求值不了 ⇒ 放行；ini `[Filter] ProgressCond` 可关；日志 `进度门槛=N(过A/藏B/未知K)` + `进度没到:` 名单。**已实测通过**（14:47 会话：过3/藏4/未知0；第 87 轮实测：A 段藏「亲爱的姐妹」/ B 段放行，见一·补二十第 6 节与 `docs/08` 第七节） | `plugin/src/SAQ_QuestCond.*`；数据 `ref/ctda_gates.json`（`tools/esm/analyze_ctda.py` → `gen_quest_table.py`） |
| ★★ INFO 门槛（第 48 轮 · 大项 D；★★ **第 78 轮扩到 DLC**；★ **110** +SFBGS003） | ✅ **84 条任务 / 425 条对话 / 512 条条件**（★★★★ 第 110 轮 +1 组：SFTA00 的总部对话）（★★ 第 106 轮：INFO 侧 OR 组纳入，18 条 orBit=1 条件，见 `docs/08` 4.5）（= 基础游戏 60/290/341 + **DLC 20/128/151**：SFBGS00D 2 / SFBGS050 7 / ShatteredSpace 11；跨 master 前置已支持 —— 前置的 master 下标按 kQuestMasters 顺序写进 `kInfoConds`）（「进度没到不显示」第二判据，对话侧）：任务自己的对话（INFO）里「引用别的任务」的同类条件；按「自引用」分成入口类/中性类（排除推进类）；**全部参与对话都有已知假条件 ⇒ 隐藏**（成对条件自动豁免）；求值不了 ⇒ 放行；★ **实测通过（21:46 会话：命中 58 条 / 藏 21 / 放行 2 / 未知 0，名单抽查合理）**；★ 同日补丁：**引擎已开始 ⇒ 放行**（判据收窄为「引擎没启动 + …」，修掉『全数到期』类误藏）；ini `[Filter] InfoCond` 可关；日志 `INFO门槛=…(过A/藏B/放行C/未知K)` + `INFO没到:` 名单 | `plugin/src/SAQ_QuestCond.*`（`EvaluateInfoGates`）；数据 `ref/info_gates_final.json`（`tools/esm/scan_info_gates.py` 逐 master 扫 4 份 `info_gates*.json` → `analyze_info_gates.py` 合并 → `gen_quest_table.py`；构建脚本在原始扫描缺失时自动补扫） |
| ★★ 任务链门槛（第 67 轮；第 68 轮实测对齐；★★ 第 69 轮扩到非编号链路；★★★★ **第 71 轮「全量入边审计」**；★★★★★ **第 77 轮「RIR 留专项」收口**；★★★★★★ **第 98 轮 DLC 收口**；★★★★★★★ **第 101 轮 DLC 二期 +15 边**） | ✅ **表内 84 条任务 / 90 边**（★★★★★★★★ **第 110 轮 +1 边**：`SFTA00 ← SFBGS003_MiscPointer@1000` —— 追踪者联盟，官方 fragment 原文核验）（★★★★★★★ **第 101 轮 +15 条 DLC 边**：另一边 MQIN ← 狂热逾界@4000 / 信念之争@7000 / 发掘过去@1600；地球舰队六级链（含 need_stage 的 **深入VOID ← 失踪的华庭号@1700**）；自由航道三条；**有意不收** MQIntro / DialogueHVDazra@5 / VKaiZ03* —— 见 `docs/06` 十节）；★★★★★★ **第 98 轮 +6 条 DLC 边**：破碎空间主线 `MQ02←MQ01@10000` / `MQ_Shell←MQ02@2000` / `MQ03·MQ04·MQ05←MQ_Shell@100` / `MQ06←MQ_Shell@1100`（官方 `.pex` 反编译取证，`tools/esm/gen_dlc_chain.py` → `ref/quest_chain_dlc.json`，host master = 3，见 `docs/06` 八节）；★★★ **第 77 轮 +8 边**：龙神全线 —— `RIR01←RI01@1300` / `RIR03←RIR02@300`（**故事事件**形态，新 op `SendStoryEvent`）/ `RI02←RIR03@300` / `RIR04←RI02@600`（玩家报告的那条）/ `RIR05←RIR04@300`（故事事件）/ `RI03←RIR05@10000` / `RIR06·RIR07←RI_Support@610·700`（Masako 可重复 radiant 的 managerial 边）= 编号链 25/26（`tools/esm/gen_quest_chain.py`，工具 31 目标 / 33 边）+ **扩展边 32/34**（第 69 轮 8/8：Eleos 静修地线 / 霓虹城帮派线 / 城市支线预启动；★★ **第 71 轮 +16 目标/18 边**：CF 线全家（深藏不露 ← UC02@860、终局两分支 ← LC088_Space@100/200、星钥站支线 ← CF08_SysDef@10、罪证的负担 ← CF01@310）+ FC08 + 城市线三连 ×2（RedTape / Viewport）+ MS05/MS06 引入任务 + 同伴 ×2（承诺：巴雷特 / 哈特家事）—— 起因 = 玩家实测「深藏不露 / 宝藏号的结局 / 夺回曾经」不该显示；全量审计 = `tools/esm/analyze_start_paths.py`（737 条启动调用逐条人工核实），其余候选按 A~H 八类记入「有意不覆盖」，见 `docs/08` 第十二节；人工核实 + 构建期源码核验）；判据「**全部启动边都没触发 ⇒ 隐藏**」（边之间是「或」：任一条已触发/求值不了 ⇒ 放行）；ini `[Filter] ChainCond` 可关；日志 `链式门槛=N(过A/藏B/未知K)`（N = **走到链式判定**的任务数；15:24 实测 **22** = 表内 25 − 3 条已被 INFO 门槛先藏）+ `链式没到:` 名单；★ 实测：A 段 过0/藏22 ✓、B 段 过1/藏21 ✓（「菜鸟觐见」在/不在名单）；★ 起因 = 玩家实测「深红舰队线的菜鸟觐见 / 风驰电掣在存档没开始时也显示」 | `plugin/src/SAQ_QuestTable.h`（`kChainGates`）；`SAQ_QuestCond.cpp`（`EvaluateChainGates`）；`SAQ_Decision.cpp`（`DecideChainGates`）；数据 `ref/quest_chain.json` + `ref/quest_chain_extra.json` + `ref/quest_chain_dlc.json`（第 98 轮） |
| 引导数据 | ✅ **225/280（80%）**（★★★★★★ 第 146 轮：**DLC world 级兜底口径修复** —— 候选 1020 → **1021**、「仍无兜底」1 → **0**、`Mode=6` 清单 1 → **0**（模式留作回归探针），见 `docs/14` 九节）（★★★★★ 第 114 轮：**两档常驻兜底**（同 cell → world 级）—— world 级 19 条、仍无兜底 20→**1**、候选 1001→**1020**、19 条 `needsApproach` 1→0，见 `docs/14`）（★★★★ 第 110 轮：+SFTA00 的 5 个候选 —— 1 号特工领衔；赏金狩猎 = noPickup 0 候选）（★★★ 第 109 轮：7 条补收任务的引导候选已补 —— 候选池 931→967（进表 996 条 / 224 条有目标），首候选见顶部第 109 轮块；新增 7 / 消失 0 / 既有候选 0 处变化）；★ 第 45 轮：**候选池 + 质量分级**（第一候选质量等级 1=204 / 2=3 / 4=2；**82 条**任务的第一候选被换成更好的目标 —— 含玩家实测抱怨的「平衡账目」「群众心态」）；★ 第 47 轮：**同 cell 常驻兜底**（`find_fallbacks`；「营救机器人」= `0x08ECA5` 非常驻 + `0x08ECA6` 常驻兜底链）；**无目标的 54 条已归因**（第 24 轮结论：不补猜的目标；★ 第 106 轮 +7 条补收任务的引导见第 109 轮） | `ref/guide_targets.json`（工具见 `docs/05` / `docs/06`；归因见 `docs/07`；持久性统计 `tools/esm/ref_flags_report.py`） |
| 代理任务（ESM） | ✅ 已挂 Reference Alias + Objective + 测试开关 / 目标高位 GLOB（0x804/0x805，第 20/21 轮，VMAD 已绑定）（手工字节补丁，幂等）；★ **第 30 轮：+11 条新建常驻 XMarker 引用**（记录号 0x900~0x90A，`Top 'CELL' > … > CellPersistent`）；★ **第 33 轮：+11 条 CELL 记录（官方「空壳」写法 = 只留 EDID + flags 0x4000，紧跟各自的 CellChildren 组）** —— 官方对同一条 cell（SFBGS003 → 霓虹城）就是这么写的；xEdit 验证 11/11 + 重存不丢；★ **第 125 轮：+1 条 GLOB `SAQ_UiNotice`（0x80E）⇒ ESM 4752 → 4805 B** | `esm/SAQ_ShowAvailableQuests.esm`（`tools/esm/patch_saq_esm.py` + `create_board_markers.py`；结构对照工具 `tools/esm/dump_cell_group.py`） |
| 引导脚本 | ✅ 已编译（`StartTimer` 轮询 + `ForceRefTo` + `SetActive` + OnInit 重挂自愈 + 菜单事件 Trace + 菜单打开重挂定时器 + 三个地点候选（第 40 轮）+ 星图请求失效校验（第 42 轮）+ **第 44 轮：星图待办改由「轮询节拍」推进**（`StarMapPendingTicks` + `ProcessStarMapPending`；旧的 1.5 秒延时定时器退役 —— 实测它被「星图提前打开 ⇒ 暂停」冻死，一次都没跑到；节拍到点 = 菜单已关、游戏在跑）；★ 第 27 轮定案：菜单开着时定时器不走，引导只在关菜单时应用 —— 与原版一致）+ ★★ 第 46 轮：HUD 提示机制通用化（`NoticeText`/`ShowNotice`/`ProcessNotice`，替代星图专用版）+ 「目标尚未加载」提示（重复 3 次、冷却 5 分钟）+ 失败 Trace 节流（前 3 次 + 每 10 次） | `scripts/SAQ_Main.psc` → `Scripts/SAQ_Main.pex`（**17666 B**，第 49 轮加测试命令执行器后） |
| SWF 内嵌回退数据 | ✅ **9.8 K 字符**载荷（**只含基础游戏 219 条任务**；★★ 第 81 轮 +10 条地标任务（含说明列）；入口条目不在内嵌里，降级窗口 ~0.3s 可接受）；★ 第 74/75 轮：顺序与 C++ **逐条同序**（前 4 条 = 四大势力开头任务 → 8 条同伴任务 → 其余），列序含「简要说明」两列 | `ui/missionmenu/saqdata/SaqEmbeddedPayload.inc` |
| UI（missionmenu.swf / _lrg） | ✅ 已重编译（★★★★★★ 第 156 轮：type 102「可招募船员」—— 子项「招募他作为船员」/“Recruit them as crew”、描述分支（可招募 + 已招募的不会再出现 + 引导操作）、「不可导航」船员专用措辞（**不说「稍后重试」**）、`crew=` 探针 `SaqCrewProbe`；`stamp=66`；SWF **733596 B / lrg 733707 B**）（+ 接取后自动取消引导 + 结果码 4 回滚 + drop= 名单（第 19 轮）+ 任务板入口文案（第 27 轮）+ **第 29 轮：「太远」文案退场** + **第 37 轮：R 传 `map=1/0` 标记（peek 三段协议）** + **第 38 轮：R 成功后界面侧走原版 `CloseMenu(true)` 关掉整个暂停菜单（peek 四段协议）** + **第 39 轮：R 不再取消引导（`SaqToggleGuide` 第三参数；重复按 R = `重复设定航线:` + 再开星图）** + **第 42 轮：报告新增 `press=[…]` / `sel=[…]`（按键那一刻用的是哪一行 + 列表选中项，怀疑「R 选错条目」时的硬证据）+ R/Enter 两条路都先取一次 `selectedEntry`（悬停到分隔行不再抛异常）** + **第 43 轮：分诊探针 `ev=[…]`（`ProcessUserEvent` 收到的 user event，名+↓/↑）/ `btn=[our:plot=1:map=0]`（选中项变化时的按钮启用状态）—— 「按 R 没反应」一次定性；SET COURSE 不再置灰（沉默失败 → 可点 + 有提示）** + **第 44 轮：删掉原版 `MissionMenu_PlotToLocation` dispatch（星图位置滞后一条的病根；换成 `SaqNoteStarMapHandoff`：只留音效 + note `星图:交给脚本(菜单关闭后)`）** + **第 46 轮：描述新增「导航目标要靠近目标区域后才加载」+ 结果码 5「目标尚未加载」分支** + **第 48 轮：诊断标签的「0xNaN」→「(子项)」；状态栏名字查不到时回退玩家任务日志（不再「当前引导:?」）** + **第 49 轮：`SAQ_TestDrive*` 测试入口（切 tab/选中/按键/展开/状态，内部走真实处理函数）+ `MissionsList.SAQ_FindEntryIndexByUID`** + **第 49 轮补丁③：入口发布清单（`SaqPublishEntryPoint`）补挂 5 个 `SAQ_TestDrive*` —— 此前只加进了类里、没挂 root ⇒ `ui.tab` 连续两轮 0 ms 失败（真因；见 `docs/09` 第八·补节）** + **第 74 轮：描述提示好感度 + `order=` 顺序探针** + **第 75 轮：载荷第 9/10 列（简要说明）→ 描述第一句；无导航目标时 `SaqNoPickupNote`（「没有可以直接导航的接取地点」）；`pin=` 探针；`stamp=57`**） + **第 82 轮：同伴条目描述首句 + 尾部落点文案改写（说清「自动开始 / 引导指向同伴本人」）** + **第 89 轮：载荷第 11 列（`bSaqRepeatable`）+ `FilterKnownQuests` 对「已完成 + 可重复」放行 + `rptk=` 探针（★ 第 90 轮**未改 SWF** —— DLC 条目不在内嵌载荷里，`SaqEmbeddedPayload.inc` 逐字节不变）** + **第 91 轮：不可导航条目的显示名加「（不可导航）」前缀（`SaqNotNavigablePrefix` + `sSaqBaseName` / `SaqBaseName` 让提示仍报原名）+ `nonav=` 探针；内嵌载荷逐字节不变** + **第 96 轮：可重复条目名加「（可重复）」前缀（`SaqRepeatablePrefix`；与「（不可导航）」叠加时在前）+ `rq=` 探针 + `order=` 新增 `|tail=` 段（含 lrg 版 `MissionsList.as` 补上漏掉的 `SAQ_OrderProbe`）** | `ui/missionmenu/build/missionmenu.swf`（**733254 B / lrg 733359 B**、`stamp=65` —— ★ 第 114 轮重编） |
| 构建验证工具 | ✅ 产物特征检查（SWF/DLL/PEX；★★★★★★★ 第 157 轮 **+3 条产品串**（船员直读指纹 ok / fail / vtable 取不到）+ **计划检查更新**（r47 结构口径 / r80 45 条 / r155 对照与判定行）+ **3 条反向检查**（旧 21 条 ×2 / 旧「A 位随招募往返」）；★★★★★★ 第 156 轮 **+约 40 条**：入口表 45 条（三类前缀各自成立 / 船员与 `hirable_crew.json` 逐项一致 / 3 位 noNav 兜底=0 / 船员样本玛丽卡与瓦斯科）+ DLL 产品串 7 条（三类计数 / 船员门槛 / 船员隐藏 / CrewCond / 可招募名字前缀）+ SWF 7 条（stamp=66 / `SAQ_CREW_TYPE` / 子项名 / 描述 / 不可导航文案 / `crew=` 探针；反向 stamp=64/65）+ PEX 3 条（模拟招募 Trace / AddToAvailableCrew / RemoveFromAvailableCrew）+ 驱动器 **v72**（反向 v71）+ 用例计划 8 条（`sim=` / `teleport.entry` / 模拟招募判定断言 / 五条反向）；★★★★ 第 155 轮 **+10 条**：DLL 6（`crew.probe` op 名 / 逐位行 `｜Papyrus: ` / 汇总行 / 数据表样本名 ×2 / 跳过未加载文案）+ PEX 3（船员探针 Trace / `IsInFaction` / `GetCrewAssignment`）+ 驱动器版本串 **v71** 正向 & v70 反向 + 用例计划 r155 五条；★ 第 147 轮 +2 条：dev 特征串 `引导探针 {}`（发布反向）+ r146 旧断言反向检查（只扫 `step =` 行）；r146 两条断言检查改「产品行前缀」写法（明细 = `docs/09` 十四·补三十七）；★ 第 30 轮 +ESM 数据侧解析检查「新建常驻 marker 11/11 条」；★ 第 33 轮 +「CELL 空壳 11/11 条」+ 空壳不许写数据字段 + DLL 静态表就绪特征 & 反向检查；★ 第 41 轮 +星图注册名 `GalaxyStarMapMenu` / 节点解析器特征字节（正反向）；★ 第 42 轮 +SWF 的 `press=[`/`sel=[`/`R@`/`E@` + DLL 的 press 抄录与「脚本已应用后才换候选」+ PEX 的星图过期校验；★ 第 43 轮 +SWF 的 `ev=[`/`btn=[` + DLL 的 `补处理了关闭瞬间的引导请求`；★ 第 45 轮 +DLL 5 条 + 静态表候选池完整性；★ 第 45 轮补丁 +DLL 2 条 + PEX 2 条；★ 第 46 轮 +SWF 5 条 + DLL 6 条 + PEX 5 条 + 反向检查；★ 第 47 轮 +静态表 2 条 + DLL 1 条 + 反向检查；★ **第 48 轮 +SWF 1 条（`(子项)` 标签）+ DLL 11 条（`全部候选都非常驻` / INFO 门槛统计与名单 / 误判告警 / ini `InfoCond` 与说明 / 认领宽限说明 / 引导更新未确认新文案 …）+ 静态表 4 条（INFO 结构/计数 290+341/切片越界/大器晚成门槛样本）+ 2 组反向检查（「首选候选非常驻」「入口引导目标更新未被脚本确认」「还开着：」）** + ★ 第 49 轮 +SWF 5 条测试入口 + DLL 10 条 harness + PEX 6 条写侧/通道 + ESM 8/8 GLOB；★ 第 49 轮补丁 +DLL 1 条（`harness 结束清菜单`）；★ 第 49 轮补丁② +DLL 3 条（候选降级观察期 / 观察结束 / 入口降级观察期）；★ **第 49 轮补丁③ +10 条（`入口已挂 root`：两个 patch 源（普通 + lrg）× 5 个 `SAQ_TestDrive*` 入口名，出现 ≥2 次 = 定义 + 挂载 —— 防「加了新入口没挂 root」再犯）**；★ 第 67 轮 **+10 条**（DLL：链式门槛统计 / 名单 / ini `ChainCond`；用例计划：`[case:r67_chain]` + 两条断言；静态表：`StaticChainGate` / `kChainGates` / 完整 26 边 25 任务 / 菜鸟觐见样本）；★★ **第 69 轮 +9 条**（静态表：链式门槛改成**与两个数据源逐边对账**（`34/34 · 33/33 · 边集合一致`）+ 扩展边样本 ×2；DLL：`驱动器 v69` + `用例文件解析失败…` 文案；用例计划：r69 两条断言 + `ui.state` 反向检查；反向检查 v68 已替换）；★★ **第 72 轮 +5 条**（用例计划：r71 三条 `[case:]` / `r71 CF 四条断言` / `遗产的结局任一门槛藏（INFO|链式）` + 两条反向检查）；★★★ **第 75 轮 +30 余条**（SWF 8 / DLL 11 / 静态表 6 / 内嵌载荷 2 / 用例计划 6，含 r65 反向检查收窄到 r65 段）+ 更新受影响旧检查（候选 208 / 行尾四列正则 / 内嵌载荷表头 12 条）累计全通过 + ★ 第 82 轮：SWF +4 条（同伴提示/落点 中英）+ stamp 60 更新 + ★★★ 第 128 轮 +6 条（operator 二期：折叠内核在场 / 两条管线接线 / 离线测试含内核自检 / 门槛 want 只含 0/1 / 反向检查旧「只支持等于」）+ 两个老缺陷修复（`AutoLoad` 取值正则跨行 ⇒ 假 MISS；输出统一 UTF-8 ⇒ GBK 管道不再崩）+ ★★★★★★ **第 146 轮 +12 条**（候选池 1021 / 4 条 world 级兜底样本（含 VKaiZ02 的 **DLC master** 样本）/ 生成器口径归一化实现标记 / Mode=6 文案 DLL+resources 双向 / r146 用例三条断言 / 反向检查旧「掩 24 位」写法） + ★★★ 第 151 轮 **+2 条**（产品日志 `列表顺序：` / `入口顺序：`）+ 用例计划检查 **+2 条**（r80 两条新断言）） | `tools/ui/verify_saq_build.py` |
| ★★ 引擎内测试驱动（★★★★★★★ **第 158 轮：驱动器 v73 —— 增量测试（`[Test] Only` 只跑改动的用例；空 = 全量；写错没命中 ⇒ 不跑 + WARN；结果 JSON `only` 字段；判读工具增量感知 —— 明细 = `docs/09` 十五节）**；★★★★★★ 第 156 轮：**驱动器 v72** —— `crew.probe` 扩展（③ 直读对照 + ④ 可选 `sim=<FormID>` 模拟招募往返）+ **Papyrus op=8**（Add→读→Remove→读，净副作用为零）+ r155 用例升级（`teleport.entry` → `sim=` → 判定断言 + 五条反向；离线预演通过）；待实机 = 主计划 **45 条**；★★★★★★ 第 155 轮：**+1 条 `r155_crew_probe`（可招募船员只读探针；驱动器 **v71**）⇒ 用例集 **45 条**；✅ 实机 **45/45 全 PASS**（21:16~21:25 会话 / 566375 ms / 判据五连全绿 —— 明细 = `docs/09` 十四·补三十九）**；★★★★★ 第 148 轮：**44 条 44/44 全 PASS**（06:32~06:41:54 / `sessionMs` 571500；判据五连全绿 + 工作区==部署六件一致 —— 明细 = 顶部块）；★★★★ 第 147 轮：`guide.probe` **补产品日志行**（红线六第二例 —— r146 首跑唯一 FAIL 的真因：结果此前只进 `[PASS] guide.probe …` 步骤行，`assert.log` 只认产品行 ⇒ 断言必然假 FAIL；修法 = 照抄 kQuestProbe 打 `REX::INFO("引导探针 {}", detail)`）+ 用例断言按产品行实际格式重写 + verify 配套（明细 = `docs/09` 十四·补三十七）；第 49 轮 · harness；★ 第 54 轮扩能力 + 落判据；★★ 第 56 轮修断言窗口 + 传送窗口；★★ 第 57 轮修「清场后星图晚开」+ `~0x…` 写回 + 两条用例红线；★★ 第 58 轮加「传送落地静默期 / 加载画面证据 / 卡死自动中止」；★★ 第 59 轮修「落地静默期绑死回执 Tick」= **每 Tick**推进 + 20 秒窗口只管等回执（见「十九」）；★★ **第 66 轮：命令回执窗口 3000 → 8000**（r44 的 `ping` 假 FAIL = 脚本执行了但晚 3.4 秒）+ 慢回执 note + 驱动器 **v66**（见「二十六」）；★★★★ **第 68 轮：断言正则解析期编译校验**（非法正则立刻报「正则非法」，不再退化成「日志里没出现」的误导性超时 —— 反向断言静默 false = 假 PASS）+ 统计行断言用 `scope=case` + 驱动器 **v68**；★★★★★ **第 69 轮：用例文件解析失败 ⇒ 该用例判 FAIL**（不再静默丢步 —— 15:43 会话 r65_icons 的 `ui.state` 踩过）+ 新增 `r69_chain_extra` / `r69_chain_extra_pass`（链式门槛扩展边 A/B）+ 驱动器 **v69**（见「二十九」/`docs/09` 十四·补九）；★★★★★★ **第 83 轮：`ui.*` 遇「桥没通」在步骤超时内自动重试（菜单刚打开那一两拍桥没解析出来）+ 清场延迟复查只在「真的看到并关掉星图」时提前结束（关任务菜单不算）+ 断言超时报文带三个打点 + 新体检工具 `tools/test/plan_regex_audit.py`** + 驱动器 **v70**（见 `docs/09` 十四·补十六）；★★★★★★★ **第 84 轮：r80 断言改判「精确目标二选一」（原板 / marker —— 活体 NPC 引用运行期可得，落原板更精确）+ 结果 JSON 非法 UTF-8 收口（`check_results.py` 容错读出）+ 新工具 `tools/test/log_hygiene.py`**（驱动器仍 v70；见 `docs/09` 十四·补十七）；★★★★★★★★ **第 87 轮：+2 条用例（`r87_or_group` / `r87_or_group_pass` —— 进度门槛 OR 组）⇒ 用例集 24 条**；★★★★★★★★★ **第 89/90 轮：+2 条用例（`r89_repeatable` 基础游戏 / `r90_dlc_repeatable` DLC —— 可重复任务做完一次后仍显示；DLC 用 `~0x…` 记录号，断言容忍加载顺序前缀）⇒ 用例集 26 条**；★★★★★★★★★★ **第 94 轮：26 条 26/26 全 PASS**（定稿判据实测收口 + `plan_regex_audit` 滚动噪声修复）+ **第 96 轮：+1 条 `r96_repeatable_group`（可重复前缀 + 整组排末尾）⇒ 用例集 27 条；第 97 轮 27 条 27/27 全 PASS**；★★★★★★★★★★★ **第 146 轮：+1 条 `r146_vkaiz02_fb`（只读候选探针：`[5]` 兜底=可得）⇒ 用例集 44 条（待实机）**；**第 98 轮 +2 条（`r98_dlc_chain` / `r98_dlc_chain_pass` —— DLC 链式门槛 A/B）⇒ 用例集 29 条；第 100 轮实机 29/29 全 PASS**（见 `docs/09` 十四·补二十三 / 十四·补二十四 / `docs/11` 11.4）；**第 101/102 轮 +3 条（DLC 二期 A/B/对照边）⇒ 32 条；★ 第 103 轮：二期 B 段判据并入 `r98_dlc_chain_pass`（`MQ_Shell` 同会话只能推一次）、删 2 条 ⇒ **30 条**；★ 第 104 轮：`quest.probe` 产品行（红线六）；★ **第 105 轮：30 条 30/30 全 PASS**（22:50~22:58 会话 / 455031 ms / 判据脚本退出码 0）**） | ✅ **`[case:smoke]` 已全绿（06:53 会话）**；★ 09:22 会话 6 条 PASS 2/FAIL 4（驱动缺陷：断言窗口清 0 / 传送 3 秒太紧 → 第 56 轮已修）；★ 09:35 会话 6 条 PASS 2/FAIL 4 —— **三条新真因仍在驱动器/用例侧、产品全对**（① 星图在清场**之后**才打开 ⇒ r26 `ping` 超时；② `ui.select/selectchild` 的 `~0x…` 漏写回 ⇒ r45 报「记录号 0x000000」；③ r47 没等推送就切 tab ⇒ 撞上内嵌回退数据 `notfound`；④ 两条断言共享同一批行 ⇒ 第二条须 `scope=case`），**已修、待重跑**；★ 10:31 会话 6 条 PASS 3/FAIL 3 —— **三个 FAIL 同源：游戏端卡在加载画面**（传送回执后 38 ms 又提交一条 MoveTo ⇒ Papyrus VM 卡在 `player.MoveTo`；r45 传送超时 + r48/r44 陪跑超时；界面 = 加载转圈 + HUD 蓝点），驱动器 **v58** 已加「落地静默期 / 菜单证据 / 卡死自动中止」，**待重跑**；★ 11:04 会话 6 条 PASS 4/FAIL 2 —— **两条 FAIL（r47/r45 的传送）是「落地静默期绑死回执 Tick」的假 FAIL**（Papyrus 两次 `MoveTo` 结果=0、超时瞬间菜单=无；与第 58 轮「真卡死」的区别就在这一行），驱动器 **v59** 已改成「静默期每 Tick 推进 / deadline 只管等回执 / 落地完成证据」，**待重跑**：用例文件（ini 风格 DSL）+ DLL 驱动器（造状态/开关菜单/选中/按键/断言）+ AS3 测试入口（**走真实处理路径**）+ 结果 JSON + 报告器。**只允许 `ini [Test] Harness=1` 时激活**（否则零开销）。通道 = ESM 追加的 8 条 GLOB（0x806~0x80D；纯追加，不动旧记录号）；写侧动作走 Papyrus 语言级 API（`Reset/Start/SetStage/CompleteQuest/MoveTo`）——**零新 RE 工作**。★ 第 54 轮新增：`assert.nolog`（反向断言）/ `scope=this\|prev\|case`（日志窗口 —— 动作与日志常在同一次 Tick）/ `ui.selectchild`（Enter 只引导走子项）/ `menu.hide <注册名>`（关星图）/ `teleport.entry`（站远走近）/ `~0x…`（记录号，免写死 DLC 前缀）；用例 6 条（smoke + r26_menu_idle / r44_starmap / r45_candidates / r47_board_marker / r48_info_gate）。★ 跑之前：ini `[Test] Mode=0`（显示全部）。设计与用法：**`docs/09-测试驱动（harness）.md`**（第十四节 = 第 54 轮；★ 第 55 轮两条用例编写红线：**断言「上一步的副作用行」必须 `scope=prev`**（smoke 漏写 ⇒ 假失败）+ **驱动器在每条用例结束都清场**（清引导/关任务菜单/关星图）并留 700 ms 间隔） | `plugin/src/SAQ_Test.{h,cpp}`（驱动器）、`SAQ_TestOps.{h,cpp}`（原语）、`scripts/SAQ_Main.psc`（`ProcessTestCommand`）、`ui/missionmenu/src/MissionMenu.as`（`SAQ_TestDrive*`）+ `MissionsList.as`（`SAQ_FindEntryIndexByUID` / `SAQ_FindChildIndexByUID`）、`tools/test/scenarios/SAQ_TestPlan.txt`、`tools/test/check_results.py`；部署开关 `build-saq.ps1 -Harness`。★★ **第 60 轮：新 op `guide.probe`（只读候选可得性探针 —— 与产品同一套查询，结果进步骤 JSON）+ r45 尾段改判（走远后判「不许早降级 + 引导仍指向 [1]」；旧「先保持」断言需读档窗口、不可达）+ 驱动器 v60**（见「二十」） |
| ★★ 离线层（第 64 轮 · 大项 K；★★ 第 87 轮 +1 用例） | ✅ **决策纯函数 11 个**（`plugin/src/SAQ_Decision.*`，零引擎依赖；调用点全部改用它；★ 第 84 轮 +`Utf8SafeCut` —— 证据通道的 UTF-8 安全截断；★★ 第 87 轮 `DecideProgressGates` 加 OR 组引擎算法）+ **单元测试 18 用例 / 929 断言**（★ 第 89 轮 +1 用例：可重复任务豁免「已完成 ⇒ 隐藏」；★ 第 90 轮**断言数不变** —— 决策逻辑未改，只扩数据；★ 第 96 轮列表顺序用例扩到「可重复垫底」+ `PinnedOrderKey` 四参数）（`plugin/tests`：自写 MiniTest + 独立 xmake target `SAQ_Tests`，不链接 commonlibsf，毫秒级）+ **数据管线黄金快照 14 件**（第 74/75/80/81/89 轮追加：同伴好感度 / 四大势力开头 / 地标任务 / 入口表 / 可重复任务…）（`tools\test\golden_snapshot.py`；规范化哈希 —— 形式改动不算变化、内容改动必报红；ESM 幂等实测）；一键 `tools\test\run-all-tests.ps1`（★ 第 130 轮起 **5 步**：决策单测 + **折叠内核自检 `tools/esm/ctda_ops.py --self-test`**（★★★ 第 145 轮扩至真值表 12 + 不可折叠 2 + cmp∉{0,1} **11** + 组边界 5 + 组装 10 例）+ 黄金快照 + tripwire + P2 计划正则自检）；★ 第 84 轮新工具 `tools\test\log_hygiene.py`（日志/结果「证据通道健康」体检：非法 UTF-8 / 控制字符）；详见 **`docs/10`** | `plugin/src/SAQ_Decision.*`、`plugin/tests/`、`tools/test/` |
| ★★ 四大势力开头任务（第 75 轮） | ✅ **4 条**（UC01 超越极限 / FC01 枝节横生 / RI01 重返职场 / CF01 深藏不露）：`factionEntry` 列（0..3 = **固定顺序**）⇒ 固定显示（跳过三类门槛）+ **固定排前四**；载荷第 9/10 列 = 「简要说明」（描述第一句）；**CF01 的引导候选在生成期被清空**（只给说明 —— 点击给提示、描述写明「按上面的说明推进」）；日志 `势力入口固定=` / `势力入口前置=` / `势力入口固定名单:`（**单独一行**）；界面 `pin=` 探针 + SWF `stamp=57` | `ref/faction_entry_quests.json`（`tools/esm/gen_faction_entry_quests.py`）｜`SAQ_QuestTable.h`（`factionEntry` / `kFactionEntryNamesZh·En` / `kFactionEntryNotesZh·En`）｜`SAQ_Decision.*`（`IsFactionEntryPinned` / `PinnedOrderKey·Less`）｜`SAQ.cpp`（`FormatPinStats`）｜`MissionMenu.as`（`SaqNoPickupNote` / `SaqPinNoteProbe`） |
| MO2 部署 | ★★★★★★★★★★★★★★★★★★★★★★★★★★★★★ **第 158 轮：增量测试（v73）已部署（测试态；`Only=` 空 = 全量；待实机 —— 建议先增量跑 `Only=smoke,r47,r80,r155` 验证机制本身，收口 / 打包前全量 45 条）**（DLL 开发构建 **1245184 B** / SHA256 `FE1DDB7B…A3C61E`（工作区 == 部署）；Papyrus / SWF / ESM 未变；`verify --dev` **1142 检查 / 0 MISS**；离线层 5 步全过；部署 ini = 测试态（`Harness=1` / `Only=` / `Plan=SAQ_TestPlan.txt` / `AutoLoad=Save1_…142854_2_0_4` / `MaxSizeMB=10`））★★★★★★★★★★★★★★★★★★★★★★★★★★★★ **第 157 轮：修复版 DLL 已重建部署（直读槽 0x174 修正 + 断言更新计划；测试态就绪 —— 待复跑主计划 45 条）**（DLL 开发构建 **1237504 B** / SHA256 `33BDD095…311E81A6`（工作区 == 部署）；Papyrus / SWF / ESM 未变；`verify --dev` 全过 + 离线层 5 步全过；`Harness=1` / `Plan=SAQ_TestPlan.txt` / `AutoLoad=Save1_…142854_2_0_4` / `MaxSizeMB=10`；待实机 = 主计划 45 条（判读要点见顶部块⑥））** ★★★★★★★★★★★★★★★★★★★★★★★★★★★ **第 156 轮：测试态已部署（含 kind=2 船员段 + op=8）**（DLL 开发构建 **1236480 B** / SWF **733596 + 733707 B**（stamp=66）/ PEX **21568 B** / 计划 **45 条**（r155 升级版）；ini 补 `[Filter] CrewCond=1`；`verify --dev` **1130 OK / 0 MISS**（工作区 == 部署）；`Harness=1` / `Plan=SAQ_TestPlan.txt` / `AutoLoad=Save1_…142854_2_0_4` / `MaxSizeMB=10`；离线层 5 步全过（快照 19 件，2 处有意变化 = 入口 21 → 45）；待实机 = 主计划 45 条（判读要点见顶部块⑥））** ★★★★★★★★★★★★★★★★★★★★★★★★★★★★ **第 155 轮：测试态已部署（含 crew.probe；21:12 看护脚本在游戏退出后自动完成 —— 首次尝试被运行中的游戏锁住）**（DLL 开发构建 **1222144 B** / PEX **20507 B** / 计划 **45 条**；`verify --dev` **1079 OK / 0 MISS**（工作区 == 部署）；`Harness=1` / `Plan=SAQ_TestPlan.txt` / `AutoLoad=Save1_…142854_2_0_4` / `MaxSizeMB=10`；离线层 5 步全过；待实机 = 主计划 45 条 ⇒ ✅ **45/45 全 PASS**（已实机收口；`docs/09` 十四·补三十九））** ★ 更早（第 152 轮及更早的沿革原文）→ 已迁 `docs/93`（第 157 轮瘦身 · 逐字剪切） | `D:\Mod Organizer 2\starfield_mods\mods\Show Available Quests (SFSE)\` |
| Nexus 上传包 | ★★★★★★★★★★ **0.1.18 已完成**（第 152 轮）：`dist\SAQ-ShowAvailableQuests-0.1.18.zip`（**1,885,694 B**，SHA256 `2C2E3717…1665E2FC`；= 0.1.17 + 第 150 轮入口排序修复（追踪者联盟总部板归位）+ 第 151 轮排序日志；版本号 6 处同步 + changelog v0.1.18；发布 DLL **924672 B**（`EC462329…F947A22`）；`verify --release` 全过 + 打包后补跑 verify 覆盖新包（7 文件不含测试资产）+ 包内 ini 玩家默认值（[Test] Mode=0 / Harness=0 / AutoLoad 空 / MaxSizeMB=1）；✅ 排序实机确认（第 153 轮，含 v0.1.18 会话）；Nexus 上传标记完成（2026-09-24 玩家约定：时机自行决定，不列入待办 / 不再提醒））★★★★★★★★★★ **0.1.17 已完成**（第 149 轮）：`dist\SAQ-ShowAvailableQuests-0.1.17.zip`（**1,884,152 B**，SHA256 `AD5C2D18605B7A0DB909234AAC99AC4CB5BA508F44D9204A611F205BF7FA6802`；= 0.1.16 + 第 146 轮 DLC world 级兜底（候选池 1021 /「需要靠近」清零 / VKaiZ02 引导任意距离即生效）+ 文案「需靠近」清零（README + Nexus 中英）+ changelog v0.1.17；版本号 6 处同步；发布 DLL **921600 B**（`EED25E3C…6914BEF`，包内哈希 == 工作区）；`verify --release` 全过 + 打包后补跑 verify 覆盖新包（7 文件不含测试资产）+ 包内 ini 玩家默认值（[Test] Mode=0 / Harness=0 / AutoLoad 空 / MaxSizeMB=1）；**可上传 Nexus**）★★★★★★★★★ **0.1.16 已完成**（第 144 轮）：`dist\SAQ-ShowAvailableQuests-0.1.16.zip`（**1,883,980 B**，SHA256 `83CC33F8382C05B05FD41B7092F4A702D64388BF6033309B6F9FD1031A2D9563`；= 0.1.15 + 双形态发布（界面被覆盖 / 缺失 ⇒ 自动切注入形态 + 结构指纹自检）+ 文案/changelog v0.1.16；版本号 6 处同步；`verify --release` 全过 + 打包后补跑 verify 覆盖新包 + 包内 DLL 哈希 == 工作区（**921088 B** 发布构建）；**可上传 Nexus**）★★★★★★ **0.1.15 已完成**（第 127 轮）：`dist\SAQ-ShowAvailableQuests-0.1.15.zip`（**1,852,719 B**，SHA256 `6359DEAF1FA8CA3E3F0A5D2F2B42DA5A4830CB6B1ED66819FFEDEAEADD95006F`；= 0.1.14 + 第 125 轮 UI 通道冲突检测 + 第 127 轮提示时机修复（提示改为「关菜单后」发出）；版本号 6 处同步 + changelog v0.1.15 + 文案更新（中英功能条目 + 已知限制）；`verify --release` 全过（发布构建 63 项 harness 特征全无）+ **打包后补跑 verify 覆盖新包**（7 文件 ⇒ 0.1.15）+ 包内 6 产物哈希逐一致（发布 DLL **852480 B** / PEX 19133 B）+ ini 玩家默认值 + README `v0.1.15`；打完后已切回开发构建（**1083392 B**）+ MO2 ini `Harness=0` / `AutoLoad` 清空）★★★★★ **0.1.14 已完成**（第 116 轮）：`dist\SAQ-ShowAvailableQuests-0.1.14.zip`（**1,850,136 B**，SHA256 `2FF1ED0804038C36FE22BA60B2B73E2D7A985D084B9DE0C9D2CC97D561624EE9`；= 0.1.13 + 第 114 轮两档常驻兜底（候选池 1020 / 19 条「远处点引导立即生效」）+ 第 115/116 轮验证收口；版本号 6 处同步 + changelog v0.1.14 + 文案更新（「需要靠近」20 → 1 条）；**可上传 Nexus**）；`verify --release` 全过 + **打包后补跑 verify 覆盖新包**（7 文件 ⇒ 0.1.14）+ 包内 6 产物哈希逐一致（发布 DLL **848384 B**）+ ini 玩家默认值 + README `v0.1.14`；打完后已切回开发构建（**1032192 B**）+ MO2 ini `Harness=0`。★ **0.1.13（历史）**：1,849,574 B / `5CD96E4C…B54D17`（第 113 轮 —— 追踪者联盟 medium + 日志上限可配 + 7 条补收引导）；**0.1.12**：1,846,610 B / `1E461BA1…CEF20C11`；**0.1.11**：1,845,427 B / `65928FCD…4A24357`；**0.1.10**：1,845,225 B / `A551D4DD…756113`；**0.1.9**：1,845,170 B / `098E6544…81327E0` | `dist\` |
| 游戏内实测 | ★★★★★★★★★ **第 157 轮：检查收口（第 156 轮会话 42 PASS / 3 FAIL —— 3 条均定性并修复：2 条断言过时 + 1 条产品级直读槽缺陷；已重建部署待复跑）**（检查明细 = 顶部块 / `docs/16` 16.9；核心判读 = `crew.probe`「对照 \d+ 位 / 不一致 0 位」/ `船员直读：槽 0x174 指纹=ok` / `船员门槛=` 的「过」= 4 / 界面 `crew=` 16 条）★★★★ **第 156 轮：已实机（42 PASS / 3 FAIL）**（21:55~22:08 / `sessionMs` 729437 / 45 用例；`[E]` 0；FAIL 明细 = `r47` / `r80`（断言过时）与 `r155`（产品缺陷）—— 均已在第 157 轮修复，见上）★★★★ **第 156 轮：待实机（主计划 45 条）** ⇒ 已实机（42 PASS / 3 FAIL，见上第 157 轮）★★★★★ **第 155 轮：主计划 45 条 45/45 全 PASS（已实机收口）**（21:16:26~21:25:53 / `sessionMs` 566375 / `驱动器 v71` / 判据五连全绿 = `check_results` 45/45 + `log_hygiene` 退出码 0（日志 2535 行零坏字节）+ `plan_regex_audit` 169/169 + `verify --dev` + 离线层 5 步；`[E]` 0 / `[W]` 49 全老现象；含 `r155_crew_probe` 9562 ms —— 船员判读 = `docs/16` 16.7：**P 位=可招募解锁（4 位命中）/ A 位=已招募候选（本存档无正样本）/ 任务状态非判据**）** ★ 更早（第 153 轮及更早的沿革原文）→ 已迁 `docs/93`（第 157 轮瘦身 · 逐字剪切） | — |

一键构建（表已生成过时；`-SkipTable` 会跳过 3~6 分钟的 ESM 遍历）：
```powershell
& ".\tools\build-saq.ps1" -SkipTable
```
全流程见 `docs/01-构建与环境.md`；引导机制 `docs/05`；**DLC 支持 `docs/06`**。

---

## 一·补六 ~ 一·补三十五、第 21~50 轮（复盘已迁 docs/96 / 97 / 98；速查）

> 完整复盘按轮次迁入 **`docs/96`**（32~34 / 35~40 / 41~46）、**`docs/97`**（48~50）、
> **`docs/98`**（9~16 / 21~25 / 26~31 / 47）；★ **第 126 轮瘦身另建** **`docs/93`**（第 106~119 轮
> 顶部块 + 旧表项）、**`docs/94`**（harness 第 55~81 轮补记）、**`docs/92`**（星图第 41~42 轮）
> —— 均为逐字迁移，原位留指针；仍然生效：① 日志/ini 在 **mod 目录内**（22）；
> 「能不能导航」界面可见（23）；无目标 52 条**不补猜**（24）；打包 `package-saq.ps1`（25）；
> ② 任务板入口 = `Mode=5` / 入口表（27）+ 置灰拦截（28）+ **候选链**（30）+ 精确优先（31）
>   + 入口目标**动态更新**（32）；③ 静态表预建 + 11 条 **CELL 空壳**（33）⇒ 任务板任意
>   位置精确导航（34）；④ 进度门槛首版（35）；星图三层（37~40）+ 注册名
>   `GalaxyStarMapMenu`（41）+ 分诊探针 `press=/sel=/ev=/btn=`（42/43）+ **轮询节拍**开星图
>   （44）+ **候选池**（45）+ 可用性收口（46）+ 同 cell 常驻兜底（47）+ **INFO 门槛** +
>   「引擎已开始 ⇒ 放行」（48）+ **harness** 落地（49）+ 「先构建部署、再启动游戏」+
>   `stamp=`（50）。

## 十一、下一步（按优先级）

0. ★★★ **第 158 轮：增量测试机制（[Test] Only + 构建脚本 -Only + 判读工具增量感知）**
  （明细 = `docs/09` 十五节 / 顶部块）：
  ① 机制 / 用法 / 「修改 → 相关用例」映射表 / 纪律 = `docs/09` 十五节；本轮已构建部署（测试态）；
  ② ★★ **待实机**（**启动游戏由玩家操作**）：先**增量跑** `-Only smoke,r47,r80,r155`
  （预期 4 条 PASS；判读 = 日志 `Only 增量筛选「…」⇒ 本次只跑 4/45 条` 行 + 结果 JSON
  `only` 字段 + `check_results.py` 的「增量筛选」提示行）；全量 45 条在收口 / 打包前跑
  （用例集无变化，45 条本身无需重跑）。
0. ★★★ **第 156 轮：可招募船员入口段落地 + 模拟招募正样本复核（玩家决策「两个都做」）**
  （明细 = `docs/16` 16.8 / 顶部块）：
  ① **入口段**（kind=2）：入口表 21 → **45** 条（+24 船员）；段序三段 + 布局硬校验（段序
  下标单调不减）；不新建 marker；3 位 noNav 丢弃兜底（界面「（不可导航）」+ 船员专用提示）；
  ② **产品判据**：新模块 `SAQ_Crew` DLL 直读 `Actor::IsInFaction` + 「显示 = P=1 /
  隐藏 = A=1」（P=0 / A=1 ⇒ 隐藏；读不到 ⇒ 保守放行）；ini `[Filter] CrewCond`；
  日志 `船员门槛=` / `船员隐藏:` + 顺序行「可招募船员 N[…]」段；
  ③ **界面**：type 102 文案（子项 / 描述 / 不可导航专用措辞）+ `crew=` 探针 + SWF
  **stamp=66**；
  ④ **正样本复核**：Papyrus **op=8** 模拟招募往返（净副作用为零）+ `crew.probe sim=`
  直读对照 + r155 用例升级（断言离线预演通过：正常 7/7 / 异常 5 条全捕获）；
  ⑤ **已构建部署**（测试态；`verify --dev` **1130 OK / 0 MISS** + 离线层 5 步全过；
  黄金快照 2 处有意变化 = 入口 21 → 45）；
  ⑥ ★★ **待实机**：主计划 **45 条**（**启动游戏由玩家操作**）—— 判读要点 = `船员门槛=` /
  `船员隐藏:` / 模拟招募判定行（A 判据是否成立）/ `crew=` 探针 / 界面「（可招募）…」段
  （见顶部块⑥）。
0. ✅ **第 155 轮：可招募船员跟踪 · 第二阶段（入口数据 + 只读 faction 判据探针）**
  （明细 = `docs/16` 16.6 / `docs/09` 十四·补三十八）：
  ① 已完成：`gen_hirable_crew.py` → `ref/hirable_crew.json`（24 位）+ `SAQ_TestCrewProbe.h`
  + 探针 `crew.probe`（Papyrus op=7 + DLL 直读；驱动器 v71）+ 用例 `r155_crew_probe`（44 → 45）
  + verify **+10 条** + 黄金快照 **19 件**；离线层 5 步全过；
  ② **部署已完成**（21:12 —— 首次尝试被运行中的 `Starfield.exe` 锁住，游戏退出后由看护
  脚本自动补部署；`verify --dev` **1079 OK / 0 MISS**；部署态 = 测试态 `Harness=1` +
  `Plan=SAQ_TestPlan.txt` + `AutoLoad=Save1_…142854_2_0_4` + `MaxSizeMB=10`）；
  ③ ✅ **已实机收口（主计划 45/45 全 PASS，21:16~21:25 会话）**：判读 = `船员探针 汇总：…`
  —— 本存档（2 级早期）**无「已招募」样本**；**P 位 = 可招募解锁（4 位命中）**、
  **A 位 = 已招募候选**（正样本待复核）、**招募任务状态非判据**（明细 = `docs/16` 16.7 /
  `docs/09` 十四·补三十九）；
  ④ ~~**下一步（待玩家定）**：入口段落地（kind=2「可招募船员」并入 `gen_entry_table.py` +
  AS3 type 102 文案 + SWF 重编）—— 建议口径「**显示 = P=1 / 隐藏 = A=1**」；可选 = 先补一轮
  正样本复核（探针加「模拟招募」op：`AddToAvailableCrew` → 复查 A 0→1）。~~
  ⇒ ✅ **第 156 轮已落地（两个都做 —— 见顶部块与上面的第 156 轮项）**。
0. ✅ **第 154 轮：可招募船员跟踪研究（纯离线、零产品改动）**（明细 = `docs/16`）：
  24 位可招募船员（`Crew_Elite_*`）**全部可做「精确引用 → 常驻兜底」引导链**
  （23/24 非常驻；兜底候选内景 11/11 · 外景 5/5 就位）；3 处位置不宜导航
  （Moara / Betty / Erick）⇒ 建议「（不可导航）」；「已招募 ⇒ 隐藏」判据
  （faction 三件套）**待实机探针**；**下一步（待玩家决定）** = 是否落地为
  「可招募船员」入口段（数据生成 `gen_hirable_crew.py` + 实机 faction 探针）。
0. ✅ **第 153 轮：第 150/151 轮排序实机确认收口（纯离线、零产品改动）**（明细 = 顶部块）：
  常规态日志 3 次菜单周期两行顺序日志全对（含 **07:18 的 v0.1.18 会话**）+ 无「顺序异常」；
  `log_hygiene` 退出码 0；r80 两条断言**离线预演 2/2 命中**（合成日志 / 计划均为原文逐字节
  抽取 —— 正式转绿挂下次 harness 复跑）；部署 == 工作区（**FFBBC53D…E40697**，v0.1.18）。
0. ✅ **第 152 轮：0.1.18 上传包完成**（明细 = 顶部块）：
  `dist\SAQ-ShowAvailableQuests-0.1.18.zip` 1,885,694 B / `2C2E3717…1665E2FC`；版本号 6 处
  同步 + changelog v0.1.18；发布构建 **924672 B** + `verify --release` 全过 +
  打包后补跑 verify 覆盖新包；打完后已还原常规部署（开发构建 **1212416 B** + `Harness=0` /
  `AutoLoad` 清空；`verify --dev` 全过）；★ 第 150/151 两轮的实机确认 ⇒ ✅ **第 153 轮已收口**
  （见上）；Nexus 上传 ⇒ ✅ **标记完成**（2026-09-24 玩家约定：上传时机由玩家自行决定，
  不列入待办、以后不再记录与提醒）。
0. ✅ **第 149 轮：0.1.17 上传包完成**（明细 = 顶部块）：`dist\SAQ-ShowAvailableQuests-0.1.17.zip`
  1,884,152 B / `AD5C2D18…7FA6802`；版本号 6 处同步 + 文案「需靠近」清零（README + Nexus 中英）
  + changelog v0.1.17；发布构建 **921600 B**（包内 == 工作区）+ `verify --release` 全过 +
  打包后补跑 verify 覆盖新包；打完后已还原常规部署（开发构建 **1207808 B** + `Harness=0` /
  `AutoLoad` 清空；`verify --dev` **1060 OK / 0 MISS**）；Nexus 上传 ⇒ ✅ 标记完成
  （2026-09-24 玩家约定：上传时机由玩家自行决定，不列入待办、以后不再记录与提醒）；
  ★ 本版发布构建尚无实机会话（第 146 轮修复是开发构建
  实机验证的）—— 若想按第 144 轮教训再真跑一次发布构建：把包内 DLL 覆盖回部署目录即可。
0. ✅ **第 148 轮：r147 复跑收口 —— 44/44 全 PASS**（明细 = 顶部块）：44 条全绿 + 判据五连
  + 工作区==部署六件哈希一致；`r146_vkaiz02_fb` 转 PASS ⇒ 第 146 轮 DLC world 级兜底实机成立
  （产品零缺陷、零产品改动）—— **0.1.17 包已由第 149 轮打出**。
0. ✅ **第 145 轮：A3 operator 三期（产物零变化）**（明细 = 顶部块 / `docs/08` 4.8）：
  cmp∉{0,1} 折叠产品化 + type 只读**低字节**（修第 87/106 轮「读 u32」统计失真）+
  tripwire 三类报红；判据 = `run-all-tests` 5 步 + `verify --dev` 1054 行 / 0 MISS +
  8 个数据产物哈希逐字节一致；A1 选题池移除（**启动游戏由玩家亲自操作**）。
0. ✅ **第 133~144 轮：UI 注入研究 P1~P5 全部收口（速查 —— 明细 = `docs/15` 十三/十四/十五节）**：
  探针 v4 全绿（U5~U10）⇒ **P3-a** 真实数据注入（分时注入闭环：扩 tab 7→8 / mask 拦截 /
  快照恢复）⇒ **P3-b** 产品化（`SaqDescriptionText` 逐字迁移 + watchdog + `UiMode=auto`）⇒
  **P4** 交互接管（按钮劫持 X/Y / `itemActivated` 拦截 / 星图交接
  `ProcessUserEvent("Missions",false)`）⇒ **P5** 结构指纹自检 + 双形态发布（**0.1.16 包**
  1,883,980 B / `83CC33F8…`）；两次会话实机全绿（常规态 **43/43** + P2 态 **6/6**）；
  「随时可回 SWF」纪律不变（SWF 侧冻结、功能只进注入层）；★ 教训 = **发布构建要定期真跑**
  （首次真跑暴露 C1075 与 verify 反向表误报 —— `docs/15` 15.5）。
0. ✅ **第 117~132 轮（UI 注入研究 → P2 全过程）—— 速查**（逐轮明细已迁 `docs/15`
  与 `docs/90` / `docs/91`；第 140 轮瘦身，原文见 `docs/91` 末尾附录）：
  **132 / 131 / 130** P2 会话全 PASS + 探针 v4（U5 回调劫持 / U6 直读 / U7 就地刷新 /
  U8 语言判定 / U10 类通道）+ 两处探针修正（`[ud]` 数组接线 / 关菜单入口判据）；**129**
  功能迁移评估（注入可行 ⇒ 立项 P3）；**128** operator 二期；**127** HUD 提示时机修复 +
  P2 双收口 + 0.1.15 包；**126** 正常态 43/43 全 PASS；**125 起** 路线 D 冲突检测
  （`ProbeChannelIdentity` + 停推降噪 + HUD 提示 GLOB 0x80E）；**124~121** P2 眼睛达成 /
  复跑 / 首跑（注入缺 `aObjectives` 真因）/ P2 落地（探针 v3 + `build-saq.ps1 -P2`）；
  **120 / 118** 42-42 · 41-41 全 PASS；**119 / 117** 探针 v2（拦截 + `filterMask` 哨兵写 +
  `SetTabsData` 补测）+ UI 注入研究落地（能力边界 U0 ✅ / U1 ❌ / U3 ✅）。

0. ✅ **第 87~116 轮的收口项与上传包记录（明细已迁 `docs/93`）** —— 0.1.6 ~ 0.1.13 上传包
  （第 87/88/94/97/100/105/108/113/116 轮）/ 第 105 轮 30/30 / 第 109 轮（大项 A+B：7 条补收 +
  operator 盘点）/ 第 110 轮追踪者联盟收口 / 第 111~116 轮检查收口等；各包 SHA256 与
  发布 DLL 尺寸见「Nexus 上传包」行。
0. ★★★ **下一步大项候选**（按建议优先级）：
   ① ~~**重打上传包 0.1.7**~~ → ✅ **第 88 轮已完成**（`dist\SAQ-ShowAvailableQuests-0.1.7.zip`
   1,839,484 B / SHA256 `1F08EB31…EC8B25F`，含第 87 轮 OR 组；**可上传 Nexus**）；
   小提醒：`verify` 跑在压缩前 ⇒ 它查的总是「上一个包」，新包要补跑一次 verify 覆盖
   （第 88 轮已补 / 全过；已在「Nexus 上传包」行记录）；
   ★★ ~~「可重复任务」全量盘点（第 89 轮）~~ → ✅ **第 89 轮落地 15 条 + 第 90 轮补
   DLC 5 条 = 20 条**（SFFL 家族；三条「未收」已定案不收 —— 见 `docs/11` 第 90 轮一节）；
   ② ~~**自动测试「虚拟桌面 / 独立会话」后台跑**~~ → ❌ **选题池已移除（2026-09-23
   玩家决定）**：**以后启动游戏都由玩家亲自操作** —— 不再考虑后台 / 独立会话 / 虚拟桌面
   方案（自动化止步于「构建 → 部署 → 玩家启动游戏（自动读档）→ 游戏内 harness 自动跑
   用例 → 离线判读」；`docs/09` 九-3 已同款标注）；
   ③ ~~**operator 语义继续产品化**~~ → ✅ **第 128 轮**（`!=`/`>`/`>=`/`<`/`<=` 折叠：
   内核 `tools/esm/ctda_ops.py` + 两条管线接线；表内零变化 —— `docs/08` 4.7）
   → ✅ **第 145 轮（A3）**：**cmp∉{0,1} 也折叠产品化** + type 解析改回低字节（修缺陷）+
   tripwire 三类报红（`docs/08` 4.8；表内零变化、8 产物哈希逐字节一致）。
   **余下**：flags 非 OR（别名 / GLOB / Pack / 交换）—— 表内外**外键形态**实测 0 条
   （INFO 侧 7 条别名全为自引用；记录级唯一特殊样本 = SFTA00 自引用）⇒ **有意不做**
   运行期通道，tripwire 报红盯数据（出现才立项）；
   ④ ~~低优先增强：「待生效」在列表里可见化 / 候选排序引入玩家位置因子 / 仍无兜底的
   20 条任务补常驻 XMarker~~ → ✅ **第 146 轮全部收口**：「仍无兜底」最后 1 条
   （`SFBGS001_VKaiZ02`）已补 DLC world 级兜底 ⇒ 「待生效」清单 **1 → 0**、可见化
   **已无对象**（`Mode=6` 保留作回归探针）；「位置因子」= 第 114 轮否定性判定（会与
   质量分级冲突、需未验证偏移、收益 ≈ 0），不再考虑 —— 见 `docs/14` 九节；
   ⑤ ~~低优先：`City_Neon_Chem03` 供应线路~~ → ✅ **第 146 轮复核收口（维持不收）**：
   2 个 CQ（450/500 = 两个结局）+ 无计数器 / 无冷却 / 无 Reset / 无外部重启调用
   ⇒ 不是可重复任务（`docs/11` 10.2）；只读数据已复查，**不需要实机**——
   若日后玩家实测能重复接，再复议。
0. ✅ **眼睛判据（验收项 —— 全部完成：2026-09-23 玩家确认）**：
   * ★ **已肉眼确认（第 97 轮）**：可重复条目带「（可重复）」前缀 + 整组排在列表末尾（无异常）；
   * ✅ 同伴固定条目（如「巴雷特：违约」）⇒ 描述首句 = 「…好感度达到一定水平后自动开始」、
     末尾 = 「…引导到这位同伴的位置」—— 已确认；
   * ✅ 列表里 10 条「XX地标任务」；选中一条描述第一句 = 「拾取《…》即可接取（地点）」—— 已确认；
   * ✅ 任务板条目不带「（可重复）」前缀（前缀只在可重复任务与 NPC 入口上）—— 已确认；
   * ✅ 第 91 轮：**不可导航的条目名字带「（不可导航）」前缀**（「（不可导航）深藏不露」+
     「（不可导航）伦敦地标任务」；选中它 ⇒ 右侧描述仍说「该任务暂无导航目标」、提示语里
     **没有**前缀）—— 已确认；
   * ✅ 第 116 轮：19 条 world 级兜底任务（远处点引导**立即生效**、描述无「需靠近」句，
     名单 = `docs/14` 八·3 节）+ 追踪者联盟三项（赏金狩猎 / 总部板 / 总部引导蓝点，
     `docs/13` 第六节）+ 第 106 轮补收 7 条（点击有蓝点）—— 已全部确认；
   ⇒ **剩余验收项清零**（可进入 0.1.14 打包）。



0. **更早的收口与约定（第 16~64 轮 / 发布与打包）—— 已迁 `docs/87`**（速查：改 SWF 后必须先构建部署再启动游戏 / 发布就绪 `-Release`+`package-saq.ps1` / 自动读档 / 离线层 / SET COURSE 星图判据见 `docs/05` / 门槛链见 `docs/08` / 打包 + Nexus 文案）。

## 十二、已知风险

| 风险 | 说明 |
| --- | --- |
| **「已开始」≠「已接取」**（第 11 轮实证） | 引擎会把「玩家仍能接到」的任务提前置成 running（RAD05 全数到期，flags=0x8001）。TESQuest 的 started 位**不能**当「玩家已接」的判据；只有玩家任务日志（QuestData）算数 |
| **GLOB（float）存不下完整 FormID**（第 21 轮踩过） | GLOB 是 float（尾数 24 位）：完整 FormID > 2^24 后**奇数不可精确表示**（±1）。已把引导目标拆成「低 24 位（0x801）+ 高 8 位（0x805）」两个 GLOB。**以后再开 float 通道，先问「值会不会超过 2^24」** |
| **Papyrus 定时器在菜单开着时不走**（第 27 轮定案） | 任务菜单暂停游戏 ⇒ `StartTimer` 不触发：三次会话的 `引导已应用` 都精确落在「菜单关闭」时刻，菜单开着 27~37 秒零应用。**不要设计「菜单开着时脚本会处理」的功能**；引导总是「关菜单生效」（与原版 HUD 交互一致）。DLL 侧也据此不再在菜单开着时轮询确认（降噪） |
| ~~任务板入口多为非常驻引用~~（第 27 轮发现 → 第 28 轮兜底 → 第 29 轮 override **被实机否定** → **第 30 轮新建常驻引用治本**） | 12 条入口里只有阿基拉城是原生常驻 ⇒ 其余 11 条在 cell 未加载时 `Game.GetForm` 取不到。★ **第 29 轮的 override 路线不成立**：override 只换数据、**不改变引用的加载分类**（官方 70 条同类 override 原记录本来就全是常驻 = 零先例；实机 12:45 会话 `可导航 2`）。第 30 轮改为**新建** 11 条常驻 XMarker（0x900~0x90A，位置 = 板坐标）+ 候选链。★ **第 33 轮补齐最后一块**：官方对同一条 cell（SFBGS003 → 霓虹城）的写法 = **空壳 CELL 记录（EDID + flags 0x4000）+ 紧跟 CellChildren 组** —— 我们也照抄了（11 条，xEdit 验证通过；**待实测 `marker 11`**）|
| ~~override 世界引用的冲突面~~（第 29 轮引入 → **第 30 轮已消除**） | 第 29 轮对 11 条任务板 REFR 的 override 已全部移除 ⇒ 不再覆盖任何基础游戏记录。第 30 轮只往那些 cell **新增**自己的记录（mod 加物件级操作），与改同一 cell 的 mod 无冲突 |
| ~~打开任务菜单偶发长停顿~~（第 11 轮发现 → 第 12 轮修复 → 第 16 轮确认） | 根因：失败解析路径里 `IsMapped`（VirtualQuery）累计 ~3.9 秒；`SafeRead` 改纯 `__try` 保护后消失。第 16 轮日志实证：收集 0 ms / 失败 0 ms / **无「主线程停顿」行** |
| **界面状态必须由回写驱动**（第 16 轮踩过） | `SaqToggleGuide` 只能在本地立即切状态，而「这条任务有没有引导目标 / 实际在引导哪条」只有 DLL 知道。凡是「界面先行、DLL 后处理」的动作都必须有回写通道（`SAQ_GuideReply`），否则界面会说谎 |
| **GLOB 锚点有次数窗口**（设计边界） | `SAQ_Notify` = 7777 + 菜单打开次数，认领窗口 7777..8776（1000 次）。单次会话极难超限；脚本重挂会把它重置回 7777（DLL 认领成功后不再读锚点，无影响） |
| **文本子记录必须带 NUL**（第 12 轮实证） | 非本地化 ESM 的 FULL/NNAM 等文本子记录，长度 = 字符数 + 1（含 NUL）。第 11 轮漏写 NUL ⇒ HUD「任务更新」显示 `[…]` |
| commonlibsf 的 `REL::ID = 0` 接口 | 已踩（BGSStoryTeller、`TESQuest::IsStageDone`）；编译期 ID 审计挡着（`docs/01` 坑 8） |
| **commonlibsf 的结构体偏移一律不可信**（第 18 轮升级为通则） | 已踩四次，其中 TESDataHandler 两次：`formArrays` 读到空表（第 5 轮，见 docs/02）、`files` 读到空链表（第 17 轮，四个 master 全被误判「未加载」⇒ 第 18 轮改前缀探测）。读引擎对象字段：先找「引擎自己怎么读它」（反汇编），或改用已实测可靠的引擎函数（`LookupByID` 等）；`TESGlobal::value`（0x48）是上一代项目修正过的 |
| **自己写的注释/文档也可能是错的** | 已踩：哈希指针、模糊 RTTI、误信「位16 = 自动启动」。结论要留原始反汇编/日志，且「改完要能被验证」 |
| **GFx 返回值要按类型读** | `GetNumber()` ≠ 万能（见 `docs/04` 2.4） |
| **TESQuest 运行时偏移来自反汇编** | `0x114 / 0x32C / 0x338` 有虚表核验 + 识别率安全阀；换游戏版本要重跑 `tools/re/quest_api_scan.py` |
| **非本地化插件的文本是内联的** | 本 ESM 的 TES4 flags = 0x01（无 Localized 位）⇒ 目标文本可以内联写 `<Alias=SAQ_GuideTarget>`，不需要动游戏的 strings |
| **非常驻引用** | 168 条引导目标里 68 条是非常驻引用：格子没加载时 `Game.GetForm` 可能取不到（日志会写 `状态=2`） |
| **patch 文件集 ≠ 源码目录** | 改任何 `.as` 都要在 `build-saq.ps1` 的同步清单里登记（普通 + lrg 两套）；lrg 源码由 `make_lrg_source.py` 生成 |
| 静态过滤是启发式 | 宁缺勿滥；规则集中在 `filter_reason()` 一处 |
| 语言判定 | AS3 侧按引擎推来的任务名判定（中文/英文）；C++ 侧 INI 值只作日志 |
| 菜单关掉后 Movie 会销毁 | 已处理：每次「菜单由关变开」都 `UI::Reset()` 清解析缓存 |
| 性能 | 菜单打开时：202 次 `LookupByID` + 202 次标志位读取 + 1~2 次 0x4000 字节扫描；开着菜单时每 500ms 一次 `SAQ_Report`、每 100ms 一次 `SAQ_PeekGuide`。实测无感 |
| **树形列表不能按「数据下标」刷新**（第 14 轮踩过） | 显示下标 ≠ 数据下标（`InitializeEntries` 会把已完成 / misc 条目挪位、展开时插入子项），而且 `BSScrollingList` 没有 `UpdateEntry` 接口 —— 第 13 轮那次调用每次抛 `Error #1006` 被 `try` 静默吞掉。就地刷新一律走 `MissionsList.SAQ_RefreshQuestRow(uID)`（按 uID 在当前显示列表里找行） |
| **DLC 的字符串表是各自一份**（第 17 轮） | 任务名要在**该插件自己的** `<master>_{语言}.strings` 里查（破碎空间在 `ShatteredSpace - Main02.ba2`，地球舰队在 `SFBGS050 - Main.ba2`）。漏抽的症状是「DLC 任务全部消失」（取不到名字 ⇒ 被当成内部任务过滤），而不是报错 |
| **medium / blueprint 档位的 FormID 方案未验证**（第 17 轮） | 本项目实测的四个 master 都是普通插件（fullIndex<<24）。light/ESL（`0xFE000000 \| smallIndex<<12`）已实现但**本机没有样本可验**；medium / blueprint 直接标记不支持并整批跳过（`SAQ_Masters::ResolveOne`），宁可少收也不错收 |
| **master 名大小写不一致**（第 17 轮） | ESM 的 MAST 里写的是 `starfield.esm`，而文件夹里是 `Starfield.esm`；`Masters::FindLoadedFile` 用大小写不敏感比较（日志里会打出实际匹配到的名字） |
| **内嵌回退数据只有基础游戏条目**（第 17 轮） | `SaqEmbeddedPayload.inc` 必须写运行期 FormID，DLC 的离线算不出来 ⇒ 只放 Starfield.esm 的 202 条。C++ 推送成功前的那 0.3~0.8 秒里看不到 DLC 条目（可接受的降级） |
