# 99 · 当前项目进度

> 最新更新：2026-09-22（**第 102 轮（下）：实机 32 条 30 PASS / 2 FAIL 的判读 + 两条 B 段改判**）：
> 两条 FAIL 同源 = `assert.nolog 链式没到:.*另一边`（推 `MQ03@4000` / `MQ05@1600` 后仍被藏）。
> **根因在写侧、产品行为正确**：探针 `quest.probe 0x01030C2B：未开始 flags=0x10100｜
> stageDone(100/3350/4000)=0`（MQ05 段同款）+ `已开始=40` / `链式门槛=65(过4/藏59)` **零变化** +
> Papyrus 三条 `结果=0` 却**没有任何引擎侧脚本活动** ⇒ 没进入运行状态的 ShatteredSpace 主线
> 任务，`Quest.Start()` / `SetStage()` 被引擎**静默忽略**（第 99 轮 `MQ01@10000` 同族）；
> 对照组成立：同会话 r98 B 段推 `MQ_Shell@100` = 满屏 fragment 报错 + `已开始 34→44` + `过4→7`。
> 处置 = 两条 B 段**改判**：先 `MQ_Shell@100` 预热（与 r98 同源、已实测有效）把宿主置为运行中，
> 再推目标 stage ⇒ 断言链 `stageDone(100)=1` → `stageDone(4000/1600)=1` → 放行；r98 B 段 +2 条
> 探针（记录预热是否真的落到引擎）；verify +7 检查 +1 反向检查；离线层 18 用例 / 929 断言 +
> 15 件快照全过；已构建部署（`Harness=1`）。
> ★ **待实机**（重开游戏跑 32 条）⇒ 随后 **0.1.11** 打包。详见 `docs/09` 十四·补二十五/二十六。
> ★ 兜底：若实测 `stageDone(100)=0`（连 fragment 的 SetStage 都推不动主线宿主）⇒ B 段按
> 「记录型用例」改判，放行方向交给 r98 B 段（同类机制）。
>
> 上一轮：2026-09-22（**第 101 轮（B3）：DLC 链式门槛二期 —— 候选清单里的边全收（+15 条）**）：
> `ref/quest_chain_dlc.json` **6 → 21 条边 / 18 条任务**；链式门槛 **66/69 → 78 条任务 / 84 边**；
> **有意不收** MQIntro / `DialogueHVDazra@5` / VKaiZ03*（verify 有反向检查）；harness +2 条
> （29 → 31）+ 指定存档切换（`Exitsave0_FDBB7678_54696D6D6568`）；内嵌载荷逐字节不变 ⇒
> 只重编 DLL。完整复盘 `docs/06` 第十节。
>
> 上一轮：2026-09-22（**第 100 轮：29/29 全 PASS + 0.1.10 上传包完成** —— 详见 `docs/06` 九节
> 与本文件「十一」；要点：r98 B 段推 `MQ_Shell@100` 一次通过 + 推送 216→219 强佐证 ⇒
> **DLC 收口**；`dist\…-0.1.10.zip` 1,845,225 B / `A551D4DD…756113`；打完后切回开发构建 +
> MO2 ini `Harness=0`）。
>
> 上一轮：2026-09-22（**第 99 轮：DLC 收口最后一块 —— 用例改推 `MQ_Shell@100`**）：
> 第 98 轮产物全量实测 29 条 28 PASS / 1 FAIL，唯一 FAIL = r98 B 段（写侧推不动 MQ01@10000：
> 回执全成功但统计行逐字段一致 = 引擎零变化）；反编译定性：MQ01 收尾 fragment =
> `SetObjectiveCompleted → MQ02.SetStage(100) → Game.AddAchievement(51) → Self.Stop()`
> ⇒ 不可观测 + 成就污染 ⇒ 改推开场边 `MQ_Shell@100`（无 Stop/无成就，覆盖三条议会任务，
> 比原设计更强）；顺带修两个名字笔误（狂热逼界→狂热逾界 / 发觉过去→发掘过去）+ verify
> 反向检查。完整复盘 `docs/06` 第九节。
>
> 上一轮：2026-09-22（**第 98 轮：DLC 链式门槛取证 —— 反编译 `.pex` 打通**）：
> ★ 完整复盘已整体迁 **`docs/06` 第八节**（内容不丢，只搬家）。仍然生效的要点：
> ① DLC 不发 `.psc`，但 `.pex`（3.12 / gameId 4）用 Champollion（Orvid v1.3.2）能反编译回
>   等价源码 ⇒ 启动边是硬数据；工具 `tools/esm/gen_dlc_chain.py`（抽 → 反编译 → 候选
>   33 条表内任务 / 137 条边 → 定稿核验）；
> ② 定稿 6 条（`MQ02 ← MQ01@10000` / `MQ_Shell ← MQ02@2000` / `MQ03·MQ04·MQ05 ← MQ_Shell@100` /
>   `MQ06 ← MQ_Shell@1100`）+ 运行时零改动（`StaticChainGate` 本来就带 `hostMaster`）；
> ③ 噪声判据（不当时启动边）：调试跳关 fragment（stage 0000/0001/0002）、版本补丁修复片段、
>   回写、`SetStage(0)`；④ SMQN 只有 2 条表内目标（二期走 `kQuestConds`）；
> ⑤ 落地：链式边 63 → 69 / 有边任务 66；verify 改「与四个数据源逐边对账」0 MISS；
>   黄金快照 +1 件；harness 用例 27 → 29 条；用例设计见 **`docs/09` 十四·补二十四**。
>
> 上一轮：2026-09-22（**第 97 轮：第 96 轮产物日志二次确认（27 条 27/27 全 PASS）+ 0.1.9 上传包**）：
> ① 二次确认（13:50~13:57 会话 + 14:26 玩家肉眼复查 / `驱动器 v70` / 406812 ms）：27 条
>   **27/27 全 PASS**（含 `r96_repeatable_group` 1532 ms）；三条判据全中（`可重复任务=20
>   (已完成保留0｜排在末尾18)` / `rq=[…]` / `order=[…|tail=…]`）；`[E]` 0 / `[W]` 3 老现象；
>   `stamp=63` 一致 ⇒ 第 96 轮产物实机收口；
> ② 0.1.9 上传包：`dist\…-0.1.9.zip`（**1,845,170 B**，SHA256 `098E6544…81327E0`）=
>   0.1.8 产物 + 第 96 轮改动；版本号六处同步 + changelog + 文案；`verify --release` 全过 +
>   打包后补跑 verify 覆盖新包 + 包内 5 产物哈希逐一致（发布 DLL 840192 B）+ ini 玩家默认值；
>   打完后切回开发构建（1013248 B，`部署 == 工作区` 全过）。复盘见 `docs/11` 11.4。
>
> 上一轮：2026-09-22（**第 96 轮：可重复任务 —— 名字「（可重复）」前缀 + 整组排在列表末尾**）：
> 完整复盘已迁 **`docs/11` 十一**（内容一字未改，只搬家；实机收口见 11.4）。仍然生效的
> 接口约定（速查）：① AS3 `SaqRepeatablePrefix()`（`sName` = 渲染字段）+ `sSaqBaseName`
> 让提示/日志报原名 + 探针 `rq=[<N>|0x<uID>=<显示名>]`；② C++ `PinnedOrderKey` **group 3**
> （可重复垫底；调用点 `AppendEntryRows` 先追加后排序）+ 统计行「｜排在末尾K」+ `order=`
> 探针新增 **`|tail=`** 段（含 lrg 版 MissionsList 补漏）；③ SWF `stamp=63`；④ 离线：
> 18 用例 / 929 断言 / 快照「末尾可重复 15」。
>
> 上一轮：2026-09-22（**第 95 轮：0.1.8 上传包已完成** —— `dist\…-0.1.8.zip`
> 1,844,547 B / SHA256 `FE1E63E7…4A93B8`；**第 94 轮：全量复跑 26 条用例 26/26 全 PASS**
> —— 12:00~12:08 / 407266 ms / `驱动器 v70`，`[E]` 0 / `[W]` 13 老现象；第 89/90/91 轮
> 产物实机收口；工具侧修 `plan_regex_audit` 滚动误报（`docs/09` 十四·补二十三）。
>
> 更早（第 91~93 轮，速查；**92 的假设已被 93 证伪**）：**93** 判据定稿 —— 引擎行为 =
> 「任务完成即从玩家日志移除」⇒「在日志 + 已完成」构造不出来、删 `qdata=`/`rptk=` 断言、
> 显示层证据 = `ui.tab` + `ui.select <uID>`；**91** 不可导航条目加「（不可导航）」名字前缀
> （`SaqNotNavigablePrefix` + `sSaqBaseName` 提示报原名 + `nonav=` 探针 + `stamp=62`）；
> 复盘见 `docs/09` 十四·补二十一/二十二。
>
> 更早（第 89/90 轮：**可重复任务** —— 「做完一次仍显示」15 条 + DLC SFFL 家族 5 条落地
> ⇒ 清单 15 → **20 条**；四处链路（`repeatable` 列 / C++ 豁免 / 载荷第 11 列 / `rptk=`）+
> `noPickup` 与三条「未收」定案——完整复盘见 `docs/11`）。
>
> 更早（第 84~88 轮，速查）：**88** 0.1.7 包（1,839,484 B / `1F08EB31…EC8B25F`）；
> **87** 进度门槛 OR 组（`orBit` / 表内 7 任务 11 条件；实测 24/24）；**86** operator 全解；
> **85** 三跑 22/22 + 0.1.6 包；**84** 结果 JSON UTF-8 收口（`Utf8SafeCut` / `log_hygiene.py`）。
>
> 上一轮：2026-09-21（**第 83 轮：自动测试首跑复查** —— 22 条 19 PASS / 3 FAIL 全在
> 「用例 / 驱动器」侧、产品全对；已由第 85 轮收口）：要点 = r80 正则全角/半角笔误 +
> 驱动器 **v70** 两项修复（`ui.*` 桥自动重试 / 清场延迟复查）+ 体检工具
> `tools/test/plan_regex_audit.py`（复盘 `docs/95`「第 81~83 轮」）。
>
> 上一轮：2026-09-21（**第 82 轮：同伴条目文案说透** —— 玩家反馈「好感度不够时这些
> 引导意义何在」）：
> ① 结论（官方机制）：同伴个人任务**没有接取点/接取对话** —— 好感度里程碑一到，宿主
>   脚本 `StartPersonalQuest()` 直接 `PersonalQuest.Start()`（自动开始）；好感度只能靠
>   **带该同伴一起冒险**积累（StoryGate 计时器要求 following player）。数据侧首选引导
>   候选 = 同伴本人（常驻引用）⇒ 引导的语义 = 「找到这位同伴」，不是「去接取」；
>   旧文案「达到一定水平后才能接取」会让人以为要跑一趟 ⇒ 改（见 `docs/08` 13.3）；
> ② 改法（SWF **`stamp=60`**）：`SaqCompanionNote` 首句 = 「…后自动开始，不需要找地方
>   接取；好感度要靠带这位同伴一起冒险来提升；「设定航线」引导到的是这位同伴当前所在的
>   位置」；同伴条目的**尾部落点文案** + 「无目标」兜底同步换成「引导到这位同伴的位置」
>   （`SaqDescriptionText` 第 4 参数分支）；
> ③ verify +4 条（同伴提示 / 同伴落点，中英）+ stamp 60 更新；构建 + 部署 + verify 全过；
>   离线层 15 用例 / 667 断言 + 快照 13 件全过（数据零变化）；
> ④ **待实机**（随第 81 轮的 22 条用例一起）：眼睛看同伴条目描述首句/末尾为新文案；
>   日志 `stamp=60` 串；上传包 0.1.5 = 第 81 轮产物 ⇒ 本条修正等实机后重打。
>
> 上一轮：2026-09-21（**第 81 轮：「地球地标」系列任务（10 条）—— 复查 + 收录**；
> 玩家反馈「『地球地标』系列在可接任务列表里一个都没有」）—— ★ 已实测收口
> （第 85 轮 22/24 条用例含 `r81_landmark` 全 PASS）；复盘已迁 `docs/95` 的「第 81~83 轮」。
> 要点：每条地标任务都有一本对应书（官方脚本 `defaultrefoncontainerchangedto` +
> `QuestToSetOrCheck` ⇒ SetStage(100) = 「接取」）⇒ 收录 10 条（伦敦只给说明）；
> `Opportunity`（火星机遇号）不收（无触发物，属主线流程）。
>
> 上一轮：2026-09-21（**第 80 轮：入口的第二形态 ——「提供无限任务的 NPC」（8 条）**）——
> ★ 顶部速览块已整体迁 `docs/97`（第 81 轮：为控制 100 KB 上限，内容一字未改）。
> 仍然生效：入口条目表 **20 条**（任务板 12 + 可重复 NPC 8）；「（可重复）」前缀只在
> NPC 条目；外景 NPC 的兜底 = world 级常驻引用；7 个内景 marker 0x90B~0x911。
>
> 上一轮：2026-09-21（**第 78 轮：同类问题全量复查 —— 审计工具补盲区 + DLC 的 INFO 门槛**）
> —— ★ 顶部速览块已整体迁 `docs/97`（第 79 轮：为控制 100 KB 上限，内容一字未改）。
> 仍然生效的关键结论：**DLC 的 INFO 门槛**（60 → **80 条任务 / 418 对话 / 492 条件**，
> 见 `docs/08` 十二·补二）+ 审计工具补盲区（DLC fragment 源码 / 故事事件形态 / 门槛状态）。
>
> 上一轮：2026-09-21（**第 77 轮：龙神线「RIR 留专项」收口**）—— ★ 顶部速览块已整体迁
> `docs/97`（同上）。仍然生效的关键结论：RIR 系列定性（主线环节 vs 可重复 radiant）+
> **+8 条边 ⇒ 表内 60 任务 / 63 边**；逐条复查见 `docs/08` 十二·补。
>
> 上一轮：2026-09-21（**第 76 轮：第 74/75 轮产物实测收口 —— 17 条用例 17/17 全 PASS**）
> —— ★ 顶部速览块已整体迁 `docs/97`（同上）；下一轮（第 80 轮）的用例集已扩到 21 条。
>
> 上一轮：2026-09-21（**第 75 轮：四大势力开头任务 —— 固定显示 + 固定排前四；深红舰队只给说明**）—— ★ 顶部速览块已迁 `docs/97`；仍然生效的接口约定：
> `kFactionEntryNamesZh/En` + `kFactionEntryNotesZh/En`（说明 = 载荷最后两列）、
> `IsGatePinned` / `PinnedOrderKey`（离线层，`Decision::` 有单测）、CF01 候选清空（只给说明）、界面 `pin=` 探针；详情见「二十九」与 `docs/97`。★ 第 76 轮已实测收口。
>
> 更早（顶部速览块已整体迁 `docs/97`，内容一字未改；仍然生效的要点）：
> **74 同伴好感度任务**（`companion`/`companionPin` + 4 条入口 `pin=1` + `order=` 探针，
> `docs/08` 十三）；**73/72** 实测收口 + CF 线；**71** 链式门槛全量入边审计（+16 目标/18 边）；
> **70** 12 条用例全 PASS；**69** 扩展边 8/8 + 驱动器 v69（解析失败 ⇒ 判 FAIL）；
> **68** 用例正则写错（产品全对）；**67** 链式门槛（「全部启动边都没触发 ⇒ 隐藏」，
> `docs/08` 十~十一）；**66** 命令回执窗口 3000→8000；**65** 任务专属图标（FTYP → 阵营枚举）；
> **64** 0.1.3 包 + 离线层（`docs/10`）；**63** 读档窗口崩溃收口（`WorldBusyForUpkeep`）；
> **62** 0.1.2 包 + 自动读档（`save.list`/`save.load`，零新 RE）。
>
> **第 43~60 轮顶部速览块已整体迁 `docs/95`**；仍然生效（速查）：**43** 分诊探针
> `ev=`/`btn=`；**44** 星图由脚本**轮询节拍**打开；**45** 引导候选池（预选/换候选/复算）；
> **46** 结果码 5 + 退避重试 + HUD 提示；**47** 同 cell 常驻兜底；**48**
> `AllCandidatesNonPersistent` 回退 + **INFO 门槛** + 「引擎已开始 ⇒ 放行」；**49** 引擎内
> harness；**50** `stamp=` 指纹 + 铁律「**先构建部署、再启动游戏**」；**52~53**
> `Error #1074` 收口 + `saq_harness` 开关 + 0.1.1 包；**54~60** 历史判据落成用例 +
> 驱动器 v56~v60 修七类缺陷（→ `guide.probe`）。
> 完整复盘：`docs/95`（43~60）/ `docs/96`（35~40）/ `docs/97`（53~55 等）/ `docs/98`（9~16）。

## 一、当前状态

| 组件 | 状态 | 位置 |
| --- | --- | --- |
| SFSE 插件（DLL） | ✅ 已构建（多 master 前缀探测 + 引导结果确认 + 认领已有引导 + 脚本活性探测 / 引导未生效收尾（第 19 轮）+ 控制台测试过滤（第 20 轮）+ 目标 FormID 拆分 / 自检上限修复（第 21 轮）+ 确认窗口改绑菜单状态（第 26 轮）+ **无限任务入口条目表 / 引导确认与停顿降噪**（第 27 轮）+ 入口引导候选链（第 30 轮）+ **候选链改「精确优先」（任务板自身）+ 引导目标动态更新 + marker 探测**（第 31 轮）+ **菜单关着时例行认领 + 通道对账**（第 32 轮）+ **菜单关着时预建静态表 / `Masters::Resolved()`（认领不再因表没建好而误判）**（第 33 轮） + **SET COURSE 星图（状态 5 + kHide 关菜单 + 星图探测）**（第 37 轮） + **星图自动重试 / 地点候选状态 5·6·7 / 引擎节点解析只读诊断（第 40 轮）** + **星图注册名修正 `GalaxyStarMapMenu` / 节点解析器特征字节修正（第 41 轮）** + **菜单关闭时现场读界面报告 / 补一次引导请求轮询（第 43 轮）** + **星图重试等待 3 s → 1.5 s 与脚本新窗口对齐 / 日志写明星图唯一入口（第 44 轮）** + **引导目标候选池（第 45 轮：预选可得候选 / 状态 2 换候选 / 菜单关着时动态复算 + 认领带下标）** + **引导可用性/提示收口（第 46 轮：`needsApproach` 判定 / 结果码 5 / `PollApproachRetry` 退避重试 / 星图本次不打开 / 测试过滤新增 `Mode=6` + 上限常量修复）** + **引导可用性收口（第 47 轮：同 cell 常驻兜底）+ 判据收口 + 大项 D（第 48 轮：`AllCandidatesNonPersistent` 判据回退（20 条）/ `EvaluateInfoGates` INFO 门槛 / 认领后 8 秒复算宽限期 / 星图与确认文案修正 + 同日补丁：**引擎已开始 ⇒ 放行**） + **harness 首测复查（第 49 轮补丁：无参入口 0 参调用 / 结束清菜单）+ 读档复算抖动（第 49 轮补丁②：候选降级观察期 20 秒）**） + **同伴好感度任务（第 74 轮：`companionPin` 固定显示 / 名字前缀 / 排序前置）** + **四大势力开头任务（第 75 轮：`factionEntry` 固定显示 + 固定排前四 / `IsGatePinned` 统一放行判据 / `PinnedOrderKey·Less` 排序（离线层）/ `FormatPinStats` 单独一行 / 说明写进载荷第 9/10 列）**）**） | `plugin/build/windows/x64/releasedbg/SAQ_ShowAvailableQuests.dll`（**991744 B**） |
| ★★ 同伴好感度任务（第 74 轮） | ✅ **4 同伴 / 8 条**：入口 4 条（个人任务，`companionPin=1` ⇒ 固定显示 + 名字前缀 + 描述好感度提示）；后续 4 条（承诺任务 ⇒ 补齐启动边并入链式门槛，前置没到不显示）；**前置到列表开头**（按同伴分组、入口在后续前；载荷/内嵌回退同序）；日志 `同伴固定=N(…)` + `同伴分组前置=N` + 界面 `order=` 顺序探针；SWF `stamp=56`（★ 第 82 轮：描述文案说透 ⇒ `stamp=60`） | `ref/companion_quests.json`（`tools/esm/gen_companion_quests.py`）｜`SAQ_QuestTable.h`（`companion`/`companionPin`/`kCompanionNamesZh·En`）｜`SAQ_Decision.*`（`DecideGateAction`）｜`MissionMenu.as`（`SaqCompanionNote`）｜`MissionsList.as`（`SAQ_OrderProbe`） |
| 静态任务表 | ✅ **271 条 / 4 个 master**（★★★ 第 81 轮 +10 条「地球地标任务」）；★ 第 45 轮引导目标升级为**候选池**：`kGuideCandidates[]` **931 条候选 / 209 条有目标**（★ 第 47 轮：为「全非常驻候选」的 76 条任务里 56 条补了**同 cell 常驻兜底**（+56 条、常驻 339 条）；`candBegin/candCount` 切片；旧单目标字段已移除）；★ **第 48 轮（大项 D）新增 `kInfoGroups[]` / `kInfoConds[]`（INFO 门槛：60 条任务 / 290 条对话 / 341 条条件 / `infoGroupBegin/infoGroupCount` 切片）**；★★ **第 74 轮行尾 +2 列（`companion` / `companionPin`）+ 名字前缀**；★★★ **第 75 轮行尾 +1 列（`factionEntry`）+ 4 个数组（`kFactionEntryNamesZh·En` / `kFactionEntryNotesZh·En`）—— 四大势力开头任务固定显示 + 固定排前四；CF01 的 7 个引导候选被清 ⇒ 候选 931→**924**、有目标 209→**208**、链式边 55；★★★★ **第 81 轮：候选 924→**960**、有目标 208→**217**（+10 条地标任务里 9 条各 4 候选 —— 伦敦只给说明 = 0 候选）****；★★★ 第 87 轮：`kQuestConds` **11 条**（进度门槛支持 OR 组 —— 见 `docs/08` 4.4）；★★★★ **第 89/90 轮：行尾 +`repeatable` 列 + `kRepeatableNotesZh·En` —— 可重复任务 15 → **20 条**（第 90 轮 +DLC 的 SFFL 家族 5 条：3 条在锚点星际站有接取人、2 条随机太空遭遇无接取点），见 `docs/11`） | `plugin/src/SAQ_QuestTable.h` |
| ★★ 入口条目表（第 27/28/29 + **80** 轮） | ✅ **20 条 = 12 任务板 + 8 可重复 NPC**（★ 第 80 轮：`（可重复）贸易管理局 · 邓肯·林奇` 等 4 位商人（RAD03）+ `（可重复）追踪者联盟探员 · <城市>` ×4（RAD04）；「（可重复）」前缀**只在 NPC 条目**上 —— 玩家明确任务板不加）；uID = 引导目标 = 条目引用；ini `Mode=5` 只显示它们；★ **第 30 轮：引导目标 = 候选链**（① 条目引用自身（精确）→ ② 新建常驻 XMarker（任务板 0x900~0x90A / NPC 0x90B~0x911）→ ③ 兜底：内景 = 同 cell 原生常驻引用 / **外景 = 该 worldspace 的世界级常驻引用**），DLL 依次 `LookupByID` 取第一个命中的 + 诊断日志；第 29 轮 override 路线被实机否定（见一·补十五） | `plugin/src/SAQ_EntryTable.h`（`tools/esm/gen_entry_table.py`，+kind 列）；清单 `ref/entry_targets.json`；NPC 数据 `ref/repeatable_givers.json`（`tools/esm/gen_repeatable_givers.py`）；新建 marker `tools/esm/create_board_markers.py`（产物 `ref/board_markers.json`，★ marker 记录签名必须 REFR）；侦查 `tools/esm/scan_entry_persistent.py` |
| ★★ 地球地标任务（第 81 轮） | ✅ **10 条**（Apollo / Cairo / Dubai / HongKong / London / LosAngeles / NewYork / Osaka / Shanghai / StLouis）：豁免「地标」过滤 + 引导 = 书（或书商）的引用 + 同 cell / **world 级**常驻兜底 + 说明文本（载荷最后两列 = 描述第一句）；**伦敦只给说明**（书在书店/书堆，无固定接取点）；`Opportunity`（火星机遇号）**不收**（无触发物品，属主线 Unearthed 流程） | `ref/landmark_quests.json`（`tools/esm/gen_landmark_quests.py`）｜`SAQ_QuestTable.h`（`landmark` 列 / `kLandmarkNotesZh·En`）｜`SAQ.cpp`（说明来源第二分支） |
| 运行时过滤 | ✅ **只挡「已完成」**；「已接取」只认玩家任务日志；**master 没加载 ⇒ 整批跳过** | `plugin/src/SAQ_QuestState.*` / `SAQ_Masters.*` |
| 进度门槛（第 35 轮；★★ 第 87 轮 OR 组） | ✅ **7 条任务 / 11 条条件**（★ 第 87 轮：支持 **OR 组** —— `orBit` 列 + 引擎算法 `acc/orAcc/inOr`、`kUnknown` 一律放行；FFConstantZ06 = `UC04 完成 AND (Z04 完成 OR Z05 完成)`，见 `docs/08` 4.4）：「进度没到不显示」首版（第 35 轮）：记录级条件里「引用别的任务」的 GetQuestRunning/GetQuestCompleted/GetStageDone；求值不了 ⇒ 放行；ini `[Filter] ProgressCond` 可关；日志 `进度门槛=N(过A/藏B/未知K)` + `进度没到:` 名单。**已实测通过**（14:47 会话：过3/藏4/未知0；第 87 轮实测：A 段藏「亲爱的姐妹」/ B 段放行，见一·补二十第 6 节与 `docs/08` 第七节） | `plugin/src/SAQ_QuestCond.*`；数据 `ref/ctda_gates.json`（`tools/esm/analyze_ctda.py` → `gen_quest_table.py`） |
| ★★ INFO 门槛（第 48 轮 · 大项 D；★★ **第 78 轮扩到 DLC**） | ✅ **80 条任务 / 418 条对话 / 492 条条件**（= 基础游戏 60/290/341 + **DLC 20/128/151**：SFBGS00D 2 / SFBGS050 7 / ShatteredSpace 11；跨 master 前置已支持 —— 前置的 master 下标按 kQuestMasters 顺序写进 `kInfoConds`）（「进度没到不显示」第二判据，对话侧）：任务自己的对话（INFO）里「引用别的任务」的同类条件；按「自引用」分成入口类/中性类（排除推进类）；**全部参与对话都有已知假条件 ⇒ 隐藏**（成对条件自动豁免）；求值不了 ⇒ 放行；★ **实测通过（21:46 会话：命中 58 条 / 藏 21 / 放行 2 / 未知 0，名单抽查合理）**；★ 同日补丁：**引擎已开始 ⇒ 放行**（判据收窄为「引擎没启动 + …」，修掉『全数到期』类误藏）；ini `[Filter] InfoCond` 可关；日志 `INFO门槛=…(过A/藏B/放行C/未知K)` + `INFO没到:` 名单 | `plugin/src/SAQ_QuestCond.*`（`EvaluateInfoGates`）；数据 `ref/info_gates_final.json`（`tools/esm/scan_info_gates.py` 逐 master 扫 4 份 `info_gates*.json` → `analyze_info_gates.py` 合并 → `gen_quest_table.py`；构建脚本在原始扫描缺失时自动补扫） |
| ★★ 任务链门槛（第 67 轮；第 68 轮实测对齐；★★ 第 69 轮扩到非编号链路；★★★★ **第 71 轮「全量入边审计」**；★★★★★ **第 77 轮「RIR 留专项」收口**；★★★★★★ **第 98 轮 DLC 收口**；★★★★★★★ **第 101 轮 DLC 二期 +15 边**） | ✅ **表内 78 条任务 / 84 边**（★★★★★★★ **第 101 轮 +15 条 DLC 边**：另一边 MQIN ← 狂热逾界@4000 / 信念之争@7000 / 发掘过去@1600；地球舰队六级链（含 need_stage 的 **深入VOID ← 失踪的华庭号@1700**）；自由航道三条；**有意不收** MQIntro / DialogueHVDazra@5 / VKaiZ03* —— 见 `docs/06` 十节）；★★★★★★ **第 98 轮 +6 条 DLC 边**：破碎空间主线 `MQ02←MQ01@10000` / `MQ_Shell←MQ02@2000` / `MQ03·MQ04·MQ05←MQ_Shell@100` / `MQ06←MQ_Shell@1100`（官方 `.pex` 反编译取证，`tools/esm/gen_dlc_chain.py` → `ref/quest_chain_dlc.json`，host master = 3，见 `docs/06` 八节）；★★★ **第 77 轮 +8 边**：龙神全线 —— `RIR01←RI01@1300` / `RIR03←RIR02@300`（**故事事件**形态，新 op `SendStoryEvent`）/ `RI02←RIR03@300` / `RIR04←RI02@600`（玩家报告的那条）/ `RIR05←RIR04@300`（故事事件）/ `RI03←RIR05@10000` / `RIR06·RIR07←RI_Support@610·700`（Masako 可重复 radiant 的 managerial 边）= 编号链 25/26（`tools/esm/gen_quest_chain.py`，工具 31 目标 / 33 边）+ **扩展边 32/34**（第 69 轮 8/8：Eleos 静修地线 / 霓虹城帮派线 / 城市支线预启动；★★ **第 71 轮 +16 目标/18 边**：CF 线全家（深藏不露 ← UC02@860、终局两分支 ← LC088_Space@100/200、星钥站支线 ← CF08_SysDef@10、罪证的负担 ← CF01@310）+ FC08 + 城市线三连 ×2（RedTape / Viewport）+ MS05/MS06 引入任务 + 同伴 ×2（承诺：巴雷特 / 哈特家事）—— 起因 = 玩家实测「深藏不露 / 宝藏号的结局 / 夺回曾经」不该显示；全量审计 = `tools/esm/analyze_start_paths.py`（737 条启动调用逐条人工核实），其余候选按 A~H 八类记入「有意不覆盖」，见 `docs/08` 第十二节；人工核实 + 构建期源码核验）；判据「**全部启动边都没触发 ⇒ 隐藏**」（边之间是「或」：任一条已触发/求值不了 ⇒ 放行）；ini `[Filter] ChainCond` 可关；日志 `链式门槛=N(过A/藏B/未知K)`（N = **走到链式判定**的任务数；15:24 实测 **22** = 表内 25 − 3 条已被 INFO 门槛先藏）+ `链式没到:` 名单；★ 实测：A 段 过0/藏22 ✓、B 段 过1/藏21 ✓（「菜鸟觐见」在/不在名单）；★ 起因 = 玩家实测「深红舰队线的菜鸟觐见 / 风驰电掣在存档没开始时也显示」 | `plugin/src/SAQ_QuestTable.h`（`kChainGates`）；`SAQ_QuestCond.cpp`（`EvaluateChainGates`）；`SAQ_Decision.cpp`（`DecideChainGates`）；数据 `ref/quest_chain.json` + `ref/quest_chain_extra.json` + `ref/quest_chain_dlc.json`（第 98 轮） |
| 引导数据 | ✅ 209/261（80%）；★ 第 45 轮：**候选池 + 质量分级**（第一候选质量等级 1=204 / 2=3 / 4=2；**82 条**任务的第一候选被换成更好的目标 —— 含玩家实测抱怨的「平衡账目」「群众心态」）；★ 第 47 轮：**同 cell 常驻兜底**（`find_fallbacks`；931 条候选 / 常驻 339 条；「营救机器人」= `0x08ECA5` 非常驻 + `0x08ECA6` 常驻兜底链）；**无目标的 52 条已归因**（第 24 轮，结论：不补猜的目标） | `ref/guide_targets.json`（工具见 `docs/05` / `docs/06`；归因见 `docs/07`；持久性统计 `tools/esm/ref_flags_report.py`） |
| 代理任务（ESM） | ✅ 已挂 Reference Alias + Objective + 测试开关 / 目标高位 GLOB（0x804/0x805，第 20/21 轮，VMAD 已绑定）（手工字节补丁，幂等）；★ **第 30 轮：+11 条新建常驻 XMarker 引用**（记录号 0x900~0x90A，`Top 'CELL' > … > CellPersistent`）；★ **第 33 轮：+11 条 CELL 记录（官方「空壳」写法 = 只留 EDID + flags 0x4000，紧跟各自的 CellChildren 组）** —— 官方对同一条 cell（SFBGS003 → 霓虹城）就是这么写的；xEdit 验证 11/11 + 重存不丢；ESM 3521 B | `esm/SAQ_ShowAvailableQuests.esm`（`tools/esm/patch_saq_esm.py` + `create_board_markers.py`；结构对照工具 `tools/esm/dump_cell_group.py`） |
| 引导脚本 | ✅ 已编译（`StartTimer` 轮询 + `ForceRefTo` + `SetActive` + OnInit 重挂自愈 + 菜单事件 Trace + 菜单打开重挂定时器 + 三个地点候选（第 40 轮）+ 星图请求失效校验（第 42 轮）+ **第 44 轮：星图待办改由「轮询节拍」推进**（`StarMapPendingTicks` + `ProcessStarMapPending`；旧的 1.5 秒延时定时器退役 —— 实测它被「星图提前打开 ⇒ 暂停」冻死，一次都没跑到；节拍到点 = 菜单已关、游戏在跑）；★ 第 27 轮定案：菜单开着时定时器不走，引导只在关菜单时应用 —— 与原版一致）+ ★★ 第 46 轮：HUD 提示机制通用化（`NoticeText`/`ShowNotice`/`ProcessNotice`，替代星图专用版）+ 「目标尚未加载」提示（重复 3 次、冷却 5 分钟）+ 失败 Trace 节流（前 3 次 + 每 10 次） | `scripts/SAQ_Main.psc` → `Scripts/SAQ_Main.pex`（**17666 B**，第 49 轮加测试命令执行器后） |
| SWF 内嵌回退数据 | ✅ **9.8 K 字符**载荷（**只含基础游戏 212 条任务**；★★ 第 81 轮 +10 条地标任务（含说明列）；入口条目不在内嵌里，降级窗口 ~0.3s 可接受）；★ 第 74/75 轮：顺序与 C++ **逐条同序**（前 4 条 = 四大势力开头任务 → 8 条同伴任务 → 其余），列序含「简要说明」两列 | `ui/missionmenu/saqdata/SaqEmbeddedPayload.inc` |
| UI（missionmenu.swf / _lrg） | ✅ 已重编译（+ 接取后自动取消引导 + 结果码 4 回滚 + drop= 名单（第 19 轮）+ 任务板入口文案（第 27 轮）+ **第 29 轮：「太远」文案退场** + **第 37 轮：R 传 `map=1/0` 标记（peek 三段协议）** + **第 38 轮：R 成功后界面侧走原版 `CloseMenu(true)` 关掉整个暂停菜单（peek 四段协议）** + **第 39 轮：R 不再取消引导（`SaqToggleGuide` 第三参数；重复按 R = `重复设定航线:` + 再开星图）** + **第 42 轮：报告新增 `press=[…]` / `sel=[…]`（按键那一刻用的是哪一行 + 列表选中项，怀疑「R 选错条目」时的硬证据）+ R/Enter 两条路都先取一次 `selectedEntry`（悬停到分隔行不再抛异常）** + **第 43 轮：分诊探针 `ev=[…]`（`ProcessUserEvent` 收到的 user event，名+↓/↑）/ `btn=[our:plot=1:map=0]`（选中项变化时的按钮启用状态）—— 「按 R 没反应」一次定性；SET COURSE 不再置灰（沉默失败 → 可点 + 有提示）** + **第 44 轮：删掉原版 `MissionMenu_PlotToLocation` dispatch（星图位置滞后一条的病根；换成 `SaqNoteStarMapHandoff`：只留音效 + note `星图:交给脚本(菜单关闭后)`）** + **第 46 轮：描述新增「导航目标要靠近目标区域后才加载」+ 结果码 5「目标尚未加载」分支** + **第 48 轮：诊断标签的「0xNaN」→「(子项)」；状态栏名字查不到时回退玩家任务日志（不再「当前引导:?」）** + **第 49 轮：`SAQ_TestDrive*` 测试入口（切 tab/选中/按键/展开/状态，内部走真实处理函数）+ `MissionsList.SAQ_FindEntryIndexByUID`** + **第 49 轮补丁③：入口发布清单（`SaqPublishEntryPoint`）补挂 5 个 `SAQ_TestDrive*` —— 此前只加进了类里、没挂 root ⇒ `ui.tab` 连续两轮 0 ms 失败（真因；见 `docs/09` 第八·补节）** + **第 74 轮：描述提示好感度 + `order=` 顺序探针** + **第 75 轮：载荷第 9/10 列（简要说明）→ 描述第一句；无导航目标时 `SaqNoPickupNote`（「没有可以直接导航的接取地点」）；`pin=` 探针；`stamp=57`**） + **第 82 轮：同伴条目描述首句 + 尾部落点文案改写（说清「自动开始 / 引导指向同伴本人」）** + **第 89 轮：载荷第 11 列（`bSaqRepeatable`）+ `FilterKnownQuests` 对「已完成 + 可重复」放行 + `rptk=` 探针（★ 第 90 轮**未改 SWF** —— DLC 条目不在内嵌载荷里，`SaqEmbeddedPayload.inc` 逐字节不变）** + **第 91 轮：不可导航条目的显示名加「（不可导航）」前缀（`SaqNotNavigablePrefix` + `sSaqBaseName` / `SaqBaseName` 让提示仍报原名）+ `nonav=` 探针；内嵌载荷逐字节不变** + **第 96 轮：可重复条目名加「（可重复）」前缀（`SaqRepeatablePrefix`；与「（不可导航）」叠加时在前）+ `rq=` 探针 + `order=` 新增 `|tail=` 段（含 lrg 版 `MissionsList.as` 补上漏掉的 `SAQ_OrderProbe`）** | `ui/missionmenu/build/missionmenu.swf`（**733081 B / lrg 733191 B**、`stamp=63`） |
| 构建验证工具 | ✅ 产物特征检查（SWF/DLL/PEX；★ 第 30 轮 +ESM 数据侧解析检查「新建常驻 marker 11/11 条」；★ 第 33 轮 +「CELL 空壳 11/11 条」+ 空壳不许写数据字段 + DLL 静态表就绪特征 & 反向检查；★ 第 41 轮 +星图注册名 `GalaxyStarMapMenu` / 节点解析器特征字节（正反向）；★ 第 42 轮 +SWF 的 `press=[`/`sel=[`/`R@`/`E@` + DLL 的 press 抄录与「脚本已应用后才换候选」+ PEX 的星图过期校验；★ 第 43 轮 +SWF 的 `ev=[`/`btn=[` + DLL 的 `补处理了关闭瞬间的引导请求`；★ 第 45 轮 +DLL 5 条 + 静态表候选池完整性；★ 第 45 轮补丁 +DLL 2 条 + PEX 2 条；★ 第 46 轮 +SWF 5 条 + DLL 6 条 + PEX 5 条 + 反向检查；★ 第 47 轮 +静态表 2 条 + DLL 1 条 + 反向检查；★ **第 48 轮 +SWF 1 条（`(子项)` 标签）+ DLL 11 条（`全部候选都非常驻` / INFO 门槛统计与名单 / 误判告警 / ini `InfoCond` 与说明 / 认领宽限说明 / 引导更新未确认新文案 …）+ 静态表 4 条（INFO 结构/计数 290+341/切片越界/大器晚成门槛样本）+ 2 组反向检查（「首选候选非常驻」「入口引导目标更新未被脚本确认」「还开着：」）** + ★ 第 49 轮 +SWF 5 条测试入口 + DLL 10 条 harness + PEX 6 条写侧/通道 + ESM 8/8 GLOB；★ 第 49 轮补丁 +DLL 1 条（`harness 结束清菜单`）；★ 第 49 轮补丁② +DLL 3 条（候选降级观察期 / 观察结束 / 入口降级观察期）；★ **第 49 轮补丁③ +10 条（`入口已挂 root`：两个 patch 源（普通 + lrg）× 5 个 `SAQ_TestDrive*` 入口名，出现 ≥2 次 = 定义 + 挂载 —— 防「加了新入口没挂 root」再犯）**；★ 第 67 轮 **+10 条**（DLL：链式门槛统计 / 名单 / ini `ChainCond`；用例计划：`[case:r67_chain]` + 两条断言；静态表：`StaticChainGate` / `kChainGates` / 完整 26 边 25 任务 / 菜鸟觐见样本）；★★ **第 69 轮 +9 条**（静态表：链式门槛改成**与两个数据源逐边对账**（`34/34 · 33/33 · 边集合一致`）+ 扩展边样本 ×2；DLL：`驱动器 v69` + `用例文件解析失败…` 文案；用例计划：r69 两条断言 + `ui.state` 反向检查；反向检查 v68 已替换）；★★ **第 72 轮 +5 条**（用例计划：r71 三条 `[case:]` / `r71 CF 四条断言` / `遗产的结局任一门槛藏（INFO|链式）` + 两条反向检查）；★★★ **第 75 轮 +30 余条**（SWF 8 / DLL 11 / 静态表 6 / 内嵌载荷 2 / 用例计划 6，含 r65 反向检查收窄到 r65 段）+ 更新受影响旧检查（候选 208 / 行尾四列正则 / 内嵌载荷表头 12 条）累计全通过 + ★ 第 82 轮：SWF +4 条（同伴提示/落点 中英）+ stamp 60 更新） | `tools/ui/verify_saq_build.py` |
| ★★ 引擎内测试驱动（第 49 轮 · harness；★ 第 54 轮扩能力 + 落判据；★★ 第 56 轮修断言窗口 + 传送窗口；★★ 第 57 轮修「清场后星图晚开」+ `~0x…` 写回 + 两条用例红线；★★ 第 58 轮加「传送落地静默期 / 加载画面证据 / 卡死自动中止」；★★ 第 59 轮修「落地静默期绑死回执 Tick」= **每 Tick**推进 + 20 秒窗口只管等回执（见「十九」）；★★ **第 66 轮：命令回执窗口 3000 → 8000**（r44 的 `ping` 假 FAIL = 脚本执行了但晚 3.4 秒）+ 慢回执 note + 驱动器 **v66**（见「二十六」）；★★★★ **第 68 轮：断言正则解析期编译校验**（非法正则立刻报「正则非法」，不再退化成「日志里没出现」的误导性超时 —— 反向断言静默 false = 假 PASS）+ 统计行断言用 `scope=case` + 驱动器 **v68**；★★★★★ **第 69 轮：用例文件解析失败 ⇒ 该用例判 FAIL**（不再静默丢步 —— 15:43 会话 r65_icons 的 `ui.state` 踩过）+ 新增 `r69_chain_extra` / `r69_chain_extra_pass`（链式门槛扩展边 A/B）+ 驱动器 **v69**（见「二十九」/`docs/09` 十四·补九）；★★★★★★ **第 83 轮：`ui.*` 遇「桥没通」在步骤超时内自动重试（菜单刚打开那一两拍桥没解析出来）+ 清场延迟复查只在「真的看到并关掉星图」时提前结束（关任务菜单不算）+ 断言超时报文带三个打点 + 新体检工具 `tools/test/plan_regex_audit.py`** + 驱动器 **v70**（见 `docs/09` 十四·补十六）；★★★★★★★ **第 84 轮：r80 断言改判「精确目标二选一」（原板 / marker —— 活体 NPC 引用运行期可得，落原板更精确）+ 结果 JSON 非法 UTF-8 收口（`check_results.py` 容错读出）+ 新工具 `tools/test/log_hygiene.py`**（驱动器仍 v70；见 `docs/09` 十四·补十七）；★★★★★★★★ **第 87 轮：+2 条用例（`r87_or_group` / `r87_or_group_pass` —— 进度门槛 OR 组）⇒ 用例集 24 条**；★★★★★★★★★ **第 89/90 轮：+2 条用例（`r89_repeatable` 基础游戏 / `r90_dlc_repeatable` DLC —— 可重复任务做完一次后仍显示；DLC 用 `~0x…` 记录号，断言容忍加载顺序前缀）⇒ 用例集 26 条**；★★★★★★★★★★ **第 94 轮：26 条 26/26 全 PASS**（定稿判据实测收口 + `plan_regex_audit` 滚动噪声修复）+ **第 96 轮：+1 条 `r96_repeatable_group`（可重复前缀 + 整组排末尾）⇒ 用例集 27 条；第 97 轮 27 条 27/27 全 PASS**；**第 98 轮 +2 条（`r98_dlc_chain` / `r98_dlc_chain_pass` —— DLC 链式门槛 A/B）⇒ 用例集 29 条；第 100 轮实机 29/29 全 PASS**（见 `docs/09` 十四·补二十三 / 十四·补二十四 / `docs/11` 11.4）） | ✅ **`[case:smoke]` 已全绿（06:53 会话）**；★ 09:22 会话 6 条 PASS 2/FAIL 4（驱动缺陷：断言窗口清 0 / 传送 3 秒太紧 → 第 56 轮已修）；★ 09:35 会话 6 条 PASS 2/FAIL 4 —— **三条新真因仍在驱动器/用例侧、产品全对**（① 星图在清场**之后**才打开 ⇒ r26 `ping` 超时；② `ui.select/selectchild` 的 `~0x…` 漏写回 ⇒ r45 报「记录号 0x000000」；③ r47 没等推送就切 tab ⇒ 撞上内嵌回退数据 `notfound`；④ 两条断言共享同一批行 ⇒ 第二条须 `scope=case`），**已修、待重跑**；★ 10:31 会话 6 条 PASS 3/FAIL 3 —— **三个 FAIL 同源：游戏端卡在加载画面**（传送回执后 38 ms 又提交一条 MoveTo ⇒ Papyrus VM 卡在 `player.MoveTo`；r45 传送超时 + r48/r44 陪跑超时；界面 = 加载转圈 + HUD 蓝点），驱动器 **v58** 已加「落地静默期 / 菜单证据 / 卡死自动中止」，**待重跑**；★ 11:04 会话 6 条 PASS 4/FAIL 2 —— **两条 FAIL（r47/r45 的传送）是「落地静默期绑死回执 Tick」的假 FAIL**（Papyrus 两次 `MoveTo` 结果=0、超时瞬间菜单=无；与第 58 轮「真卡死」的区别就在这一行），驱动器 **v59** 已改成「静默期每 Tick 推进 / deadline 只管等回执 / 落地完成证据」，**待重跑**：用例文件（ini 风格 DSL）+ DLL 驱动器（造状态/开关菜单/选中/按键/断言）+ AS3 测试入口（**走真实处理路径**）+ 结果 JSON + 报告器。**只允许 `ini [Test] Harness=1` 时激活**（否则零开销）。通道 = ESM 追加的 8 条 GLOB（0x806~0x80D；纯追加，不动旧记录号）；写侧动作走 Papyrus 语言级 API（`Reset/Start/SetStage/CompleteQuest/MoveTo`）——**零新 RE 工作**。★ 第 54 轮新增：`assert.nolog`（反向断言）/ `scope=this\|prev\|case`（日志窗口 —— 动作与日志常在同一次 Tick）/ `ui.selectchild`（Enter 只引导走子项）/ `menu.hide <注册名>`（关星图）/ `teleport.entry`（站远走近）/ `~0x…`（记录号，免写死 DLC 前缀）；用例 6 条（smoke + r26_menu_idle / r44_starmap / r45_candidates / r47_board_marker / r48_info_gate）。★ 跑之前：ini `[Test] Mode=0`（显示全部）。设计与用法：**`docs/09-测试驱动（harness）.md`**（第十四节 = 第 54 轮；★ 第 55 轮两条用例编写红线：**断言「上一步的副作用行」必须 `scope=prev`**（smoke 漏写 ⇒ 假失败）+ **驱动器在每条用例结束都清场**（清引导/关任务菜单/关星图）并留 700 ms 间隔） | `plugin/src/SAQ_Test.{h,cpp}`（驱动器）、`SAQ_TestOps.{h,cpp}`（原语）、`scripts/SAQ_Main.psc`（`ProcessTestCommand`）、`ui/missionmenu/src/MissionMenu.as`（`SAQ_TestDrive*`）+ `MissionsList.as`（`SAQ_FindEntryIndexByUID` / `SAQ_FindChildIndexByUID`）、`tools/test/scenarios/SAQ_TestPlan.txt`、`tools/test/check_results.py`；部署开关 `build-saq.ps1 -Harness`。★★ **第 60 轮：新 op `guide.probe`（只读候选可得性探针 —— 与产品同一套查询，结果进步骤 JSON）+ r45 尾段改判（走远后判「不许早降级 + 引导仍指向 [1]」；旧「先保持」断言需读档窗口、不可达）+ 驱动器 v60**（见「二十」） |
| ★★ 离线层（第 64 轮 · 大项 K；★★ 第 87 轮 +1 用例） | ✅ **决策纯函数 11 个**（`plugin/src/SAQ_Decision.*`，零引擎依赖；调用点全部改用它；★ 第 84 轮 +`Utf8SafeCut` —— 证据通道的 UTF-8 安全截断；★★ 第 87 轮 `DecideProgressGates` 加 OR 组引擎算法）+ **单元测试 18 用例 / 929 断言**（★ 第 89 轮 +1 用例：可重复任务豁免「已完成 ⇒ 隐藏」；★ 第 90 轮**断言数不变** —— 决策逻辑未改，只扩数据；★ 第 96 轮列表顺序用例扩到「可重复垫底」+ `PinnedOrderKey` 四参数）（`plugin/tests`：自写 MiniTest + 独立 xmake target `SAQ_Tests`，不链接 commonlibsf，毫秒级）+ **数据管线黄金快照 14 件**（第 74/75/80/81/89 轮追加：同伴好感度 / 四大势力开头 / 地标任务 / 入口表 / 可重复任务…）（`tools\test\golden_snapshot.py`；规范化哈希 —— 形式改动不算变化、内容改动必报红；ESM 幂等实测）；一键 `tools\test\run-all-tests.ps1`；★ 第 84 轮新工具 `tools\test\log_hygiene.py`（日志/结果「证据通道健康」体检：非法 UTF-8 / 控制字符）；详见 **`docs/10`** | `plugin/src/SAQ_Decision.*`、`plugin/tests/`、`tools/test/` |
| ★★ 四大势力开头任务（第 75 轮） | ✅ **4 条**（UC01 超越极限 / FC01 枝节横生 / RI01 重返职场 / CF01 深藏不露）：`factionEntry` 列（0..3 = **固定顺序**）⇒ 固定显示（跳过三类门槛）+ **固定排前四**；载荷第 9/10 列 = 「简要说明」（描述第一句）；**CF01 的引导候选在生成期被清空**（只给说明 —— 点击给提示、描述写明「按上面的说明推进」）；日志 `势力入口固定=` / `势力入口前置=` / `势力入口固定名单:`（**单独一行**）；界面 `pin=` 探针 + SWF `stamp=57` | `ref/faction_entry_quests.json`（`tools/esm/gen_faction_entry_quests.py`）｜`SAQ_QuestTable.h`（`factionEntry` / `kFactionEntryNamesZh·En` / `kFactionEntryNotesZh·En`）｜`SAQ_Decision.*`（`IsFactionEntryPinned` / `PinnedOrderKey·Less`）｜`SAQ.cpp`（`FormatPinStats`）｜`MissionMenu.as`（`SaqNoPickupNote` / `SaqPinNoteProbe`） |
| MO2 部署 | ✅ **第 101 轮产物已部署**（DLL 开发构建（harness）+ 用例文件 **31 条**；ESM **4752 B** / SWF **733081 + 733191 B**（内嵌载荷逐字节不变 ⇒ 第 101 轮没重编 SWF）/ PEX 17666 B；`stamp=63`、入口 **20 条**、**链式边 84 / 78 条有边任务**、任务表 **271 条**、可重复任务 **20 条**）；★ 第 98 轮基线（DLL 开发构建 **1013248 B** / 发布构建 840192 B「0.1.9 包内」+ 用例文件 **29 条**；链式边 63、任务表 271）；verify 全过（第 96 轮 +SWF 10 项与 `stamp=63`（旧指纹 62 反向检查）+ 载荷「末尾连续 repeatable=1 共 15/15」+ 用例计划 r96 三条；第 91 轮 +SWF 六条与 `nonav=` 断言；第 90 轮 +DLC 样本）；★ **第 94 轮 26/26 + 第 97 轮 27/27 全 PASS**（12:00~12:08 / 13:50~13:57 会话 / `驱动器 v70`；`[E]` 0 / `[W]` 13→3 老现象；日志 316472 → 173235 B；工作区 == MO2 哈希一致；见顶部第 97 轮块；★ 第 100 轮：全量 29 条 **29/29 全 PASS**（r98 A/B 双向实证）；打包后切回开发构建（1013248 B）+ ini `Harness=0`） | `D:\Mod Organizer 2\starfield_mods\mods\Show Available Quests (SFSE)\` |
| Nexus 上传包 | ★★★★★ **0.1.10 已完成**（第 100 轮）：`dist\SAQ-ShowAvailableQuests-0.1.10.zip`（**1,845,225 B**，SHA256 `A551D4DD0C04CC146618EE467D0DC730CCFC182FDB7076848F58CE2CBE756113`；= 0.1.9 全部产物 + 第 98 轮 DLC 链式门槛（破碎空间主线 6 条启动边）+ 版本号六处同步 + changelog v0.1.10 + Nexus 文案；**可上传 Nexus**）；`verify --release` 全过 + **打包后补跑一次 verify 覆盖新包**（0.1.10 无测试资产 / 7 文件）+ 包内 5 产物与工作区哈希逐一致（DLL 发布构建 **840192 B**）+ ini 玩家默认值 + README `v0.1.10`；打完后已切回开发构建（DLL 1013248 B，`部署 == 工作区` verify 全过，MO2 ini 恢复 `Harness=0`）。★ **0.1.9（历史）**：1,845,170 B / `098E6544…81327E0`；**0.1.8**：1,844,547 B / `FE1E63E7…4A93B8`；**0.1.7**：1,839,484 B / `1F08EB31…EC8B25F` | `dist\` |
| 游戏内实测 | ✅ 第 21 轮 10:39~10:46 通过（连续切换 50 次 + 三种 DLC 目标）；✅ 第 23 轮 10:50 通过（无目标提示）；✅ 第 26 轮 11:12 通过（菜单里停 27 秒无假失败 + 关菜单 0.9 秒生效）；✅ 第 27/28 轮入口链路通过（列表/点击/就近引导成功）；❌ 第 30 轮 `marker 0 兜底 10`（11 条常驻 marker 取不到）→ 第 31 轮修偏移（精确优先 + 动态更新）→ 第 32 轮补「不进菜单自愈」→ 第 33 轮补「静态表预建 + CELL 空壳记录」；★ **第 34 轮复查（14:23~14:26 会话，日志侧全部通过 = 治本成功）**：① 玩家**不进菜单**：`静态表已就绪（菜单关着时预建，认领已有引导）` → `认领已有引导（任务板入口）` → `入口引导目标已更新：…0x0011B754 → 0x00137573（任务板自身（精确））`，Papyrus 14:24:42 真实应用；② `入口=12(可导航 12｜marker 9 原板 3 兜底 0 不可用 0)`；③ 远处点霓虹城/新亚特兰蒂斯城 → 引用 `0x0F000903`/`0x0F00090A`（新建常驻 marker（精确）），Papyrus `引导已应用：SAQ_BoardMarker_21001E (0F00090A)` = **引擎接受 CELL 空壳写法 + 新建常驻引用**；唯一遗留 = 1 行 WARN（读档 VM 冻结期确认窗口耗尽，脚本 1 秒后应用，无害）。**仅剩玩家眼睛最终确认蓝点落板**（详见一·补十九）；★ **第 40 轮待实测**：按 R ⇒ ① `星图诊断（R 请求）` 的地点链每层节点值（0 还是不 0）；② 星图**开起来**（第 1 次尝试，或重试的第 2/3 次 —— 日志写明）；③ 星图聚焦**接取地点所在行星**并画好航线；**同一条再按 R ⇒ 引导不取消、星图再开一次**（判据见一·补二十五第 5 节）；★ **第 41 轮待实测**：按 R ⇒ `星图：已打开（GalaxyStarMapMenu 在屏幕上…第 1 次尝试）`（此前 4 轮全是假阴性）+ `星图诊断` 节点值（特征修正后第一次有真实数据）（判据见一·补二十六第 5 节）；★ **第 42 轮待实测**：按 R ⇒ `引导请求：…｜界面按键行 press=R@0x…@… sel=idx=N:0x…@…`（press 的 uID 必须与请求的任务一致）+ 正常路径**不应**再出现 `星图：第 2 次尝试`（判据见一·补二十七第 5 节）；★ **第 43 轮待实测**：悬停「任务板 · 阿基拉城」按 R ⇒ `界面状态`/`菜单关闭：界面最后状态` 行里应带 `btn=[our:plot=1:map=0] ev=[… XButton↓ XButton↑]`，随后是 `press=[R@0x14d497@…]` → `引导请求：任务板 · 阿基拉城…`（三段任一缺失都能对号入座定位「按 R 没反应」的病灶，见一·补二十八第 5 节 / `docs/05` 第十七节）；★ **第 44 轮待实测**：依次点两条不同的任务板各按一次 R ⇒ Papyrus 每次都应出现 `星图请求：下一个轮询节拍（约 0.5 秒）打开（目标 …）` **且紧接着** `星图请求：打开星图并设定航线 → …（采用地点=…）`（这一行此前从未出现过）+ DLL `星图：已打开（… R 后约 N 秒，第 1 次尝试）`（N ≈ 0.5~1.5）；**眼睛**：星图聚焦的是**刚才按 R 的那条**任务板所在星球（不再滞后一条）（判据见一·补二十九第 5 节 / `docs/05` 第十八节）；★★ **第 44 轮已收口**（玩家 19:0x 会话实测 + 眼睛验收「肉眼观察未异常」；星图由脚本节拍打开、位置不再滞后）；★★ **第 45 轮待实测**：点「平衡账目」/「群众心态」引导 ⇒ `候选 [1/N]「黄」`（有名字的 NPC） + 远处点任务 ⇒ 预选直接落常驻候选（`… 个候选此刻都取不到` 只在全不可得时出现）+ 飞近后 `引导目标已更新（候选复算）`（判据见一·补三十第 5 节 / `docs/05` 第十九节）；★★ **第 46 轮待实测**（**ini `[Test] Mode=6` 会把列表筛到只剩这 76 条「需要靠近」的任务**）：远处点「营救机器人」⇒ 描述写明「需靠近」+ `引导确认：目标尚未加载 —— 保持待生效`（竖条保持、无 OFF 音）+ HUD 提示 3 次 + `引导重试（等待目标加载）第 N 次`（10/20/40/60 秒退避）；靠近目标区域 ⇒ `引导延迟生效` + 蓝点出现（判据见一·补三十一第 4 节 / `docs/05` 第二十节）；★★ **第 47 轮已实测通过**（20:41~20:47 会话）：① 远处点「营救机器人」⇒ `候选 [2/2]「Ref_08ECA6」` + `引导已生效 脚本状态=1`；② 走进 The Well ⇒ `引导目标已更新（候选复算）：0x0308ECA6 → 0x0308ECA5（[2]→[1]「G型」）`；③ R ⇒ 星图 1.6 秒打开（第 1 次尝试）；④ 接取后自动取消引导。P0 未实测项：无兜底的 20 条待生效链路（第 46 轮已验过）。★★ **第 48 轮已实测（21:45~21:47 会话，大部分通过）**：① 判据回退 ✓（`Mode=6` 推送 19 条 ≈ 20、新文案生效）；② 大项 D ✓（`INFO门槛=58(过37/藏21/未知0)` + 21 条名单；抽查合理 —— 『孤立无援』要『新亚特兰蒂斯城有棵树』完成，与玩家日志一致）；③ 顺手项 ✓（读档认领**无复算抖动** ×2 会话、星图等待新文案、星图链路两次完整 R 后 1.5 秒）；④ **同日补丁**：交叉验证抓出 2 条误藏（『全数到期』『陌生人的善意』）⇒ **引擎已开始 ⇒ 放行**；⑤ **待复测**：补丁后这两条应重新出现在列表里、日志 `INFO门槛=…(…/藏19/放行2/…)`；`Mode=6` 的「真会待生效」链路（挑一条远处点引导，看结果码 5 + HUD 提示 + 靠近后生效）；⑥ 「大器晚成」的隐藏 ✓（在 `INFO没到:` 名单里；记录级门槛也名列其中，双重判据）；★★ **第 49 轮首测（22:25 会话）**：harness 自动跑 smoke —— `harness：通道就绪（就绪（seq=0））` → `===== 开始用例 smoke =====`，前 6 步全过（`ping` / `quest.reset` / `menu.open` / 推送 249 条成功）；第 7 步 `ui.tab` ❌；★★ **复测（22:39~22:41 会话）**：前 6 步全过、`ui.tab` 仍然 0 ms ❌（`_root.SAQ_TestDriveTab=fail(路径不存在或调用失败)`）—— 复查定真因 = **测试入口没挂 root**（补丁③：`SaqPublishEntryPoint` 漏挂；与参数个数无关）⇒ **待复测**（重进游戏应 `summary.pass=1`）；★★ **第 52 轮实测（2026-09-21 06:53 会话）：`[case:smoke]` 首次全绿** —— `pass 1 / fail 0`、2640 ms、9 步全 PASS：`ui.tab`=`ok|tab=7|mask=64|n=245`（`Error #1074` 修复生效）+ `stamp=52` + `ep=pub=ok,ep=ok`（新版 SWF 已加载、入口挂载全好）；`press=R@0x6e0a1@沙布罗的解决方案 sel=idx=27` → `引导请求` → 菜单关闭 → 828 ms `引导已生效`（脚本状态=1）→ 星图 1.1 秒打开（`GalaxyStarMapMenu`，第 1 次尝试）→ `guide.clear` 对账重置；补丁②复查通过（无 `[1]→[3]→[1]` 复算抖动、无「未被脚本确认」WARN）；`INFO门槛=58(过37/藏19/放行2/未知0)`；`入口=12(可导航 12｜marker 10 原板 2 兜底 0 不可用 0)`；仅 2 条 `[W]`（开菜单首次推送桥解析失败 → 0.8 s 重试成功）、无 `[E]`；日志 555755 B（滚动正常）；★★ **第 56 轮实测（09:22 会话）**：6 条用例 PASS 2 / FAIL 4 —— 四个 FAIL 全是**驱动器**缺陷（① 断言窗口起点被清 0：r26 的 `推送成功` 断言 0 ms 命中 4 秒前那行、r48 假 FAIL「窗口从 idx 0 起」；② 传送回执要等 cell 加载：Papyrus 证明两次 `MoveTo` 成功、回执 4.3/4.5 秒 ⇒ 3 秒窗口判超时）；产品链路（推送 249 / `ui.tab` / R → 引导 → 星图 / INFO 门槛放行）全绿，已修、待重跑；★★ **第 59 轮实测（11:04 会话）**：6 条 PASS 4 / FAIL 2 —— 两条 FAIL（r47/r45 传送）是**驱动器假 FAIL**（落地静默期绑死回执 Tick；Papyrus 两次 `MoveTo` 结果=0、超时瞬间菜单=无、后续 r48/r44 全 PASS），驱动器 v59 已修、待重跑；★★ **第 60 轮实测（11:26 会话）：6 条 PASS 5 / FAIL 1 —— 第 59 轮的传送修复全部生效**（4 次 `teleport.entry` 全 PASS：回执后 3860~4172 ms 落地、加载画面已关 1500 ms；r47「入口 12/12 + 引导落 `0x0E00090A` 新建常驻 marker（精确）」、r48 `INFO门槛=58(过37/藏19/放行2/未知0)`、r44 星图第 1 次尝试 1.3 秒）；唯一 FAIL = `r45_candidates` 末步「`候选复算：…先保持`」断言 —— 真因 = **用例期望不可达**（走远后引导目标仍被脚本别名持有、`LookupByID` 一直可得 ⇒ 复算按设计无动作：DLL 侧 15 秒里按 1.5 秒节拍跑却零日志 + Papyrus `seq=11 MoveTo` 到 `CityCydoniaMainLevel` 成功）；处置 = 新 op `guide.probe` 探针 + r45 尾段改判「不许早降级 + 引导仍指向 `候选 [1]「G型」`」+ 驱动器 **v60** —— ★ **第 61 轮已实测收口**（11:55 会话 6/6 全 PASS）；★★ **第 62/63 轮（自动读档 + 崩溃加固）已实测收口（13:06~13:09 会话）**：7/7 用例 PASS + SAS 菜单日志读档后无 `MainMenu opening`（不再退回主菜单）+ 读档窗口暂缓生效（见「二十三」第 4 节）；★★ **第 64 轮实测回归（13:37~13:40 会话）**：离线层改造后的产物游戏内重跑 **7/7 全 PASS**（137 步 / 215 s；部署 13:31 早于启动 13:37 ⇒ 测的就是本轮产物）；★★ **第 65 轮产物实测（14:34~14:37 会话）**：8 条用例 PASS 7 / FAIL 1 —— **`r65_icons` PASS**（`icon=[0x351a:3/-1=Misc,0x9136:2/5=BlackFleet,…]`）+ `r62` 读档 PASS；唯一 FAIL = `r44` 的 `ping`（Papyrus 证明脚本执行了 `seq=17`、晚 3.4 秒）⇒ **第 66 轮已修**（见「二十六」）。★★ **第 67 轮已实测（15:21~15:24 会话）：10 条用例 9 PASS / 1 FAIL，产品侧全对** ——
① A 段 `链式门槛=22(过0/藏22/未知0)` + `链式没到:` 名单里**有**「菜鸟觐见」「风驰电掣」
（玩家截图里的 CF02/CF06 不再进列表）✓；② B 段 `过1/藏21` 且名单里没有「菜鸟觐见」✓
（`r67_chain_pass` PASS）；③ 唯一 FAIL = `r67_chain` 的统计断言 —— **用例正则写成双反斜杠**
（`\\d` 永不匹配）+ 统计行在推送前 3 ms 打印（`scope=prev` 差几毫秒）⇒ **第 68 轮**已修
（驱动器 v68 解析期校验 + 用例改 `scope=case`）；★★ **第 69 轮复查（15:43 会话）：10 条用例
10/10 全 PASS**（241813 ms，`驱动器 v68` 串在）—— 第 68 轮处置全部生效；唯一遗留 = 用例文件里
的**未知步骤 `ui.state`**（旧驱动器只 WARN + 丢步 ⇒ 用例照样 PASS）⇒ 第 69 轮已清 + 驱动器 v69
把「解析失败」变成硬失败，并新增 r69 两条用例 —— ★★ **已实测收口（第 70 轮 · 16:05 会话）：12 条用例 12/12 全 PASS**（263984 ms：链式门槛 N=30、r67/r69 A/B 名单、r65 图标帧、r62 自动读档 18593 ms 全中；`[E]` 0 条 / `[W]` 15 条全为老现象；日志 289963 B）；★★ **第 72 轮实测（17:04 会话）：15 条用例 13 PASS / 2 FAIL —— 两条 FAIL 全在用例侧**（r65_icons 的 `ui.select 深藏不露` notfound = 第 71 轮链式门槛修复生效；r71_chain_cf 的「遗产的结局」被 INFO 门槛先藏 = 期望不可达）⇒ 用例修正 + verify（+5 条）已部署；★★ **第 73 轮实测收口（18:00~18:05 会话）：15 条用例 15/15 全 PASS**（291531 ms / `驱动器 v69`；两条修正全部生效；`[E]` 0 / `[W]` 18 全为老现象；日志 318786 B）；★★★ **第 85 轮实测收口（06:25~06:31 会话）：22 条用例 22/22 全 PASS**（348234 ms / `驱动器 v70`；r80 改判 + 判据通道 UTF-8 修复双实证；`[E]` 0 / `[W]` 26 全为老现象；日志 574835 B / 结果 JSON 判据通道恢复）；★★★★ **第 87 轮实测收口（07:15~07:21 会话 / 385937 ms / `驱动器 v70`）：24 条用例 24/24 全 PASS**（A 段名单命中 + B 段 `assert.nolog` 命中；`check_results.py` / `log_hygiene.py` 退出码均 0；`[E]` 0 / `[W]` 28 老现象；日志 606108 B）；★★★★ **第 94 轮实测收口（12:00~12:08 会话 / 407266 ms / `驱动器 v70`）：26 条用例 26/26 全 PASS** —— 第 93 轮定稿判据首跑即全绿（r89/r90 三条判据全中、r75「（不可导航）」前缀与提示报原名两条同中）；`check_results.py` / `log_hygiene.py` 退出码均 0；`[E]` 0 / `[W]` 13（全为「推送失败（第 1 次）」老现象）；日志 316472 B；★★★★★ **第 97 轮实测收口（13:50~13:57 会话 / 406812 ms / `驱动器 v70`）：27 条用例 27/27 全 PASS**（含 `r96_repeatable_group` 1532 ms）—— 第 96 轮三条判据（`可重复任务=…｜排在末尾18` / `rq=[…=（可重复）…]` / `order=[…|tail=…=（可重复）…]`）全中；`check_results.py` / `log_hygiene.py` 退出码均 0；`[E]` 0 / `[W]` 3（全老现象）；日志 173235 B（超 1MB 已滚动，223 行）；14:26 玩家肉眼复查列表无异常；★★★ **第 100 轮产物全量 29 条 29/29 全 PASS**（19:40~19:47 会话 / 467687 ms；r98 A/B DLC 链式门槛双向实证 + 推送 216→219 强佐证 ⇒ **DLC 收口完成**；详见顶部第 100 轮块） | — |

一键构建（表已生成过时；`-SkipTable` 会跳过 3~6 分钟的 ESM 遍历）：
```powershell
& ".\tools\build-saq.ps1" -SkipTable
```
全流程见 `docs/01-构建与环境.md`；引导机制 `docs/05`；**DLC 支持 `docs/06`**。

---

## 一·补六 ~ 一·补三十五、第 21~50 轮（复盘已迁 docs/96 / 97 / 98）

> 第 21~50 轮的完整复盘已按轮次迁入 **`docs/96`**（32~34 / 35~40 / 41~46）、
> **`docs/97`**（48~50）、**`docs/98`**（9~16 / 21~25 / 26~31 / 47）（内容一字未改，只搬家）。
> 仍然生效的关键约定（第 93 轮压缩为速查块）：
> ① 日志与 ini 落在 **mod 目录内**（22，`docs/05` 第八节）；「能不能导航」界面可见（23）；
> 无引导目标的 52 条**不补猜目标**（24，`docs/07`）；发布打包 `tools\package-saq.ps1`（25）；
> ② **无限任务入口（任务板）** = ini `Mode=5`、入口表 12 条（27）；不可导航条目置灰 +
> 点击拦截（28）；任务板引导 = **候选链**（30 起）+ **精确优先**（31）+ 入口目标
> **动态更新**（32 扩到「菜单关着时」）；
> ③ 菜单关着时例行「认领 + 对账」（32）+ **静态表预建** + 11 条 **CELL 空壳**（只留 EDID +
> flags 0x4000；33）—— 任务板「任意位置精确导航」收口（34）；
> ④ 进度门槛首版（35）；SET COURSE 星图三层（37~40）；星图注册名 `GalaxyStarMapMenu` +
> 节点特征字节（41）；R 键分诊探针 `press=/sel=/ev=/btn=`（42/43）；星图由**轮询节拍**
> 打开（44）；引导目标**候选池**（45）；引导可用性收口（结果码 5 / 退避重试 / HUD 提示 /
> `Mode=6`；46）；同 cell 常驻兜底（47）；判据回退 + **INFO 门槛** + 「引擎已开始 ⇒ 放行」
> （48，详见 `docs/08` 第九节）；**引擎内 harness** 落地（49，完整设计 `docs/09` 第十四节起）；
> `ui.tab` 三连败真因 = **游戏进程启动早于部署** ⇒ 「先构建部署、再启动游戏」+ `stamp=`
> （50，`docs/09` 第八·补节）。

## 十一、下一步（按优先级）

0. ★★★ **第 102 轮（下）已判读 + 已改判：待实机跑 32 条** —— 第 101 轮 31 条 30 PASS / 1 FAIL
   （`r101_dlc_chain2_pass`）与本轮 32 条 30 PASS / 2 FAIL（+ `r101_dlc_chain2_mq05`）**同源**：
   推 `MQ03@4000` / `MQ05@1600` 后引擎零变化 = **写侧被引擎静默忽略**（探针实证：`未开始 +
   stageDone=0`；Papyrus 调用都成功但零脚本活动；对照 r98 B 段 `MQ_Shell@100` 完全生效）。
   ⇒ 两条 B 段已改成「先 `MQ_Shell@100` 预热、再推目标 stage」+ probe 断言链（`stageDone(100)=1`
   → `stageDone(4000/1600)=1` → 放行）；r98 B 段 +2 条探针；verify +7 检查 +1 反向检查；
   已构建部署（`Harness=1`）、verify / 离线层全过。
   **待办 = 实机**：重开游戏跑全量 **32 条**（判据：两段 probe 给出 `stageDone(100)=1` +
   `stageDone(4000)/(1600)=1` + 两条放行断言 + `check_results.py` / `log_hygiene.py` 退出码 0）
   ⇒ 通过即重打 **0.1.11** 上传包；若 `stageDone(100)=0` ⇒ 按「记录型用例」改判（见上）。
   复盘 `docs/09` 十四·补二十五/二十六 / `docs/06` 十节。
0. ★★★ **第 100 轮：全量 29 条 29/29 全 PASS ⇒「DLC 收口」完成 + 0.1.10 上传包已完成**
   （`dist\SAQ-ShowAvailableQuests-0.1.10.zip` 1,845,225 B / `A551D4DD…756113`；打完后已
   切回开发构建 + MO2 ini `Harness=0`）。★ 眼睛判据（下次进游戏顺手看一眼）见本列表末条。
   （红线仍有效：不许再对「在日志 + 已完成」（qdata/rptk）写断言。）
0. ✅ **0.1.9 上传包已完成**（第 97 轮 · 见顶部块与「Nexus 上传包」行；已切回开发构建）。
   ★★ **眼睛判据剩余项**（见本列表末条）：同伴文案 / 地标条目 /「（不可导航）」前缀
   —— 下次进游戏顺手看一眼（可随下次实测一起）。
0. ★★★ **第 87 轮实测收口（07:15~07:21 会话）：24 条用例 24/24 全 PASS** ——
   第 80~87 轮产物全部实机收口。判据：`check_results.py` / `log_hygiene.py` 退出码均 0 +
   A 段 `亲爱的姐妹[0x002117CA 0x002149FA completed=0 期望 1]`（OR 组不满足被藏）+
   B 段补做 Z04 后 `assert.nolog` 命中（不再被藏）+ 用例自清场（UC04/Z04/Z05 全 reset）；
   `[E]` 0 / `[W]` 28（全为「首次推送桥解析失败」老现象）；日志 606108 B。
   ★ 第 85 轮（三跑 22/22）+ **0.1.6 上传包已完成**（`dist\`，1,838,510 B，SHA256
   `195F602B…71DFA81`，可上传 Nexus；**不含第 87 轮 OR 组**）。
0. ★★★ **下一步大项候选**（按建议优先级）：
   ① ~~**重打上传包 0.1.7**~~ → ✅ **第 88 轮已完成**（`dist\SAQ-ShowAvailableQuests-0.1.7.zip`
   1,839,484 B / SHA256 `1F08EB31…EC8B25F`，含第 87 轮 OR 组；**可上传 Nexus**）；
   小提醒：`verify` 跑在压缩前 ⇒ 它查的总是「上一个包」，新包要补跑一次 verify 覆盖
   （第 88 轮已补 / 全过；已在「Nexus 上传包」行记录）；
   ★★ ~~「可重复任务」全量盘点（第 89 轮）~~ → ✅ **第 89 轮落地 15 条 + 第 90 轮补
   DLC 5 条 = 20 条**（SFFL 家族；三条「未收」已定案不收 —— 见 `docs/11` 第 90 轮一节）；
   ② **自动测试「虚拟桌面 / 独立会话」后台跑**（不再占用你的电脑；中期大项）；
   ③ **operator 语义继续产品化**（第 86 轮已全解、第 87 轮只用了 `==` + OR 位 ——
   余下 `!=`/`>`/`>=`/`<`/`<=` 与 GLOB/别名等 flags 仍放行，可盘一遍数据侧
   「进度类外键条件」里还有多少条能收），见 `docs/08` 4.3 / 4.4；
   ④ 低优先增强：「待生效」在列表里可见化 / 候选排序引入玩家位置因子 / 仍无兜底的
   20 条任务补常驻 XMarker；
   ⑤ 低优先：`City_Neon_Chem03` 供应线路的「任务不 Stop + 两个结局」结构值得日后
   实机确认一次（本轮按「无重启机制 ⇒ 不收」处理；若实测能重复接，再补进清单）。
0. **眼睛判据（剩余验收项 —— 玩家进游戏看一眼）**：
   * ★ **已肉眼确认（第 97 轮）**：可重复条目带「（可重复）」前缀 + 整组排在列表末尾（无异常）；
   * 同伴固定条目（如「巴雷特：违约」）⇒ 描述首句 = 「…好感度达到一定水平后自动开始」、
     末尾 = 「…引导到这位同伴的位置」；
   * 列表里 10 条「XX地标任务」；选中一条描述第一句 = 「拾取《…》即可接取（地点）」；
   * （可选）任务板条目不带「（可重复）」前缀（前缀只在可重复任务与 NPC 入口上）；
   * ★★ 第 91 轮：**不可导航的条目名字带「（不可导航）」前缀** —— 最省事的一条 =
     「（不可导航）深藏不露」（四大势力开头任务、固定排第 4）+「（不可导航）伦敦地标任务」；
     选中它 ⇒ 右侧描述仍说「该任务暂无导航目标」（提示语里**没有**前缀）。



0. ~~第 52 轮：`ui.tab` 的 `Error #1074`~~ → ✅ **已实测收口（2026-09-21 06:53 会话）**：
   `[case:smoke]` 首次全绿（判据见 `docs/98` 的「第 51~52 轮」一节）。★ 仍有效的流程：
   **改 SWF 后必须先构建部署、再启动游戏**。
   ★★ **第 53 轮（大项 F）：发布就绪已完成** —— `saq_harness` 编译开关（发布 DLL 不含
   harness：911360 → 784896 B）+ `package-saq.ps1` 自动「发布构建 → `verify --release`
   → 打包」+ **0.1.1 包重打**（`dist\SAQ-ShowAvailableQuests-0.1.1.zip`，1,810,262 B，
   含第 49~52 轮全部修复）+ README/Nexus 文案更新 —— 详见「十四、第 53 轮」。
   剩余后续（按需排期，即下一步的大项）：
   ① ~~**大项 G：把历史判据落成 harness 用例**~~ → ✅ **第 61 轮正式收口：11:55 会话 6/6 全 PASS**
   （第 54 轮落 6 条用例 + 第 55~60 轮七轮驱动器/用例修复全部实测通过；驱动器 v60，
   判据 = 日志串 + `python tools\test\check_results.py`；详见「二十一、第 61 轮」/ `docs/09` 十四·补七
   （2026-09-22 第 84 轮已迁 `docs/95` 的「第 58~61 轮」一节））；
   ★ 有意留着的**覆盖缺口**：观察期尾段（候选取不到 ⇒ 先保持 20 秒 ⇒ 降级）只能靠
   「读档 / 加载窗口」触发 —— 等下面第 ② 项（自动读档）落地后补用例；
   ② ~~自动读档~~ → ✅ **已完成并实测收口**（第 62 轮落地 + 第 63 轮加固，13:06~13:09 会话
   判据全中）：`QueueLoadGame` 在 commonlibsf 里是**内联实现**（只写 `queuedEntryToLoad` +
   `queuedTasks` 的 `kLoadGame` 位）⇒ 完全不需要那几个 ID=0 的函数；单例 ID 883588 可用
   ⇒ **零新 RE**。新 op `save.list` / `save.load <子串>`（驱动器 v61~v63）+ 用例 7
   `r62_reload_observe`（★ 只用指定存档 `Save7_3AB5A2FA…`）；详见「二十二」/「二十三」/
   `docs/09` 十五·补；
   ③ 虚拟桌面/独立会话启动（后台跑）；
   ④ ~~**离线层**~~ → ✅ **第 64 轮已完成**：10 个决策纯函数（调用点全部改用，DLL 重编 +
      verify 全过）+ 11 用例 / 608 断言（毫秒级、零游戏）+ 数据管线黄金快照 8 件（规范化哈希，
      形式改动不算变化、内容改动必报红）—— 一键 `tools\test\run-all-tests.ps1`；
      详见 `docs/10` / 「二十四」；
   ⑤ ~~发布包不含 harness~~ → ✅ 第 53 轮已完成。
   ★ 想「像玩家一样」跑一遍：`build-saq.ps1 -Release` 后再进游戏；回开发构建加 `-Harness`。

1. ~~第 34 轮：玩家侧最终验收~~ → ✅ **玩家已验收（2026-09-20 会话确认）**：蓝点/扫描仪路径线
   精确落板等**眼睛判据通过** —— 第 30~33 轮「任务板任意位置精确导航」四轮闭环正式收口。
   剩余小项（低优先，不影响验收）：14:24:41「更新未被脚本确认」WARN 的时机优化 ——
   读档 VM 冻结期确认窗口耗尽（脚本解冻后 1 秒即应用，通道未动、结果正确，只是文案容易误导）。
2. **第 18/26 轮已实测通过**（一·补四第 1 节 / 一·补十二第 1 节）——
   只剩「DLC 任务名显示、蓝点出现」这类需要**眼睛**确认的部分，随第 1 条一起看。
3. ~~任务板入口「任何位置都能精确导航」~~ → 第 29 轮（override）被否定 → 第 30 轮
   （新建常驻 XMarker）**实机判据 `marker 0` 也被否定** → 第 31 轮「精确优先 + 动态更新」
   修掉玩家可见的偏移 → **第 32 轮补上「重启/读档后不进菜单也自愈」**（例行认领 + 对账）。
   ★ 第 33 轮复查给出的答案（一·补十八第 4/6 节）：**缺 CELL 记录** ——
   官方在动基础游戏 cell 的引用时一定同时写一条该 cell 的 CELL 记录（6/6），且写法是
   **空壳（只留 EDID + flags 0x4000）**；我们已照抄（11 条），xEdit 与产物校验都过，
   **待实测 `marker 11` 判定**。次选路线：整条照抄 Starfield.esm 做对照 /
   Papyrus `PlaceAtMe(forcePersist=true)`（治不了「远处」）。

4. ★ **SET COURSE（R）的星图**：第 37 轮落地、第 38 轮修「暂停菜单没了结」、第 39 轮修「R 会取消引导」、
   第 40 轮修「调用时机 + 指错星球诊断」（三层实现见一·补二十二 ~ 一·补二十五 / `docs/05` 第十一~十四节），
   **第 41 轮修两个确定性 bug**：① 星图注册名 `GalaxyStarMapMenu`（此前误查 `MapMenu` ⇒ 4 轮假阴性）；
   ② 节点解析器特征字节抄反（诊断一直不可用）。
   **第 42 轮**（玩家反馈「R 的导航结果不稳定，有时目标不是悬停那条」）：先补「界面用了哪一行」的
   硬证据（`press=`/`sel=` 进报告与引导请求日志），再修两条**会改掉目的地**的缺陷 ——
   ③ DLL 星图重试抢在脚本 1.5 s 延时前面换地点候选（星图被重画 = 航线/焦点跳变）；
   ④ 脚本的星图延时待办在新引导/取消后没作废（会开出上一条任务的航线）。
   **第 43 轮**：补「按 R 没反应」的分诊探针（`ev=`/`btn=`）+ 关闭时刻现场读报告 +
   关闭前补一次请求轮询。
   ★★ **第 44 轮**：查明「星图位置永远是上一个导航」的真因 —— 第 36 轮那条原版 dispatch
   **其实生效**（第 37 轮「无 sink」结论不成立）：它用代理任务**当前**的目标位置打开星图，
   而那一刻脚本还没 ForceRefTo ⇒ 永远是**上一次**的引用；且它先把星图打开（暂停 ⇒
   脚本定时器冻结）⇒ 补救调用再也跑不到。⇒ 已删除 dispatch，星图改由脚本的**轮询节拍**
   打开（`StarMapPendingTicks` + `ProcessStarMapPending`，节拍到点 = 菜单已关、游戏在跑）。
   ★★ **已收口（第 44 轮实测 + 第 45 轮复查）**：玩家 19:0x 会话按 R 四次全部链路完整
   （星图 = 脚本节拍打开、R 后约 1.5 秒、第 1 次尝试），眼睛验收「未异常」；
   `星图诊断` 节点值第一次有真实数据（行星 FormID，如 `0x0005E2B6` 阿基拉）。
   剩余已知差别（接受）：星图是「关掉暂停菜单后」打开的（返回时回到游戏），
   且**不做**原版「未追踪时先问是否追踪」的弹窗（我们传的是地点、不是任务）。
   遗留小项（低优先，来自第 45 轮日志复查）：
   * `星图：等待中（…还开着：无）` 文案自相矛盾（每次成功前 ~38ms 都会先打一行）；
   * 19:01:23 一次「R 后约 0.0 秒」= 上一次星图未关的残留（玩家没报异常，留观）；
   * `星图诊断` 在目标 cell 未加载时退化为 `引导引用取不到（所在格子没加载）`（只影响诊断，
     脚本侧地点链照常工作）。
4. ★★ **引导可用性 + 提示收口（大项 B）→ 第 46 轮已完成、待实测**（一·补三十一 / `docs/05` 第二十节）：
   点引导时「所有候选都取不到」不再 19 秒后静默放弃 —— 描述**提前**写明「需要靠近」+
   结果码 5（保持竖条）+ HUD 提示 + DLL 退避重试（10~60 秒，靠近即自动生效）。
   判据见一·补三十一第 4 节；顺带收口的旧小项：界面文案里「靠近后可导航」的提示已落地。
   后续可选（低优先）：把「待生效」也在列表里做成可见状态（如竖条变暗/小图标），
   或给候选排序引入「玩家当前位置/同星系」因子（目前靠 LookupByID 可得性 + 退避重试已够用）。

4b. ★★ **引导可用性收口（大项 C）→ 第 47 轮已完成、✅ 已实测通过**（一·补三十二 / `docs/05` 第二十一节）：
   玩家实测「营救机器人」的「导航点被锁定在某处、导航没用」—— 真因 = 该任务的全部候选都是
   非常驻引用（远处一个都取不到）+ 脚本取不到时**不改别名** ⇒ 世界里的标记是**上一条引导的残留**。
   修法 = 数据侧 `find_fallbacks`（76 条里 56 条补到「同 cell 常驻兜底」）+ DLL 判据改
   「**首选**候选非常驻」。行为：远处点引导落到兜底（蓝点在目标附近）、靠近后自动升级精确目标。
   后续可选（低优先）：① 仍无兜底的 20 条（目标 cell 解析不到 / 该 cell 里确无常驻引用）——
   可离线往那些 cell 新增常驻 XMarker（第 30~33 轮做法）或让脚本 `PlaceAtMe(forcePersist)`；
   ② 兜底候选再分层（优先挑「任务自己的装置 / 名字带任务前缀」的常驻引用，而非最近的任意常驻引用）。

5. ~~引导质量（覆盖率 + 质量侧）~~ → ★★ **第 45 轮（大项 A）已完成**：引导目标升级为
   **候选池**（质量分级 + 点引导时预选 + 状态 2 换候选 + 菜单关着时动态复算，见一·补三十 /
   `docs/05` 第十九节）；52 条无目标维持「不补猜的目标」（`docs/07`）。**待实测**（一·补三十第 5 节）。
   后续可选（低优先）：给候选排序引入「玩家当前位置/同星系」因子 —— 目前 LookupByID 可得性
   已经覆盖大部分场景；`tools/esm/ref_flags_report.py`（第 27 轮的持久性统计工具）仍然可用。
6. ★ **「进度没到不显示」**：第 35 轮**首版已落地**（7 条任务 / 9 条门槛，`docs/08`）、
   ✅ 实测通过（14:47 会话：过3/藏4/未知0）；**★ 第 48 轮（大项 D）扩到对话侧** ——
   INFO 门槛（60 条任务 / 290 条对话 / 341 条条件；两例与记录级门槛完全一致），
   构建/verify 全过、**待实测**（判据见 `docs/08` 第九节）。后续可扩：
   * **DLC 的 INFO**（3 个 DLC 的 QUST/INFO 还没扫；保守：DLC 任务暂不做 INFO 判定）；
   * operator 0x01/0xA0/0xA4 的语义（要反汇编条件求值器本体）—— 解出来后「位置/遭遇」类
     条件也能覆盖（当前一律放行）。
7. **可扩展性（可选）**：把其它官方 Creation（`SFBGS003/004/006/007/008`、`Constellation`、
   `BlueprintShips*`）也加进 `fetch_sources.py::SOURCES` —— 表里已经有 master 概念，
   DLL 不用改；主要成本是每个 master 要抽一次字符串表。
8. **打包 + Nexus 文案**：第 25 轮已产出 0.1.0（`tools\package-saq.ps1` 可随时重打）；
   ★★ **第 62 轮（大项 H）已重打 0.1.2**（含第 54~55 轮的产品侧修复）：
   `dist\SAQ-ShowAvailableQuests-0.1.2.zip` 1,810,839 B（SHA256 `296B0210…CF9C1A8A`）；
   ★★★ **0.1.3 上传包已完成**（第 64 轮 · 大项 J）：版本号六处同步（xmake ×2 / main.cpp /
   package-saq.ps1 / build-saq.ps1 的 meta.ini / Nexus 文案）+ changelog 新增 v0.1.3
   （读档稳定性修复）⇒ `dist\SAQ-ShowAvailableQuests-0.1.3.zip` **1,811,446 B**
   （SHA256 `8BBFBA68…39B28B`；dll 786944 = 发布构建，无 harness）+ `verify --release` 全过
   + 包内 5 产物与工作区 SHA256 逐一一致；打包后已切回开发构建 —— **可上传 Nexus**；
   历史记录：第 46 轮重打一次（含第 45/46 轮全部产物：DLL 765440 B / SWF 726531+726812 B /
   PEX 12064 B / ESM 3521 B），文案里补上「需靠近的任务会在描述里写明」与
   「点引导后 HUD 会提示目标是否已加载」。
   文案里要写明的已知限制：
   * 首次引导后，任务日志里会出现一条名为「可接任务」的引导用任务（借引擎标记系统的代价）；
   * 部分任务 / 部分任务板入口没有可用的导航目标（列表照常显示，只是点了不会有标记）；
   * 引导总在**关闭菜单后**生效（与原版一致：HUD 蓝点本来就要关菜单才可见）；
   * 地球舰队的 5 条「鸭嘴猪」活动在游戏数据里就叫同一个名字（列表里看起来是重复项）。

### 已完成（保留记录）

* ✅ **第 33 轮 A/B 判据实测通过**（14:23~14:26 会话，第 34 轮复查）：自愈链路
  （静态表预建 → 认领 → 动态更新，玩家没开菜单）+ CELL 空壳 + 新建常驻 marker 被引擎
  真实应用（Papyrus `SAQ_BoardMarker_21001E`）——**任务板「任意位置精确导航」治本成功**
  （一·补十九）。
* ✅ **第 26 轮实测通过**（11:12~11:14 会话）：菜单里停 27 秒无假失败；关菜单 0.9 秒
  `引导已生效`（一·补十二第 1 节）。
* ✅ **第 18 轮实测通过**（09:53~09:56 会话）：前缀探测 4 master 全解析（命中 100%）、
  推送 260 条、认领已有引导、界面 257 条（一·补四第 1 节）。
* ✅ 验证「全数到期」修复 + 卡顿埋点（第 16 轮日志确认：`隐藏=1` 只剩「食宿」、`收集耗时=0 ms`、
  推送失败路径 0 ms、**无「主线程停顿」行**）。

## 十二、已知风险

| 风险 | 说明 |
| --- | --- |
| **「已开始」≠「已接取」**（第 11 轮实证） | 引擎会把「玩家仍能接到」的任务提前置成 running（RAD05 全数到期，flags=0x8001）。TESQuest 的 started 位**不能**当「玩家已接」的判据；只有玩家任务日志（QuestData）算数 |
| **GLOB（float）存不下完整 FormID**（第 21 轮踩过） | GLOB 是 float（尾数 24 位）：完整 FormID > 2^24 后**奇数不可精确表示**（±1）。已把引导目标拆成「低 24 位（0x801）+ 高 8 位（0x805）」两个 GLOB。**以后再开 float 通道，先问「值会不会超过 2^24」** |
| **Papyrus 定时器在菜单开着时不走**（第 27 轮定案） | 任务菜单暂停游戏 ⇒ `StartTimer` 不触发：三次会话的 `引导已应用` 都精确落在「菜单关闭」时刻，菜单开着 27~37 秒零应用。**不要设计「菜单开着时脚本会处理」的功能**；引导总是「关菜单生效」（与原版 HUD 交互一致）。DLL 侧也据此不再在菜单开着时轮询确认（降噪） |
| ~~任务板入口多为非常驻引用~~（第 27 轮发现 → 第 28 轮兜底 → 第 29 轮 override **被实机否定** → **第 30 轮新建常驻引用治本**） | 12 条入口里只有阿基拉城是原生常驻 ⇒ 其余 11 条在 cell 未加载时 `Game.GetForm` 取不到。★ **第 29 轮的 override 路线不成立**：override 只换数据、**不改变引用的加载分类**（官方 70 条同类 override 原记录本来就全是常驻 = 零先例；实机 12:45 会话 `可导航 2`）。第 30 轮改为**新建** 11 条常驻 XMarker（0x900~0x90A，位置 = 板坐标）+ 候选链。★ **第 33 轮补齐最后一块**：官方对同一条 cell（SFBGS003 → 霓虹城）的写法 = **空壳 CELL 记录（EDID + flags 0x4000）+ 紧跟 CellChildren 组** —— 我们也照抄了（11 条，xEdit 验证通过；**待实测 `marker 11`**）|
| ~~override 世界引用的冲突面~~（第 29 轮引入 → **第 30 轮已消除**） | 第 29 轮对 11 条任务板 REFR 的 override 已全部移除 ⇒ 不再覆盖任何基础游戏记录。第 30 轮只往那些 cell **新增**自己的记录（mod 加物件级操作），与改同一 cell 的 mod 无冲突 |
| ~~打开任务菜单偶发长停顿~~（第 11 轮发现 → 第 12 轮修复 → 第 16 轮确认） | 根因：失败解析路径里 `IsMapped`（VirtualQuery）累计 ~3.9 秒；`SafeRead` 改纯 `__try` 保护后消失。第 16 轮日志实证：收集 0 ms / 失败 0 ms / **无「主线程停顿」行** |
| **界面状态必须由回写驱动**（第 16 轮踩过） | `SaqToggleGuide` 只能在本地立即切状态，而「这条任务有没有引导目标 / 实际在引导哪条」只有 DLL 知道。凡是「界面先行、DLL 后处理」的动作都必须有回写通道（`SAQ_GuideReply`），否则界面会说谎 |
| **GLOB 锚点有次数窗口**（设计边界） | `SAQ_Notify` = 7777 + 菜单打开次数，认领窗口 7777..8776（1000 次）。单次会话极难超限；脚本重挂会把它重置回 7777（DLL 认领成功后不再读锚点，无影响） |
| **文本子记录必须带 NUL**（第 12 轮实证） | 非本地化 ESM 的 FULL/NNAM 等文本子记录，长度 = 字符数 + 1（含 NUL）。第 11 轮漏写 NUL ⇒ HUD「任务更新」显示 `[…]` |
| commonlibsf 的 `REL::ID = 0` 接口 | 已踩（BGSStoryTeller、`TESQuest::IsStageDone`）；编译期 ID 审计挡着（`docs/01` 坑 8） |
| **commonlibsf 的结构体偏移一律不可信**（第 18 轮升级为通则） | 已踩四次，其中 TESDataHandler 两次：`formArrays` 读到空表（第 5 轮，见 docs/02）、`files` 读到空链表（第 17 轮，四个 master 全被误判「未加载」⇒ 第 18 轮改前缀探测）。读引擎对象字段：先找「引擎自己怎么读它」（反汇编），或改用已实测可靠的引擎函数（`LookupByID` 等）；`TESGlobal::value`（0x48）是上一代项目修正过的 |
| **自己写的注释/文档也可能是错的** | 已踩：哈希指针、模糊 RTTI、误信「位16 = 自动启动」。结论要留原始反汇编/日志，且「改完要能被验证」 |
| **GFx 返回值要按类型读** | `GetNumber()` ≠ 万能（见 `docs/04` 2.4） |
| **TESQuest 运行时偏移来自反汇编** | `0x114 / 0x32C / 0x338` 有虚表核验 + 识别率安全阀；换游戏版本要重跑 `tools/re/quest_api_scan.py` |
| **非本地化插件的文本是内联的** | 本 ESM 的 TES4 flags = 0x01（无 Localized 位）⇒ 目标文本可以内联写 `<Alias=SAQ_GuideTarget>`，不需要动游戏的 strings |
| **非常驻引用** | 168 条引导目标里 68 条是非常驻引用：格子没加载时 `Game.GetForm` 可能取不到（日志会写 `状态=2`） |
| **patch 文件集 ≠ 源码目录** | 改任何 `.as` 都要在 `build-saq.ps1` 的同步清单里登记（普通 + lrg 两套）；lrg 源码由 `make_lrg_source.py` 生成 |
| 静态过滤是启发式 | 宁缺勿滥；规则集中在 `filter_reason()` 一处 |
| 语言判定 | AS3 侧按引擎推来的任务名判定（中文/英文）；C++ 侧 INI 值只作日志 |
| 菜单关掉后 Movie 会销毁 | 已处理：每次「菜单由关变开」都 `UI::Reset()` 清解析缓存 |
| 性能 | 菜单打开时：202 次 `LookupByID` + 202 次标志位读取 + 1~2 次 0x4000 字节扫描；开着菜单时每 500ms 一次 `SAQ_Report`、每 100ms 一次 `SAQ_PeekGuide`。实测无感 |
| **树形列表不能按「数据下标」刷新**（第 14 轮踩过） | 显示下标 ≠ 数据下标（`InitializeEntries` 会把已完成 / misc 条目挪位、展开时插入子项），而且 `BSScrollingList` 没有 `UpdateEntry` 接口 —— 第 13 轮那次调用每次抛 `Error #1006` 被 `try` 静默吞掉。就地刷新一律走 `MissionsList.SAQ_RefreshQuestRow(uID)`（按 uID 在当前显示列表里找行） |
| **DLC 的字符串表是各自一份**（第 17 轮） | 任务名要在**该插件自己的** `<master>_{语言}.strings` 里查（破碎空间在 `ShatteredSpace - Main02.ba2`，地球舰队在 `SFBGS050 - Main.ba2`）。漏抽的症状是「DLC 任务全部消失」（取不到名字 ⇒ 被当成内部任务过滤），而不是报错 |
| **medium / blueprint 档位的 FormID 方案未验证**（第 17 轮） | 本项目实测的四个 master 都是普通插件（fullIndex<<24）。light/ESL（`0xFE000000 \| smallIndex<<12`）已实现但**本机没有样本可验**；medium / blueprint 直接标记不支持并整批跳过（`SAQ_Masters::ResolveOne`），宁可少收也不错收 |
| **master 名大小写不一致**（第 17 轮） | ESM 的 MAST 里写的是 `starfield.esm`，而文件夹里是 `Starfield.esm`；`Masters::FindLoadedFile` 用大小写不敏感比较（日志里会打出实际匹配到的名字） |
| **内嵌回退数据只有基础游戏条目**（第 17 轮） | `SaqEmbeddedPayload.inc` 必须写运行期 FormID，DLC 的离线算不出来 ⇒ 只放 Starfield.esm 的 202 条。C++ 推送成功前的那 0.3~0.8 秒里看不到 DLC 条目（可接受的降级） |

## 十三、第 51 / 52 轮（已迁至 docs/98）

> 已迁 `docs/98` 的「第 51~52 轮」一节。仍然生效的结论：① 第 51 轮 —— `ui.tab` 失败分诊增强
> （`ep=` 入口自检 / try-catch 回传异常 / 多路径写法尝试）；② 第 52 轮 —— 真因 = **向只读属性
> 写入**（`Error #1074`）⇒ 改走原版 `MissionTabbedSelection.SetSelectedCategoryIndex`
> （拒绝码 `tab-refused`）；③ smoke 链路 06:53 首次全绿（`stamp=52`，见 `docs/09` 第十三节）。

## 十四、第 53 轮（大项 F · 发布就绪）：发布包不含 harness + 0.1.1 重打

> 起源：0.1.0 包缺第 49 轮补丁②与第 51/52 轮 SWF 修复；发布包不该含测试代码。
> 仍然生效的接口约定（细节见 `docs/01` 与 `plugin\xmake.lua` / `build-saq.ps1` 注释）：

* **编译开关 `saq_harness`**（xmake option，默认 y）：`n` = 发布构建 —— `SAQ_Test.cpp` /
  `SAQ_TestOps.cpp` 不参与编译，DLL 里没有任何 harness 字符串（911360 → 784896 B）；
  发布版发现 ini `Harness=1` ⇒ 打一行 WARN（不静默）。
* ★ 三个 xmake 坑（别再退回去）：① 「无条件 remove_files + 条件 add_files 加回」不生效
  （LNK2019）；② `add_files(..., {exclude=…})` 不生效（C2039）⇒ **只在关闭分支
  remove_files**；③ `xmake f` 只传 `--saq_harness` 会重置其它参数 ⇒ 每次带全套。
* **三个入口**：`build-saq.ps1`（开发构建 + 部署；`-Harness` 另写 ini + 拷用例）/
  `build-saq.ps1 -Release`（发布构建；不拷测试资产）/ `package-saq.ps1`（发布构建 →
  `verify --release` → 打包，一键）。
* **verify 两模式**：默认按 DLL 自动探测；release = 13 项反向检查（出现任何 harness 特征串
  即 MISS）+ 1 项正向；两模式都查「MO2 部署 == 工作区」（构建了没部署 = 玩家跑的还是旧的）。
* SWF / PEX / ESM 两版相同：AS3 测试入口与测试 GLOB **保留**（发布版 DLL 无调用路径）。
* 产物：`dist\SAQ-ShowAvailableQuests-0.1.1.zip`（1,810,262 B；dll 784896 / swf
  727572+727799 `stamp=52`）；版本号四处同步（xmake ×2 / main.cpp / package / meta.ini）。

## 二十九、第 75 轮（四大势力开头任务）（已迁至 docs/95）

> 2026-09-21 第 83 轮：`docs/99` 再次超过 100 KB 上限（AGENTS.md 的单文件限制），把第 75 轮
> 的复盘整体挪到 **`docs/95-历史轮次记录（第43~60轮）.md`** 的「第 75 轮」一节
> （内容一字未改，只搬家）。仍然生效的接口约定（速查）：
> ① 四条 = UC01 **超越极限** / FC01 **枝节横生** / RI01 **重返职场** / CF01 **深藏不露**
>   （认任务请用英文名 / EDID）；四条**固定显示**（跳过进度/INFO/链式三类门槛）+ **固定排前四**；
> ② 静态表行尾 `factionEntry` 列（-1 = 不是；0..3 = 固定顺序：联合殖民地 → 自由星 → 龙神 →
>   深红舰队）+ 四个数组 `kFactionEntryNamesZh·En` / `kFactionEntryNotesZh·En`；
> ③ 载荷加第 9/10 列（简要说明，描述第一句用它）；④ 离线层 `IsFactionEntryPinned` /
>   `IsGatePinned`（两个 pin 来源的唯一合并判据）/ `PinnedOrderKey·Less`；
> ⑤ `FormatPinStats` **单独一行**（与三类门槛名单分行 —— 防跨字段假命中）；
> ⑥ CF01 的引导候选在**生成期清空**（只给说明：点击给提示、描述写明「按上面的说明推进」）；
> ⑦ 界面 `SaqNoPickupNote`（不可导航说明）/ `SaqPinNoteProbe` + `pin=` 探针。

## 十五、第 54 / 55 轮（已迁至 docs/97）

> 2026-09-21 第 56 轮：`docs/99` 逼近 100 KB 上限（AGENTS.md 的单文件限制），把第 54、55 轮
> 的复盘整体挪到 **`docs/97-历史轮次记录（第53~55轮）.md`**（内容一字未改，只搬家）。
> 仍然生效的关键结论：
> ① 第 54 轮：历史判据落成 **6 条用例**（`smoke` + r26/r44/r45/r47/r48）；harness 补了
> `assert.nolog` / `scope=prev|case` / `ui.selectchild` / `menu.hide` / `teleport.entry` / `~0x…`；
> SWF `stamp=53`、DLL 923136 B；
> ② 第 55 轮：**两条用例编写红线** —— 断言「上一步的副作用行」必须 `scope=prev`；用例结束
> 必须清场（驱动器已内置 `CleanupAfterCase()` + 700 ms 间隔）；同轮还收了两个星图问题
> （换候选重试 1.5→3 s、残留重试请求回收）；
> ③ 第 56 轮修掉「断言窗口起点被清 0」后（见下），第 55 轮的「期待 6 条全 PASS」才真可判读。

## 十六 / 十七、第 56 / 57 轮（已迁至 docs/97）

> 已迁 `docs/97`。仍然生效的结论：① 第 56 轮 —— 断言窗口起点**按步保留**（清 0 ⇒
> `scope=prev` 退化成整个环形缓冲 = 假 PASS/假 FAIL）；传送回执要等 cell 加载（20 秒窗口）；
> 断言**只认产品日志**；② 第 57 轮 —— `~0x…` 参数要写回 `formId`；「按过 R 的用例」结束后
> 开 **4 秒延迟复查窗口**；两条断言命同一批行时第二条 `scope=case`；③ 用例红线（第 55 轮起）：
> 「上一步的副作用行」必须 `scope=prev`；每条用例结束必须清场。

## 十八 / 十九、第 58 / 59 轮（已迁至 docs/97）

> 已迁 `docs/97`。仍然生效的结论：① 第 58 轮 —— 传送「落地静默期」（加载画面消失 +
> 连续静默 1.5 秒 + 距回执 ≥3.5 秒）；「回执后紧接着再传送」会让加载流程卡死；
> ② 第 59 轮 —— 静默期与「读到回执那一 Tick」解耦（**每 Tick**推进）、20 秒窗口只管等回执。

## 二十、第 60 轮（已迁至 docs/97）

> 2026-09-21 第 74 轮：为控制 100 KB 上限（AGENTS.md 的单文件限制），把第 60 轮
> （大项 G 修复 · 六：r45 唯一 FAIL = 用例期望不可达）的复盘整体挪到
> **`docs/97-历史轮次记录（第53~55轮）.md`**（内容一字未改，只搬家）。仍然生效的结论：
> ① 新 op `guide.probe`（只读候选可得性探针）+ r45 尾段改判「不许早降级 + 引导仍指向
>   `候选 [1]「G型」`」+ 驱动器 v60（第 61 轮已实测收口 6/6）；
> ② 「引导目标被脚本 ForceRefTo 到别名上 ⇒ 走远后 LookupByID 仍可得」= 候选复算零日志
>   是**按设计保持**（蓝点不抖），不是缺陷。

## 二十一、第 61 轮（大项 G 收口）（已迁至 docs/97）

> 2026-09-21 第 63 轮：把第 61 轮的实测复盘（6 条用例全 PASS 的判据表 / 要留的结论 /
> 遗留项）整体挪到 **`docs/97-历史轮次记录（第53~55轮）.md`**（内容一字未改）。
> 仍然生效的关键结论：
> ① **大项 G 正式收口**：用例集 = `smoke` + r26/r44/r45/r47/r48（6 条），驱动器 v60；
> ② 实测证实「引导别名持有 ⇒ 走远仍可得」—— 候选复算「零日志」= 按设计保持（蓝点不抖）；
> ③ 唯一覆盖缺口 = 观察期尾段（需读档窗口）—— 已由第 62 轮的自动读档用例补上
>   （第 63 轮又给它加了「读档窗口不许建表」的回归断言）。

## 二十二、第 62 轮（大项 H + I）（已迁至 docs/97）

> 2026-09-21 第 63 轮：`docs/99` 逼近 100 KB 上限（AGENTS.md 的单文件限制），把第 62 轮
> （0.1.2 上传包 + 自动读档）的复盘整体挪到 **`docs/97-历史轮次记录（第53~55轮）.md`**
> （内容一字未改，只搬家）。仍然生效的关键结论：
> ① 大项 H：**0.1.2 上传包已完成**（`dist\SAQ-ShowAvailableQuests-0.1.2.zip`，1,810,839 B，
>   打包流程自动把 ini 还原到玩家默认值、不含测试资产）；
> ② 大项 I：**自动读档**（`save.list` / `save.load`，零新 RE —— BGSSaveLoadManager 内联排队）
>   —— 第 63 轮（补②）已给它加固，见「二十三」。

## 二十三、第 63 轮（已迁至 docs/97）

> 2026-09-21 第 68 轮：为控制 100 KB 上限（AGENTS.md 的单文件限制），把本轮回溯整体
> 挪到 **`docs/97-历史轮次记录（第53~55轮）.md`**（内容一字未改，只搬家）。仍然生效的结论：
> ① 真因：`Tick()` 的「菜单关着」此前只认 `BSMissionMenu` ⇒ 读档窗口（MainMenu /
>   LoadingMenu / FaderMenu）被当成「菜单关着」，例行认领 + 静态表预建落在引擎重建世界
>   的时刻 ⇒ 读档失败退回主菜单、6 秒后引擎空指针崩溃（`exe+0x1999E57` rcx=0）；
> ② 修法：产品侧 `WorldBusyForUpkeep()`（三个「忙」菜单任一开着 ⇒ 暂缓例行认领/候选复算）
>   + 驱动器 v63 + 用例回归断言（读档窗口不许建表）+ verify 4 条；新工具 `tools/re/crash_ctx.py`；
> ③ **已实测收口**（13:06~13:09 会话四条判据全中）：读档后无 `MainMenu opening`、7/7 用例
>   PASS、无「自动读档似乎失败了」WARN。

## 二十四、第 64 轮（大项 J + K）（已迁至 docs/97）

> 2026-09-21 第 66 轮：`docs/99` 逼近 100 KB 上限（AGENTS.md 的单文件限制），把第 64 轮
> （0.1.3 上传包 + 离线层：决策单测 + 数据管线黄金快照）的复盘整体挪到
> **`docs/97-历史轮次记录（第53~55轮）.md`**（内容一字未改，只搬家）。仍然生效的关键结论：
> ① 大项 J：**0.1.3 上传包已完成**（`dist\SAQ-ShowAvailableQuests-0.1.3.zip`，1,811,446 B，
>   SHA256 `8BBFBA68…39B28B`；`verify --release` 全过、包内 5 产物与工作区哈希逐一一致）；
> ② 大项 K：**离线层**（`plugin/src/SAQ_Decision.*` 10 个决策纯函数 + `plugin/tests`
>   11 用例 / 608 断言 + `tools/test/golden_snapshot.py` 8 件数据快照）—— 完整说明见 `docs/10`，
>   一键 `tools\test\run-all-tests.ps1`；
> ③ 实测回归（13:37~13:40 会话）：第 64 轮产物游戏内 harness 重跑 **7 用例 / 137 步全 PASS**
>   ⇒ 决策纯函数化没有改变任何运行时行为。

---

## 二十五、第 65 / 66 轮（已迁至 docs/97）

> 2026-09-21 第 67 轮：两次把复盘整体挪到 **`docs/97-历史轮次记录（第53~55轮）.md`**
> （内容一字未改，只搬家）。仍然生效的关键结论：
> ① **第 65 轮（任务专属图标）**：QUST 的 FTYP 关键字 → 原版 UI 阵营枚举
>   （`ref/faction_types.json`；表内 74/261 条有阵营）；payload Q 行第 7 列 = 阵营；
>   条目带真实 iType + iFaction（`SaqSafeFaction` 收敛边界）；图标 sprite 实测 **13 帧**
>   （含 `None`）⇒ `SaqSafeType` 归一化退役；探针 `MissionsList.SAQ_IconProbe` → `icon=`
>   字段；实测 `r65_icons` PASS、`stamp=54`。
> ② **第 66 轮（驱动器窗口太紧）**：命令回执窗口 **3000 → 8000**（不再被
>   `kDefaultStepTimeoutMs(5000)` 封顶）、`ping` 支持 `timeout=`、回执 ≥2000 ms 打
>   `命令回执偏慢` note。

---

## 二十七、第 67 轮（已迁至 docs/97）

> 2026-09-21 第 68 轮：`docs/99` 再次超过 100 KB 上限（AGENTS.md 的单文件限制），把第 67 轮
> （任务链门槛：玩家实测「深红舰队线的后续任务不该出现在『可接任务』里」）的复盘整体挪到
> **`docs/97-历史轮次记录（第53~55轮）.md`**（内容一字未改，只搬家）。仍然生效的关键结论：
> ① 数据 = 官方 Papyrus 源码里的跨任务启动调用（`tools/esm/gen_quest_chain.py` →
>   `ref/quest_chain.json` → `kChainGates`）：工具 **31 目标任务 / 33 边** ⇒ **表内 25 条任务 /
>   26 条边**；运行时 `链式门槛=N` 只数「走到链式判定」的（15:24 实测 **22** = 表内 25 − 3 条
>   已被 INFO 门槛先藏：破坏 / 霓虹城中的暗影 / 希望之基）；
> ② 判据 = 「全部启动边都没触发 ⇒ 隐藏」（边之间是「或」：任一条已触发/求值不了 ⇒ 放行），
>   ini `[Filter] ChainCond` 可关；日志 `链式门槛=N(过A/藏B/未知K)` + `链式没到:` 名单；
> ③ **已实测（第 68 轮复查）**：A 段 `过0/藏22`（「菜鸟觐见」「风驰电掣」在名单里 ✓）、
>   B 段（推进 CF01@1000）`过1/藏21` ✓ —— 玩家截图里的 CF02/CF06 不再进列表；
> ④ 判据细节 / 边界见 `docs/08` 第十节；harness 复盘与「红线四」见 `docs/09` 十四·补八。

---

## 二十八、第 68 / 69 / 70 轮（已迁至 docs/97）

> 2026-09-21 第 70 轮：为控制 100 KB 上限（AGENTS.md 的单文件限制），把第 68 轮（用例正则
> 写错 · 产品全对）、第 69 轮（同类问题排查 + 链式门槛扩展边 + 驱动器 v69）两节，连同
> **第 70 轮实测收口（12 条用例 12/12 全 PASS）**的记录，整体挪到
> **`docs/97-历史轮次记录（第53~55轮）.md`**（内容一字未改，只搬家）。仍然生效的关键结论：
> ① **红线四**：对 DLL 周期性统计行（`运行时状态` / `链式门槛` / `INFO门槛` / `进度门槛`）
>  的断言一律 `scope=case`；用例文件解析失败 ⇒ 该用例判 FAIL（驱动器 v69）；非法正则
>  解析期即报错（驱动器 v68）；
> ② 链式门槛表 = **33 条任务 / 34 条边**（编号链 25/26 + 扩展 8/8）；运行时 N 实测 **30**
>  （= 表内 33 − 3 条被 INFO 门槛先藏；原编号链时代是 22）；
> ③ **12 条用例为回归基线**（smoke + r26/r44/r45/r47/r48/r65/r67×2/r69×2/r62）——
>  已全 PASS；判据 = 日志 `驱动器 v69` 串 + `python tools\test\check_results.py`。