# 99 · 当前项目进度

> 最新更新：2026-09-20（**第 33 轮：日志复查 —— 第 32 轮自愈「跑了但认不出」的真因（静态表只在开菜单时建）+ 关键新证据：我们往基础游戏 cell 新增的引用缺 CELL override，官方先例 6/6 都带**）

## 一、当前状态

| 组件 | 状态 | 位置 |
| --- | --- | --- |
| SFSE 插件（DLL） | ✅ 已构建（多 master 前缀探测 + 引导结果确认 + 认领已有引导 + 脚本活性探测 / 引导未生效收尾（第 19 轮）+ 控制台测试过滤（第 20 轮）+ 目标 FormID 拆分 / 自检上限修复（第 21 轮）+ 确认窗口改绑菜单状态（第 26 轮）+ **无限任务入口条目表 / 引导确认与停顿降噪**（第 27 轮）+ 入口引导候选链（第 30 轮）+ **候选链改「精确优先」（任务板自身）+ 引导目标动态更新 + marker 探测**（第 31 轮）+ **菜单关着时例行认领 + 通道对账**（第 32 轮）） | `plugin/build/windows/x64/releasedbg/SAQ_ShowAvailableQuests.dll` |
| 静态任务表 | ✅ **261 条 / 4 个 master**（含 `guideRef`，209 条有值） | `plugin/src/SAQ_QuestTable.h` |
| 入口条目表（第 27/28/29 轮） | ✅ **12 条任务板**（新亚特兰蒂斯/阿基拉城/霓虹城/塞多尼亚/霍普镇/新家园/陋室/龙神集团/三家星船厂/星钥站）；uID = 引导目标 = 任务板引用；ini `Mode=5` 只显示它们；★ **第 30 轮：引导目标 = 候选链**（① 新建常驻 XMarker（记录号 0x900~0x90A，精确落点）→ ② 任务板引用自身 → ③ 同 cell 原生常驻引用兜底），DLL 依次 `LookupByID` 取第一个命中的 + 诊断日志；第 29 轮 override 路线被实机否定（见一·补十五） | `plugin/src/SAQ_EntryTable.h`（`tools/esm/gen_entry_table.py`）；清单 `ref/entry_targets.json`；新建 marker `tools/esm/create_board_markers.py`（产物 `ref/board_markers.json`）；侦查 `tools/esm/scan_entry_persistent.py`；先例核查 `tools/esm/check_persist_precedent.py` |
| 运行时过滤 | ✅ **只挡「已完成」**；「已接取」只认玩家任务日志；**master 没加载 ⇒ 整批跳过** | `plugin/src/SAQ_QuestState.*` / `SAQ_Masters.*` |
| 引导数据 | ✅ 209/261（80%），常驻 133 条（`ref` 型 87 全常驻；`actor` 型 46 常驻 / 76 非常驻）；**无目标的 52 条已归因**（第 24 轮，结论：不补猜的目标） | `ref/guide_targets.json`（工具见 `docs/05` / `docs/06`；归因见 `docs/07`；持久性统计 `tools/esm/ref_flags_report.py`） |
| 代理任务（ESM） | ✅ 已挂 Reference Alias + Objective + 测试开关 / 目标高位 GLOB（0x804/0x805，第 20/21 轮，VMAD 已绑定）（手工字节补丁，幂等）；★ **第 30 轮：+11 条新建常驻 XMarker 引用**（记录号 0x900~0x90A，`Top 'CELL' > … > CellPersistent`，xEdit 验证 11/11 + 重存不丢）；第 29 轮的无效 override 已移除 | `esm/SAQ_ShowAvailableQuests.esm`（`tools/esm/patch_saq_esm.py` + `create_board_markers.py`） |
| 引导脚本 | ✅ 已编译（`StartTimer` 轮询 + `ForceRefTo` + `SetActive` + OnInit 重挂自愈 + 菜单事件 Trace + 菜单打开重挂定时器；★ 第 27 轮定案：菜单开着时定时器不走，引导只在关菜单时应用 —— 与原版一致） | `scripts/SAQ_Main.psc` → `Scripts/SAQ_Main.pex` |
| SWF 内嵌回退数据 | ✅ 7.1 K 字符载荷（**只含基础游戏 202 条任务**；入口条目不在内嵌里，降级窗口 ~0.3s 可接受） | `ui/missionmenu/saqdata/SaqEmbeddedPayload.inc` |
| UI（missionmenu.swf / _lrg） | ✅ 已重编译（+ 接取后自动取消引导 + 结果码 4 回滚 + drop= 名单（第 19 轮）+ 任务板入口文案（第 27 轮）+ **第 29 轮：「太远」文案退场**） | `ui/missionmenu/build/missionmenu.swf`（725389 B） |
| 构建验证工具 | ✅ 产物特征检查（SWF/DLL/PEX；★ 第 30 轮 +ESM 数据侧解析检查「新建常驻 marker 11/11 条」+ 候选链特征 & 反向检查；累计全通过） | `tools/ui/verify_saq_build.py` |
| MO2 部署 | ✅ 第 30 轮产物已部署（ESM（含 11 条新建常驻 marker）/ DLL / PEX / SWF + 测试开关 ini；日志与配置都落在 mod 目录内） | `D:\Mod Organizer 2\starfield_mods\mods\Show Available Quests (SFSE)\` |
| Nexus 上传包 | ✅ 已生成（第 25 轮）：`dist\SAQ-ShowAvailableQuests-0.1.0.zip`；一键重打 `tools\package-saq.ps1`；文案 `resources\nexus-description.md`；★ **第 27 轮产物需重打一次** | `dist\` |
| 游戏内实测 | ✅ 第 21 轮 10:39~10:46 通过（连续切换 50 次 + 三种 DLC 目标）；✅ 第 23 轮 10:50 通过（无目标提示）；✅ 第 26 轮 11:12 通过（菜单里停 27 秒无假失败 + 关菜单 0.9 秒生效）；✅ 第 27/28 轮入口链路通过（列表/点击/就近引导成功）；❌ 第 30 轮实测失败（13:11 会话 `marker 0 兜底 10`）：新建的 11 条常驻 marker 引擎里一个都取不到，引导落到离板 1.8~8.1 m 的兜底引用；★ 第 31 轮修复后（13:11/13:12 会话仍是第 30 轮 DLL）玩家重启测试仍报「偏差仍然存在」——**本轮（第 32 轮）的起点**：13:30/13:32 两次重启后**没有开过菜单**（日志无「菜单打开」）⇒ 第 31 轮的动态更新因为「没认领」而短路，存档里的旧兜底目标继续生效；★ **第 32 轮待实测**：重启/读档后**不进菜单**直接看 HUD → ≤2 秒 `认领已有引导` + `入口引导目标已更新` → 蓝点上板（见一·补十七第 3 节判据）；★ **第 33 轮复查（13:43 会话）**：自愈机制**跑了但认领失败** —— 静态表未就绪（真因见一·补十八）；同 cell 精确目标 ✅（13:45:01 陋室）、远处兜底 ✅、脚本活性 ✅ | — |

一键构建（表已生成过时；`-SkipTable` 会跳过 3~6 分钟的 ESM 遍历）：
```powershell
& ".\tools\build-saq.ps1" -SkipTable
```
全流程见 `docs/01-构建与环境.md`；引导机制 `docs/05`；**DLC 支持 `docs/06`**。

---

## 一·补、第 17 轮：日志复查（09:14~09:18 会话）找出并修掉的三处引导缺口

把当天的 `SFSE\Logs\SAQ_ShowAvailableQuests.log` 逐行与代码对照，**先说好消息**：
第 16 轮的改动全部按设计工作 ——

| 日志实证 | 结论 |
| --- | --- |
| 只有 1 条 `菜单尚未就绪…`，紧接着 `推送成功`（另有 1 次等待） | 「菜单未就绪」短退避分流生效，没有「推送失败（完整诊断）」 |
| `收集耗时=0 ms`、失败路径 0 ms、**没有**「主线程停顿」 | 第 12 轮的 `SafeRead` 改动确认有效 |
| `guide=1\|2806741\|已设为引导:全数到期` → `引导请求：全数到期…→ 引用 0x002AD366` → `引导结果已回写界面：seq=1 … 应答=same` | 选中/下发/回写三段都通；取消（seq=2）、再设（seq=3）也正常 |
| `keep=198` / `raw=201` / `qdata[9]=[…]` | AS3 侧过滤掉的 3 条正是玩家任务日志里已有的 |

也正因为日志只说了这些，暴露了**三个「日志能证明的空白」**（全部已在本轮修掉）：

| # | 缺口（日志证据） | 后果 | 本轮修法 |
| --- | --- | --- | --- |
| 1 | 09:18:39 下发成功 → 09:18:46 关菜单，**没有任何一行**说明脚本处理结果（`脚本状态` 只在开菜单时读） | 玩家点完引导抬头看 HUD 有没有蓝点的那几秒，日志证明不了任何事；失败（状态 2/4）也没人重试 | DLL `PollGuideVerify`：下发后 900ms 起轮询状态，`1=已生效` 记 INFO，`2/3` 自动重发（≤2 次），`4` 明确报「别名不存在」，一直 0 则超时 WARN |
| 2 | 引导状态存在 GLOB 里（**跟着存档走**），而 DLL 内存每次启动/读档都是空的 ⇒ 游戏里还在引导 A，日志却打 `引导状态：无` | ① 重开游戏后 UI 竖条不亮；② 点「正在引导的那条」第一次变成重设而不是取消；③ `AutoClearGuideIfAccepted` / `ReissueGuideIfScriptLost` 两条自愈路径都因 `questFormID==0` 直接返回 ⇒ 玩家没有任何办法取消旧引导 | DLL `AdoptExistingGuide`：开菜单时若「自己认为没在引导」而通道里有目标，就按目标引用反查静态表**认领**那条任务（日志 `认领已有引导：…`） |
| 3 | `docs/99` 第十一节的遗留：玩家**接到**任务后引导不自动消失（只在「已完成」时自动取消） | HUD 一直指着已经无用的地点，玩家任务日志里一直躺着一条「可接任务」的代理任务 | AS3 `SaqAutoCancelIfAccepted`：`SaqRefresh` 里发现被引导的 uID 出现在玩家任务日志中 ⇒ 序号 +1、目标清 0（与玩家手动取消同一条路，DLL 侧无需新协议） |

顺手做的日志卫生：删掉每次开菜单都刷的 `DNAM 位分布…`（它要回答的问题在 xEdit 的
flag 名里已有答案，且那一位不是可用的「进度没到」判据）。保留的小瑕疵：
`引用 0x002AD366（）` 里括号为空 —— 那条任务的引导目标离线没算出「所在地」，
只影响日志可读性，不影响功能。

---


## 一·补二、第 17 轮：官方 DLC 支持（破碎空间 + 地球舰队）

需求原文：**「支持 DLC」**（AGENTS.md），本轮落地范围为官方内容 ——
`ShatteredSpace.esm`（破碎空间）+ `SFBGS050.esm`（地球舰队）+ 它要求的前置
`SFBGS00D.esm`（自由航道更新）。第三方 MOD / 其它 Creation 不在范围内。

| 改动 | 说明 |
| --- | --- |
| 表结构 | 每行由「FormID」改成 **`master`（插件名）+ `localFormID`（记录号）**；`guideRef` 同样带 `guideRefMaster` |
| 新模块 | `plugin/src/SAQ_Masters.*`：按当前加载顺序把 `TESDataHandler.files` 里的 `fileIndex` 解析成前缀，`MakeFormID()` 拼运行期 FormID，**并做抽样自校验**（每个 master 挑 6 条问引擎，日志打 `样本 6/6`） |
| 数据源 | `tools/esm/fetch_sources.py`（新）：每个 master 的 **strings 表 + QUST 导出**一起准备（缺什么补什么） |
| 字符串 | ★ DLC 的任务名在**它自己那份** strings 里（`shatteredspace_*.strings` 在 `ShatteredSpace - Main02.ba2`，`sfbgs050_*.strings` 在 `SFBGS050 - Main.ba2`）；漏了会让 DLC 条目被误判成「无本地化名」全部消失 |
| 引导目标 | `gen_guide_targets.py` 重写：**直接解析 ESM 的别名子记录**（`ALST/ALID/ALUA/ALFR/ALFL`），不再依赖 xEdit 导出；每条目标带 `refrMaster`（DLC 任务引用基础游戏 NPC 的情况已处理） |
| 过滤补两条 | `_test`/「测试」→ 测试内容（`SFBGS00D_CruiseMode_TestSupport`）；`*_companions` → 同伴系统（`SFTER_Companions`） |
| 条数 | **202 → 261 条**（+破碎空间 22 / 地球舰队 22 / 自由航道 15），引导目标 209 条（80%） |
| 不装 DLC | 整批跳过（日志 `未加载（或档位不支持，跳过其任务）`），引导请求按「没有引导目标」拒绝；内嵌回退数据只含基础游戏 202 条 |

细节（FormID 位宽、字符串位置、抽样自校验、如何再加 master）见 **`docs/06-官方DLC支持.md`**。

---

## 一·补三、第 18 轮：实测日志复查 —— 「DLC 支持」在实机上完全失效的根因（已修）

对象：2026-09-20 09:38~09:41 会话的 `SFSE\Logs\SAQ_ShowAvailableQuests.log`。
**结论：部署、UI、降级链都没问题，但第 17 轮的「多 master」整条机制在实机上没工作。**

### 证据链（日志原文）

```
[09:40:49.489] 数据源：Starfield.esm 未加载（跳过其任务）；SFBGS00D.esm 未加载…；SFBGS050.esm 未加载…；ShatteredSpace.esm 未加载…
[09:40:49.489] 运行时状态：引擎存在=0 虚表识别=0 … 隐藏=0 跳过(master未加载)=0
[09:40:49.492] 菜单打开：静态表=261 引擎里存在=0 待推送=0
[09:40:49.492] ESM 通道：前缀=0x0F 目标=2806630(0x2AD366) 状态=1 通知=7778
[W] ESM 通道里有引导目标 0x002AD366，但静态表里没有哪条任务的引导目标是它（另一个存档留下的？）
```

* 游戏 Data 目录里四个 master **都在**；MO2 plugins.txt 正常；ESM 通道前缀=0x0F
  证明至少 16 个插件已加载 ⇒ 不是游戏没加载，是 **DLL 读不到插件列表**。
* 后果：261 条全被当成「master 未加载」→ 待推送=0 → **C++ 通路静默停摆**：
  玩家看到的是**内嵌回退数据**（基础游戏 202 条，无 DLC、无运行时过滤、引导不可用）；
  「认领已有引导」也失败（那条 WARN 其实是上一会话「全数到期」的引导目标 0x2AD366 没被认出来）。
* 本次会话 UI 侧完全正常（`src=embedded raw=202 keep=199`、切 tab 后 `list=199`），
  部署也确认是最新产物（09:35~09:36 部署 → 09:38 启动；verify 全过）——
  **问题只在 DLL 的数据源解析这一环。**

### 根因

`SAQ_Masters::FindLoadedFile` 读 `TESDataHandler::files`（commonlibsf 头文件标 0x14F0）。
同一个结构体的 `formArrays` **第 5 轮就实测读到空表**（docs/02 第二节），第 17 轮又用了
第二个成员 ⇒ 同款失败。**commonlibsf 的结构体偏移在本游戏版本上不可信**（风险表第四例）。

### 本轮修法

| 改动 | 说明 |
| --- | --- |
| **前缀探测**（`SAQ_Masters.cpp` 重写） | 不再读 TESDataHandler —— 拿表里该 master 的记录号当样本，扫 `prefix<<24 \| local` 问 `LookupByID` + 虚表核验；全量命中率 ≥90% 认定（日志 `记录命中 22/22`）；Starfield.esm 必须解析出 0x00 的自检锚点（不符则 WARN 且不缓存） |
| **会话级缓存** | 一次游戏运行内加载顺序不变 ⇒ 基础游戏解析成功后不再重扫（零成本） |
| stats 清零 bug | `CollectAvailableQuests` 开头的 `a_stats = {}` 会把 `skippedMaster` 清零（日志出现「引擎存在=0 跳过=0」自相矛盾行）→ 改为保留上游统计 |
| 空推送日志 | 待推送=0 时原来是**静默 return**（链路停摆但日志无痕）→ 补一行 INFO 说明原因（每轮菜单一次） |

### 验证（已做）

* DLL 重编 + 部署（`build-saq.ps1 -SkipTable -SkipSwf -SkipPapyrus`，09:49 完成）；
* `verify_saq_build.py` 全部通过（含第 18 轮新特征：前缀探测日志 / 记录命中率 /
  空推送说明 + 旧文案反向检查）。

细节（探测算法、日志形态、成本、light 分支）见 **`docs/06-官方DLC支持.md` 3.4 节**；
下次进游戏的判据见本文第十一节第 1 条。

---

## 一·补四、第 19 轮：第 18 轮实测复查（全通过）+ 补三处日志盲区

对象：2026-09-20 09:53~09:56 会话的 `SFSE\Logs\SAQ_ShowAvailableQuests.log`。

### 1. 第 18 轮验证结果：**核心机制全部通过**

| 判据 | 日志证据 | 结论 |
| --- | --- | --- |
| 前缀探测 | `Starfield.esm 序号=0x00 记录命中 202/202；SFBGS00D 0x02 15/15；SFBGS050 0x03 22/22；ShatteredSpace 0x01 22/22` | ✅ 4 master 全解析，命中 100%，无 `前缀探测：` WARN |
| 数据源恢复 | `引擎存在=261 虚表识别=261 跳过(master未加载)=0` | ✅ stats 清零 bug 修复生效 |
| 推送链路 | `待推送=260` → `推送成功 260 条/11063 字节`（1 次「菜单未就绪」等待，~166ms） | ✅ 不再走内嵌回退 |
| 认领已有引导 | `认领已有引导：全数到期（0x002AD3D5）目标引用=0x002AD366｜脚本状态=1` | ✅ 第 17 轮遗留闭环 |
| 界面同步 | `引导状态已同步界面：当前=0x002AD3D5 应答=ok`；切 tab 后 `list=257 ourTabMax=257` | ✅ 257 = 260 − 3（AS3 过滤玩家已有） |
| 性能 | `收集耗时=0 ms`、无「主线程停顿」行 | ✅ 第 12 轮修复保持 |

### 2. 复查发现的三处盲区（全部已在本轮修掉）

| # | 盲区（日志证据） | 后果 | 本轮修法 |
| --- | --- | --- | --- |
| 1 | **脚本活性无法证明**：本次会话 Papyrus 日志（09:54~09:56，749 行活跃）**没有一条 SAQ_Main 痕迹**（历史会话都有 `OnInit`）；DLL 读到的 `通知=7778` 与上次会话完全相同 | 无法区分「脚本实例从存档恢复后僵死（收不到菜单事件/定时器）」和「DLL 读通道比脚本早」；若真僵死，玩家点引导 = 写入成功但无人处理（界面说谎） | ① `SAQ_Main.psc` 开菜单打 `Debug.Trace("[SAQ] 菜单打开 通知=N")`；② DLL `CheckScriptLiveness`：开菜单 1.5 秒后复读 `SAQ_Notify`，没 +1 ⇒ WARN 留证 |
| 2 | **「引导未生效」没有收尾**：`PollGuideVerify` 原超时/状态 4 只打 WARN，不碰通道与界面 | 通道里的目标一直留着 ⇒ 下次开菜单又被认领回来（无限重复），界面持续显示「正在引导」 | `AbortUnverifiedGuide`：清通道 + 清本侧状态 +（菜单开着时）结果码 **4** 回写界面（OFF 音 + 竖条回滚 + 文案「引导未生效:脚本未响应」） |
| 3 | **keep 缺口不可证 + 报告被截断**：`raw=260 keep=257` 少 3 条，日志无从确认是哪 3 条；`qdata[...]` 尾部被 512 字节缓冲截断（还切出乱码 `0:�`） | 排查「某条任务为什么没显示」时缺少名单证据 | AS3 `FilterKnownQuests` 记录被过滤 uID ⇒ `SAQ_Report` 输出 `drop[N]=[...]`；`CallAs3NoArg` 缓冲 512→2048、两处 `EscapeForLog` 400→900 |

顺手降噪：首次 UI 桥解析遇到「UI 里没找到 BSMissionMenu 条目」也降为 INFO（推送侧早有同样分流的第 16 轮设计，桥解析这行漏了）。

### 3. 验证（已做）

* 全量构建（SWF / DLL / PEX / ESM 幂等 / 部署）成功：SWF 724565 B / 724855 B，DLL 672256 B；
* `python tools\ui\verify_saq_build.py` 全部通过（含第 19 轮新特征：脚本活性探测 / 脚本未响应提示 /
  引导未生效收尾 / drop= 名单 / PEX 菜单 Trace + 2 条反向检查）。

### 4. 待实测判据（下次进游戏）

见本文第十一节第 1、2 条（第 19 轮判据与「带操作」的交互清单）。

---

## 一·补五、第 20 轮：测试过滤开关（ini 文件 / 控制台 GLOB）

需求原文（玩家反馈）：**「任务太多有点难找」，希望测试期能只显示「适合用来测试的条目」。**

做法（为什么不是「注册一个控制台命令」）：SFSE 的接口只有 Messaging / Trampoline /
Menu / Task（`SFSE/Interfaces.h`），**没有** SKSE 那种 `RegisterCommand`。所以用
「ESM 里的一条 GLOB + 游戏自带控制台 `set` 命令」：

| 改动 | 说明 |
| --- | --- |
| ESM（`patch_saq_esm.py`） | 新增 GLOB `SAQ_TestMode`（记录号 0x804，初值 0）；已有则**保留当前值**；顺带把 TES4 HEDR 的 numRecords +1、nextObjectID → 0x805 |
| DLL（`SAQ.cpp`） | `PassesTestFilter()` + `CollectAvailableQuests(..., testMode)`：每次开菜单读一次 GLOB，按模式过滤（与既有过滤是「与」的关系）；新增 `测试模式：N（…）` 与 `运行时状态：… 测试过滤=N …` 两处日志 |
| 通道（`SAQ_Guide.*`） | `Channel.testMode`（默认 -1 = ESM 旧版没有这条 GLOB ⇒ 不过滤；通道摘要里打 `测试=无(ESM 旧版)`） |
| 验证工具 | `verify_saq_build.py` 新增 3 条 DLL 特征 + 2 条 ESM 特征（工作区 + MO2 部署副本） |

模式与表内条数（261 全量基准）：
`0=关（默认）/ 1=有引导目标(209) / 2=无引导目标(52) / 3=DLC(59) / 4=有目标+有具名地点(85)`；
控制台写法 `set SAQ_TestMode to 1`（`to` 可省）。完整说明见 **`docs/05` 第八节**。

**实机反馈（同日 10:13 会话）**：

* ✅ 新 ESM 生效：`ESM 通道：… 测试=0`（GLOB `SAQ_TestMode` 已在游戏里）、统计行有 `测试过滤=0`；
* ❌ 控制台 `set SAQ_TestMode to 4` → `Unknown variable 'SAQ_TestMode'.` ⇒
  这台机器的控制台**不按 EDID 解析 mod 的 GLOB**（ESM 明明加载正常）。
* ⇒ **补 ini 兜底**：`Documents\My Games\Starfield\SAQ_ShowAvailableQuests.ini` 的
  `[Test] Mode=N`（改完关开菜单即生效，不用重启游戏；DLL 首次运行自动生成模板）。
  优先级：**GLOB 非 0 > ini > 0**；日志 `测试模式：N（…）[来源=ini/控制台]`。

### 验证（已做）

* ESM：patch 连跑两次产物**字节级一致**；模拟「值已设成 1.0」后重跑，值被保留；
  HEDR `numRecords=7 nextObjectID=0x805`；
* 全量构建 + 部署（ESM 部署副本已含新 GLOB；SWF/PEX 同步重编）；
* `verify_saq_build.py` 全部通过（含第 20 轮 7 条新特征：测试模式日志/说明/统计 +
  ini 兜底 + 来源 + 2 条 ESM GLOB）。

---

## 一·补六、第 21 轮：修「DLC 目标引导后全链路卡死」（自检上限 bug）+ 目标 FormID 拆分

### 玩家反馈（第 20 轮部署后的实测，10:23~10:25 会话）

> 「点亮一个可接任务后，再点击其他可接任务条目，不会自动切换到新的，表现为点亮瞬间
> 就自己取消了；而且即使把之前点亮的条目取消，其他的也都点不亮了。」

### 日志证据：事故从「第一个 DLC 目标」开始

```
[10:23:57.501] 引导请求：星海萨普之旅（0x0021710B）→ 引用 0x0021C69A（新家园）    ← 成功
[10:23:59.906] 引导请求：取消｜…                                                ← 成功
[10:24:04.281] 引导请求：阿基拉城任务（0x002A4401）→ 引用 0x0012253A             ← 成功
[10:24:11.501] 引导请求：同情生者（0x010D5E84）→ 引用 0x0107BDB2（瓦鲁贮藏所）   ← 成功（首个 DLC 目标）
[10:24:11.940] [W] 引导请求：取消失败｜拒绝写入：SAQ_GuideTargetRef 读出来是 17284530（不像 FormID）
[10:24:17.626] [W] 引导请求：阿基拉城任务（0x002A4401）写 ESM 通道失败｜（同上）    ← 之后全部失败
…（结果码 2 一直刷到 10:25:46 关菜单）…
```

`0x0107BDB2` = 17,284,530 —— **第一个超过 0xFFFFFF 的引导目标**（DLC 的引用），事故起点。

### 根因（都在 `SAQ_Guide.cpp::SetGuideTarget`）

| # | 错 | 后果 |
| --- | --- | --- |
| 1 | **自检上限写成 `16777215`（0xFFFFFF）** —— 那是「低 24 位」的上限，而 target 里存的是**完整 FormID** | 通道里一旦存过 DLC 目标（> 0xFFFFFF），**之后每次写入都被这个自检拒绝**（结果码 2）⇒「点亮瞬间取消 / 全都点不亮」 |
| 2 | GLOB 是 float（尾数 24 位），完整 FormID > 2^24 后**奇数不可精确表示**（±1） | 轻则 `GetForm` 取不到（状态 2），重则**指向相邻的另一条记录 = 错误的引导目标**（DLC 目标约一半是奇数） |

### 修法

| 改动 | 说明 |
| --- | --- |
| 目标 FormID 拆两个 GLOB | `SAQ_GuideTargetRef`（0x801）改存**低 24 位**，新增 `SAQ_GuidePrefix`（0x805）存**高 8 位** —— 两个分量都在 float 精确区（脚本 `GuidePrefix != None` 自动兼容旧通道） |
| VMAD | `build_saq.pas` 追加 `GuidePrefix` 属性绑定（VMAD 96 → 119 B），**重跑了一次 `-RebuildEsm`**；FormID 0x800~0x805 不变（追加在末尾） |
| 自检修正 | 有拆分 GLOB 时按分量检查（低 24 位 ≤ 0xFFFFFF / 高位 ≤ 0xFF）；旧 ESM 用完整 FormID 上限 0xFEFFFFFF |
| 遗留值兼容 | 修复前存档里残留的完整 FormID（prefix 还是初值 0）由 `CombineTargetFormID` 按旧语义识别，并在下一次写入时被新语义覆盖（不会卡死） |
| 日志 | 通道摘要 `目标=0x{:08X}`（拼好的完整值）；写入详情 `写入 SAQ_GuideTargetRef=0x… + SAQ_GuidePrefix=…（完整 0x…）` |

### 验证（已做）

* ESM：xEdit 重建 + patch 幂等；GLOB = 0x801..0x805、QUST formid=02000800（未变）；
  VMAD 解析确认 4 个属性绑定（`GuidePrefix → 0x02000805`）；
* 全量构建 + 部署；`verify_saq_build.py` 全部通过（含第 21 轮 5 条新特征）。

### 实测结果（2026-09-20 10:39~10:46 会话，**已通过**）

* `测试模式：3（只显示 DLC 条目）[来源=ini]` —— ini 通路生效（第 20 轮补丁）；
* 连续 50 次切换条目（含破碎空间 / 自由航道 / 地球舰队三种 DLC 的引用）全部成功：
  `写入 SAQ_GuideTargetRef=0x0C98E2 + SAQ_GuidePrefix=1（完整 0x010C98E2）`、
  `引导已生效：绝非虚言（0x02033A2A）脚本状态=1`；
* **没有任何**「拒绝写入」行 —— DLC 目标 > 0xFFFFFF 的路线已根治；
* 本次会话唯一「奇怪现象」（部分条目选中即回滚）是无引导目标的正常回滚 ⇒ 见一·补八。

---

## 一·补七、第 22 轮：日志与配置改放 mod 目录（不落 C 盘用户目录）

需求原文（玩家新增进 AGENTS.md）：

> 「日志、配置文件要放在 mod 目录里，不要放在 C 盘用户目录下，否则删除 mod 的时候
> 会导致残留文件产生，不合理」

| 改动 | 说明 |
| --- | --- |
| 新增 `SAQ::PluginDir()`（`SAQ.cpp`） | `GetModuleHandleExW(FROM_ADDRESS)` + `GetModuleFileNameW` 反查本 DLL 目录（MO2 下 = mod 目录） |
| 日志（`main.cpp`） | 限长 sink 改写 `<插件目录>\SAQ_ShowAvailableQuests.log`；建不出时回退 SFSE 默认目录并 WARN；启动时打一行 `日志文件：插件目录（mod 目录）内的 …` |
| ini（`SAQ.cpp`） | `TestModeIniPath()` 改为 `<插件目录>\SAQ_ShowAvailableQuests.ini` |
| 旧文件清理 | 已删除 `Documents\My Games\Starfield\SAQ_ShowAvailableQuests.ini` 与 `SFSE\Logs\SAQ_ShowAvailableQuests.log`（都是本 mod 生成的）；部署时在 mod 目录预放了一份 ini 模板 |

注意：`Documents\My Games\Starfield\SFSE\Logs\sfse*.txt` 与 `Logs\Script\Papyrus*.log`
是 **SFSE 框架 / 游戏本身**写的，不属于本 mod，无法也不应移动（Papyrus 日志仍是排查脚本
问题的重要来源）。

验证（已做）：全量构建 + 部署（mod 目录内 DLL 681984 B + ini 680 B）；
`verify_saq_build.py` 全部通过（含第 22 轮 3 条新特征）。

**实测确认（10:39 会话）**：MO2 的 VFS 把「插件新建的文件」写到了
`starfield_mods\overwrite\SFSE\Plugins\` ——「不落 C 盘」已达成，但删 mod 时 overwrite 里
仍会留下日志。**补一手**：`build-saq.ps1` 部署时在 mod 目录里**预置一个空日志文件**
（usvfs 对「已存在于 mod 的文件」的写入会重定向回 mod 目录）；本次已把 overwrite 里的
日志移进 mod 目录并清掉旧位置。

---

## 一·补八、第 23 轮：把「能不能导航」变成看得见的信息（实测反馈：有些任务「选中即被取消」）

玩家反馈（第 21 轮修复后的实测）：

> 「有些任务选不中，选中后会瞬间被取消，比如『地球舰队侵袭』。」

日志证据 —— **不是 bug，是「无引导目标」的正常回滚**，但反馈太弱：

```
[W] 引导请求：地球舰队侵袭（0x030059A0）没有引导目标（离线没算出「去哪里接」的引用）
[I] 引导结果已回写界面：seq=43 实际=0x03030EB0 结果码=1     ← 界面回滚（OFF 音 + 竖条灭）
```

261 条里 **52 条**没有引导目标，列表照常显示是设计行为；但此前唯一的反馈是「一声 OFF 音 +
竖条不亮」，玩家看不出原因（实测里玩家反复点了「地球舰队侵袭」×12、「困于虚空」×4）。
根治方向（补引导目标：CELL.XLCN 地点兜底）见第十一节第 3 条。

### 修法：让「能不能导航」在界面里可见

| 层 | 改动 |
| --- | --- |
| 载荷 | `Q\t<FormID>\t<type>\t<中文名>\t<英文名>` **+ `\t<有无引导目标>`**（第 5 列 1/0；旧 AS3 忽略多余列，向后兼容） |
| C++ | `QuestEntry.hasGuideTarget`（静态表 `guideRefLocal != 0`）；`CollectAvailableQuests` 填充；`BuildPayloadUtf8` 输出 |
| AS3 | 解析第 5 列 → 条目带 `bSaqHasTarget`；**选中无目标条目**：描述写明「还没有导航目标」、SET COURSE 按钮置灰、点击**不发请求**（不再有「亮起→瞬间回滚」的闪烁，只给 OFF 音 + 日志） |
| 内嵌回退数据 | `gen_quest_table.py::build_payload` 同步加列（内嵌载荷 6.7K → 7.1K 字符） |

### 验证（已做）

* 单独重跑 `gen_quest_table.py`（261 条 / 209 有目标；内嵌 202 条 / 7147 字符）；
* 全量构建 + 部署（SWF 724870 B / 725153 B，DLL 更新）；
* `verify_saq_build.py` 全部通过（含第 23 轮 2 条新特征）。

### 下次实测（顺便看）

* 选中「地球舰队侵袭」这类条目 → 右侧描述应写明「还没有导航目标」，SET COURSE 置灰；
  点击不再有「亮起→回滚」（只一声 OFF 音）；
* 有目标的条目照旧：点击 → 蓝点 + `引导已生效`。

---

## 一·补九、第 24 轮：无引导目标的 52 条 —— 归因分析（结论：不补「猜的导航」）

玩家要求：**先分析清楚「为什么有些任务无法导航」，再决定要不要补** —— 不要为了导航而
硬凑（以免指向错误地点、误解游戏本来的接取方式）。

新增分析工具 `tools/esm/analyze_no_targets.py`（两步：QUST 层面 → 世界数据层面，
`--world`），完整报告见 **`docs/07-无引导目标任务分析.md`**。结论摘要：

| 归因（有重叠） | 条数 | 形态 |
| --- | --- | --- |
| ALUA = Player | 21 | 事件/遭遇类（太空信号、派对舰、求救信标……）：玩家「遇到」才触发，无发布者 NPC |
| 完全没有 ALUA/ALFR/ALFL | 19 | 任务板 / 场景派发类：入口是「任务板/对话」这个交互，不是坐标 |
| 地点没有地图标记（MNAM） | 12 | 扫描/发现类（鸭嘴猪 ×5 等）：连游戏自己都没给这个地点标记 |
| 落在别名暂存格 | 4 | 保卫先锋队/摄影师/独立盟之巅/生命之器：脚本用对象，硬指 = 指向空气 |
| NPC 有放置引用但无名字 | 1 | 「送货卡车」的 4 个跨太空港飞船技师 —— 可救但目标不唯一 |

**结论：这 52 条在游戏数据层面就没有「从哪里接」的定义**（不是没算出来，是不存在）——
补「猜的目标」会让玩家跑冤枉路，比「不能导航」更糟。已采用的正确定位是**如实说明**
（第 23 轮：描述写明 + 按钮置灰 + 不闪烁）。将来若提高覆盖率，正路是在引擎里确认
「事件触发点是否可推导」（研究型工作，本阶段不做）。

---

## 一·补十、第 25 轮：打包 Nexus 上传包（发布交付）

AGENTS.md 最后一条需求（「打包成 nexus mods 能接受的上传包文件 + 介绍文案」）落地。
**本轮没有改任何游戏逻辑**，纯粹是把已实测通过的功能交付化：

| 交付物 | 说明 |
| --- | --- |
| `tools/package-saq.ps1` | 打包脚本：产物检查 → 跑 `verify_saq_build.py`（失败即终止）→ 组装 staging → 压缩 + 输出 SHA256 |
| `dist\SAQ-ShowAvailableQuests-0.1.0.zip` | 上传包（1,752,449 B）：zip 根 = mod 根，MO2 可直接「从压缩包安装」 |
| `resources\README-mod.txt` | 包内 README（中英双语；`{{VERSION}}` 由脚本替换） |
| `resources\nexus-description.md` | Nexus 介绍文案（中英 Summary / Description + 发布检查清单） |
| `resources\SAQ_ShowAvailableQuests.ini` | 测试开关 ini 模板**入库**（此前只在 MO2 目录手工存在）；`build-saq.ps1` 部署时「缺则补、已存在不覆盖」 |

包内清单（7 项）：`README.txt` + `SAQ_ShowAvailableQuests.esm` + `SFSE\Plugins\`（DLL + ini）+
`Interface\`（missionmenu.swf + missionmenu_lrg.swf）+ `Scripts\SAQ_Main.pex`。
打包时 70 项产物特征校验全部通过；版本号三处一致性检查（xmake.lua / main.cpp / 脚本参数）无告警。

> 发布前的最后一道门：先跑第十一节的验收清单（第 23 轮「无目标提示」+ 交互判据 + 第 22 轮
> 日志落点），验收通过后即可直接上传 —— 包随时可用 `& ".\tools\package-saq.ps1"` 重打。

---

## 一·补十一、第 26 轮：日志复查 —— 「引导未生效」的三次假失败（确认窗口改绑菜单状态）

对象：2026-09-20 10:38~10:58 三个会话的 DLL 日志 + **Papyrus 日志交叉**。

### 1. 复查结论：两轮修复已在日志里确认

| 判据 | 证据 | 结论 |
| --- | --- | --- |
| 第 21 轮 DLC 目标修复 | 全程 0 次「拒绝写入」；`SAQ_GuideTargetRef=0x… + SAQ_GuidePrefix=1/2/3` 正常 | ✅ |
| **第 23 轮无目标提示**（10:50 会话首次实测） | 界面出现 `该任务暂无导航目标:地球舰队侵袭/玩家房屋任务/MQIntro星际遭遇战/生命之器/失踪的爱人`，且 **DLL 不再收到对应请求**（会话 2 的旧 SWF 无提示、DLL 收到一堆 `没有引导目标` WARN） | ✅ 实测通过 |
| drop 名单 / 认领 / 前缀探测 / 测试模式 3 / 脚本活性探测 | `drop[3]=[270718,2c5401,2c9c97]`；10:39、10:50 两次 `认领已有引导`；4 master 命中 100%；`测试模式：3 [来源=ini]`；无活性告警 | ✅ |

### 2. ★ 新发现：引导只在「菜单关闭」时生效 —— 菜单里超过 ~11 秒 = 假失败 + 真失效

三次 `引导未生效：… 脚本状态一直是 0`：星海派对（10:42:21）、回收行动（10:43:41）、
失踪的地球人（10:50:40）——每一次都伴随界面回滚 `引导未生效:脚本未响应`。

证据链（DLL 日志 × Papyrus 日志）：

```
Papyrus.1.log   10:41:56 [SAQ] 引导已应用：<SFFL_ChanHuangRef (02025B99)>
DLL 日志        10:41:56.097 菜单关闭
⇒ 脚本「应用引导」的时刻 = 菜单关闭时刻（一一对应）
```

* `Papyrus.0~3.log` 里三个会话**都没有 `OnInit`**，`引导已应用` 只出现在菜单关闭那一刻；
  没有任何一条「菜单开着时」的引导应用。
* ⇒ 脚本实例从存档恢复时 `OnInit` 不跑 ⇒ `StartTimer` 从未注册 ⇒ **菜单开着时脚本不轮询**，
  引导只在 `OnMenuOpenCloseEvent(关闭)` 的 `ApplyGuide()` 里被应用一次。
* 旧逻辑下发后 ≈11 秒（900ms + 5×2s）确认超时 ⇒ 玩家在菜单里挑条目久一点，
  请求就被 DLL 当失败清掉 —— **假失败（脚本其实正常）+ 真失效（玩家最终什么也没有）**。
* 唯一一次 `引导已生效`（绝非虚言）是因为玩家 **1.6 秒内**就关了菜单。

### 3. 修法（两侧）

| 侧 | 改动 |
| --- | --- |
| DLL `PollGuideVerify` | **菜单还开着时不判失败**：状态 0 ⇒ 继续等（不计数、不 Abort，每请求打一行 `引导确认：脚本状态还是 0，但菜单还开着 …`）；状态 1/4 与重发（2/3）逻辑不变，重发改独立计数 `verifyResends`（≤2） |
| DLL `OnMissionMenuClosed` | 菜单关闭 = 脚本真正拿到请求的时刻 ⇒ 若有未确认的下发，则重置确认窗口（900ms 首验 + 6×2s）并打 `引导确认：菜单已关 …`；超时判定只发生在菜单关着时 |
| 脚本 `SAQ_Main.psc` | 菜单打开事件里 `StartTimer` 重挂定时器（补齐实例恢复后缺失的轮询）+ `ApplyGuide()` 补一次（处理「菜单关着时 DLL 重发的请求」）。若引擎允许，引导在菜单开着时 0.5 秒内即应用 |

### 4. 验证（已做）

* 全量构建（`-SkipTable -SkipSwf`：DLL + PEX + ESM 幂等 + 部署）成功；
* `verify_saq_build.py` 全部通过（含第 26 轮 2 条新特征：`菜单开着不判失败` /
  `关菜单重置确认窗口`）。

### 5. 下次进游戏（判据）

* 菜单里点一条有目标的条目、**不关菜单**：应出现 `引导确认：脚本状态还是 0，但菜单还开着 …`，
  **不应**再出现 `引导未生效`；界面不再有「亮起 → 11 秒后回滚」的假失败；
* 关菜单：应出现 `引导确认：菜单已关 …` → ≤11 秒内 `引导已生效：… 脚本状态=1`；
* 若脚本定时器重挂生效：菜单开着时就应看到 `引导已生效`（1~2 秒内），
  Papyrus 里 `[SAQ] 引导已应用` 不再只挨着「菜单关闭」；
* 顺带：菜单里连点/切换多条 ⇒ 只有最后一条生效（通道只有一个目标，语义正确）。

> ★ 第 27 轮复查（11:12~11:14 会话）：以上判据两条通过（菜单开着不判失败 / 关菜单 0.9 秒
> 生效）；「定时器重挂让菜单开着时也生效」**被证伪** —— 引擎语义不允许，详见下面一·补十二。

---

## 一·补十二、第 27 轮：日志复查收尾（A1）+ 无限任务入口「任务板」（B1）

需求原文（AGENTS.md）：**「无限生成任务不显示，但接取无限生成任务的目标可以作为一条
数据显示在可接任务列表里（如任务板、提供无限任务的 NPC）」** —— 本轮把「任务板入口」落地。

### 1. 日志复查（A1）：第 26 轮修复已实测通过 + 定时器语义定案

对象：2026-09-20 11:12~11:14 会话的 DLL 日志（新版本首次实测）+ Papyrus 交叉。

| 判据 | 证据 | 结论 |
| --- | --- | --- |
| 第 26 轮「菜单开着不判失败」 | 11:13:58 起在菜单里连点 12 条、停留 27 秒，**零「引导未生效」**；关菜单（11:14:25.201）后 0.9 秒 `引导已生效：博士的命令 脚本状态=1` | ✅ 假失败根治 |
| 第 21/23/18/19 轮回归 | 无「拒绝写入」；「地球舰队侵袭」只有界面提示、DLL 无请求；4 master 100%；无活性 WARN | ✅ |

★ **定案：Papyrus 定时器在菜单打开时不走**（三次会话一致）：
`Papyrus 10:41:56 引导已应用` ↔ `DLL 10:41:56.097 菜单关闭`（一一对应）；
11:12 会话菜单开着 37 秒（11:13:48~11:14:25）**零应用**。
⇒ 第 26 轮设想的「菜单开着时 1~2 秒生效」**不可能达成**（菜单暂停游戏 ⇒ StartTimer 不走），
引导只在「菜单关闭」时刻应用 —— 与原版交互一致（HUD 蓝点本来就要关菜单才可见）。
本轮收尾（降噪）：

* DLL `PollGuideVerify`：菜单开着时**不再轮询**（连 GLOB 都不读），「等待中」说明改成
  **每菜单一行**（原实现每请求一行 —— 实测 27 秒 9 行，纯噪声）；
* `主线程停顿` 行加「有事在等」条件（推送重试 / 引导确认未完成才统计）—— 原实现把玩家
  在菜单里浏览（7.7s / 8.6s / 14.8s）和挂机切出（432s）全记成「停顿」，没有一行指向真卡顿；
* `SAQ_Main.psc` 注释更正（重挂定时器的实际作用 = 保证菜单**关着**时轮询可用，
  例如 DLL 在菜单关着时重发的请求）。

### 2. 无限任务入口（任务板）—— B1

数据：任务板 = ACTIVATOR `MissionBoardConsole*`（`esm_probe list ACTI --grep board`
可复现；`explore_boards.py` 列出全部 102 个放置引用）。白名单挑 **12 条**代表性入口
（每个城市/据点一块；完整清单与持久性见 `ref/entry_targets.json`）。

| 层 | 改动 |
| --- | --- |
| 生成 | 新工具 `tools/esm/gen_entry_table.py` → `plugin/src/SAQ_EntryTable.h`（新亚特兰蒂斯/阿基拉城/霓虹城/塞多尼亚/霍普镇/新家园/星座小屋/龙神工业/狄摩斯·三叉戟·斯特劳德星船厂/星钥站）；`build-saq.ps1` 静态表步骤内自动跑（约 1~2 分钟） |
| DLL | 入口表 `kEntryTable`：**uID = 引导目标 = 任务板引用的运行期 FormID**（与任务 FormID 空间不冲突，且「目标就是它自己」）；`CollectAvailableQuests` 末尾追加（默认模式与模式 5；模式 1~4 不加）；引导请求 / 认领 / 全部日志支持入口条目（`DisplayNameOf` 统一取名） |
| 测试开关 | **ini `Mode=5` = 只显示「无限任务入口」条目**（GLOB 上限同步 4→5） |
| AS3 | payload `type=100` → 子项名「前往任务板 / Go to the mission board」+ 专用描述（「这是一块任务板…」） |
| 日志 | `数据源：…；入口条目表=12 条（任务板）`、`运行时状态：… 入口=12 …` |

★ **待实测的关键未知（下次进游戏）**：12 条入口里只有**阿基拉城是常驻引用**，其余 11 条
非常驻 —— 脚本 `Game.GetForm` 对非常驻引用在 cell 未加载时可能取不到（状态 2）。
**但有成功先例**：`02025B99`（黄陈，非常驻）在 10:41:56 被脚本真实应用过；
`002AD366`（兰德里·霍利菲尔德，非常驻）的引导在多会话保持状态 1。
⇒ 判据：点「任务板 · XX」→ `引导已生效 … 脚本状态=1` 为成功；`脚本状态=2` 说明该引用
未加载（届时再上 Plan B：ESM override 给 REFR 加 persistent 标志）。

### 3. 验证（已做）

* 全量构建 + 部署成功（SWF 725184 / 725464 B；DLL 更新；PEX、ESM 幂等）；
* `verify_saq_build.py` 全部通过（含第 27 轮 11 条新特征 + 1 条反向检查）；
* 新工具 `ref_flags_report.py`：209 条引导目标 = `ref` 型 87 条**全常驻** / `actor` 型
  46 常驻、76 非常驻。

---

## 一·补十三、第 28 轮：实测反馈修复 —— 任务板入口「取不到目标」的诚实收尾

### 1. 玩家实测（11:40~11:41 会话，`Mode=5`）

好消息先说：**入口条目本身全链路正常** —— `待推送=12`、列表 12 条「任务板 · XX」、
子项「前往任务板」、点击后 `引导请求：任务板 · 新亚特兰蒂斯（0x0021001E）→ 引用 0x0021001E`。

**但引导没生效**：

```
[11:41:36] 引导重发：任务板 · 新亚特兰蒂斯（0x0021001E）脚本状态=2（引用当时没加载）…
[11:41:40] ESM 通道：… 目标=0x0021001E 状态=2 …
```

玩家看到的是：**蓝点没跳到任务板，还留在上一条任务（存档里之前选的 NPC）上**。

### 2. 根因：12 条入口里 11 条是**非持久引用**

`ref/check_entry_flags.py`（本轮新增的检查脚本）扫出的完整 flags：

| 入口 | REFR | flags | persistent |
| --- | --- | --- | --- |
| 阿基拉城 | 0x0014D497 | 0x400 | ✅ **常驻**（所以它可用） |
| 其余 11 条 | — | 0x0 | ❌ 非常驻（cell 未加载时 `Game.GetForm` 取不到） |

* 没有一条带「初始禁用 / 隐藏」位 ⇒ 数据本身都是正常的可见引用（星座小屋那条的
  base `MissionBoardConsole_Constellation` 有真实模型 `SetDressing\Mission_Board_Terminals\…`，
  VMAD 挂 `TerminalScript` + `MissionBoardActivatorScript`）——**不是硬编码/编造**。
* 玩家问题「陋室里的任务板没看到」：它在 Lodge 内部 cell（`CityNewAtlantisLodgeInt`，
  坐标 8,-60,-4），数据里是启用且可见的；游戏里可用控制台 `player.moveto 00137573`
  验证位置。
* 星钥站（0x00197D22）带 **XESP（启用父级）**——启用状态由剧情控制，留意（备查）。

### 3. 修法：运行时可用性判定（与脚本同源的引擎查询）

**思路**：脚本 `Game.GetForm` 取不到 = DLL `TESForm::LookupByID` 也取不到（同一个引擎函数）。
在 DLL 侧提前判，把「注定失败的引导」变成「如实告知」：

| 层 | 改动 |
| --- | --- |
| DLL 收集（`AppendEntryRows`） | `hasGuideTarget = LookupByID(引用) != nullptr` —— 取不到就按第 23 轮的「无导航目标」显示（按钮置灰 + 不发请求）；统计行改 `入口=12(可导航 N)` |
| DLL 点击（`ApplyGuideRequest`） | 写通道前**复检**一次：取不到 ⇒ 结果码 1 回滚界面，**不写通道**（避免「通道里塞一个脚本注定取不到的目标、旧蓝点还留着」的混乱） |
| AS3 描述 | 入口的「无目标」文案与任务分开：**「你现在离这个位置还很远 —— 暂时无法导航。到它所在的城市/据点附近后，再打开一次任务菜单即可」** |
| AS3 点击提示 | `暂时无法导航（离得太远）: 任务板 · XX`（OFF 音，不发请求） |

效果：玩家在别处看列表 → 远的任务板置灰并说明原因；走到那座城市再开菜单 → 该条变可导航。

### 4. 验证（已做）

* 全量构建 + 部署（SWF 725395 / 725671 B）；
* `verify_saq_build.py` 全部通过（含第 28 轮 3 条新特征）；
* `check_entry_flags.py` 输出与上表一致。

### 5. 遗留（下一步，按价值）

1. **让任务板真正可导航**（治本）：给 11 条非持久 REFR 做 **ESM override 并加 persistent 标志**
   （xEdit 脚本：`copy as override` → 设 persistent → 存进 `SAQ_ShowAvailableQuests.esm`；
   项目已有 xEdit Pasca 脚本先例与 `-RebuildEsm` 流程）。风险：override 世界引用需正确组结构，
   且与其它改这些 cell 的 mod 可能冲突 —— 做成开关/需实测。
2. 若 1 暂不做：现状已是「诚实可用」（近距离可用 + 远距离说明原因）。

---

## 一·补十四、第 29 轮：任务板入口「常驻化」override —— 任何位置都能导航（治本）

### 1. 玩家反馈（11:55 会话）与日志证据

> 「我现在就是亚特兰蒂斯城，但却还是无法导航，而且也不应该存在所谓『太远就无法导航』，这本身就不合理。」

日志实证（同一会话内两个对照）：

| 时刻 | 操作 | 结果 |
| --- | --- | --- |
| 11:55:16 起 | 在**赛多尼亚**点「任务板 · 赛多尼亚」(0x001DF853) | UI `该任务暂无导航目标`（未被拦）、DLL 写通道、关菜单后 `引导已生效：任务板 · 赛多尼亚 脚本状态=1` ✅ |
| 同一会话 | 点「任务板 · 新亚特兰蒂斯城」(0x0021001E) | DLL `LookupByID` 取不到 ⇒ 界面置灰 + `该任务暂无导航目标`（第 28 轮的兜底） |

⇒ 玩家「人在亚特兰蒂斯城」但任务板在**井区（CityNewAtlantisWell）**这个**内部 cell** 里，
没走到跟前时 cell 未加载、引用取不到 —— 第 28 轮设计的「就近可用 + 远处如实说明」在体验上不成立
（同一个城市里也可能隔得很远），玩家明确否定「太远就不能导航」这个设定。

### 2. 数据侦查（新工具 `tools/esm/scan_entry_persistent.py`）

全表扫一遍 Starfield.esm（1~2 分钟），把 12 条入口的**组位置**和同 cell 的常驻引用都摸清：

| 结论 | 数据 |
| --- | --- |
| 11 条在 `CellTemporary` 组（**非常驻**） | 新亚特兰蒂斯城 / 霓虹城 / 赛多尼亚 / 霍普镇 / 新家园 / 陋室 / 龙神集团 / 三家星船厂 / 星钥站 |
| 1 条在 `CellPersistent` 组（常驻，flags 0x400） | 阿基拉城（所以只有它「任何位置可用」） |
| 全部是内部 cell | 组链 `Top 'CELL' > InteriorBlock > InteriorSubBlock > CellChildren > …`，world=0 |
| 同 cell 常驻引用 | 每个 cell 里都有 14~309 条常驻引用（最近的 1.8~8.1 m，只能当备选方案 —— 蓝点落点不精确） |

### 3. ★ 写法来自官方先例（不是自创）

`tools/esm/scan_override_persistent.py` 扫本机所有插件，找「override 基础游戏引用 + 常驻组」的样本：

```
SFBGS003.esm    override 引用 248 条，其中 66 条在 CellPersistent 组（flags 0x400）
                样本 REFR 0x00164B37 flags=0x000400 组链=Top > InteriorBlock > InteriorSubBlock > CellChildren > CellPersistent
SFBGS008.esm    4 条同款
```

⇒ 官方 Creation 就是这么把基础游戏引用改成常驻的（组结构 + flags 两条都要对）。**照抄。**

### 4. 实现（新工具 `tools/esm/persist_entry_refs.py`）

| 做什么 | 细节 |
| --- | --- |
| override 记录 | 字节**照抄** Starfield.esm 的 REFR（位置等字段一律不动），只 `flags \|= 0x400` |
| 组结构 | `Top 'CELL' > InteriorBlock > InteriorSubBlock > CellChildren > CellPersistent > REFR`；组头照抄原组（只改 size/label/type），`CellChildren`/`CellPersistent` 组的 label = cell 的 FormID |
| HEDR | numRecords 按「MAST 数 + 记录总数」重算（8 → 19） |
| 幂等 | 先删旧 CELL 组重建；已有正确结构 ⇒ 快速跳过（不读 1.4 GB 的 ESM） |
| 自校验 | 重新解析：11 条记录存在 / `flags & 0x400` / 组链尾部 = `[2,3,6,8]` / 所有组边界不越界 |
| 顺序 | ★ 必须在 `patch_saq_esm.py` **之后**（后者是线性重建、不认识嵌套组；已加防呆：检测到嵌套组即拒绝运行并提示 `--clean`） |
| 其它开关 | `--check` 只打印组树；`--clean` 只移除 CELL 组 |

构建脚本 `build-saq.ps1` 的 ESM 段现在是 **clean → patch → persist**（无论从什么状态开始都能收敛到正确结果）。
只处理 `ref/entry_targets.json` 里 `persistent=false` 的条目 ⇒ 阿基拉城不动；将来换板/加板会自动跟上。

### 5. xEdit 权威验证（新工具 `tools/run-verify-persist.ps1` + `tools/xedit-scripts/verify_persist.pas`）

xEdit（社区标准解析器）的验证输出（`ref/xedit-out/verify_persist.txt`，可复现）：

```
GRUP Interior Cell Block 0
  GRUP Interior Cell Sub-Block 2
    GRUP Cell Children of CityNewAtlantisWell "The Well" [CELL:000F4ED4]
      GRUP Cell Persistent Children of CityNewAtlantisWell "The Well" [CELL:000F4ED4]
        REFR 0021001E flags=[…]
```

* xEdit 把 cell 名字（`"The Well"`）都解析出来了 ⇒ 组结构与 CELL 记录的绑定正确；
* FormID 显示 `0021001E` ⇒ 认成 **Starfield.esm 空间的 override**（不是新记录）；
* 让 xEdit **重新保存**一份：11 条记录**仍在 CellPersistent 组、flags 仍 0x400** ⇒ 结构合法、语义被读懂。

### 6. 配套改动

| 层 | 改动 |
| --- | --- |
| UI 文案 | 「离得太远」退场（玩家明确否定这个设定）→ 中性兜底：「这个位置此刻取不到（多半是所在区域还没加载出来）」/ 点击提示 `暂时无法导航:`；verify 加**反向检查**（旧文案不得再出现） |
| DLL | `AppendEntryRows` 判定逻辑不变（persistent 后 `LookupByID` 恒命中）；新增「入口不可导航」名单日志（override 失效时一眼看出是哪几条） |
| 验证工具 | `verify_saq_build.py` 新增：ESM **数据侧解析**检查（11/11 条 CellPersistent + 0x400，工作区 + MO2 两处）+ 新文案特征 + 反向检查 |

### 7. 验证（已做）

* 全量构建通过（SWF 725389 / 725660 B；DLL 重编并部署；PEX；ESM 2772 B / 19 条记录）；
* `verify_saq_build.py` 全部通过（含第 29 轮新特征）；
* xEdit 验证通过（第 5 节）。

### 8. 待实测判据（下次进游戏）

1. 开菜单 → `运行时状态：… 入口=12(可导航 12) …` —— **任何位置都应是 12**；
   若 < 12，同一行会跟 `入口不可导航: 名字[FormID] …`（override 没生效的证据）；
2. **不走近**、在任意地点点一条「任务板 · XX」→ 关菜单 ≤11 秒
   `引导已生效：任务板 · XX 脚本状态=1`，蓝点出现在那块板上（不再是旧目标）；
3. 界面里**不应再出现**任何「太远」字样；
4. 回归：`drop[N]` 名单 / 无目标条目不发光请求 / 菜单里停 20 秒无 `引导未生效`。

> ★ 以上「第 29 轮的判据」已被第 30 轮取代（override 路线被否定，见下）。

---

## 一·补十五、第 30 轮：入口引导改「新建常驻 XMarker + 候选链」—— 治本修复

### 1. 玩家反馈与实机证据（2026-09-20 12:45 会话，游戏已加载第 29 轮 ESM）

> 「我等了很久，还是提示没加载出来（这个位置此刻取不到）」

```
[12:47:13] 运行时状态：… 入口=12(可导航 2) 入口不可导航: 任务板 · 新亚特兰蒂斯城[0x0021001E]
          任务板 · 霓虹城[0x00148C93] …（共 10 条）
```

部署时间 12:11、游戏进程 12:43 启动 ⇒ **加载的就是含 override 的最新 ESM** ⇒ 第 29 轮的
「override 成常驻」**在引擎层面没有生效**（12 条里只有阿基拉城（原生常驻）+ 玩家当前
所在地赛多尼亚（cell 已加载）可导航）。

### 2. 根因（数据侧钉死）：override 只换数据、**不改变引用的加载分类**

新工具 `tools/esm/check_persist_precedent.py --self` 对照三类记录：

| 记录 | 原记录的组 / flags | 覆盖后 | 结论 |
| --- | --- | --- | --- |
| 官方 SFBGS003（66 条）/ SFBGS008（4 条） | 本来就是 CellPersistent + 0x400 | 同 | **零先例**：官方从没把「非常驻」变常驻 |
| 我们的 11 条 | CellTemporary、flags=0x000000 | CellPersistent + 0x400 | 引擎不认（实测如上） |

⇒ 引用的「常驻 / 临时」分类在**记录第一次被加载时**确定；override 改不了。

### 3. 修法

| 层 | 改动 |
| --- | --- |
| ESM | 新工具 `tools/esm/create_board_markers.py`：**新建** 11 条引用（本插件空间，记录号 0x900~0x90A）= XMarker(0x3B) + 原板 DATA（位置逐字节一致）+ flags 0x400 + 放进对应 cell 的 `CellPersistent` 组（组头模板照抄该 cell 真实常驻引用的组）。每次构建幂等重建（先清 CELL 组，同时清掉第 29 轮的无效 override）。★ 自身记录的空间索引 = 文件 MAST 数量（实测 = 2；写死 1 会落进 BlueprintShips 的 master 空间，已踩已修） |
| 入口表 | `gen_entry_table.py`：每条入口带**候选链**（新建 marker + 同 cell 原生常驻引用 ≤2 个，XMarker 系优先）→ `SAQ_EntryTable.h` |
| DLL | `AppendEntryRows` 依次 `LookupByID`：① marker（运行期 FormID = Guide 通道前缀左移 24 位再或上 markerLocal）→ ② 任务板自身 → ③ 兜底；统计行改 `入口=12(可导航 N｜marker a 原板 b 兜底 c 不可用 d)`；没走 ① 的条目打「入口候选诊断」（列出每个候选的命中情况）；`ApplyGuideRequest` / `AdoptExistingGuide` 同步支持候选链 |
| 构建 | `build-saq.ps1`：ESM 段 = clean → patch → **create markers**；入口表挪到 ESM 之后（依赖 board_markers.json，-SkipTable 时按时间戳增量） |
| 验证 | `verify_saq_build.py`：ESM 数据侧解析「新建常驻 marker 11/11（XMarker + CellPersistent + 0x400 + 空间索引正确）」+ 候选链特征 + 反向检查 |

### 4. 验证（已做，全部通过）

* ESM 自校验：11 条 marker（组链 [2,3,6,8]、flags 0x400、DATA 与任务板逐字节一致）；
* **xEdit 权威验证**（`tools/run-verify-persist.ps1`，脚本已按新判据改写）：
  `marker hits = 11 (expect 11)`，组名解析出 cell 名（The Well / The Lodge / Neon Core …），
  **重存 SAQ_resaved.esm 后 11 条仍在**（0x02000900~0A、flags 0x400、组链 [0,2,3,6,8]）
  = 结构被社区标准解析器完全读懂；
* 全量构建 + 部署（DLL / ESM / MO2 副本）；`verify_saq_build.py` 全部通过。

### 5. 待实测判据（下次进游戏，一次会话看完）

1. 开菜单 → `入口=12(可导航 12｜marker 11 原板 1 兜底 0 不可用 0)`：
   * **marker 11** ⇒ 新建常驻引用被引擎接受 = 治本成功（首选判据）；
   * 若 `兜底 11` ⇒ marker 不被接受、但原生常驻引用在远处可用（导航仍可用，落点差几米）；
   * 若大面积 `不可用` ⇒ 连原生常驻引用也需要 cell 加载（引擎语义与预期不符，看日志再议）；
2. 不走近、在任意地点点一条「任务板 · XX」→ 关菜单 ≤11 秒
   `引导已生效：任务板 · XX 脚本状态=1`，蓝点出现在那块板上（不再是旧目标）；
3. 回归：界面无「太远」字样；无目标条目不发光请求；菜单里停 20 秒无 `引导未生效`。

---

## 一·补十六、第 31 轮：任务板蓝点偏移 —— 候选链改「精确优先」+ 目标动态更新

### 1. 玩家反馈与证据（13:15 截图 + 13:11 会话日志）

反馈原文：**「任务板的导航蓝点和任务板本身的位置稍微有点偏差」**（截图：板在高亮，
蓝点在板前 2~3 m 的地板上）。日志给了两行决定性证据：

```
[13:11:54] 入口=12(可导航 12｜marker 0 原板 2 兜底 10 不可用 0)      ← 第 30 轮的判据：marker 0
[13:11:57] 引导请求：任务板 · 陋室（0x00137573）→ 引用 0x0011B754（）
```

* `0x0011B754` = `MQ204_NoelBasement_Marker01a`（陋室地下室的**沙盒标记**，不是板），
  离线数据里它距板 **3.41 m** ⇒ 蓝点当然不在板上；
* `marker 0` ⇒ 第 30 轮新建的 11 条常驻 XMarker **引擎里一个都取不到**
  （不只是「远处取不到」，连玩家所在 cell 的那条也取不到 —— 见下面第 3 节）。

（顺带排掉一个误判：截图里板身的高亮来自 always-scan 类 mod 的扫描高亮，
**不是**我们引导的高亮 —— 第 30 轮的引导目标是隐形 XMarker，不会高亮任何东西。）

### 2. 本轮修法（三处，全在 DLL）

| # | 改动 | 位置 |
| --- | --- | --- |
| 1 | 候选链顺序改**精确优先**：`任务板自身 → 新建常驻 marker → 同 cell 常驻兜底`。玩家走到板前时 cell 已加载 ⇒ 取到板自身（距离 0）—— 这是第 30 轮把兜底排在板前造成的疏漏 | `EvaluateEntryGuide` |
| 2 | 引导目标的**动态更新**（菜单关着时 1.5 s 复算一次）：从远处选（兜底）→ 走到板所在 cell 后自动换成精确目标（蓝点上板）；目标随 cell 卸载消失时换回兜底。静默更新不碰界面，确认失败**不清通道**（`verifySilent`） | `UpdateEntryGuideTarget` |
| 3 | marker 取不到的**数据侧探针**（每会话一行）：XMarker 基类是否存在 / marker 记录号是否落在别的加载序号上 / 同前缀下自己的 QUST 是否在（区分「序号映射坏了」与「CELL 组的新引用没被加载」） | `LogEntryMarkerProbe` |

### 3. ★ 新增待查问题：为什么 CELL 组里的新引用没被加载

第 30 轮的结论原本是「新建常驻引用 = 任意位置可用」，实机判据 `marker 0` 否定了它。
数据侧已排除的可能（本轮工具 `tools/esm/diff_entry_marker.py`）：

* 记录结构与真实文件**逐字段一致**：组链 `CELL > InteriorBlock > InteriorSubBlock >
  CellChildren > CellPersistent`、每层组头 label 与 Starfield.esm 里同一 cell 完全一致
  （`441129/0x0006BB29` 等）、`flags=0x400`、`base=XMarker(0x3B)`、`DATA` 与任务板
  逐字节相同（位置 + 旋转）；
* FormID 空间没问题：本 ESM 的 MAST=2 ⇒ 自身记录号 0x02xxxxxx，运行期映射到通道前缀
  `0x0F`（同一映射下 GLOB `0x0F000803` 一直是好的、脚本能收到引导）；
* 文件确实被引擎加载（`ESM 通道：前缀=0x0F`、引导能应用 = QUST/GLOB 都在）。

⇒ 剩下最可能的解释：**Starfield 对「插件往 master 拥有的 cell 里新增引用」的处理**
（非常驻/常驻、何时合并）。下一步靠 `入口 marker 探测` 的对照项定性，再决定路线
（候选：改成 override 某个已存在的常驻引用？用 Papyrus `PlaceAtMe(forcePersist)` 在
玩家到达时自建常驻标记？）。

**注意**：第 31 轮的修法**不依赖**这个问题 —— 蓝点该在板上时用的是「任务板引用自身」，
只在「板所在 cell 还没加载」（玩家在别的星球/城市）时才用兜底引用给方向。

### 4. 验证（已做）

* DLL 重编 + 部署（13:26）；`verify_saq_build.py` 全部通过（新增 5 条第 31 轮特征：
  精确来源文案 / 动态更新日志 / marker 探测 / 静默更新不清通道 + 1 条旧文案反向检查）；
* 离线核对 12 条兜底距离（`ref/entry_persistent_scan.json`）：1.83~8.13 m，
  陋室 3.41 m 与截图一致；阿基拉城（唯一原生常驻的板）无 marker、直接用板自身；
* ESM 未改（第 30 轮的 11 条 marker 保留 —— 探针要它在场才能定性）。

### 5. 待实测判据（下次进游戏）

1. 走到任意任务板前（同 cell）点它 → 日志 `引导请求：…→ 引用 0x…（任务板自身（精确））`，
   蓝点落在板上（首选判据）；
2. 在别处点一条远的任务板 → `（同 cell 常驻兜底（大致位置））`；**走进那个 cell 后 ≤2 秒**
   应出现 `入口引导目标已更新：… 0x… → 0x…（任务板自身（精确））`，蓝点上板；
3. `入口 marker 探测` 一行：把三个对照项抄回来（这一步决定第 32 轮的路线）；
4. 回归：反向走（离开 cell）时蓝点回到大致位置、HUD 不丢；普通任务的引导不受影响
   （`UpdateEntryGuideTarget` 只处理入口条目）。

---

## 一·补十七、第 32 轮：重启/读档后不进菜单也能自愈 —— 菜单关着时例行「认领 + 对账」

### 1. 玩家反馈与证据链（截图「偏差仍然存在」+ 13:26 后的会话日志）

第 31 轮部署时间是 **13:26:48**（MO2 里 DLL 已确认）。玩家随后 13:30:35 / 13:32:34
两次启动游戏，但这两段会话的日志里**没有任何「菜单打开」记录** —— 玩家重启、读档、
直接看 HUD（没有进任务菜单）。于是：

| 环节 | 状态（日志/存档可证） |
| --- | --- |
| 存档里（GLOB）的引导目标 | 还是 13:12 会话设的兜底引用 `0x0011B754`（陋室那块板旁 3.41 m 的沙盒标记） |
| 脚本（SAQ_Main） | 实例从存档恢复，别名仍绑着它 ⇒ 世界里照常显示那颗**偏掉的蓝点** |
| DLL | `g_guide.questFormID == 0`（本会话没设过引导）⇒ **第 31 轮的动态更新直接短路** |

根因一句话：**第 31 轮的「认领」此前只发生在菜单打开时**（`OnMissionMenuOpened`）——
玩家不进菜单，认领和动态更新就永远不跑。

### 2. 修法（全在 DLL）

| # | 改动 | 位置 |
| --- | --- | --- |
| 1 | 新增 `PollGuideUpkeep()`（菜单关着时每 2 秒一次）：① 没在引导 + 通道里有目标 ⇒ **认领**；② 在引导 + 通道没目标（读档到无引导存档）⇒ 重置本侧；③ 在引导 + 通道目标 ≠ 本侧（同会话读档/外部改动）⇒ 清本侧按通道重新配对 | `SAQ.cpp` |
| 2 | Tick 的「菜单关着」分支先跑 `PollGuideUpkeep()` 再跑 `UpdateEntryGuideTarget()` —— 认领成功后**同帧**进入动态更新（精确目标立即可用） | `Tick` |
| 3 | 认领成功时重置 `lastTargetCheckMs = 0`（允许立刻复算）；「认不出的通道目标」的 WARN 用 `adoptWarnRef` 去重（同一目标只打一次，不因例行检查刷屏） | `AdoptExistingGuide` |

### 3. 待实测判据（下次进游戏，一次会话看完）

1. **核心**：重启游戏/读档，**不进菜单**，走到任意任务板前 ≤2 秒：
   * `认领已有引导（任务板入口）：任务板 · XX …`（或 `认领已有引导：…`）；
   * 紧接着 `入口引导目标已更新：… → 0x…（任务板自身（精确））`；
   * `引导已生效：…脚本状态=1`（**前提是脚本在轮询**）→ 蓝点落在板上；
   * 若只有前两行、没有 `引导已生效`：说明本次会话脚本定时器没跑（从未开过菜单）——
     开一次任务菜单再关掉，脚本会在关闭事件里应用通道里的精确目标（蓝点上板）。
2. 顺带：`入口 marker 探测` 一行（决定第 33 轮路线）；若日志没有这行，说明 12 条入口
   全部走了精确目标（原板命中）—— 那更好。
3. 隔离验证（可选）：在板前原地读一次档（同会话）→ `引导状态对账` 或重新认领的日志，
   蓝点 ≤2 秒回到板上。
4. 回归：普通任务引导不受影响（`PollGuideUpkeep`/`UpdateEntryGuideTarget` 只处理入口条目）；
   菜单里停 20 秒+ 无 `引导未生效`；无目标条目只有界面提示、DLL 无请求。

---

## 一·补十八、第 33 轮：日志复查 —— 第 32 轮自愈「跑了但认不出」的真因 + 「新增引用」缺 CELL override

### 1. 复查对象与部署确认

`mod 目录\SFSE\Plugins\SAQ_ShowAvailableQuests.log`（249 KB，最后一行 13:45:02；滚动删除正常）。
mod 目录 DLL = **13:40:48 / 697856 B**，与工作区构建产物一致，且含全部第 32 轮特征串
（`引导状态对账` / `入口引导目标已更新` / `入口 marker 探测` …）⇒ 本轮会话跑的就是第 32 轮版本。
会话：13:43:16（PID 27076）+ 之前两次「只加载不操作」的 13:30 / 13:32（各 2 行、无菜单）。

### 2. 第 32 轮判据的实测结论（一次会话看完）

| 判据 | 证据（日志原文） | 结论 |
| --- | --- | --- |
| 菜单关着时例行认领 | 13:44:09.880 `[W] ESM 通道里有引导目标 0x0011B754，但静态表里没有哪条任务/入口的引导目标是它（另一个存档留下的？）` | ❌ **机制跑了、认领失败**（真因见第 3 节） |
| 开菜单后同目标认领 | 13:44:48.293 `认领已有引导（任务板入口）：任务板 · 陋室（0x00137573）引导目标=0x0011B754` | ✅ 数据没问题（同一目标、同一数据） |
| 第 31 轮目标动态更新 | 全会话 **0 次** `入口引导目标已更新` | ⚠️ 仍未验（本轮没机会：认领失败 + 玩家 3 秒后就切到别的板） |
| 精确优先（同 cell） | 13:45:01.345 `引导请求：任务板 · 陋室 → 引用 0x00137573（任务板自身（精确））`；Papyrus 13:45:02 `[SAQ] 引导已应用：[TerminalScript < (00137573)>]` | ✅ |
| 远处选板（兜底） | 13:44:51.111 `任务板 · 霓虹城 → 0x002BAB78（同 cell 常驻兜底）`；Papyrus `引导已应用：[ObjectReference <FFNeonZ12_VoliiRef (002BAB78)>]` | ✅（1.8~8 m 偏差的病根仍在） |
| 脚本活性 / 菜单事件 | Papyrus `[SAQ] 菜单打开 通知=7781 / 7782`；DLL 无 `脚本活性探测…没有 +1` | ✅ |
| 回归 | 无 `拒绝写入`；第 27 轮之后无 `引导未生效`；`drop[N]` 正常；4 master 命中 100%；`入口=12(可导航 12)` | ✅ |
| 推送稳定性 | 12:47:13 一次 `推送失败（…没解析出 ASMovieRoot）` → 0.8 s 后 `推送成功` | ✅ 自愈 |

### 3. ★ 真因：认领要用「静态表」，而静态表**只在开菜单时**才建

`AdoptExistingGuide` 的三条反查（任务 `guideRef` / 入口 uID / 入口候选）全都要经过
`Masters::MakeFormID()` 或 `g_runtimeRows`；而 `Masters::Refresh()` 只在 `BuildRuntimeRows()`
里被调用 —— 那是**菜单打开**分支。
`Masters::MakeFormID`（`SAQ_Masters.cpp` 237~248）：master 未解析 ⇒ **返回 0** ⇒ 三个反查全部落空
⇒ 打出那句误导人的「另一个存档留下的？」。

证据对照（同一会话、同一目标、静态数据完全没变，只差就绪状态）：

```
13:44:09.880（本次会话还没开过菜单）认不出 → WARN
13:44:48.293（开菜单之后）          认出了 → 认领已有引导（任务板入口）
```

⇒ 第 32 轮的「重启/读档后不进菜单也自愈」**在当前代码下不可能生效**（修法见十一·1A）。

### 4. ★ 新证据（离线、可复现）：官方动基础游戏 cell 的引用时，**一定连 CELL 记录一起覆盖**

```powershell
python tools\esm\scan_override_persistent.py sfbgs
  SFBGS003：override 引用 248（常驻组 66） → ★ 这些 cell 4/4 同时被 CELL 记录覆盖
  SFBGS008：override 引用 4（常驻组 4）    → ★ 2/2
python tools\esm\find_added_persistent_refs.py      # 产物 ref\added_refs_scan.txt
  全机插件里**只有我们**往基础游戏 cell 新增常驻引用（11 条）；
  BlueprintShips-SFBGS050 的 2382 条新增常驻引用全在它**自己的** cell 里
python tools\esm\scan_override_persistent.py saq
  我们的 ESM：新增引用 11 条、CELL 记录覆盖 = 0
```

⇒ 我们是本机唯一的「只写引用、不写 CELL」写法。第 30 轮的 `marker 0`（新建的 11 条常驻
XMarker 引擎里一个都取不到）最可能就是**缺这条 CELL override**（引擎不把「本插件没有该 CELL
记录」的 CellChildren 组并进那个 cell）。这条线索同时解释第 29 轮（override 引用）与第 30 轮
（新增引用）两次失败 —— 两轮都没写 CELL override。

### 5. 顺手记下的两个小问题

1. 「认不出通道目标」的 WARN 文案在静态表未就绪时会误导 —— 应区分「静态表尚未就绪，稍后重试」。
2. 兜底引用质量：霓虹城兜底 = `FFNeonZ12_VoliiRef`（enable marker 系），不是「地点门牌」——
   远处看这个蓝点意义有限（第 30 轮遗留的「质量分级」问题，见十一·1C）。

---

## 二~十、历史轮次记录（第 9~16 轮）

> 2026-09-20 第 31 轮：`docs/99` 接近 100 KB 上限，把第 9~16 轮的复盘整体挪到
> **`docs/98-历史轮次记录（第9~16轮）.md`**（内容一字未改，只是搬家）。

## 十一、下一步（按优先级）

1. ★ **第 33 轮（本轮主线：两件事 + 一次实测）**：
   * **A（必修 bug，第 32 轮实测暴露）**：菜单关着时的认领必须先保证静态表就绪 ——
     `PollGuideUpkeep` 里补 `EnsureStaticReady()`（`Masters::Refresh()` + `BuildRuntimeRows()`；
     已就绪零开销、失败不缓存下次重试），并把「静态表未就绪」与「另一个存档留下的」分开报。
     判据：重启/读档、**不进菜单** ≤2 秒 → `认领已有引导…` +
     `入口引导目标已更新：…（任务板自身（精确））`（+ `引导已生效`），蓝点落在板上
     （一·补十七第 3 节）。
   * **B（治本探索，第 30 轮悬案的答案候选）**：`tools/esm/create_board_markers.py` 给这 11 个
     cell 补 **CELL override**（记录字节照抄 Starfield.esm；组结构照抄官方 SFBGS003 的 CELL
     覆盖写法），xEdit 验证 + 自校验后进游戏。判据：`入口=12(可导航 12｜marker 11 …)`；
     任意位置点板 → 蓝点直接落在板上（不再偏 1.8~8 m）。成立 ⇒「任意位置精确导航」收口；
     不成立 ⇒ 走 C。
   * **C（B 失败时的兜底质量）**：兜底引用从「最近的原生常驻引用」换成「该 cell 的入口门引用」
     （有意义 + 零冲突面），或接受现状（近处精确 / 远处大致位置）。
   * 回归与欠账：一·补十七第 3 节的全部判据 + `入口 marker 探测` 一行；
     **欠的交互判据**：把某条被引导的任务接到手 → `已接取，自动取消引导`（日志 + 竖条灭）；
     眼睛确认：蓝点 / 扫描仪路径线 / DLC 任务名。
   （第 27/28 轮的判据历史：入口链路已在 11:40~11:55 会话通过；第 28 轮的「太远」文案
   已在第 29 轮退场，不要再照旧文档核对。）
2. **第 18/26 轮已实测通过**（一·补四第 1 节 / 一·补十二第 1 节）——
   只剩「DLC 任务名显示、蓝点出现」这类需要**眼睛**确认的部分，随第 1 条一起看。
3. ~~任务板入口「任何位置都能精确导航」~~ → 第 29 轮（override）被否定 → 第 30 轮
   （新建常驻 XMarker）**实机判据 `marker 0` 也被否定** → 第 31 轮「精确优先 + 动态更新」
   修掉玩家可见的偏移 → **第 32 轮补上「重启/读档后不进菜单也自愈」**（例行认领 + 对账）。
   ★ 第 33 轮复查给出的答案候选（一·补十八第 4 节）：**缺 CELL override** ——
   官方 6/6 个 cell 在动基础游戏引用时都同时覆盖 CELL 记录，我们 0/6，全机零先例；
   探针已抄回（`marker 0x90A@0x0F=无`／`对照 QUST@0x0F=有`／该记录号在别的序号上
   `0x05 0x07 0x08 0xFF`）。次选路线：Papyrus `PlaceAtMe(forcePersist=true)` 在玩家到达时
   自建常驻标记（治不了「远处」）。

4. **引导目标覆盖率**：52 条无目标**已归因**（`docs/07`）—— 结论是**不补猜的目标**
   （21 条事件类 / 19 条任务板类 / 12 条地点无标记 / 4 条暂存格 / 1 条目标不唯一）。
   剩余可做的是**质量**侧：
   * 非常驻引用（`actor` 型 76 条）的「引导目标候选链」（多候选，脚本报 `状态=2` 时自动换下一个）；
     ★ 第 27 轮的持久性统计见 `tools/esm/ref_flags_report.py`（非常驻也有成功先例：
     02025B99 黄陈 / 002AD366 兰德里在实测里被成功应用过）；
   * 质量分级：优先 NPC / 命名引用，`*EnableMarker*` / `*XMarker*` 降权（实测群众心态被引到
     `FFNeonZ08_HeadlockEnableMarker001`）。
5. **「进度没到不显示」**：真正靠谱的判据是任务/对话的**条件（CTDA）**，需要引擎的条件求值器；
   `docs/04` 第五节列了入口，属于「有闲再挖」的深水区。
6. **可扩展性（可选）**：把其它官方 Creation（`SFBGS003/004/006/007/008`、`Constellation`、
   `BlueprintShips*`）也加进 `fetch_sources.py::SOURCES` —— 表里已经有 master 概念，
   DLL 不用改；主要成本是每个 master 要抽一次字符串表。
7. **打包 + Nexus 文案**：第 25 轮已产出 0.1.0（`tools\package-saq.ps1` 可随时重打）；
   第 27/28 轮（任务板入口 + 降噪 + 可用性判定）**需重打一次**（已在各轮构建后重打）。
   文案里要写明的已知限制：
   * 首次引导后，任务日志里会出现一条名为「可接任务」的引导用任务（借引擎标记系统的代价）；
   * 部分任务 / 部分任务板入口没有可用的导航目标（列表照常显示，只是点了不会有标记）；
   * 引导总在**关闭菜单后**生效（与原版一致：HUD 蓝点本来就要关菜单才可见）；
   * 地球舰队的 5 条「鸭嘴猪」活动在游戏数据里就叫同一个名字（列表里看起来是重复项）。

### 已完成（保留记录）

* ✅ **第 26 轮实测通过**（11:12~11:14 会话）：菜单里停 27 秒无假失败；关菜单 0.9 秒
  `引导已生效`（一·补十二第 1 节）。
* ✅ **第 18 轮实测通过**（09:53~09:56 会话）：前缀探测 4 master 全解析（命中 100%）、
  推送 260 条、认领已有引导、界面 257 条（一·补四第 1 节）。
* ✅ 验证「全数到期」修复 + 卡顿埋点（第 16 轮日志确认：`隐藏=1` 只剩「食宿」、`收集耗时=0 ms`、
  推送失败路径 0 ms、**无「主线程停顿」行**）。

## 十二、已知风险

| 风险 | 说明 |
| --- | --- |
| **「已开始」≠「已接取」**（第 11 轮实证） | 引擎会把「玩家仍能接到」的任务提前置成 running（RAD05 全数到期，flags=0x8001）。TESQuest 的 started 位**不能**当「玩家已接」的判据；只有玩家任务日志（QuestData）算数 |
| **GLOB（float）存不下完整 FormID**（第 21 轮踩过） | GLOB 是 float（尾数 24 位）：完整 FormID > 2^24 后**奇数不可精确表示**（±1）。已把引导目标拆成「低 24 位（0x801）+ 高 8 位（0x805）」两个 GLOB。**以后再开 float 通道，先问「值会不会超过 2^24」** |
| **Papyrus 定时器在菜单开着时不走**（第 27 轮定案） | 任务菜单暂停游戏 ⇒ `StartTimer` 不触发：三次会话的 `引导已应用` 都精确落在「菜单关闭」时刻，菜单开着 27~37 秒零应用。**不要设计「菜单开着时脚本会处理」的功能**；引导总是「关菜单生效」（与原版 HUD 交互一致）。DLL 侧也据此不再在菜单开着时轮询确认（降噪） |
| ~~任务板入口多为非常驻引用~~（第 27 轮发现 → 第 28 轮兜底 → 第 29 轮 override **被实机否定** → **第 30 轮新建常驻引用治本**） | 12 条入口里只有阿基拉城是原生常驻 ⇒ 其余 11 条在 cell 未加载时 `Game.GetForm` 取不到。★ **第 29 轮的 override 路线不成立**：override 只换数据、**不改变引用的加载分类**（官方 70 条同类 override 原记录本来就全是常驻 = 零先例；实机 12:45 会话 `可导航 2`）。第 30 轮改为**新建** 11 条常驻 XMarker（0x900~0x90A，位置 = 板坐标）+ 候选链（marker → 板自身 → 同 cell 原生常驻兜底）。**待实测**：`入口=(…marker 11…)` |
| ~~override 世界引用的冲突面~~（第 29 轮引入 → **第 30 轮已消除**） | 第 29 轮对 11 条任务板 REFR 的 override 已全部移除 ⇒ 不再覆盖任何基础游戏记录。第 30 轮只往那些 cell **新增**自己的记录（mod 加物件级操作），与改同一 cell 的 mod 无冲突 |
| ~~打开任务菜单偶发长停顿~~（第 11 轮发现 → 第 12 轮修复 → 第 16 轮确认） | 根因：失败解析路径里 `IsMapped`（VirtualQuery）累计 ~3.9 秒；`SafeRead` 改纯 `__try` 保护后消失。第 16 轮日志实证：收集 0 ms / 失败 0 ms / **无「主线程停顿」行** |
| **界面状态必须由回写驱动**（第 16 轮踩过） | `SaqToggleGuide` 只能在本地立即切状态，而「这条任务有没有引导目标 / 实际在引导哪条」只有 DLL 知道。凡是「界面先行、DLL 后处理」的动作都必须有回写通道（`SAQ_GuideReply`），否则界面会说谎 |
| **GLOB 锚点有次数窗口**（设计边界） | `SAQ_Notify` = 7777 + 菜单打开次数，认领窗口 7777..8776（1000 次）。单次会话极难超限；脚本重挂会把它重置回 7777（DLL 认领成功后不再读锚点，无影响） |
| **文本子记录必须带 NUL**（第 12 轮实证） | 非本地化 ESM 的 FULL/NNAM 等文本子记录，长度 = 字符数 + 1（含 NUL）。第 11 轮漏写 NUL ⇒ HUD「任务更新」显示 `[…]` |
| commonlibsf 的 `REL::ID = 0` 接口 | 已踩（BGSStoryTeller、`TESQuest::IsStageDone`）；编译期 ID 审计挡着（`docs/01` 坑 8） |
| **commonlibsf 的结构体偏移一律不可信**（第 18 轮升级为通则） | 已踩四次，其中 TESDataHandler 两次：`formArrays` 读到空表（第 5 轮，见 docs/02）、`files` 读到空链表（第 17 轮，四个 master 全被误判「未加载」⇒ 第 18 轮改前缀探测）。读引擎对象字段：先找「引擎自己怎么读它」（反汇编），或改用已实测可靠的引擎函数（`LookupByID` 等）；`TESGlobal::value`（0x48）是上一代项目修正过的 |
| **自己写的注释/文档也可能是错的** | 已踩：哈希指针、模糊 RTTI、误信「位16 = 自动启动」。结论要留原始反汇编/日志，且「改完要能被验证」 |
| **GFx 返回值要按类型读** | `GetNumber()` ≠ 万能（见 `docs/04` 2.4） |
| **TESQuest 运行时偏移来自反汇编** | `0x114 / 0x32C / 0x338` 有虚表核验 + 识别率安全阀；换游戏版本要重跑 `tools/re/quest_api_scan.py` |
| **非本地化插件的文本是内联的** | 本 ESM 的 TES4 flags = 0x01（无 Localized 位）⇒ 目标文本可以内联写 `<Alias=SAQ_GuideTarget>`，不需要动游戏的 strings |
| **非常驻引用** | 168 条引导目标里 68 条是非常驻引用：格子没加载时 `Game.GetForm` 可能取不到（日志会写 `状态=2`） |
| **patch 文件集 ≠ 源码目录** | 改任何 `.as` 都要在 `build-saq.ps1` 的同步清单里登记（普通 + lrg 两套）；lrg 源码由 `make_lrg_source.py` 生成 |
| 静态过滤是启发式 | 宁缺勿滥；规则集中在 `filter_reason()` 一处 |
| 语言判定 | AS3 侧按引擎推来的任务名判定（中文/英文）；C++ 侧 INI 值只作日志 |
| 菜单关掉后 Movie 会销毁 | 已处理：每次「菜单由关变开」都 `UI::Reset()` 清解析缓存 |
| 性能 | 菜单打开时：202 次 `LookupByID` + 202 次标志位读取 + 1~2 次 0x4000 字节扫描；开着菜单时每 500ms 一次 `SAQ_Report`、每 100ms 一次 `SAQ_PeekGuide`。实测无感 |
| **树形列表不能按「数据下标」刷新**（第 14 轮踩过） | 显示下标 ≠ 数据下标（`InitializeEntries` 会把已完成 / misc 条目挪位、展开时插入子项），而且 `BSScrollingList` 没有 `UpdateEntry` 接口 —— 第 13 轮那次调用每次抛 `Error #1006` 被 `try` 静默吞掉。就地刷新一律走 `MissionsList.SAQ_RefreshQuestRow(uID)`（按 uID 在当前显示列表里找行） |
| **DLC 的字符串表是各自一份**（第 17 轮） | 任务名要在**该插件自己的** `<master>_{语言}.strings` 里查（破碎空间在 `ShatteredSpace - Main02.ba2`，地球舰队在 `SFBGS050 - Main.ba2`）。漏抽的症状是「DLC 任务全部消失」（取不到名字 ⇒ 被当成内部任务过滤），而不是报错 |
| **medium / blueprint 档位的 FormID 方案未验证**（第 17 轮） | 本项目实测的四个 master 都是普通插件（fullIndex<<24）。light/ESL（`0xFE000000 \| smallIndex<<12`）已实现但**本机没有样本可验**；medium / blueprint 直接标记不支持并整批跳过（`SAQ_Masters::ResolveOne`），宁可少收也不错收 |
| **master 名大小写不一致**（第 17 轮） | ESM 的 MAST 里写的是 `starfield.esm`，而文件夹里是 `Starfield.esm`；`Masters::FindLoadedFile` 用大小写不敏感比较（日志里会打出实际匹配到的名字） |
| **内嵌回退数据只有基础游戏条目**（第 17 轮） | `SaqEmbeddedPayload.inc` 必须写运行期 FormID，DLC 的离线算不出来 ⇒ 只放 Starfield.esm 的 202 条。C++ 推送成功前的那 0.3~0.8 秒里看不到 DLC 条目（可接受的降级） |
