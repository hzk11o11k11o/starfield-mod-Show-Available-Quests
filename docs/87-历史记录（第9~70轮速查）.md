# 87 · 历史记录：早期轮次速查（第 9 ~ 70 轮）

> 本文 = `docs/99-当前项目进度.md` 的两段速查**逐字迁移**（第 137 轮瘦身 —— docs/99 触
> 100KB 上限）：① 「十三 ~ 二十八、第 51 ~ 70 轮」节（原文）；② 十一节的
> 「更早的收口与约定（第 16~64 轮 / 发布与打包）」条目（原文，见文末）。

## 十三 ~ 二十八、第 51 ~ 70 轮（已迁 docs/95 / docs/97；速查）

> ① **51/52**：`ui.tab` 真因 = 向只读属性写入（`Error #1074`）⇒ 改走
>   `MissionTabbedSelection.SetSelectedCategoryIndex`（拒绝码 `tab-refused`）；smoke 首次全绿。
> ② **53**（发布就绪）：编译开关 `saq_harness` + `build-saq.ps1 -Release` / `package-saq.ps1`
>   + verify 两模式 + 「MO2 部署 == 工作区」（三个 xmake 坑见 `docs/01`）。
> ③ **54/55**：6 条用例（smoke + r26/r44/r45/r47/r48）+ 新能力（`assert.nolog` /
>   `scope=prev|case` / `ui.selectchild` / `menu.hide` / `teleport.entry` / `~0x…`）+
>   两条红线（副作用行 `scope=prev`；每案清场 + 700 ms）。
> ④ **56/57**：断言窗口按步保留；传送等 cell 加载；只认产品日志；同一批行第二条 `scope=case`。
> ⑤ **58/59**：传送「落地静默期」（每 Tick 推进）；**60/61**：`guide.probe` + r45 尾段改判
>   ⇒ 大项 G 收口（「别名持有 ⇒ 走远仍可得」按设计保持）；**62/63**：0.1.2 包 + 自动读档
>   （零新 RE）+ `WorldBusyForUpkeep()`（读档窗口暂缓）；**64**：0.1.3 包 + 离线层（`docs/10`）。
> ⑥ **65**：图标（FTYP → 阵营枚举，`icon=`，13 帧）；**66**：回执窗口 3000→8000；
>   **67**：任务链门槛（`kChainGates`，判据 `docs/08` 第十节）；**68~70**：红线四（周期
>   统计行断言一律 `scope=case`）；解析失败 ⇒ FAIL；非法正则解析期报错。
> ⑦ 第 75 轮（四大势力开头，复盘 `docs/95`）：`factionEntry` 列 + 固定排前四 +
>   `IsGatePinned` / `PinnedOrderKey·Less` + `FormatPinStats` 单独一行 + CF01 候选清空。
---

0. **更早的收口与约定（第 16~64 轮 / 发布与打包）—— 明细已迁 `docs/93`**：
  ① 仍有效的流程：**改 SWF 后必须先构建部署、再启动游戏**（第 50/52 轮）；
  ② 发布就绪（第 53 轮 `-Release` / `package-saq.ps1` / 0.1.1~0.1.3 包）、大项 G（历史判据落成
  用例）、自动读档（第 62/63 轮）、离线层（第 64 轮）明细见归档；
  ③ SET COURSE 星图（第 37~45 轮）与引导可用性（第 46/47 轮）完整判据见 `docs/05` 第十一~二十一节；
  ④ 「进度没到不显示」门槛链（记录级 / INFO / 链式）见 `docs/08`；
  ⑤ 打包 + Nexus 文案（含「文案里要写明的已知限制」4 条）见归档与 `N网介绍草稿.txt`。
