# 历史轮次记录（第 43~60 轮）

> 本文件由 2026-09-21 第 75 轮建立：`docs/99-当前项目进度.md` 逼近 100 KB 上限
> （AGENTS.md 的单文件限制），把该文件顶部的「第 43~60 轮速览块」**整体搬到这里**
> （内容一字未改，只是搬家）。更早的轮次见 `docs/96` / `docs/97` / `docs/98`。
>
> 仍然生效的关键结论的**速查版**留在 `docs/99` 的「一·补」区（第 43~60 轮那一小段）。

---

> 上一轮：2026-09-21（**第 60 轮（大项 G 修复 · 六）**：r45 唯一 FAIL = **用例期望不可达**
> （产品全对：引导目标被 `ForceRefTo` 后走远仍可得 ⇒ 复算按设计无动作、蓝点不抖）；
> 处置 = 新 op `guide.probe` + 用例改判 + 驱动器 v60；**已由第 61 轮实测收口（6/6 PASS）**。
> 详见「二十」/ 本文件「第 58~61 轮」一节（原 `docs/09` 十四·补六，2026-09-22 第 84 轮迁入）。
>
> 上一轮：2026-09-21（**第 59 轮（大项 G 修复 · 五）**：r47/r45 两条假 FAIL = **纯驱动器缺陷**
> ——「落地静默期」绑死在「读到回执那一 Tick」（`Poll` 是一次性消费）；修法 = 静默期每 Tick
> 推进、20 秒窗口只管等回执（驱动器 **v59**）。详见「十九」/ 本文件「第 58~61 轮」一节
> （原 `docs/09` 十四·补五，2026-09-22 第 84 轮迁入）。
>
> 上一轮：2026-09-21（**第 58 轮（大项 G 修复 · 四）**：三个 FAIL **同源** —— 一次 `MoveTo`
> 把游戏端卡在加载画面（距上一条回执仅 38 ms 就提交下一条传送；不是产品缺陷）。处置（只动
> 驱动器 **v58**）：传送落地静默期 / 加载画面证据 / 卡死自动中止。详见「十八」/ `docs/09`。
>
> 上一轮：2026-09-21（**第 57 轮（大项 G 修复 · 三）**：四条 FAIL 仍全在驱动器/用例侧 ——
> ① 星图在清场后 1~1.5 秒才打开 ⇒ 加 4 秒复查窗口；② `ui.select` 的 `~0x…` 漏写回；
> ③ r47 没等推送就切 tab（撞内嵌回退数据）；④ 两条断言共享同批行须 `scope=case`。
> 详见「十七」/ `docs/09` 十四·补三。
>
> 上一轮：2026-09-21（**第 56 轮（大项 G 修复 · 二）：09:22 会话 6 条用例 PASS 2 / FAIL 4 ——
> 四个 FAIL 全是**驱动器**缺陷，产品侧全对** ——
> ① **断言窗口起点被清 0**（真凶）：`CompleteStep` 里 `g_cur = StepState{}` 把 `logMark` 一起
>   清空 ⇒ 下一步的 `scope=prev` 退化成「整个环形缓冲」：r26 的 `推送成功` 断言 0 ms 命中了
>   4 秒前那一行、r44 因此没等真正的新推送（实测 ~800 ms）就调 `ui.tab` ⇒「桥没通」；
>   r48 的反向断言则是**假 FAIL**（报文「窗口从 idx 0 起」—— 而 09:23:16.607 的
>   `INFO门槛=58(过38/藏18/放行2/未知0)` 名单里**已经**没有『无凭无据』= 产品正确）；
> ② **传送回执 3 秒窗口太紧**：Papyrus 证明两次 `MoveTo` 都**成功**（`seq=6 结果=0 ——
>   MoveTo [FCMissionBoardREF (0014D497)]（cell=CityAkilaCityTheRock）`），但回执是 **cell 加载
>   完成后**才写的（实测 4.3 / 4.5 秒）⇒ r47/r45 被判超时；
> ③ 顺带两条：断言会命中 harness **自己的行**（复述正则与命中行 = 回声）⇒ `LogFind` 现在跳过
>   `harness：` 行；smoke 的「脚本已应用」断言去掉裸 `引导确认`（那句恰恰是「还没应用」）。
> 修法：窗口起点按步保留（+兜底：起点 0 ⇒ 退到用例起点）+ 传送单独 **20 秒**窗口 + 只认产品日志
> + **驱动器版本串 `驱动器 v56`**（判据与 SWF 的 `stamp=` 同理）。DLL **924672 B**、用例文件
> **18759 B**，已部署；verify **全过（+5 条检查）**。★ **需重进游戏**再跑一轮（期望 6 条全 PASS）。
> 详见「十六、第 56 轮」/ `docs/09` 十四·补二。
>
> 上一轮：2026-09-21（**第 54 轮（大项 G）：历史判据落成 harness 用例** —— 5 条新用例
> （`r26_menu_idle` / `r47_board_marker` / `r45_candidates` / `r48_info_gate` / `r44_starmap`）
> + harness 四类新能力（`assert.nolog` / `scope=this|prev|case` / `ui.selectchild` /
> `menu.hide`+`teleport.entry`+`~0x…`）；产物 SWF 727801/727931 B、DLL 923136 B；
> **首测结果见第 55 轮**。详见「十五、第 54 轮」。第 47 轮小节已迁 `docs/98`。
>
> 上一轮：2026-09-21（**第 53 轮（大项 F · 发布就绪）：发布包不含 harness + 0.1.1 重打** ——
> 起因：`dist\` 里的 0.1.0 包是 9/20 21:50 打的 —— **缺**第 49 轮补丁②「候选降级观察期」
> （读档抖动根治）与第 51/52 轮的 SWF 修复（`stamp=52`）⇒ 玩家拿到手读档时蓝点会抖。
> 做三件事：
> ① xmake 编译开关 **`saq_harness`**（默认 y = 开发构建；`n` = 发布构建：`SAQ_Test*.cpp`
>   不参与编译、调用点全在 `#if SAQ_WITH_HARNESS` 内 ⇒ DLL **911360 → 784896 B**、
>   无任何 harness 字符串；发布版对 ini `Harness=1` 打 WARN —— 不静默）；
> ② `package-saq.ps1` 自动「发布构建 + 部署 → `verify --release`（13 项反向检查）→ 打包」，
>   verify 另加「MO2 部署副本与工作区**字节一致**」（第 50 轮教训）；
> ③ 版本 0.1.1（xmake/启动日志/package/meta.ini 四处同步）+ README/Nexus 文案更新
>   + 删死变量 `kStarMapRequest`（C4189）。
> 产物：**`dist\SAQ-ShowAvailableQuests-0.1.1.zip` 1,810,262 B**
> （SHA256 `9EE4491C…344136`；7 文件：esm / dll 784896 / swf×2 / pex / ini / README v0.1.1）。
> ✅ 两条链路都实测通过：dev 模式（13 项正向 + 部署一致）与 release 模式（13 项反向 + 提示串）。
> ★ 两个 xmake 踩坑记档（`remove_files→add_files` 不恢复 / `add_files{exclude}` 不生效）。
> 当前工作区与 MO2 部署 = **开发构建**（含 harness，可继续跑用例）。详见「十四、第 53 轮」。
>
> 更早：2026-09-21（**第 52 轮实测收口：harness `[case:smoke]` 首次全绿** ——
> `summary.pass=1`（2640 ms，9 步全 PASS）：① `ui.tab` 回 `ok|tab=7|mask=64|n=245`
> （`Error #1074` 修复生效）× `SAQ_Report` 带 `stamp=52` + `ep=pub=ok,ep=ok`
> （新版 SWF 已加载、入口挂载全好）—— 第 50~52 轮「`ui.tab` 三连败」正式收口；
> ② 引导链完整：`press=R@…` → `引导请求` → 菜单关闭 → 828 ms `引导已生效`（脚本状态=1）
> → 星图 1.1 秒打开（`GalaxyStarMapMenu`，第 1 次尝试）→ `guide.clear` 对账重置；
> ③ 补丁②复查通过（认领后候选稳定 `[1]`，无 `[1]→[3]→[1]`、无「未被脚本确认」WARN）；
> ④ 全会话仅 2 条 `[W]`（开菜单首次推送的桥解析失败 → 0.8 s 重试成功，老现象自愈）、
> 无 `[E]`；日志 555755 B（滚动正常）；`INFO门槛=58(过37/藏19/放行2/未知0)`（放行补丁生效）、
> `入口=12(可导航 12｜marker 10 原板 2 兜底 0 不可用 0)`。详见十四·第 6 节。
> 顺带：`docs/99` 再次超 100 KB ⇒ 第 32~34 轮复盘再迁 `docs/96`（见「一·补十七 ~ 一·补十九」指引段）。
>
> 更早：2026-09-20（**第 50 轮：`ui.tab` 三连败收口 —— 真因 = 游戏进程启动早于 SWF 部署** ——
> 补丁③ 之后 22:48 会话仍 0 ms 失败；本轮用 **FFDec P-code（ABC 字节码）**证明 SWF 的挂载/方法/类 traits
> 全在（标准 + lrg 两版）、DLL 的全部补丁特征串在、mods/overwrite/真实 Data 里只有一份 SWF、usvfs 日志
> 证明游戏进程走 MO2 ⇒ **产物全对**，而 22:48 的界面行为 = 部署前那份旧 SWF ⇒ **游戏进程在部署
> （22:44:00）之前就启动了**（UI 资源在**游戏启动阶段**加载，早于 SFSE 插件加载日志的 22:46:41）
> ⇒ **进程起来之后部署的 SWF 不会被已加载的界面采用**。
> 收口：① **SWF 构建指纹 `stamp=50`**（进 `SAQ_Report` 输出 —— 日志里有没有它是「新版/旧版 SWF」的
> 唯一判据）；② `ui.*` 失败时 DLL 自动读一次 `SAQ_Report`、把指纹结论写进失败详情；③ verify +3 条；
> ④ 流程写死（`docs/09` 第七节）：**先构建部署、再启动游戏**。详见一·补三十五 / `docs/09` 第八·补第 4 节。
> **已实测通过（2026-09-21 06:53 会话）**：`summary.pass=1`、`SAQ_Report` 带 `stamp=52`
> （第 50 轮判据全中；`ui.tab` 的原始真因另由第 52 轮修掉，见十四节）。
>
> 同日补丁①/②/③（第 49 轮 harness 首测/复测的修正 —— 「0 参调用」「失败清菜单」「候选降级观察期」
> 「测试入口挂 root」都已做并入 verify；与上面的「SWF 加载时机」是两个独立问题）：详见一·补三十四第 6~8 节 / `docs/09` 第八·补节。
>
> 上一轮：2026-09-20（**第 49 轮（大项 E）：引擎内 harness —— 「不用人进游戏点，也能把判据跑完」** ——
> 起因：玩家提出「进游戏测试很麻烦，人工操作不一定覆盖所有测试要求，存档也不一定满足测试条件」。
> 方案（评审后定稿 + 实施）：把判据写成**用例文件**，由 DLL 在引擎里自动执行 ——
> 自己造任务进度、自己开关任务菜单、自己选中条目/按键、自己断言，跑完写结果 JSON。
> 关键设计决策（三条，都有理由）：
> ① **写侧动作绕道 Papyrus**：`docs/04` 早已定案「Papyrus 原生函数 `rcx=VM/rdx=栈帧/r8=self`，
>    DLL 直调要伪造 VM 栈帧」⇒ 造状态改成「DLL 写 GLOB 命令 → 脚本用语言级 API 执行」
>    （`Quest.Reset/Start/SetStage/CompleteQuest`、`Actor.MoveTo`）—— **本轮零新 RE 工作**；
> ② **通道 = ESM 追加 8 条 GLOB（0x806~0x80D）**，脚本用**自己任务的 FormID 前缀**动态
>    `Game.GetForm()` 取 ⇒ **VMAD 一个字节都不用碰**；「1→2」握手解决「脚本初始化会追平序号
>    ⇒ 第一条命令必超时」；DLL 侧还会追平/跳号，防「撞上已存在的 Ack」的假成功；
> ③ **开菜单有对称原语**：`UIMessageQueue::AddMessage(kShow)`（与已实测的 kHide 同一函数）；
>    界面侧新增 `SAQ_TestDrive*` 测试入口，内部**调用真实处理函数**（`ProcessUserEvent` /
>    `onEntryPress` / `ScrollingEvent.SELECTION_CHANGE`），不复制逻辑 —— 否则测的是测试代码。
> 顺带解决：日志环形缓冲（1MB 滚动会吃掉现场 ⇒ 断言与证据都走内存）、失败证据抄进结果 JSON、
> 用例 DSL（ini 风格，零依赖）、`-Harness` 一键部署开关、`tools/test/check_results.py` 报告器。
> 构建/部署/verify 全过（SWF 5 条 + DLL 10 条 + PEX 6 条 + ESM 8/8 GLOB 检查）。
> **待实测**：跑 `[case:smoke]`（判据见 `docs/09` 第八节）。详见一·补三十四 / `docs/09`）
>
> 上一轮：2026-09-20（**第 48 轮：判据收口 + 大项 D —— 「进度没到不显示」扩到对话侧（INFO 门槛）** ——
> 起因：上轮日志复查（20:41~20:47 会话，见下）确认第 47 轮判据实测通过，同时抓出两件事：
> ① **判据过宽**：Mode=6 / 描述提示的「首选候选非常驻」覆盖 158/209 条（实测推送 155 条、
> 76% 的任务都带「需靠近」提示，噪音化）⇒ 回退为「**全部**候选都非常驻」（20 条 =
> 远处点真会待生效的那一类）；② 大项 D 数据侦查完成。本轮实现：
> ① 判据回退（`AllCandidatesNonPersistent`；ini 说明/文档/verify 同步）；
> ② **大项 D：INFO 门槛（对话侧条件）** —— 全表扫对话（2077 QUST / 126347 INFO / 8541 带条件）：
>    按「自引用」分成入口类（含自己==0）/ 中性类（无自引用）/ 推进类（含自己==1，排除），
>    提取「引用别的任务」的进度条件，合成 **60 条任务 / 290 条对话 / 341 条条件**；运行时
>    「全部参与对话都有已知假条件 ⇒ 隐藏」（成对条件自动豁免 + 「引擎已开始」交叉验证 WARN）；
>    两例与记录级门槛完全一致（大器晚成↔孤立无援、亲爱的姐妹↔FFConstantZ05）；
> ③ E 小项：认领后 8 秒复算宽限期（读档抖动）、「入口引导目标更新未被脚本确认」去「入口」、
>    星图等待文案（「还开着：无」→「其它打开的菜单：…」）、界面「当前引导:?」与「0xNaN」。
> 构建/部署/verify（+22 条检查）全过。★ **同日实测（21:45~21:47 会话）大部分通过**：
> 读档认领**无复算抖动** ✓、`Mode=6` 推送 **19 条**（≈20）✓、INFO 门槛
> `58(过37/藏19/放行2/未知0)` ✓、星图链路两次完整 ✓、新文案全部生效 ✓；
> 交叉验证（设计内的 WARN）**抓出 2 条误藏**（『全数到期』RAD05 —— 第 11 轮验证过
> 「玩家能接到」的案例）⇒ **同日补丁：引擎已开始 ⇒ 放行**（隐藏条件收窄为
> 「引擎没启动 + 全部对话都有已知假条件」）。详见一·补三十三 / `docs/08` 第九节）
>
> 上一轮：2026-09-20（**第 47 轮（大项 C）：引导可用性收口 —— 「同 cell 常驻兜底」候选** ——
> 起因是玩家实测「营救机器人」的报告：「导航点被锁定在亚特兰蒂斯城的同一个地铁上、导航基本没用」。
> 两份日志取证：该任务（候选池只有 1 个：新亚特兰蒂斯城 The Well 内景里的损坏机器人，非常驻）
> **从未生效过**（玩家连点 30+ 次全部「目标尚未加载」），而世界里的标记**仍是上一条生效引导的残留**
> （脚本取不到时不改别名，见 `ApplyGuide`）⇒ 玩家观感 =「点了没反应、标记锁死在某处」。
> 数据侧的决定性发现：机器人 **3.3 米**处就有**常驻**的任务装置（`0x0208ECA6`
> 「损坏 Model G 家具标记」）却没被用上 —— 旧候选生成只从任务别名的引用里选。⇒
> ① 数据：`gen_guide_targets.py` 新增 `find_fallbacks`（第三遍扫描：为「全部候选都非常驻」的
> 任务在候选目标所在 cell 里找**最近的常驻引用**，作为候选池**最后一位**兜底；**76 条里 56 条**
> 补到，候选总数 875 → **931**、常驻 283 → **339**）；② 表：自动切片带上；③ DLL：判据
> `AllCandidatesNonPersistent` → **`FirstCandidateNonPersistent`**（只看**首选**候选 —— 兜底是
> 常驻的，不能把这一类判没；描述提示与测试模式 6 的 76 条清单照旧）。行为：远处点引导
> **落到兜底**（蓝点落在 The Well 的机器人附近、不再「没反应」），走进 The Well 后由「候选复算」
> 自动升级为精确目标。构建/部署/verify 全过（3 条新检查，含「营救机器人兜底链
> 0x08ECA5 非常驻 → 0x08ECA6 常驻」）。
> ★ **已实测通过（20:41~20:47 会话，日志侧全绿）**：
> ① 远处点「营救机器人」⇒ `引导请求：…｜候选 [2/2]「Ref_08ECA6」` → `引导已生效 脚本状态=1`；
> ② 走进 The Well ⇒ `引导目标已更新（候选复算）：0x0308ECA6 → 0x0308ECA5（[2]→[1]「G型」）`；
> ③ R ⇒ 星图 1.6 秒打开（第 1 次尝试）、Papyrus 地点链完整（采用地点 = SAlphaCentauri_PJemison）；
> ④ 接取后自动取消引导（`已接取，自动取消引导` → `引导已清除`）。
> 复查顺手排除一项「疑似 mod 问题」：Papyrus 里 26 条 `RevertToOpenStateOnCellLoad`
> 报错 = **官方数据/脚本的既有问题**（全游戏只有 The Well 的 29 条 ref 挂该脚本，其中
> VMAD 属性为空的 26 条会报错；与我们的 CELL 空壳无关）。详见一·补三十二 / `docs/05` 第二十一节）
>
> 上一轮：2026-09-20（**第 46 轮（大项 B）：引导可用性 + 提示收口 —— 「有目标、但此刻取不到」** ——
> 起因是日志复查抓到的实测形态：玩家在远处点「营救机器人」（候选池只有 1 个、且非常驻）⇒
> Papyrus 连报 5 次 `引导失败…取不到引用`、DLL **19 秒后静默放弃 + 界面回滚**，玩家体感
> 「按了没反应，过一会儿自己取消了」—— 与「不可引导要提示玩家」的需求冲突。数据侧统计：
> **209 条有目标的任务里 76 条的全部候选都非常驻**。⇒ ① 数据/推送：`QuestEntry.needsApproach`
> （运行时由候选持久位算出）+ 载荷**第 6 列**；② 界面：描述**提前**写明「导航目标要靠近目标区域
> 后才加载」，并新增结果码 **5 = 目标尚未加载**（保持竖条、不回滚、不播 OFF 音、不关菜单）；
> ③ DLL：点引导时全不可得 ⇒ `approachPending`，不安排 19 秒确认、不开星图，菜单关着时按
> **10/20/40/60 秒退避慢速重试**（≈35 分钟）—— 玩家靠近、cell 一加载即 `引导延迟生效`；
> ④ 脚本：HUD 提示机制通用化（`ShowNotice`），第一次失败发「目标地点尚未加载，暂时无法导航
> —— 靠近后会自动生效」（重复 3 次、冷却 5 分钟）。构建/部署/verify 全过（SWF 5 + DLL 6 + PEX 5
> 条新检查），**待实测**。详见一·补三十一 / `docs/05` 第二十节）
>
> 上一轮：2026-09-20（**第 45 轮（大项 A）：引导质量收口 —— 引导目标升级为「候选池」+ 质量分级** ——
> 玩家实测抱怨的两条（「群众心态」被引到 `FFNeonZ08_HeadlockEnableMarker001`、「平衡账目」被引到
> `FFNeonZ09_EnableRef`）的真因：旧排序「常驻优先」把**有名字的 NPC 发布者**压住了（31 条任务同形态）。
> ⇒ ① 数据侧：每条任务输出按质量排序的候选列表（有名字的 NPC > 可读名落脚点 > 通用名 NPC >
> 内部名落脚点；同级常驻优先；质量 top 6 + 最多补 2 个常驻备胎）—— **875 条候选 / 209 条任务**，
> **82 条**任务的第一候选被换成更好的目标；② 表/DLL：`kGuideCandidates[]` + 三件套
> （点引导时 `LookupByID` **预选**「此刻可得的最优候选」一次到位 / 脚本报状态 2 时**换候选**
> （循环，≤2 次）/ 菜单关着时**动态复算**（飞近自动升级回 NPC、目标失效自动回退））+ 认领带下标。
> 构建/部署/verify 全过（含 DLL 5 条 + 静态表 4 条新检查），**待实测**；
> ★ 同日补丁（实机日志复查：任务「朱诺的计谋」的星图里没有目标位置 —— 目标在飞船
> **动态创建的内部地点**，星图节点表只登记星系/星球层）：脚本在「地点链无行星」时
> **不打开星图**、改发 HUD 通知；DLL 配套不重试、超时文案改「按预期未打开」（见一·补三十第 6 节）。
> 详见一·补三十 / `docs/05` 第十九节）
>
> 上一轮：2026-09-20（**第 44 轮：R 键「星图位置永远是上一个导航的位置」—— 第 36 轮那条原版 dispatch 其实生效（第 37 轮结论被推翻）** ——
> 玩家反馈「点了阿基拉城任务板，再去点火卫二任务板，那么导向目标才会是阿基拉城」。
> 两份日志对齐（18:34~18:35 会话）锤定：DLL 写完引导通道后 **9 毫秒**星图就在屏幕上
> （`星图：已打开（… R 后约 0.0 秒）`，探测名已是第 41 轮修正过的 `GalaxyStarMapMenu`）
> —— 只可能是 AS3 在按键那一刻同步 dispatch 的 `MissionMenu_PlotToLocation`（它**确实生效**，
> 第 37 轮「引擎无 sink」的离线结论不成立）；而 Papyrus 三次「星图请求：1.5 秒后打开」
> 之后**一次都没有**「打开星图并设定航线」—— 星图被提前打开 ⇒ 星图是暂停菜单 ⇒ 游戏暂停
> ⇒ 脚本定时器冻结 ⇒ 补救调用永远跑不到。那条 dispatch 用的是**代理任务当前的目标位置**
> （此刻脚本还没 ForceRefTo）= **上一次**引导的目标 ⇒ 星图永远滞后一条（也解释了此前的
> 「都导航至沃利阿尔法星」（别名空着 ⇒ 默认焦点）与「有一次打开了其他任务的位置」）。
> ⇒ ① **删掉 dispatch**（AS3 只留音效 + note `星图:交给脚本(菜单关闭后)`）；
> ② 脚本侧星图待办改由**轮询节拍**推进（`StarMapPendingTicks` + `ProcessStarMapPending`：
> 节拍到点 = 菜单已关、游戏在跑，不会被暂停吃掉；旧的 1.5 秒延时定时器退役）；
> ③ DLL 重试等待 3 s → 1.5 s 与脚本新窗口对齐。构建/部署/verify 全过（含 4 条新检查），
> **待实测**。详见一·补二十九 / `docs/05` 第十八节）
>
> 上一轮：2026-09-20（**第 43 轮：任务板条目「按 R 没反应」—— 现场分诊（探针）+ 两处会吞掉现场/请求的缺陷** ——
> 玩家反馈「悬停阿基拉城任务板按 R，导航却导向纳里昂星」（= 旧引导「补给之行 → 星际诊所」还在）。
> 复查 17:54~17:57 会话：**全日志没有一条 `press=[R@…]`、也没有一条 `引导请求`**，而「界面状态」
> 显示玩家反复做「悬停 → 展开 → 悬停子项 → 关菜单」——「按键没进界面」与「事件到了但回调没跑」
> 两种病分不开。⇒ ① **新增分诊探针**：`ev=[XButton↓ XButton↑ …]`（`ProcessUserEvent` 收到的
> user event，名+按下/松开）+ `btn=[our:plot=1:map=0]`（选中项变化时的按钮启用状态）——
> 按 R 一次即可对号入座；② 顺带定位两个**吞现场/吞请求**的缺陷：`菜单关闭：界面最后状态` 原来
> 用 500ms 轮询缓存（按 R 与关菜单同周期时打印的是**按键前**的值）⇒ 关闭时刻改**现场读一次**；
> `PollGuideRequest` 只在菜单开着时跑而关闭时立刻重置 lastSeq ⇒ **「按 R 后菜单立刻关掉」的请求
> 会被丢掉** ⇒ 关闭前**补一次 poll**（日志 `补处理了关闭瞬间的引导请求`）；③ 顺手去掉 SET COURSE
> 的**置灰**（沉默失败 → 改回可点，没目标时点击给提示，符合「不可引导要提示玩家」的需求）。
> 构建/部署/verify 全过（含 5 条新检查），**待实测**。详见一·补二十八 / `docs/05` 第十七节）
>

## 第 56 / 57 轮（原 docs/09 十四·补二 / 补三）

> 2026-09-21 第 79 轮：从 docs/09 整体搬来（内容一字未改，只搬家）。仍然生效的结论见
> docs/09 同位置的索引段。本节保留原始 `## 十四·补二` / `## 十四·补三` 标题编号。

## 十四·补二、第 56 轮：第二次首测（09:22 会话）—— 两个**驱动器**缺陷（产品侧全对）

### 1. 结果：6 条用例 PASS 2 / FAIL 4

| 用例 | 结果 | 真因 |
| --- | --- | --- |
| `smoke` | PASS | 但「脚本已应用」一步曾误命中（§4） |
| `r26_menu_idle` | PASS | 断言命中 4 秒前的旧行 = 假 PASS（§2） |
| `r47_board_marker` | FAIL | 传送回执超时（传送其实成功，§3） |
| `r45_candidates` | FAIL | 同上（两次传送都被判超时） |
| `r48_info_gate` | FAIL | **假 FAIL**（§2）—— 产品行为正确 |
| `r44_starmap` | FAIL | `ui.tab`「桥没通」= 上游假 PASS 的连带（§2） |

### 2. ★★ 缺陷①：断言窗口起点被清 0（假 PASS 与假 FAIL 同源）

`CompleteStep()` 收尾时 `g_cur = StepState{}` 把 `logMark` 一起清成 0 ⇒ 下一步开跑时
`g_prevMark = g_cur.logMark = 0` ⇒ **所有 `scope=prev` 断言退化成「整个环形缓冲」**：

* 假 PASS：r26 的 `assert.log 推送成功 scope=prev` 在 09:22:30.173（**0 ms**）命中了
  **4 秒前**（09:22:25.927）那一行；本次真正的推送 09:22:30.985 才发生（实测 ~800 ms）；
* 连带：r44 因此没等新推送就调 `ui.tab` ⇒「桥没通」（桥是开菜单推送那一刻才解析的）；
* 假 FAIL：r48 的反向断言报文写着「窗口从 idx 0 起」，匹配到 09:22:25.109 的旧名单 ——
  而 09:23:16.607 的新名单 `INFO门槛=58(过38/藏18/放行2/未知0)` 里**已经**没有『无凭无据』。

修法：`CompleteStep` 保留 `logMark`（只重置其余字段）+ `EffectiveLogFrom` 兜底
（起点 0 ⇒ 退到本用例起点）。**红线三**：任何「窗口起点」都不许是 0（= 整段缓冲）。

### 3. ★ 缺陷②：传送回执要等 **cell 加载**（3 秒窗口太紧）

DLL 09:22:58.689 提交 `seq=6 传送玩家(MoveTo)`、09:23:01.695 放弃（3000 ms）；Papyrus 侧：

```text
[09:23:03] 测试命令：seq=6 op=6 结果=0 —— MoveTo [FCMissionBoardREF (0014D497)]（cell=CityAkilaCityTheRock）
[09:23:12] 测试命令：seq=9 op=6 结果=0 —— MoveTo [ (001DF853)]（cell=CityCydoniaMainLevel）
```

回执在 `MoveTo` **返回之后**才写（提交 → 回执 = **4.3 / 4.5 秒**）⇒ `kTeleportAckTimeoutMs = 20000`
（其余命令仍 3 秒），并把等待窗口写进日志（`传送等回执最多 … ms`）。

### 4. 顺带：断言要「只认产品日志」+ 收紧 smoke 的宽松断言

* r26 最后一条 `assert.log 引导已生效 scope=prev` 命中的是 harness 自己的 `[PASS]` 行
  （它原样复述正则与命中行 = **回声**）⇒ `LogFind` 跳过所有 `harness：` 行；
* smoke 的 `引导已生效|引导延迟生效|引导确认` 会匹配「引导确认：菜单还开着」（那句恰恰是
  「还没应用」）⇒ 收紧为 `引导已生效|引导延迟生效`。

### 5. 产物与验证

DLL **924672 B**（开发构建）/ 用例文件 **18759 B**（工作区与 MO2 字节一致）；
`build-saq.ps1 -SkipTable -SkipSwf -SkipPapyrus -Harness` ✓（ESM 重建后仍 3947 B）；
`verify_saq_build.py` **全过（+5 条）**：`harness 驱动器版本串`
（`驱动器 v56：日志窗口按步保留 / 传送 20 秒窗口`）、`传送回执等待窗口文案`、
`用例计划 · smoke 引导已生效断言（不带裸「引导确认」）` + 反向检查。

**待重测**：重进游戏读档 ⇒ `check_results.py` 期望 6 条全 PASS；日志里先确认 `驱动器 v56` 串。

## 十四·补三、第 57 轮：第三次实测（09:35 会话）—— 三条新真因（仍全在驱动器/用例侧）

### 1. 结果：6 条用例 PASS 2 / FAIL 4，产品侧依旧全对

| 用例 | 结果 | 真因 |
| --- | --- | --- |
| `smoke` | PASS（2640 ms） | ——（R 链路完整：推送 249 / `ui.tab` / 引导 813 ms / 星图 1.1 s） |
| `r26_menu_idle` | FAIL | 第一条 `ping` 超时 —— **smoke 的星图在清场之后才打开**（§2） |
| `r47_board_marker` | FAIL | `ui.selectchild` `err\|notfound` —— **没等 C++ 推送就切 tab**（§4） |
| `r45_candidates` | FAIL | `ui.selectchild ~0x0008EBDC` 报「没有记录号 0x000000」—— `~0x…` 解析漏写回（§3） |
| `r48_info_gate` | FAIL | **假 FAIL**：`INFO没到:` 断言的窗口被上一条断言消耗（§5）—— 产品行为正确 |
| `r44_starmap` | PASS（6625 ms） | 星图第 1 次尝试 1.2 s、`assert.nolog` 通过（上一轮的连带问题已消） |

### 2. ★ 真因①：星图在**清场之后**才打开（r26 的 `ping` 超时）

第 55 轮的「每条用例结束都清场」救不了这种时序：

```text
[09:35:18.315] smoke 最后一步 guide.clear → 用例结束 → 清场（此刻星图还没出现）
[09:35:18.495] 星图：已打开（GalaxyStarMapMenu 在屏幕上，R 后约 1.1 秒，第 1 次尝试）
[09:35:19.218] r26 第一条 `ping` 提交
[09:35:22.215] FAIL：命令没有回执（超时 3000 ms）—— 星图 = 暂停菜单 ⇒ 脚本定时器冻结
[09:35:22.215] 用例收尾：星图还开着 —— 已请求关闭（这次它才被看到）
```

R 链路的星图由**脚本节拍**在菜单关闭后 1~1.5 秒打开（第 44 轮的产品设计，正确）；清场
发生时它还没出现 ⇒ 落在下一条用例开头。

修法（驱动器 v57）：**「按过 R（XButton）」的用例结束后开一段 4 秒延迟复查窗口** ——
每帧查一次星图/任务菜单，观察到就关掉并**提前结束窗口**（脚本一次引导只开一次星图，
出现过就不会再来）；一直没出现才等满 4 秒（覆盖脚本「第 2 次尝试」才成功的情形）。
窗口没结束不开下一条用例。（只有 R 用例会触发，其它用例间隔仍是 700 ms。）

### 3. ★ 真因②：`ui.select/selectchild/expand` 的 `~0x…` 参数漏写回 `formId`

`ParseStep` 的 kUi 分支解析 `~0x0008EBDC` 成功，但**没有把解析值存进 `a_step.formId`**
（`a_step.text.clear()` 之后就丢了）⇒ `ResolveStepFormID` 拿到 0 ⇒
`静态表里没有记录号 0x000000（master 任意）`（r45 的原始报文）。
对照：`quest.reset ~0x0008EBDC` 正常（走 `needFormID` 分支，值存进了 `formId`）。
修法：`a_step.formId = uid;` 一行。

### 4. ★ 真因③：没等 C++ 推送就切 tab（r47 撞上内嵌回退数据）

`assert.log 入口=12(…)` 命中的是**收集阶段**的「运行时状态」行（推送**之前**打印）——
本机开菜单的第一次推送常因桥解析失败、0.8 秒后才重试成功：

```text
[09:35:30.506] 运行时状态：… 入口=12(可导航 12｜marker 10 原板 2 兜底 0 不可用 0) …
[09:35:30.507] [W] 推送失败（第 1 次，完整诊断）：Movie/ASMovieRoot 解析失败…
[09:35:30.534] 界面状态[1]：src=embedded raw=202 keep=199   ← 界面此刻还是**内嵌回退数据**
[09:35:30.542] ui.tab → ok|tab=7|mask=64|n=199（= embedded 的 keep 数）
[09:35:30.701] ui.selectchild 0x0021001E → err|notfound|n=199  ← 内嵌数据不含任务板入口
```

修法（用例）：`ui.tab` 前加 `assert.log 推送成功 scope=case timeout=12000`
——「内嵌回退数据不含入口条目」这条降级窗口（第 17 轮记录）在 harness 里第一次咬人。

### 5. ★ 真因④（用例红线）：两条断言共享同一批行时，第二条要 `scope=case`

`scope=prev` 的窗口起点 = **上一步开始**时刻 —— 会被上一条断言「消耗」掉：
同一 Tick 早先打印的行落在窗口之外。r48 的 `INFO门槛=` 与 `INFO没到:` 是**同一行**
（统计与名单一起打印），第二条因此超时；r26 的 `引导请求` / `引导确认：菜单还开着`
同理（本次没跑到，但必定假 FAIL）。修法：这两处的第二条断言改 `scope=case`。

### 6. 产物与验证

DLL **925696 B**（开发构建）/ 用例文件 **21093 B**（工作区与 MO2 字节一致）；
`build-saq.ps1 -SkipTable -SkipSwf -SkipPapyrus -Harness` ✓（ESM 重建后仍 3947 B；
日志里「移除 CELL 组 1 个（1271 B）」是 `--clean → 重建` 的**中间态**，最终 3947 B）；
`verify_saq_build.py` **全过（+5 条、0 MISS）**：`harness 驱动器版本串`
（`驱动器 v57：~0x 参数写回 / 清场延迟复查 / 日志窗口按步保留 / 传送 20 秒窗口`）、
`harness 清场延迟复查文案`、`用例计划 · r47 切 tab 前等推送成功（scope=case）`、
`用例计划 · r26 引导确认断言（scope=case）`、`用例计划 · r48 INFO没到断言（scope=case）`
+ 反向检查（旧的 `scope=prev` 写法必须不在）。

**待重测**：重进游戏读档 ⇒ `check_results.py` 期望 6 条全 PASS；日志里先确认 `驱动器 v57` 串
（没有它 = 还是旧驱动器）与 `用例收尾复查：星图在清场后才打开`（若星图又晚开）。

---

## 第 75 轮（四大势力开头任务：固定显示 + 固定排前四 + 深红舰队只给说明）

> 2026-09-21 第 83 轮：`docs/99-当前项目进度.md` 超过 100 KB 上限（AGENTS.md 的单文件限制），
> 把这一节**整体挪到这里**（内容一字未改，只搬家）。仍然生效的接口约定见 docs/99 的
> 「二十九（已迁 docs/95）」小节。

> 起源（玩家 2026-09-21）：见「最新更新」块。四条 = UC01 **Supra et Ultra**（超越极限）/
> FC01 **Job Gone Wrong**（官方中文名**枝节横生**）/ RI01 **Back to the Grind**（**重返职场**）/
> CF01 **Deep Cover**（深藏不露）—— 玩家给的 FC/RI 中文名是社区译名，认任务请用英文名 / EDID。

### 1. 数据（`tools/esm/gen_faction_entry_quests.py` → `ref/faction_entry_quests.json`）

* 每条记：`key`(EDID) / `faction`(原版 UI 枚举) / 势力名(中英) / `quest`(EDID+记录号+官方名)
  / `guide`(bool) / `incoming`(入边) / `noteZh`·`noteEn`(简要说明) / `evidence`。
* **构建期核验**（任一不符 ⇒ 不写产物、退出码 1）：① EDID ↔ 官方英文名逐字一致；
  ② 「第一环」结构证据：UC01/FC01/RI01 **无入边**、CF01 的入边恰为 `UC02@860`
  （`QF_UC02_002B1808.psc:706` `CF01.SetStage(205)`，与玩家「需先加入 UC 先锋队并完成
  其前两个任务」一致）；③ `guide=true` 的三条**必须有引导候选**；④ 阵营枚举与静态表一致；
  ⑤ 说明文本非空、无 Tab/换行、≤200 字符。
* **引导二次确认**（玩家要求「你可以二次确认」）—— 工具打印的首选候选：
  UC01 = 约翰·图阿拉指挥官（MAST 大楼，另有 3 个先锋队大厅/考试中心常驻 marker 兜底）；
  FC01 = 伊丽莎白·卡德威尔（**常驻**，盖尔银行）+ 丹尼尔·布雷克治安官等 6 候选；
  RI01 = 伊莫金·萨尔佐（龙神集团总部）+ 霓虹城 TerraBrew 的招聘亭常驻 marker，共 8 候选。
  三条都是「远处点引导 → 落到常驻候选；走近 → 自动升级精确目标」（第 45/47 轮机制）。
* **CF01 为什么「只给说明」**：它的接取点（图阿拉派你卧底）在 UC 线两个任务之后才存在，
  数据侧也证明它只有 UC02@860 这一条启动边 ⇒ **生成期清空它的 7 个引导候选**（`guide=false`），
  界面据此走「不可导航」通路（点击给提示、描述写明「按上面的说明推进」），不会引错地方。

### 2. 静态表 / 载荷 / 顺序（`gen_quest_table.py`）

* 静态表行尾 +1 列 `factionEntry`（-1 = 不是；0..3 = **固定顺序**：联合殖民地 → 自由星 →
  龙神 → 深红舰队），并新增 4 个数组：`kFactionEntryNamesZh/En`（势力名，日志用）、
  `kFactionEntryNotesZh/En`（简要说明，载荷用）。
* **载荷协议加第 9/10 列**（`简要说明` 中 / 英；其余任务空串）—— C++ 的 `BuildPayloadUtf8`
  与内嵌回退载荷（`SaqEmbeddedPayload.inc`，`payload_order()`）**逐条同序同步**：
  前 4 条 = 四大势力开头任务（固定顺序），接着 8 条 = 同伴任务（第 74 轮），其余保持表顺序。
* 表内引导候选 931 → **924**（CF01 的 7 条被清），有目标任务 209 → **208**。

### 3. C++（`SAQ_Decision.*` / `SAQ.cpp` / `SAQ_UI.*`）

* **离线层**（有单测）：`IsFactionEntryPinned(factionEntry)`（≥0 ⇒ 固定显示）、
  `IsGatePinned(companionPin, factionEntry)`（两个 pin 来源的**唯一**合并判据）、
  `PinnedOrderKey` / `PinnedOrderLess`（列表顺序：`group 0 = 势力`（rank = 下标）
  → `group 1 = 同伴`（rank = 同伴×2 + 入口优先）→ `group 2 = 其余`（两个「其余」返回 false ⇒
  stable_sort 保持原顺序）。
* `CollectAvailableQuests`：`pinned = IsGatePinned(...)` 用于三类门槛的放行（`DecideGateAction`
  的原语义不变）；统计新增 `factionPinned / factionGateMiss / factionOrdered / factionSamples`；
  排序整段改用离线层判据；**「已完成 ⇒ 隐藏」与「已接取（玩家日志）」两条例外照旧生效**
  （做过的任务不再出现在「可接」里 —— 固定显示只跳过「进度/INFO/链式」三类门槛）。
* ★★ **`FormatPinStats` 拆行（本轮纪律）**：两类「固定显示」的统计与名单**单独一行**
  （`固定显示（这两类条目跳过进度/INFO/链式三类门槛）：同伴固定=… 势力入口固定名单: …`）。
  起因：它们与三类门槛名单同处一行时，用例里的反向断言 `assert.nolog 链式没到:.*深藏不露`
  会**跨字段假命中**（`链式没到:` 在后面的 `势力入口固定名单: 深藏不露[…]` 之前）——
  与「断言窗口 / 共享同一批行」同一类纪律（见 `docs/09` 红线）。
* `SAQ_UI`：`QuestEntry` +`factionEntry`/`noteZh`/`noteEn`；载荷尾部补两列（说明也过
  「Tab/换行 → 空格」清洗）。

### 4. 界面（`MissionMenu.as`，SWF `stamp=57`）

* 解析第 9/10 列（`sSaqNoteZh`/`sSaqNoteEn`，缺列 ⇒ 空串，向后兼容）。
* `SaqDescriptionText` 新增第 5 参数（说明）：非空 ⇒ 描述第一句就是它（取代「这条任务
  当前可以接取」—— 对固定显示的四条不成立）；`param1 != true`（无导航目标）时，
  有说明的条目改走新函数 `SaqNoPickupNote()`（「没有可以直接导航的接取地点 —— 按上面的
  说明推进即可」），而不是普通任务那句「暂时还没有导航目标」（后者听着像 bug）。
* 新探针 `SaqPinNoteProbe` → `SAQ_Report` 的 `pin=[0x2c5401=加入联合殖民地先锋队…]`
  （第一条带说明条目的 uID + 描述前 24 字 —— 「说明真的进了描述」的运行期证据）。

### 5. 用例 / 校验 / 离线层

* 新 `[case:r75_faction_entry]`（用例 17 条）：reset 四条 → 断言 `势力入口固定=4(其中N条…放行)`
  + 名单含「超越极限」「深藏不露」+ 深藏不露**不在**「链式没到」名单 + `势力入口前置=4`
  + `order=[0x2c5401,0x29a8f0,0x2c9c97,0x9136`（**前四条精确**）+ `pin=[0x2c5401=加入联合殖民地先锋队`
  + 选中深藏不露按 R ⇒ `guide=…|该任务暂无导航目标:深藏不露`。
* r71_chain_cf 改判（深藏不露升格 ⇒ 断言「不再进链式名单」）；r74 的顺序断言放宽为
  「首条是 8 条固定显示条目之一」（精确顺序交给 r75）；r65 的「不选深藏不露」反向检查
  **收窄到 r65 段落**（r75 里那句是正当的）。
* verify：SWF +8 条（stamp 57 / `sSaqNoteZh/En` / `SaqNoPickupNote` / `SaqPinNoteProbe` /
  `pin=[` / 内嵌说明文本 ×2）、DLL +11 条（`势力入口固定=` / `势力入口固定名单:` / `势力入口
  前置=` / 固定顺序文案 / 说明与势力名数据）、静态表 +6 条（4 条标记与下标 [0,1,2,3] 且与
  ref 逐项一致 + 4 条样本：下标/阵营/候选>0 或 =0）、内嵌载荷「前 4 条 = 势力 + 随后 8 条 =
  同伴」「说明两列在」、用例计划 +6 条；并更新受影响的旧检查（候选 208、行尾四列正则、
  内嵌载荷表头 12 条）。
* 离线层：单测 **15 用例 / 667 断言**（+2 用例：势力固定显示与合并判据 / 列表顺序全序）；
  黄金快照 **11 件**（+`ref/faction_entry_quests.json`「势力 4（可引导 3）」）；
  一键 `tools\test\run-all-tests.ps1` 全过。

### 6. 判据（**待实机**，开一次游戏跑 r75）

`势力入口固定=4(其中≥1条被门槛判「进度没到」但放行)` + `势力入口前置=4(固定顺序…)` +
`固定显示（…）：… 势力入口固定名单: 超越极限[0x002C5401 势力=联合殖民地先锋队] …`
深藏不露[0x00009136 势力=深红舰队 跳过门槛]` + `order=[0x2c5401,0x29a8f0,0x2c9c97,0x9136,…]`
+ `pin=[0x2c5401=加入联合殖民地先锋队…]` + 深藏不露按 R 只给提示（`saqresults.json` 里
`r75_faction_entry` 全 PASS）。**眼睛**：列表最前四条 = 四条势力开头任务（图标分别是
联合殖民地 / 自由星 / 龙神 / 深红舰队徽记），右边描述第一句是「加入…」。

### 7. 已知取舍（记档）

* **「已完成 / 已接取」仍然隐藏**：固定显示只跳过三类进度门槛；玩家已经接过/做过的
  不会留在「可接」里（要重看说明得先 `quest.reset`）。
* **CF01 永远没有导航**（哪怕 UC02@860 已过）：玩家要求「只保留简要说明」；
  将来若想「条件到了就恢复引导」，把它放回 `guide_targets` 即可（数据侧一行开关）。
* 玩家给的 FC/RI 中文名（出错的差事 / 重回正轨）≠ 官方名（枝节横生 / 重返职场）——
  列表显示官方名，说明文本里也按官方叫法写（丹尼尔·**布雷克**治安官）。

---

# 第 58~61 轮（harness 复盘，自 docs/09 迁入 · 2026-09-22 第 84 轮）

## 十四·补四、第 58 轮：第四次实测（10:31 会话）—— 游戏端卡在加载画面（驱动器加防护 + 诊断）

### 1. 现象（玩家侧看到的 + 日志侧证实的）

自动会话跑到 r45 的「再走远」那一步（`teleport.entry 0x001DF853`）时，游戏**卡在加载画面**：

* 玩家侧：右下角一直转圈（加载中），画面上**同时**还有 HUD 的任务引导蓝点 ——
  正常游玩这两个元素不会同时出现；其实这正是「加载流程没走完」的表现：它没走到
  「关掉加载菜单、恢复 HUD」那一步；
* **Papyrus 日志**（`Documents\My Games\Starfield\Logs\Script\Papyrus.0.log`）**停在 10:34:37**，
  最后一行是 `[SAQ] 引导已应用：[elitecrewdebugscript < (0308ECA5)>]`，之后再没有任何行；
* **DLL 日志**一直写到 10:35:03（harness 全部用例结束，含两条收尾用例的陪跑超时）
  ⇒ 主线程还活着，卡住的是 **Papyrus VM**（`player.MoveTo` 这个调用永远没返回）。

### 2. 取证：这次传送为什么卡（时间线）

| 时刻 | 事件 |
| --- | --- |
| 10:34:35.393 | `seq=10 传送玩家(MoveTo)`（→ 新亚特兰蒂斯城 The Well 任务板）**回执**（Papyrus `结果=0`） |
| 10:34:35.431 | harness 提交 `seq=11 传送玩家(MoveTo)`（→ 赛多尼亚）—— **距上一条回执仅 38 ms** |
| 10:34:37 | 脚本节拍：先 `引导已应用`（候选复算升级到 `0x0308ECA5`），紧接着执行 seq=11 的 `MoveTo` ⇒ **卡住** |
| 10:34:55 | harness 放弃 seq=11（20 秒窗口到）⇒ r45 FAIL |
| 10:34:56~10:35:03 | r48 / r44 的 `ping` 全部超时（VM 冻结）⇒ 两条用例**陪跑** |
| 之后 | 游戏一直停在加载画面（用户截图为证），直到关掉进程 |

历史对照：这条「再走远」的传送在第 54~57 轮**从没跑到过**（都在更早的步骤失败退出），
而历史上成功的传送间隔是 **5~10 秒**（seq=6→9 隔 10 s、seq=9→10 隔 5 s）—— 只有这次是
「回执后 38 ms」。

### 3. 结论与处置（本轮修的是**驱动器**，不是产品）

* **不是产品缺陷**：这段时间里 mod 没做任何异常事（DLL 只读通道/记日志；脚本只做
  「应用引导」这件正常事）。卡死点在**引擎处理玩家 `MoveTo` 的加载流程**上 —— 而
  `MoveTo` 只由 harness 使用（玩家不会在 2 秒内被连传两次）。
* 驱动器 v58 三件事：
  1. **传送落地静默期**（`kTeleportSettleCleanMs` / `kTeleportMinGapMs` /
     `kTeleportSettleMaxMs`）：`teleport.*` 步骤要等「加载画面（LoadingMenu/FaderMenu）
     消失 + 连续静默 1.5 秒 + 距回执 ≥3.5 秒」才算做完 ⇒ 传送步骤比过去多 2~5 秒（正常）。
  2. **加载画面证据**：命令超时/落地等待时把**此刻打开的菜单**写进日志与证据
     （`LoadingMenu, FaderMenu` 出现 = 卡在加载画面）。旧的静态提示
     「确认此刻菜单是关的」已被替换（它什么也证明不了）。
  3. **卡死自动中止**（`MarkStuck` / `FillRemainingSkipped`）：确认卡死（有加载画面，
     或连续 2 次命令无回执）⇒ 剩余用例标 SKIP 并写明原因，不再逐条陪跑超时；
     同时用一条 **Ping 冲刷通道**（脚本只执行「最新序号」那条命令 ⇒ 万一游戏之后恢复，
     执行的是这条无害的 Ping，而不是那条排队的传送）。
* 判据：日志里的 `驱动器 v58：传送落地静默期 / 加载画面证据 / 卡死自动中止`。

### 4. 产物与验证（已做）

DLL **934400 B**（开发构建；工作区与 MO2 部署字节一致）、用例文件 **22248 B**（同）；
`verify_saq_build.py` **全过（423 条 OK、0 MISS）** —— 新增 5 条正向
（v58 版本串 + 落地静默期/超限判定/卡死/菜单证据文案）+ 2 条反向
（旧的静态提示「确认此刻菜单是关的」、旧 `驱动器 v57` 串必须不在）。

### 5. 待重测（重进游戏一次会话，什么都不用点）

1. `python tools\test\check_results.py` ⇒ 期望 **6 条全 PASS**；
2. 日志里先确认 `驱动器 v58` 串（没有 = 还是旧驱动器）；
3. 若又卡加载画面，应能看到
   `harness：  传送落地中（回执已到，但加载画面还没关：LoadingMenu, FaderMenu）`
   与 `harness：游戏端疑似卡死 —— …`，且 r48/r44 报的是 **SKIP（原因写明）**，
   而不是两条独立的 FAIL；
4. 传送步骤（`teleport.entry`）的耗时从 ~5 秒变成 ~8 秒属**正常**（落地静默期）。

## 十四·补五、第 59 轮（11:04 会话）：r47/r45 两条假 FAIL —— 落地静默期与回执解耦

### 1. 实测结果：6 条用例 PASS 4 / FAIL 2，两条 FAIL 同源、**产品与游戏侧全对**

| 用例 | 结果 | 真因 |
| --- | --- | --- |
| `smoke` | PASS（3047 ms） | 推送 249 / `ui.tab` / R → 引导 828 ms 生效 / 星图 1.1 s |
| `r26_menu_idle` | PASS（32515 ms） | 菜单里停 27 秒无假失败；关菜单 828 ms 生效 |
| `r47_board_marker` | FAIL（20609 ms） | `teleport.entry` 报「命令没有回执」—— **假 FAIL**（§2） |
| `r45_candidates` | FAIL（23594 ms） | 同上（第二次传送） |
| `r48_info_gate` | PASS（6141 ms） | `INFO门槛=58(过37/藏19/放行2/未知0)` + `INFO没到:` 名单 |
| `r44_starmap` | PASS（6656 ms） | 星图第 1 次尝试 1.4 s、`assert.nolog` 通过 |

### 2. 真因：`Poll` 是一次性消费，落地静默期被绑死在「读到回执那一 Tick」

时间线（r47；r45 同形态）：

```text
11:04:37.499  提交 seq=6 传送（→ 阿基拉城 CityAkilaCityTheRock）
11:04:41.907  传送落地中（回执已到，但加载画面还没关：LoadingMenu, FaderMenu）  ← 唯一一次检查
11:04:57.496  [FAIL] 命令没有回执（超时 20000 ms；此刻打开的菜单：无）          ← 20 秒 deadline
```

* Papyrus 侧两次 `MoveTo` 都成功：`11:04:41 seq=6 结果=0` / `11:05:06 seq=9 结果=0`，
  且整段 VM 一直在跑（seq=7~16 全部正常执行）；
* 超时瞬间「此刻打开的菜单：无」= **加载画面其实早就关了**；
* 病根在 `Poll` 的语义（SAQ_TestOps.cpp:347）：读到回执后 `g_busy = false`，之后永远
  返回 0；而 v58 把整套静默期检查写在 `rc == 1` 分支里 ⇒ 回执之后一个 Tick 也跑不到，
  20 秒 deadline 一到就报「命令没有回执」。
* 与第 58 轮现场（真卡死）的区分点 = **超时瞬间的菜单列表**：
  `LoadingMenu/FaderMenu` = 卡在加载画面；`无` = 正常落地（本轮就是）。

### 3. 修法（驱动器 v59）

1. **登记与推进分离**：收到回执只登记（`settleAckAtMs` + `settleAckDetail`），
   静默期检查移到 `rc==1` 之外、**每 Tick** 推进；
2. **deadline 只管「等回执」阶段**：静默期块位于 deadline 检查之前 ⇒ 回执到了之后
   「没有回执」的 20 秒窗口立即失效；加载画面 25 秒不结束仍走 `MarkStuck`（转 SKIP）；
3. **落地完成证据**：完成时步骤结果写明
   `…；落地静默期完成（回执后 N ms，加载画面已关 M ms）`。

### 4. 产物与验证（已做）

* DLL **935424 B**（开发构建；工作区与 MO2 部署字节一致）；用例文件 22248 B（未动）；
* `verify_saq_build.py` **全过（+3 条、0 MISS）**：`驱动器 v59：落地静默期每 Tick 推进` /
  `落地静默期完成（回执后` + 反向检查（`驱动器 v57`、`驱动器 v58` 必须不在）。

### 5. 待重测（重进游戏一次会话，什么都不用点）

1. `python tools\test\check_results.py` ⇒ 期望 **6 条全 PASS**；
2. 日志里必须出现 `驱动器 v59` 串（没有 = 还是旧驱动器）；
3. `teleport.entry` 步骤应回 PASS，且结果里带「落地静默期完成（回执后 …）」；
4. r47 应跑到「入口 12/12 + 引导落新建常驻 marker」、r45 应跑到候选池三段判据
   （这两条用例本轮因假 FAIL 中断，重跑里补齐）。

## 十四·补六、第 60 轮（11:26 会话）：r45 唯一 FAIL = **用例期望不可达**（走远后目标仍可得）

### 1. 实测结果：6 条用例 PASS 5 / FAIL 1；第 59 轮的传送修复**全部生效**

| 用例 | 结果 | 备注 |
| --- | --- | --- |
| `smoke` | PASS（2859 ms） | 推送 249 / `ui.tab` / R → 引导 828 ms 生效 / 星图 1.3 s |
| `r26_menu_idle` | PASS（32391 ms） | 菜单里停 27 秒无假失败；关菜单 781 ms 生效 |
| `r47_board_marker` | PASS（13265 ms） | 传送「落地静默期完成（回执后 3860 ms，加载画面已关 1516 ms）」；入口 12/12；引导落 `0x0E00090A`（新建常驻 marker 精确） |
| `r45_candidates` | FAIL（52891 ms） | 末步 `assert.log 候选复算：.*先保持` 超时 —— **期望不可达**（§2） |
| `r48_info_gate` | PASS（6109 ms） | `INFO门槛=58(过37/藏19/放行2/未知0)` + `INFO没到:` 名单 |
| `r44_starmap` | PASS（6687 ms） | 星图第 1 次尝试 1.3 s、`assert.nolog` 通过 |

★ 4 次 `teleport.entry`（seq=6/9/10/11）**全部 PASS**，结果都带
「落地静默期完成（回执后 3860~4172 ms，加载画面已关 1500 ms）」—— v59 的
「静默期每 Tick 推进」正式收口。

### 2. 真因：走远后「当前候选」**仍可得** ⇒ 候选复算按设计无动作

时间线（DLL 日志 × Papyrus 日志）：

```text
11:25:20.822  引导请求：营救机器人 → 候选 [2/2]「Ref_08ECA6」（赛多尼亚；[1] 未加载 = 预选正确）
11:25:21.758  引导已生效（脚本状态=1）
11:25:21.799  提交 seq=10 传送（→ 新亚特兰蒂斯城 The Well 任务板）
11:25:23.280  引导目标已更新（候选复算）：0x0308ECA6 → 0x0308ECA5（[2]「Ref_08ECA6」→ [1]「G型」）
11:25:28      Papyrus seq=10 结果=0 —— MoveTo [...(0E00090A)]（cell=CityNewAtlantisWell）
11:25:30      [SAQ] 引导已应用：[elitecrewdebugscript < (0308ECA5)>]      ← 别名持有了该引用
11:25:33.028  提交 seq=11 传送（→ 赛多尼亚）
11:25:37      Papyrus seq=11 结果=0 —— MoveTo [...(001DF853)]（cell=CityCydoniaMainLevel）
11:25:42.097  harness：落地静默期完成（回执后 4172 ms，加载画面已关 1500 ms）
11:25:42~57   候选复算按 1.5 秒节拍一直在跑，但**零日志**（= 每次 best == 当前候选）
11:25:57.120  [FAIL] 日志里没出现 /候选复算：.*先保持/（超时 15000 ms）
```

* **「零日志」= 当前候选仍可得**：`UpdateQuestGuideTarget` 只在 `!currentAlive`
  （`currentAlive = anyAlive && best <= candIndex`）时才写「先保持」；判定 [1] 可得时
  函数直接 `return`（无日志、无动作）；
* **为什么走远后它还可得**：目标引用被脚本 `ForceRefTo` 到代理任务别名之后
  （上表 11:25:30 那行），换星球后 `LookupByID` 依然查得到 —— 与「点引导前在远处取不到
  [1]」（11:25:20 预选落 [2] 的证据）正好形成对照；
* **产品行为是正确的那一半**：目标仍可得 ⇒ 保持精确候选（蓝点不抖、不退化）；
  只有「当前候选真的取不到」才需要观察期，而那要**读档 / 加载窗口**才会发生
  （第 49 轮补丁②的 21:57 现场）。

### 3. 处置（只动「用例 + 驱动器」，产品侧不动）

1. **新 op `guide.probe <任务>`**：只读探针，与产品的候选复算**同一套查询**
   （静态表候选池 + `TESForm::LookupByID`），把「每个候选此刻是否可得」写进日志与
   步骤结果 JSON —— 「零日志」到底是「按设计保持」还是「复算没跑」，一眼可判；
2. **r45 尾段改判**（`SAQ_TestPlan.txt`）：走远后 ① 15 秒内**不许出现**
   `引导目标已更新（候选复算）`（防「一取不到就降级」的抖动 —— 旧行为 1.5 秒内就降级）；
   ② 开菜单时 `引导状态：` 仍指向 `候选 [1]「G型」`；③ 走远前/后各插一条 `guide.probe` 留证；
3. **驱动器 v60**：版本串 `驱动器 v60：候选可得性探针（guide.probe）`；
4. **不在本轮**：观察期尾段（先保持 20 秒 → 降级）标注为「等自动读档能力落地后补用例」
   （进展文档「下一步」第 ② 项）。

### 4. 产物与验证（已做）

* DLL **937984 B**（开发构建；工作区与 MO2 部署字节一致）、用例文件 **23206 B**（同）；
* `verify_saq_build.py --dev` **全过（+3 条、0 MISS）**：
  `驱动器 v60：候选可得性探针（guide.probe）` / `候选可得性：` / `guide.probe`
  + 反向检查（`驱动器 v57`/`v58`/`v59` 必须不在）+ 用例计划三条
  （探针步骤在 / 「引导仍指向 [1]」在 / 旧「先保持」断言不在）。

### 5. 重测结果（11:55 会话已收口 —— 见「十四·补七」）

## 十四·补七、第 61 轮（11:55 会话）：6 条用例**全 PASS** —— harness 用例集正式全绿

### 1. 实测结果（判据逐条命中）

* 日志串 `驱动器 v60：候选可得性探针（guide.probe）`（11:55:57.965 启用）；
* `python tools\test\check_results.py` ⇒ **6 条全 PASS**（`sessionMs` 231015；
  结果 JSON `generatedAt 2026-09-21 11:59:48`）：

| 用例 | 结果 | 关键证据 |
| --- | --- | --- |
| `smoke` | PASS（3047 ms） | 推送 249 / `ui.tab` tab=7 / `引导已生效` 844 ms / `stamp=53` |
| `r26_menu_idle` | PASS（32484 ms） | 27 秒 `assert.nolog 引导未生效` PASS；关菜单 734 ms 生效 |
| `r47_board_marker` | PASS（13516 ms） | 落地静默期 3812 ms；入口 12/12；落 `0x0E00090A`（新建常驻 marker 精确） |
| `r45_candidates` | PASS（58375 ms） | 探针 `[1]取不到｜[2]可得` ⇒ 预选 [2]；走近升级 [1]；再走远 `[1]=可得` ⇒ 15 秒无降级 + `引导状态：[1]「G型」` |
| `r48_info_gate` | PASS（6062 ms） | `INFO门槛=58(过37/藏19/放行2/未知0)`；推到位后 `assert.nolog 无凭无据` PASS |
| `r44_starmap` | PASS（6687 ms） | 星图第 1 次尝试 1.3 秒 |

### 2. 第 60 轮判断的实测证实（要留的结论）

* **引导别名持有 ⇒ 走远仍可得**：`guide.probe` 走远后两次输出都是
  `[1]「G型」0x0308ECA5=可得｜[2]「Ref_08ECA6」0x0308ECA6=可得`
  ⇒ 「复算零日志」= **按设计保持**（不是没跑）—— 蓝点不抖；
* **观察期尾段仍无覆盖**：本会话没触发「候选取不到」（探针无 `=取不到`），
  该半段继续挂「等自动读档」；
* **无害 `[W]` 两类**：① 开菜单首次推送桥解析失败（0.8 s 重试自愈）；
  ② 11:59:04「引导目标更新未被脚本确认」（复算发生在加载窗口内、脚本 VM 冻结 ⇒
  确认窗口耗尽；通道保持不动，11:59:29 脚本状态=1 已应用）—— 全会话无 `[E]`。

### 3. 结论：大项 G 收口

用例集（`smoke` + r26/r44/r45/r47/r48）第 54 轮落地、第 55~60 轮七轮修复，
第 61 轮 6/6 全绿 ⇒ **可以把它当作「回归闸门」使用**（以后每次改产品都跑一遍，
判据 = 日志里的 `驱动器 v60` 串 + 6 条全 PASS）。

---

# 第 81~83 轮（自 docs/99 迁入 · 2026-09-22 第 88 轮，内容一字未改）

## 第 83 轮（自动测试首跑复查：22 条用例 19 PASS / 3 FAIL，全在「用例 / 驱动器」侧、产品全对）

> 上一轮：2026-09-21（**第 83 轮：自动测试首跑复查 —— 22 条用例 19 PASS / 3 FAIL，
> 三条 FAIL 全在「用例 / 驱动器」侧、产品全对**）：
> ① `r80_repeatable_npc` = **用例正则写错**：产品那行是**全角** `（…）`，用例写成半角转义
>   `\)` ⇒ 正则合法但**永不匹配**（同第 68 轮 `\\d` 一类病；症状 = 「日志里没出现 /…/」的
>   12 秒超时）。改全角括号 + 窗口 `scope=prev` → `scope=case`（该行由 menu.open **同一次
>   Tick 的产品处理**打印，窗口基准在个别时序下会落在它之后；r74/r75 对同类行就是这么写的）。
> ② `r81_landmark` 的第二段 `ui.tab` = 「桥没通」：菜单刚（重）打开那一两拍 DLL 还没解析出
>   `Movie/ASMovieRoot`（**同一拍的产品首次推送也失败**、0.84 秒后第 2 次成功）⇒ 驱动器
>   **v70** 让 `ui.*` 在步骤超时内**自动重试**（节流 250 ms）+ 用例补「等推送成功再切 tab」。
> ③ `r62_reload_observe` 首步 `ping` 超时 = **清场延迟复查窗口被提前结束**：第 57 轮把
>   「关掉残留的**任务菜单**」也算成「星图出现过」，而 r81 按过 R 的星图由**脚本节拍**
>   1.18 秒后才打开（22:52:19.732 按 R → 22:52:20.914 星图开）⇒ 没人关它 ⇒ 下一条用例的
>   `ping` 撞上「星图开着 = 游戏暂停 = 脚本定时器冻结」。v70：只有**真的看到并关掉星图**
>   才提前结束窗口；关掉星图后再留 700 ms 才开下一条。
> ④ 顺带：断言超时/反向断言报文带**三个打点**（窗口/本步/用例）；新工具
>   `tools/test/plan_regex_audit.py`（拿真实日志按用例分段体检所有断言正则 —— 本次一眼
>   锁定 r80 那条）。
> ⑤ 构建 + 部署 + verify（+4 条 DLL / 3 条用例计划检查，全过）+ 离线层 15 用例 / 667 断言 +
>   快照 13 件全过；**待实机重跑 22 条用例**（判据：`check_results.py` 退出码 0 + 日志
>   `驱动器 v70` 串）。★ 第 81/82 轮的**产品判据首跑已全中**（阿波罗地标任务按 R 落常驻兜底
>   `候选 [2/4]`、`stamp=60`、同伴文案）。

## 第 81 轮（「地球地标」系列任务（10 条）—— 复查 + 收录）

> 上一轮：2026-09-21（**第 81 轮：「地球地标」系列任务（10 条）—— 复查 + 收录**；
> 玩家反馈「我发现『地球地标』系列任务好像在可接任务列表里一个都没有，检查一下」）：
> ① 复查：11 条 `Landmark_*` 此前被第 7 轮的「地标」过滤规则排除（当时的判断
>   「走近即完成、无从『接』」**不准确**）。数据证据（新工具 `tools/esm/gen_landmark_quests.py`
>   的构建期核验）：每条地标任务都有一本对应的书（如 `Landmark_ApolloBook_NOCLUTTER`
>   「利文斯通爵士的第二篇日志」），书上挂着官方脚本 `defaultrefoncontainerchangedto`，
>   其 VMAD 属性 `QuestToSetOrCheck` = 对应任务、`StageToSet` = **100** ——
>   书被拾取 / 购买（进入玩家物品栏）⇒ 任务 SetStage(100)，这就是「接取」；
> ② 收录 10 条（Apollo / Cairo / Dubai / HongKong / London / LosAngeles / NewYork /
>   Osaka / Shanghai / StLouis）：`filter_reason` 的「地标」规则**豁免**（`is_landmark`）+
>   引导目标 = 书（或书商）的引用 + 同 cell / **world 级**常驻兜底（Cairo 的书在书店库存里
>   ⇒ 首选 = 书商 `AhnjongSinclairREF`，兜底 = 阿基拉城 world 级常驻 —— 外景 cell 没有
>   per-cell 常驻组，同第 80 轮）+ 说明文本（载荷最后两列：去哪拿哪本书）；
>   伦敦特例：书有 20 处放置（书店 / 书堆）⇒ `guide=false` 只给说明（同 CF01 通路）；
>   ★ **Opportunity（火星机遇号）不收**：没有可拾取的触发物（字节级复查）—— 它由主线
>   「发掘（Unearthed）」期间的博物馆展品互动触发，属主线流程内容；
> ③ 表 / 界面：`StaticQuestInfo` +1 列 `landmark` + `kLandmarkNotesZh/En`（C++ 说明来源
>   在 factionEntry 之后多一个分支）；**SWF `stamp=59`**（AS3 代码没改，重编译只为让内嵌
>   回退载荷与 C++ 载荷重新逐条对齐）；任务 261 → **271**、候选 924 → **960**、有目标
>   208 → **217**、内嵌回退 202 → 212 条；
> ④ 用例 +1（`r81_landmark`：阿波罗能选中 + 按 R 落**第 2 候选常驻兜底 0x0008C3E8** ⇒
>   `候选 [2/4]` + 伦敦只给说明）⇒ **22 条**；verify +20 余条（含 3 条用例计划断言）；
>   离线层 15 用例 / 667 断言 + 快照 **13 件**（+`landmark_quests.json`）全过；
> ⑤ 构建 + 部署 + verify 全过；**待实机**：跑 22 条用例（判据：`r81_landmark` 全 PASS +
>   `check_results.py` 退出码 0 + 日志 `stamp=59` 串）；眼睛：列表里能看到 10 条
>   「XX地标任务」，选中一条描述第一句是「拾取《…》」。

