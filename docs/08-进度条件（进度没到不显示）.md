# 08 · 进度条件（「游戏进度还不能让玩家接到 ⇒ 不显示」）

> 2026-09-20 第 35 轮。需求原文（AGENTS.md）：
> 「任务必须是可接的，如果当前游戏进度还不能让玩家接到对应任务，则不显示」。
>
> 这是需求里最后一个没有实质机制支撑的点（`docs/04` 第五节：🟡 只被「已开始」层
> 间接覆盖一部分）。本文记录：怎么把「进度门槛」这条判据从游戏数据里挖出来、
> 为什么只敢用其中一部分、怎么求值、怎么自证。

---

## 一、问题的形状

「进度没到」在游戏里有三种表达，成本完全不同：

| 表达 | 例子 | 能不能判定 |
| --- | --- | --- |
| 引擎自动启动类（faction/活动） | 引擎按条件自己 Start | ✅ 已被现有的「已开始」层间接覆盖 |
| **任务记录级条件（QUST 的 CTDA）** | 「大器晚成」要「孤立无援」完成 | ✅ **本文**（覆盖 261 条里的 7 条） |
| 对话选项条件（INFO 的 CTDA） | 「做完 A 后 NPC 才多一句对话」 | ❌ 工程量大（几十万条 INFO 的关联与求值），本阶段不做 |

真正的玩家感知大部分来自第二、三种；第二种有**直接可用的数据**：

```
FFConstantZ06「亲爱的姐妹」的记录级条件（xEdit 原文）：
  GetQuestCompleted(UC04 "Eyewitness") == 1      ← 前置完成
  GetQuestCompleted(FFConstantZ06 自己) == 0     ← 自己还没做过
  GetQuestCompleted(FFConstantZ04) == 1   [op=0x01]
  GetQuestCompleted(FFConstantZ05) == 1   [op=0x01]
```

## 二、CTDA 字节结构（实测定的，不是抄 commonlibsf）

`ref/quests.json`（原始 hex）与 `ref/xedit/quests_typed.txt`（xEdit 解码）配对验证
（`tools/esm/analyze_ctda.py`，p1 与 xEdit 的 Parameter #1 全部一致）：

```
偏移  类型   含义
0     u8     operator（0x00 = 等于；0x01/0xA0/0xA4 语义见下）
4     f32    比较值
8     u16    函数索引（GetQuestRunning=0x38 / GetStageDone=0x3B /
              GetInFaction=0x47 / GetIsID=0x48 / GetGlobalValue=0x4A /
              GetQuestCompleted=0x21F / LocationHasKeyword=0x232 …）
10    u16    （xEdit 显示为 Unused，常见 f9 44）
12    u32    参数 1（GetXxx 的任务引用；GetStageDone 里 = 目标任务）
16    u32    参数 2（GetStageDone 里 = stage 索引）
20    u32    Run On（0 = Subject；事件类是别的值）
24    u32    reference
28    i32    参数 3（常见 -1）
```

**未解的部分（本阶段刻意不碰）**：

* operator `0xA0/0xA4`（只出现在「位置/扫描/遭遇」类条件里）与 `0x01` 的精确语义
  没有反汇编定论 ⇒ **带这些位的条件一律不放行**（见第四节）。
* `0x01` 出现在 FFConstantZ06 的「Z04/Z05 完成」两条上，语义「像」OR，但没证据
  不猜 —— 代价只是这条任务的 gate 退化成「UC04 完成」（少过滤一点，不会误伤）。

## 三、★ 「门槛」的定义（保守到极致）

一条 CTDA 能当**进度门槛**，必须同时满足
（`tools/esm/analyze_ctda.py::build_gate`）：

1. `operator == 0x00`（等于比较，语义唯一确定）；
2. `Run On == Subject`；
3. 函数 ∈ { GetQuestRunning, GetQuestCompleted, GetStageDone }（DLL 侧可安全求值）；
4. 比较值 ∈ { 0.0, 1.0 }；
5. **引用的是「别的任务」**（`p1 != 自己`）。

第 5 条是踩过坑之后补的 —— **自引用条件不是进度门槛**：

> 第 11 轮实证：RAD05「全数到期」的记录级条件是 `GetQuestRunning(自己) == 0`，
> 引擎提前把它置成 running（flags=0x8001）—— 按「条件为假就隐藏」跑，会把一条
> **玩家明明还能接**的任务藏掉。这类自引用条件是引擎**启动流程的防重入守卫**，
> 描述的是「任务有没有在跑」，与「玩家能不能接到」无关。

任务的**全部门槛为真 ⇒ 显示；任一为假 ⇒ 隐藏（进度没到）；没有门槛 ⇒ 不做条件
过滤**。

当前覆盖（261 条里 **7 条任务 / 9 条条件**，全在基础游戏）：

| 任务 | 门槛 |
| --- | --- |
| FFNewAtlantis03 | UC04 未运行 |
| FFDenZ01 | UC01 到 stage 1000 |
| FFConstantZ06 | UC04 完成 |
| FFNewAtlantis01 | FFNewAtlantis03 到 stage 100 + UC04 未运行 |
| FFNewAtlantis02 | FFNewAtlantis03 到 stage 20 + UC04 未运行 |
| City_NA_Botany03（大器晚成 / Late Bloomer） | City_NA_Botany02（孤立无援 / Out On a Limb）完成 |
| FFNeonZ10 | City_Neon_Gang03 完成 |

> 其余 32 条带记录级条件的任务都含「位置/扫描/遭遇」类函数（依赖 event data /
> 当前星球），本阶段**不猜** ⇒ 放行。`docs/07` 的归因结论同样适用：宁可少过滤。

## 四、引擎侧求值（反汇编，非猜）

### 4.1 `GetStageDone` 拿到引擎实现

Papyrus `Quest.IsStageDone` 的实现（`ref/quest_natives2.txt`：`impl=0x20CB530`）
内部 `call 0xD0DBD0` —— 那才是真正的「stage 是否完成」：

```asm
00D0DBD0  mov r8d, [rcx+0x1AC]   ; stage 哈希表掩码
00D0DBD5  mov rbx, [rcx+0x1A0]   ; stage 哈希表（每项 4 字节：低 16 位 + 高 16 位下标）
00D0DBDC  movzx eax, dx          ; stage 参数
00D0DBE3  and r8, rax
00D0DBE9  mov r9d, [rbx + r8*4]  ; 0xFFFF???? = 空槽
00D0DBFC  mov r10, [rcx+0x140]   ; ★ stage 指针数组
00D0DC11  cmp word [rax+0x10], dx ; stage 对象 +0x10 = stage 索引
00D0DC58  test byte [rax+0x12], 1 ; ★ +0x12 bit0 = done 位
```

⇒ 新增知识：**TESQuest +0x140 = stage 指针数组、+0x1A0/0x1AC = stage 哈希表、
stage 对象 +0x10 = 索引、+0x12 = flags（bit0 = done）**。

### 4.2 DLL 怎么用它

* 直接调用 `bool __fastcall(TESQuest*, u16)@0xD0DBD0`（不再自己遍历哈希表 ——
  引擎的语义、引擎的实现，100% 一致）；
* 调用前做 **9 字节函数头特征校验**（`48 89 5C 24 08 44 8B 81 AC`）——
  换游戏版本后特征不符 ⇒ 完全不用它（返回 kUnknown ⇒ 放行），绝不错调；
* `__try` 只出现在**纯 POD 函数**里（MSVC 不允许 `__try` 与需要析构展开的对象
  共存 —— 项目已踩过，见 `SAQ_QuestState.cpp` 注释）。

`GetQuestRunning` / `GetQuestCompleted` 走已有的 `ReadQuestRuntimeState`
（flags 位 + 虚表核验，第 9 轮实测）。

## 五、判断语义（三态，保守优先）

`SAQ_QuestCond.cpp::EvaluateProgressGates` 返回四态：

| 结果 | 条件 | 行为 |
| --- | --- | --- |
| `kNoGates` | condCount == 0 | 正常显示 |
| `kPass` | 全部门槛为真 | 显示 |
| `kFail` | 有门槛为假 | **隐藏**（进度没到） |
| `kUnknown` | 目标取不到 / 状态读不了 / IsStageDone 不可用 | **放行**（按「没有门槛」处理） |

`kUnknown` 放行是刻意的：**误藏一条真任务比多显示一条难受得多**
（玩家报「某条明明能接却不显示」无法自解）。日志里 `未知` 计数会暴露这一类。

## 六、实现链

| 层 | 产物 / 入口 |
| --- | --- |
| 离线数据 | `ref/quests.json`（原始 CTDA hex）+ `ref/xedit/quests_typed.txt`（xEdit 字段名） |
| 提取工具 | `tools/esm/analyze_ctda.py` → `ref/ctda_parsed.json`（全量解析）+ `ref/ctda_gates.json`（门槛） |
| 表生成 | `tools/esm/gen_quest_table.py`（读 ctda_gates.json）→ `SAQ_QuestTable.h` 的 `kQuestConds[]` + 每行 `condBegin/condCount` |
| 求值 | `plugin/src/SAQ_QuestCond.{h,cpp}`（引擎函数 + flags 读取） |
| 过滤 | `SAQ.cpp::CollectAvailableQuests`（在「已完成」过滤之后）+ `RuntimeFilterStats` |
| 开关 | ini `[Filter] ProgressCond`（1 = 过滤（默认）；0 = 只统计/记日志不隐藏） |
| 日志 | `运行时状态：… 进度门槛=7(过N/藏M/未知K)` + `进度没到: 名字[0x… 没过的检查]` |
| 构建 | `build-saq.ps1` 第 1 步内自动跑 `analyze_ctda.py`（在 gen_quest_table.py 之前） |
| 验证 | `verify_saq_build.py`：DLL 5 条特征 + 静态表 7 条（结构/枚举/被检查任务记录号/计数） |

## 七、实测（2026-09-20 14:47~14:48 会话）

```
运行时状态：… 过滤=生效 进度门槛=7(过3/藏4/未知0)
进度没到: 再再次的申请[0x001E7941 0x002C5401 stageDone=0 期望 1] 亲爱的姐妹[0x002117CA 0x002AAE8D completed=0 期望 1]
          大器晚成[0x00270717 0x0027071B completed=0 期望 1] 不可告人的秘密[0x002E8297 0x002250C4 completed=0 期望 1]
```

| 判据 | 结果 |
| --- | --- |
| 门槛覆盖 = 7 条任务（9 条条件） | ✅ 与静态表 `condCount>0` 的行数一致 |
| `未知` = 0（求值器无死角） | ✅ |
| `藏` 只含「前置确实没做」的 | ✅ 四条前置全没做：全局 `已完成=1`；且玩家日志里正在「超越极限」（⇒ 其 stage 1000 未到）、「新亚特兰蒂斯城有棵树」（⇒ 同链的「孤立无援」未完成） |
| `过` 名单 | 过 3 = FFNewAtlantis03（新亚特兰蒂斯城介绍）+ FFNewAtlantis01（外交豁免权）+ FFNewAtlantis02（维护和平）——★ 日志不打印「过」名单，后两条「FFNewAtlantis03 到 stage 100/20」这一格属**推断**（下一轮补打印即可） |
| 回归（`drop[N]` / 入口 12 条 / 引导链） | ✅ 推送 268 = 261 + 12 − 1(已完成) − 4(门槛)；`drop[4]` 与历次一致；入口 12 全可导航；引导「霓虹城中的暗影」脚本状态=1；`[E]` 0 行 |
| `ini [Filter] ProgressCond=0` 对照 | ⬜ 未做（ini 当前 = 1） |
| 眼睛：藏名单里的任务确实接不到 | ⬜ 待玩家在游戏里确认 |

## 八、已知风险

* **语义假设**：「记录级条件为假 ⇒ 不会给接」对**外部引用**条件的成立概率很高
  （这些条目就是前置检查），但仍不是 100% 引擎级证明 —— 所以有 ini 开关、
  有名单日志，出问题一开关就能回到旧行为。
* **覆盖有限**：7/261 条（★ 第 48 轮起由「九、INFO 门槛」补上 60 条，见下）。
* **operator 未全解**：0x01/0xA0/0xA4 的语义没反汇编定论（0xA0/0xA4 只在
  遭遇类条件里出现）；一旦将来做遭遇类任务，需要先补这一课。
* `IsStageDone` 的硬编码 RVA（0xD0DBD0）绑定 1.16.244；换版本靠特征校验兜底
  （不符即放行，不会错调）。

---

## 九、★★ 第 48 轮（大项 D）：对话侧条件 —— INFO 门槛

> 需求原文（AGENTS.md）：「任务必须是可接的，如果当前游戏进度还不能让玩家接到对应任务，
> 则不显示」。第三节的记录级门槛只有 7 条；**玩家感知的大头在对话（INFO）侧**。

### 9.1 数据形状（离线全表扫描，`tools/esm/scan_info_gates.py`）

Starfield 与 Skyrim/FO4 不同：**对话挂在 QUST 的 children 组下**
（`GRUP type=10, label=quest FormID` → `DIAL` → `GRUP type=7` → `INFO`）——
**「INFO → 所属任务」从组链直接可得**（不需要过 DIAL 的 QNAM）。

Starfield.esm 实测规模：

| 项 | 数量 |
| --- | --- |
| QUST 记录 | 2077 |
| INFO 记录 | 126347 |
| 带 CTDA（条件）的 INFO | 8541 |
| 带 VMAD 的 INFO | 604 |
| 条件里「引用**别的任务**」的进度检查 | 613 条（涉及 307 个任务） |

### 9.2 ★ 判据设计（保守到极致，两例实测对照）

「任务自己的对话条件」不等于「接取门槛」，所以**先分类再合成**：

| 分类（按条件组里对该任务**自己**的引用） | 含义 | 参与判定 |
| --- | --- | --- |
| **入口类**：含「自引用 == 0」（如 `GetQuestRunning(自己)==0`） | 任务**还没开始**时才出现的对话 | ✅ |
| **中性类**：无自引用 | 无法区分入口/中途 | ✅ |
| **推进类**：含「自引用 == 1」（如 `GetStageDone(自己, x)==1`） | 任务**进行中**的对话（回报/分支） | ❌（绝不能拿来判接取） |

合成规则（`analyze_info_gates.py` → 静态表；运行时 `EvaluateInfoGates`）：

* **一条对话 = 一组条件（AND）**；只要「有一条**已知为假**」的条件 ⇒ 这条对话不可用；
* **全部参与判定的对话都不可用** ⇒ 隐藏（进度没到）；
* 任何一条对话「没有已知为假的条件」（条件全真 / 有不可判定项 / 求值不了）⇒ 显示；
* 任务里**任何一条参与判定的对话没有可判定条件** ⇒ 整条任务不进表（保守）；
* 只收 `want ∈ {0,1}` 的 `GetQuestRunning / GetQuestCompleted / GetStageDone`
  （形态与第三节记录级门槛完全一致）。

**为什么这套规则安全**：条件「成对」的任务（如 `X==0` 与 `X==1` 各有一条对话）
会自动豁免 —— 任何时刻至少有一条为真 ⇒ 永不隐藏。它只在「**全部**对话都被事件条件
挡住」时才隐藏，而这正意味着「玩家还没推进到这段剧情」。

**两例实测对照**（记录级门槛 ↔ INFO 条件完全一致）：

| 任务 | 记录级门槛（第 35 轮） | INFO 条件（本轮） | 一致 |
| --- | --- | --- | --- |
| 大器晚成（City_NA_Botany03） | 孤立无援（City_NA_Botany02）完成 | `GetQuestCompleted(City_NA_Botany02)==1` | ✅ |
| 亲爱的姐妹（FFConstantZ06） | UC04 完成 +（未解 op 的）Z04/Z05 完成 | `GetQuestCompleted(FFConstantZ05)==1` ×3 + `GetStageDone(FFConstantZ05,270)==1` | ✅ |

覆盖：**60 条任务 / 290 条对话 / 341 条条件**（全在基础游戏；DLC 的 INFO 待后续扩展）。
名单（按任务）：CF02/CF03/CF04/CF05/CF06/CF08_*、COM_Quest_*、UC01~UC09 系列、
FF 系列（大器晚成/孤立无援/亲爱的姐妹/外交豁免权/维护和平/特快专递……）、RI/RIR/FC/RAD、
City_* 杂项等。

### 9.3 实现链

| 层 | 产物 / 入口 |
| --- | --- |
| 全表扫描 | `tools/esm/scan_info_gates.py` → `ref/info_gates.json`（约 1 分钟，数据要刷新时手动跑） |
| 合成 | `tools/esm/analyze_info_gates.py` → `ref/info_gates_final.json` |
| 表生成 | `gen_quest_table.py` → `SAQ_QuestTable.h` 的 `kInfoGroups[]` / `kInfoConds[]` + 每行 `infoGroupBegin/infoGroupCount` |
| 求值 | `SAQ_QuestCond.cpp::EvaluateInfoGates`（单条条件求值复用 `EvalOneCond`） |
| 过滤 | `SAQ.cpp::CollectAvailableQuests`（记录级门槛之后）+ 交叉验证 WARN |
| 开关 | ini `[Filter] InfoCond`（1 = 过滤（默认）；0 = 只统计/记日志不隐藏） |
| 日志 | `运行时状态：… INFO门槛=60(过N/藏M/未知K)` + `INFO没到: 名字[0x… 第一个已知假条件]` |
| 交叉验证 | 要隐藏但「引擎已开始」的任务 ⇒ `[W] INFO 门槛要隐藏…但引擎已开始 —— 可能误判` |
| 验证 | `verify_saq_build.py`：静态表 4 条（结构/计数 290+341/切片越界/大器晚成门槛样本）+ DLL 6 条、SWF 1 条 |

### 9.4 已知风险

* **误藏**：若某任务的「全部事件条件对话」都被挡住、而它的接取另有途径（脚本/场景触发），
  会被误藏。缓解：成对条件自动豁免 + 交叉验证 WARN + ini 开关 + 名单日志（出问题可一条命令回到旧行为）。
* **DLC 未覆盖**：DLC 的 INFO 还没扫（保守：DLC 任务不做 INFO 判定）。
* **「无条件对话」被忽略**：任务自己的无条件对话大多是「运行中」的（引擎隐式要求
  quest running），拿它们当「入口」会让判据完全失效；忽略它们是**判据能工作的前提**，
  其风险由「成对豁免 + 交叉验证」兜住。

### 9.5 ★ 第 48 轮补丁（同日实机日志复查）：引擎已开始 ⇒ 放行

21:46 实测会话（`Mode=6`）里，交叉验证自动抓出两条**误藏**：

```
[W] INFO 门槛要隐藏『陌生人的善意』（0x0009BD47）但引擎已开始（0x00013497 stageDone=0 期望 1）
[W] INFO 门槛要隐藏『全数到期』（0x002AD3D5）但引擎已开始（0x0029EF60 completed=0 期望 1）
```

『全数到期』（RAD05）正是**第 11 轮验证过「玩家能接到」的案例**（Galaxy Bank 的 NPC
对话可接 —— 那一轮它被「已开始」位误挡，才定下「引擎已开始 ≠ 已接取」的共识）。

⇒ 判据收窄：**「引擎已开始」的任务不因 INFO 门槛隐藏**（放行），隐藏条件变为
「**引擎没启动** + 全部参与对话都有已知假条件」—— 引擎都没为它做启动准备的任务，
大概率确实还没到接取点。

日志形态：`INFO门槛=58(过37/藏19/放行2/未知0)`；放行单独记一行 INFO
（不再是 WARN —— 这已是设计内行为）；`INFO没到:` 名单只含真正隐藏的。

同会话其它判据全绿：读档认领**无复算抖动**（两次会话都没出现 `[1]→[3]→[1]`）、
`Mode=6` 推送 **19 条**（≈全部候选非常驻的 20 条）、星图链路两次完整
（R 后 1.5 秒 / 第 1 次尝试）、新文案生效（「其它打开的菜单：无」）。
名单里的 19 条抽查合理（任务链前置，如『孤立无援』要『新亚特兰蒂斯城有棵树』完成 ——
与玩家任务日志一致）。

---

## 十、★★ 第 67 轮：任务链门槛（第三条判据）—— 「编号任务链」的后续任务

### 10.1 起因（玩家实测，2026-09-21）

存档里**深红舰队任务线根本没开始**，但「可接任务」列表里出现了：

```
平衡账目 / 深藏不露 / 所罗门的宝藏 / … / 【菜鸟觐见】/ 【风驰电掣】/ 灰白夫人号 / …
```

红框两条 = `CF02`「菜鸟觐见」、`CF06`「风驰电掣」—— 它们是**编号任务链**
（CF01→CF02→…→CF08）的中后段环节，只能由前一个任务的**收尾阶段**自动启动，
玩家根本接不到。前两类门槛都拦不住它们：

* **记录级 CTDA**：CF02 有 32 条记录级条件，但都是**事件条件**
  （`GetIsCurrentLocation` / `GetInFaction` / `GetStageDone(自己)`…），
  没有「前置任务完成」这种形态（`analyze_ctda.py` 的 gate 子集一条都没收）；
* **INFO 门槛**：CF02 的 5 条参与对话里有 2 条是**成对条件的否定侧**
  （`CF01 stage 367 == 0`、`CF01 stage 205 == 0`，在「CF01 没做」时为真）
  ⇒ 判据「任一条对话没有已知假条件 ⇒ 放行」被这两条兜住 ——
  而它们只是「流程分支对话」，**不是接取入口**。

### 10.2 数据：从官方 Papyrus 源码里挖「启动边」

CK 自带全部官方脚本源码（`Data\Scripts\Source\Base`，5033 个 `.psc`）。
`tools/esm/gen_quest_chain.py` 扫出**跨任务启动调用**，只认「编号链路」形态：

| 条件 | 理由 |
| --- | --- |
| 调用方 A 的 EDID 是**纯编号**（`CF01`，不是 `CF01_xxx`） | 排除「子任务 / 随机事件」回写（`QF_BE_CF02_Ragana…` 这类也 SetStage） |
| A、B 字母前缀相同且 **B 的编号 = A 的编号 + 1** | 排除反向回写（CF06 的 stage 0~8 会回写 `CF05.SetStage(2200)`）与跨系列调用 |
| 调用发生在 A 的 **stage fragment**（`Fragment_Stage_XXXX_Item_NN`） | ⇒ 触发条件 = 「A 的 stage XXXX 已完成」（可用 `IsStageDone` 求值） |
| 跳过 `SetStage(0)` | 0 是重置 / 回退，不是启动 |

产出 **31 个目标任务 / 33 条边 / 覆盖 CF、UC、FC、RI、MQ、FFNewAtlantis、RIR 七个系列**
（`ref/quest_chain.json` → 静态表 `kChainGates`）。

★ **三个数字别混**（第 68 轮拿实机日志对齐过）：

| 口径 | 数字 | 说明 |
| --- | --- | --- |
| 工具输出 | 31 目标任务 / 33 边 | 含主线 MQ106 / MQ302b / MQ402 等 |
| 进静态表 | **25 条任务 / 26 条边** | `kChainGates`；verify 钉死这个数 |
| 运行时 `链式门槛=N` | 15:24 实测 **22** | 只数「走到链式判定」的任务 ⇒ 比表内少 3（「破坏」/「霓虹城中的暗影」/「希望之基」已被**更早的 INFO 门槛**先藏掉，不再进链式判定）|

玩家反馈的那条链（源码原文）：

```
CF02「菜鸟觐见」 ← CF01「深藏不露」@ stage 1000
   QF_BF01_00009136.psc:650  Fragment_Stage_1000_Item_00(){ … CF02.SetStage(10) … Stop() }
CF06「风驰电掣」 ← CF05「当世最强」@ stage 10000
   QF_CF05_0021D792.psc:1454 Fragment_Stage_10000_Item_00(){ … CF06.SetStage(10) … }
```

### 10.3 判据（边之间是「或」，与进度门槛相反）

`Decision::DecideChainGates`（离线层纯函数，有单测）：

* `chainCount == 0` ⇒ kNoGates（不是链式后续 ⇒ 不做链式过滤）；
* **任一条边 kPass**（前置 stage 已完成）⇒ kPass（放行）；
* 任一 kUnknown（前置取不到 / 求值器不可用）⇒ kUnknown（放行，保守）；
* **全部边 kFail** ⇒ kFail（**进度没到 ⇒ 隐藏**）。

求值侧 `SAQ_QuestCond.cpp::EvaluateChainGates`：每条边查一次
`IsStageDone(前置任务, stage)`（与进度门槛同一个引擎函数、同一套 9 字节特征校验）。

### 10.4 实现链

| 环节 | 位置 |
| --- | --- |
| 数据 | `tools/esm/gen_quest_chain.py` → `ref/quest_chain.json`（build-saq.ps1 第 1 步） |
| 表 | `StaticQuestInfo.chainBegin/chainCount` + `StaticChainGate kChainGates[]` |
| 求值 | `SAQ_QuestCond.cpp::EvaluateChainGates`（逐边 `IsStageDone`） |
| 聚合 | `SAQ_Decision.cpp::DecideChainGates`（单测钉死真值表） |
| 过滤 | `SAQ.cpp::CollectAvailableQuests`（INFO 门槛之后）+ 统计 |
| 开关 | ini `[Filter] ChainCond`（1 = 过滤（默认）；0 = 只统计/记日志不隐藏） |
| 日志 | `链式门槛=N(过A/藏B/未知K)` + `链式没到: 名字[0x… 前置 0x… stage N 未完成]` |
| 用例 | harness **`r67_chain` + `r67_chain_pass`**（A：前置没做 ⇒ 菜鸟觐见在「链式没到」名单里；B：推进 CF01@1000 ⇒ 本条用例窗口里不在） |
| 验证 | verify：静态表 4 条（结构/数组/完整 26 边 25 任务/菜鸟觐见样本）+ DLL 3 条 + ★ 第 68 轮 `驱动器版本串 v68` / `正则非法文案` + 用例计划 4 条（A/B 断言 + **统计断言 `scope=case` + 单反斜杠**）+ **通用守卫**（用例里每条断言正则必须可编译、不许出现连续两个反斜杠）|

### 10.5 已知边界（有意接受）

* **漏判（方向安全）**：只认「编号 +1」。像 `CF08`（由 `LC088` 系列终章任务驱动）这种
  跨系列交接识别不到 ⇒ 该类任务**不会被隐藏**（保持现状：可能误显，但绝不误藏）。
* **误藏**：若某链式任务还有别的接取入口（罕见 —— 剧情链后续大多是纯自动开启），
  会被隐藏。缓解：ini 开关 + 名单日志 + 26 条边全部可人工复核（都是势力线 / MQ 的
  第二环以后）。
* **DLC 未覆盖**：启动边只扫了基础游戏的 Papyrus 源码（DLC 源码不在同一目录）。
* **调试工具影响**：`quest.reset` 把前置任务的 stage 位清掉后，链式任务会重新隐藏
  （与游戏行为一致 —— 门槛跟引擎状态走，不做缓存）。

---

## 十一、★★ 第 69 轮：链式门槛的**扩展边**（编号链路之外的同类形态）

**起因**：复查第 67/68 轮的产物时确认 —— 「收尾 stage 启动下一个任务」不止编号链路
（`CF01→CF02`）这一种写法，同形态的还有：

| 线 | 边 | fragment 证据 |
| --- | --- | --- |
| Eleos 静修地 | 完全停止@1000 → 幽灵狩猎 → 驱魔 → 调停者 | 每条都是 `CompleteAllObjectives()` + `Stop()` + `下一个.Start()` |
| 霓虹城帮派 | 面试@500 → 展示力量@700 → 决战 | 注释 `Start up the follow up quest` / `Start up the next quest in the line` |
| 城市支线预启动 | 黑暗中的光芒@50 → 主要来源 | 注释 `Start Primary Sources Z Quest` |
| 城市支线预启动 | 新亚特兰蒂斯城介绍@1 → 橄榄枝 / 临别礼物 | 与 `FFNewAtlantis01TopLevels` 一起 Start |

这些任务与 CF02「菜鸟觐见」是**同一个问题类别**（玩家接不到，只能被前一个任务的
收尾/流程带出来）⇒ 并进同一套判据。

**数据**：`tools/esm/gen_quest_chain_extra.py` → `ref/quest_chain_extra.json`
（**人工逐条核实** + 构建期对着官方 Papyrus 源码**核验**每条边：任务/宿主/stage/
调用形态任一不符直接报错退出，不静默写错数据）；`gen_quest_table.py` 用
`merge_chain()` 与编号链**按 (宿主, stage) 去重后合并** ⇒ 表内 **33 条任务 / 34 条边**
（编号链 25/26 + 扩展边 8/8）。

**为什么不全自动扫**：宽口径扫描（`ref/_probe3.py`）会捞出 100+ 候选，其中大量误报 ——
对话管理任务（`Dialogue*`）的 stage 是**对话**驱动的（玩家能接）、`Patch_Update*` 是
存档迁移、stage 0 的 `close out previous quests in the series` 是**收尾**不是启动、
`BE_*` / `SE_KT*` / `RI_Support` 这类是引擎/故事管理器/随机任务管理器驱动（Papyrus 里
看不到真正的起点）。逐个复核后**有意不覆盖**的候选与理由都写在
`gen_quest_chain_extra.py` 的文件头（靴子/陌生人、错过目标/错过接驳点、夺回曾经/
博士的命令、RIR* 系列、彼方之力/无限的尽头、空间遭遇类 …）。

**判据 / 日志 / 开关**：与第 67 轮完全一致（`kChainGates` 共用；`链式门槛=N(过/藏/未知)`
+ `链式没到:` 名单 + ini `[Filter] ChainCond`）—— 运行时/决策层**零改动**。

**用例**：harness **`r69_chain_extra` + `r69_chain_extra_pass`**（霓虹城帮派线 A/B：
A：三条都回滚 ⇒「展示力量」「决战」都在「链式没到」名单里；B：推进「面试」@500 ⇒
本条用例窗口里「展示力量」不在名单）。

**验证**：verify 的期望值改为从**两个数据源现算**（先按「目标在表内 + 宿主是基础游戏」
过滤，再与 `kChainGates` 逐边对账：条数 / 有边任务数 / 边集合三者必须相等）+ 两条扩展边
样本（幽灵狩猎 ← 完全停止@1000、展示力量 ← 面试@500）。

**已知边界（新增）**：扩展边是**人工清单** ⇒ 复查到新的同类线时要手动加一条（工具会在
构建期核验它是否还成立）。这是有意的：自动扫描的误报率太高（会把玩家**能接**的任务藏掉，
那比误显更糟）。

---

## 十二、★★ 第 71 轮：链式门槛的**全量入边审计**（把「同类情况」一次查清）

### 12.1 起因（玩家实测）

存档里**深红舰队线根本没开始**，但「可接任务」列表里能看到：

* 「深藏不露」（CF01）——玩家已指出：它应该由**联殖先锋队线**做到一定程度后自动接取；
* 「宝藏号的结局」（CF08_SysDef）——深红舰队线**终局**任务；
* 「夺回曾经」（FFKeyZ01）——星钥站支线。

第 67/69 轮只拦住了 CF02/CF06 这类**编号链后续**；这一批的启动边写法 / 位置落在
前两轮规则的覆盖面之外（详见 12.2）。玩家要求：**顺带把其它任务的同类情况一并查清、一并修复**。

### 12.2 根因（三条任务各缺什么）

| 任务 | 真实启动边（官方 Papyrus 源码证据） | 为什么前两轮没覆盖 |
| --- | --- | --- |
| CF01 深藏不露（`0x00009136`） | `QF_UC02_002B1808.psc:706` Fragment_Stage_0860 `CF01.SetStage(205)` | **跨系列**（UC→CF）；编号链规则只认同前缀 +1 |
| CF08_SysDef 宝藏号的结局（`0x001B41D0`） | `QF_LC088_Space_0021E73C.psc:1266` Fragment_Stage_0200 `CF08_SysDef.SetStage(10)`（注释 commits to the SD path） | 跨系列 + 宿主 LC088_Space 非纯编号 |
| FFKeyZ01 夺回曾经（`0x001BB164`） | `QF_CF08_SysDef_001B41D0.psc:88` `FFKeyZ01.SetStage(66)`（终章简报开始） | 第 69 轮**已发现**、列为「有意不覆盖」（本轮复查收口） |

三类判据为什么全都不生效：

* **记录级 CTDA**：三条任务的 `condCount` 都是 0（没有可用的前置检查）；
* **INFO 门槛**：CF01 / FFKeyZ01 没有参与判定的对话；CF08_SysDef 的 7 组对话条件
  大量是**自引用成对条件**（自己 stage 1000 的 `0/1` 两侧）—— 按既有设计
  「成对条件自动豁免 + 入口类自引用在任务未开始时为真」⇒ **天然放行**；
* **链式门槛**：0 条入边（见上表）—— 这正是本轮修的对象。

### 12.3 全量入边审计（工具 + 方法）

`tools/esm/analyze_start_paths.py`（本轮升级）：

* 扫全部 5033 个 Papyrus 脚本 → **737 条**指向表内任务的启动调用
  （`Start` / `SetStage>0` / `CompleteQuest`，排除自调用）；
* 114 个「无对话·终端·Perk 起点」的任务按调用方分类（任务 fragment / 包 / 场景 / 系统脚本）；
* 逐条人工核实（读 fragment 上下文 + 注释 + 全部引用），按 12.5 的类别处置。

★ 审计工具的缺陷（本轮修）：覆盖集此前只读编号链数据 ⇒ 第 69 轮加过的扩展边目标
被误标「☆未覆盖」（现已合并 `quest_chain_extra.json`）。

### 12.4 新增的 18 条边（表内 33 任务/34 边 → **49 任务/52 边**）

| 目标 | 新边（宿主@stage） | 证据（源码形态） |
| --- | --- | --- |
| CF01 深藏不露 | `UC02@860` | `CF01.SetStage(205)`（先锋队线自动接取） |
| CF08_SysDef 宝藏号的结局 | `LC088_Space@200` | 注释 commits to the SD path（`CF07@1000 + CF08_SysDef@10` 同一 fragment） |
| CF08_Fleet 遗产的结局 | `LC088_Space@100` | 注释 commits to the CF path（`CF07@1500 + CF08_Fleet@10`） |
| FFKeyZ01 夺回曾经 | `CF08_SysDef@10` | `FFKeyZ01.SetStage(66)` |
| FFKeyZ02 博士的命令 | `CF08_SysDef@10` + `DialogueCFTheKey@850` | `SetStage(66)` / `SetStage(100)`（星钥站是剧情锁地点，对话同样受门控） |
| CFSD01 罪证的负担 | `CF01@310` | fragment 注释 **Kick Off CFSD01** |
| FC08 奋勇当先，视死如归 | `FC_EncryptedSlateQuest@1000` | fragment 注释 **Start FC08** + `Stop()` |
| FFNeonZ10 不可告人的秘密 | `City_Neon_Gang03@1100 / @1200` | `CompleteAllObjectives + FFNeonZ10.Start()`（两个分支互为「或」） |
| City_CY_RedTape02 官僚习气搪塞 | `City_CY_RedTape01@10000` | 收尾 fragment 只做 `RedTape02.Start()` |
| City_CY_RedTape03 官僚习气纠正 | `City_CY_RedTape02@10000` | 同上形态 |
| City_NA_Viewport02 流动资产 | `City_NA_Viewport01@2000` | 注释 conditions met for Quest 02 to run |
| City_NA_Viewport03 老板请酒 | `City_NA_Viewport02@1000` | 收尾 `Viewport03.Start() + Stop()` |
| MS05 过度设计 | `MS05Intro@1000` | Intro 卡片完成 → Start 正式任务 |
| MS06 第一次接触 | `MS06Intro@200` | 同上 |
| COM_Quest_Barrett_Commitment 承诺：巴雷特 | `Com_Companion_Barrett@900` | 注释 **Begin Quest** ⇒ `SetStage(50)` |
| COM_Quest_SamCoe_Q01 哈特家事 | `COM_Companion_SamCoe@650` | 好感里程碑启动个人任务 `SetStage(100)` |

（18 条边 / 16 个目标；构建期由 `gen_quest_chain_extra.py` 对着官方 Papyrus 源码
**逐条核验**——任务 / 宿主 / stage / 调用形态任一不符直接报错退出。）

### 12.5 有意不覆盖的类别（A~H，权威记录在 `gen_quest_chain_extra.py` 文件头）

| 类别 | 代表 | 理由 |
| --- | --- | --- |
| A 对话管理器（Dialogue*）驱动 | DialogueFCNeon → FFNeonZ01~09/12；DialogueCydonia → City_CY_* / FFCydoniaZ*；Paradiso / ECSConstant / AkilaCity 等 | 玩家在对应城市对话即可接（可触发）。**例外**：星钥站的 DialogueCFTheKey@850 **已收**（剧情锁地点） |
| B stage 0 的「收尾/关闭/初始化」 | Botany03@0、Well02@0、Viewport03@0、MQ204@10（shut down Lodge quests）、FFNewAtlantis06@0 | 形态是收尾不是启动（stage 0 语义未确证者搁置，可用 harness 实验定论） |
| C Quickstart / 调试 / 存档迁移 | DialogueCydonia@5/@8、LC082@10（Set by Startup）、LC088_Space@24（CF Initial Briefing Quickstart）、AutumnMacM@0（set stage and stop）、Patch_Update* | 不是正常接取路径 |
| D 引擎 / 管理器驱动 | BE_KT* → SE_KT*、MS01SpaceEncounter* → MS01、RI_Support → RIR*（疑似 Radiant，**若确证应改从表里剔除**）、LandyScript.RestartRAD05 | 离线无法证明「接不到」；RIR 留专项 |
| E 同伴里程碑存疑项 | Barrett@69 → 违约（SetStage(7401)+尾声计时器）、Andreja@750 → 难侍二主（SetStage(1000) 语义存疑） | 语义未确证，暂不覆盖 |
| F 主线流程 | MQ_TempleQuest_01 / MQ304b / MQ401·402 推进边 | 主线不在关注范围 |
| G 跨线「推进/回写」 | CF08_SysDef@1000 → RAD02、UC04@1000 → RAD02、RedTape03@850/860 → Psych01 等 | 目标早已运行（不是启动） |
| H 已由其它判据覆盖 | FFNewAtlantis01（记录级 CTDA） | 不加边也安全 |

### 12.6 验证（本轮）

* 数据：`gen_quest_chain_extra.py` 核验通过（**24 任务 / 26 边**）→ 静态表 **49/52**；
* 离线层：决策单测 **12 用例 / 620 断言** + 黄金快照（链式边 **34 → 52**，审查过 diff）全绿；
* harness：新增 **3 条用例** —— `r71_chain_cf`（CF 终局四条在「链式没到」名单）、
  `r71_chain_redtape`（A：02/03 被藏）、`r71_chain_redtape_pass`（B：推进 01@10000
  只放行 02，03 仍被藏 —— 顺带验证「或」语义的精确性）；
* verify：部署后跑（本轮游戏在运行 ⇒ 部署由守护任务在游戏退出后自动完成）。

### 12.7 已知边界

* 「深藏不露」的用例断言（UC02@860 未触发）只在**先锋队线没做到 860 的存档**上成立 ——
  存档状态变化会让该断言失效（不是产品回归）；
* FFKeyZ02 的两条边是「或」（任一触发即放行），避免误藏；
* 城市对话（DialogueFCNeon 等）驱动的任务有意维持现状 —— 玩家走到对应地点即可接，
  与「进度锁」不同类；
* ~~RIR01~07 若确证是 Radiant（无限生成），正确处理是**从静态表剔除**而不是加边（留专项）。~~
  → ★★ **第 77 轮已收口**（不是全 Radiant：RIR01~RIR05 是主线环节 ⇒ 收边；
  RIR06/RIR07 是可重复 radiant ⇒ 用管理者边压到「已经给过这个差事」之后），见十二·补。

---

## 十二·补、★★ 第 77 轮：龙神（Ryujin）全线的「RIR 留专项」收口

### 补.1 起因（玩家实测）

存档**只接了龙神的入口任务**（重返职场 RI01），「可接任务」列表里却能看到后续的
「人生处处有意外」（RIR04）。玩家问：「是不是链式判据有问题，把入口任务接到了，
后续任务就全显示了？」

### 补.2 真因：不是判据算错，是**这条线一条边都没有**

RIR04（0x002A8319）的 `chainCount == 0` + `condCount == 0` + `infoGroupCount == 0`
⇒ 进度 / INFO / 链式三类门槛全放行 ⇒ 照常显示。第 69/71 轮把它留在「[D] 有意不覆盖 ·
留专项」（`RI_Support → RIR*` 疑似 Radiant）—— **整条龙神线（RIR01~RIR07 + RI02/RI03）
都没进链式门槛**，所以玩家看到的不止一条（同类症状）。

### 补.3 定性（官方数据 + Wiki 双证）

| 类 | 任务 | 证据 | 处置 |
| --- | --- | --- | --- |
| **主线环节** | RIR01 领先一步 / RIR02 全新故事 / RIR03 通行是关键 / RIR04 人生处处有意外 / RIR05 保持领先 / RI02 挑拨离间 / RI03 最高机密 | `Run Once` 标记 + 只由**前一个任务的收尾 fragment** 启动（见下表）；NAM3 的「Go on a radiant mission」指**目标点**随机（ChosenLocation 别名），不是可重复接取 | 加链式边 |
| **可重复 radiant** | RIR06 处理特工 / RIR07 口舌之力 | NAM3「Go on a radiant mission…」+ Wiki「Masako 任务：主线结束后可重复接取」+ 数据侧只有 `RI_Support` 的职位管理器起点（对话 fragment 驱动） | 用 managerial 边压到「已经给过这个差事」之后（那一刻任务已进玩家日志 ⇒ 本来就不显示）；接取点由「龙神集团」入口条目覆盖 |

新增 8 条边（`tools/esm/gen_quest_chain_extra.py` → `ref/quest_chain_extra.json`，
构建期对着官方 Papyrus 源码逐条核验）：

| 目标 | 宿主@stage | 调用（官方 Papyrus 原文） |
| --- | --- | --- |
| RIR01 领先一步 | RI01@1300 | `QF_RI01_002C9C97.psc:600` `RIR01.Start()`（面试晋升后自动接取） |
| RIR03 通行是关键 | RIR02@300 | `QF_RIR02_002C2561.psc:81` `RIR03_QuestStartKeyword.SendStoryEvent()` |
| RI02 挑拨离间 | RIR03@300 | `QF_RIR03_002BABF3.psc:201` `RI02.Start()` |
| RIR04 人生处处有意外 | RI02@600 | `QF_RI02_002AAC9C.psc:413` `RIR04.Start()`（**玩家报告的那条**） |
| RIR05 保持领先 | RIR04@300 | `QF_RIR04_002A8319.psc:160` `RIR05_QuestStartKeyword.SendStoryEvent()` |
| RI03 最高机密 | RIR05@10000 | `QF_RIR05_002A3945.psc:196` `RI03.SetStage(100)`（首次完成时） |
| RIR06 处理特工 | RI_Support@610 | `QF_RI_Support_002A8318.psc:505` `RIR06.SetStage(1)` |
| RIR07 口舌之力 | RI_Support@700 | `QF_RI_Support_002A8318.psc:575` `RIR07.SetStage(1)` |

★ **新 op `SendStoryEvent`**：龙神线的移交写法是 `<目标EDID>_QuestStartKeyword.SendStoryEvent()`
（故事管理器据此启动目标），不是直接调用 —— 工具按同一口径核验（宿主该 stage 的 fragment
里能找到这一调用；只认不带 `AndWait` 的形态，后者是 `RI_Support` 那一侧的写逻辑）。

### 补.4 规模与判据

* 扩展边 **24 任务/26 边 → 32/34**；表内 **60 任务 / 63 边**（`kChainGateCount = 63`）；
* 用例 **`r77_chain_ryujin`**（A）：六条后续（含 0x002A8319）都在「链式没到」名单里 ——
  ★ 前提 = 本存档的「重返职场」还没到 stage 1300（存档状态变化会让该断言失效，
  与 r71_chain_cf 的存档前提同一类）；
* 用例 **`r77_chain_ryujin_pass`**（B）：推进「挑拨离间」@600 ⇒「人生处处有意外」不再被藏，
  「保持领先」仍被藏（「或」语义）；
* verify：与两个数据源**逐边对账**（63/63 边 · 60/60 任务 · 边集合一致）+ 三条新样本
  （人生处处有意外 ← 挑拨离间@600 / 领先一步 ← 重返职场@1300 / 通行是关键 ← 全新故事@300）
  + 两条用例计划断言；黄金快照摘要（链式边 55 → 63，其余逐项不变）。

### 补.5 已知边界

* 「人生处处有意外」等任务在**推进到对应环节之后**照常显示（那是真的可接了）；
* B 段用例会短暂启动 RIR04（fragment 副作用：给 Imogene 一件 quest 道具），收尾 reset 清场；
* 尚**未实机**：等一轮 19 条用例跑完再收口。

---

## 十二·补二、★★ 第 78 轮：同类问题全量复查（审计升级）+ DLC 的 INFO 门槛

### 补二.1 起因

玩家（2026-09-21，第 77 轮收口后）：「再检查一下其他任务是不是也有类似现象，一并修复」。

### 补二.2 审计工具的三处盲区（`tools/esm/analyze_start_paths.py`）

| 盲区 | 旧行为 | 修法 |
| --- | --- | --- |
| DLC 源码没扫 | 目标集只有 `Starfield.esm` 的任务 ⇒ **DLC 的起点调用全被丢掉**（假收敛） | 目标集改成**表内全部 master**（读 `quest_table_debug.json`）；源码本来就在 `Base\{SFBGS00D,SFBGS003,DLC03}\Fragments\*`，一并 rglob |
| 故事事件看不见 | 正则只认 `<任务>.Start/.SetStage/.CompleteQuest` | 新增 `STORY` 正则：`<任务>_QuestStartKeyword.SendStoryEvent(AndWait)?()`（第 77 轮龙神线那种形态） |
| 看不出「危不危险」 | 只报调用类别 | 每条候选附**门槛状态**（chain / cond / info 计数 + 同伴/势力开头固定显示）；`[暴露]` = 三类门槛全 0 且不固定显示 |
| 宿主解析不健壮 | 只按文件名的 EDID 精确查表 ⇒ 改名/大小写就「未解析」（**3 条假警报**） | 兜底：① EDID 大小写不敏感；② 文件名末尾 8 位记录号（低 24 位）查表 |

重跑结果：**5033 个 .psc / 769 条起点调用 / 116 条候选（46 条「暴露」）**；
「对话管理器驱动」（`Dialogue*` / `*Convo`）类维持第 71 轮的 A 类判定（玩家在对应地点
对话即可接 ⇒ 照常显示）。

### 补二.3 DLC 的 INFO 门槛（本轮主要修复）

官方 DLC **不发 .psc**（ShatteredSpace / SFBGS050 只有 BA2 里的 **.pex**）⇒ 改用
**ESM 里的对话条件**（DIAL/INFO 的 CTDA）：

* `scan_info_gates.py` 加 `--self-master` / `--out`：逐 master 扫（4 份 `info_gates*.json`）。
  ★ 文件内 FormID 形态（探针实测）：**该 DLC 自己的记录引用写 `0x01xxxxxx`**（它自身的
  「文件内 master 序号」= 1），**基础游戏写 `0x00xxxxxx`** —— 按此把前置解析成
  `(master 名, 记录号)`（`preMaster` / `pre` 字段）。
* `analyze_info_gates.py` 合并四份扫描；前置 master 用**表里的 master 顺序**
  （kQuestMasters：Starfield.esm 恒为 0，其余按名字排序）写 `quest_master`。
* 结果：门槛 **60 → 80 条任务 / 418 对话 / 492 条件**（DLC 20 条 = SFBGS00D 2 /
  SFBGS050 7 / ShatteredSpace 11；含跨 master 前置：虚妄的得诺者 ← 安可列娅个人任务、
  失踪的华庭号 ← 地球舰队侵袭@75、隐蔽入侵 ← 地球舰队侵袭@75）。
* 空存档模拟（按 DLL 的 `DecideInfoGates` 语义）：**26 条会真被藏** ——
  地球舰队：失踪的华庭号 / 隐蔽入侵 / 生命之器 / 电力短缺 / 前哨三连；
  基础游戏 21 条（与旧口径一致）。
* ★ 为何破碎空间 MQ02~MQ06 仍放行：参与对话里含**只带 want=0 的「中性组」**
  （成对条件的一半）⇒ 按既有判据「任一对话可能可用 ⇒ 显示」⇒ 不隐藏（保守，宁缺勿滥）。

### 补二.4 为什么没给破碎空间主线加猜测性门槛（记档）

* 官方不发 .psc；新工具 `tools/re/pex_strings.py`（抽 .pex 字符串表 = 属性名 / 函数名 /
  `Fragment_Stage_NNNN`）显示 **MQ_Shell / DialogueHV\* 与各 MQ 之间是双向引用**
  ⇒ 定不出「谁启动谁」的方向；
* CTDA 记录级条件里只有 4 条 DLC 任务有跨任务引用，且多为**成对 / 非表内**引用
  （当门槛会永久隐藏或毫无作用）；
* Wiki 能给出先后顺序，但**没有**「前置到达哪个 stage 才算完成」的数据 ⇒ 加边就得赌
  完成阶段，赌错 = **误藏**（比误显更糟）⇒ 本轮**不做**，记为已知边界；要收口需要
  「ShatteredSpace 主线的实机推进 + harness 验证」这条路。

### 补二.5 基础游戏剩下的候选（本轮复查结论：维持现状）

| 任务 | 唯一/主要起点 | 为什么不动 |
| --- | --- | --- |
| 靴子 `City_NewAtlantis_Z_TheBoot` | 陌生人的善意（TheKindnessOfStrangers）**stage 0** 的 fragment | stage 0 语义未确证（第 71 轮 B 类）；它是「兄弟任务」，随 陌生人的善意 一起被带出（后者有 INFO 门槛） |
| 错失目标 ↔ 错过的接驳点 | `City_GG_Mark@1/@2 ↔ City_GG_Connections@1000` **互启**，起点不可确证 | 第 69 轮起即记为「互启且起点不可确证」；TIF 只读它的 stage |
| 食宿 `FFLodge01` | `MQ101@2728 FFLodge01.SetStageNoWait(10)`（注释 **Completing this quest starts up FFLodge01**） | 玩家开局 MQ101 一完成它就有 ⇒ 照常显示（正确） |
| 步兵 `MS01`（Groundpounder） | `MS01SpaceEncounter01~03` 的**推进** stage（830/850） | 空间遭遇驱动；离线无法证明「接不到」（第 71 轮 D 类）⇒ 维持显示 |
| 独立盟之巅 `Rad01_LIST` | `Rad01_Convo`（对话 hub）+ StartGameEnabled + CTDA（LocationHasRefType） | 对话/遭遇可达 ⇒ 照常显示 |
| 新亚特兰蒂斯城有棵树 `City_NA_Botany01` | 只有「收尾」调用（`City_NA_Botany03@0 SetStage(200)`），起点不在 psc | 城市支线、玩家可达；无「接不到」证据 ⇒ 维持 |
| 网络入侵 `City_NA_Well01` | **世界物件脚本**（`City_NA_JunctionBox04Script` / `Well01_Lever01Script`）+ `Well02@0` 收尾 | 世界交互可达（撬接电箱 / 拉杆）⇒ 不是「接不到」 |

### 补二.6 判据与产物

* verify：INFO 期望值改成**从 `ref/info_gates_final.json` 现算**（写死会掩盖漏跑生成器）
  + 2 条 **DLC 跨 master 样本**（`{ 0x0000599Fu, 2u, 2u, 1u, 75u }` /
  `{ 0x0001D3C6u, 3u, 2u, 1u, 940u }`）；链式对账仍 63/63 · 60/60；
* 构建：`build-saq.ps1` 的 1/6 步新增「四个 master 的 INFO 扫描」——**原始扫描缺失时才跑**
  （已存在即跳过，日常构建零开销）；
* 黄金快照：`info_gates_final.json` 60/290/341 → **80/418/492**、静态表同步（其余逐项
  不变：候选 924 / 进度门槛 9 / 链式 63 / 同伴 4 / 势力入口 4）；
* 离线层全过（15 用例 / 667 断言）；构建 + 部署 + verify 全过；**待实机跑 19 条用例**。

---

## 十三、同伴好感度任务（第 74 轮）

### 13.1 起因（玩家要求）

> 「把所有达到一定好感度才能接到的同伴任务**固定**在可接任务列表里，任务名称前面写上
> 同伴的名字，并在提示里提示到达一定好感度才能接取」
> 随后补充：「注意，链式关系的后续任务还是不要显示，只显示入口任务」。

即：同伴线的**入口**（个人任务）要一直看得见、名字带同伴前缀、描述里说明好感度要求；
同伴线的**后续**（承诺/恋爱任务）仍然按「进度没到就不显示」处理。

### 13.2 数据（`ref/companion_quests.json`；`tools/esm/gen_companion_quests.py`）

每位同伴 2 条（表内共 8 条），按「入口 / 后续」分类：

| 同伴 | 入口（个人任务，`pin=1`） | 后续（承诺任务，`pin=0` + 启动边） |
| --- | --- | --- |
| 莎拉·摩根 | 难忘逝者 `COM_Quest_SarahMorgan_Q01`（0x2C7C11） | 承诺（0x27B667）← `COM_Quest_SarahMorgan_Q01@1000` |
| 巴雷特 | 违约 `COM_Quest_Barrett_Q01`（0x369AB） | 承诺（0x1C7185）← `Com_Companion_Barrett@900` |
| 安德列娅 | 难侍二主 `COM_Quest_Andreja_Q01`（0x21ECD0） | 承诺（0xB8633）← `COM_Companion_Andreja@2000` |
| 萨姆·科尔 | 哈特家事 `COM_Quest_SamCoe_Q01`（0x263262） | 承诺（0xDF7AD）← `COM_Companion_SamCoe@825` |

* **同伴显示名**从官方 strings 的承诺任务名解析（「承诺：巴雷特」/「Commitment: Barrett」
  ⇒ 冒号后半段）—— 不写死；解析失败直接报错退出。
  ★ 踩坑：**不能从静态表读**（`quest_table_debug.json` 的名字会被 `gen_quest_table.py`
  加上前缀 ⇒ 重跑时解析出的同伴名变成「承诺」）；必须读官方 strings。
* **启动路径核验**（对官方 Papyrus 源码 `Data\Scripts\Source\Base`，任一不符 ⇒ 不写产物）：
  ① 宿主 `COM_Companion_<同伴>` 存在（EDID 大小写不敏感 —— 巴雷特那条真实 EDID 是
  `Com_Companion_Barrett`）；② 入口有宿主侧 `<Q01>.Start/.SetStage(` 调用；
  ③ 后续必须找到启动边（直接调用 / `StartCommitmentQuest()` 所在 stage fragment /
  莎拉·摩根的**场景 fragment** 启动 ⇒ 退一步用「个人任务完成 @1000」当必要条件）；
  ④ `COM_CompanionQuestScript.psc` 里 `StartPersonalQuest` / `StartCommitmentQuest` /
  `COM_AffinityLevel_1_Friendship` / `_3_Commitment` 齐全（好感度机制证据）。
* ★ 宿主任务被 DLC override（SFBGS050 里也有 `COM_Companion_*`）⇒ 取 **Starfield.esm**
  那条（与 `gen_quest_chain_extra.py` 同一约定），否则启动边会落到 DLC 的 master 空间。

### 13.3 运行时判据

* 静态表新增两列：`companion`（同伴下标，-1 = 不是）+ `companionPin`
  （1 = **入口**同伴任务 ⇒ 固定显示）；名字在生成期就加了同伴前缀
  （「巴雷特：违约」/「Barrett: Breach of Contract」；承诺任务换位成「巴雷特：承诺」）。
* `SAQ.cpp`：`companionPin == 1` 的任务跳过**三类门槛**（进度 / INFO / 链式）——
  判据统一走离线层 `Decision::DecideGateAction(verdict, pinned)`：
  `kFail` ⇒ `kHide`（普通）/ `kPinBypass`（同伴入口 ⇒ 放行 + 统计）。
  **不**跳过「已完成 ⇒ 隐藏」（做过的任务不该再出现在「可接」里），也不跳过
  `testMode` 过滤（诊断工具，与产品语义正交）。
* 日志：`同伴固定=N(其中M条被门槛判「进度没到」但放行)` + `同伴固定名单: …`
  （每条带 `同伴=<名>` 与 `跳过门槛` 标记）；M > 0 = 固定显示真的起了作用。
* 载荷第 8 列 = `companionPin`；界面（`SaqCompanionNote`）把描述首句换成
  「这条同伴任务需要与同伴的好感度达到一定水平后才能接取（在那之前会固定显示在这里）」
  —— 普通任务的首句仍是「这条任务当前可以接取。」。SWF 指纹 `stamp=55`。
  ★ **第 82 轮改写**（起因：玩家反馈「好感度不够时这些引导意义何在」—— 旧句让人以为
  要跑到某处「接取」）：新首句 = 「会在好感度达到一定水平后**自动开始**、不需要找地方
  接取；好感度要靠带这位同伴一起冒险来提升；「设定航线」引导到的是这位同伴当前所在的位置」；
  同伴条目的**尾部落点文案**（`SaqDescriptionText` 第 4 参数分支）同步换成「引导到这位
  同伴的位置」，不再写「接取地点」。机制依据：官方 `StartPersonalQuest()` 直接
  `PersonalQuest.Start()`（无接取环节）+ StoryGate 计时器要求 companion following
  player（好感度只能靠同行的里程/事件积累）。SWF 指纹 `stamp=60`。
* **后续**（承诺任务）不做固定显示：启动边（见 13.2）并入 `kChainGates`
  ⇒ 「全部边没触发 ⇒ 隐藏」，与 CF02「菜鸟觐见」同一套判据（链式边 **52 → 55**）。

### 13.4 判据（harness `[case:r74_companion]`）

1. `推送成功` 后出现 `同伴固定=\d+(其中\d+条被门槛判「进度没到」但放行)`；
2. `同伴固定名单:` 里有 `巴雷特：违约[0x000369AB …]`（名字已带同伴前缀）；
3. `巴雷特：承诺[0x001C7185 …]` 在**链式没到**名单里（后续不被固定显示放行）。

★ 断言 3 依赖「宿主 `Com_Companion_Barrett@900` 未触发」的存档状态（同第 71 轮的
「深藏不露」用例），换存档跑可能失效（不是产品回归）。

### 13.5 验证与产物

* 数据：`gen_companion_quests.py` 核验通过（4 同伴 / 8 任务，入口 4）→ 静态表
  `companionPin` 4 条、链式边 55；离线层单测 **13 用例 / 635 断言**（新增
  `同伴固定显示_入口任务跳过三类门槛`）；黄金快照 +1 件（`companion_quests.json`）。
* verify 新增：SWF 5 条（`bSaqCompanion` / `SaqCompanionNote` / 中英文案 /
  内嵌载荷名字前缀）+ `stamp=55`；DLL 4 条（统计 / 名单 / 跳过门槛 / 同伴名）；
  静态表 1 组（8 条标记 / 4 条入口 / 名字表与 ref 逐项一致 + 4 条样本）；
  链式边期望值改为**三个数据源**现算（55/55、边集合一致）；用例计划 +3 条断言。
* 产物：DLL（开发构建）/ SWF 728592 + 728722 B（`stamp=55`）/ ESM 3947 B /
  PEX 17666 B；用例集 **16 条**（新增 r74）；MO2 部署 = 工作区（verify 全过）。
* **待实机** → ✅ **第 76 轮已实测收口**（19:54~20:00 会话，`r74_companion` 全 PASS）：
  `同伴固定=4(其中1条被门槛判「进度没到」但放行)` + `同伴分组前置=4(按同伴分组，入口在后续前)`
  + 名单四条（巴雷特：违约 / 安德列娅：难侍二主 / 萨姆·科尔：哈特家事 `跳过门槛` /
  莎拉·摩根：难忘逝者）+「巴雷特：承诺」在**链式没到**名单里 + `order=` 首条为固定显示条目
  （精确顺序由 r75 保证）；
  眼睛判据 = 打开「可接任务」tab ⇒ 入口同伴任务在列表里、名字带同伴前缀、
  右侧描述带好感度提示；承诺任务不出现（或只在链式前置满足后出现）。

### 13.6 「把它们放在一起」（同日续；玩家反馈：「我观察到任务板都是按顺序放在一起的」）

需求：同伴任务要**集中**在列表里（与任务板条目在末尾成块相对称）。

* **C++**（`SAQ.cpp::CollectAvailableQuests`）：收集完成后 `std::stable_sort` —— 同伴条目
  **前置**到列表开头，按 `companion` 下标分组、同一位同伴的「入口」（`companionPin=1`）
  在「后续」之前；非同伴条目保持表顺序（稳定排序），任务板入口照旧追加在末尾。
  `QuestEntry` 新增 `companion`（**只用于排序**，不进载荷 —— 名字已带前缀、pin 单列）。
* **内嵌回退载荷**（`gen_quest_table.py::payload_order`）：与 C++ 逐条同序 ——
  「C++ 推送失败的头 0.3~0.8 秒」用这份数据，顺序不一致会让列表开头跳变
  （与第 65 轮「列不对齐」同一类协议纪律）；黄金快照新增该 `.inc` 一件。
* **日志**：`同伴分组前置=N(按同伴分组，入口在后续前)`。
* **界面顺序探针**：`SAQ_Report` 新增 `order=[uID,…]`（`MissionsList.SAQ_OrderProbe`；
  只看 `bSaqAvailable` 条目 = 我们 tab 里看得见的行）—— 载荷顺序 → `BuildMergedList`
  → `InitializeEntries`（`_loc2_` 正常段保持输入顺序）这条链路只靠读代码保证，
  `order=` 给出**运行期真实顺序**。SWF `stamp=56`（同日续，见 13.5）。
* **判据（harness r74 追加两步）**：`同伴分组前置=\d+(按同伴分组…)` 统计行 +
  `assert.ui order=[0x(21ecd0|369ab|263262|2c7c11)`（第一条 = 4 条入口同伴任务之一）。
* **verify**：内嵌载荷前 8 条 = 按同伴分组、入口在后续前的 8 条同伴任务（+ 反向检查
  第一条不再是 0x351A「平衡账目」= 未分组的旧顺序）；SWF `order=[` / `SAQ_OrderProbe`
  / `stamp=56`；DLL `同伴分组前置=`；用例计划两条新断言。
* 观察到的效果（实机眼睛判据）：打开「可接任务」tab，头几条就是同伴任务，按同伴名字
  成块（安德列娅 → 巴雷特 → 萨姆·科尔 → 莎拉·摩根；每条前面带同伴前缀）。

---

## 十四、★★ 第 75 轮：四大势力开头任务（固定显示 + 固定排前四；深红舰队只给说明）

### 14.1 起因（玩家要求，2026-09-21）

> 「把四大势力开头任务（UC 超越极限 / 自由星 枝节横生 / 龙神 重返职场 / 深红舰队 深藏不露）
> **固定显示**，并固定排在可接任务列表的**前四个**；除了深红舰队，其他都能正常引导
> （你可以二次确认）；深红舰队**只保留简要说明**，显示在说明里即可。」

⇒ 这是继第 74 轮（同伴「入口」任务）之后**第二类「固定显示」**：三条门槛（进度 / INFO /
链式）判定为「进度没到」时**放行**，且这两类条目在列表里**前置**。区别只在于：

* 第 74 轮的来源 = `companionPin`（好感度入口任务）+ 描述里提示好感度；
* 第 75 轮的来源 = `factionEntry`（四大势力开头任务）+ 描述里写**各自的简要说明**
  （加入方式 / 前置条件，载荷最后两列）。

### 14.2 数据（`ref/faction_entry_quests.json`；`tools/esm/gen_faction_entry_quests.py`）

四条 = 各自势力线的**第一环**：UC01 `Supra et Ultra`（超越极限）/ FC01 `Job Gone Wrong`
（官方中文名**枝节横生**）/ RI01 `Back to the Grind`（**重返职场**）/ CF01 `Deep Cover`
（深藏不露）。构建期核验（任一不符 ⇒ 不写产物）：

① EDID ↔ 玩家对照表里的英文名逐字一致（**认任务用英文名**：玩家给的 FC/RI 中文名是社区
译名）；② 「第一环」结构证据：UC01/FC01/RI01 在链式门槛数据里**没有任何入边**、CF01 的
入边恰为 `UC02@860`（`QF_UC02_002B1808.psc:706` `CF01.SetStage(205)` —— 与玩家「需先加入
UC 先锋队并完成其前两个任务」一致）；③ `guide=true` 的三条必须有引导候选
（UC01 = 图阿拉指挥官 + 3 个常驻 marker；FC01 = 常驻的伊丽莎白·卡德威尔（盖尔银行）等；
RI01 = 伊莫金·萨尔佐（龙神集团总部）+ 霓虹城招聘亭常驻 marker）；④ 阵营枚举与静态表一致；
⑤ 说明文本非空、无 Tab/换行、≤200 字符。

### 14.3 判据与顺序（离线层有单测）

* `Decision::IsFactionEntryPinned(factionEntry ≥ 0)`、`IsGatePinned(companionPin, factionEntry)`
  （**唯一**的合并放行判据）、`DecideGateAction`（原语义不变：只有 kFail 才隐藏）；
* 顺序 = `PinnedOrderKey` / `PinnedOrderLess`：`group 0 = 势力开头任务`（rank = 下标，
  即固定顺序 UC → 自由星 → 龙神 → 深红舰队）→ `group 1 = 同伴`（rank = 同伴×2 + 入口优先）
  → `group 2 = 其余`（比较器对两个「其余」返回 false ⇒ `stable_sort` 保持表顺序）；
  内嵌回退载荷（`payload_order()`）与 C++ **逐条同序**；
* 日志拆行：两类固定显示的统计与名单进**独立一行**（`FormatPinStats`）——理由见
  `docs/09` 十四·补十二（同一行里两个名单会让反向断言跨字段假命中）。

### 14.4 深红舰队「只给说明」：**生成期清空引导候选**

CF01 的接取点（图阿拉派你卧底）在 UC 线两个任务之后才存在 ⇒ 数据里 `guide=false`，
`gen_quest_table.py` 在候选循环里**清空它的 7 个引导候选**（候选 931 → 924、有目标 209 → 208）。
界面因此走既有的「不可导航」通路：点击给 OFF 音 + 提示（`该任务暂无导航目标:深藏不露`）、
描述里写「它没有可以直接导航的接取地点 —— 按上面的说明推进即可」（`SaqNoPickupNote`），
而不是普通任务那句「暂时还没有导航目标」（后者听着像 bug —— 与第 46 轮「不可引导也要
提示玩家」的需求同源）。

### 14.5 描述（载荷第 9/10 列 → `SaqDescriptionText` 第 5 参数）

* C++/内嵌载荷 `Q` 行尾部 +2 列 = 说明（中 / 英，其余任务空串；旧载荷缺列 ⇒ 空串）；
* 描述第一句：有说明 ⇒ 说明本身（取代「这条任务当前可以接取」——对固定显示的四条不成立）；
  否则照旧（同伴提示 / 「当前可以接取」）；
* 运行期证据：`SAQ_Report` 的 `pin=[<uID>=<描述前 24 字>]` 探针（`SaqPinNoteProbe`）。

### 14.6 判据（harness `[case:r75_faction_entry]`，见 `docs/09` 十四·补十二）

`势力入口固定=4(其中≥1条…放行)` + 名单含「超越极限」「深藏不露」+ 深藏不露**不在**「链式没到」
名单 + `势力入口前置=4(固定顺序…)` + `order=[0x2c5401,0x29a8f0,0x2c9c97,0x9136,…]`（前四条精确）
+ `pin=[0x2c5401=加入联合殖民地先锋队…]` + 深藏不露按 R 只给提示。

★ **已实测收口**（第 76 轮 19:54~20:00 会话）：`r75_faction_entry` **全 PASS**（29 步）——
八条断言逐条命中：`势力入口固定=4(其中1条被门槛判「进度没到」但放行)` /
`势力入口前置=4(固定顺序：联合殖民地→自由星→龙神→深红舰队)` / 名单两条（含深藏不露的
`跳过门槛` 标记）/ `assert.nolog 链式没到:.*深藏不露`（拆行纪律生效）/
`order=[0x2c5401,0x29a8f0,0x2c9c97,0x9136,0x21ecd0,0x369ab]`（**前四条精确**，随后接同伴固定
显示条目）/ `pin=[0x2c5401=加入联合殖民地先锋队…]`（说明进了描述）/ 深藏不露按 R ⇒
`guide=0|0|该任务暂无导航目标:深藏不露`（不发引导请求、只给提示）+ 收尾 `guide.clear`。

### 14.7 已知边界

* **「已完成 / 已接取」照旧隐藏**：固定显示只跳过三类**进度门槛**，做过的任务不再出现在
  「可接」里（这符合「可接任务」的语义）；
* CF01 永远没有导航（哪怕 UC02@860 已过）—— 玩家要求「只保留简要说明」；要让它在条件到了
  之后恢复引导，把 `guide_targets.json` 里的候选放回（数据侧一个开关）；
* 这四条是**硬编码的固定顺序**（静态表下标 0..3）：以后要加/换势力开头任务，
  改 `gen_faction_entry_quests.py` 的 `ENTRIES` 即可（工具会重新核验）。

## 十五、★★ 第 79 轮：门槛名单的**截断留痕**（日志缺陷 —— `kMaxSamples = 40`）

### 15.1 起因（自动测试唯一 FAIL 的复查）

* 20 条用例集 19 PASS / 1 FAIL —— 唯一 FAIL = `r77_chain_ryujin` 的断言
  `assert.log 链式没到: .*领先一步\[0x002C572B`（3000 ms 超时：「日志里没出现」）；
* 三层取证（结论 = **产品全对**）：
  * **日志**：A 段 `链式门槛=49(过4/藏43/未知0)`、B 段 `过5/藏42` —— 打印出的名单里
    其余五条龙神线后续都在（「人生处处有意外」在 B 段被放行也是预期）；
  * **静态表**：「领先一步」的边 = `RI01@1300`（chainBegin=60 —— **表内顺序第 60 条**，
    也就是名单尾部）；这张表本身早已被 verify 逐边对账过（63/63）；
  * **上限**：`SAQ.cpp` 的 `kMaxSamples = 40` —— A 段 43 条 kFail 只打得下 40 条 ⇒
    表内顺序最后 3 条（**通行是关键 / 全新的故事 / 领先一步**）被静默截掉；B 段藏 42 条
    ⇒ 恰好轮到「通行是关键」可见、仍截 2 条 —— 与打印名单逐条吻合；
  * **账目**：`链式门槛 49 = 过 4 + 藏 43 + 固定显示放行 2`（CF01 与萨姆入口任务 ——
    chainCount=1 且 pinned，与 `FormatPinStats` 的「跳过门槛」标记对得上）。
* ⇒ FAIL = 用例断言依赖了「**有上限的打印名单**」= **不可达**（不是产品回归）。
  （与第 68 轮「正则写错」、第 72 轮「期望不可达」同一类：**先怀疑用例/日志，再怀疑产品**。）

### 15.2 修法（日志侧，一次到位四个名单）

* 四个名单（`进度没到:` / `INFO没到:` / `链式没到:` / `隐藏:`）超出上限的条目**不再静默
  丢弃**（此前 `if (count < kMaxSamples)` 没有 else）—— 以紧凑形式（名字 + FormID，
  不带 detail，每条 ~25 字符）追加在各自名单之后：
  `（另有 N 条未列出：通行是关键[0x002BABF3] 全新的故事[0x002C2561] 领先一步[0x002C572B]）`；
* 断言 `名单:.*<名字>[<FormID>` 对**截断区同样可达** ⇒ r77 用例**一字未改**；
* 这段留痕的意义：**「被藏」不再看起来像「没被藏」** —— 本轮误判的根源就在这里
  （看名单时以为「领先一步没被藏」，实际只是没打出来）。

### 15.3 判据（待实机）

* 重跑 `r77_chain_ryujin` ⇒ 日志出现 `链式没到: …（另有 3 条未列出：…领先一步[0x002C572B]）`，
  用例转 PASS（`check_results.py` 退出码 0）；
* 顺带：任何一条名单超过 40 条时，行尾都应带「（另有 N 条未列出：…）」（verify 有
  正向特征检查 + 用例计划里 r77 那条断言的反向钉住）。
