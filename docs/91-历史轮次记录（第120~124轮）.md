# 91 · 历史轮次记录（第 120~130 轮）

> 本文件由 `docs/99` 瘦身时**逐字迁移**而来（第 128 轮：第 120~124 轮块；第 129 轮：
> 第 125 轮块；★ 第 130 轮：第 126~127 轮块；★★ 第 134 轮：第 128~130 轮块）—— 内容与
> 迁移前完全一致，只是换了存放位置（原位在 docs/99 保留一行指针；文件名保持不变、引用不断链）。

---

> 上一轮：2026-09-23（**第 130 轮：功能迁移探针 v4 落地 + 纯离线全绿（P2 实验态部署）**——
> 该块明细已并入 `docs/15` 第十二节/十二·补；速查：`ui.research4` / `…4b` + 2 个 op +
> `[case:r130_gfx_migrate]` + `CreateObject` 带类名造 AS3 实例（换按钮回调的钥匙）+
> `verify --dev --p2` 782 行 + 离线层 5 步 + P2 正则自检；**已实机 = 第 131 轮**）。
>
> 上一轮：2026-09-23（**第 129 轮：`docs/15` 第七节「功能迁移」评估（纯离线）**——
> 结论 = 注入形态值得推进（2301 行 AS3 约 60% 原版自带，要重建的是「数据构造 + 接管 +
> 自检」）+ 契约/接管点全清单 + 探针 v4 设计 + 产品形态 B/C + 分期 P3~P5；完整评估 =
> `docs/15` 第十一节）。
>
> 上一轮：2026-09-23（**第 128 轮：operator 二期落地 —— 比较运算符折叠
> （`!=` / `>` / `>=` / `<` / `<=`）产品化：新内核 `tools/esm/ctda_ops.py`（真值表 +
> OR 组语义 + `--self-test`）+ 两条数据管线接线；5 个 master 全量重扫 ⇒ 7 条运算符条件
> 被收（全在**非表内**任务）+ **表内零变化**（静态表 / 内嵌载荷逐字节不变、快照全绿）；
> tripwire 升级 + verify +6 条；顺带修 verify 的 `AutoLoad` 跨行取值假 MISS 与 GBK 管道崩**）：
> ① 起因 = 下一步候选 ③（第 109 轮盘点留的 tripwire）—— 第 86 轮已把运算符语义全解、
>   第 87/106 轮只产品化了 `==` + OR 位；本轮把余下五个运算符也**折叠产品化**：
>   ★ `tools/esm/ctda_ops.py`：`fold_operator`（cmp∈{0,1} 下折叠成 `want ∈ {0,1}`
>   或常量恒真/恒假）+ `or_group_end` / `group_spans`（OR 组边界，与第 87 轮引擎语义
>   逐字一致）+ `assemble_gates`（组级处置：不可门槛 / 恒假 ⇒ 整组放弃，恒真 ⇒ 整组丢弃，
>   全 `want` ⇒ 整组保留）——**运行时零改动**（`want` 本来就是生成期字段）；
> ② 接线：记录级 `analyze_ctda.py`（`cond_to_gate` / `build_gates`）+ INFO 侧
>   `scan_info_gates.py`（`fold_of` + `assemble_gates`）；**INFO 对话分类改读折叠方向**
>   —— 修正旧实现「只看 cmp」的误判（`GetStageDone(自己, X) != 1` ⇒ want=0 = 入口类；
>   旧实现当「推进类」排除，实测影响 66+ 条自引用对话，均无外键条件 ⇒ 不入门槛数据）；
> ③ ★ **数据实测**（5 个 master 全量重扫 + 合并 + 生成表 + 快照全绿）：记录级表内 11 条
>   全是 `==` ⇒ `ctda_gates.json` 规范哈希不变；INFO 侧 **7 条**运算符条件被收
>   （`>=` cmp=1 ×5 / `!=` cmp=1 ×2，全在**非表内**任务：人群闲聊 / 对话容器 /
>   `SFFL_MS01_MiscDialogue` / `SFTER_CREW_EliteCrew_Delta`）⇒ `info_gates_final.json`
>   规范哈希不变、静态表与内嵌载荷**逐字节不变**（= 表内零变化）；
> ④ tripwire 升级（`survey_gate_coverage.py`）：报红条件换成「① 折叠已覆盖却仍 pending
>   ⇒ 扫描没重跑 / 折叠退化；② 折叠管不到的形态（flags 非 OR / cmp∉{0,1}）在表内 ⇒
>   后续阶段对象」+ 记录级结构扫描加 `uncoveredShapes` + 盘点纳入 SFBGS003；
> ⑤ 判据与产物：`ctda_ops.py --self-test`（真值表 12 + 不可折叠 3 + 组边界 5 + 组装 10）
>   接进 `run-all-tests.ps1`（离线层 **4 步**）；`verify --dev` **946 OK / 0 MISS**
>   （+6 条 operator 二期检查）；构建部署（DLL **1083392 B** / PEX 19133 B / ESM 4805 B，
>   部署 == 工作区）；黄金快照 `gate_coverage.json` 新摘要「表内 pending 12 /
>   折叠已覆盖 0 / 管不到 0」（有意更新，其余逐项不变）；
> ⑥ 顺带（判据通道加固）：修 `verify` 的 `AutoLoad` 取值正则**跨行**缺陷（AutoLoad 为空时
>   把下一行的 `;` 当值 ⇒ 正常玩部署下永久假 MISS）+ 该分支的 `⇒` 在 GBK 管道下
>   UnicodeEncodeError 崩掉整份校验 ⇒ 正则改行内取值 + verify 输出统一 UTF-8；
> ⑦ ★ **环境备注**：本轮 `SAQ_Tests.exe`（C++ 决策单测）**无法执行** —— 环境在进程启动时
>   拦截 / 删除新构建的 exe（Defender 实时防护已关，非杀软）。本轮**未改** `SAQ_Decision.*`
>   （离线层逻辑无涉），判据由折叠内核自检 + 快照 + tripwire 承担；环境允许时补跑
>   `run-decision-tests.ps1` 即可；
> ⑧ 文档：`docs/08` 新增 **4.7**（折叠规则落地全记录）+ `docs/10`（离线层第 2 步 /
>   工具表）；`docs/99` 触到 100 KB 上限 ⇒ 第 120~124 轮块**逐字迁移**到
>   `docs/91-历史轮次记录（第120~124轮）.md`（原位留指针；本块即新内容）。
>
> 上一轮：2026-09-23（**第 127 轮：P2 双收口（2/2 PASS + 眼睛双达成）+ 提示时机修复
> （「UI 通道不可用」提示改为「关菜单后再发」）—— 发布条件齐，下一步 = 打 0.1.15**）：
> ① 会话 16:48~16:50（118828 ms / `Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖禁用 / 开发 DLL
>   1083392 B / PEX **19133 B**）—— **2/2 PASS**：
>   `r120_gfx_poc` PASS（42532 ms；六段全 ok + `注入=ok（entryCount 1→3）`）+
>   `r125_ui_conflict` PASS（4812 ms；第二个菜单周期**独立判定行** 16:50:15.809 +
>   `assert.nolog 推送放弃` 通过）；
> ② ★ **提示时机修复生效（本轮小修）**：`SAQ_Main.psc` 新增 `MissionMenuOpen` ——
>   菜单开着时提示不发（GLOB 的 1 保留），**关菜单事件里立刻补发**；实测提示首发
>   `16:50:03` = 关菜单瞬间（DLL `16:50:03.922 菜单关闭` 同一秒）+ 补发 06/09
>   （共 3 次）—— 与第 126 轮「首发在菜单里（16:25:02）」对照 = 修复精确生效；
> ③ ★ **眼睛双达成（玩家确认）**：① 第 8 个 tab「SAQ-PoC」+ 3 条 `SAQ-PoC-Item 0/1/2`
>   界面可见（注入条目显示在「当前选中的第 1 个 tab」下 —— 探针末次回调 idx=0 后
>   才注入，属设计行为）；② 关菜单后 HUD 中英提示可见；
> ④ 判据三连全绿：`check_results` 退出码 0（2/2）+ `log_hygiene` 退出码 0（日志
>   39959 B / 95 行零坏字节；结果 JSON 4081 B / 39 行）+ `plan_regex_audit`（P2 计划）
>   **4/4 全命中**；`verify --dev --p2` 全过（756 OK）+ 离线层 3/3；
> ⑤ ★ **已恢复常规部署**：`build-saq.ps1 -SkipTable -SkipSwf -Harness`（SWF 拷回 +
>   `*.p2off` 清理 + ini `Plan` 改回主计划）+ `verify --dev`（非 P2）**全部通过**；
>   顺带：`wait` 按「游戏运行时间」计（16:37 会话 `wait 10000` 实测 51438 ms ——
>   期间游戏被暂停 ~41 秒，驱动器正常行为）；
> ⑥ ★ **下一步 = 发布 0.1.15**（含第 125 轮冲突检测 + 本轮提示时机修复）：
>   版本号 6 处 + changelog + 文案 + `verify --release` + 打包 + MO2 部署还原
>   （打完后评估功能迁移 `docs/15` 七节与产品形态）。
>
> 上一轮：2026-09-23（**第 126 轮：第 125 轮产物实机检查收口 —— 正常态 43 条 43/43 全 PASS
> （首次 43 条全量全绿，含 `r125_ui_channel_ok` 防误报；`[E]` 0 / `[W]` 48 全老现象）+
> 判据五连全绿；已按 P2 实验部署（下一步 = P2 会话 2 条 + 眼睛）**）：
> ① 会话 16:01~16:10（577703 ms / `驱动器 v70` / `stamp=65` / 用例集 **43 条 / 734 步**）——
>   **43 PASS / 0 FAIL / 0 SKIP**；`[E]` 0 / `[W]` 48（47×「推送失败（第 1 次，完整诊断）」
>   + 1×「UI 桥解析失败」= 老现象：菜单首开桥未就绪、重试即成功）；日志 **1106423 B**
>   （上限 10 MB，未滚动；2117 行）；
> ② 判据五连全绿（退出码均 0）：`check_results.py` / `log_hygiene.py`（结果 JSON 445226 B /
>   1086 行零坏字节）+ `plan_regex_audit.py` **160/160 全命中**（3 条 assert.nolog 提示 = 正常）+
>   `verify_saq_build.py --dev` 全过（部署 == 工作区，DLL 1083392 B）+ 离线层 3/3（19 用例 /
>   936 断言 + 快照 16 件 + tripwire）；
> ③ ★ **第 125 轮新用例实测**：`r125_ui_channel_ok` PASS（4562 ms）—— `推送成功`（16:10:36
>   命中）+ `assert.nolog UI 通道不可用`（整段窗口零判定行）⇒ **防误报成立**（我们的 SWF 下
>   绝不误判）；全日志除用例段头 ×2 + 断言行 ×1 外无任何「UI 通道」产品行（也无「推送放弃」）；
> ④ 产品侧全绿（与基线同型）：`引擎存在=280 虚表识别=280 未识别=0` /
>   `入口=21(可导航 21｜marker 10~11 原板 9~10 兜底 1 不可用 0)` /
>   `INFO门槛=80(过52~53/藏24~25/放行2/未知0)` / `链式门槛=68(过2~4/藏62~64/未知0)` /
>   `可重复任务=21(已完成保留0｜排在末尾19)` / `同伴固定=4 + 势力入口固定=4`；
>   「需靠近 / 目标尚未加载 / 保持待生效 / 引导重试」= **0 次**（统计随用例内推进行为浮动，
>   均为同型值）；
> ⑤ ★ **已按 P2 实验部署（待实机）**：`build-saq.ps1 -SkipTable -SkipSwf -Harness -P2` ——
>   SWF 覆盖改名 `*.p2off`（游戏加载原版）+ ini `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`；
>   P2 计划 **2 条** = `r120_gfx_poc` + 新增 `r125_ui_conflict`（判定行出现 + 不再出现
>   「推送放弃」）；眼睛 = 关菜单后 HUD 提示（中英）；`verify --dev --p2` 全过（756 OK /
>   P2 禁用证据 + Plan 一致性 + 部署副本一致）；跑完恢复 = `build-saq.ps1 -SkipTable -SkipSwf -Harness`；
> ⑥ ★ **顺带：文档瘦身（AGENTS.md「单文件 ≤100KB」）**：`docs/99` **158540 → ~92.7 KB**
>   （第 106~119 轮顶部块 + 「游戏内实测」旧会话记录 + 十一节已完成项 → 新建
>   `docs/93-历史轮次记录（第106~119轮）.md`，逐字迁移）；`docs/09` **103676 → 85331 B**
>   （十四·补、~补十五（第 55~81 轮）→ `docs/94`）；`docs/05` **101482 → 89412 B**
>   （第十五/十六节（第 41/42 轮星图）→ `docs/92`）；三处原位都保留章节标题 + 指针，
>   引用不断链（迁移逐字、未改写）。
>
> 上一轮：2026-09-23（**第 125 轮：路线 D · 冲突检测落地 —— UI 通道身份探测
> （`UI::ProbeChannelIdentity`）+ 判定「界面不是我们的 SWF」即停推降噪 + 玩家 HUD 提示
> （GLOB `SAQ_UiNotice` 0x80E → 脚本 `ProcessUiChannelNotice`）；ESM 4752 → 4805 B；
> 主计划 +1 条 `r125_ui_channel_ok`（防误报）/ P2 计划 +1 条 `r125_ui_conflict`（正向）；
> `verify --dev` 全过 + 离线层 3/3；**待实机**（下次会话跑 43 条 + P2 正向）**）**：
> ① 背景 = `docs/15` 八节路线 D + 九·补六「产品化观察①」：补丁 SWF 被其它改
>   `missionmenu.swf` 的 mod 覆盖 / 未安装 / 版本过旧时，旧行为是每菜单退避重试 14 次
>   （刷日志）+ 玩家看不出「mod 没生效」（像静默失效）；
> ② DLL：`UI::ProbeChannelIdentity`（桥 OK + `_root.SAQ_Report` 带 `stamp=` ⇒ ours；
>   调用失败 / 无指纹 ⇒ notOurs；桥没通 ⇒ unknown 不判定）；`SAQ.cpp` 第 2 次真失败起
>   探测，notOurs ⇒ 本菜单停推 + WARN「UI 通道不可用（第 N 次推送失败后判定）：…」+
>   进程内一次写 `SAQ_UiNotice`；`PollUiReport` / `PollGuideRequest` / 关闭现场读全短路降噪；
> ③ 脚本：`ProcessUiChannelNotice`（轮询读 0x80E，=1 ⇒ ShowNotice 中英文案 + 清 0）；
> ④ 验证：构建部署（DLL **1083392 B** / PEX **19026 B** / ESM **4805 B**，部署 == 工作区）
>   + `verify --dev` **全部通过**（+12 条第 125 轮检查）+ 离线层 3/3（快照 ESM 有意变化
>   已 --update）+ 断言正则编译校验全过；
> ⑤ ★ **待实机**：正常态 43 条（新增 `r125_ui_channel_ok`：推送成功且**无**判定行 ——
>   防误报）；P2 态 2 条（新增 `r125_ui_conflict`：判定行出现 + 不再出现「推送放弃」+
>   眼睛 HUD 提示）。完整复盘见 `docs/15` 第十节。
>

> 上一轮：2026-09-23（**第 124 轮：P2 眼睛判据达成 + 恢复部署 —— 玩家截图（30 秒窗口内）
> 取得眼睛证据（第 8 个 tab `SAQ-PoC` + 列表 3 条 `SAQ-PoC-Item i`）⇒ **P2 判据 + 眼睛
> 双绿、完全收口**；已按恢复流程还原部署（SWF 拷回 + `*.p2off` 清理 + Plan 改回主计划），
> `verify --dev` 全过；下一大项 = 「无覆盖形态」产品化（路线 D 冲突检测先行）**）：
> ① 会话 15:43~15:45（`Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖禁用 / 开发 DLL 1079296 B）——
>   `r120_gfx_poc` PASS（32344 ms）；探针行六段全 ok + `注入=ok（entryCount 1→3）`；
> ② ★ **眼睛证据**（30 秒窗口 15:44:48~15:45:18 内玩家截图）：tab 条 8 项（前 7 = 探针
>   假数据 `$SAQ-keep0..6`、第 8 = `SAQ-PoC`）+ 列表 3 条 `SAQ-PoC-Item 0/1/2` ——
>   原版 SWF 上「扩 tab + 列表注入」肉眼可见；关菜单重开无残留；
> ③ 会话日志侧（侧面证据）：「推送失败（重试）」循环 + `菜单关闭：界面状态一条都没读回来`
>   = 原版 SWF 无产品 AS3 入口（产品化观察①，兼容模式要处理）；
> ④ ★ **恢复部署完成**：`build-saq.ps1 -SkipTable -SkipSwf -Harness`（SWF 覆盖拷回 +
>   `*.p2off` 清理 + ini `Plan` 改回主计划）；`verify --dev`（非 P2）**全部通过**；
> ⑤ ★ **下一步**：开工**路线 D · 冲突检测**（`docs/15` 八节）—— UI 通道不可用检测 ⇒
>   停推降噪 + 明确提示玩家；之后评估功能迁移（第七节）。
>
> 上一轮：2026-09-23（**第 123 轮：P2 复跑实机检查收口 —— `r120_gfx_poc` PASS
> （六段全 ok + 注入段修复一击生效 `entryCount 1→3`）⇒★ P2 通过（「无 SWF 覆盖」形态
> 技术成立）；判据五连全绿；眼睛窗口 8 → 30 秒并重新部署（P2 态保留，供亲验）**）：
> ① P2 复跑会话 15:25~15:27（82172 ms / `Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖已禁用 /
>   开发 DLL 1079296 B）—— `r120_gfx_poc` **PASS**（10375 ms）；实测行：
>   `界面研究探针3 Menu_mc=ok｜环境=(numTabs 7,entryCount 1,mask 0xFFFFFFFF)｜扩tab=ok（7→8）
>   ｜切3=ok（mask→0x00000008）｜切7=ok（拦截 1 次,mask→0x20000000）｜切0=ok（mask→0xFFFFFFFF）
>   ｜回调=3 次（末次 idx=0）｜清理=ok｜**注入=ok（entryCount 1→3）**` —— 第 122 轮修复
>   （注入条目补 `aObjectives`）**一击生效**；
> ② ★ **判据五连全绿**（退出码均 0）：`check_results.py`（1 条 PASS）+ `log_hygiene.py`
>   （日志 39910 B / 89 行零坏字节；结果 JSON 干净）+ `plan_regex_audit.py` **2/2 全命中**
>   + `verify_saq_build.py --dev --p2` 全过（含 `*.p2off` 禁用证据 + 部署 == 工作区）+
>   离线层 3/3（19 用例 / 936 断言 + 快照 16 件 + tripwire）；
> ③ ★ **P2 通过**（判据原文：六段全 ok ⇒「无 SWF 覆盖」目标形态成立）—— 即**不替换
>   missionmenu.swf 也能**：扩 tab（7→8）/ 拦截原版越界处理 / 列表数据注入 / 交互接管
>   （第 120 轮 P1.5 已验）；「第 8 个 tab 必须拦截」的硬证据再次成立（哨兵 `0x20000000`
>   存活）；
> ④ ★ **眼睛窗口说明（玩家截图 = 正常状态）**：探针副作用（8 个 tab / 列表 3 条）只存在于
>   「探针执行后 → `menu.close`」之间（本次 ~8 秒；tab 条前 7 项 = 假数据 `$SAQ-keep0..6`、
>   第 8 项 `SAQ-PoC`；列表被替换为 3 条 `SAQ-PoC-Item i`）；菜单关闭后 Movie 销毁、
>   重开时原版重建 ⇒ 回到 7 tab + 原版 entries（**无残留**）。为便于亲验：计划窗口
>   **8 → 30 秒**（`wait 30000`）+ 保持 P2 部署；
> ⑤ ★ **P2 形态的两条产品化观察（功能迁移的输入）**：① 产品推送在原版 SWF 下必然失败
>   （`SAQ_PushData` / `SAQ_Report` 等 AS3 入口不存在）⇒ 日志持续「推送失败（重试）」+
>   「界面状态一条都没读回来」—— 未来「兼容模式」需检测原版 SWF 即停推（降频降噪）或把
>   产品通道整体切换为注入形态；② 重开菜单时偶见「菜单尚未就绪（UI 表里还没有
>   BSMissionMenu 条目），稍后重试」= 老时序现象（稍后重试即恢复，与 P2 无关）；
> ⑥ ★ **待办（下一轮）**：①（可选）眼睛会话（30 秒窗口亲验第 8 个 tab + 3 条注入条目）；
>   ② 恢复部署（`build-saq.ps1 -SkipTable -SkipSwf -Harness`）；③ 评估**功能迁移**
>   （`docs/15` 第七节：迁移面 = MissionMenu +2113 行 / MissionsList +186 / QuestUtils +2）
>   与产品形态（建议「兼容模式」，默认仍走 SWF）；完整复盘见 `docs/15` 九·补六。
>
> 上一轮：2026-09-23（**第 122 轮：P2 首跑实机检查 —— 探针 v3 五段 ok / 注入段 fail
> （真因离线取证 = 注入条目缺 `aObjectives`）⇒ 已修 + 重建部署（P2 态），待复跑**）：
> ① P2 会话 15:15（94953 ms / `Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖已禁用 / 开发 DLL
>   1078784 B）—— `r120_gfx_poc` **FAIL**（唯一断言 = 六段同现正则）；判据其余全绿：
>   `log_hygiene` 退出码 0（日志 2189614 B / 4204 行零坏字节）+ `plan_regex_audit`
>   2 条里 1 条未命中（= 与本 FAIL 同源的产品行缺失，非正则写错）+ `verify --dev --p2`
>   全过 + 离线层 3/3（19 用例 / 936 断言 + 快照 16 件 + tripwire）；
> ② ★ **探针 v3 判读**（实测行）：`环境=(numTabs 7,entryCount 1,mask 0xFFFFFFFF)` ✅
>   原版 SWF 生效（P2 部署正确）｜`扩tab=ok（7→8）` ✅｜`切3=ok`（对照）✅｜
>   `切7=ok（拦截 1 次,mask→0x20000000）` ✅（拦截 + 哨兵写双成立）｜`切0=ok`（放行）✅｜
>   `清理=ok` ✅｜**`注入=fail（entryCount 1→0，期望 3）`** ❌；
> ③ ★ **注入失败真因（离线取证）**：原版 `FilterRootEntries` 的第一道门 =
>   `IsRootEntry` = `MissionsListEntry.IsMission(param1)` =
>   `param1.hasOwnProperty("aObjectives")`（`_tmp_ffdec_base/scripts/MissionsList.as:157`
>   + `MissionsListEntry.as:42`）—— 初版注入条目没带 `aObjectives` ⇒ 3 条全被过滤
>   （rawEntries 收下、entryList 一条不留 ⇒ entryCount 0）。我们 SWF 版的产品条目本来就带
>   该字段（靠 `bSaqAvailable` 分支放行），原版没有该分支 ⇒ **运行时注入必须把条目做成
>   「像原版 mission 条目」**；
> ④ **修复**（`SAQ_UI.cpp` 的 `ResearchGfxInjection3` / `buildEntries`）：每条注入条目补
>   `aObjectives`（空数组）+ 渲染防御字段 `bActive=false` / `iRemainingTime=-1`
>   （避免 `GetQuestTimeRemainingString(undefined)` 路径）+ 代码注释写明两道门；
>   verify 新增特征串 `aObjectives`（dev 正向 / 发布反向，防「改回只设 iType」再犯）；
>   重建 DLL + 按 P2 态重新部署 + `verify --dev --p2` 全过（新检查项 OK）；
>   完整复盘见 `docs/15` 九·补五节；
> ⑤ ★ **待复跑（下一轮）**：再跑一次 P2 会话 ⇒ `界面研究探针3 …` 六段同现
>   （`注入=ok（entryCount 1→3）`）+ **眼睛**：菜单 tab 条上第 8 个 tab「SAQ-PoC」、
>   列表 3 条「SAQ-PoC-Item i」；六段全 ok ⇒ **P2 通过**（「无 SWF 覆盖」目标形态成立）
>   ⇒ 按恢复流程还原部署（`build-saq.ps1 -SkipTable -SkipSwf -Harness`）。
>
> 上一轮：2026-09-23（**第 121 轮：P2（原版 SWF 完整 PoC）落地 —— `ui.research3`
> （扩 tab 7→8 / 切 7 拦截哨兵 / `InitializeEntries` 列表注入）+ P2 实验部署开关
> （`build-saq.ps1 -P2`：SWF 覆盖改名禁用 + ini Plan 切 p2 计划）+ verify `--p2`；
> **已按 P2 部署（测试态），待实机**）：
> ① **设计依据**（`docs/15` 九·补三 + 九·补四）：P1.5 五段全 ok ⇒ 把「扩 tab + 拦截 +
>   列表注入」拼成**原版环境**下的完整链路；补上最后一块未验证能力 = **列表数据注入**
>   （`MissionsList.InitializeEntries` —— 第 117 轮离线取证 = public override）；
> ② **探针 v3**（`ResearchGfxInjection3`，harness 段内、发布零残留）九段判据链：
>   R1 环境（numTabs / entryCount / filterMask）｜R2 挂 priority=100 监听｜
>   R3 **扩 tab**（构造 N0+1 项 → `SetTabsData` → numTabs 7→8）｜R4 切 3（对照：原版执行）｜
>   R5 切 N0（新 tab）→ **哨兵 `0x20000000` 存活 = 拦截 + 写双成立**（原版若不拦会读
>   `FilterInfoA[7].flag` ⇒ TypeError —— 这正是「第 8 个 tab」必须拦截的硬证据）｜
>   R6 切 0（放行）｜R7 清理｜R8 自设 `filterMask`=1<<6｜R9 **注入**（3 条 iType=6 条目 →
>   `InitializeEntries` → entryCount → 3）；汇总一行（红线六）；
> ③ **用例**：新 op `ui.research3` + P2 专用计划 **`SAQ_TestPlan_p2.txt`**
>   （`r120_gfx_poc`：只用 C++ 侧原语 `ping/menu.open/wait/menu.close` + 探针 ——
>   原版下 `ui.*` 测试入口全不可用）；42 条主计划不动；
> ④ **部署开关**（`build-saq.ps1 -P2`）：SWF 覆盖改名 `*.p2off`（游戏加载原版）+
>   拷 p2 计划 + ini `Plan=SAQ_TestPlan_p2.txt`；恢复 = 不带 `-P2` 再跑一次
>   （例如 `-SkipTable -SkipSwf -Harness`：SWF 拷回 + `*.p2off` 清理 + Plan 改回）；
> ⑤ **verify 新增 +13 条**：探针3 特征串 dev/release 双向 ×4 + r120 计划形状/断言 ×4 +
>   只读反向 ×1 + `*.p2off` 证据/残留 ×2 + Plan 一致性 ×2（后两组按 `--p2` 模式二选一）；
>   `--p2` = 查 p2off 证据（SWF 有意缺失）、非 P2 = 反向查残留；**已实测**：非 P2
>   `verify --dev` 全过 + P2 `verify --dev --p2` 全过 + 离线层 3/3（19 用例 / 936 断言 +
>   快照 16 件 + tripwire）+ 断言正则预检（2 条合法 + 与预期输出匹配）；
> ⑥ ★ 部署状态：**已按 P2 部署**（测试态 —— DLL 开发构建 **1078784 B** 含探针 v3 /
>   SWF 覆盖**已禁用**（`*.p2off`）/ ini `Harness=1` + `AutoLoad=Save1_…142854_2_0_4` +
>   `Plan=SAQ_TestPlan_p2.txt`）；
> ⑦ ★ **待实机（下一轮）** ⇒ ✅ **已首跑（第 122 轮）**：`界面研究探针3` 五段 ok /
>   **注入段 fail（entryCount 1→0）** —— 真因 = 注入条目缺 `aObjectives`（原版
>   `IsMission` 判定），已修 + 重建部署（修复版 DLL **1079296 B**，P2 态），**待复跑**
>   （判读与修复见顶部第 122 轮块 + `docs/15` 九·补五）。
>
> 上一轮：2026-09-23（**第 120 轮：第 119 轮产物实机检查收口 —— 42 条 42/42 全 PASS
> （首次 42 条全量全绿）+ 判据五连全绿 + 探针 v2 判读**五段全 ok**（拦截 + 哨兵写 + U2
> 补测全成立）⇒ 绕过路径实机确认，下一步 = P2（原版 SWF 完整 PoC）**）：
> ① 会话 14:44~14:53（550156 ms / `驱动器 v70` / `stamp=65` / 用例集 **42 条 / 728 步**）——
>   **42 PASS / 0 FAIL / 0 SKIP**；判据五连全绿：`check_results.py` / `log_hygiene.py`
>   退出码 0（日志 **2162393 B** / 4141 行零坏字节；结果 JSON 442949 B / 1072 行）+
>   `plan_regex_audit.py` **158/158 全命中**（3 条 assert.nolog 提示 = 正常）+
>   `verify_saq_build.py --dev` 全过（部署 == 工作区）+ 离线层 3/3（决策单测 19 用例 /
>   936 断言 + 快照 16 件 + tripwire）；`[E]` 0 / `[W]` 47（46×「推送失败（第 1 次）」
>   + 1×「UI 桥解析失败」= 老现象，日志未滚动）；
> ② 产品侧全绿（与第 118 轮基线**逐项一致** ⇒ 无回归）：`引擎存在=280 虚表识别=280 未识别=0`
>   / `入口=21(可导航 21｜marker 11 原板 9 兜底 1 不可用 0)` / `进度门槛=7(过1/藏6/未知0)` /
>   `INFO门槛=80(过52/藏25/放行2/未知0)` / `链式门槛=68(过2/藏64/未知0)` /
>   `可重复任务=21(已完成保留0｜排在末尾19)`；「需靠近 / 目标尚未加载 / 保持待生效 /
>   引导重试」= **0 次**；
> ③ ★ **探针 v2 判读**（`r119_gfx_inject2` PASS，1266 ms）—— 实测行：
>   `界面研究探针2 Menu_mc=ok｜枚举=(82 个,FilterInfoA=无)｜切3=ok（mask 0xFFFFFFBF→0x00000008）
>   ｜切7=ok（拦截 1 次,mask→0x20000000）｜切0=ok（mask→0xFFFFFFBF）｜回调=3 次（末次 idx=0）
>   ｜清理=ok｜U2=ok（TabsData 调用 ok，numTabs 8→9→8）` ⇒ **五段全 ok**：
>   **哨兵 `0x20000000` 存活 = 拦截 + 写双成立**（原版 `onFilterChanged` 被拦住、没把 mask
>   覆盖回去；`SetMember(filterMask)` 生效）；对照段 / 放行段 / 清理 / U2 全成立；
>   R1 枚举 `FilterInfoA=无` = U1 能力边界再次确认（不影响结论）；
> ④ ★ **下一步 = P2**（原版 SWF 完整 PoC）⇒ **已落地（第 121 轮）**：探针 v3
>   `ui.research3`（设计注意点见 `docs/15` 九·补三；落地见九·补四）+ P2 实验部署开关
>   （`build-saq.ps1 -P2`）+ verify `--p2` —— **已按 P2 部署**（SWF 覆盖禁用 +
>   `Plan=SAQ_TestPlan_p2.txt`），待实机判读（顶部第 121 轮块 ⑦）。
>


---

# 附录：docs/99 头部「第 137~43 轮速查块」原文（2026-09-23 第 140 轮 docs/99 瘦身迁移）

> 更早：2026-09-23（**第 137 轮：P3-b 落地 —— 注入形态产品化（完整描述文案迁移 /
> watchdog / `UiMode` 运行期开关）；P2 实验态已按 P3-b 重新部署，待实机 5 条**）：
> ① 三块 = ①**完整 `SaqDescriptionText` 迁移**（SWF 版逐字搬运 + `KeyHelper` 按键名；
>   两形态描述一致 —— 改文案要两处同步）②**watchdog**（我们的 tab 上 500 ms 检查
>   `entryCount` + 首条 uID，引擎刷新覆盖 ⇒ 重放；日志节流）③**`UiMode` 运行期开关**
>   （ini `[UI] UiMode=swf|auto|inject`，默认 auto；HUD 提示改「延迟判定」—— 激活成功
>   不提示、3 秒没起来才补发）；
> ② ★ 分层：注入层不再整体在 harness 内 —— **产品路径发布构建也编译**（隔离纪律延续
>   「随时可回 SWF」：`swf` 档行为逐字节不变），harness 探针 `RunInjectPoC` 输出格式与
>   第 135 轮**逐字一致**；SAQ.cpp 接线 = `ResolveUiMode` / Tick open 分支 / 菜单关闭
>   清理 / `TryPushPending` 的 inject 短路 / `PollUiReport` 跳过；
> ③ 判据（离线全绿）：DLL 开发构建（harness）**1160704 B**（+18432）+ `verify --dev --p2`
>   **824 行 / 0 MISS**（+12 条产品串 dev/release **双向** + 源码结构/分层检查 + ini 模板
>   [UI] 段 + P2 计划形状）+ 离线层 **5 步全过**（P2 正则自检 **12 条 / 9 样例**）；
>   ini 模板与部署 ini 均含 `[UI] UiMode=auto`；P2 计划 **+1 条** `r137_product_inject`；
> ④ 待实机（P2 会话 **5 条**）：r120 / r125 / r130 / r133 / **r137**（产品路径 —— 不依赖
>   `ui.inject` 原语）；眼睛 = 第 8 个 tab「可接任务」+ 真实列表；观察 = watchdog 计数。
>
> 更早：2026-09-23（**第 136 轮：第 135 轮修复产物 P2 复跑收口（4/4 全 PASS）+ 眼睛
> 达成 —— P3-a 完全收口** —— 明细 = `docs/15` 十三·补二；速查：`InjectCtx::list` 接线
> 一击生效、判读表五段与预测逐字一致（`对账=ok（0x002C5401:超越极限）` 等）；判据五连
> 全绿（`verify --dev --p2` 800 行 / 0 MISS；DLL 1142272 B）；玩家确认第 8 个 tab
> 「可接任务」+ 真实任务列表都看到了；玩家已定 = 直接进 P3-b）。
>
> 更早（第 133~135 轮，速查）：**135** 第 134 轮修复产物复跑（3/4）+ 第二处产品级缺陷
> 修复（`InjectCtx::list` 未接线 ⇒ mask=0x40 写入失败；修复 = `ctx.list = list;` +
> 上下文自检 + 恢复判据加 passInject 前置）/ **134** 数据生命周期解耦（notOurs 分支不再
> 清 `g_pending.quests`）/ **133** P3-a 落地（`SAQ_UiInject.{h,cpp}` + `ui.inject`）——
> 明细见 `docs/15` 十三节与十三·补。
>
> 上一轮：2026-09-23（**第 133 轮：P3-a 产品化 PoC 落地（真实数据注入）** —— 明细已并入
> `docs/15` 十三节；速查：新模块 `plugin/src/SAQ_UiInject.{h,cpp}` + op `ui.inject` + P2 用例
> `r133_inject_data`（+8 特征串 / +5 计划形状）；纯离线全绿；**已实机 = 第 134 轮**）。
>
> 上一轮：2026-09-23（**第 132 轮：第 131 轮产物实机检查收口（P2 会话 3/3 全 PASS）**——
> 两处探针修正验证生效（`接管=ok（回调收到 1 次）` / `关菜单入口=ok`）+ 造对象新判据
> `接线 UserEvents=1` 首测成立 ⇒ 探针 v4 全绿 ⇒ **P3 立项条件达成**；判据五连全绿；
> **眼睛达成**（玩家确认 2 条 `SAQ-Mig-0/1`）；DLL **1117184 B** + `verify --dev --p2`
> 784 行；玩家已定 = 直接进 P3 + 硬要求「随时可回 SWF 版本」；明细 = `docs/15` 十二·补二）。
>
> 更早（第 128~130 轮，已迁 `docs/91-历史轮次记录（第120~124轮）.md`；速查：**130** 探针 v4
> 落地（`ui.research4`/`…4b` + `CreateObject` 回调劫持）/ **129** 功能迁移评估（注入形态
> 值得推进）/ **128** operator 二期（比较运算符折叠产品化 —— 表内零变化））。
>
> 更早（第 120~127 轮，已迁 `docs/91-历史轮次记录（第120~124轮）.md`（第 129/130 轮补迁
> 第 125 轮块 + 第 126~127 轮块）；速查）：**127** P2 双收口（2/2 PASS + 眼睛双达成）+
> HUD 提示时机修复（关菜单后再发）+ **0.1.15 包**；**126** 正常态 43 条 43/43 全 PASS
> （首次全量全绿 + `r125_ui_channel_ok` 防误报）；**125** 路线 D 冲突检测
> （`ProbeChannelIdentity` + 停推降噪 + HUD 提示 GLOB 0x80E）；**124** P2 眼睛判据达成
> + 恢复部署；**123** P2 复跑 PASS（注入段一击修复生效）；**122** P2 首跑（五段 ok /
> 注入 fail —— 真因 = 注入条目缺 `aObjectives`）；**121** P2 落地（探针 v3 +
> `build-saq.ps1 -P2`）；**120** 42/42 + 探针 v2 五段全 ok。
> 更早（第 106~119 轮，已迁 `docs/93-历史轮次记录（第106~119轮）.md`；速查）：
> **119** 探针 v2（P1.5：拦截 + `filterMask` 哨兵写 + `SetTabsData` 补测）；**118** 41/41 + 研究探针
> 判读（U0 ✅ / U1 ❌ 能力边界 / U2 ❓ / U3 ✅）；**117** UI 注入研究落地（`ui.research` 探针 + 用例
> 39→41）；**116** 39/39 + 4 条待定任务全部定案「不收」+ **0.1.14 包**；**115** 38/39（sg02 传送目标
> 非常驻 ⇒ 换常驻门）；**114** 引导质量 2.0（两档常驻兜底 / 候选 1020 / 19 条 needsApproach→0）；
> **113** 35/35 + 0.1.13 包；**112** 日志上限可配（ini `[Log] MaxSizeMB`）；**111** 34/35（r109 断言
> 过窄 ⇒ 交替名单）；**110** 追踪者联盟（SFBGS003）+ medium 档；**109** 7 条补收任务补引导候选 +
> operator 二期盘点收口；**108** 32/32 + 0.1.12 包；**107** 32 条 30 PASS / 2 FAIL（断言多写 `=`）；
> **106** operator 全量产品化（INFO OR 组）+ 覆盖面全量复盘。
> 更早（第 96~105 轮，速查）：**105** 30/30 + 0.1.11 包；**104** probe 产品行（红线六）；
> **103** 用例 32→30（`MQ_Shell` 一会话一推）；**101/102** DLC 链式二期 +15 边 ⇒ 78 任务 /
> 89 边（有意不收 MQIntro / `DialogueHVDazra@5` / VKaiZ03*）；**100** 29/29 + 0.1.10 包；
> **98/99** DLC 链式取证（反编译官方 `.pex`）；**97** 27/27 + 0.1.9 包；**96** 可重复任务
> （「（可重复）」前缀 + 整组垫底 + `rq=` / `order=|tail=` 探针 + `stamp=63`）。
> 完整复盘 `docs/11` 十一 / `docs/06`。
>
> 更早（第 61~95 轮，速查）：**95** 0.1.8 包 + **94** 26/26 全 PASS + `plan_regex_audit`；
> **93** 判据定稿（「完成即从玩家日志移除」⇒ 显示层证据 = `ui.tab` + `ui.select`）；
> **91** 不可导航条目「（不可导航）」前缀（`nonav=` / `stamp=62`）；**89/90** 可重复任务
> 20 条（`repeatable` 列 / C++ 豁免 / 载荷第 11 列 / `rptk=`，`docs/11`）；**88** 0.1.7 包；
> **87** 进度门槛 OR 组（`orBit`，7 任务 11 条件）；**86** operator 全解；**85** 22/22 +
> 0.1.6 包；**84** 结果 JSON UTF-8 收口（`Utf8SafeCut` / `log_hygiene.py`）；
> **83** 驱动器 **v70**（`ui.*` 桥重试 / 清场延迟）；**82** 同伴条目文案说透（同伴任务
> **无接取点**、好感度到点自动开始；SWF `stamp=60`，`docs/08` 十三）；**81** 地球地标
> 10 条收录（书/书商触发；`Opportunity` 不收）；**80** 入口第二形态（可重复 NPC 8 条，
> 前缀只在 NPC 条目）；**78** DLC 的 INFO 门槛（60 → 80 任务）；**77** 龙神线 +8 边；
> **76** 17/17；**75** 四大势力开头固定显示 + 固定排前四（CF01 只给说明，
> `IsGatePinned` / `PinnedOrderKey`）；**74** 同伴好感度任务固定显示
> （`companion`/`companionPin`）；**71** 链式全量入边审计；**67** 链式门槛；
> **65** 任务专属图标（FTYP → 阵营枚举）；**64** 离线层（`docs/10`）；**62/63** 自动读档 +
> 读档窗口加固。仍然生效的接口约定见「一、当前状态」表各行；细节复盘
> `docs/95`（81~83）/ `docs/96`（35~40）/ `docs/97`（75~80 等）/ `docs/98`（9~16）。
>
> 更早（第 43~60 轮，速查）：**43** 分诊探针 `ev=`/`btn=`；**44** 星图由脚本**轮询节拍**
> 打开；**45** 引导候选池（预选/换候选/复算）；**46** 结果码 5 + 退避重试 + HUD 提示；
> **47** 同 cell 常驻兜底；**48** `AllCandidatesNonPersistent` 回退 + **INFO 门槛** +
> 「引擎已开始 ⇒ 放行」；**49** 引擎内 harness；**50** `stamp=` 指纹 + 铁律「**先构建部署、
> 再启动游戏**」；**52~53** `Error #1074` 收口 + `saq_harness` 开关 + 0.1.1 包；
> **54~60** 历史判据落成用例 + 驱动器 v56~v60 修七类缺陷（→ `guide.probe`）。
> 完整复盘：`docs/95`（43~60）/ `docs/96`（35~40）/ `docs/97`（53~55 等）/ `docs/98`（9~16）。


---

# 附录二：docs/99「十一、下一步」第 117~132 轮长块原文（2026-09-23 第 140 轮瘦身迁移）

0. ✅ **第 132 轮：P2 会话重跑收口（3/3 全 PASS）+ 眼睛达成 + 玩家决定** —— 探针 v4 全绿
  （`接管=ok（回调收到 1 次）` / `关菜单入口=ok` / 新判据 `接线 UserEvents=1`）⇒ P3 立项
  条件达成；DLL **1117184 B** + `verify --dev --p2` 784 行；玩家已定：直接进 P3 +
  硬要求「随时可返回 SWF 版本」（P3 起注入层独立模块 + 运行期开关；P5 才决策发布形态）。
0. ✅ **第 131 轮：第 130 轮产物实机检查收口（3 条 2 PASS / 1 FAIL）+ 探针 v4 两处缺陷修正
  （已实机收口 = 第 132 轮）** —— 明细见 `docs/15` 十二·补（U5 接线缺陷 / U9 判据错 ⇒ 均
  探针侧修正 + 重建部署；眼睛 = 玩家看到 2 条 `SAQ-Mig-0/1`；DLL **1117184 B**）。
0. ✅ **第 130 轮：功能迁移探针 v4 落地（纯离线全绿；已实机 = 第 131 轮）** ——
  产物 / `CreateObject` 带类名新发现 / 判据链 / 验证数字见 `docs/15` 第十二节（块已并入该节）。
0. ✅ **第 129 轮：`docs/15` 第七节「功能迁移」评估（纯离线 —— 迁移面分组 + 原版契约
  全清单 + 8 项落地设计 + 探针 v4 设计 + 产品形态三选项 + 分期计划）**：
  ① 结论 = **注入形态值得推进**（可行性已被 P1.5/P2 证完，剩余为工程化）；2301 行 AS3 里
  **约 60% 的功能原版自带**，真正要重建的是「数据构造 + 接管 + 自检」（估 C++ 新增
  2000~3100 行）；
  ② 本轮新取证（`docs/15` 11.3）：`GetDataForEntry()` / `selectedEntry` public ⇒ 引擎数据
  直读；按钮回调可劫持（`UserEventData.funcCallback`/`sCodeCallback` public +
  `MinimalButton.HandleUserEvent` public）；关菜单 / 回游戏走 public 的
  `MissionMenu.ProcessUserEvent`；条目必补字段 14 + 接管点 10；
  ③ 落地设计要点：按 tab **分时注入**（不碰 ALL 掩码）；按钮回调**保存-替换-委托**；
  推送 / peek / reply / sync **四条通道退休**；诊断与测试入口改 C++ 直读（净简化）；
  ④ ★ **下一步 = 探针 v4（`ui.research4`，U5~U9）**，P2 态跑一次：U5 回调劫持 /
  U6 读条目 / U7 就地刷新 / U8 渲染文本 + 语言判定 / U9 关闭原语 —— 判据链全 ok ⇒
  P3 产品化 PoC（2 轮，只做数据）；之后 P4 交互接管（2~3 轮）→ P5 双形态与发布（1~2 轮）；
  ⑤ 产品形态：**推荐 B = 双形态 + 身份探测自动切换**（默认仍走 SWF；第 125 轮 `notOurs`
  从「停推 + 提示」升级为「可切注入」）；C = 注入为唯一形态（删 SWF）作为长期目标
  （触发 = 判据等价 + 过一个游戏版本周期）。
0. ✅ **第 128 轮：operator 二期落地（比较运算符折叠产品化 —— 表内零变化）** —— 产物 =
  `tools/esm/ctda_ops.py`（真值表 + OR 组边界 + 组装 + `--self-test`）+ 两条管线接线；
  INFO 侧 7 条运算符条件被收（全在非表内）+ **表内零变化**（哈希/字节不变）；判据 = 离线层
  4 步 + `verify --dev` **946 OK / 0 MISS** + tripwire 升级；顺带修 verify 两处老缺陷
  （`AutoLoad` 假 MISS + GBK 管道崩）；★ 遗留 = `SAQ_Tests.exe` 被环境拦截无法执行 ——
  第 132 轮离线层已恢复正常执行（19 用例 / 936 断言）⇒ 遗留解除；细节 = `docs/08` 4.7 /
  `docs/10`。
0. ✅ **第 127 轮：P2 双收口 + 提示时机修复（发布条件齐 —— 下一步 = 打 0.1.15）**：
  ① P2 态 2/2 PASS（`r120_gfx_poc` 六段全 ok + 注入 1→3 ｜ `r125_ui_conflict` 第二个菜单
  周期独立判定行 16:50:15.809 + 无「推送放弃」）+ **提示时机修复生效**（首发 = 关菜单
  瞬间 `16:50:03`，对照第 126 轮 16:25:02 落在菜单里）+ **眼睛双达成**（第 8 个 tab
  「SAQ-PoC」+ 3 条注入条目界面可见 / 关菜单后 HUD 中英提示）；
  ② 判据三连全绿（`check_results` 2/2 / `log_hygiene` 95 行零坏字节 / P2 计划
  `plan_regex_audit` **4/4**）+ `verify --dev --p2` 全过 + 离线层 3/3；
  ③ 已恢复常规部署（`build-saq.ps1 -SkipTable -SkipSwf -Harness` + `verify --dev` 全过）；
  ④ 完整复盘 = `docs/15` 十·补；
  ⑤ ✅ **0.1.15 上传包已完成**：`dist\SAQ-ShowAvailableQuests-0.1.15.zip`（**1,852,719 B**，
  SHA256 `6359DEAF…5006F`）= 第 125 轮冲突检测 + 本轮提示时机修复；版本号 6 处同步 +
  changelog v0.1.15 + 文案更新 + `verify --release` 全过（63 项 harness 特征全无）+
  **补跑 verify 覆盖新包** + 包内 6 产物哈希逐一致（发布 DLL **852480 B**）；打完后已
  切回开发构建（**1083392 B**）+ MO2 ini `Harness=0` / `AutoLoad` 清空（正常玩）；
  ⑥ ★ **下一步（大项决策）**：评估功能迁移（`docs/15` 七节 8 项清单）与产品形态
  （建议默认仍走 SWF；冲突检测已兜住体验）。
0. ✅ **第 126 轮：第 125 轮产物实机检查收口 —— 正常态 43 条 43/43 全 PASS + 判据五连全绿；
  已按 P2 实验部署（P2 会话待实机）**：
  ① 会话 16:01~16:10（577703 ms / `驱动器 v70` / `stamp=65` / 用例集 **43 条 / 734 步**）——
  **43 PASS / 0 FAIL / 0 SKIP**；判据五连全绿 = `check_results.py` / `log_hygiene.py` 退出码 0
  （日志 1106423 B / 2117 行零坏字节；结果 JSON 445226 B / 1086 行）+ `plan_regex_audit.py`
  **160/160 全命中**（3 条 assert.nolog 提示 = 正常）+ `verify_saq_build.py --dev` 全过 +
  离线层 3/3（19 用例 / 936 断言 + 快照 16 件 + tripwire）；`[E]` 0 / `[W]` 48（全老现象）；
  ② ★ `r125_ui_channel_ok` PASS（4562 ms）= `推送成功` + `assert.nolog UI 通道不可用`
  （整段窗口零判定行）⇒ **防误报成立**（我们的 SWF 下绝不误判）；
  ③ ★ **已按 P2 实验部署（待实机）**：P2 计划 2 条（`r120_gfx_poc` + `r125_ui_conflict`：
  判定行出现 + 不再出现「推送放弃」；眼睛 = 关菜单后 HUD 提示）；`verify --dev --p2` 全过；
  跑完恢复 = `build-saq.ps1 -SkipTable -SkipSwf -Harness`；
  ④ 之后：评估功能迁移（`docs/15` 七节 8 项清单）与产品形态（建议默认仍走 SWF）。
0. ✅ **第 125 轮：路线 D · 冲突检测落地（正常态已收口 —— 第 126 轮 43/43；P2 正向待实机）**：
  ① 产物：`UI::ProbeChannelIdentity`（`SAQ_UI.{h,cpp}`）+ `SAQ.cpp` 判定 / 停推 / 降噪 +
  `Guide::SetUiNotice` + ESM GLOB `SAQ_UiNotice`（0x80E）+ 脚本 `ProcessUiChannelNotice`
  （HUD 提示，中英）；
  ② 验证：`verify --dev` 全过（+12 条）+ 离线层 3/3（ESM 4805 B）+ 断言正则编译校验全过；
  ③ ✅ **正常态已实测收口（第 126 轮）**：43 条 43/43 + `r125_ui_channel_ok` 防误报成立；
  ★ **P2 正向待实机**：P2 会话跑 2 条（`r125_ui_conflict` = 判定 + 停推降噪正向；眼睛 =
  HUD 提示）—— 已按 P2 实验部署；
  ④ 之后：评估功能迁移（`docs/15` 七节 8 项清单）与产品形态（建议默认仍走 SWF）。
0. ✅ **第 124 轮：P2 眼睛判据达成 + 恢复部署（P2 完全收口）**：
  ① 会话 15:43~15:45（`Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖禁用 / DLL 1079296 B）——
  `r120_gfx_poc` PASS（32344 ms）；探针行六段全 ok + 注入 `entryCount 1→3`；
  ② ★ **玩家截图（30 秒窗口 15:44:48~15:45:18 内）= 眼睛证据**：tab 条 8 项（前 7 = 探针
  假数据 `$SAQ-keep0..6`、第 8 = `SAQ-PoC`）+ 列表 3 条 `SAQ-PoC-Item 0/1/2`（关菜单
  重开无残留）⇒ **判据 + 眼睛双绿，P2 完全收口**；
  ③ ★ **恢复部署已完成**：`build-saq.ps1 -SkipTable -SkipSwf -Harness`（SWF 覆盖拷回 +
  `*.p2off` 清理 + ini `Plan` 改回主计划）；`verify --dev`（非 P2）**全部通过**；
  ④ ★ **下一大项 = 路线 D · 冲突检测**（见下一条）。
0. ★★★ **第 124 轮起：路线 D · 冲突检测 + 「兼容模式」降噪（进行中）** ——
  依据 = `docs/15` 八节（无论路线 A 是否成立都值得做）+ 九·补六观察①（原版 / 第三方 SWF 下
  产品推送必然失败 ⇒ 日志持续「推送失败（重试）」+ 界面状态读不回来）：
  ① 目标：检测到我们的 UI 通道不可用（原版 / 第三方 `missionmenu.swf` 覆盖）时 ——
  **停推降噪**（不再每 500 ms 空转重试）+ **明确提示玩家**（不再是「看着像失效」）；
  ② 之后评估功能迁移（`docs/15` 七节 8 项清单）与产品形态（建议默认仍走 SWF）。
0. ✅ **第 123 轮：P2 复跑实机检查收口 —— `r120_gfx_poc` PASS ⇒★ P2 通过
  （「无 SWF 覆盖」形态技术成立）**：
  ① 会话 15:25~15:27（82172 ms / `Plan=SAQ_TestPlan_p2.txt` / SWF 覆盖禁用 / 开发 DLL
  1079296 B）—— 用例 **PASS**（10375 ms）；实测行六段全 ok + **注入=ok（entryCount 1→3）**
  —— 第 122 轮修复（注入条目补 `aObjectives`）**一击生效**；完整判读见顶部第 123 轮块
  / `docs/15` 九·补六；
  ② **判据五连全绿**（退出码均 0）：`check_results.py` / `log_hygiene.py`（日志 39910 B /
  89 行零坏字节）+ `plan_regex_audit.py` **2/2 全命中** + `verify --dev --p2` 全过 +
  离线层 3/3（19 用例 / 936 断言 + 快照 16 件 + tripwire）；
  ③ ★ **眼睛窗口说明**：探针副作用（8 个 tab / 列表 3 条）只在「探针执行后 → menu.close」
  之间可见（本次 ~8 秒；玩家截图 = menu.close 后重开的原版状态 = 7 tab + 原版 entries，
  **无残留**）⇒ 计划窗口已改 **30 秒**（`wait 30000`）+ 重新部署（P2 态保留，供亲验）；
  ④ ★ **待办（下一轮）**：①（可选）眼睛会话（30 秒窗口亲验第 8 个 tab「SAQ-PoC」+
  3 条「SAQ-PoC-Item i」；注意前 7 个 tab 显示为假数据 `$SAQ-keep0..6`）；② 恢复部署
  （`build-saq.ps1 -SkipTable -SkipSwf -Harness`）；③ 评估**功能迁移**（`docs/15` 第七节：
  迁移面 = MissionMenu +2113 行 / MissionsList +186 / QuestUtils +2）与产品形态
  （建议「兼容模式」，默认仍走 SWF）。
0. ✅ **第 122 轮：P2 首跑检查 —— 探针 v3 五段 ok / 注入段 fail，真因已定位并修复
  （第 123 轮复跑 PASS 收口 —— 见上一条）**：
  ① P2 会话 15:15 实测（完整数据见顶部块）：`环境 numTabs 7` ✅（原版 SWF 生效）/
  `扩tab=ok（7→8）` ✅ / `切3` 对照 ✅ / `切7=ok（拦截 1 次 + 哨兵 0x20000000 存活）` ✅ /
  `切0` 放行 ✅ / `清理` ✅ / **`注入=fail（entryCount 1→0）`** ❌ —— 唯一 FAIL
  （用例一条断言；`plan_regex_audit` 的 1 条未命中与它同源）；
  ② 真因 = 注入条目缺 `aObjectives` —— 原版 `IsRootEntry` =
  `MissionsListEntry.IsMission(param1)` = `param1.hasOwnProperty("aObjectives")`
  （我们 SWF 版的产品条目本来就带该字段、靠自加的 `bSaqAvailable` 分支放行；原版没有
  该分支）⇒ 3 条全被过滤（entryCount 0）；离线出处与判读见 `docs/15` 九·补五；
  ③ 修复（`SAQ_UI.cpp` `buildEntries`）= 注入条目补 `aObjectives` 空数组 +
  `bActive=false` / `iRemainingTime=-1` 防御字段 + verify 特征串 `aObjectives`
  （dev 正向 / 发布反向，防「改回只设 iType」再犯）；已重建 DLL + 按 P2 态重新部署 +
  `verify --dev --p2` 全过（新检查项 OK）+ 离线层 3/3；
  ④ ★ **待复跑（下一轮）**：再跑一次 P2 会话 ⇒ `注入=ok（entryCount 1→3）` +
  `界面研究探针3 …` 六段同现 + **眼睛**（菜单 tab 条上第 8 个「SAQ-PoC」/ 列表 3 条
  「SAQ-PoC-Item i」）⇒ **P2 通过**（「无 SWF 覆盖」目标形态成立）⇒ 按恢复流程还原部署
  （`build-saq.ps1 -SkipTable -SkipSwf -Harness`）。
0. ★★★ **第 121 轮：P2（原版 SWF 完整 PoC）落地 —— 已首跑（第 122 轮）**：
  ① 探针 v3 `ui.research3`（九段判据链：环境 / 扩 tab 7→8 / 切 3 对照 / 切 7 拦截哨兵 /
  切 0 放行 / 清理 / `InitializeEntries` 列表注入）+ P2 计划
  **`SAQ_TestPlan_p2.txt`（`r120_gfx_poc`，只用 C++ 侧原语）** + `build-saq.ps1 -P2`
  部署开关（SWF 改名禁用 + Plan 切换 + 恢复流程）+ verify +13 条（含 `--p2` 模式）；
  设计与判据见 `docs/15` 九·补四；
  ② 已实测：非 P2 `verify --dev` 全过 + P2 `verify --dev --p2` 全过 + 离线层 3/3
  （19 用例 / 936 断言 + 快照 16 件 + tripwire）+ 断言正则预检（2 条合法且与预期输出
  匹配）；部署 = **P2 实验态**（详见顶部块 ⑥）；
  ③ ★ **待实机** ⇒ ✅ **已首跑（第 122 轮）**：`界面研究探针3` 五段 ok（环境 numTabs 7 /
  扩tab 7→8 / 切3 / 切7 拦截哨兵 / 切0 / 清理）/ **注入段 fail（entryCount 1→0）** —— 真因 =
  注入条目缺 `aObjectives`（原版 `IsMission` 判定），已修 + 重建部署（P2 态）、**待复跑**
  （见上一条第 122 轮块；判读见 `docs/15` 九·补五）。
0. ✅ **第 120 轮：第 119 轮产物实机检查收口 —— 42 条 42/42 全 PASS + 探针 v2 五段全 ok
  （P2 已落地 ⇒ 见第 121 轮）**：
  ① 检查全绿：42 条 **42/42**（判据五连全绿：`check_results` / `log_hygiene` 退出码 0 +
  `plan_regex_audit` **158/158** + `verify --dev` + 离线层 3/3）；产品侧与第 118 轮基线
  **逐项一致**（无回归）；`[E]` 0 / `[W]` 47 全老现象；日志 2162393 B（未滚动）——
  完整数据见顶部块；
  ② ★ **探针 v2 判读 = 五段全 ok**（`docs/15` 九·补三已回写实测表）：`切3` 对照（原版
  执行 mask 变）/ `切7` **拦截 1 次 + 哨兵 `0x20000000` 存活**（= 拦截 + 写双成立）/
  `切0` 放行（不越权）/ `清理=ok` / `U2=ok（numTabs 8→9→8）`；R1 枚举 `FilterInfoA=无`
  = U1 能力边界再确认（不影响结论）⇒ **路线 A 的绕过路径实机确认**；
  ③ ★ **下一步 = P2（原版 SWF 完整 PoC）**：MO2 临时禁用我们的 SWF 覆盖 ⇒ 原版 7 个 tab
  环境里验证「7 → 8 个 tab + 列表数据注入 + 交互接管」；探针顺序须改为「先 `SetTabsData`
  扩到 8 → 再切 7 验证拦截」（原版无 idx 7，直接切会被 `tab-refused` 拒绝 —— 第 51 轮
  机制）；成功后 = "无 SWF 覆盖"目标形态成立，再评估功能迁移（`docs/15` 七节）与产品形态
  （建议"兼容模式"，默认仍走 SWF —— `docs/15` 七节风险）。
0. ✅ **第 119 轮：探针 v2（P1.5）落地 —— 绕过 U1 的三项能力（第 120 轮已实机收口 ——
   五段全 ok）**：
  ① 产物：`ui.research2`（`ResearchGfxInjection2`：R1 成员枚举 / R2 事件拦截 /
  R3 `filterMask` 哨兵写 / R4 `SetTabsData` 补测）+ 用例 **`r119_gfx_inject2`
  （用例集 42 条 / 728 步）** + verify +10 条（5 条 DLL 特征 dev/release 双向 +
  4 条用例计划形状 + 1 条只读反向）；设计 / 判据链见 `docs/15` 九·补二；
  ② 验证：构建（DLL 开发构建 **1062400 B**）+ 部署（测试态 `Harness=1` +
  `AutoLoad=Save1_…142854_2_0_4`）+ `verify --dev` **全部通过** + 离线层 3/3 +
  断言正则本地全编译（161 条 / 0 非法）+ ESM / SWF / PEX 未变（marker 18/18 复查）；
  ③ ✅ **已实机（第 120 轮）**：42 条用例 **42/42 全 PASS** + `界面研究探针2` 五段同现
  （全 ok）⇒ 做 **P2**；判读详见顶部块的 ③（实测行 + 哨兵判据）。
0. ✅ **第 118 轮：第 117 轮产物实机检查收口 —— 41 条 41/41 全 PASS + 研究探针判读
   （路线 A 不终止）**：
  ① 检查全绿：41 条 **41/41**（判据五连全绿：`check_results` / `log_hygiene` 退出码 0 +
  `plan_regex_audit` **155/155** + `verify --dev` + 离线层 3/3）；产品侧与第 116 轮基线
  **逐项一致**（无回归）；`[E]` 0 / `[W]` 46 全老现象 —— 完整数据见顶部块；
  ② ★ **研究探针判读**（`docs/15` 第九节已回写实测表）：**U0 ✅**（GetVariable 槽在 +
  `Menu_mc=ok`）/ **U1 ❌**（private 读不到：`HasMember=0` —— 能力边界，AVM2 private trait
  无 public 名）/ **U2 ❓**（`TabbedFilterSelection_mc` + `numTabs` getter 可达；
  `SetTabsData` 调用被 U1 短路 —— 探针设计耦合缺陷，需补测）/ **U3 ✅**
  （`CreateFunction` + `addEventListener` 注册 ok + 回调真实收到，priority=100 生效）；
  ③ ★ **U1 的替代路径已离线确认（路线 A 不终止）**：绕过 `FilterInfoA` =
  拦截 `"BSTabbedSelection::selectionChange"`（挂 `TabbedFilterSelection_mc`、priority=100、
  `stopImmediatePropagation`）+ 自己 `SetMember(MissionsList_mc, "filterMask", 64)`
  —— `filterMask` 是 public setter（set 内自动 `FilterRootEntries()` 刷新）；
  ④ ★ **待实机（下一轮 = 探针 v2 / P1.5）**：① U2 补测（C++ 构造数组 → `SetTabsData` →
  读回 `numTabs`，独立于 U1）；② 拦截实验（挂监听 + `stopImmediatePropagation` → 切 tab
  验证原版 `onFilterChanged` 被拦）；③ `filterMask` 写测试（`SetMember` → 读回 + 列表刷新）；
  三项全过 ⇒ 做 **P2**（原版 SWF 完整 PoC：MO2 临时禁用我们的 SWF 覆盖，7 → 8 个 tab）；
  ⑤ 顺带候选（低成本）：路线 D「冲突检测」（检测到第三方 missionmenu.swf 时提示），
  见 `docs/15` 八节。
0. ✅ **第 117 轮：无 SWF 覆盖的 UI 注入研究 —— 离线取证 + 能力探针落地（第 118 轮已实机
   检查收口）**：
  ① 研究问题与判据 / 6 条路线盘点 / 3 个技术未知点（U1 私有成员可读写 / U2 public 调用 /
  U3 事件注入）见 **`docs/15`**；离线取证关键事实 = root 实例名 `Menu_mc`（两版 SWF 一致）/
  `FilterInfoA` private 且越界风险点单一 / `SetTabsData`·`InitializeEntries`·`ITEM_ACTIVATED`·
  `BSUIDataManager.Subscribe` 全 public；改动面 = 只改 3 个类（+2301 行）；
  ② 产物：`ui.research` 探针（GFx 直操 `_root.Menu_mc`；harness 段内，发布零残留）+
  用例集 **39 → 41**（`r117_gfx_research` / `r117_gfx_research_events`）+ verify +10 条
  （dev/release 双向）；构建 + 部署 + `verify --dev` **0 MISS** + 离线层 3/3
  （DLL 开发构建 **1049088 B**，部署 == 工作区）；
  ③ ★ 待实机 = ✅ **第 118 轮已完成**：41 条用例 41/41 + 探针判读（结论与下一步见上一条）。


---

# 迁入：docs/15 第十三节 · P3-a 产品化 PoC（第 133 轮 · 2026-09-23 第 144 轮迁移）

## 十三、P3-a 产品化 PoC（2026-09-23 · 第 133 轮；`ui.inject`）

**目标**（第十一节 11.7 的 P3）：**真实数据注入** —— 扩 tab + 分时注入 + 对账 + 恢复，
**不含交互**（交互接管 = P4；完整描述文案迁移与 watchdog = P3-b）。

**产物**：

| 层 | 产物 | 说明 |
| --- | --- | --- |
| 模块 | `plugin/src/SAQ_UiInject.{h,cpp}`（新） | 注入层独立模块（与 SAQ_UI 分文件；GFx 安全调用工具自带一份 —— 「随时可回 SWF」的隔离纪律） |
| 接口 | `SAQ_UI::ResolvedAsMovieRoot()` / `SAQ::PendingQuests()` | 解析层复用（不重复猜偏移）+ 数据源接线（与 SWF 推送同一份数据） |
| op | `SAQ_Test.cpp`：`ui.inject`（`Kind::kUiInject`） | harness 段内；一行产品日志 `界面注入PoC …`（红线六） |
| 计划 | P2 计划 +`[case:r133_inject_data]` | 两条断言（首行 + 五段同现）+ 眼睛窗口 30 s |
| verify | +8 条 DLL 特征串 + 5 条 P2 计划形状检查 | dev 正向 / 发布反向（注入模块整体在 `SAQ_WITH_HARNESS` 内编译） |
| 正则自检 | `p2_plan_regex_check.py` +样例「界面注入PoC」 | 编译 + 样例匹配（9 条正则 / 6 条样例） |

**链路（`RunInjectPoC`，一次跑完 —— 判据一行汇总）**：
R1 环境 → R2 语言（引擎任务名 CJK，U8a 同源）→ R2.5 **回 $ALL**（防「菜单恢复上次分类」
导致快照只是子集）→ R3 引擎快照（逐条 `GetDataForEntry` 的**引用数组**）→ R4 构造我们的
条目（真实数据）→ R5 扩 tab（7→8）→ R6 挂 priority=100 拦截 → R7 切 7（拦截 + 设 mask +
注入）→ R8 对账（前 2 条 uID/名字/置灰）→ R9 切 0（**恢复引擎快照**）→ R10 再切 7（供眼睛）
→ 汇总。

**关键机制（与原版行为逐项对齐 —— 本轮的新事实）**：
- ★ 原版 `onFilterChanged`（`MissionMenu.as:2336-2346`）在切 tab 时**只设 `filterMask`、
  不重建列表** ⇒ 分时注入必须「切到我们 tab 注入 / 切走恢复快照」**两边都自己做**
  （与 SWF 版「一次性合并 + 掩码过滤」完全不同）；
- 原版 `FilterInfoA` 只有 7 项（private、改不了）⇒ 第 8 个 tab 必须拦截
  （否则原版读 `FilterInfoA[7].flag` = TypeError）；
- 我们的 tab flag = `1<<6`（`AVAILABLE_QUEST_TYPE=6`，与条目 `iType=6` 配对才有显示）；
  `$ALL` 用**原版值** `0xFFFFFFFF`（不像 SWF 版改 `0xFFFFFFBF` —— 切走即恢复，
  「全部」里不会出现我们的条目 ⇒ 不需要改）；
- 原版 7 项 tab 的 flag 映射（从 SWF 版 `PopulateTabs` 逐项抄）：`$ALL=0xFFFFFFFF` /
  `$Main=1<<1` / `$Faction=1<<2` / `$Misc=1<<3` / `$MISSION=1<<4` / `$Activity=1<<0` /
  `$Completed=1<<5`（`QuestUtils` 枚举 ACTIVITY=0 / MAIN=1 / FACTION=2 / MISC=3 /
  MISSION=4 / COMPLETED=5 / AVAILABLE=6 —— P2 实测「切 3 → mask 0x08」吻合）；
- 条目字段 = 探针 v4 已验证最小集 + 真实值（`uID`=真实任务 FormID / `iType=6` /
  `aObjectives=[]` / `bCanShowOnMap`=有无引导目标 …）；名字前缀（可重复 / 不可导航）与
  描述主干按 SWF 版规则拼。

**边界 / 已知差异（P3-b 或以后处理）**：
- 描述文案 = **主干简化版**（缺 SWF 版 `SaqDescriptionText` 的「按键名提示」等细节分支）；
- 图标：`iType=6` + `iFaction=-1` 时原版 `GetQuestIconLabel` 走 default 分支（PoC 接受）；
- watchdog（引擎推送覆盖我们的注入后重放）与 `UiMode` 运行期开关 = P3-b；
- 点击条目：原版 activate 路径尚未接管（P4）—— 眼睛窗口建议**只切 tab 看列表**；
- 监听**故意保留**（不 `removeEventListener`）：眼睛窗口里玩家切 tab 要靠它
  （没有它切到第 8 个 tab 会触发原版越界 TypeError）；副作用随 `menu.close` 清理。

**离线验证（全绿）**：DLL 开发构建（harness）**1141248 B**（1117184 → +24064；含注入
模块）；P2 实验态部署（`*.p2off` + `Harness=1` + `Plan=SAQ_TestPlan_p2.txt` +
`AutoLoad=…142854_2_0_4`；工作区 == 部署哈希一致）；`verify --dev --p2` **797 行 / 0 MISS**
（+8 特征串 + 5 计划形状）；离线层 **5 步全过**；`p2_plan_regex_check` **9 条正则 / 6 条
样例全过**。

**判读表（P2 会话 4 条 = r120 / r125 / r130 / +`r133_inject_data`；首跑 = 第 134 轮，见下）**：

| 段 | ok 的含义 |
| --- | --- |
| `快照=ok（N 条）` | 引擎条目引用可全量读回（`GetDataForEntry` 逐个）⇒ 恢复手段成立 |
| `扩tab=ok（7→8）` | `SetTabsData` 8 项（前 7 项原版 text/flag）被接受 |
| `注入数据=ok（entryCount N→M，期望 M）` | **真实数据注入**生效（M = 产品待推送列表长度） |
| `对账=ok（0x…:名字｜可导航0/1）` | 注入后读回的前 2 条与构造期望逐项一致（uID/名字/置灰） |
| `恢复=ok（切0 后 entryCount →N，期望 N）` | 切走我们 tab 后引擎列表**完整恢复**（分时注入闭环） |
| `再注入=ok` + `眼睛=…` | 界面停在我们的 tab（真实列表），供眼睛确认 |
| 眼睛 | 第 8 个 tab「可接任务」+ **真实可接任务列表**（真任务名/描述）；切回原版 tab = 原版内容 |

**实机首跑与收口（第 134 轮 · 19:14~19:16 P2 会话 4 条）**：

① 结果：`r120_gfx_poc` **PASS**（六段全 ok + 注入 1→3）/ `r125_ui_conflict` **PASS** /
`r130_gfx_migrate` **PASS**（U5~U10 全绿）/ **`r133_inject_data` FAIL**（唯一）——
`ui.inject` 汇总 = 「待推送=0（菜单还没打开或这次没有可接任务 —— 先 menu.open 再看）」，
两条断言无行。判据 = `check_results` 3/4 + `log_hygiene` 退出码 0（日志 57036 B / 130 行
零坏字节）+ `plan_regex_audit` 8/10（未命中 2 条 = r133 两条断言，同源）。

② ★ 真因（**产品级缺陷，不是用例问题**）：`TryPushPending` 的 notOurs 分支
（`SAQ.cpp` 第 125 轮代码）里 `g_pending.quests.clear()` —— notOurs（原版 / 第三方 SWF）
**恰恰是注入形态要工作的场景** ⇒ 菜单打开时收集的 206 条在推送判定（停推降噪）时被清空
⇒ `SAQ::PendingQuests()`（注入唯一数据源）读到空 ⇒ `RunInjectPoC` 在空数据分支提前返回。
日志证据链：19:15:41.099 `菜单打开：…待推送=206` → :41.950 `UI 通道不可用（第 2 次推送
失败后判定）` → :42.909 `界面注入PoC 待推送=0`。r120/r130 探针（假数据）不依赖
`PendingQuests()` ⇒ 不受影响 —— `r133` 是首条依赖它的用例，一次把缺陷照出来。

③ ★ 修复（生命周期解耦）：`g_pending.quests` 生命周期 = **一次菜单打开周期**
（`OnMissionMenuOpened` 收集 → `OnMissionMenuClosed` 释放）；4 个推送子分支
（推送成功 / 菜单就绪超时 / notOurs / 推送放弃）一律**不再清数据**。产品化理由：P4 分时注入
（每次切到我们 tab 都要注入）本来就要求数据在菜单打开期间常驻 —— 前置修掉，P4 不再踩。

④ 离线判据（全绿）：DLL **1141248 B**（哈希 `0D28150206FC8117…67112DAD`；工作区 == 部署）
+ `verify --dev --p2` **798 行 / 0 MISS**（+1 条源码级检查：`g_pending.quests.clear();`
恰好 2 处 —— 防「推送子分支顺手清」回归）+ 离线层 5 步全过 + P2 正则自检 9/6。

⑤ 复跑判据（下一会话，4 条用例）：`r133_inject_data` 期望上表五段全 ok（`entryCount`
期望 206 = 当次待推送条数）+ 眼睛 = 第 8 个 tab「可接任务」+ 真实 206 条列表。

### 十三·补、复跑实测与第二处缺陷修复（2026-09-23 · 第 135 轮）

**复跑（19:25~19:29 P2 会话 · 4 条）**：第 134 轮修复（数据生命周期）一击生效 ——
`回ALL=ok（mask 0xFFFFFFFF,entryCount 1）/ 快照=ok（1 条）/ 扩tab=ok（7→8）/ 监听=ok`，
`RunInjectPoC` 也拿到了 **206 条**（不再提前返回「待推送=0」）；但 `r133_inject_data`
仍 FAIL（4 条 **3 PASS / 1 FAIL**），其余三条与基线同型。

**① 新真因（产品级缺陷 · 注入上下文未接线）**：`InjectCtx::list`（= `MissionsList_mc`）
**从未被赋值**（默认构造的空 `GFx::Value`），而拦截 handler 的两条路径（切到我们 tab：
设 `filterMask` + `InitializeEntries`；切走：恢复快照）**全部操作 `ctx.list`** ⇒ 二者
必然失败。日志逐行吻合：
- `界面注入：切到我们的 tab（idx=7）→ mask=0x40（写入失败！） + 注入 206 条（fail）`；
- `对账=fail（0x002C5401:一小步｜可导航1）` —— 列表压根没换（玩家看到的还是原版那条
  「一小步」；旧诊断打「期望 uID + 实际名字」的混合值，把「没换」读成了「uID 对上」）；
- `恢复=ok` 是**假 PASS**（注入从未生效，`entries2 == engineCount` 因 1 == 1 巧合成立）。
- 为什么此前没暴露：R1~R6（环境 / 回 ALL / 快照 / 构造 / 扩 tab / 挂监听）用的都是
  **局部** `list`；只有「切 tab 的注入路径」（handler 内）走 `ctx` —— P3-a 之前没有任何
  用例触发过 handler 的这两个分支（r120 / r130 探针各自持有自己的对象）。

**② 修复（三处，`SAQ_UiInject.cpp`）**：
- ★ 核心：R3 快照前 `ctx.list = list;`（异步 handler 在上下文里持有菜单打开期间的引用）；
- **上下文自检**段 `｜上下文=ok`（读一次 `filterMask`）—— 这类缺陷以后一眼可见；
- 对账诊断改打**实际读回值**（`实际 0x…:名字｜可导航N（期望 0x…）`）—— 混合值不再误导；
- 恢复判据加 `passInject` 前置（注入没成功 ⇒ `恢复=fail`，消灭「1 == 1」假 PASS）。

**③ 离线判据（全绿）**：DLL **1142272 B**（+1024）；P2 实验态部署（`*.p2off` +
`Harness=1` + `Plan=SAQ_TestPlan_p2.txt` + `AutoLoad=…142854_2_0_4`；工作区 == 部署）；
`verify --dev --p2` **800 行 / 0 MISS**（+1 特征串「｜上下文=」+1 源码级检查
「`ctx.list = list;` 恰好 1 处」）；离线层 **5 步全过**；P2 正则自检 9/6。

**④ 复跑判据（下一会话 · 4 条）**：`r133_inject_data` 期望五段全 ok（`entryCount` 期望
206 = 当次待推送条数；`对账=ok（实际 0x002C5401:超越极限｜可导航1（期望 0x002C5401））`）
+ 眼睛 = 第 8 个 tab「可接任务」+ 真实 206 条列表（切回原版 tab 恢复原版内容）。

### 十三·补二、复跑收口（2026-09-23 · 第 136 轮 —— 4/4 全 PASS）

**会话（19:55~19:59 P2 会话 · 4 用例 / 35 步 / 184000 ms）**：**PASS 4 / FAIL 0 /
SKIP 0**（P2 实验态全量全绿）。`r133_inject_data` **PASS（32063 ms）** —— 第 135 轮
修复（`InjectCtx::list` 接线）**一击生效**，判读表五段与预测**逐字一致**：

| 判据 | 实测 |
| --- | --- |
| `上下文=ok` | ✅（第 135 轮新增自检段 —— 「这类缺陷以后一眼可见」成立） |
| `快照=ok（1 条）` | ✅ |
| `扩tab=ok（7→8）` | ✅ |
| `注入数据=ok（entryCount 1→206，期望 206）` | ✅ 与当次待推送 206 一致 |
| `对账=ok（实际 0x002C5401:超越极限｜可导航1（期望 0x002C5401））` | ✅ **与第 135 轮预测逐字一致**（首条 = 势力入口固定「超越极限」） |
| `恢复=ok（切0 后 entryCount →1，期望 1）` | ✅ 真恢复（`passInject` 前置下成立 —— 不再是「1 == 1」假 PASS） |
| `再注入=ok（entryCount →206，供眼睛）` | ✅（30 秒眼睛窗口） |

★ 两条关键诊断行（修复前 = 「mask=0x40（写入失败！） + 注入 206 条（fail）」）：
`界面注入：切到我们的 tab（idx=7）→ mask=0x40 + 注入 206 条（ok）` —— **写 + 注入双成立**；
`界面注入：从我们的 tab 切走（idx=0）→ 恢复引擎列表（ok）`。

**其余三条与基线同型**：`r120_gfx_poc` PASS（六段全 ok + 注入 1→3）/ `r125_ui_conflict`
PASS（`UI 通道不可用` 判定行 + `assert.nolog 推送放弃`）/ `r130_gfx_migrate` PASS
（U5~U10 全绿：`接管=ok（回调收到 1 次）` / `关菜单入口=ok` / `造对象=ok（三级 + 接线
UserEvents=1）`）。

**判据（五连全绿）**：
- `check_results` **4/4**（退出码 0）；
- `log_hygiene` 退出码 0（日志 59655 B / 135 行 = **122 I + 13 W + 0 E** —— W 全为
  「原版 SWF 推送失败」预期现象（4 个菜单周期 × 3 条 + 1 条桥解析诊断）；结果 JSON
  75 行零坏字节）；
- `plan_regex_audit`（P2 计划）**10/10 全命中**（4 个用例段全在）；
- `verify --dev --p2` **800 行 / 0 MISS**；`p2_plan_regex_check` **9 条正则 / 6 样例**；
- 离线层 **5 步全过**（决策单测 / 折叠内核自检 / 黄金快照 4 件一致 / tripwire / P2 正则自检）。
- DLL 工作区 == 部署（**1142272 B**，SHA256 `3FF24385…43723553`）。

**眼睛（★ 玩家已确认）**：30 秒窗口（19:57:57.373 再注入 → 19:58:27 menu.close）第 8 个
tab「可接任务」+ **真实 206 条列表**（真任务名/描述）—— 玩家确认「第 8 个 tab「可接任务」
+ 真实任务列表都看到了」⇒ **判据 + 眼睛双达成，P3-a 完全收口**。

**结论**：P3-a（真实数据注入）**完全收口（判据 + 眼睛双达成）** —— 「分时注入」闭环
（切到我们 tab ⇒ 注入 206 条 / 切走 ⇒ 完整恢复引擎列表）在「无 SWF 覆盖」形态下
**产品级成立**。

**下一步（玩家已定 = 直接进 P3-b；P2 实验态继续保留）**：完整 `SaqDescriptionText`
文案迁移 + watchdog（引擎推送覆盖我们的注入后重放）+ `UiMode` 运行期开关
（ini `[UI] UiMode=swf|auto|inject`，默认 `auto`；`swf` = 行为与现状逐字节不变）
⇒ P4 交互接管。

### 十三·补三、P3-b 落地（2026-09-23 · 第 137 轮 —— 注入形态产品化）

**三块内容**（玩家已定「直接进 P3-b、P2 实验态继续保留」）：

① **完整 `SaqDescriptionText` 迁移**（`SAQ_UiInject.cpp` `BuildDescription`）：SWF 版
   `MissionMenu.as` 882~983 行的**逐字搬运** —— 入口（板 100 / NPC 101）× 有无按键名 /
   同伴完整句 / 势力「简要说明」/「需要靠近」句 / 无导航目标三分支 / 尾部句（同伴 vs
   普通）× 有无按键名；按键名 = `Menu_mc.KeyHelper.GetButtonNameForEvent("XButton","")`
   （取不到 ⇒ 空串 ⇒ 走「使用底部的『设定航线』」分支）。**两形态描述从此一致**
   （以后改文案要两处同步：`SaqDescriptionText` + `BuildDescription`）。

② **watchdog**（`OnMenuTick`，500 ms 节拍）：玩家在我们的 tab 上时，检查列表是否仍是
   我们的注入数据（`entryCount` + 首条 `uID` 双判据）—— 引擎刷新覆盖（任务状态推送 /
   原版重建列表）⇒ 重放（设 mask + `InitializeEntries(ourEntries)`）；日志节流
   （首次 + 每 10 次 —— 引擎频繁刷新不刷屏）。

③ **`UiMode` 运行期开关**（ini `[UI] UiMode=swf|auto|inject`，默认 `auto`）：
   · `swf` = 行为与现状逐字节不变（注入代码不激活）；
   · `auto` = SWF 优先；推送失败判定 notOurs ⇒ **自动切注入**（Tick 里 150 ms 节流激活）；
   · `inject` = 只用注入（菜单打开后不推送）。
   ★ HUD 提示改「延迟判定」：auto / inject 下注入激活成功 ⇒ **不提示**（功能已生效）；
   3 秒（`kInjectNoticeGraceMs`）还没起来才补发「UI 通道不可用」（与第 125 轮语义衔接，
   且保持进程内一次）。
   ★ 激活对玩家「无感」：记原 tab（`selectedIndex`）→ 切 0 取全量快照 → 切回原 tab
   （挂监听之前走原版路径）。

**分层改造**（隔离纪律的延续）：
- `SAQ_UiInject.{h,cpp}` 不再整体包在 `SAQ_WITH_HARNESS` 里：**产品路径**（类型探测 /
  完整描述 / 激活 / watchdog / UiMode）**发布构建也编译**；harness 探针
  （`RunInjectPoC` / `ui.inject`）仍在 harness 段（输出格式与第 135 轮**逐字一致** ——
  verify / P2 计划依赖它）。
- `SAQ.cpp` 接线：`ResolveUiMode()`（ini 读取，未知值 WARN + 按 auto）+ Tick 的 open
  分支（`UiInject::OnMenuTick` + 提示兜底）+ 菜单打开读形态 + 菜单关闭清理
  （`UiInject::OnMenuClosed`）+ `TryPushPending` 的 inject 短路 + `PollUiReport` 跳过。

**离线判据（全绿）**：DLL 开发构建（harness）**1160704 B**（1142272 → +18432）；
P2 实验态部署（工作区 == 部署；SWF 覆盖 `*.p2off` + `Harness=1` + `Plan=SAQ_TestPlan_p2.txt`
+ `AutoLoad=…142854_2_0_4`）；`verify --dev --p2` **824 行 / 0 MISS**（+12 条产品路径
特征串 dev/release **双向**正向 + 源码级结构/分层检查 + ini 模板 [UI] 段 + P2 计划形状）；
离线层 **5 步全过**（P2 正则自检 **12 条正则 / 9 条样例**）；ini 模板
（`resources/`）与部署 ini 均含 `[UI] UiMode=auto`。

**P2 计划 +1 条用例 = `r137_product_inject`**（产品路径验收 —— **不依赖 `ui.inject`
原语**）：`界面形态：UiMode=auto` + `界面注入：已激活（UiMode=auto，…）`（约菜单打开后
1~2 秒：推送 2 次失败 + 判定 + 节流）+ `菜单关闭：本轮为注入形态（已激活；watchdog
重放 N 次）`；30 秒眼睛窗口（玩家切到第 8 个 tab 看真实列表）。

**待实机（下一次 P2 会话 5 条）**：`r120` / `r125` / `r130` / `r133` / **`r137`**；
眼睛 = 第 8 个 tab「可接任务」+ 真实列表（**产品路径**，不靠探针）；观察项 = watchdog
是否触发（引擎刷新场景 —— 平时应保持 0 次）。

### 十三·补四、P2 会话判读与两处修复（2026-09-23 · 第 138 轮 —— 5 条 3 PASS / 2 FAIL 的根因与收口）

**会话（20:20~20:24 / 187094 ms / P2 实验态 · 原版 SWF / 5 用例）**：
`r120_gfx_poc` **FAIL**（10078 ms）/ `r125_ui_conflict` PASS（4828 ms）/ `r130_gfx_migrate`
PASS（33406 ms）/ `r133_inject_data` **FAIL**（17079 ms）/ **`r137_product_inject` PASS**
（37531 ms）。判据 = `check_results` 3/5 + `log_hygiene` 退出码 0（250 行零坏字节）+
`plan_regex_audit` 11/13（未命中 2 条与 FAIL 同源）+ `verify --dev --p2` 0 MISS + 离线层 5 步。

**★ 产品路径本身成立（r137 全绿 —— 第 137 轮三块全部实机验证）**：
`20:23:00.954 界面形态：UiMode=auto` → `20:23:01.781 界面注入：已激活（UiMode=auto，
tab 7→8，条目 206，按键名=(空)，语言=zh）`（推送 2 次失败 → notOurs 判定 → 自动激活，
时机 ≈ 菜单打开后 1.2 秒）→ `20:23:36.017 菜单关闭：本轮为注入形态（已激活；
watchdog 重放 0 次）` ⇒ **watchdog 0 次（符合预测：无引擎刷新）**；r125 判定行 + 无
「推送放弃」照旧。

**根因（两条 FAIL 同源）= 第 137 轮产品路径抢先激活，打破探针的「原版 7 tab 干净环境」
前提**：
- `r120`：产品先在 20:21:53.442 激活（tab 7→8）⇒ 探针看到 `环境=(numTabs 8,…)`、
  自己再扩 `8→9`、切的是 `切8=ok（拦截 1 次）` —— 探针五段本身全 ok，只是断言硬编码
  `切7=ok` 等不到（实际是 `切8`）。**探针自适应正常，断言过窄（用例侧）。**
- `r133`：产品先激活（20:22:43.884）⇒ 探针 `扩tab=fail（8→8）`（`SetTabsData` 8 项后
  `numTabs` 不变，判据 `tabs1 == tabs0+1` 不成立）⇒ 后续级联 fail（注入数据/对账/恢复）。
  且 `ui.inject` 开头 `g_ctxStore = InjectCtx{}`（重建共享上下文）把产品 `menuActive`
  清成 false ⇒ 产品每 150 ms 重试激活（tab 数已是 8 ⇒ 扩 tab 永远 fail）——
  **20:22:44.883~20:22:59.873 共 96 条 `激活失败` WARN / 15 秒，每次全量重建
  （切0 + 快照 + 构造 206 条）= 真产品缺陷：失败无退避、日志无节流、纯烧 CPU。**

**修复（两条线）**：
1. **产品侧（真缺陷）**：`ActivateForMenu` 区分失败性质 —— 早退（数据未收集 / 桥未通 /
   `Menu_mc` 缺）保持静默 150 ms 重试；走到快照/构造/扩tab/监听任一失败 = **决定性
   失败** ⇒ `ctx.activateFailed = true`（本菜单周期不再重试，菜单重开再试）+ 一条 WARN
   （含 `tab x→y` 数字 + 「本次菜单不再重试」—— 既是诊断也是回归特征串）。
   `OnMenuTick` 激活分支开头据此提前返回。
2. **测试侧（用例隔离）**：新 harness op **`ui.mode <swf|auto|inject|reset>`** ——
   运行期强制形态（`ForceMode` / `ClearForceMode`；强制期间忽略菜单打开时的 ini 解析）。
   P2 计划各用例显式设形态：探针（`r120`/`r130`/`r133`）= **`ui.mode swf`**（产品不激活、
   干净 7 tab 环境）；`r125`/`r137` = **`ui.mode auto`**（产品路径行为）。判读新增一行
   产品日志 `界面形态覆盖：UiMode=swf（harness 强制；reset 清除）`。
   （`ForceMode` 实现放文件末尾 harness 段 —— 产品段保持「无预处理指令」的分层纪律。）

**离线判据（全绿）**：DLL 开发构建（harness）**1161728 B**（+1024）；`verify --dev --p2`
**829 行 / 0 MISS**（+2 harness 特征串 +1 产品特征串 +2 计划形状；分层检查修正 =
`ForceMode` 移入 harness 段）；离线层 **5 步全过**（P2 正则 12/9）；P2 态已重新部署
（工作区 == 部署；`ui.mode` 5 处已进部署副本）。

**待实机（P2 会话重跑 5 条）**：预期 **5/5 全 PASS**（探针恢复干净环境 7→8；产品路径
照旧）+ 眼睛（第 8 个 tab「可接任务」+ 真实列表 ×2 形态）。

### 十三·补五、P2 会话重跑收口（2026-09-23 · 第 139 轮 —— 5 条 5/5 全 PASS）

**会话（20:36~20:39 P2 会话 · 5 用例 / 224750 ms）**：**PASS 5 / FAIL 0 / SKIP 0**
（P2 实验态首次全量全绿）—— 第 138 轮两处修复**同时验证生效**，各用例判读与预测**逐项一致**：

| 用例 | 实测 | 判读 |
| --- | --- | --- |
| `r120_gfx_poc`（42407 ms） | `环境=(numTabs 7,…)` → `扩tab=ok（7→8）` → `切7=ok（拦截 1 次）` → `注入=ok（1→3）` | ★ 第 138 轮修复①（`ui.mode swf` 隔离）**一击生效** —— 探针恢复「原版 7 tab 干净环境」（对照第 138 轮：产品抢先激活 ⇒ `numTabs 8`、断言等不到「切7」） |
| `r125_ui_conflict`（4953 ms） | `UI 通道不可用（第 2 次推送失败后判定）` 命中 + `assert.nolog 推送放弃`（1000 ms 零命中） | 停推降噪照旧（每菜单周期独立判定） |
| `r130_gfx_migrate`（33297 ms） | 4a：`接管=ok（回调收到 1 次）` / `造对象=ok（三级 + 接线 UserEvents=1）` / `注入=ok（1→2）` / `置灰=ok`；4b：`刷新=ok` / `文本=ok` / `关菜单入口=ok` | 探针 v4 全绿（与第 132 轮逐字一致） |
| `r133_inject_data`（32062 ms） | `上下文=ok｜快照=ok（1 条）｜扩tab=ok（7→8）｜注入数据=ok（1→206，期望 206）｜对账=ok（0x002C5401:超越极限）｜恢复=ok（→1）｜再注入=ok（→206）｜拦截=(3 回调/2 注入/1 恢复)` | 分时注入五段全 ok（与第 136 轮逐字一致） |
| `r137_product_inject`（37562 ms） | `界面形态：UiMode=auto` → `界面注入：已激活（UiMode=auto，tab 7→8，条目 206，按键名=(空)，语言=zh）` → `菜单关闭：本轮为注入形态（已激活；watchdog 重放 0 次）` | ★ 产品路径（P3-b 三块）二次验证成立 |

**两处修复的验证结论（第 138 轮 → 第 139 轮）**：
- **测试侧隔离（`ui.mode`）**：5 处覆盖行全部生效（`界面形态覆盖：UiMode=swf/auto` ×5，
  探针 = swf / 产品 = auto）⇒ 探针与产品不再互撞；
- **产品侧退避（`activateFailed`）**：本次会话**零「激活失败」WARN**（对照第 138 轮
  96 条 / 15 秒刷屏）。本次没有出现「决定性失败」场景（产品两次 auto 激活都成功）——
  退避路径本身未被触发，属**旁证**；该场景在 P4 若再出现（结构性不匹配），
  `激活失败` + 「本次菜单不再重试」将是回归判据。

**★ 产品路径的真实操作二次验证（会话结束后）**：20:39:23 玩家读档 → 20:39:24.438 手动
打开任务菜单（`UiMode=auto`）→ 24.923 产品自动激活（`tab 7→8，条目 206`）→
**20:39:25.804 玩家切到第 8 个 tab**（`界面注入：切到我们的 tab（idx=7）→ mask=0x40 +
注入 206 条（ok）`）→ 20:39:46.973 菜单关闭（`watchdog 重放 0 次`）。⇒ 产品路径在
**玩家手动操作**下照常工作（非 harness 驱动）—— 「眼睛」的客观行为证据。

**判据（五连全绿）**：
- `check_results` **5/5**（退出码 0）；
- `log_hygiene` 退出码 0（日志 164796 B / 448 行；**本次会话 PID 26376 = 179 I + 19 W +
  0 E**，W 全为原版 SWF 推送失败系列（6 个菜单周期 × 3 条 + 首周期 1 条桥解析诊断）=
  预期现象；结果 JSON 97 行零坏字节）；
- `plan_regex_audit`（P2 计划）**13/13 全命中**；`p2_plan_regex_check` 12 条正则 / 9 样例；
- `verify --dev --p2` **0 MISS**；
- 离线层 **5 步全过**（决策单测 / 折叠内核自检 / 黄金快照 6 件一致 / tripwire / P2 正则自检）；
- DLL 工作区 == 部署（**1161728 B**）。

**产品侧（与基线逐项一致）**：静态表 280 / 引擎存在 280 / 待推送 206 / 入口 21（可导航 21）/
可重复 21（排末尾 19）/ 进度门槛 7(过1/藏6) / INFO 门槛 80(过52/藏25/放行2) / 链式 68(过2/藏64)。

**⚠ 读取判据的两个注意点**：① 日志未滚动（164796 B < 1 MB）—— 第 138 轮旧会话的行仍在
日志里（**PID 25356**，含 96 条历史「激活失败」）；统计 `[I]/[W]/[E]` 时必须按会话 PID
过滤，勿把历史 WARN 算作本轮。② 第 138 轮修复前的刷屏是真缺陷证据，保留在日志里备查。

**眼睛（三个窗口 + 手动一次）**：r133 再注入 20:37:58.642 → 关闭 20:38:28.673；r137 激活
20:38:30.632 → 关闭 20:39:04.892；玩家手动窗口 20:39:24.923 → 关闭 20:39:46.973（内含
20:39:25.804 切 tab 注入 206 条 = 客观证据）。**待玩家点头** = 第 8 个 tab「可接任务」+
真实列表（×2 形态）。

**结论**：**P3-b 完全收口**（判据 + 修复验证 + 真实操作二次验证）——
「随时可回 SWF」纪律下，注入形态产品化路径（auto 自动激活 + watchdog + `UiMode`）成立。

**下一步 = P4 交互接管**（按钮劫持 X/Y + activate 拦截 + 引导态竖条 + 就地刷新 +
星图交接 / 真关菜单原语）；P2 实验态继续保留（P4 验证仍以原版 SWF 为主）。
