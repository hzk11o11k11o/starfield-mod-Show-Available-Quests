第 60 轮（大项 G 修复·六）：11:26 会话 6 条 PASS 5 / FAIL 1 —— 唯一 FAIL = 用例期望不可达

真因：r45 尾段旧断言要求「走远 ⇒ cell 卸载 ⇒ 当前候选取不到 ⇒ 候选复算：…先保持」，
但引导目标一旦被脚本 ForceRefTo 到别名上，走远（换星球）后 LookupByID 一直可得 ——
走远 15 秒里复算按 1.5 秒节拍一直在跑却零日志（best == 当前候选，按设计无动作）；
Papyrus seq=11 MoveTo 到 CityCydoniaMainLevel 成功、后续 r48/r44 全 PASS。
产品行为正确（保持精确目标）；观察期那半段要读档/加载窗口才会触发（第 49 轮补丁②的 21:57 现场）。

处置（只动用例+驱动器）：
* 新 op guide.probe（只读探针：与产品候选复算同一套查询，候选可得性进结果 JSON）；
* r45 尾段改判：15 秒内不许出现「引导目标已更新（候选复算）」（防一取不到就降级的抖动）
  + 开菜单时「引导状态：」仍指向 候选 [1]「G型」+ 走远前/后各插一条探针留证；
* 驱动器版本串 v60；verify +3 条 / 0 MISS；产物 DLL 937984 B、用例文件 23206 B，已部署。

文档：docs/99 加「二十、第 60 轮」+ 第 58/59 轮复盘迁 docs/97（给 100KB 上限留余量）；
docs/09 加十四·补六。
