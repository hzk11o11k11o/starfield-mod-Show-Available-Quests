## 项目目标
给B社游戏starfield做一个MOD

## 项目背景及目标
- 这款游戏并不会主动告诉玩家任务去哪接，得靠玩家自己发现
- 在游戏原版任务菜单里新增一个tab，显示可接任务（tab名称就叫这个）

## MOD内容
- 任务必须是可接的，如果当前游戏进度还不能让玩家接到对应任务，则不显示
- 只关注非主线任务
- 任务必须是支线、势力、活动、事件等可以“做完”的任务，无限生成任务不用显示，但接取无限生成任务的目标可以作为一条数据显示在可接任务列表里（如任务板、提供无限任务的NPC）
- 高度整合进游戏原版的任务系统，可以选中跟踪，能调用游戏的任务引导系统正常引导到接取任务的地点（任务目标蓝点，扫描仪任务路径线）
- 支持所有官方DLC，第三方MOD不需要
- 支持语言：中文/英文（游戏本身中文之外的语言全部用英文）
- 基于SFSE
- 要有ESM文件
- 要有日志，但公开发布版日志文件不能超过1MB（开发10M），旧内容要做到滚动删除
- 日志、配置文件要放在mod目录里，不要放在C盘用户目录下，否则删除mod的时候会导致残留文件产生，不合理
- 对于不可引导的任务，要做到能提示玩家，而不是单纯无法选中，因为这样看上去像bug，体验不好
- 以日志为主，不要老让我用眼睛看

## starfield安装位置
D:\SteamLibrary\steamapps\common\Starfield（sfse已安装）

## starfield Creation Kit安装位置
D:\SteamLibrary\steamapps\common\Starfield

## Mod Organizer 2安装位置
D:\Mod Organizer 2

## 注意事项
- Visual Studio 2026已安装，MSVC v143 生成工具已包含
- 已完成内容要记录在：docs/99-当前项目进度.md，其他经验总结文档也可以记录在这个文件夹里，单个文件不得超过100KB，超过后可以简化、迁移或删除过老和已经不再重要的记录
- 如果需要操作其他图形化界面程序，尽量使用后台发送命令的方式操作，不要抢占真实键盘和鼠标，不要打扰我操作电脑（但启动游戏由我来）
- 有任何需要的其他工具，你可自行下载并配置
- “D:\workspace\starfield mod\always scan”是之前一个mod项目，里面有各种工具和代码，你可以把需要的工具复制过来用，部分代码可能也有参考价值
- 所有工具的源代码允许你可以自己修改，添加自己想要的能力
- 每次任务完成后，需要提交本地git，同时.gitignore也要添加必要忽略项
- git 提交信息（commit message）一律用英文（2026-09-25 约定；仓库内容面向 GitHub 公开）
- mod文件生成后要部署到Mod Organizer并配置启用
- mod文件生成后帮我打包成nexus mods能接受的上传包文件，并帮我写一个介绍文案，注意打包时不要把自动化测试功能开启了，所有配置项也要还原到正常玩的默认值
- Nexus上传时机由我自己决定：打包与介绍文案照常做，但不要提醒上传、不要把「上传Nexus」列入待办或进度记录（2026-09-24约定）
- 日志文件位置：D:\Mod Organizer 2\starfield_mods\mods\Show Available Quests (SFSE)\SFSE\Plugins
- harness自动化测试在修改新功能后只需要测试修改的这一部分，也就是增量测试，直到最后打nexusmods包之前，再做全量测试，这样可以节约大量时间
  （工具 = `tools\build-saq.ps1 -Only <用例id子串>`（不传 = 清空 = 全量；如 `-Only r47,r80`）；「修改 → 相关用例」映射表与红线见 `docs/09` 第十五节）